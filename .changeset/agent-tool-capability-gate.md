---
"@kilocode/cli": patch
"kilo-code": patch
---

Enforce per-agent tool disables as an independent runtime capability: a tool disabled via agent `tools: false` can no longer be re-enabled by permission presets, allow rules, approvals, or allowEverything.

---
"@kilocode/cli": patch
"kilo-code": patch
---

Read agent requirements private-first over the CLI private channel with exactly one SDK fallback. The extension keeps existing caching, augmentation, and error behavior.

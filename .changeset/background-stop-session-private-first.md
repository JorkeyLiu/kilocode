---
"@kilocode/cli": patch
"kilo-code": patch
---

Stop session background processes private-first over the CLI private channel with exactly one SDK fallback. Session switch, delete, and tab-close cleanup keeps existing best-effort behavior.

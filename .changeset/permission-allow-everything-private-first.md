---
"@kilocode/cli": patch
"kilo-code": patch
---

Make permission `allowEverything` private-first over the existing FD carrier with ordered private attempt and exactly-one same-tuple SDK fallback preserved.

---
"@kilocode/cli": patch
---

Return the subagent's actual report as the task result even when transient snapshot progress cleanup is delayed, and run nested task sessions without interrupting for the slow-snapshot question.

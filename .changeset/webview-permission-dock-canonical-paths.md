---
"kilo-code": patch
---

Align webview permission-dock fallback classification to canonical `.kilo/` and `kilo.json`/`kilo.jsonc`/`AGENTS.md` only. Legacy `.kilocode/` and `opencode.json`/`opencode.jsonc` are no longer classified by the webview fallback; authoritative backend `protectedPaths` remain displayed verbatim.

---
"kilo-code": patch
---

Fix the Agent Manager session timer so the final elapsed duration stays pinned to the right edge of the working indicator after a session stops, instead of jumping to the left when the spinner and status text hide.

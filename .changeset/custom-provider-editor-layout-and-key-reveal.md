---
"kilo-code": patch
---

Improve the custom provider editor with a wider x-large dialog layout, remove nested scroll constraints, and add saved API key reveal with show/hide toggle for existing API-backed custom providers. The key is loaded on demand without marking the form touched, preserving save semantics for untouched credentials.

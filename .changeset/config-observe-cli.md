---
"@kilocode/cli": patch
---

Request fail-safe cold convergence for external canonical config edits through a descriptor-only private observe hint. The server authorizes canonical descriptors, registers a cold obligation, and boots latest disk; repeated hints stay cold by design.

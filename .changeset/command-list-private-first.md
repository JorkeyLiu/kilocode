---
"kilo-code": patch
---

Read VS Code commands private-first over the CLI private channel with exactly one SDK fallback. The extension keeps existing command caching, ordering, and error behavior.

---
"kilo-code": patch
---

Stop revealing stored provider credentials in the VS Code settings UI: saved API keys are no longer echoed into the webview and are replaced through the secure extension-host prompt instead.

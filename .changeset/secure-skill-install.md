---
"kilo-code": patch
---

Harden project skill installs: download only from the Kilo Marketplace release over HTTPS, validate the archive in memory with strict size and path bounds, and install without shelling out to tar

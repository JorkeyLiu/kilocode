---
"@kilocode/cli": patch
"kilo-code": patch
---

Route the Settings MCP sign-in through one CLI-owned private call per click with no automatic retry. Failures now show a short message and always clear the loading state instead of hanging.

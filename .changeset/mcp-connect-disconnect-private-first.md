---
"@kilocode/cli": patch
"kilo-code": patch
---

Restore the Settings MCP connect/disconnect switch through one CLI-owned private call per click with no automatic retry. Failures now show a short message and always clear the loading state instead of hanging.

---
"kilo-code": minor
---

Align file-authoritative GUI configuration with XDG_CONFIG_HOME, accept and filter `$schema` as meta, and bring canonical parity for `default_agent` and provider model variants — 12-field closed JSONC registry including `$schema` (model, model_variant, model_variant_overrides, subagent_model, subagent_variant, subagent_variant_overrides, default_agent, provider, mcp, permission, instructions plus `$schema` meta), plus typed asset directories, atomic WYSIWYG writes, and derived selector indexes.

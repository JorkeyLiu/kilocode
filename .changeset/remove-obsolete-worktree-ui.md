---
"kilo-code": patch
---

Remove the obsolete Agent Manager diff/review panel, review tab, and tab-bar Run controls, and remove the dead sidebar worktree selector and migration actions. The shared sidebar Diff Viewer now owns all diff rendering.

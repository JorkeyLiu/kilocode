---
"kilo-code": patch
---

Start fresh sessions without a draft from the configured or valid provider default instead of inheriting a recently used or pending composer model choice; automatic options stay hidden until settings are ready.

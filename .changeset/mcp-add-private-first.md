---
"@kilocode/cli": patch
"kilo-code": patch
---

Register the browser automation MCP server through one CLI-owned private call per registration with no automatic retry. Failures now log a short message and leave automation disabled instead of hanging.

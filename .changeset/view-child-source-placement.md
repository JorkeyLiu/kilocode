---
"kilo-code": patch
---

Open a sub-agent session from a task tool call inside Agent Manager chat in a tab immediately to the right of its source session tab instead of appending it at the far right. Opening an already-open sub-agent focuses it without reordering.

# VS Code Orchestrator — Current State

Reality that changes the next judgment. Code and observed behavior outrank
this note; when they disagree, the code is right.

## What holds today

- Agent Manager is the only chat surface in the product. The older single-chat
  sidebar and editor-tab chat are gone from the tree.
- The extension still runs as a client of one shared editor-owned backend
  process reached over HTTP with server-sent events through the generated SDK.
  That path is the current authority for sessions, not a future contract.
- Private file-descriptor carriers into the same backend exist as incremental
  work. They provide parity and diagnostics for most surfaces; Agent Manager
  local `session/create` is now private-first with authoritative `SessionCreateDispatch.dispatch` commit (same `create:<token>` durable tuple for `opId`/`idempotencyKey` + `requestId` + `directory`/`parentSessionId:null`, 3 s timeout/epoch, valid `succeeded` authoritative without SDK else exactly one SDK `POST /session` fallback with same tuple; `sandboxInheritanceToken` bypasses private with exactly one SDK `create`).
- Durable session operations with an atomic commit plus a bounded derived
  changefeed exist as an incremental foundation. `session/create` private path now commits `SessionTable`/`session_operation`/`changefeed`/`Event` atomically via `fd-carrier.ts` → `SessionCreateDispatch.dispatch` (not replay-only `dispatchPrivate`); idempotent replay returns same session without duplicate. Title-only `session/update` (rename) is private-first the same way via `fd-carrier.ts` → `SessionUpdateDispatch.dispatch` with the same durable tuple (`opId`/`idempotencyKey` + `requestId` + `context`) across private and the exactly-one SDK `session.update` fallback; a valid private `succeeded`+`accepted` session/title result returns with zero SDK mutation, otherwise exactly one SDK fallback with the identical tuple. `session/fork` is private-first the same way via `fd-carrier.ts` → `SessionForkDispatch.dispatch` (authoritative commit with `data.session` wire wrap) and `forkSessionPrivateFirst` (same `fork:<sessionId>:<token>` durable tuple for `opId`/`idempotencyKey` + `requestId` + `context`, 3 s timeout/epoch; valid `succeeded`+`accepted` returns with zero SDK mutation, validated `failed` with `failure.retryable === false` such as `session.not_found`/`conflict` closes terminally with zero SDK, validated `failed` with `retryable === true` plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one SDK `session.fork` fallback with the identical tuple, never retried). `session/delete` is private-first the same way via `fd-carrier.ts` → `SessionDeleteDispatch.dispatch` (authoritative full-family delete) and `deleteSessionPrivateFirst` (same `delete:<sessionId>:<token>` durable tuple for `opId`/`idempotencyKey` + `requestId` + `context {directory, sessionId, parentSessionId:null}`, 3 s timeout/epoch; valid `succeeded`+`accepted` returns with zero legacy `DELETE /session/:sessionID` mutation, validated terminal `failed` with `retryable === false` closes with zero SDK, known-safe unavailable plus validated `retryable === true` takes exactly one durable `DELETE /session/:sessionID` fallback with the identical tuple, unknown outcome runs one private reconciliation with the same tuple then closes terminally without destructive SDK fallback). The `dispatchPrivate` replay-only paths remain for diagnostics but are not used by `fd-carrier.ts` for `session/create`, `session/update`, `session/fork`, or `session/delete`. The draft create/close race orphan cleanup in `KiloProvider.resolveSession()` (session created after its `closedDrafts` draft closed) is converged on the same helper: it calls `deleteSessionPrivateFirst({client, connection, sessionId, directory})` with the identical durable tuple (op/idempotency/request/context, private-first, durable HTTP fallback, unknown reconcile) instead of the former direct SDK `session.delete`; no bypassing direct delete remains on any production delete path (the only `client.session.delete` in production is the helper-internal durable fallback). The orphan is untracked (no stop/prune/`sessionDeleted`/error UI); cleanup failure is logged with session id/directory and still returns undefined, so closing a still-creating draft never surfaces `sendMessageFailed`.
- `session/cancelQueued` is private-first the same way via `fd-carrier.ts` → `CancelQueuedDispatch.dispatch` and `KiloProvider.handleCancelQueued` (same `cancelQueued:<session>:<message>` `opId` + `legacy:<session>:<message>` `idempotencyKey` + `requestId` + `directory`/`session`/`message` tuple, 3 s exact-cancel; valid `succeeded`+`accepted` and validated terminal `failed` with `retryable === false` close with zero SDK, validated `failed` with `retryable === true` (`InstanceUnavailableDuringConfigRebuild`) takes exactly one SDK fallback with the same identity, unavailable/invalid/ambiguous/transport/timeout/closed surface explicit failure with no second heterogeneous cancel).
- Checkpoint `session/revert` + `session/unrevert` are private-first the same way via `fd-carrier.ts` → `SessionRevertDispatch.dispatchRevert`/`dispatchUnrevert` (authoritative `SessionRevert.revert`/`unrevert` commit with `revert:<session>:<token>` / `unrevert:<session>:<token>` durable tuple for `opId`/`idempotencyKey` + `requestId` + `context`, 3 s timeout/epoch; valid `succeeded`+`accepted` and validated terminal `failed` with `retryable === false` close with zero SDK, validated `retryable === true` plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one SDK `session.revert`/`unrevert` fallback with the same session/message/part/directory, never retried; HTTP `revert`/`unrevert` semantics unchanged).
- VS Code Settings writes are canonical-only and file-authoritative:
  `KiloProvider` accepts only stamped `canonical: true` `updateConfig`
  messages through `CanonicalConfigService.writeConfigScopes`; non-canonical,
  missing-canonical, and missing/stale-stamp messages fail fast with a
  structured `configUpdateFailed`. The legacy `client.config.transaction`
  caller and its pending/reconcile/retry coordination are removed from the
  extension (the CLI transaction endpoint itself is unchanged). Canonical
  reads publish the service snapshot; providers without canonical authority
  return explicit empty/unsupported payloads instead of racing the SDK.
- Provider API-key/custom writes are canonical-only:
  `connectProvider`/`disconnectProvider`/`saveCustomProvider`/`deleteCustomProvider`
  go through `CanonicalConfigService` plus `SecretStorage`; non-canonical
  payloads fail fast with a structured `providerActionError` and never fall
  back to the SDK bridge. The legacy SDK mutation callers are removed.
  OAuth `authorize`/`complete` keeps its legacy path with canonical
  `unsupported`; the explicit credential-reveal compatibility seam is
  removed (no `getProviderCredential` / `providerCredentialLoaded` /
  `providerCredentialError` messages, no `handleGetProviderCredential`,
  no `authorizeCredentialRead`, no production `client.provider.list`
  caller — the CLI `GET /provider` endpoint itself is unchanged).
  Stored credentials never enter the webview: every regular provider
  read is redacted via authenticated
  `GET /provider/catalog` (`provider.catalog`, closed redacted
  `all,default,connected,failed` with no `key`/`options`/`headers` and
  `hasCredential` derived from non-empty runtime key, and credential
  set/resolve happens only in the extension-host `SecretStorage` owner
  (replacements via the host prompt, never an echoed key). Stored
  model-discovery credentials no longer cross the regular refresh (no
  extension-side key cache): editing an existing legacy provider without
  retyping the key discovers models through the runtime narrow endpoint
  `POST /provider/:providerID/models` (exact provider plus exact stored
  `baseURL`, backend-held Bearer key); freshly typed keys, custom headers,
  and canonical `SecretStorage` discovery keep their existing paths.
- Authored canonical custom-provider protocol is converged: `CanonicalProviderPayload`
  persists only `{name, endpoint, protocol, models}` plus the extension-host
  credential reference, with protocol exactly `openai/completions`,
  `openai/responses`, or `anthropic/messages`; legacy protocol tokens are
  rejected with no mapping or migration, the webview serializer and canonical
  edit init use the protocol state directly, and headers are not part of the
  authored contract. The legacy unary stepping seam has been removed; generation runs only over the streaming host seam `provider/httpExecute` described below. The remaining streaming `ServePrivatePeer` streaming path (`serve-private-provider-http-execute.ts` → `canonical-http-executor.ts` via single-owner `CanonicalConfigService` `resolveSecret`, no duplicate `SecretStorage`): `KiloConnectionService` wires the activation-owned `CanonicalConfigService` into `ServePrivatePeer` and advertises only `provider/httpExecute` when execution deps are installed (fail-closed otherwise). The deleted unary `provider/execute` handler, `ProviderExecuteBroker`/`CanonicalProviderExecute` services, and their localhost fixtures (`canonical-provider-host-execution.test.ts`, `serve-private-provider-execute.test.ts`) are gone; session generation runs only over streaming `provider/httpExecute`. Plaintext secrets never leave the extension host.
- ConfigV1-only canonical provider preservation is inert and authenticated: `ConfigProviderV1.Info` preserves `endpoint`/`protocol`/`credential` via shared `ProviderCredentialRef` at `@opencode-ai/core/kilocode/credential-ref.ts` (`PROVIDER_CREDENTIAL_REF_PATTERN` OpenAPI pattern, `parseOwnedCredentialRef` parser validation, fixed redacted `Invalid credential reference` without token in `SchemaError`/`HttpApiSchemaError`, productive `Schema.toArbitrary` via `toArbitraryConstraint`) and `@opencode-ai/core/kilocode/provider-execute.ts` `PROVIDER_EXECUTE_PROTOCOLS`/`isProviderExecuteProtocol`/`PROVIDER_EXECUTE_PATH_BY_PROTOCOL` through `ConfigV1` parse/load and isolated global/project merge, plus `GET`/`PATCH /config` and `GET`/`PATCH /global/config` authenticated via `Basic` (`KILO_SERVER_PASSWORD`) — plaintext/malformed `credential` is rejected before persistence and neither HTTP 400 (`Invalid credential reference`) nor server logs contain the submitted distinctive token; both authenticated project and global flows assert `endpoint`/`protocol`/`credential` in `PATCH` response, exact persisted `kilo.jsonc` file, and subsequent `GET`, with auth rejection (401), invalid-ref no-persist, project-over-global precedence, and cleanup retained. Root SDK `KiloClient` (`packages/sdk/js/src/client.ts`) handwritten `config.get`/`update` refinement over the frozen legacy runtime client (project-only `GET`/`PATCH /config` via `V2Config`) vs generated `@kilocode/sdk/v2` current `ProviderConfig` and `global.config` methods; `isCanonicalOnlyProviderV1` (`@opencode-ai/core/kilocode/canonical-provider.ts`) excludes canonical-only providers from legacy opencode `Provider` DB while hybrids with any legacy provider/model key (`api`, `npm`, `env`, `options`, `id`, `whitelist`, `blacklist`, `prompt`, `isFree`, `ai_sdk_provider`, `release_date`, `attachment`, `temperature`, `tool_call`, `interleaved`, `cost`, `limit`, `experimental`, `status`, `provider`, `options`, `headers`, `variants` including `enable_thinking`/`reasoningEffort`/`effort`/`thinking`/`reasoning_split`/`chat_template_args`) remain operational; unknown nested keys follow ConfigV1 excess-property policy. Non-goals remain explicit: no ConfigV2 migration, no V2 catalog/generator expansion, no CLI SecretStorage resolution, no CLI provider selection, no streaming, no session generation.
- Canonical file-backed custom-agent create/edit/import/remove is production:
  stamped `canonical: true` `mutateAgent`/`removeAgent` through
  `CanonicalConfigService.writeAsset`/`deleteAsset` CAS (project-absent on
  create/import, scope-plus-asset-hash on edit/remove); non-canonical agent
  messages fail fast with exactly one structured `agentMutationError` and never
  reach the SDK (the legacy `client.kilocode.removeAgent` write is removed);
  failures/exceptions post exactly one structured `agentMutationError` with a
  freshly re-read stamp. The webview serializes per-agent edits through one
  coordinator (debounced coalescing, full-snapshot reconciliation, inflight
  error settling the whole queued batch with no replay); unmount/agent-switch
  flush still reports to the provider-level diagnostic without touching dead
  view state. Agent markdown is CLI-loader-compatible (unknown keys preserved
  for rest/options merging; credential-bearing keys rejected in validation,
  import, export, and host writes; prototype-polluting keys never merged).
  Native/system agents are read-only; no global-scope picker and no
  tool-disable/permission-preset implementation exist.
- A private observation service exists in the extension, but the remaining
  target ownership and cutover are unfinished. Effective runtime
  configuration still comes from the CLI loader with its multi-source merge
  and convergence pass.
- Production permission decisions come only from
  `packages/opencode/src/permission/evaluator.ts`. Across
  global/project/agent/session-restriction layers, deny wins first,
  ask/ceiling next, and allow holds only when every applicable layer allows.
  The old last-match helper is not on this path.
- The extension restrictive `{global,project}` product from
  `packages/kilo-vscode/src/config/compose.ts` feeds only extension
  materialization/webview projection today, not CLI evaluator input; the CLI
  still assembles its layers from canonical files.
- The private runtime and storage cutover is unfinished. The legacy store
  still serves sessions until that landing.
- Agent Manager session-list re-observation decision is now owned by the extension standalone `PrivateObservationService` (snapshot/read/ack with observation coordinator, generation and singleflight) at `requestState` plus panel `visible` and active session-switch boundaries; the generated SDK remains the sole data authority, changefeed entries are decision/provenance only, and the old unconditional `refreshSessions()` is removed. Worker-restart/peer-close now drives the provider from the one bounded `PrivateObservationLifecycleTriggers.onPeerClosed()` reconnect+`read(persistedCursor)` — read failure (result absent, `readError`, `requestedCursor`/`readResult` absent/invalid, validated via `decideFromReadResult` without second read) -> precomputed singleflight fallback with `{shouldRefresh:true}`, exactly one SDK `refreshSessions()`, no second private read, no ack (singleflight-shared with `requestState`/`visible`); temporal staleness (current persisted `undefined` or not exactly equal to `requestedCursor`, whether < or >) -> normal fresh `handleObservationRefresh()` decision which may read again (the only second read); valid fresh -> no second read, re-checked before ack to avoid stale ack/regression. Window and config lifecycle reads remain observation-only. Extension `activate` constructs `PrivateObservationService`/triggers before the provider but defers `initialize()` until after `AgentManagerProvider` and `wirePeerCloseObservation` hook (fire-and-forget fail-closed, no TDZ), closing the activation race. Shared SDK/SSE transport reconnect (`KiloConnectionService` `onStateChange` `connected`) is distinct from private `peer:closed`; it re-enters the same private decision only after the provider has observed a prior `connected` baseline (`seenConnected` initialized from `getConnectionState()==="connected"` at construction), first `connected` remains owned by `requestState` hydration, and visibility is rechecked after `stateReady` with generation/sessions identity.
- Agent Manager session-list paging is private-first via the standalone no-lease canonical-DB projection (`observation/list` `PrivateObservationService` → `createSessionListDeps` over `SessionTable` with `directory` + `archived` + `time_updated DESC, id DESC`, `limit` 1..500, fields `id/title/parentID/directory/projectID/createdAt/updatedAt`, `nextCursor` iff truncated, `privateSessionReader` (`isEnabled`/`isStarted`/`list`/`get`/`messages`) injected non-owningly via `VscodeHost` → `KiloProvider` `sessionRefreshContext.listSessions` with `archived:false` and `normalizeSessionListNextCursor` reuse, bounded exactly-one SDK `experimental.session.list` fallback on gate-off/not-started/worker error/protocol invalidity preserving detached parity diagnostics). Paged list + bounded detail + paged and full messages are now private-first; lifecycle stays extension-owned; no sole private runtime authority beyond these surfaces.
- Standalone no-lease `observation/get` detail projection (`observation/get` `PrivateObservationService` → `createSessionGetDeps` over `SessionTable` by `sessionId` with canonical directory scoping, statuses `found`/`not_found`/`scope_mismatch` as versioned result union, fields `id/title/parentID/directory/projectID/createdAt/updatedAt` plus optional `agent`/`summary`/`revert`) is private-first for narrow `SessionDetail` via `PrivateSessionReader.get` (`validatePrivateGetResult` defensively revalidates `found` at provider boundary, `sdkSessionToDetail`/`observationSessionToDetail`/`detailToWebview` pure mappers preserve `parentID`/`revert`/`summary` null and ISO semantics plus empty `agent`/`summary`/`revert`). `found` is authoritative with no SDK and no parity; `not_found`/`scope_mismatch` are authoritative terminals with no SDK and no raw directory/id log; malformed/`InternalError`/`MethodNotFound`/transport/host-closed are bounded warnings then exactly one SDK `client.session.get` with `AbortSignal` preserved for strict `replace` and detached parity. `refreshSessionDetails`, strict `focus`/`replace` preludes, `handleSyncSession` (metadata helper plus child-sync full-history `fetchMessagePage limit:0` via `Promise.all`), and `exportTranscript` (narrowed formatter) all use the central `getSessionDetail`; creation/rename/revert/unrevert/SSE writers map SDK `Session` → `SessionDetail`. `currentSession`/`setCurrentSession` narrowed to `SessionDetail`; dead `Host.getSessionInfo` full-`Session` promise removed. Lifecycle remains extension-owned; `KiloProvider`/`VscodeHost` never init/reconnect/dispose the private service.
- Bounded raw `observation/messages` page (`observation/messages` → `createSessionMessagesDeps` over `MessageTable`/`PartTable` with continuation `(time_created < t) OR (= and id < id)`, `time_created DESC, id DESC`, `limit` 1..100, `nextCursor` iff truncated, chronological ASC, storage-stripped via shared `message-read`) is private-first for paged and full-history reads via `PrivateSessionReader.messages` (`validatePrivateMessagesResult` defensively revalidates `found`/order/cursor/session binding plus per-part `sessionID`/`messageID` ownership at provider boundary, `found`/`not_found`/`scope_mismatch` authoritative with no SDK and private-authoritative paged/full results run no detached parity, malformed/`InternalError`/`MethodNotFound`/transport/closed bounded warnings then per bounded paged read at most one private attempt and one logical SDK `session.messages` fallback with the existing transient retry preserved and original `AbortSignal`, full `limit=0` iterates private pages at wire limit 100 until exhaustion preserving ASC/projected shape with `input.signal.throwIfAborted()` before and after each awaited private page and before the SDK fallback so no further private pages or SDK read run after abort, cycle/bound overflow falling back to exactly one SDK `limit:0` with no retry and original `AbortSignal`, SDK paths retain detached parity bounded once per outer `fetchMessagePage` operation so multi-page assistant-boundary fill may issue one logical SDK fallback per page while full fallback issues exactly one SDK full-read and `exportTranscript` plus child-sync `handleSyncSession` pass the non-owning reader); `messagesLoaded` then scheduler capture `commit` replay (monotonic occurrence token in `since`, never wall-clock; capture flushes pre-token queued state, same-session latest-wins with `commit` as the authoritative validation consuming the post-token live queue, real full corrective replay with snapshot-present deltas dropped and snapshot-absent delta-derived emitting only the unflushed pending queue while already flushed deltas stay in the projection, latest attempt retained across expiry/overflow with latest-only child terminal via `pruneDeletedSession`, callback call-order not delivery acknowledgement, bounded capture of max 200 keys / 512KiB / 30s scheduler-owned expiry with fail-closed no-snapshot on invalidation, real fulls replaying correctively, delta-derived replay only for snapshot-absent keys, strict webview snapshot for same-ID parts) plus stale-generation checks, with mapped `messageRemoved`/`partRemoved` voiding active captures via `retire` before flush-then-post (live queue/derived/latest and lane timers preserved, no token advance, no `drop`), voided replace refetching once while still latest then surfacing the existing snapshot-expired error on the second latest failure with superseded attempts silent, and reconcile fail-soft without `lastReconciledAt` so the next focus refetches; lifecycle stays extension-owned and `KiloProvider`/`VscodeHost` never init/reconnect/dispose the private service.
- Runtime prerequisite exists as internal contract only: single-owner per-session `Runner` epoch is the sole owner of the active-epoch busy transition (one `busy` per epoch, ordering `TurnOpen` -> `busy` -> body -> `TurnClose` -> `idle`, no per-runLoop-iteration busy; persistent runner with creation-only gate) carries optional `generationID` on `session.turn.open`/`session.turn.close`, and internal `CancelTreeResult` is exactly { targetSessionIDs, generations, queue, intake, planFollowup } with no background collection (background cancellation stays a side effect); private `session/abort` is production over the fd carrier (`session-abort.ts` -> `KiloSessionPrompt.cancelTree` over `SessionRunState`, terminal only after targeted epoch/finalizer convergence, affected generations from the owner never the request token, `session.not_found`/`scope_mismatch` as terminal `terminal-failure` with zero SDK, validated terminal survives post-response drift, only unresolved drift falls back to exactly one legacy SDK `POST /session/:id/abort` with the identical `abort:<sessionId>:<token>` tuple, single-directory scope, no fabricated `idle`/`sessionTurnClosed`, HTTP/SDK boolean wire unchanged, no durable operation row).
- VS Code `question.reply`/`question.reject` is private-first over the same fd carrier (`kilocode/question/question-private.ts` -> `Question.Service` pending map as authority, `pending.delete` + event publish + Deferred settle before `terminal`, unknown/double as `question.not_found` `terminal-failure`, `op`/`context`/`opId` binding as `scope_mismatch`, no `sessionID` in request with carrier-backfilled terminal `sessionID`, reply payload reuses `Question.Reply`, `question/reply` + `question/reject` capabilities, drain-control `questionReply`/`questionReject` snapshot-first lane held to completion, no durable operation/revision/tombstone; extension `question-privatefirst.ts` + `serve-private-question-contract.ts` + `ServePrivatePeer` + single `privateQuestionWithHandle` via `wrapEpochHandle`, 3 s exact-cancel, validated `terminal`/`terminal-failure` survives post-response drift with zero SDK and existing stale/recover UI preserved, only unresolved drift falls back to exactly one same-tuple SDK). Question `list` is private-first re-observation over the same carrier (`fd-carrier.ts` `question/list` + `question/list` capability invoking exact `Question.Service.list()` under canonical directory + `acquireDrainControl` snapshot-first `InstanceRef` lane, strict `question-list:<token>` `opId==idempotencyKey` + `requestId` + `context.directory` + empty payload with unknown-field reject, redacted `validation.failed`/`scope_mismatch`/`internal` terminal non-retryable plus retryable `InstanceUnavailableDuringConfigRebuild`, no drain-control classification, no durable operation; extension `serve-private-question-list-contract.ts` + `ServePrivatePeer.privateQuestionListWithHandle` + `serve-private-question-list-connection.ts` via `wrapEpochHandle` + `question-privatefirst.ts` `listQuestionsPrivateFirst`/`readQuestionsForDir` (3 s exact-cancel, success including empty zero SDK, terminal unknown-dir zero SDK, fallback exactly one SDK `question.list`); `fetchAndSendPendingQuestions` reads per directory preserving order, first-wins dedupe, scanned/failed/complete, revision retry, prune; fixture `backendSnapshot` `question.list` stays SDK-only).
- Session model usage is private-first over the same fd carrier (`fd-carrier.ts` `session/model-usage` + `session/model-usage` capability invoking the same `ModelUsage.get` used by HTTP with same-directory scope via `Session.Service.get`, exact `ModelUsage.Info` shape/order; extension `serve-private-session-model-usage-contract.ts` + `ServePrivatePeer` + `serve-private-session-model-usage-connection.ts` via `wrapEpochHandle` + `kilo-provider/session-model-usage.ts`, 3 s exact-cancel, same `session-model-usage:<sessionId>:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + `context`; accepted success posts identical `sessionModelUsageLoaded` with zero SDK, validated terminal `session.not_found`/`scope_mismatch`/`validation.failed` posts the empty payload with zero SDK, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one SDK `sessionModelUsage` with no retry; no durable operation row, no UI change).
- Permission `saveAlwaysRules` + `reply` are private-first over the same fd carrier (`kilocode/permission/permission-private.ts` -> exact `Permission.Service` methods used by HTTP under canonical directory + `acquireDrainControl` + `InstanceRef` drain-control lane held to completion, strict `permission:<requestId>:<token>` `opId==idempotencyKey` + `requestId`/`context {directory,requestID}` + permission request ID binding, strict payload/unknown-field reject, redacted `permission.not_found`/`scope_mismatch`/`validation.failed`/`internal` terminal-failures, unknown/double reply as terminal `permission.not_found`, no durable DB op row; extension `permission-privatefirst.ts` + `serve-private-permission-contract.ts` + `ServePrivatePeer` + `serve-private-permission-connection.ts` via `wrapEpochHandle`, 3 s exact-cancel/epoch coherence, two distinct ordered operations with save completing before reply, save terminal short-circuits reply/SDK, save/reply fallback-eligible (retryable/unavailable/invalid/ambiguous/transport/closed/timeout) takes exactly one same-tuple SDK call each, `handlePermissionResponse` + toggle-auto-approve drain/`approve` preserve UI messages/stale-404/recoveryDirs/fan-out/approval semantics, `allowEverything` is private-first capable (ordered private attempt with exactly-one same-tuple SDK fallback preserved, no UI call-site invoking it yet)). Permission `list` is private-first re-observation over the same carrier (`fd-carrier.ts` `permission/list` + `permission/list` capability invoking exact `Permission.Service.list()` under canonical directory + `acquireDrainControl` snapshot-first `InstanceRef` lane, strict `permission-list:<token>` `opId==idempotencyKey` + `requestId` + `context.directory` + empty payload with unknown-field reject, redacted `validation.failed`/`scope_mismatch`/`internal` terminal non-retryable plus retryable `InstanceUnavailableDuringConfigRebuild`, no drain-control classification, no durable operation; extension `serve-private-permission-list-contract.ts` + `ServePrivatePeer.privatePermissionListWithHandle` + `serve-private-permission-list-connection.ts` via `wrapEpochHandle` + `listPermissionsPrivateFirst`/`readPermissionsForDir`, 3 s exact-cancel/epoch, accepted success zero SDK, terminal non-retryable yields unknown-dir with zero SDK, fallback-eligible takes exactly one SDK `permission.list`, `fetchAndSendPendingPermissions` + toggle-auto-approve drain read per directory preserving recoveryDirs order, first-wins dedupe, valid/prune, and generation guards with no UI change).
- Agent requirements is private-first over the same fd carrier (`fd-carrier.ts` `agent/requirements` + `agent/requirements` capability invoking the same `agents.requirementStatus` used by HTTP under canonical directory + `acquireDrainControl` + `InstanceRef`, exact `AgentRequirementResult` shape including `state:error` domain payloads as succeeded; extension `serve-private-agent-requirements-contract.ts` + `ServePrivatePeer` + `serve-private-agent-requirements-connection.ts` via `wrapEpochHandle` + `kilo-provider/agent-requirements-privatefirst.ts`, 3 s exact-cancel, same `agent-requirements:<agent>:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + `context directory+agent`; accepted success feeds existing `applyVSCodeExtensionRequirements` augmentation then cache/post with zero SDK, validated terminal `validation.failed`/`scope_mismatch`/`internal` maps to existing `request_failed` with zero SDK/no cache, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one SDK `agentRequirements` with no retry; augmentation stays the sole post-read transform, no UI/message-shape change).
- MCP `status` is private-first over the same fd carrier (`fd-carrier.ts` `mcp/status` + `mcp/status` capability invoking the same `MCP.Service.status()` used by HTTP under canonical directory + `acquireDrainControl` + `InstanceRef`, exact five-state `Record<string, Status>` shape, no cache, no mutation, no durable operation; extension `serve-private-mcp-status-contract.ts` + `ServePrivatePeer.privateMcpStatusOutcomeWithHandle` + `serve-private-mcp-status-connection.ts` via `wrapEpochHandle` + shared `kilo-provider/mcp-status-privatefirst.ts`, 3 s exact-cancel, `mcp-status:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + `context.directory`; accepted success returns with zero SDK, validated terminal `validation.failed`/`scope_mismatch`/`internal` closes with zero SDK, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.mcp.status` with no retry; all three production readers (`KiloProvider.fetchAndSendMcpStatus`, Agent Manager backend snapshot, agent-manager mcp warmup) go through the shared helper preserving existing fail-soft/cache/log behavior; connect/disconnect are private-only mutations over the same carrier (`kilocode/mcp-connection-private.ts` -> same-directory `MCP.Service.connect`/`disconnect` under canonical directory + `acquireDrainControl` + `InstanceRef` lane held to completion, strict `mcp-connect:<token>` / `mcp-disconnect:<token>` `opId==idempotencyKey` + `requestId` + `context.directory` + `payload.name` with unknown-field reject, typed terminal `mcp.not_found`/`validation.failed`/`scope_mismatch`/`internal` plus retryable `InstanceUnavailableDuringConfigRebuild`, no cache, no durable journal, no replay; extension `mcp-connection-privatefirst.ts` + `serve-private-mcp-connection-contract.ts` + `ServePrivatePeer` + `serve-private-mcp-connection(-owner).ts` via `wrapEpochHandle`, 3 s exact-cancel, at most one private call per user action with zero SDK fallback and zero retry, every outcome (success, terminal, ambiguous, unavailable) re-observes `mcp/status` and a failed re-observation posts the minimal `mcpActionDone` completion that clears only the matching webview `mcpLoading` slot without touching the status cache, terminal/ambiguous/unavailable surface a short `showErrorMessage` with no payload/secret; authenticate is a private-only mutation over the same carrier (`kilocode/mcp-connection-private.ts` -> same-directory `MCP.Service.authenticate` under canonical directory + `acquireDrainControl` + `InstanceRef` lane held to completion, strict `mcp-authenticate:<token>` `opId==idempotencyKey` + `requestId` + `context.directory` + `payload.name` with unknown-field reject, typed terminal `mcp.not_found`/`validation.failed`/`scope_mismatch`/`internal` plus retryable `InstanceUnavailableDuringConfigRebuild`, no cache, no durable journal, no replay; extension `mcp-connection-privatefirst.ts` + `serve-private-mcp-connection-contract.ts` + `ServePrivatePeer` + `serve-private-mcp-connection(-owner).ts` via `wrapEpochHandle`, one private call per user action with zero SDK fallback and zero retry and the OAuth-appropriate 5 min exact-cancel bound, every outcome converging authoritative `mcp/status` with the same `mcpActionDone` completion semantics as connect/disconnect; `mcp/add` is a private-only registration over the same carrier (`kilocode/mcp-connection-private.ts` -> same-directory `MCP.Service.add` under canonical directory + `acquireDrainControl` + `InstanceRef` lane held to completion, strict `mcp-add:<token>` `opId==idempotencyKey` + `requestId` + `context.directory` + `payload.name+config` with unknown-field reject, accepted success carrying the full five-state status map, typed terminal `validation.failed`/`scope_mismatch`/`internal` plus retryable `InstanceUnavailableDuringConfigRebuild`, no cache, no durable journal, no replay; extension `serve-private-mcp-add-contract.ts` + `ServePrivatePeer.privateMcpAddOutcomeWithHandle` + `serve-private-mcp-connection(-owner).ts` via `wrapEpochHandle` + shared `kilo-provider/mcp-add-privatefirst.ts`, 3 s exact-cancel, at most one private call per registration with zero SDK fallback and zero retry, the returned status map as the sole convergence source); toggle stays SDK-only with no lifecycle change; browser automation register goes through the private-only add helper preserving the connected/failed/disconnected status mapping with zero SDK `client.mcp.add` caller remaining, and disable/unregister goes through the existing private-only disconnect helper over the same connection owner with zero SDK fallback/retry, fail-soft to `disabled`).
- VS Code `path/get` for model-state is private-first over the same fd carrier (`fd-carrier.ts` `path/get` + `path/get` capability reading the same production source as `GET /path` with mixed ownership: process-global `Global.Path.home/state/config` plus directory-routed `InstanceState` `worktree`/`directory`; extension `serve-private-path-contract.ts` + `ServePrivatePeer.privatePathOutcomeWithHandle` + shared `kilo-provider/path-privatefirst.ts`, 3 s exact-cancel/epoch, `path:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + routing-only `context.directory`; valid private `succeeded`+`accepted` returns `Path.state` with zero SDK, validated terminal `failed` (`retryable === false`) closes with zero SDK, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-spawn-cwd SDK `client.path.get({directory})` with no retry; no private connection or no active spawn cwd stays exactly one SDK `client.path.get()` with no args and no private request, never `process.cwd()`/session-dir; per-backend-identity cache (active spawn cwd + private epoch/availability) re-reads on worker/peer epoch or routing change so a backend restart never reuses a stale model.json path, failures never populate the cache, `model.json` read/write/ENOENT/webview semantics unchanged; mutations stay out of scope, no worktree freshness or global-field isolation claim).
- VS Code `remote/status` reads plus `remote/enable|disable` writes are private-first over the same fd carrier (`fd-carrier.ts` `remote/status` + `remote/enable` + `remote/disable` capabilities calling the same process-global `KiloSessions` owner as the HTTP routes, no `InstanceRef` lane, no second store; extension `serve-private-remote-status.ts` strict `remote-status:<token>` binding + `kilo-provider/remote-status-privatefirst.ts` shared read helper plus `serve-private-remote-toggle-contract.ts`/`serve-private-remote-toggle.ts` per-action `remote-enable|disable:<token>` binding with distinct write types + `kilo-provider/remote-toggle-privatefirst.ts` shared mutation helper, 3 s exact-cancel, `opId`/`idempotencyKey` + `requestId` + routing-only `context.directory`/`workspace?`; valid private `succeeded`+`accepted` adopts the owner status with zero SDK, validated terminal `failed` (`retryable === false` such as `validation.failed`/`auth.missing`/`auth.invalid`/`internal`) closes with zero SDK and throws for mutations, unavailable/retryable/invalid/ambiguous/transport/closed/timeout takes exactly one same-action SDK fallback with no retry (the owner is idempotent, so ambiguous safely repeats); `RemoteStatusService.refresh()`, toggle pre-read, and `setEnabled()` all go through the helpers so one user action issues at most one private attempt plus at most one SDK fallback, preserving status-bar/event/listener updates and refresh warn-only vs toggle/setEnabled-throw failure UX with SDK failure keeping the old state; SSE `remote-status-changed` remains the subsequent transition authority; the old SDK-first detached parity observer is removed from production while `compareRemoteStatusParity` stays as pure diagnostic/test evidence only with no third request).
- Profile logout is private-first over the same fd carrier (`auth-remove-private.ts` `auth/remove` capability running the same global cold `invalidateAfterProviderAuthChange` over `Auth.remove` as the HTTP route with no `cleanupDisabled`, routing-only directory, no drain/`InstanceRef`/scope-mismatch/journal; extension `serve-private-auth-remove-contract.ts` + `serve-private-auth-remove.ts` strict `auth-remove:<token>` binding + `ServePrivatePeer`/`connection-service` `privateAuthRemoveOutcomeWithHandle` + `kilo-provider/auth-remove-privatefirst.ts` `removeAuthPrivateFirst`, 3 s exact-cancel; `handleLogout` stays the sole entry with the SDK `client.auth.remove` call living only in the helper fallback; validated success and terminal (`retryable === false`) close with zero SDK, unavailable/invalid/ambiguous/retryable/transport/closed/timeout takes exactly one same-identity SDK fallback, failures still surface and success still clears profile plus refreshes providers with never `global.dispose`; `Auth.remove` is idempotent so ambiguous safely repeats). Next gap: `auth.set`/OAuth callback still uses its legacy SDK path with no private carrier.
- Organization switch is private-first over the same fd carrier (`organization-set-private.ts` `kilo/organization/set` capability running the same global cold mutation as the HTTP `kilo.organization.set` route via shared `organizationSetMutate` over `invalidateAfterProviderAuthChange` with fence-internal `Auth.get`, refresh/access/expires preservation, `accountId` written only from `organizationId`, `clearModesCache` in mutate, no `cleanupDisabled`, routing-only directory, no drain/`InstanceRef`/scope-mismatch/journal; extension `serve-private-organization-set-contract.ts` + `serve-private-organization-set.ts` strict `organization-set:<token>` binding + `ServePrivatePeer`/`connection-service` `privateOrganizationSetOutcomeWithHandle` + `kilo-provider/organization-set-privatefirst.ts` `setOrganizationPrivateFirst`, 3 s exact-cancel; `handleSetOrganization` stays the sole entry with the SDK `client.kilo.organization.set` call living only in the helper fallback; validated success and terminal (`retryable === false`, including `unauthorized` for the backend's real auth-not-found semantics) close with zero SDK, unavailable/invalid/ambiguous/retryable/transport/closed/timeout takes exactly one same-`organizationId` SDK fallback, success still refreshes profile/providers/agents and failure keeps the best-effort profile reset with never `global.dispose`; repeating the same `organizationId` is a safe overwrite so ambiguous safely repeats, and neither path retries.
- VS Code `agent/list` reads are private-first over the same fd carrier (`agent-list.ts` `agent/list` capability running the same shared `fetchAgentListData` (`Agent.Service.list()`) as `GET /agent` with routing-only directory (`directory`/`workspace?` never reach the service beyond `InstanceState` selection), existing drain-control + `InstanceRef` lane inside `agentListPrivate` with no new lifecycle/manual `InstanceRef`/new lease/network/secret/cache; extension `serve-private-agent-list-contract.ts` + `ServePrivatePeer.privateAgentListOutcomeWithHandle` + `serve-private-agent-list-connection.ts` `agentListHandle` (extracted `kilo/profile` owner shape, no new `connection-service` wrapper) + `kilo-provider/agent-list-privatefirst.ts` `fetchAgentsPrivateFirst`, requestId-only observation identity (no `opId`/`idempotencyKey`, empty payload, strict `succeeded`/`failed`/`ambiguous` result with exact `Agent.Info` wire), 3 s exact-cancel/settled-first epoch; validated success (including empty) returns the full wire with zero SDK, validated terminal `failed` (`retryable === false`, including `validation.failed`/`scope_mismatch`/`internal`) closes with zero SDK, retryable fence (`InstanceUnavailableDuringConfigRebuild`) plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.app.agents` fallback with no retry inside the helper; `KiloProvider.fetchAndSendAgents` keeps the canonical `sendCanonicalAgents` short-circuit untouched and the legacy caller `retry()`/`filterVisibleAgents`/order/`cachedAgentsMessage`/`postMessage` semantics with the SDK call living only in the helper fallback; read-only and safely repeatable with no durable op row. full config reads, notebook, OAuth, and agent UI/sort rules stay out of scope.
- VS Code `kilo/profile` reads are private-first over the same fd carrier (`kilo-profile.ts` `kilo/profile` capability running the same shared `fetchKiloProfileData` (`Auth.get("kilo")` + `fetchProfile`/`fetchBalance`/`fetchKiloPassState`) as `GET /kilo/profile` with routing-only directory (`directory`/`workspace?` never reach the gateway), no drain/`InstanceRef`/journal/reconcile; extension `serve-private-kilo-profile-contract.ts` + `ServePrivatePeer.privateKiloProfileOutcomeWithHandle` + `serve-private-kilo-profile-connection.ts` `kiloProfileHandle` + `kilo-provider/kilo-profile-privatefirst.ts` `fetchKiloProfilePrivateFirst`, requestId-only observation identity (no `opId`/`idempotencyKey`, empty payload, strict `succeeded`/`failed`/`ambiguous` result), 3 s exact-cancel; local unauthenticated state closes as terminal `unauthorized` with zero SDK, validated terminal `failed` (`retryable === false`, including `validation.failed`/`internal`) closes with zero SDK, upstream (`retryable === true`) plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.kilo.profile` fallback with no retry; all six production readers (`KiloProvider.syncWebviewState`, SSE-connected, login post-callback, org-switch success, org-switch failure reset, manual refresh) go through the shared helper with the SDK call living only in the fallback, preserving warn-only/best-effort UX; read-only and safely repeatable with no durable op row. OAuth `authorize`/`complete` keeps its legacy SDK path with no private carrier; `instance.reload` is private-first (see disposed-lifecycle bullet).
- VS Code `provider/catalog` reads are private-first over the same fd carrier (`provider-catalog.ts` `provider/catalog` capability running the same shared `fetchProviderCatalogData` (`Config.Service.get()` + `Provider.Service.list()` + `filterPromptTrainingModels` + `toCatalogResult`) as `GET /provider/catalog` with routing-only directory, existing drain-control + `InstanceRef` lane inside `providerCatalogPrivate` with no new lane/fence/cache/external network; extension `serve-private-provider-catalog-contract.ts` + `ServePrivatePeer.privateProviderCatalogOutcomeWithHandle` + `serve-private-provider-catalog-connection.ts` `providerCatalogHandle` (extracted `kilo/profile` owner shape, no new `connection-service` wrapper) + `kilo-provider/provider-catalog-privatefirst.ts` `fetchProviderCatalogPrivateFirst`, requestId-only observation identity (no `opId`/`idempotencyKey`, empty payload, strict `succeeded`/`failed`/`ambiguous` result with exact closed `CatalogResult` wire and `key`/`options`/`headers` fail-closed at every layer), 3 s exact-cancel/settled-first epoch; validated success (including empty) returns the full wire with zero SDK, validated terminal `failed` (`retryable === false`, including `validation.failed`/`scope_mismatch`/`internal`) closes with zero SDK, retryable fence (`InstanceUnavailableDuringConfigRebuild`) plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.provider.catalog` fallback with no retry inside the helper; `provider-actions.fetchProviderData` keeps private-first auth soft degradation and `kilo.authStatus` `catch`-to-`null` isolation with catalog authority unchanged, catalog failure still rejects the whole fetch with the outer `KiloProvider` keeping its old cache; read-only and safely repeatable with no durable op row. canonical provider pipeline, OAuth, snapshot endpoint, and HTTP/SDK schema stay out of scope.
- VS Code `provider/auth` reads are private-first over the same fd carrier (`provider-auth.ts` `provider/auth` capability running the same shared `fetchProviderAuthData` (sole `ProviderAuth.Service.methods()` caller with strict closed validation) as `GET /provider/auth` with routing-only directory, existing drain-control + `InstanceRef` lane inside `providerAuthPrivate` with no new lane/fence/cache/external network/secret/journal; extension `serve-private-provider-auth-contract.ts` + `ServePrivatePeer.privateProviderAuthOutcomeWithHandle` + `serve-private-provider-auth-connection.ts` `providerAuthHandle` (extracted `kilo/profile` owner shape, no new `connection-service` wrapper) + `kilo-provider/provider-auth-privatefirst.ts` `fetchProviderAuthPrivateFirst`, requestId-only observation identity (no `opId`/`idempotencyKey`, empty payload, strict `succeeded`/`failed`/`ambiguous` result with exact open providerID-to-Method[] map and closed methods/text-select prompts with absent-valid/null-invalid optionals and unknown nested fields fail-closed), 3 s exact-cancel/settled-first epoch; validated success (including empty) returns the full map with zero SDK, validated terminal `failed` (`retryable === false`, including `validation.failed`/`scope_mismatch`/`internal`) closes with zero SDK and the caller degrades to `{}`, retryable fence (`InstanceUnavailableDuringConfigRebuild`) plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.provider.auth` fallback with no retry inside the helper (old SDK without the method degrades to `unavailable` without a call); `provider-actions.fetchProviderData` keeps three branches parallel with catalog failure still rejecting and auth/status failures soft, UI `authMethods` derivation unchanged; read-only and safely repeatable with no durable op row. OAuth authorize/callback, canonical provider pipeline, snapshot endpoint, and HTTP/SDK schema stay out of scope.
- VS Code `config/ui-defaults` reads are private-first over the same fd carrier (`config-ui-defaults.ts` `config/ui-defaults` capability running the same effective `Config.Service.get()` (global/project merge) as `GET /config` through shared `fetchUiDefaultsData` with an immediate closed projection (`workStyle:{hasPermission,terminalCommandDisplay?,autoCollapseReasoning?}`, `sandbox:{enabled}`) and no HTTP route for the op, with routing-only directory (`directory`/`workspace?` never reach the service beyond `InstanceState` selection), existing drain-control + `InstanceRef` lane inside `configUiDefaultsPrivate` with no new lane/fence/cache/write/journal; extension `serve-private-config-ui-defaults-contract.ts` + `ServePrivatePeer.privateConfigUiDefaultsOutcomeWithHandle` + `serve-private-config-ui-defaults-connection.ts` `configUiDefaultsHandle` (extracted `kilo/profile` owner shape, no new `connection-service` wrapper) + `shared/config-ui-defaults-privatefirst.ts` `fetchConfigUiDefaultsPrivateFirst`, requestId-only observation identity (no `opId`/`idempotencyKey`, empty payload, strict `succeeded`/`failed`/`ambiguous` result with presence-only `hasPermission` covering empty objects and full rulesets, scalar display values, strict `sandbox.enabled === true`, and permission/provider/MCP content fail-closed at every layer), 3 s exact-cancel/settled-first epoch; validated success returns the closed projection with zero SDK, validated terminal `failed` (`retryable === false`, including `validation.failed`/`scope_mismatch`/`internal`) closes with zero SDK and both callers throw (no silent degrade), retryable fence (`InstanceUnavailableDuringConfigRebuild`) plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.config.get` fallback projected locally to the same closed shape with no retry inside the helper; the work-style apply reader maps the projection onto the untouched `buildWorkStyleApplyPlan` (presence marker only, never rule content; preset, write order, rollback, and `global.config.update` owner unchanged) and the sandbox readers keep `config.sandbox?.enabled === true` semantics with `sandboxSessionMetadata` callers passing the connection service; read-only and safely repeatable with no durable op row. Private config writes, full config reads, permission presets, provider/MCP secrets, OAuth, and HTTP/SDK schema stay out of scope.
- VS Code `kilo/auth-status` reads are private-first over the same fd carrier (`kilo-auth-status.ts` `kilo/auth-status` capability running the same shared `fetchKiloAuthStatusData` (sole `Auth.Service.get("kilo")` + `getToken` projection into closed `{authenticated,type?}`, no token/`Auth.Info` return, exact `AuthStatus` wire with no cross-field constraint) as `GET /kilo/auth-status` with routing-only directory, no drain-control/`InstanceRef` lane/fence/cache/network/timeout/signal; extension `serve-private-kilo-auth-status-contract.ts` + `ServePrivatePeer.privateKiloAuthStatusOutcomeWithHandle` + `serve-private-kilo-auth-status-connection.ts` `kiloAuthStatusHandle` (extracted `kilo/profile` owner shape, no new `connection-service` wrapper) + `kilo-provider/kilo-auth-status-privatefirst.ts` `fetchKiloAuthStatusPrivateFirst`, requestId-only observation identity (no `opId`/`idempotencyKey`, empty payload, strict `succeeded`/`failed`/`ambiguous` result with closed `authenticated` required boolean plus optional non-null `type:"api"|"oauth"` and unknown/secret fields fail-closed), 3 s exact-cancel/settled-first epoch; validated success returns the closed shape with zero SDK, validated terminal `failed` (`retryable === false`, including `validation.failed`/`internal`) closes with zero SDK and the caller degrades to `null`, unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.kilo.authStatus` fallback with no retry inside the helper (SDK failure/malformed returns `unavailable` for the caller to degrade); `provider-actions.fetchProviderData` keeps three branches parallel with catalog failure still rejecting and auth/status failures soft, `authStates` kilo coverage unchanged; read-only and safely repeatable with no durable op row. OAuth authorize/callback, canonical provider pipeline, snapshot endpoint, UI, and HTTP/SDK schema stay out of scope.
- VS Code `provider/models-discover` reads are private-first over the same fd carrier (`provider-models-discover.ts` `provider/models-discover` capability running the same shared `fetchProviderModelsDiscoverData` (exact provider plus exact stored `baseURL` with backend-held Bearer key, strict URL plus exact-match gate, exact `{models:[{id,name}]}` wire) as `POST /provider/:providerID/models` with routing-only directory plus strict `{providerID,baseURL}` payload, existing drain-control + `InstanceRef` lane inside `providerModelsDiscoverPrivate` with no new lane/fence/cache/journal; extension `serve-private-provider-models-discover-contract.ts` + `ServePrivatePeer.privateProviderModelsDiscoverOutcomeWithHandle` + `serve-private-provider-models-discover-connection.ts` `providerModelsDiscoverHandle` (extracted `kilo/profile` owner shape, no new `connection-service` wrapper) + `kilo-provider/models-discover-privatefirst.ts` `discoverModelsPrivateFirst`, requestId-only observation identity (no `opId`/`idempotencyKey`, strict `succeeded`/`failed`/`ambiguous` result with exact models wire and secret entry fields fail-closed at every layer), 3 s exact-cancel/settled-first epoch; validated success (including empty) returns the exact wire with zero SDK, validated terminal `failed` (`retryable === false`, including `validation.failed`/`scope_mismatch`/`unauthorized`/`invalid_response`/`upstream_error`/`internal`) closes with zero SDK (caller posts the redacted message with the `unauthorized` auth UX), retryable fence (`InstanceUnavailableDuringConfigRebuild`) plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.provider.models.discover` fallback with no retry inside the helper; `KiloProvider.handleFetchCustomProviderModels` keeps freshly typed keys, custom headers, missing providerID, and canonical `SecretStorage` direct paths untouched with the `customProviderModelsFetched` shape unchanged; read-only and safely repeatable with no durable op row. OAuth authorize/callback, canonical provider pipeline, snapshot endpoint, and HTTP/SDK schema stay out of scope.
- VS Code `session/status` reads are private-first over the same fd carrier (`fd-carrier.ts` `session/status` + `session/status` capability reading the same same-directory `SessionStatus.Service.list()` map as `GET /session/status` under the `acquireDrainControl` snapshot lane, `idle` omitted by semantics; extension `serve-private-peer.ts` strict `status:<token>` binding + shared `kilo-provider/session-status-privatefirst.ts`, 3 s exact-cancel/epoch, `status:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + `context.directory`; valid private `succeeded`+`accepted` returns the full map with zero SDK, validated terminal `failed` (`retryable === false`) closes with zero SDK, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.session.status` fallback with no retry and SDK error/malformed returns `unavailable`; seed (`seedSessionStatuses`) only applies a successful map (set+post, `reconcile=true` still only resets locally non-idle but server-absent entries to idle) while failure/terminal/empty-invalid writes nothing and never reconciles, sidebar fork guard (`handleForkSession`) reads the local map first and only on miss calls the helper (absent defaults idle, failure/terminal conservative `busy`) with no map/post/reconcile/cache before the unchanged fork mutation, Agent Manager fork adds no guard; `KiloProvider.refreshSessionDetails` status tail goes through the same helper (same directory, tracked-`sessionID` filter, same `sessionStatus` shape/log, abort-checked, failure/terminal only logs, no map/reconcile/cache, revision/detail behavior unchanged); SSE `session.status` remains the transition authority with order/filter unchanged; no new owner/cache; the old SDK-first detached parity observer plus deferred status wiring is removed from production while `compareStatusParity` stays as pure diagnostic/test evidence only with no third request).
- VS Code `project/current` narrow projection for `hasGit` is private-first over the same fd carrier (`fd-carrier.ts` `project/current` + `project/current` capability reading `(yield* InstanceState.context).project` through the existing drain-control + `InstanceRef` lane, narrow `{vcs?: "git"}` only with any other `vcs` value mapping to redacted `internal`; extension `serve-private-project-current-contract.ts` + `ServePrivatePeer.privateProjectCurrentOutcomeWithHandle` + shared `kilo-provider/project-current-privatefirst.ts`, 3 s exact-cancel, `project-current:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + routing-only `context.directory`; valid private `succeeded`+`accepted` returns the derived `vcs === "git"` boolean with zero SDK, validated terminal `failed` (`retryable === false` except `transport`, such as `validation.failed`/`internal`) closes fail-closed `false` with zero SDK, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.project.current` fallback with no retry and SDK failure/malformed returns `false`; `KiloProvider.initializeConnection` still issues one `hasGit` call caching to `cachedGitRepo` with the `gitStatus` message unchanged so one user action issues at most one private attempt plus at most one SDK read, never two SDK reads; `initGit`/`update`/`directories`/`list`, full `Project.Info`, `worktree`/`sandboxes` derivation, freshness/ordering, and Git mutation stay out of scope with no new owner or cache; the old SDK-first detached parity observer plus deferred wiring is removed from production while `compareProjectCurrentParity` stays as pure diagnostic/test evidence only with no third request).
- VS Code `config/warnings` safe projection for `checkConfigWarnings` is private-first over the same fd carrier (`fd-carrier.ts` `config/warnings` + `config/warnings` capability reading the same `Config.Service.warnings()` source as `GET /config/warnings` through the existing drain-control + `InstanceRef` lane, safe `{pathCategory,messageCategory}` only with raw paths/text/detail never crossing; extension `serve-private-config-warnings-contract.ts` + `ServePrivatePeer.privateConfigWarningsOutcomeWithHandle` + shared `kilo-provider/config-warnings-privatefirst.ts`, 3 s exact-cancel, `config-warnings:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + routing-only `context.directory`; valid private `succeeded`+`accepted` returns the safe list with zero SDK, validated terminal `failed` (`retryable === false` except `transport`, such as `validation.failed`/`internal`) closes with zero SDK, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-directory SDK `client.config.warnings` fallback with no retry and SDK failure/malformed returns `unavailable`; `KiloProvider.checkConfigWarnings` still shows one consolidated warning once per lifecycle with the `Show Details` channel unchanged so one check issues at most one private attempt plus at most one SDK read, never two SDK reads; full warning text stays SDK-fallback-only, ordering/freshness stay out of scope with no new owner or cache; the old SDK-first detached parity observer plus deferred wiring is removed from production while `compareConfigWarningsParity` stays as pure diagnostic/test evidence only with no third request).
- VS Code `find/files` bounded search for `handleFileSearch` is private-first over the same fd carrier (`fd-carrier.ts` `find/files` + `find/files` capability reading the same `FileSystem.Service.find` source as `GET /find/file` through the existing drain-control + `InstanceRef` lane with explicit `type` and bounded `limit`, safe `{path,type}` relative POSIX only; extension `serve-private-find-files-contract.ts` + `ServePrivatePeer.privateFindFilesOutcomeWithHandle` + shared `kilo-provider/find-files-privatefirst.ts`, 3 s exact-cancel, `find-files:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + routing-only `context.directory` + `payload query/type/limit`; each logical query (`type:file` and `type:directory`, limit 50) runs one private attempt first with valid private `succeeded`+`accepted` (including empty) authoritative with zero SDK, validated terminal `failed` (`retryable === false` except `transport`) closing fail-soft with zero SDK, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout taking exactly one same-tuple SDK `client.find.files` fallback per type with no retry and SDK failure/malformed mapping to `[]` for that type; merge/dedup/order and fail-soft `fileSearchResult` post unchanged so one search issues at most two private attempts plus at most two SDK fallbacks, never two SDK reads per type; the old SDK-first detached parity observer plus deferred wiring is removed from production while `compareFindFilesTypeParity` stays as pure diagnostic/test evidence only with no third request).
- `CanonicalConfigService`-controlled GUI config/asset writes use the private `config/convergence/acquire` + `config/convergence/resolve` fence: acquire blocks the write on failure, the runtime re-reads disk and converges noop/hot/cold, resolve loss stays persisted with a `pending` diagnostic and backend auto-resolve from disk. External canonical config/asset edits request fail-safe cold convergence via a descriptor-only private hint (`config/convergence/observe`, FD-only, no SDK fallback): the watcher hint carries only request/op identity plus canonical config/asset descriptors after successful local scan/materialization (asset ids from direct `<asset>/<id>.md` only, except skill ids from exact `<name>/SKILL.md` under canonical singular/plural roots (one logical `skill` asset); every asset descriptor always cold), the server authorizes directories and guards every resolved descriptor file from its authorized root (project directory or global config dir: each existing intermediate/leaf component must not be a symlink, nearest existing ancestor plus not-yet-created tail stay inside the root; absent paths stay allowed) and registers a cold obligation under the existing begin→commit lifecycle (worker boots latest disk; missing/unreadable/malformed still cold-converges to normal loader diagnostics, never false noop), and hint failure leaves local state intact with pending diagnostics. No hot/noop classification and no baseline; repeated same-state hints are cold by design (may rebuild). Extension coalescing is a bounded per-scope descriptor accumulator (one inflight plus trailing pending per scope with per-descriptor dedup; batches of max 8 drain until empty; rename/delete keep old and new ids; own-write hash hits send none; non-.md/invalid/nested-subdir keep fail-soft with diagnostics).
- Dual `JsonRpcPeer` (`packages/opencode/src/private-worker/peer.ts` ↔ `packages/kilo-vscode/src/private-worker/peer.ts`) now carries wire-level best-effort `$/cancelRequest` plus generic per-request correlated `$/event` (`requestWithId`/`requestWithEvents` register `onEvent` atomically before write, `RequestContext.emit` scoped to active incoming ownership (`state==="open"` and exact `AbortController` still owned), explicit `id` routing via `{id, event}`, FIFO `$/event` before terminal `result`/`error`, cleaned on terminal success/error, local drop via `tryCancelPending`/`cancel`, peer close/dispose, and send failure with late events ignored and never recreating pending, malformed `$/event` ignored with peer staying open) with no unary reverse method: `provider/execute` has been removed and generation runs only over streaming `provider/httpExecute` (see streaming/session-generation bullets). `tryCancelPending` stays local-only; `cancel(id)` only guarantees the signal plus local pending ownership (handler must consume the signal, no forced termination); cancellation ownership remains existing `$/cancelRequest`. Session generation runs over streaming `provider/httpExecute`; see streaming/session-generation bullets.
- Process-owned private peer registry exists in default CLI `AppRuntime` as discoverability + typed streaming broker foundation (`private-peer-registry.ts` + `ProviderHttpExecuteBroker` via `AppLayer`, no peer/stream ownership; fd carrier stays sole resource owner): single-owner exact install/release (duplicate live install fails closed, closed holder swept, request only on open initialized peer), 5 s bounded `awaitCarrierReady` (failure/timeout disposes started carrier and serve continues carrier-less HTTP; only `ready` publishes the carrier, port `stdout` after the wait), close/dispose plus late install completion reconcile through the lifecycle handoff barrier drained before shutdown with no registry epoch. The legacy CLI-side typed unary broker `ProviderExecuteBroker` has been removed; only the streaming `ProviderHttpExecuteBroker` remains over the process-owned registry (see streaming bullet). Shared protocol/path/failure-code primitives live in `@opencode-ai/core/kilocode/provider-execute.ts` (unary `PROVIDER_EXECUTE_METHOD`/request/result envelope removed; full `CanonicalProviderPayload` AST stays host-side). No session prompt/command, generation lifecycle beyond streaming generation, provider record discovery, or built-in timeout wiring exists beyond streaming.
- Internal scope-aware canonical provider provenance snapshot plus typed resolver exists as a bounded internal state (`packages/core/src/kilocode/canonical-record.ts` shared pure closed-record/model validation + `packages/opencode/src/kilocode/provider/canonical-provenance.ts` `{id,scope:"global"|"project",source,record}` + conflicts `{duplicate,missing/malformed credential,scope/id mismatch,invalid endpoint/protocol/models}` + `packages/opencode/src/kilocode/provider/canonical-resolver.ts` typed `resolve(providerId,modelId)` returning exact closed record with opaque `credentialRef` and closed-record shape; `Config.State` stores `canonical` alongside `config` from the single `Config.loadInstanceState` pass (raw global/project canonical-only entries before deep merge, validated via shared/host-compatible logic, scope+ID must match file scope+map key, duplicate cross-scope omits that ID with typed conflict, no Frankenstein merge, per-provider omission with warnings not crash); `Config.Service` exposes internal `getCanonicalProvenance()`/`getCanonicalProviders()`/`getWithCanonical()` and `CanonicalProviderSnapshotRef`; `withConfigSnapshot` atomically pins `Config.Info` + provenance together (single `InstanceState.use` read, no two-step refresh race, `Config.get`/`getCanonicalProvenance` prefer the pinned refs); `Config.get`/HTTP `/config` never enumerates/serializes provenance and effective merge/precedence unchanged for non-execution consumers; admitted generation snapshots pin both together, subsequent invalidation/edits affect only subsequent snapshots; resolver never executes/selects on host and surfaces typed `notFound`/`conflict`/`modelNotFound` without leaking plaintext-like secret values; external edits preserve existing lifecycle (no watchers, global refresh via stamp, project external edit explicit, convergence writes invalidate/rebuild with old snapshot retained). No session generation/streaming/provider catalog/SecretStorage wiring in CLI, no `SessionProcessor`/private-runtime link, no streaming protocol.
- Legacy CLI canonical unary provider execution service (`CanonicalResolver`+`ProviderExecuteBroker` composition at `packages/opencode/src/kilocode/provider/canonical-provider-execute.ts` via `AppLayer`) has been removed along with `ProviderExecuteBroker` and its `canonical-provider-execute.test.ts` proof; `AppLayer` wires only streaming `ProviderHttpExecuteBroker`. Session generation runs only over streaming `provider/httpExecute` (see session-generation bullet).
- Shared `provider/httpExecute` streaming contract (`@opencode-ai/core/kilocode/provider-http-execute.ts` `PROVIDER_HTTP_EXECUTE_METHOD`/`ProviderHttpExecuteWire`: `{providerId,modelId,record,body,headers?}` request, `{seq:0,status,headers}` metadata plus ordered `{seq,bytes}` base64 chunks plus `{seq,chunks,bytes}` terminal, bounded sizes/counters) runs over the trusted private carrier: CLI supplies the exact admission-pinned closed `record` plus `body` plus non-sensitive `headers` (validated closed record, body `model===modelId`, duplicate top-level `model` rejected, forbidden/sensitive headers rejected); host uses the exact supplied pinned record and does not reread latest disk config, resolves the exact owned `SecretStorage` credential ref, injects it on the fixed `POST` route by protocol (`openai/completions→/chat/completions`, `openai/responses→/responses`, `anthropic/messages→/messages`), uses `redirect:manual` with no follow and no secret to redirect target, rejects sensitive response headers and redacts secret-carrying values, and streams response as correlated `$/event` metadata then ordered base64 chunks with terminal counts (`seq===chunks`, `bytes` sum, `seq 1..8192`, `64 KiB` chunk/`16 MiB` total). CLI `ProviderHttpExecuteBroker` (`packages/opencode/src/kilocode/server/provider-http-execute-broker.ts` via `AppLayer` over `PrivatePeerRegistry` `requestWithEvents`, no peer/stream ownership, no Promise facade) is metadata-first scoped bounded stream: validates request keeping `record` opaque, registers `onEvent` atomically before write, emits `metadata` `seq:0` immediately then ordered chunks over exact `$/event` correlation (`{id,event}` routing, monotonic `seq`, `Queue.bounded(32)`), validates terminal counts against emitted bytes/chunks, fiber interruption/`Scope` close drops the exact `PrivatePeer.Call` so `$/cancelRequest` reaches host `AbortSignal` which cancels `fetch`; `Stream`/`Scope` owns the `Call`, not the registry, with bounded `Queue` and exact `drop` once. Parsing/tools stay CLI-side; host is only secret-bearing HTTP executor. CLI now has a canonical-only `RequestExecutor` adapter (next bullet); `LLMClient` parsing is proven, now wired via `LLM.run`/`SessionPrompt`/`CanonicalNative` (see session-generation bullet); the legacy unary `provider/execute` seam has been removed.
- CLI canonical-only `RequestExecutor` adapter (`packages/opencode/src/kilocode/provider/canonical-request-executor.ts` `make`/`layer` via `AppLayer`): captures immutable resolved `providerId`/`modelId`/`record` (deepClone + deepFreeze at construction and via `layer`) plus `ProviderHttpExecuteBroker`; per-invocation strict validation of prepared `POST` JSON body (`ProviderHttpExecuteWire` `model===modelId`, duplicate-key rejection), URL against captured `record`/`protocol` fixed route/origin (no query/hash/userinfo), and headers (`content-length`/`host` excluded, forbidden/sensitive rejected, comma values forwarded); converts broker metadata-first `HttpStream {status, headers, stream: Stream<Uint8Array>}` (seq 0 metadata, ordered base64 chunks, terminal) into incremental `HttpClientResponse` (`Response` + `ReadableStream<Uint8Array>`) via `Scope`-owned detached `Fiber` with exact single `Scope.close`/`Fiber.interrupt` on completion/cancel/error/interrupt (`Scope` owns the `Call`, not the registry, `Stream`/`Scope` drop once → `$/cancelRequest` → host `AbortSignal`); provider HTTP `>=400` buffered and mapped consistent with `packages/llm/src/route/executor.ts` (`401`/`403` auth, `429` RateLimit/QuotaExceeded with retry-after/rate-limit, `400/404/409/422` InvalidRequest, `>=500` ProviderInternal, else UnknownProvider incl. `408`; body redacted/truncated `16 KiB`; `content_filter` → ContentPolicy); `LLMClient` structured system/messages/tools and incremental split-chunk SSE → `LLMEvent` proven (OpenAIChat `Auth.none`); now selected by `LLM.run`/`SessionPrompt` via `CanonicalNative` (see session-generation bullet); Provider DB still untouched (no insertion). Recovery: `CanonicalRequestExecutor` retries only pre-exposure retryable `429`/`5xx` responses with the existing 2-retry/`Retry-After` policy, every attempt owning/closing its own broker scope, with no replay after `2xx` stream exposure; configured positive `chunkTimeout` is threaded per canonical invocation to an arrival-based byte-stream watchdog that resets per broker chunk and cancels the exact call with Transport Timeout/`$/cancelRequest` (absent/nonpositive disabled).

- Supplied prompt `messageID` is first-writer-wins in `SessionPrompt`: the first accepted durable user message is authoritative and later reuse never overwrites/duplicates parts or side effects; concurrent same-ID calls share one process-local singleflight result; durable V1 replay with no assistant lineage and no per-ID queue owner reattaches once behind the queue, while existing lineage or per-ID ownership suppresses duplicate generation; supplied command `messageID` shares the same first-writer/singleflight/replay (first writer owns lookup/template/shell/plugin/intake/enqueue or legacy static assistant, replay never repeats shell/plugin/intake/generation). VS Code `session/prompt` is private-first over the same fd carrier (`session-prompt-dispatch.ts` → `SessionPrompt.prompt` fork-detached accept, `fd-carrier.ts` `session/prompt` + `session/prompt` capability, extension `session-prompt.ts` + `serve-private-prompt-contract.ts` + `ServePrivatePeer.privatePromptWithHandle` + `connection-service.privatePromptWithHandle`, 3 s exact-cancel/epoch): single `prompt:<messageId>` durable tuple for `opId`/`idempotencyKey` + `requestId` + `directory`/`sessionId`/`messageId`, validated `succeeded`+`accepted` and validated terminal `failed` with `retryable === false` (`session.not_found`/`scope_mismatch`/`validation.failed`) close with zero SDK, validated `retryable === true` plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-tuple SDK `promptAsync` fallback, never retried; ambiguous server outcomes normalize to fallback so the existing first-writer-wins/singleflight/replay guarantees no second generation. Both VS Code `promptAsync` call sites (`KiloProvider.handleSendMessage`, Agent Manager local session start) converge on `promptSessionPrivateFirst` with no UI change; `prompt_async` HTTP with `messageID` now routes to the same `SessionPromptDispatch` owner (stable `prompt_async:<sessionID>:<messageID>` requestId, full payload passthrough, 204/400/404/409/500 mapping) while without `messageID` keeps the legacy fork; `session/command` is private-first accept-only over the same carrier (`session-command-dispatch.ts` → `SessionPrompt.command` fork-detached accept, `fd-carrier.ts` `session/command` + `session/command` capability, extension `session-command.ts` + `serve-private-command-contract.ts` + `ServePrivatePeer.privateCommandWithHandle` + `connection-service.privateCommandWithHandle`, 3 s exact-cancel/epoch): same `prompt:<messageId>` tuple with `command`/`arguments`/`model` string payload and sync `command.not_found` terminal precheck, validated `succeeded`+`accepted` and validated terminal `failed` (`session.not_found`/`scope_mismatch`/`validation.failed`/`command.not_found`) close with zero SDK, validated retryable plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one same-tuple SDK `commandAsync` fallback through the same `SessionCommandDispatch` owner (FD plus `command_async` HTTP dual transport; legacy synchronous `session.command` stays compatible), never retried; `KiloProvider.handleSendCommand` converges on `commandSessionPrivateFirst` with no UI change; `command/list` inventory is private-first (`fd-carrier.ts` `command/list` + `command/list` capability invoking the same `Command.Service.list()` used by HTTP with same-directory scope via drain-control + `InstanceRef` lane, strict `command-list:<token>` `opId==idempotencyKey` + `requestId` + `context.directory` + empty payload, safe `{name, description?, source?, hints?}` projection preserving carrier order; extension `serve-private-command-list-contract.ts` + `ServePrivatePeer`/`connection-service.privateCommandListOutcomeWithHandle` + `kilo-provider/command-list-privatefirst.ts` + `kilo-provider/commands.ts loadCommands`, 3 s exact-cancel/epoch, validated `succeeded`+`accepted` returns with zero SDK, validated `failed` with `retryable === false` rejects terminally with zero SDK, retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one SDK `client.command.list` fallback through the existing retry wrapper with per-directory dedupe shared by concurrent callers).
- VS Code `suggestion.accept`/`suggestion.dismiss` is private-first over the same fd carrier (`kilocode/suggestion/suggestion-private.ts` -> globally unique pending lookup with no directory ownership filtering, exact `Suggestion.accept`/`dismiss` with delete-wins/event/waiter preserved and invalid index side effect then `suggestion.not_found`, `suggestion:<requestID>:<token>` identity with index/action echo, `suggestion/accept` + `suggestion/dismiss` capabilities, drain-control `suggestionAccept`/`suggestionDismiss` snapshot-first lane held to completion, no durable operation/revision/tombstone; extension `suggestion-privatefirst.ts` + `serve-private-suggestion-contract.ts` + `ServePrivatePeer` + single `privateSuggestionWithHandle` via `wrapEpochHandle`, 3 s exact-cancel, validated `terminal`/`terminal-failure` survives post-response drift with zero SDK and stale `suggestionResolved` on `not_found`, only unresolved drift falls back to exactly one same-tuple SDK). Suggestion `list` is private-first re-observation over the same carrier (`fd-carrier.ts` `suggestion/list` + `suggestion/list` capability invoking exact process-global `Suggestion.list()` under canonical directory + `acquireDrainControl` snapshot-first `InstanceRef` lane, strict `suggestion-list:<token>` `opId==idempotencyKey` + `requestId` + `context.directory` + empty payload with unknown-field reject, insertion-order global results with no ownership filtering exactly as HTTP `GET /suggestion`, redacted `validation.failed`/`scope_mismatch`/`internal` terminal non-retryable plus retryable `InstanceUnavailableDuringConfigRebuild`, no drain-control classification, no durable operation; extension `serve-private-suggestion-list-contract.ts` + `ServePrivatePeer.privateSuggestionListWithHandle` + `serve-private-suggestion-list-connection.ts` via `wrapEpochHandle` + `suggestion-privatefirst.ts` `listSuggestionsPrivateFirst`/`readSuggestionsForDir` (3 s exact-cancel, success including empty zero SDK, every validated failed fallback exactly one SDK `suggestion.list`, unknown only when the single SDK fallback fails); `fetchAndSendPendingSuggestions` reads per directory preserving order, first-wins dedupe, tracked-session filter, `suggestionRequest` posts with no directory maps/revisions/pruning). `dismissAll` stays server-internal.
- Skill removal is CLI-owned through one private-only mutation (`packages/opencode/src/kilocode/skill-remove-execute.ts`: exact registry match, absolute `SKILL.md`, builtin and URL-cache rejection, `containsPath` project/global scope, cold convergence, manifest-only unlink with siblings preserved) reached solely by the private `skill/remove` FD op (`skill-remove:<token>` tuple for `opId`/`idempotencyKey` + `requestId` + routing directory, 3 s exact-cancel/epoch, redacted terminal `skill.builtin`/`skill.url`/`skill.not_found` plus retryable fence, no session_operation row); there is no HTTP skill-remove route and no generated SDK method. Settings (`KiloProvider.removeSkillViaCli`) removal is private-only with zero SDK fallback: every outcome re-observes authoritative skills/commands and never inspects/deletes paths in the extension; ambiguous same-scope duplicates fail closed. There is no downloadable catalog product; skills load locally from canonical directories, `.claude`/`.agents`, `skills.paths`, and user-configured `skills.urls`. Skills listing is private-first over the same fd carrier (`fd-carrier.ts` `skill/list` + `skill/list` capability invoking the same `Skill.Service.all()` used by `GET /skill` under canonical directory + `acquireDrainControl` snapshot-first `InstanceRef` lane, strict `skill-list:<token>` `opId==idempotencyKey` + `requestId` + `context.directory` + empty payload with unknown-field reject, safe `{name, description?, location}` projection with `content`/file bytes never crossing, insertion order preserved, empty authoritative, fixed redacted `validation.failed`/`scope_mismatch`/`internal` plus retryable `InstanceUnavailableDuringConfigRebuild`, no durable operation; extension `serve-private-skill-list-contract.ts` + `ServePrivatePeer.privateSkillListOutcomeWithHandle` + `connection-service.privateSkillListOutcomeWithHandle` via `wrapSkillListOutcomeForOwner` + `kilo-provider/skill-list-privatefirst.ts` + `kilo-provider/skills.ts` `loadSkills` (3 s exact-cancel, success including empty zero SDK, every failed/unavailable/ambiguous/invalid/transport/closed/timeout takes exactly one same-directory SDK `client.app.skills` fallback with existing retry and safe projection before post/cache); `KiloProvider.fetchAndSendSkills` preserves refresh triggers/caching/retry/error/raw order/remove-by-location, and `skill/remove` paired skills+commands refresh is unchanged).
- Canonical session generation is production over `provider/httpExecute`: canonical-only records synthesize an in-memory `Provider.Model` (`CanonicalModel.synthesize`, conservative unknown cost/limits, authored variants only, no DB insertion); `SessionPrompt.getModel`/`LLM.run` resolve canonical from the active admitted snapshot (`CanonicalResolver` over `CanonicalProviderSnapshotRef`), `NotFound`/hybrid fall through legacy, conflict/model-missing fail closed (`InvalidRequest`), missing broker fails closed (`Transport` `Unavailable`); `CanonicalNative.stream` keeps CLI-owned structured messages/system/tools plus protocol parsing and uses `Auth.none` with a per-invocation `CanonicalRequestExecutor` over `ProviderHttpExecuteBroker`; `Layer.fresh` rebuilds `LLMClient` per request so `ManagedRuntime` memoization cannot reuse the ambient fetch executor; outer cancellation drops the exact peer call so one `$/cancelRequest` reaches the host; all three protocols (`openai/completions`, `openai/responses`, `anthropic/messages`) are covered. Shared session tool-call repair (`packages/opencode/src/session/llm/repair.ts`) is used by AI SDK (`experimental_repairToolCall` via `repairToolCall`), legacy native, and canonical native (via `repaired` before local dispatch): exact match preserves input, otherwise trim-lowercase match, unknown routes to runtime-only `invalid` with deterministic payload, `providerExecuted` untouched, event id/metadata preserved. Remaining gaps/deferred: prompt submission SDK fallbacks plus other generation-path calls, permission presets. The legacy unary `provider/execute` stepping seam has been removed; generation has only the streaming host seam.
- Disposed lifecycle full refresh is per-provider singleflight/trailing (KiloProvider lifecycleRefresh over kilo-provider/lifecycle-refresh-coordinator): concurrent/reentrant reloadAfterAuthChange calls share one in-flight gate, arrivals during a round only mark dirty with at least one strictly-serial trailing round and no debounce timer, so one global auth/config cold pass converges N same-cold server.instance.disposed plus one global.disposed into one in-flight plus one trailing round per provider. Each round keeps existing order/fail-soft (requirements.clear, fetchAndSendConfig first, then parallel providers/agents/skills/commands; providers-internal coalescer unchanged). Call sites/events unchanged (global.disposed unconditional, same-directory server.instance.disposed, cross-directory manual reload direct), immediate login/logout/org/provider-OAuth ack refreshes stay direct with no cross-provider sharing, and dispose closes the coordinator with no timers/tasks and no unhandled rejection. Instance reload is private-first over the same fd carrier (`kilocode/instance-reload-private.ts` `instance/reload` capability running the same `hasActiveSession -> 409` guard plus the unique `InstanceStore.reload` path as the HTTP route with lease sealing plus fence handshake plus exactly-one disposed event per success, strict `instance-reload:<token>` `opId==idempotencyKey` plus `requestId` plus canonical `directory`/`workspace?` plus empty payload, no durable op row; extension `serve-private-instance-reload-contract.ts` plus `ServePrivatePeer`/`connection-service` `privateInstanceReloadOutcomeWithHandle` plus `kilo-provider/instance-reload-privatefirst.ts` attempt with 3 s exact-cancel/settled-first epoch plus `kilo-provider/instance-reload.ts` private-first owner with valid success/terminal zero SDK and retryable fence plus unavailable/invalid/ambiguous/transport/closed/timeout exactly one same-directory SDK `client.instance.reload` fallback with no retry and ambiguous fallback at most two underlying reloads/two disposed events merged by the coordinator with no exactly-once/new dedup promise): both entries (`KiloProvider.handleReload` webview current session, `kilo-code.new.reload` Agent Manager resolver) go through the same private-first helper (typed `succeeded`/`conflict`/`failed`, 409 detected inside for the SDK fallback, shared warning/error copy); the private result or HTTP response is only an ack and `server.instance.disposed`/`global.disposed` plus the per-provider coordinator own final convergence. The provider keeps its existing success projection (clear commands cache plus cross-directory `reloadAfterAuthChange` because a cross-directory disposed event is filtered for that provider); the command adds no second refresh owner and converges via the same disposed events, so a cross-directory command reload does not synchronously refresh a provider whose directory differs.
- Onboarding session-existence probe is private-first boolean over the existing `experimental/session/list` v2 fd carrier (`packages/kilo-vscode/src/kilo-provider/session-existence-privatefirst.ts` `hasAnySession` for work-style init, strict same-directory `context {directory}` + `filter {limit:1, archived:true}`, one private attempt with 3 s exact-cancel/epoch via current `privateSessionListOutcomeWithHandle`; valid `succeeded`+`accepted` authoritative boolean `sessions.length>0` with zero SDK and zero `getClientAsync`, every other private outcome takes exactly one same-directory SDK `experimental.session.list({directory, limit:1, archived:true}, {throwOnError:true})` with current error propagation so transient failure posts `skipped` without persistence). The directory tuple intentionally corrects the prior omitted-directory global probe so private availability cannot change onboarding. No FD protocol/carrier/peer/connection change; general paginated session list stays SDK-authoritative with detached parity plus deferred observers intact.

- Journal writers hold a write-anchored external drift guard inside the worktree exclusive (`packages/opencode/src/tool/write-cas.ts` shared by `edit`/`write`/`apply_patch`): disk is re-read before prepare and before write/remove and must match the pre-read baseline on existence plus raw bytes (directories always mismatch, error carries the path only, never file content); drift fails closed with no applied fact and no disk touch, preferring pre-prepare refusal so no meaningless row is left, while `apply_patch` tracks batch-sequential expected state so its own applied prefix never misreads as drift and a conflicting hunk stops the batch with the prefix intact. This stays an optimistic double-check; a narrow check-to-write TOCTOU against non-cooperative writers remains.
- Formatter single-target path is an implemented intentional boundary (`packages/opencode/src/tool/format-target.ts` shared by `edit`/`write`/`apply_patch`; `apply_patch` move checks only the formatter target): after `format.file` the target must still be a regular readable file before sync/raw read/`journal.apply` (in-place and atomic rename allowed; missing, directory/device/FIFO/socket/unknown, dangling symlink, unreadable fail closed with prepared settled failed and coverage failed, disk left as-is with no speculative restore; symlink to regular stays allowed). Files outside the target never enter metadata/journal/planner/revert/CAS and are never scanned, cleaned, or reified. Non-zero formatter exit keeps log-then-read-final-target with exit-code handling unchanged.

## Unknowns that matter next

- FD carriers terminate in the same backend `AppLayer`; `session/create`, title-only `session/update`, `session/fork`, `session/delete`, `session/cancelQueued`, `session/prompt` (accept-only; generation still runs in the CLI runtime), and `session/command` (accept-only; generation still runs in the CLI runtime), plus checkpoint `session/revert` + `session/unrevert`, are now authoritative via `dispatch`; VS Code currently issues no share/archive mutations; remaining SDK-invoked mutations include allowEverything (private-first capable with no UI call-site invoking it yet), MCP toggle (connect/disconnect/authenticate/add are private-only, status read is private-first), auth/instance (OAuth `authorize`/`complete` still on its legacy SDK path; `instance.reload` is private-first, not a gap; `kilo/profile`, `agent/list`, `provider/catalog`, `provider/auth`, `config/ui-defaults`, `kilo/auth-status`, and `provider/models-discover` reads are private-first, not a gap; runtime convergence backend-owned; extension dead `global.dispose` surface removed), plus session prompt/command SDK fallbacks and other generation-path calls, with no transport removal; generation runtime/session ownership stays open and, with the peer `$/cancelRequest` transport carrying only the streaming reverse method `provider/httpExecute` (unary `provider/execute`, `ProviderExecuteBroker`, and `CanonicalProviderExecute` removed), prompt submission transport, permission presets, host-side provider/scope selection, and any `SessionProcessor`/private-runtime link beyond generation remain open; session generation itself is now wired (`LLM.run`/`SessionPrompt` via `CanonicalNative` over `provider/httpExecute`), with no unary legacy stepping seam and there is still no other CLI-side `RequestExecutor` interception beyond the streaming host seam; `CanonicalConfigService`-controlled GUI config/asset writes are private-fenced (not SDK-owned) while external watcher edits stay unfenced; private `session/abort` is production (terminal only after owner convergence, no invented terminal turn, no durable row); private `question/reply`/`question/reject` is production (pending-map authority, terminal only after delete+publish+settle, no durable row); standalone observation remains the ownership path for reads.
- Agent Manager session-list paging, bounded single-session detail, and paged plus full-read (`limit=0`) messages now consume the private `observation/list` + `observation/get` + `observation/messages` projections, and transcript export plus child-sync `handleSyncSession` follow the same full-read boundary; the remaining unresolved boundary is that session lifecycle beyond `session/create` + title-only `session/update` + `session/fork` + `session/delete` + `session/cancelQueued` + `session/abort` + `question/reply` + `question/reject` + `permission/save-always-rules` + `permission/reply` + `session/prompt` (accept-only) + `session/command` (accept-only) + `session/revert` + `session/unrevert` still depends on the SDK path, with no private authority beyond `session/create` + `session/update` (title-only) + `session/fork` + `session/delete` + `session/cancelQueued` + `session/abort` + `question/reply` + `question/reject` + `permission/save-always-rules` + `permission/reply` + `session/prompt` + `session/command` + `session/revert` + `session/unrevert` + list + detail + messages and no transport removal.
- Permission preset target in `direction.md` is not implemented; the existing complex editor/permission authority (CLI evaluator plus extension composition/projection) remains the reality.
- Snapshot is retained and entering a write-anchored redesign: declared file-tool metadata (`edit`/`write`/`apply_patch` via shared `packages/opencode/src/tool/filediff.ts`) is now rebuilt from the formatter-final disk truth (`format.file` + `EncodedIO.sync`), while permission `ask` intentionally keeps the pre-format expected diff as a decided contract (ask is human preview/audit of agent intent before any write/formatter, never an evaluator/CAS/journal input; completion metadata and journal hold formatter-final disk truth; pre-authorization formatter runs or temp copies to chase display consistency are forbidden without a new product decision); fail-closed correctness base plus per-session `SessionRevert` serialization is done (`Snapshot.restore`/`revert` typed `SnapshotRestoreError`/`SnapshotRevertError`/`SnapshotPathError`, worktree-bound validation, aggregated batch failure, rollback-best-effort marker protection, `KeyedMutex` per-session `revert`/`unrevert`/`cleanup` mutual exclusion, file rollback window guarded by worktree-keyed cross-process `Snapshot.exclusive`, HTTP 500/400/409 mapping with SDK regen), `track`/`patch`/`diff`/`diffFull` soft-fallback unchanged; journal writers (`edit`/`write`/`apply_patch` via shared `packages/opencode/src/tool/journal-window.ts`) now hold the same worktree-keyed `Snapshot.exclusive` from the first `SnapshotJournal.prepare` through write/format/sync to `apply`/`fail` (`edit` ask stays inside the per-file lock but outside the worktree window with per-file-outer/worktree-inner order, `apply_patch` holds one window for the whole batch, LSP/config validation stays outside, `journal.fail` completes before release), so same-worktree concurrent writes and `revert`/`unrevert` serialize while different worktrees stay parallel; `Session.Info.revert` schema unchanged (no SDK regen); complete coverage reverts/unreverts now execute bounded journal CAS, incomplete stays whole old Snapshot with no mixing, and every non-empty successful revert still saves a raw track backup as the future incomplete fallback; apply_patch move settle failure compensates blind bilateral fail (prepared→failed, failed idempotent, applied Conflict ignored, no extra get, one less DB read in the window).
- Snapshot v2 journal restore coverage planner has landed as a read-only adjudicator (`packages/opencode/src/snapshot/coverage-plan.ts`, boundary shared with `SessionRevert` via `packages/opencode/src/session/revert-boundary.ts`): verdict `complete` enters bounded journal CAS (`packages/opencode/src/snapshot/journal-cas.ts`, redo in applyOrder, undo in reverse, combined redo-old plus undo-new for chained reverts in one exclusive with raw track backup; first path gated only by absent marker, stale/failed/incomplete prev forces whole old Snapshot; empty first/combined chains finish without a snapshot), `incomplete` falls back to the old Snapshot path as a whole with no mixing; CAS prechecks without writes from actual disk state, executes per-step disk CAS with reverse CAS compensation that never overwrites unknown data, forward stays interruptible with uninterruptible CAS-guarded minimal compensation before exclusive release then still interrupts with marker unchanged, never mutates journal rows, and never calls raw restore rollback on CAS failure; bounded batch read (`SnapshotJournal.load`) eliminates the per-step journal DB roundtrip, but end-to-end still has no performance promise.

- Temporary custom-only provider boundary is enforced in the VS Code product surface: webview shows only custom configured providers plus the custom add entry (built-in configured/add/select/API-key/ChatGPT/OAuth/Anaconda rows hidden with a static notice); `ProviderConnectDialog` renders an unavailable notice while its dormant OAuth/API implementation stays in source and never opens the Anaconda backend; `ProfileView` shows only the unavailable notice with no sign-in action or disabled Login button and no login/organization/logout/refresh triggers; speech-to-text shows no sign-in action; host routing rejects webview-direct `login`/`logout`/`setOrganization`/`refreshProfile` and `cancelLogin` without incrementing login attempts, OAuth `authorize`/`complete`, non-custom `connect`/`disconnect`, non-custom/reserved `save`/`delete` (currently configured non-custom entries are never overwritten or deleted via custom flows and legacy keys fail closed; unconfigured ordinary IDs may still be created as custom with future collisions handled if they arise; reserved internal IDs `kilo`/`anaconda-desktop`/`_custom` stay rejected as internal routing reservations, not a future provider denylist), and `anacondaDesktopStatus`/`Open`/`Sync`/`cancelAnacondaDesktopRequest` with existing `providerActionError`/`deviceAuthFailed`/`deviceAuthCancelled`/`error`/`profileData`/`anacondaDesktopActionError` shapes and no secrets, while canonical custom `save`/`delete`/`disconnect` keep existing stamp/credential guards and custom model discovery fails closed for configured non-custom entries (no stored-credential use, no custom models) while configured custom, fresh typed keys, and unconfigured ordinary IDs keep existing discovery semantics. There is no webview rendering harness; UI surface tests are source/helper-logic level and real rendered interaction is unverified (host and payload tests still pass). Backend OAuth/Anaconda routes/plugins/SDK schema stay dormant and unchanged; CLI/TUI login paths outside the VS Code orchestrator are out of scope and unchanged.
- Open decision: when and how to restore built-in providers and OAuth in the VS Code surface; no deletion of dormant implementation is planned as part of this boundary.

No backlog lives here. Anything that does not change the next judgment is
omitted.

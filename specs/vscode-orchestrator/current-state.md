# VS Code Orchestrator — Current State

Reality that changes the next judgment. Code and observed behavior outrank
this note; when they disagree, the code is right.

## What holds today

- Agent Manager is the only chat surface in the product. The older single-chat
  sidebar and editor-tab chat are gone from the tree.
- The extension still runs as a client of one shared editor-owned backend
  process reached over HTTP with server-sent events through the generated SDK.
  That path is the current authority for sessions, not a future contract.
- Private file-descriptor carriers into the same backend exist as incremental
  work. They provide parity and diagnostics for most surfaces; Agent Manager
  local `session/create` is now private-first with authoritative `SessionCreateDispatch.dispatch` commit (same `create:<token>` durable tuple for `opId`/`idempotencyKey` + `requestId` + `directory`/`parentSessionId:null`, 3 s timeout/epoch, valid `succeeded` authoritative without SDK else exactly one SDK `POST /session` fallback with same tuple; `sandboxInheritanceToken` bypasses private with exactly one SDK `create`).
- Durable session operations with an atomic commit plus a bounded derived
  changefeed exist as an incremental foundation. `session/create` private path now commits `SessionTable`/`session_operation`/`changefeed`/`Event` atomically via `fd-carrier.ts` → `SessionCreateDispatch.dispatch` (not replay-only `dispatchPrivate`); idempotent replay returns same session without duplicate. Title-only `session/update` (rename) is private-first the same way via `fd-carrier.ts` → `SessionUpdateDispatch.dispatch` with the same durable tuple (`opId`/`idempotencyKey` + `requestId` + `context`) across private and the exactly-one SDK `session.update` fallback; a valid private `succeeded`+`accepted` session/title result returns with zero SDK mutation, otherwise exactly one SDK fallback with the identical tuple. `session/fork` is private-first the same way via `fd-carrier.ts` → `SessionForkDispatch.dispatch` (authoritative commit with `data.session` wire wrap) and `forkSessionPrivateFirst` (same `fork:<sessionId>:<token>` durable tuple for `opId`/`idempotencyKey` + `requestId` + `context`, 3 s timeout/epoch; valid `succeeded`+`accepted` returns with zero SDK mutation, validated `failed` with `failure.retryable === false` such as `session.not_found`/`conflict` closes terminally with zero SDK, validated `failed` with `retryable === true` plus unavailable/invalid/ambiguous/transport/closed/timeout takes exactly one SDK `session.fork` fallback with the identical tuple, never retried). `session/delete` is private-first the same way via `fd-carrier.ts` → `SessionDeleteDispatch.dispatch` (authoritative full-family delete) and `deleteSessionPrivateFirst` (same `delete:<sessionId>:<token>` durable tuple for `opId`/`idempotencyKey` + `requestId` + `context {directory, sessionId, parentSessionId:null}`, 3 s timeout/epoch; valid `succeeded`+`accepted` returns with zero legacy `DELETE /session/:sessionID` mutation, validated terminal `failed` with `retryable === false` closes with zero SDK, known-safe unavailable plus validated `retryable === true` takes exactly one durable `DELETE /session/:sessionID` fallback with the identical tuple, unknown outcome runs one private reconciliation with the same tuple then closes terminally without destructive SDK fallback). The `dispatchPrivate` replay-only paths remain for diagnostics but are not used by `fd-carrier.ts` for `session/create`, `session/update`, `session/fork`, or `session/delete`.
- `session/cancelQueued` is private-first the same way via `fd-carrier.ts` → `CancelQueuedDispatch.dispatch` and `KiloProvider.handleCancelQueued` (same `cancelQueued:<session>:<message>` `opId` + `legacy:<session>:<message>` `idempotencyKey` + `requestId` + `directory`/`session`/`message` tuple, 3 s exact-cancel; valid `succeeded`+`accepted` and validated terminal `failed` with `retryable === false` close with zero SDK, validated `failed` with `retryable === true` (`InstanceUnavailableDuringConfigRebuild`) takes exactly one SDK fallback with the same identity, unavailable/invalid/ambiguous/transport/timeout/closed surface explicit failure with no second heterogeneous cancel).
- VS Code Settings writes are canonical-only and file-authoritative:
  `KiloProvider` accepts only stamped `canonical: true` `updateConfig`
  messages through `CanonicalConfigService.writeConfigScopes`; non-canonical,
  missing-canonical, and missing/stale-stamp messages fail fast with a
  structured `configUpdateFailed`. The legacy `client.config.transaction`
  caller and its pending/reconcile/retry coordination are removed from the
  extension (the CLI transaction endpoint itself is unchanged). Canonical
  reads publish the service snapshot; providers without canonical authority
  return explicit empty/unsupported payloads instead of racing the SDK.
- Provider API-key/custom writes are canonical-only:
  `connectProvider`/`disconnectProvider`/`saveCustomProvider`/`deleteCustomProvider`
  go through `CanonicalConfigService` plus `SecretStorage`; non-canonical
  payloads fail fast with a structured `providerActionError` and never fall
  back to the SDK bridge. The legacy SDK mutation callers are removed.
  OAuth `authorize`/`complete` keeps its legacy path with canonical
  `unsupported`; credential reads and custom model discovery keep their
  legacy read paths.
- Authored canonical custom-provider protocol is converged: `CanonicalProviderPayload`
  persists only `{name, endpoint, protocol, models}` plus the extension-host
  credential reference, with protocol exactly `openai/completions`,
  `openai/responses`, or `anthropic/messages`; legacy protocol tokens are
  rejected with no mapping or migration, the webview serializer and canonical
  edit init use the protocol state directly, and headers are not part of the
  authored contract. A standalone extension-host execution seam
  (`packages/kilo-vscode/src/canonical-provider/canonical-executor.ts`) now
  has a bounded unary reverse handler `provider/execute` over the existing
  `ServePrivatePeer` (`packages/kilo-vscode/src/services/cli-backend/serve-private-provider-execute.ts` → `canonical-executor.ts` via single-owner `CanonicalConfigService` `resolveSecret`, no duplicate `SecretStorage`): `KiloConnectionService` wires the activation-owned `CanonicalConfigService` into `ServePrivatePeer` and advertises `provider/execute` only when execution deps are installed (fail-closed otherwise); incoming `provider/execute` strictly validates `{providerId, modelId, record, prompt}` (record mandatory, `__proto__`/`constructor` rejected, `InvalidParams` on malformed) and executes the exact authored `record` through the shared native LLM routes by protocol, with `RequestContext.signal` aborting the `Effect`/`RequestExecutor` network operation on `$/cancelRequest`/peer-close/dispose, and preserves the structured `CanonicalFailureCode` (`invalid-record`/`invalid-endpoint`/`unknown-protocol`/`unknown-model`/`missing-credential-ref`/`invalid-credential-ref` → `InvalidParams` with `data.code`, `missing-secret`/`provider` → `InternalError` with `data.code`) without leaking secrets (deep redaction plus scrubbed `provider` message; `data` carries only the code). The focused localhost fixtures (`canonical-provider-host-execution.test.ts` plus `serve-private-provider-execute.test.ts` covering advertisement, routing/secret resolution, strict invalid-param rejection, canonical failure classification/redaction, and cancellation via `$/cancelRequest`/peer-close/dispose) prove the handler boundary; CLI generation/session prompt wiring, streaming protocol, and host-side provider/scope selection remain open and no CLI `POST /session/:id/prompt` or `SessionProcessor` private-runtime link exists yet. Plaintext secrets never leave the extension host.
- ConfigV1-only canonical provider preservation is inert and authenticated: `ConfigProviderV1.Info` preserves `endpoint`/`protocol`/`credential` via shared `ProviderCredentialRef` at `@opencode-ai/core/kilocode/credential-ref.ts` (`PROVIDER_CREDENTIAL_REF_PATTERN` OpenAPI pattern, `parseOwnedCredentialRef` parser validation, fixed redacted `Invalid credential reference` without token in `SchemaError`/`HttpApiSchemaError`, productive `Schema.toArbitrary` via `toArbitraryConstraint`) and `@opencode-ai/core/kilocode/provider-execute.ts` `PROVIDER_EXECUTE_PROTOCOLS`/`isProviderExecuteProtocol`/`PROVIDER_EXECUTE_PATH_BY_PROTOCOL` through `ConfigV1` parse/load and isolated global/project merge, plus `GET`/`PATCH /config` and `GET`/`PATCH /global/config` authenticated via `Basic` (`KILO_SERVER_PASSWORD`) — plaintext/malformed `credential` is rejected before persistence and neither HTTP 400 (`Invalid credential reference`) nor server logs contain the submitted distinctive token; both authenticated project and global flows assert `endpoint`/`protocol`/`credential` in `PATCH` response, exact persisted `kilo.jsonc` file, and subsequent `GET`, with auth rejection (401), invalid-ref no-persist, project-over-global precedence, and cleanup retained. Root SDK `KiloClient` (`packages/sdk/js/src/client.ts`) handwritten `config.get`/`update` refinement over the frozen legacy runtime client (project-only `GET`/`PATCH /config` via `V2Config`) vs generated `@kilocode/sdk/v2` current `ProviderConfig` and `global.config` methods; `isCanonicalOnlyProviderV1` (`@opencode-ai/core/kilocode/canonical-provider.ts`) excludes canonical-only providers from legacy opencode `Provider` DB while hybrids with any legacy provider/model key (`api`, `npm`, `env`, `options`, `id`, `whitelist`, `blacklist`, `prompt`, `isFree`, `ai_sdk_provider`, `release_date`, `attachment`, `temperature`, `tool_call`, `interleaved`, `cost`, `limit`, `experimental`, `status`, `provider`, `options`, `headers`, `variants` including `enable_thinking`/`reasoningEffort`/`effort`/`thinking`/`reasoning_split`/`chat_template_args`) remain operational; unknown nested keys follow ConfigV1 excess-property policy. Non-goals remain explicit: no ConfigV2 migration, no V2 catalog/generator expansion, no CLI SecretStorage resolution, no CLI provider selection, no streaming, no session generation.
- Canonical file-backed custom-agent create/edit/import/remove is production:
  stamped `canonical: true` `mutateAgent`/`removeAgent` through
  `CanonicalConfigService.writeAsset`/`deleteAsset` CAS (project-absent on
  create/import, scope-plus-asset-hash on edit/remove); non-canonical agent
  messages fail fast with exactly one structured `agentMutationError` and never
  reach the SDK (the legacy `client.kilocode.removeAgent` write is removed);
  failures/exceptions post exactly one structured `agentMutationError` with a
  freshly re-read stamp. The webview serializes per-agent edits through one
  coordinator (debounced coalescing, full-snapshot reconciliation, inflight
  error settling the whole queued batch with no replay); unmount/agent-switch
  flush still reports to the provider-level diagnostic without touching dead
  view state. Agent markdown is CLI-loader-compatible (unknown keys preserved
  for rest/options merging; credential-bearing keys rejected in validation,
  import, export, and host writes; prototype-polluting keys never merged).
  Native/system agents are read-only; no global-scope picker and no
  tool-disable/permission-preset implementation exist.
- A private observation service exists in the extension, but the remaining
  target ownership and cutover are unfinished. Effective runtime
  configuration still comes from the CLI loader with its multi-source merge
  and convergence pass.
- Production permission decisions come only from
  `packages/opencode/src/permission/evaluator.ts`. Across
  global/project/agent/session-restriction layers, deny wins first,
  ask/ceiling next, and allow holds only when every applicable layer allows.
  The old last-match helper is not on this path.
- The extension restrictive `{global,project}` product from
  `packages/kilo-vscode/src/config/compose.ts` feeds only extension
  materialization/webview projection today, not CLI evaluator input; the CLI
  still assembles its layers from canonical files.
- The private runtime and storage cutover is unfinished. The legacy store
  still serves sessions until that landing.
- Agent Manager session-list re-observation decision is now owned by the extension standalone `PrivateObservationService` (snapshot/read/ack with observation coordinator, generation and singleflight) at `requestState` plus panel `visible` and active session-switch boundaries; the generated SDK remains the sole data authority, changefeed entries are decision/provenance only, and the old unconditional `refreshSessions()` is removed. Worker-restart/peer-close now drives the provider from the one bounded `PrivateObservationLifecycleTriggers.onPeerClosed()` reconnect+`read(persistedCursor)` — read failure (result absent, `readError`, `requestedCursor`/`readResult` absent/invalid, validated via `decideFromReadResult` without second read) -> precomputed singleflight fallback with `{shouldRefresh:true}`, exactly one SDK `refreshSessions()`, no second private read, no ack (singleflight-shared with `requestState`/`visible`); temporal staleness (current persisted `undefined` or not exactly equal to `requestedCursor`, whether < or >) -> normal fresh `handleObservationRefresh()` decision which may read again (the only second read); valid fresh -> no second read, re-checked before ack to avoid stale ack/regression. Window and config lifecycle reads remain observation-only. Extension `activate` constructs `PrivateObservationService`/triggers before the provider but defers `initialize()` until after `AgentManagerProvider` and `wirePeerCloseObservation` hook (fire-and-forget fail-closed, no TDZ), closing the activation race. Shared SDK/SSE transport reconnect (`KiloConnectionService` `onStateChange` `connected`) is distinct from private `peer:closed`; it re-enters the same private decision only after the provider has observed a prior `connected` baseline (`seenConnected` initialized from `getConnectionState()==="connected"` at construction), first `connected` remains owned by `requestState` hydration, and visibility is rechecked after `stateReady` with generation/sessions identity.
- Agent Manager session-list paging is private-first via the standalone no-lease canonical-DB projection (`observation/list` `PrivateObservationService` → `createSessionListDeps` over `SessionTable` with `directory` + `archived` + `time_updated DESC, id DESC`, `limit` 1..500, fields `id/title/parentID/directory/projectID/createdAt/updatedAt`, `nextCursor` iff truncated, `privateSessionReader` (`isEnabled`/`isStarted`/`list`/`get`/`messages`) injected non-owningly via `VscodeHost` → `KiloProvider` `sessionRefreshContext.listSessions` with `archived:false` and `normalizeSessionListNextCursor` reuse, bounded exactly-one SDK `experimental.session.list` fallback on gate-off/not-started/worker error/protocol invalidity preserving detached parity diagnostics). Paged list + bounded detail + paged and full messages are now private-first; lifecycle stays extension-owned; no sole private runtime authority beyond these surfaces.
- Standalone no-lease `observation/get` detail projection (`observation/get` `PrivateObservationService` → `createSessionGetDeps` over `SessionTable` by `sessionId` with canonical directory scoping, statuses `found`/`not_found`/`scope_mismatch` as versioned result union, fields `id/title/parentID/directory/projectID/createdAt/updatedAt` plus optional `agent`/`summary`/`revert`) is private-first for narrow `SessionDetail` via `PrivateSessionReader.get` (`validatePrivateGetResult` defensively revalidates `found` at provider boundary, `sdkSessionToDetail`/`observationSessionToDetail`/`detailToWebview` pure mappers preserve `parentID`/`revert`/`summary` null and ISO semantics plus empty `agent`/`summary`/`revert`). `found` is authoritative with no SDK and no parity; `not_found`/`scope_mismatch` are authoritative terminals with no SDK and no raw directory/id log; malformed/`InternalError`/`MethodNotFound`/transport/host-closed are bounded warnings then exactly one SDK `client.session.get` with `AbortSignal` preserved for strict `replace` and detached parity. `refreshSessionDetails`, strict `focus`/`replace` preludes, `handleSyncSession` (metadata helper plus child-sync full-history `fetchMessagePage limit:0` via `Promise.all`), and `exportTranscript` (narrowed formatter) all use the central `getSessionDetail`; creation/rename/revert/unrevert/SSE writers map SDK `Session` → `SessionDetail`. `currentSession`/`setCurrentSession` narrowed to `SessionDetail`; dead `Host.getSessionInfo` full-`Session` promise removed. Lifecycle remains extension-owned; `KiloProvider`/`VscodeHost` never init/reconnect/dispose the private service.
- Bounded raw `observation/messages` page (`observation/messages` → `createSessionMessagesDeps` over `MessageTable`/`PartTable` with continuation `(time_created < t) OR (= and id < id)`, `time_created DESC, id DESC`, `limit` 1..100, `nextCursor` iff truncated, chronological ASC, storage-stripped via shared `message-read`) is private-first for paged and full-history reads via `PrivateSessionReader.messages` (`validatePrivateMessagesResult` defensively revalidates `found`/order/cursor/session binding plus per-part `sessionID`/`messageID` ownership at provider boundary, `found`/`not_found`/`scope_mismatch` authoritative with no SDK and private-authoritative paged/full results run no detached parity, malformed/`InternalError`/`MethodNotFound`/transport/closed bounded warnings then per bounded paged read at most one private attempt and one logical SDK `session.messages` fallback with the existing transient retry preserved and original `AbortSignal`, full `limit=0` iterates private pages at wire limit 100 until exhaustion preserving ASC/projected shape with `input.signal.throwIfAborted()` before and after each awaited private page and before the SDK fallback so no further private pages or SDK read run after abort, cycle/bound overflow falling back to exactly one SDK `limit:0` with no retry and original `AbortSignal`, SDK paths retain detached parity bounded once per outer `fetchMessagePage` operation so multi-page assistant-boundary fill may issue one logical SDK fallback per page while full fallback issues exactly one SDK full-read and `exportTranscript` plus child-sync `handleSyncSession` pass the non-owning reader); `messagesLoaded` then scheduler capture `commit` replay (monotonic occurrence token in `since`, never wall-clock; capture flushes pre-token queued state, same-session latest-wins with `commit` as the authoritative validation consuming the post-token live queue, real full corrective replay with snapshot-present deltas dropped and snapshot-absent delta-derived emitting only the unflushed pending queue while already flushed deltas stay in the projection, latest attempt retained across expiry/overflow with latest-only child terminal via `pruneDeletedSession`, callback call-order not delivery acknowledgement, bounded capture of max 200 keys / 512KiB / 30s scheduler-owned expiry with fail-closed no-snapshot on invalidation, real fulls replaying correctively, delta-derived replay only for snapshot-absent keys, strict webview snapshot for same-ID parts) plus stale-generation checks unchanged; lifecycle stays extension-owned and `KiloProvider`/`VscodeHost` never init/reconnect/dispose the private service.
- Runtime prerequisite exists as internal contract only: single-owner per-session `Runner` epoch is the sole owner of the active-epoch busy transition (one `busy` per epoch, ordering `TurnOpen` -> `busy` -> body -> `TurnClose` -> `idle`, no per-runLoop-iteration busy; persistent runner with creation-only gate) carries optional `generationID` on `session.turn.open`/`session.turn.close`, and internal `CancelTreeResult` is exactly { targetSessionIDs, generations, queue, intake, planFollowup } with no background collection (background cancellation stays a side effect); private `session/abort` is production over the fd carrier (`session-abort.ts` -> `KiloSessionPrompt.cancelTree` over `SessionRunState`, terminal only after targeted epoch/finalizer convergence, affected generations from the owner never the request token, `session.not_found`/`scope_mismatch` as terminal `terminal-failure` with zero SDK, validated terminal survives post-response drift, only unresolved drift falls back to exactly one legacy SDK `POST /session/:id/abort` with the identical `abort:<sessionId>:<token>` tuple, single-directory scope, no fabricated `idle`/`sessionTurnClosed`, HTTP/SDK boolean wire unchanged, no durable operation row).
- VS Code `question.reply`/`question.reject` is private-first over the same fd carrier (`kilocode/question/question-private.ts` -> `Question.Service` pending map as authority, `pending.delete` + event publish + Deferred settle before `terminal`, unknown/double as `question.not_found` `terminal-failure`, `op`/`context`/`opId` binding as `scope_mismatch`, no `sessionID` in request with carrier-backfilled terminal `sessionID`, reply payload reuses `Question.Reply`, `question/reply` + `question/reject` capabilities, drain-control `questionReply`/`questionReject` snapshot-first lane held to completion, no durable operation/revision/tombstone; extension `question-privatefirst.ts` + `serve-private-question-contract.ts` + `ServePrivatePeer` + single `privateQuestionWithHandle` via `wrapEpochHandle`, 3 s exact-cancel, validated `terminal`/`terminal-failure` survives post-response drift with zero SDK and existing stale/recover UI preserved, only unresolved drift falls back to exactly one same-tuple SDK).
- `CanonicalConfigService`-controlled GUI config/asset writes use the private `config/convergence/acquire` + `config/convergence/resolve` fence: acquire blocks the write on failure, the runtime re-reads disk and converges noop/hot/cold, resolve loss stays persisted with a `pending` diagnostic and backend auto-resolve from disk. External watcher edits and `MarketplaceInstaller` direct writes stay unfenced gaps.
- Dual `JsonRpcPeer` (`packages/opencode/src/private-worker/peer.ts` ↔ `packages/kilo-vscode/src/private-worker/peer.ts`) now carries wire-level best-effort `$/cancelRequest` transport plus one unary reverse method `provider/execute` (`provider/execute` request carries the existing `CanonicalExecuteInput` semantics `{providerId, modelId, record, prompt}` with mandatory `record`; extension host resolves the exact authored secret ref via the single `CanonicalConfigService` owner, `onRequest` receives exact `id` plus `AbortSignal`, active incoming ids are tracked with duplicate-active-id fail-closed, peer close/EOF/writer/child failure aborts the signal, and `ServePrivatePeer` preserves structured `CanonicalFailureCode` in JSON-RPC `error.data` without leaking secrets while mapping malformed params to `InvalidParams`). `tryCancelPending` stays local-only; `cancel(id)` only guarantees the signal plus local pending ownership (handler must consume the signal, no forced termination). No `SessionProcessor`/private-runtime/provider-stream wiring, no streaming protocol, and no host-side provider/scope selection yet.
- Process-owned private peer registry exists in default CLI `AppRuntime` as discoverability + typed unary broker foundation (`private-peer-registry.ts` + `ProviderExecuteBroker` via `AppLayer`, no peer/stream ownership; fd carrier stays sole resource owner): single-owner exact install/release (duplicate live install fails closed, closed holder swept, request only on open initialized peer), 5 s bounded `awaitCarrierReady` (failure/timeout disposes started carrier and serve continues carrier-less HTTP; only `ready` publishes the carrier, port `stdout` after the wait), close/dispose plus late install completion reconcile through the lifecycle handoff barrier drained before shutdown with no registry epoch. A CLI-side typed unary broker `ProviderExecuteBroker` (`packages/opencode/src/kilocode/server/provider-execute-broker.ts` via `AppLayer`, caller supplies `{providerId, modelId, record, prompt}` with wire-envelope-only validation keeping `record` opaque except plain-object/prototype checks, success validation via shared `ProviderExecuteWire` plus `@opencode-ai/llm` `LLMEvent`, typed `ProviderExecuteUnavailable`/`Unsupported`/`Failure`/`ProtocolError`, canonical failure codes including `aborted` decoded with sanitized message, fiber-interruption drops the exact `PrivatePeer` call so `$/cancelRequest` reaches the host, no detached fibers, no Promise facade, no second peer owner, single canonical service) now calls the reverse `provider/execute` over the process-owned registry; the shared cross-process contract (`PROVIDER_EXECUTE_METHOD`, failure codes, request/result shapes/validators) lives in `@opencode-ai/core/kilocode/provider-execute.ts` and is consumed by both `ServePrivatePeer`/`canonical-executor` and the broker (full `CanonicalProviderPayload` AST stays host-side). No session prompt/command, generation lifecycle, provider record discovery, streaming, or built-in timeout wiring exists.
- Internal scope-aware canonical provider provenance snapshot plus typed resolver exists as a bounded internal state (`packages/core/src/kilocode/canonical-record.ts` shared pure closed-record/model validation + `packages/opencode/src/kilocode/provider/canonical-provenance.ts` `{id,scope:"global"|"project",source,record}` + conflicts `{duplicate,missing/malformed credential,scope/id mismatch,invalid endpoint/protocol/models}` + `packages/opencode/src/kilocode/provider/canonical-resolver.ts` typed `resolve(providerId,modelId)` returning exact closed record with opaque `credentialRef` and `ProviderExecuteWire.validateRequest`-compatible shape; `Config.State` stores `canonical` alongside `config` from the single `Config.loadInstanceState` pass (raw global/project canonical-only entries before deep merge, validated via shared/host-compatible logic, scope+ID must match file scope+map key, duplicate cross-scope omits that ID with typed conflict, no Frankenstein merge, per-provider omission with warnings not crash); `Config.Service` exposes internal `getCanonicalProvenance()`/`getCanonicalProviders()`/`getWithCanonical()` and `CanonicalProviderSnapshotRef`; `withConfigSnapshot` atomically pins `Config.Info` + provenance together (single `InstanceState.use` read, no two-step refresh race, `Config.get`/`getCanonicalProvenance` prefer the pinned refs); `Config.get`/HTTP `/config` never enumerates/serializes provenance and effective merge/precedence unchanged for non-execution consumers; admitted generation snapshots pin both together, subsequent invalidation/edits affect only subsequent snapshots; resolver never executes/selects on host and surfaces typed `notFound`/`conflict`/`modelNotFound` without leaking plaintext-like secret values; external edits preserve existing lifecycle (no watchers, global refresh via stamp, project external edit explicit, convergence writes invalidate/rebuild with old snapshot retained). No session generation/streaming/provider catalog/SecretStorage wiring in CLI, no `SessionProcessor`/private-runtime link, no streaming protocol.
- CLI canonical provider execution service exists as bounded `CanonicalResolver`+`ProviderExecuteBroker` composition (`packages/opencode/src/kilocode/provider/canonical-provider-execute.ts` `CanonicalProviderExecute.Service`/`layer`/`defaultLayer` via `AppLayer` `buildAppLayer`): captures canonical `Config.Service` and `ProviderExecuteBroker.Service` at layer construction, exposes `execute({providerId,modelId,prompt}) → Effect<Success, CanonicalResolveError|BrokerError, never>` with no environment, still reads active snapshot `FiberRef` per call via `CanonicalResolver.resolve` → `CanonicalProviderSnapshotRef` (so `withConfigSnapshot`/`withGenerationAdmission` pinning is honored), resolver failure never calls broker, broker receives exact resolved `record` plus prompt, preserves sanitized `CanonicalFailureCode`+message and typed `aborted`/`unavailable`/`unsupported`/`protocol` categories, preserves interruption/cancellation without mapping, no credential values in errors/logs, single canonical service registered/AppRuntime-layer wired (`providerExecute` → `canonicalProviderExecute` via `Layer.provideMerge`); proven by `packages/opencode/test/kilocode/canonical-provider-execute.test.ts` (AppRuntime graph, exact-record success, no-broker on resolver failure, broker error propagation, snapshot coherence over `withConfigSnapshot`/`withGenerationAdmission`, interruption, environment-free execution). No session generation/`SessionProcessor`/prompt or streaming wiring exists yet.

## Unknowns that matter next

- FD carriers terminate in the same backend `AppLayer`; `session/create`, title-only `session/update`, `session/fork`, `session/delete`, and `session/cancelQueued` are now authoritative via `dispatch`; VS Code currently issues no share/archive mutations; remaining SDK-owned mutations are checkpoint revert/unrevert, permission, MCP, auth/instance, plus session prompt/command and other generation-path calls, with no transport removal; generation runtime/session ownership stays open and, despite the peer `$/cancelRequest` transport now carrying one unary reverse method `provider/execute` with strict validation, structured canonical failure in `error.data`, and `RequestContext.signal` abort of the host `Effect`/`RequestExecutor` operation (advertised only when deps installed via single-owner `CanonicalConfigService`, tested with real executor over localhost fixtures), plus the process-owned registry now exposing a typed CLI broker `ProviderExecuteBroker` (wire-envelope-only record checks, success `LLMEvent` validation, typed unavailable/unsupported/failure/aborted/protocol errors, fiber-interruption `drop` → `$/cancelRequest`, shared `ProviderExecuteWire` at `@opencode-ai/core/kilocode/provider-execute` with full config schema staying host-side, no generation/streaming wiring), plus a bounded CLI `CanonicalProviderExecute` service composing that resolver+broker (`packages/opencode/src/kilocode/provider/canonical-provider-execute.ts` via `AppLayer`: canonical `Config`/`Broker` captured at layer construction, environment-free `Effect`, per-call active snapshot `FiberRef` via `CanonicalResolver`, single canonical service, no `SessionProcessor`/prompt wiring), the CLI generation/session prompt wiring, streaming protocol, host-side provider/scope selection, and any `SessionProcessor`/private-runtime link remain unfinished; there is still no `SessionProcessor`/`LLM` wiring of `CanonicalProviderExecute` into `POST /session/:id/prompt` and no CLI-side `RequestExecutor` HTTP interception beyond the host seam; `CanonicalConfigService`-controlled GUI config/asset writes are private-fenced (not SDK-owned) while external watcher edits and `MarketplaceInstaller` direct writes stay unfenced; private `session/abort` is production (terminal only after owner convergence, no invented terminal turn, no durable row); private `question/reply`/`question/reject` is production (pending-map authority, terminal only after delete+publish+settle, no durable row); standalone observation remains the ownership path for reads.
- Agent Manager session-list paging, bounded single-session detail, and paged plus full-read (`limit=0`) messages now consume the private `observation/list` + `observation/get` + `observation/messages` projections, and transcript export plus child-sync `handleSyncSession` follow the same full-read boundary; the remaining unresolved boundary is that session lifecycle beyond `session/create` + title-only `session/update` + `session/fork` + `session/delete` + `session/cancelQueued` + `session/abort` + `question/reply` + `question/reject` still depends on the SDK path, with no private authority beyond `session/create` + `session/update` (title-only) + `session/fork` + `session/delete` + `session/cancelQueued` + `session/abort` + `question/reply` + `question/reject` + list + detail + messages and no transport removal.
- Permission preset target in `direction.md` is not implemented; the existing complex editor/permission authority (CLI evaluator plus extension composition/projection) remains the reality.

No backlog lives here. Anything that does not change the next judgment is
omitted.

# P4.2 R11-R14 Failure/Outcome/Recovery Contract Evidence — Evidence-Only (Bounded Wiring Update)

> **Status: EVIDENCE-ONLY — Pure-module contract proof with bounded provider lifecycle wiring and bounded local-helper envelope normalization proof.**
> This artifact records line-referenced §7.2 contract coverage via additive pure modules and focused unit tests against historical baseline `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02` (prepared 2026-08-24). Prior reconciliation at `287539d58b5ad7abcfd569c15d74dce871a8d0bf` (2026-08-29); current committed base is `f4e0ed79f04c6114698e29c35ee556f68f5b4222` (2026-08-29); pending worktree updates for bounded panel-envelope normalization are validated in this worktree and not contained in `f4e0ed79` itself. **Current focused verification 2026-09-02:** `operation-r11` 15 pass / 176 expects (9 kinds), `operation-p42` 8/111, `failure` 55/473 (both packages), `recovery` 32/202 (PASS after --timeout 15000 retry from hook timeout flake), `disposition` 25/141 — aggregate 135/1103 (combined failure+recovery+disposition 112/816); historical 7-kind/48-count (105/665 combined, 127/902 aggregate) and 14/126 R11 remain historical records, not rewritten. **Bounded production wiring exists only** for provider-operation lifecycle at `packages/opencode/src/session/processor.ts:1275-1321,1411` (`SessionOperation.providerId` + `SessionOperation.put` in-flight/terminal lifecycle); panel-envelope normalization is a **bounded local-helper proof** at `packages/opencode/src/private-worker/failure.ts:443 normalizeRecord` / `458 buildPanelEnvelope` with parity in kilo-vscode (scrub + caps 500/1000/2000, idempotent; no production caller/transport, no live wiring). It does **not** claim private-worker transport, recovery-accounting retry integration (existing provider retry at `packages/opencode/src/session/processor.ts:1413-1427` remains outside recovery-budget proof), worker crash hooks, or five-boundary convergence; P4 remains Active and P4/P4.2/P4.3/P4.4/P4.5 completion is not claimed. Gate `migration-tracker.md` §4 Active Gates (P4 Failure/Outcome/Recovery gate) remains unchecked.

## Preparation Metadata

| Field | Value |
|---|---|
| Title | P4.2 R11-R14 Failure/Outcome/Recovery Contract Evidence — Evidence-Only |
| Date prepared | 2026-08-24 |
| Baseline commit (HEAD at preparation, historical) | `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02` (2026-08-24) |
| Prior reconciliation commit | `287539d58b5ad7abcfd569c15d74dce871a8d0bf` (2026-08-29; not current HEAD) |
| Current committed base (HEAD) | `f4e0ed79f04c6114698e29c35ee556f68f5b4222` (2026-08-29) |
| Branch | `jorkey/refactor/vscode-agent-orchestrator` |
| Worktree state (historical baseline) | Clean at `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02`; dirty — uncommitted documentation changes (this file and migration-tracker.md); `stash@{0}` untouched (2026-08-24) |
| Worktree state (reconciliation) | Dirty at `287539d58b...` (prior reconciliation) — 11 modified + 2 untracked (per `git status`): includes `packages/core/src/config.ts`, `packages/core/test/config/config.test.ts`, `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md`, `packages/kilo-vscode/src/private-worker/failure.ts`, `packages/opencode/src/private-worker/failure.ts`, `specs/vscode-orchestrator/archive/migration-tracker-history-2026-08-28.md`, `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md`, `specs/vscode-orchestrator/evidence/p4.4-g3-private-request-schema-draft.md`, `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md`, `specs/vscode-orchestrator/migration-tracker.md`, `specs/vscode-orchestrator/runtime-and-configuration-direction.md`, plus untracked `.changeset/p44-core-canonical-config-removal.md`, `.changeset/p44-persisted-operation-redaction.md`; `stash@{0}` untouched |
| Worktree state (envelope normalization 2026-08-29) | Dirty in pending worktree based on `f4e0ed79f04c...` (current base) — modified `packages/kilo-vscode/src/private-worker/failure.ts`, `packages/opencode/src/private-worker/failure.ts`, `packages/kilo-vscode/tests/unit/failure.test.ts`, `packages/opencode/test/private-worker/failure.test.ts`, `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md`, `specs/vscode-orchestrator/migration-tracker.md`, plus untracked `.changeset/p44-panel-envelope-normalization.md`; prior reconciliation untracked changes are not pending in this worktree; validated in this worktree, not contained in `f4e0ed79` itself; `stash@{0}` untouched |
| Evidence kind | Static inspection + dynamic focused unit-test verification (pure modules only) |
| Phase gate | P4 Active; P4.2 remains Active; P4.3/P4.4/P4.5 pending |
| Bounded wiring attestation | Existing canonical `session_operation` aggregate acknowledged; bounded provider lifecycle wiring proven at `packages/opencode/src/session/processor.ts:1275-1321,1411` (production wiring) and bounded local-helper panel-envelope normalization proof at `packages/opencode/src/private-worker/failure.ts:443 normalizeRecord` / `458 buildPanelEnvelope` (parity `packages/kilo-vscode/src/private-worker/failure.ts:443/458`) scrub + caps 500/1000/2000 idempotent, pure helper with no production caller/transport; no second DB/store/separate ledger; no queue, timer, or recovery-accounting retry integration (existing provider retry at `packages/opencode/src/session/processor.ts:1413-1427` remains outside recovery-budget proof), or dual-read/import; private-worker transport, retry accounting, worker crash hooks, and five-boundary convergence remain unproven in this unit |

## Authoritative Sources / Spec References

| Document | Section | Authorizes |
|---|---|---|
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §7.2 Failure/Outcome/Recovery (normative P4.2 foundation) | Runtime-owned normalization boundary; minimal schema without taxonomy freeze; operation/outcome identity; field tiers; redaction; panel envelope; cancellation provenance; sole recovery authority; recovery accounting; nested visibility; worker-crash disposition; version ownership; canonical storage |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §7.1 Runtime observation and hydration contract | Operation/outcome identity converging under observation; payload-free `changed` feed; revision ownership |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §7.3 Post-reconstruction error maturity | Excludes taxonomy freeze, byte/depth sanitizer, rich telemetry from P4.2 scope |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §9 Bounded decisions R11-R14 | R11 record location/retention; R12 schema/tiers/redaction/envelope/cancellation; R13 accounting; R14 crash disposition |
| `specs/adr/0004-architecture-first-direct-reconstruction.md` | LOCK-014/015 | Principle-first, no default compatibility entitlement |
| `specs/adr/0005-bounded-private-runtime-storage.md` | LOCK-016/017 + §5-8 | Canonical aggregate storage, retention, artifact registry, cutover |
| `specs/storage/session-storage-rewriting.md` | §5-8 | Storage foundation, revision, changefeed, retention, cutover gates |
| `specs/vscode-orchestrator/migration-tracker.md` | §4 Active Gates (P4 Failure/Outcome/Recovery gate) | Failure/Outcome/Recovery gate remains unchecked — evidence only |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §5 / §5.4 | Config commit outcomes are not R11 generation-path operations |

## Requirement-to-Artifact Matrix (§7.2)

All rows are evidenced by pure additive modules; bounded provider-operation lifecycle wiring exists at `packages/opencode/src/session/processor.ts:1275-1321,1411` (production wiring) and bounded local-helper panel-envelope normalization proof exists at `packages/opencode/src/private-worker/failure.ts:443 normalizeRecord` / `458 buildPanelEnvelope` (parity kilo-vscode `443/458`; pure helper, no production caller/transport) — private-worker transport, recovery-accounting retry integration (existing provider retry at `packages/opencode/src/session/processor.ts:1413-1427` remains outside recovery-budget proof), crash disposition, and five-boundary convergence remain unproven.

| §7.2 Requirement | Artifact (source:line) | Test proof (focused) | Notes |
|---|---|---|---|
| Runtime-owned normalization boundary — exactly one place converts provider/session/tool/permission/worker/transport errors into Failure record; classification never schedules retry | `packages/opencode/src/private-worker/failure.ts:323` `normalize()` + `packages/kilo-vscode/src/private-worker/failure.ts:323` parity; `classify()` at `failure.ts:243` | `packages/opencode/test/private-worker/failure.test.ts` (R12 classification/normalization) + `packages/kilo-vscode/tests/unit/failure.test.ts` parity | Pure module, zero production imports; `normalize` is sole entry; `classify` separate, no timers |
| Minimal Failure/Outcome schema without taxonomy freeze — 9-field record, stable unknown fallback codes, no invented codes | `packages/opencode/src/private-worker/failure.ts:46` `FailureRecord` (opId/opKind/outcome/code/message/time/cancel/detail/stack) + `packages/core/src/session/operation.ts:23` `FailureRecord` ; `failure.ts:243` `classify` closed code set | `packages/opencode/test/private-worker/failure.test.ts` closed sets + `packages/core/test/operation-r11.test.ts` field shape | 9-field shape enforced by `validateRecord`; small code set with unknown fallback, no freeze |
| Operation/outcome identity — accepted generation-path operations (prompt, provider attempt, tool call, permission/question wait, child/background task) have stable identities plus control kinds cancelQueued/sessionUpdate/fork/create; outcome in {succeeded, failed, ambiguous, in-flight, superseded, abandoned} | `packages/core/src/session/operation.ts:15` `OP_KINDS` (9 kinds), `139` `promptId()`, `145` `providerId()`, `153` `toolId()`, `162` `permissionId()`, `168` `taskId()`, `182` `cancelQueuedId()`, `190` `sessionUpdateId()`, `201` `forkId()`, `212` `createId()`, `218` `parseOpId()`, `18` `OUTCOMES` ; parity in `failure.ts:32` `OUTCOMES`, `recovery.ts:22` `OP_KINDS`, `disposition.ts:16` `OP_KINDS/OUTCOMES` | `packages/core/test/operation-r11.test.ts` identity constructors stable, colon/attempt/cross-kind validation, nine kinds exactly (current 2026-09-02: 15/176), rejects config kind — prior 5-kind/7-kind records remain historical | Nine-form identity (current 9 kinds); `provider:assistantId:attempt` nonnegative int; colon-free; task optional parentCallId; control kinds validate 1-2 segments; historical 5-kind/7-kind counts preserved as historical records |
| Field tiers — durable / diagnostic / panel-visible ceilings | `packages/opencode/src/private-worker/failure.ts:58` `FIELD_TIERS` + `packages/core/src/session/operation.ts:46,50` reuses same tiers | `packages/opencode/test/private-worker/failure.test.ts` FIELD_TIERS per-key, `select()` project/diagnose + `operation.ts:119-129` `toPersistedRecord`/`toPanelRecord` tier projections | `opKind/time` durable; `detail/stack` diagnostic; `opId/outcome/code/message/cancel` panel-visible; persist is redacted full record via `normalizeRecord()` at `packages/core/src/session/operation.ts:80-94` (`toPersistedRecord` at `119`) — not `select(record,"persist")`; `select(...,"project")`/`select(...,"diagnose")` are projection filters |
| Runtime-side redaction before persistence/projection — secret-key scrub, string value scrub, cycle guard, depth cap 8, caps 500/1000/2000 | `packages/opencode/src/private-worker/failure.ts:91` `secretKey`/`valueScrub`/`jsonQuotedScrub` + `100` `decodeJsonEscapes`/`170` `isValidJsonEscapes`/`194` unicode-aware `scrubString` (idempotent `[redacted]`, malformed/unknown escapes `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via validity gate, dangling trailing `\` safe), `286` `redact()`, `237` `cap()` inside `normalize()` at `failure.ts:323` and `normalizeRecord()` at `failure.ts:443`; `packages/core/src/session/operation.ts:80-94` `normalizeRecord()` + `290-295` `SessionOperation.put()` validates then `normalizeRecord()` before persistence; `packages/opencode/src/private-worker/failure.ts:458` `buildPanelEnvelope()` normalizes via `normalizeRecord()` before `select(record,"project")`; parity in kilo-vscode `443/458` | `packages/opencode/test/private-worker/failure.test.ts` proves `normalize()` redaction (object secrets, string scrub, cyclic, depth 8, caps 500/1000/2000) and `buildPanelEnvelope()` raw-secret scrub/caps/tier/idempotent regression (48 tests, 322 expects: JSON-encoded quoted Secret scrub included, Unicode-escaped JSON scrub included, malformed `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via validity gate); `packages/core/test/operation-p42.test.ts` proves `SessionOperation.put()` redaction closure via `normalizeRecord()`  (8 tests, 111 expects: raw caller-supplied secrets scrubbed before persistence, quoted/bearer scrub, caps, panel projection redacted); parity `packages/kilo-vscode/tests/unit/failure.test.ts` (48 tests, 322 expects) | Redaction proven inside `normalize()` (message 500, detail 1000, stack 2000), persistence closure proven via `SessionOperation.put()` → `normalizeRecord()` , and panel-projection closure proven via `buildPanelEnvelope()` → `normalizeRecord()` at `packages/opencode/src/private-worker/failure.ts:458` (and kilo-vscode parity `458`) with idempotent scrub/cap regression — direct raw-secret `buildPanelEnvelope()` boundary now proven; malformed adjacent escapes `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via `isValidJsonEscapes` validity gate without spuriously redacting, dangling trailing `\` safe, valid `{"api\u004bey":"secret"}` still redacts; see narrowed limitation row |
| Structured redacted panel projection through versioned private envelope 1.0 | `packages/opencode/src/private-worker/failure.ts:27` `FAILURE_ENVELOPE_VERSION="1.0"`, `458` `buildPanelEnvelope()` → `443` `normalizeRecord()` + `391` `select(...,"project")` (idempotent scrub/cap, tier preserved) | `packages/opencode/test/private-worker/failure.test.ts` envelope version 1.0, payload is project-tier only, no detail/stack, plus raw-secret direct-projection regression (48 tests, 322 expects: JSON-encoded quoted Secret scrub included, Unicode-escaped JSON scrub included, malformed `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via validity gate); parity `packages/kilo-vscode/tests/unit/failure.test.ts` (48 tests, 322 expects) | Version ownership per R1/R9/R12; projection emits panel-visible only; direct raw-secret projection now normalized via `normalizeRecord()` before `select` (idempotent, preserves version/tier) — previously unproven limitation now closed for scrub/cap; malformed `{"api\\key":"keep-me"}` unchanged, valid `{"api\u004bey":"secret"}` redacts |
| Narrowed limitation — projection transport/5-boundary convergence (remaining) | `packages/opencode/src/private-worker/failure.ts:458` `buildPanelEnvelope()` now normalizes via `443` `normalizeRecord()` before projection (scrub + caps, idempotent, tier preserved); `packages/core/src/session/operation.ts:290-295` `SessionOperation.put()` normalizes before persistence | `packages/opencode/test/private-worker/failure.test.ts` direct raw-secret `buildPanelEnvelope()` regression (48 tests, 322 expects: raw `apiKey`/`token`/`password`/`credential` scrubbed, caps 500 enforced, tier preserved, idempotent, JSON-encoded scrub included, Unicode-escaped scrub included, malformed escaped preserve verbatim) + parity `packages/kilo-vscode/tests/unit/failure.test.ts` (48 tests, 322 expects) | Previously unproven direct `buildPanelEnvelope()` on raw caller-supplied record is now proven via `normalizeRecord()` before projection (scrub + caps 500/1000/2000, idempotent, preserves `opId`/`opKind`/`code`/`outcome`/`time`/`cancel`); persistence via `SessionOperation.put()`  remains proven — remaining unproven boundaries are private-worker transport delivery, retry accounting, worker crash hooks, and five-boundary convergence (P4-G7 Active); malformed adjacent `secret\q`/`x\secret`/`secret\u00`/`x\password` remain unchanged/no-throw via `isValidJsonEscapes` |
| Cancellation provenance — at least user_stop/steering/timeout/network_disconnect/unknown → abandoned | `packages/opencode/src/private-worker/failure.ts:35` `CANCEL_SOURCES` five, `323` `normalize` maps cancel→abandoned | `packages/opencode/test/private-worker/failure.test.ts` all five produce abandoned + cancel provenance | Closed set exactly five; distinguishes user_stop vs timeout |
| Private runtime sole semantic recovery authority — no client replay of accepted operations | Contract stated in matrix; `recovery.ts:2` and `disposition.ts:2` docs; `operation.ts:307` cross-identity rejection + `recovery` replay safety | `packages/opencode/test/private-worker/recovery.test.ts` unsafe nextAt requires `replaySafe===true` else `replay-unsafe` termination; `disposition` `replayed:false` | No queue/timer wiring; enforcement is pure validation, not a scheduler |
| Recovery accounting/coordination — owner `runtime`, scope `operation`, budget consumed/termination, nextAt occurrence time, provenance `runtime,user,offline-restore`, layer `semantic,sdk,provider,transport,task` | `packages/opencode/src/private-worker/recovery.ts:20` `RECOVERY_VERSION="1.0"`, `25` `OWNERS=["runtime"]`, `28` `SCOPES=["operation"]`, `31` `PROVENANCES`, `34` `LAYERS`, `37` `TERMINATIONS`, `54` `RecoveryRecord`, `87` `ChargeInput.nextAt/replaySafe` | `packages/opencode/test/private-worker/recovery.test.ts` closed sets, budget, nextAt, provenance, nested layers | Single operation budget; `create`/`charge`/`terminate`/`snapshot` pure, no DB/Map |
| Nested low-level attempt visibility — low-level retries visible to owning operation, shared budget consumption | `packages/opencode/src/private-worker/recovery.ts:2` doc + `258` `charge` increments `consumed` exactly once per attempt | `packages/opencode/test/private-worker/recovery.test.ts` semantic + nested sdk/provider/transport/task share one budget until limit | Attempt idempotent on exact replay, conflicting same id rejects; consumed == attempts.length |
| Worker-crash in-flight disposition converging under §7.1 with resource cleanup and no silent replay | `packages/opencode/src/private-worker/disposition.ts:14` `DISPOSITION_VERSION="1.0"`, `19` `OUTCOMES` incl. in-flight, `22` `PROVENANCES=["worker-crash"]`, `25` `Cleanup{released}`, `29` `DispositionRecord{replayed:false}` | `packages/opencode/test/private-worker/disposition.test.ts` five-forms, cleanup, occurrence vs receipt, no replay, idempotent/conflicting, terminal forward/replay | `create(in-flight)` + `apply(crash)` with `validateRecord`/`isTerminal`; receipt >= occurrence, distinct caller-supplied times |
| Version ownership — private envelopes/ledgers versioned 1.0 | `failure.ts:27` `FAILURE_ENVELOPE_VERSION`, `recovery.ts:20` `RECOVERY_VERSION`, `disposition.ts:14` `DISPOSITION_VERSION` | Tests assert version === "1.0" in each suite | No cross-module version drift |
| Canonical storage — session_operation aggregate via existing storage: FK cascade, CHECKs (9 kinds), indexes, inspectable columns, revision, payload-free changed feed, atomic BEGIN IMMEDIATE, deterministic ordering, family cascade retains deleted tombstone, retention via 8/6 GiB | `packages/core/src/session/sql.ts:182` `SessionOperationTable` (check `op_kind` 9 kinds, `outcome` per `operation-r11.test.ts:142`), `packages/core/src/database/migration/20260824000000_add_operation_record.ts:10` DDL (PRIMARY KEY op_id, REFERENCES session ON DELETE CASCADE, 3 indexes), `packages/core/src/session/operation.ts:290-295` `put()` normalizeRecord + BEGIN IMMEDIATE + `SessionRevision.advanceTx` + payload-free changed | `packages/core/test/operation-r11.test.ts` migration DDL/CHECK/indexes/rerunnable, pre-redacted round trip, atomic row+revision+changed, idempotent no-revision, cross-kind/identity/terminal regression rejected, narrowest in-flight→terminal, deterministic list, family cascade with deleted tombstone, rollback unchanged, retention eligibility (current 2026-09-02: 15 tests, 176 expects — 9 kinds; does not prove raw secret scrub; historical 14/126 preserved as historical) ; `packages/core/test/operation-p42.test.ts` proves raw caller-supplied secret persistence scrub via `normalizeRecord()` at `operation.ts:290-295` (current 2026-09-02: 8/111) | No new ledger/store; retention is existing family budget + cascade; existing `changed`/`deleted` kinds/caps unchanged; raw secret scrub attribution is `operation-p42`, not `operation-r11`; current 9-kind CHECK noted, historical 7-kind/5-kind preserved as historical |

Post-reconstruction maturity (§7.3 taxonomy refinement, sanitizer hardening, logs/telemetry keep-lists, rich actions) is explicitly **not** a P4.2 gate and returns no evidence here.

## Exact Source Paths (additive/pure plus bounded provider lifecycle wiring)

| Module | Path | Line anchor |
|---|---|---|
| Canonical persistence — table + operation API | `packages/core/src/session/sql.ts` `SessionOperationTable` | table definition + `CHECK` declarations |
| Migration — session_operation DDL | `packages/core/src/database/migration/20260824000000_add_operation_record.ts` | `10` CREATE TABLE + `24` indexes |
| Migration registry | `packages/core/src/database/migration.gen.ts` | generated entry for `20260824000000_add_operation_record` |
| Canonical operation API | `packages/core/src/session/operation.ts` | `23` FailureRecord, `139,145,153,162,168` constructors, `181` parseOpId, `214` validateRecord, `290-295` put normalizeRecord + BEGIN IMMEDIATE |
| Provider lifecycle wiring (bounded) | `packages/opencode/src/session/processor.ts` | `1275-1321,1411` providerId + put in-flight/terminal lifecycle via Admission |
| Failure — runtime normalization | `packages/opencode/src/private-worker/failure.ts` | `27` version, `35` CANCEL_SOURCES, `46` FailureRecord, `58` FIELD_TIERS, `100` decodeJsonEscapes/`170` isValidJsonEscapes (malformed/unknown `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via validity gate, dangling `\` safe), `194` scrubString (unicode-aware, validity-gated), `243` classify, `286` redact, `323` normalize, `391` select, `443` normalizeRecord (scrub/cap idempotent), `458` envelope (normalized projection) |
| Failure — VS Code parity | `packages/kilo-vscode/src/private-worker/failure.ts` | semantic parity, same line anchors |
| Recovery — accounting | `packages/opencode/src/private-worker/recovery.ts` | `20` version, `25` owner, `28` scope, `31` provenances, `34` layers, `54` RecoveryRecord, `116` parseOpId, `154` validateRecord, `221` create, `258` charge, `314` terminate, `348` snapshot |
| Recovery — VS Code parity | `packages/kilo-vscode/src/private-worker/recovery.ts` | semantic parity |
| Disposition — crash disposition | `packages/opencode/src/private-worker/disposition.ts` | `14` version, `16` OP_KINDS/OUTCOMES, `22` worker-crash, `29` DispositionRecord replayed:false, `77` parseOpId, `110` validateRecord, `166` create, `221` apply |
| Disposition — VS Code parity | `packages/kilo-vscode/src/private-worker/disposition.ts` | semantic parity |

Bounded provider lifecycle wiring exists at `packages/opencode/src/session/processor.ts:1275-1321,1411` (production wiring); bounded local-helper panel-envelope normalization proof exists at `packages/opencode/src/private-worker/failure.ts:443,458` (parity kilo-vscode `443,458`; pure helper, no production caller/transport, no live wiring). No second DB/store/separate ledger beyond the existing canonical `session_operation` aggregate, no queue, no timer, no recovery-accounting retry integration (existing provider retry at `packages/opencode/src/session/processor.ts:1413-1427` remains outside recovery-budget proof), no panel transport wiring, and no private-worker transport, worker crash hook, or five-boundary convergence beyond those bounded boundaries.

## Exact Test Paths and Dynamic Counts (Current 2026-09-02 verified + Historical baseline d112c377... + Prior reconciliation 287539d... + Current base f4e0ed79...)

Focused dynamic validation current 2026-09-02 verified counts (9 kinds): `operation-r11` 15/176, `operation-p42` 8/111, `failure` 55/473 (both packages), `recovery` 32/202, `disposition` 25/141 — aggregate 135 pass / 1103 expects (combined failure+recovery+disposition 112 pass / 816 expects). Historical baseline `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02` (2026-08-24) and prior reconciliation `287539d58b...` remain historical records (see historical rows below); current committed base is `f4e0ed79f04c6114698e29c35ee556f68f5b4222` (2026-08-29); envelope normalization validated in the pending worktree based on `f4e0ed79f04c...` confirms direct raw-secret panel projection scrub via `normalizeRecord` (validated in this worktree, not contained in `f4e0ed79` itself). Recovery suite initial hook timeout is a known flake but validated on retry with `--timeout 15000` (PASS 32/202); documented as current verification only.

| Test file | Tests | expect() | Result | Baseline |
|---|---|---|---|---|
| `packages/core/test/operation-r11.test.ts` | 15 | 176 | PASS | current 2026-09-02 (contract: DDL/CHECK/identity/retention; 9 kinds; does not prove raw secret scrub; historical 14/126 preserved as historical) |
| `packages/core/test/operation-p42.test.ts` | 8 | 111 | PASS | current 2026-09-02 (raw caller-supplied secret persistence scrub via `normalizeRecord()` at `operation.ts:290-295`; prior 287539d reconciliation) |
| `packages/opencode/test/private-worker/failure.test.ts` | 55 | 473 | PASS | current 2026-09-02 — envelope normalization (direct raw-secret regression + idempotent; 55/473 includes JSON-encoded quoted Secret scrub, Unicode-escaped JSON scrub, malformed `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via validity gate; historical 48/322 preserved as historical) |
| `packages/opencode/test/private-worker/recovery.test.ts` | 32 | 202 | PASS | current 2026-09-02 (validated with --timeout 15000 retry after hook timeout flake; d112c377 + prior 287539d historical) |
| `packages/opencode/test/private-worker/disposition.test.ts` | 25 | 141 | PASS | current 2026-09-02 (d112c377 + prior 287539d historical) |
| **Aggregate (current 2026-09-02, 5-suite)** | **135** | **1103** | **0 failures** | current focused: 15+8+55+32+25=135 pass; 176+111+473+202+141=1103 expects (combined failure+recovery+disposition 55+32+25=112 pass, 473+202+141=816 expects; 9 kinds) |
| **Aggregate (historical 4-suite)** | **111** | **618** | **0 failures** | d112c377 — historical |
| **Aggregate (reconciled 5-suite)** | **119** | **729** | **0 failures** | prior 287539d — historical |
| **Aggregate (envelope-normalized 5-suite)** | **127** | **902** | **0 failures** | pending worktree based on f4e0ed79 (failure 48/322) — historical (now superseded by 55/473 current) |

Dual VS Code parity suites exist at `packages/kilo-vscode/tests/unit/failure.test.ts` (current 2026-09-02: 55 tests, 473 expects with same direct raw-secret regression including JSON-encoded scrub, Unicode-escaped JSON scrub, malformed `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via validity gate; historical 48/322 preserved as historical), `recovery.test.ts` (32/202), `disposition.test.ts` (25/141) with semantic parity but are not counted in the focused aggregates above except for the combined 112/816 note; they were validated separately in their package (static parity, `bun test tests/unit/failure.test.ts` 55 pass/473 expect current 2026-09-02). The authoritative dynamic gate for this artifact is the current 135/1103 aggregate (historical 127/902 envelope-normalized, 119/729 reconciled, 111/618 historical without `operation-p42` remain historical).

Historical counts (HEAD `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02`) are 14 / 126 (`operation-r11`), 40 / 149 (`failure`), 32 / 202 (`recovery`), 25 / 141 (`disposition`) aggregate 111/618; prior reconciliation at `287539d58b...` adds `operation-p42` 8 / 111 for raw secret scrub, aggregate 119/729; envelope normalization validated in the pending worktree based on `f4e0ed79f04c...` (current base) updates `failure` to 46 / 267 (opencode) and parity 46 / 267 (kilo-vscode) with direct raw-secret regression including JSON-encoded quoted Secret scrub, aggregate 125/847; narrow P4-G7 correction validated in this worktree preserves unknown/malformed escapes verbatim (`{"api\\key":"keep-me"}` / `{"se\\cret":"keep-me"}` unchanged, dangling trailing `\` safe) and updates `failure` to 47 / 282 (opencode) and parity 47 / 282 (kilo-vscode), aggregate 126/862; final P4-G7 validity-gate correction validates adjacent malformed escapes `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via `isValidJsonEscapes` without spurious redaction while valid `{"api\u004bey":"secret"}` still redacts and updates `failure` to 48 / 322 (opencode) and parity 48 / 322 (kilo-vscode) with direct envelope regression (105/665 combined, 127/902 aggregate) — all historical rows remain historical records, not rewritten; historical tracker counts dated 2026-08-23 for R12 were 39+39 / 146+146 (`failure` opencode + kilo-vscode) and remain as historical record; current focused run 2026-09-02 is 55 / 473 (opencode, 9 kinds) and parity 55 / 473 (kilo-vscode) — parity preserved; prior 7-kind taxonomy and 48/322 counts remain historical snapshots.

`operation-r11` (current 2026-09-02: 15 tests, 9 kinds) remains contract coverage only; `operation-p42` (8 tests) is the raw caller-supplied secret persistence proof at `operation.ts:290-295`. Stale tracker reference `packages/core/test/operation-r11.test.ts` previously cited as 13 tests is superseded by the current 15-test verification above; stale 7-kind references are superseded by current 9-kind taxonomy (historical rows preserved).

## Explicit Exclusions — What This Unit Does NOT Do

- No production config reader changes, no dual-read/import/compatibility reader, no second DB/store/separate ledger (existing canonical `session_operation` aggregate acknowledged), no queue, timer, or recovery-accounting retry integration (existing provider retry at `packages/opencode/src/session/processor.ts:1413-1427` remains outside recovery-budget proof); production R11-R14 wiring limited to bounded provider lifecycle at `packages/opencode/src/session/processor.ts:1275-1321,1411` (only production wiring); panel-envelope normalization is local-helper proof at `packages/opencode/src/private-worker/failure.ts:443,458` (parity kilo-vscode `443,458`; pure helper, no production caller/transport, no live wiring) — no wider private-worker transport wiring.
- No legacy-reader cutover, no P4.3/P4.4/P4.5 transport or CLI deletion.
- No panel transport envelope delivery beyond local `buildPanelEnvelope` normalized projection at `packages/opencode/src/private-worker/failure.ts:458` (scrub + caps, idempotent, tier preserved; transport to UI remains unproven); no worker crash hook, recovery-accounting retry integration (existing provider retry at `packages/opencode/src/session/processor.ts:1413-1427` remains outside recovery-budget proof), or client replay enforcement beyond tested modules and bounded boundaries.
- No change to runtime source beyond the two parity failure modules (narrow `decodeJsonEscapes` default preserve `\\` + dangling `\` safe), no SDK/generated files, no P4.3 checklist, no `source-links.md`; no architecture-doc update was needed because this pure helper change has no mapped architecture impact (checker 0 signals).

## Explicit Unknowns — Live Integration Beyond Pure Modules (Bounded Update)

- Bounded — provider-operation lifecycle IS wired: `SessionOperation.providerId` + `SessionOperation.put` in-flight/terminal at `packages/opencode/src/session/processor.ts:1275-1321,1411`; Unknown: whether `session_operation` `put`/`get`/`list` is wired to other generation-path operations (prompt, tool, permission, task) or broader session/permission flows beyond provider.
- Bounded — panel-envelope normalization IS proven as local helper (not production wiring): `buildPanelEnvelope()` normalizes via `normalizeRecord()` before projection at `packages/opencode/src/private-worker/failure.ts:443,458` (parity kilo-vscode `443,458`), proven by direct raw-secret regression (48 tests, 322 expects; now includes quoted/bearer/JSON-encoded forms, Unicode-escaped, malformed preserve verbatim) — scrub + caps 500/1000/2000 idempotent, tier preserved; no production caller or live transport wiring exists for this helper — private-worker transport delivery remains unproven. Unknown: whether `failure.normalize` is invoked on live provider/session/tool/permission/worker/transport failures, or whether panel transport carries the versioned envelope to the UI — private-worker transport delivery remains unproven.
- Proven: persistence redaction IS enforced via `SessionOperation.put()` → `normalizeRecord()` , proven by `packages/core/test/operation-p42.test.ts` (raw caller-supplied secrets scrubbed, quoted/bearer scrub, caps); `operation-r11` does not prove raw secret scrub; Proven: panel-projection redaction IS enforced via `buildPanelEnvelope()` → `normalizeRecord()` at `packages/opencode/src/private-worker/failure.ts:458` (parity kilo-vscode `458`), proven by `packages/opencode/test/private-worker/failure.test.ts` and `packages/kilo-vscode/tests/unit/failure.test.ts` direct raw-secret regression (48 tests, 322 expects each, JSON-encoded quoted Secret scrub included, Unicode-escaped JSON scrub included, malformed `{"api\\key":"keep-me"}` / `{"se\\cret":"keep-me"}` preserve verbatim, dangling `\` safe, valid `{"api\u004bey":"secret"}` redacts) — idempotent, preserves `opId`/`opKind`/`code`/`outcome`/`time`/`cancel`, caps 500/1000/2000.
- Unknown: whether `recovery.charge`/`terminate` budgets are enforced at live retry call-sites (existing provider retry at `packages/opencode/src/session/processor.ts:1413-1427` is not proven to be wired to recovery-budget accounting), or whether nested SDK/provider/transport attempts decrement the same live budget — recovery-accounting retry integration remains unproven.
- Unknown: whether `disposition.apply` is invoked on actual worker crash with correct occurrence/receipt times and cleanup release facts — worker-crash disposition remains unproven.
- Unknown: five-boundary convergence of Failure/Outcome/Recovery facts under §7.1 (panel close/reopen, reload, session switch, transport reconnect, worker restart) with real Extension Host — still unproven.
- Unknown: P4.2 overall completeness — failure/recovery/disposition modules remain pure proofs except bounded provider lifecycle wiring and bounded envelope normalization; lifecycle, observation, and storage convergence beyond provider remain independent gates.

These unknowns are not proven by pure-module tests and require separate production wiring/observation evidence before any P4.2 exit claim.

## Verification Commands (per unit acceptance)

| Command | Result |
|---|---|
| `bun run script/check-md-table-padding.ts` | PASS (no padded tables) |
| `bun run script/check-architecture-impact.ts --worktree` | 0 signals — docs-only sync of current 2026-09-02 verified counts (9 kinds, 15/176, 55/473) outside checker taxonomy (no high/medium mapping); observed `0 architecture signal(s) in changed files. none` despite doc updates |
| `bun run script/extract-source-links.ts --check` | PASS (no URL change) |
| `bun run script/check-opencode-promise-facades.ts` | PASS (no Promise facade added) |
| `bun run script/check-workflows.ts` | PASS (no workflow added/removed) |
| `git diff --check` | PASS (no whitespace errors) |
| `git status` | Dirty — modified `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md`, `specs/vscode-orchestrator/migration-tracker.md`; historical baseline was clean at `d112c377`; prior reconciliation dirty at `287539d58b...`; `stash@{0}` untouched; no production code change (docs-only sync to current 2026-09-02 verified counts) |
| `bun test ./test/operation-r11.test.ts` (from `packages/core`) | 15 pass / 176 expect — PASS (focused; contract coverage only, 9 kinds; does not prove raw secret scrub; historical 14/126 preserved) |
| `bun test ./test/operation-p42.test.ts` (from `packages/core`) | 8 pass / 111 expect — PASS (focused; raw caller-supplied secret persistence scrub via `normalizeRecord()` at `operation.ts:290-295`) |
| `bun test ./test/private-worker/failure.test.ts` (from `packages/opencode`) | 55 pass / 473 expect — PASS (focused; includes raw-secret envelope regression with JSON-encoded quoted Secret scrub, Unicode-escaped JSON scrub, malformed `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via validity gate; both packages 55/473; historical 48/322 preserved) |
| `bun test ./test/private-worker/recovery.test.ts` (from `packages/opencode`) | 32 pass / 202 expect — PASS (focused; validated with --timeout 15000 retry after initial hook timeout flake; documented as current verification only) |
| `bun test ./test/private-worker/disposition.test.ts` (from `packages/opencode`) | 25 pass / 141 expect — PASS (focused) |
| `bun test ./test/private-worker/failure.test.ts ./test/private-worker/recovery.test.ts ./test/private-worker/disposition.test.ts` (from `packages/opencode`) | 55+32+25=112 pass / 473+202+141=816 expect — PASS (focused; combined failure+recovery+disposition; aggregate 5-suite 135/1103) |
| `bun test tests/unit/failure.test.ts` (from `packages/kilo-vscode`) | 55 pass / 473 expect — PASS (focused; parity raw-secret envelope regression with JSON-encoded quoted Secret scrub, Unicode-escaped JSON scrub, malformed `secret\q`/`x\secret`/`secret\u00`/`x\password` preserve verbatim via validity gate; historical 48/322 preserved) |
| `bun test tests/unit/recovery.test.ts tests/unit/disposition.test.ts` (from `packages/kilo-vscode`) | 32+25=57 pass / 202+141=343 expect — PASS (focused parity; both packages) |

Focused test results above are already dynamically verified for this unit; do not broaden scope.

## Remaining Risks (evidence-only)

| Risk | Impact | Mitigation / Next Evidence Required |
|---|---|---|
| Pure-module proof misread as production integration | False confidence in live Failure/Outcome/Recovery | Explicit exclusions/unknowns in this artifact; no P4.2/P4 exit claim; gate `976` remains unchecked |
| Panel projection envelope not transported | UI never shows redacted facts despite correct shape | Separate private transport + observation convergence evidence required (R9/P4.2b) |
| Budget accounting not enforced at retry sites | Retries may overrun or duplicate without shared consumption | Recovery must be wired at explicit call-sites with occurrence-time plumbing; requires live test |
| Crash disposition not triggered | In-flight ops may orphan resources or silently replay | Disposition must be wired to crash detection with occurrence vs receipt times; requires lifecycle evidence |
| Stale tracker counts cited | Evidence appears inconsistent | Current 2026-09-02 verified 15/55/32/25 counts (15/176, 55/473, 32/202, 25/141; aggregate 135/1103; combined 112/816; 9 kinds) recorded here supersede stale 13-test/14/48/7-kind rows; historical rows preserved as historical records elsewhere |

## Change Log

| Date | Change | By |
|---|---|---|
| 2026-08-24 | Initial creation: evidence-only R11-R14 contract artifact — line-referenced §7.2 matrix (normalization, schema/OUTCOMES, five-form identity, tiers, redaction, envelope 1.0, cancellation provenance five, sole recovery authority, accounting/coordination, nested visibility, worker-crash disposition with cleanup/replayed:false, version ownership, canonical storage with session_operation FK cascade/CHECKs/indexes/revision/payload-free changed/retention), exact source/test paths, dynamic counts 14/40/32/25 aggregate 111/618, explicit no-wiring and unknowns, verification commands, remaining risks. Baseline `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02`. | Manifestor execution of p4.2-r11-r14 contract evidence task |
| 2026-08-29 | Reconciliation: recast d112c377 as historical baseline; record prior reconciliation commit `287539d58b...` (not current HEAD) with dirty worktree listing; correct `select(record,"persist")` to `normalizeRecord()`/`toPersistedRecord()` at `operation.ts:80-94,119,290-295`; reattribute raw caller-supplied secret persistence scrub to `operation-p42.test.ts` (8/111) and retain `operation-r11` (14/126) for contract-only coverage; add 5-suite aggregate 119/729 (111/618 historical); update verification `git status` and add `operation-p42` command; preserve bounded provider wiring and prior envelope limitation. | Manifestor reconciliation of p4.2-r11-r14 contract evidence (audit) |
| 2026-08-29 | Bounded envelope normalization: make `buildPanelEnvelope()` normalize caller-supplied `FailureRecord` via `normalizeRecord()` before projection (scrub + caps 500/1000/2000, idempotent, tier preserved) in both `packages/opencode/src/private-worker/failure.ts:414,429` and parity `packages/kilo-vscode/src/private-worker/failure.ts:414,429`; add direct unnormalized raw-secret panel-envelope regression tests on both sides (`packages/opencode/test/private-worker/failure.test.ts` and `packages/kilo-vscode/tests/unit/failure.test.ts` 46/267 each); narrow limitation row to remaining transport/5-boundary unknowns; keep P4-G7 Active; update counts to envelope-normalized aggregate 122/798. Validated in pending worktree based on `f4e0ed79f04c...` (current base, not contained in that commit). | Manifestor execution of P4-G7 envelope-normalization batch |
| 2026-08-29 | Bounded JSON-encoded scrub: extend `scrubString` with `jsonQuotedScrub` (`["']secretKey["']\s*[:=]\s*"(?:[^"\\]\|\\.)*"` etc.) before existing quoted/bearer/value scrubs in both parity modules; add direct raw `buildPanelEnvelope` JSON regression tests (`{"apiKey":"sk-json"}` and escaped `{"secret":"foo\"bar-secret"}` variants) in both suites (46/267 each, 103/610 combined); update evidence counts to 125/847 aggregate and correct stale test comment to current `normalizeRecord→select` behavior; keep P4-G7 Active and no-live-wiring; architecture checker remains 0 signals (pure-helper runtime changes outside taxonomy). Validated in pending worktree based on `f4e0ed79f04c...`. | Manifestor execution of P4-G7 JSON-encoded scrub batch |
| 2026-08-29 | Bounded Unicode-escaped JSON scrub: replace/extend `jsonQuotedScrub` with robust local property-key handling that decodes valid JSON escapes (`\uXXXX` at minimum alongside `\"`, `\\`, `\/`, `\b`, `\f`, `\n`, `\r`, `\t`) via `decodeJsonEscapes` before secret-key check in `scrubString` (parity `packages/opencode/src/private-worker/failure.ts:100,169,414,429` and `packages/kilo-vscode/src/private-worker/failure.ts:100,169,414,429`), preserving arbitrary non-JSON error text and idempotence; add direct-envelope regressions for `{"api\u004bey":"sk-unicode-key"}` and Unicode-escaped key with escaped value (`{"api\u004bey":"foo\"bar-escaped"}`) in both suites (46/267 each, 103/610 combined); update exact suite/aggregate counts to 125/847 and source anchors to 414/429 and correct architecture-doc statement to no-update rationale per LOCK-005; keep P4-G7 Active and no-live-wiring; checker 0 signals (pure-helper, no mapped impact). Validated in pending worktree based on `f4e0ed79f04c...`. | Manifestor execution of P4-G7 Unicode-escaped JSON scrub batch |
| 2026-08-29 | Narrow P4-G7 correction: ensure `decodeJsonEscapes()` preserves unknown/malformed escape sequences verbatim (`\\` + next char) rather than dropping backslash, and safely handles dangling trailing `\` without throw, so non-JSON malformed text is not spuriously transformed/redacted; add parity direct-envelope regressions proving `{"api\\key":"keep-me"}` and `{"se\\cret":"keep-me"}` remain unchanged/no-throw while valid `{"api\u004bey":"secret"}` still redacts; update parity `decodeJsonEscapes` default branch to `out += "\\"` + `out += nxt`; update line anchors to 100/170/415/430 and counts to 47/282 each (104/625 combined, 126/862 aggregate). Validated in pending worktree based on `f4e0ed79f04c...`. | Manifestor execution of P4-G7 narrow correction batch |
| 2026-08-29 | Adjacent malformed escapes validity gate — enforce `isValidJsonEscapes()` (`\"` `\\` `/` `b` `f` `n` `r` `t` `\uXXXX` only) before quoted-key secret match in both parity modules `packages/opencode/src/private-worker/failure.ts:100,170,194,443,458` and `packages/kilo-vscode/src/private-worker/failure.ts:100,170,194,443,458` so `secret\q`/`x\secret`/`secret\u00`/`x\password`/`secret\u00zz`/`api\u004` preserve verbatim/no-throw even though lenient decode would look secret, while valid `{"api\u004bey":"secret"}` still redacts; add parity direct-envelope regressions (48/322 each, 105/665 combined, 127/902 aggregate). Validated in pending worktree based on `f4e0ed79f04c...`. | Manifestor execution of P4-G7 validity-gate batch |
| 2026-09-02 | Synchronize current-state counts and taxonomy to focused verification 2026-09-02 — operation-r11 15/176 (9 kinds), operation-p42 8/111, failure 55/473 (both packages), recovery 32/202 (validated with --timeout 15000 retry after hook timeout flake), disposition 25/141; recompute aggregates 15+8+55+32+25=135 pass / 176+111+473+202+141=1103 expects and combined failure+recovery+disposition 112/816; update narrative/matrix/verification rows to 9-kind taxonomy with clear note that historical 7-kind/48-count/105/665/127/902 remain historical records; preserve phase statuses P4-G7/G3/P4.4 Active/open and no closure claim; docs-only, no code/test change | Manifestor execution of P4-G7 current-state synchronization (docs-only) |

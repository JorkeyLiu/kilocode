# P4.2 R11-R14 Failure/Outcome/Recovery Contract Evidence — Evidence-Only (Bounded Wiring Update)

> **Status: EVIDENCE-ONLY — Pure-module contract proof with bounded provider lifecycle wiring.**
> This artifact records line-referenced §7.2 contract coverage via additive pure modules and focused unit tests against historical baseline `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02` (prepared 2026-08-24). Reconciled at `287539d58b5ad7abcfd569c15d74dce871a8d0bf` (2026-08-29). **Bounded production wiring exists** for provider-operation lifecycle at `packages/opencode/src/session/processor.ts:1275-1321,1411` (`SessionOperation.providerId` + `SessionOperation.put` in-flight/terminal lifecycle). It does **not** claim private-worker normalization/projection transport, retry accounting call-sites, worker crash hooks, or five-boundary convergence; P4 remains Active and P4/P4.2/P4.3/P4.4/P4.5 completion is not claimed. Gate `migration-tracker.md` §4 Active Gates (P4 Failure/Outcome/Recovery gate) remains unchecked.

## Preparation Metadata

| Field | Value |
|---|---|
| Title | P4.2 R11-R14 Failure/Outcome/Recovery Contract Evidence — Evidence-Only |
| Date prepared | 2026-08-24 |
| Baseline commit (HEAD at preparation, historical) | `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02` (2026-08-24) |
| Reconciliation HEAD (current) | `287539d58b5ad7abcfd569c15d74dce871a8d0bf` (2026-08-29) |
| Branch | `jorkey/refactor/vscode-agent-orchestrator` |
| Worktree state (historical baseline) | Clean at `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02`; dirty — uncommitted documentation changes (this file and migration-tracker.md); `stash@{0}` untouched (2026-08-24) |
| Worktree state (reconciliation) | Dirty at `287539d58b...` — 11 modified + 2 untracked (per `git status`): includes `packages/core/src/config.ts`, `packages/core/test/config/config.test.ts`, `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md`, `packages/kilo-vscode/src/private-worker/failure.ts`, `packages/opencode/src/private-worker/failure.ts`, `specs/vscode-orchestrator/archive/migration-tracker-history-2026-08-28.md`, `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md`, `specs/vscode-orchestrator/evidence/p4.4-g3-private-request-schema-draft.md`, `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md`, `specs/vscode-orchestrator/migration-tracker.md`, `specs/vscode-orchestrator/runtime-and-configuration-direction.md`, plus untracked `.changeset/p44-core-canonical-config-removal.md`, `.changeset/p44-persisted-operation-redaction.md`; `stash@{0}` untouched |
| Evidence kind | Static inspection + dynamic focused unit-test verification (pure modules only) |
| Phase gate | P4 Active; P4.2 remains Active; P4.3/P4.4/P4.5 pending |
| Bounded wiring attestation | Existing canonical `session_operation` aggregate acknowledged; bounded provider lifecycle wiring proven at `packages/opencode/src/session/processor.ts:1275-1321,1411`; no second DB/store/separate ledger; no queue, timer, retry scheduler, or dual-read/import; private-worker normalization/projection transport, retry accounting, worker crash hooks, and five-boundary convergence remain unproven in this unit |

## Authoritative Sources / Spec References

| Document | Section | Authorizes |
|---|---|---|
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §7.2 Failure/Outcome/Recovery (normative P4.2 foundation) | Runtime-owned normalization boundary; minimal schema without taxonomy freeze; operation/outcome identity; field tiers; redaction; panel envelope; cancellation provenance; sole recovery authority; recovery accounting; nested visibility; worker-crash disposition; version ownership; canonical storage |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §7.1 Runtime observation and hydration contract | Operation/outcome identity converging under observation; payload-free `changed` feed; revision ownership |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §7.3 Post-reconstruction error maturity | Excludes taxonomy freeze, byte/depth sanitizer, rich telemetry from P4.2 scope |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §9 Bounded decisions R11-R14 | R11 record location/retention; R12 schema/tiers/redaction/envelope/cancellation; R13 accounting; R14 crash disposition |
| `specs/adr/0004-architecture-first-direct-reconstruction.md` | LOCK-014/015 | Principle-first, no default compatibility entitlement |
| `specs/adr/0005-bounded-private-runtime-storage.md` | LOCK-016/017 + §5-8 | Canonical aggregate storage, retention, artifact registry, cutover |
| `specs/storage/session-storage-rewriting.md` | §5-8 | Storage foundation, revision, changefeed, retention, cutover gates |
| `specs/vscode-orchestrator/migration-tracker.md` | §4 Active Gates (P4 Failure/Outcome/Recovery gate) | Failure/Outcome/Recovery gate remains unchecked — evidence only |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §5 / §5.4 | Config commit outcomes are not R11 generation-path operations |

## Requirement-to-Artifact Matrix (§7.2)

All rows are evidenced by pure additive modules; bounded provider-operation lifecycle wiring exists at `packages/opencode/src/session/processor.ts:1275-1321,1411` — private-worker normalization/projection transport, retry accounting, crash disposition, and five-boundary convergence remain unproven.

| §7.2 Requirement | Artifact (source:line) | Test proof (focused) | Notes |
|---|---|---|---|
| Runtime-owned normalization boundary — exactly one place converts provider/session/tool/permission/worker/transport errors into Failure record; classification never schedules retry | `packages/opencode/src/private-worker/failure.ts:198` `normalize()` + `packages/kilo-vscode/src/private-worker/failure.ts:198` parity; `classify()` at `failure.ts:117` | `packages/opencode/test/private-worker/failure.test.ts` (R12 classification/normalization) + `packages/kilo-vscode/tests/unit/failure.test.ts` parity | Pure module, zero production imports; `normalize` is sole entry; `classify` separate, no timers |
| Minimal Failure/Outcome schema without taxonomy freeze — 9-field record, stable unknown fallback codes, no invented codes | `packages/opencode/src/private-worker/failure.ts:42` `FailureRecord` (opId/opKind/outcome/code/message/time/cancel/detail/stack) + `packages/core/src/session/operation.ts:23` `FailureRecord` ; `failure.ts:13` closed code set | `packages/opencode/test/private-worker/failure.test.ts` closed sets + `packages/core/test/operation-r11.test.ts` field shape | 9-field shape enforced by `validateRecord`; small code set with unknown fallback, no freeze |
| Operation/outcome identity — accepted generation-path operations (prompt, provider attempt, tool call, permission/question wait, child/background task) have stable identities; outcome in {succeeded, failed, ambiguous, in-flight, superseded, abandoned} | `packages/core/src/session/operation.ts:139` `promptId()`, `145` `providerId()`, `153` `toolId()`, `162` `permissionId()`, `168` `taskId()`, `181` `parseOpId()`, `17` `OUTCOMES` ; parity in `failure.ts:25` `OUTCOMES`, `recovery.ts:22` `OP_KINDS`, `disposition.ts:16` `OP_KINDS/OUTCOMES` | `packages/core/test/operation-r11.test.ts` identity constructors stable, colon/attempt/cross-kind validation, five kinds exactly, rejects config kind | Five-form identity; `provider:assistantId:attempt` nonnegative int; colon-free; task optional parentCallId |
| Field tiers — durable / diagnostic / panel-visible ceilings | `packages/opencode/src/private-worker/failure.ts:54` `FIELD_TIERS` + `packages/core/src/session/operation.ts:46,50` reuses same tiers | `packages/opencode/test/private-worker/failure.test.ts` FIELD_TIERS per-key, `select()` project/diagnose + `operation.ts:119-129` `toPersistedRecord`/`toPanelRecord` tier projections | `opKind/time` durable; `detail/stack` diagnostic; `opId/outcome/code/message/cancel` panel-visible; persist is redacted full record via `normalizeRecord()` at `packages/core/src/session/operation.ts:80-94` (`toPersistedRecord` at `119`) — not `select(record,"persist")`; `select(...,"project")`/`select(...,"diagnose")` are projection filters |
| Runtime-side redaction before persistence/projection — secret-key scrub, string value scrub, cycle guard, depth cap 8, caps 500/1000/2000 | `packages/opencode/src/private-worker/failure.ts:88` `secretKey`/`valueScrub`, `160` `redact()`, `227` `cap()` inside `normalize()` at `failure.ts:198`; `packages/core/src/session/operation.ts:80-94` `normalizeRecord()` + `290-295` `SessionOperation.put()` validates then `normalizeRecord()` before persistence; `packages/opencode/src/private-worker/failure.ts:289-290` `buildPanelEnvelope()` selects caller-supplied record without normalize; parity in kilo-vscode | `packages/opencode/test/private-worker/failure.test.ts` proves `normalize()` redaction (object secrets, string scrub, cyclic, depth 8, caps 500/1000/2000); `packages/core/test/operation-p42.test.ts` proves `SessionOperation.put()` redaction closure via `normalizeRecord()` at `packages/core/src/session/operation.ts:290-295` (8 tests, 111 expects: raw caller-supplied secrets scrubbed before persistence, quoted/bearer scrub, caps, panel projection redacted) | Redaction proven inside `normalize()` (message 500, detail 1000, stack 2000) and persistence closure proven via `SessionOperation.put()` → `normalizeRecord()` at `packages/core/src/session/operation.ts:290-295` and `packages/core/test/operation-p42.test.ts`; `operation-r11` retains only contract coverage (DDL/CHECK/identity/retention) and does not prove raw secret scrub; direct `buildPanelEnvelope()` at `packages/opencode/src/private-worker/failure.ts:289-290` on caller-supplied record remains unproven projection boundary |
| Structured redacted panel projection through versioned private envelope 1.0 | `packages/opencode/src/private-worker/failure.ts:23` `FAILURE_ENVELOPE_VERSION="1.0"`, `289-290` `buildPanelEnvelope()` + `266` `select(...,"project")` | `packages/opencode/test/private-worker/failure.test.ts` envelope version 1.0, payload is project-tier only, no detail/stack | Version ownership per R1/R9/R12; projection emits panel-visible only; direct `buildPanelEnvelope()` at `failure.ts:289-290` on caller-supplied record without `normalizeRecord()` remains unproven (see limitation row) |
| Limitation — projection redaction boundary enforcement (unproven) | `packages/opencode/src/private-worker/failure.ts:289-290` `buildPanelEnvelope()` projects via `select(record,"project")` on caller-supplied record without `normalizeRecord()` | `packages/opencode/test/private-worker/failure.test.ts` envelope asserts panel-visible only on pre-redacted input | Not proven: unredacted caller-supplied record could be projected if not originating from `normalize()`; persistence via `SessionOperation.put()` IS proven through `normalizeRecord()` at `packages/core/src/session/operation.ts:290-295` — guarantee holds for `put()`-derived records only, not direct `buildPanelEnvelope()` at `failure.ts:289-290` |
| Cancellation provenance — at least user_stop/steering/timeout/network_disconnect/unknown → abandoned | `packages/opencode/src/private-worker/failure.ts:31` `CANCEL_SOURCES` five, `198` `normalize` maps cancel→abandoned | `packages/opencode/test/private-worker/failure.test.ts` all five produce abandoned + cancel provenance | Closed set exactly five; distinguishes user_stop vs timeout |
| Private runtime sole semantic recovery authority — no client replay of accepted operations | Contract stated in matrix; `recovery.ts:2` and `disposition.ts:2` docs; `operation.ts:307` cross-identity rejection + `recovery` replay safety | `packages/opencode/test/private-worker/recovery.test.ts` unsafe nextAt requires `replaySafe===true` else `replay-unsafe` termination; `disposition` `replayed:false` | No queue/timer wiring; enforcement is pure validation, not a scheduler |
| Recovery accounting/coordination — owner `runtime`, scope `operation`, budget consumed/termination, nextAt occurrence time, provenance `runtime,user,offline-restore`, layer `semantic,sdk,provider,transport,task` | `packages/opencode/src/private-worker/recovery.ts:20` `RECOVERY_VERSION="1.0"`, `25` `OWNERS=["runtime"]`, `28` `SCOPES=["operation"]`, `31` `PROVENANCES`, `34` `LAYERS`, `37` `TERMINATIONS`, `54` `RecoveryRecord`, `87` `ChargeInput.nextAt/replaySafe` | `packages/opencode/test/private-worker/recovery.test.ts` closed sets, budget, nextAt, provenance, nested layers | Single operation budget; `create`/`charge`/`terminate`/`snapshot` pure, no DB/Map |
| Nested low-level attempt visibility — low-level retries visible to owning operation, shared budget consumption | `packages/opencode/src/private-worker/recovery.ts:2` doc + `258` `charge` increments `consumed` exactly once per attempt | `packages/opencode/test/private-worker/recovery.test.ts` semantic + nested sdk/provider/transport/task share one budget until limit | Attempt idempotent on exact replay, conflicting same id rejects; consumed == attempts.length |
| Worker-crash in-flight disposition converging under §7.1 with resource cleanup and no silent replay | `packages/opencode/src/private-worker/disposition.ts:14` `DISPOSITION_VERSION="1.0"`, `19` `OUTCOMES` incl. in-flight, `22` `PROVENANCES=["worker-crash"]`, `25` `Cleanup{released}`, `29` `DispositionRecord{replayed:false}` | `packages/opencode/test/private-worker/disposition.test.ts` five-forms, cleanup, occurrence vs receipt, no replay, idempotent/conflicting, terminal forward/replay | `create(in-flight)` + `apply(crash)` with `validateRecord`/`isTerminal`; receipt >= occurrence, distinct caller-supplied times |
| Version ownership — private envelopes/ledgers versioned 1.0 | `failure.ts:23` `FAILURE_ENVELOPE_VERSION`, `recovery.ts:20` `RECOVERY_VERSION`, `disposition.ts:14` `DISPOSITION_VERSION` | Tests assert version === "1.0" in each suite | No cross-module version drift |
| Canonical storage — session_operation aggregate via existing storage: FK cascade, CHECKs, indexes, inspectable columns, revision, payload-free changed feed, atomic BEGIN IMMEDIATE, deterministic ordering, family cascade retains deleted tombstone, retention via 8/6 GiB | `packages/core/src/session/sql.ts:182` `SessionOperationTable` (check `op_kind`, `outcome` per `operation-r11.test.ts:142`), `packages/core/src/database/migration/20260824000000_add_operation_record.ts:10` DDL (PRIMARY KEY op_id, REFERENCES session ON DELETE CASCADE, 3 indexes), `packages/core/src/session/operation.ts:290-295` `put()` normalizeRecord + BEGIN IMMEDIATE + `SessionRevision.advanceTx` + payload-free changed | `packages/core/test/operation-r11.test.ts` migration DDL/CHECK/indexes/rerunnable, pre-redacted round trip, atomic row+revision+changed, idempotent no-revision, cross-kind/identity/terminal regression rejected, narrowest in-flight→terminal, deterministic list, family cascade with deleted tombstone, rollback unchanged, retention eligibility (14 tests, 126 expects; does not prove raw secret scrub) ; `packages/core/test/operation-p42.test.ts` proves raw caller-supplied secret persistence scrub via `normalizeRecord()` at `operation.ts:290-295` | No new ledger/store; retention is existing family budget + cascade; existing `changed`/`deleted` kinds/caps unchanged; raw secret scrub attribution is `operation-p42`, not `operation-r11` |

Post-reconstruction maturity (§7.3 taxonomy refinement, sanitizer hardening, logs/telemetry keep-lists, rich actions) is explicitly **not** a P4.2 gate and returns no evidence here.

## Exact Source Paths (additive/pure plus bounded provider lifecycle wiring)

| Module | Path | Line anchor |
|---|---|---|
| Canonical persistence — table + operation API | `packages/core/src/session/sql.ts` `SessionOperationTable` | table definition + `CHECK` declarations |
| Migration — session_operation DDL | `packages/core/src/database/migration/20260824000000_add_operation_record.ts` | `10` CREATE TABLE + `24` indexes |
| Migration registry | `packages/core/src/database/migration.gen.ts` | generated entry for `20260824000000_add_operation_record` |
| Canonical operation API | `packages/core/src/session/operation.ts` | `23` FailureRecord, `139,145,153,162,168` constructors, `181` parseOpId, `214` validateRecord, `290-295` put normalizeRecord + BEGIN IMMEDIATE |
| Provider lifecycle wiring (bounded) | `packages/opencode/src/session/processor.ts` | `1275-1321,1411` providerId + put in-flight/terminal lifecycle via Admission |
| Failure — runtime normalization | `packages/opencode/src/private-worker/failure.ts` | `23` version, `31` CANCEL_SOURCES, `42` FailureRecord, `54` FIELD_TIERS, `117` classify, `160` redact, `198` normalize, `266` select, `289-290` envelope (direct unnormalized projection) |
| Failure — VS Code parity | `packages/kilo-vscode/src/private-worker/failure.ts` | semantic parity, same line anchors |
| Recovery — accounting | `packages/opencode/src/private-worker/recovery.ts` | `20` version, `25` owner, `28` scope, `31` provenances, `34` layers, `54` RecoveryRecord, `116` parseOpId, `154` validateRecord, `221` create, `258` charge, `314` terminate, `348` snapshot |
| Recovery — VS Code parity | `packages/kilo-vscode/src/private-worker/recovery.ts` | semantic parity |
| Disposition — crash disposition | `packages/opencode/src/private-worker/disposition.ts` | `14` version, `16` OP_KINDS/OUTCOMES, `22` worker-crash, `29` DispositionRecord replayed:false, `77` parseOpId, `110` validateRecord, `166` create, `221` apply |
| Disposition — VS Code parity | `packages/kilo-vscode/src/private-worker/disposition.ts` | semantic parity |

Bounded provider lifecycle wiring exists at `packages/opencode/src/session/processor.ts:1275-1321,1411`; no second DB/store/separate ledger beyond the existing canonical `session_operation` aggregate, no queue, no timer, no retry scheduler, no panel transport, and no private-worker normalization/projection transport, retry call-site, worker crash hook, or five-boundary convergence beyond that provider lifecycle wiring.

## Exact Test Paths and Dynamic Counts (Historical baseline d112c377... + Reconciliation 287539d...)

Focused dynamic validation at historical baseline `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02` (2026-08-24) passed (no broadening beyond these suites). Reconciliation at `287539d58b...` confirms persisted redaction via `operation-p42`.

| Test file | Tests | expect() | Result | Baseline |
|---|---|---|---|---|
| `packages/core/test/operation-r11.test.ts` | 14 | 126 | PASS | d112c377 (contract: DDL/CHECK/identity/retention; does not prove raw secret scrub) |
| `packages/core/test/operation-p42.test.ts` | 8 | 111 | PASS | 287539d reconciliation (raw caller-supplied secret persistence scrub via `normalizeRecord()` at `operation.ts:290-295`) |
| `packages/opencode/test/private-worker/failure.test.ts` | 40 | 149 | PASS | d112c377 + 287539d |
| `packages/opencode/test/private-worker/recovery.test.ts` | 32 | 202 | PASS | d112c377 + 287539d |
| `packages/opencode/test/private-worker/disposition.test.ts` | 25 | 141 | PASS | d112c377 + 287539d |
| **Aggregate (historical 4-suite)** | **111** | **618** | **0 failures** | d112c377 |
| **Aggregate (reconciled 5-suite)** | **119** | **729** | **0 failures** | 287539d |

Dual VS Code parity suites exist at `packages/kilo-vscode/tests/unit/failure.test.ts`, `recovery.test.ts`, `disposition.test.ts` with semantic parity but are not counted in the focused aggregates above; they were validated separately in their package (static parity). The authoritative dynamic gate for this artifact is the 119/729 reconciled aggregate (111/618 historical without `operation-p42`).

Historical counts (HEAD `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02`) are 14 / 126 (`operation-r11`), 40 / 149 (`failure`), 32 / 202 (`recovery`), 25 / 141 (`disposition`) aggregate 111/618; reconciliation at `287539d58b...` adds `operation-p42` 8 / 111 for raw secret scrub, aggregate 119/729; historical tracker counts dated 2026-08-23 for R12 were 39+39 / 146+146 (`failure` opencode + kilo-vscode) and remain as historical record; current focused run is 40 / 149 (opencode) as recorded above — parity unchanged.

`operation-r11` (14 tests) remains contract coverage only; `operation-p42` (8 tests) is the raw caller-supplied secret persistence proof at `operation.ts:290-295`. Stale tracker reference `packages/core/test/operation-r11.test.ts` previously cited as 13 tests is superseded by the current 14-test verification above.

## Explicit Exclusions — What This Unit Does NOT Do

- No production config reader changes, no dual-read/import/compatibility reader, no second DB/store/separate ledger (existing canonical `session_operation` aggregate acknowledged), no queue, timer, or retry scheduler; production R11-R14 wiring limited to bounded provider lifecycle at `packages/opencode/src/session/processor.ts:1275-1321,1411` — no wider private-worker normalization/projection transport wiring.
- No legacy-reader cutover, no P4.3/P4.4/P4.5 transport or CLI deletion.
- No panel transport envelope delivery beyond pure `buildPanelEnvelope` shape at `packages/opencode/src/private-worker/failure.ts:289-290` (direct unnormalized projection remains unproven); no worker crash hook, retry call-site, or client replay enforcement beyond tested modules and bounded provider lifecycle wiring.
- No change to runtime source, tests, SDK, generated files, P4.3 checklist, architecture docs, or `source-links.md`.

## Explicit Unknowns — Live Integration Beyond Pure Modules (Bounded Update)

- Bounded — provider-operation lifecycle IS wired: `SessionOperation.providerId` + `SessionOperation.put` in-flight/terminal at `packages/opencode/src/session/processor.ts:1275-1321,1411`; Unknown: whether `session_operation` `put`/`get`/`list` is wired to other generation-path operations (prompt, tool, permission, task) or broader session/permission flows beyond provider.
- Unknown: whether `failure.normalize` is invoked on live provider/session/tool/permission/worker/transport failures, or whether panel transport carries the versioned envelope to the UI — private-worker normalization/projection transport remains unproven.
- Proven: persistence redaction IS enforced via `SessionOperation.put()` → `normalizeRecord()` at `packages/core/src/session/operation.ts:290-295`, proven by `packages/core/test/operation-p42.test.ts` (raw caller-supplied secrets scrubbed, quoted/bearer scrub, caps); `operation-r11` does not prove raw secret scrub; Unknown: whether direct `buildPanelEnvelope()` at `packages/opencode/src/private-worker/failure.ts:289-290` on caller-supplied unnormalized `FailureRecord` enforces redaction — current envelope tests (`packages/opencode/test/private-worker/failure.test.ts`) use pre-redacted records.
- Unknown: whether `recovery.charge`/`terminate` budgets are enforced at live retry call-sites, or whether nested SDK/provider/transport attempts decrement the same live budget — retry accounting remains unproven.
- Unknown: whether `disposition.apply` is invoked on actual worker crash with correct occurrence/receipt times and cleanup release facts — worker-crash disposition remains unproven.
- Unknown: five-boundary convergence of Failure/Outcome/Recovery facts under §7.1 (panel close/reopen, reload, session switch, transport reconnect, worker restart) with real Extension Host — still unproven.
- Unknown: P4.2 overall completeness — failure/recovery/disposition modules remain pure proofs except bounded provider lifecycle wiring; lifecycle, observation, and storage convergence beyond provider remain independent gates.

These unknowns are not proven by pure-module tests and require separate production wiring/observation evidence before any P4.2 exit claim.

## Verification Commands (per unit acceptance)

| Command | Result |
|---|---|
| `bun run script/check-md-table-padding.ts` | PASS (no padded tables) |
| `bun run script/check-architecture-impact.ts --worktree` | 0 signals expected (test/doc-only change; no runtime boundary) |
| `bun run script/extract-source-links.ts --check` | PASS (no URL change) |
| `bun run script/check-opencode-promise-facades.ts` | PASS (no Promise facade added) |
| `bun run script/check-workflows.ts` | PASS (no workflow added/removed) |
| `git diff --check` | PASS (no whitespace errors) |
| `git status` | Dirty — 11 modified + 2 untracked at `287539d58b...`: `packages/core/src/config.ts`, `packages/core/test/config/config.test.ts`, `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md`, `packages/kilo-vscode/src/private-worker/failure.ts`, `packages/opencode/src/private-worker/failure.ts`, `specs/vscode-orchestrator/archive/migration-tracker-history-2026-08-28.md`, `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md`, `specs/vscode-orchestrator/evidence/p4.4-g3-private-request-schema-draft.md`, `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md`, `specs/vscode-orchestrator/migration-tracker.md`, `specs/vscode-orchestrator/runtime-and-configuration-direction.md`, plus untracked `.changeset/p44-core-canonical-config-removal.md`, `.changeset/p44-persisted-operation-redaction.md`; Historical baseline was clean at `d112c377`; `stash@{0}` untouched |
| `bun test ./test/operation-r11.test.ts` (from `packages/core`) | 14 pass / 126 expect — PASS (focused; contract coverage only, does not prove raw secret scrub) |
| `bun test ./test/operation-p42.test.ts` (from `packages/core`) | 8 pass / 111 expect — PASS (focused; raw caller-supplied secret persistence scrub via `normalizeRecord()` at `operation.ts:290-295`) |
| `bun test ./test/private-worker/failure.test.ts ./test/private-worker/recovery.test.ts ./test/private-worker/disposition.test.ts` (from `packages/opencode`) | 40+32+25=97 pass / 149+202+141=492 expect — PASS (focused) |

Focused test results above are already dynamically verified for this unit; do not broaden scope.

## Remaining Risks (evidence-only)

| Risk | Impact | Mitigation / Next Evidence Required |
|---|---|---|
| Pure-module proof misread as production integration | False confidence in live Failure/Outcome/Recovery | Explicit exclusions/unknowns in this artifact; no P4.2/P4 exit claim; gate `976` remains unchecked |
| Panel projection envelope not transported | UI never shows redacted facts despite correct shape | Separate private transport + observation convergence evidence required (R9/P4.2b) |
| Budget accounting not enforced at retry sites | Retries may overrun or duplicate without shared consumption | Recovery must be wired at explicit call-sites with occurrence-time plumbing; requires live test |
| Crash disposition not triggered | In-flight ops may orphan resources or silently replay | Disposition must be wired to crash detection with occurrence vs receipt times; requires lifecycle evidence |
| Stale tracker counts cited | Evidence appears inconsistent | Current 14/40/32/25 counts recorded here supersede stale 13-test row; historical rows preserved elsewhere |

## Change Log

| Date | Change | By |
|---|---|---|
| 2026-08-24 | Initial creation: evidence-only R11-R14 contract artifact — line-referenced §7.2 matrix (normalization, schema/OUTCOMES, five-form identity, tiers, redaction, envelope 1.0, cancellation provenance five, sole recovery authority, accounting/coordination, nested visibility, worker-crash disposition with cleanup/replayed:false, version ownership, canonical storage with session_operation FK cascade/CHECKs/indexes/revision/payload-free changed/retention), exact source/test paths, dynamic counts 14/40/32/25 aggregate 111/618, explicit no-wiring and unknowns, verification commands, remaining risks. Baseline `d112c37780c8d1d844ecd94e40d3fe6e83ec3e02`. | Manifestor execution of p4.2-r11-r14 contract evidence task |
| 2026-08-29 | Reconciliation: recast d112c377 as historical baseline; record current HEAD `287539d58b...` with dirty worktree listing; correct `select(record,"persist")` to `normalizeRecord()`/`toPersistedRecord()` at `operation.ts:80-94,119,290-295`; reattribute raw caller-supplied secret persistence scrub to `operation-p42.test.ts` (8/111) and retain `operation-r11` (14/126) for contract-only coverage; add 5-suite aggregate 119/729 (111/618 historical); update verification `git status` and add `operation-p42` command; preserve bounded provider wiring and direct `buildPanelEnvelope()` limitation per LOCK-001/002/003. | Manifestor reconciliation of p4.2-r11-r14 contract evidence (audit) |

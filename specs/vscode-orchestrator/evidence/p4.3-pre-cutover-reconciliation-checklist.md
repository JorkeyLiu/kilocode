# P4.3 Pre-Cutover Manual Reconciliation Checklist — Reconciled (Pre-Cutover Decision Recorded; Cutover PENDING)

> **Status: RECONCILIATION COMPLETE — Pre-cutover decision recorded (clean reset / manual rebuild accepted). CUTOVER PENDING — readers NOT deleted, no reader or runtime changes in this unit.**
> Reconciliation was performed manually by the sole user. All legacy readers remain unchanged; cutover remains a later atomic operation. No dual-read, no import tooling, no reader deletion in this unit.

## Artifact Metadata

| Field | Value |
|---|---|
| Title | P4.3 Pre-Cutover Manual Reconciliation Checklist — Reconciled (pre-cutover decision; cutover PENDING) |
| Date prepared | 2026-08-24 |
| Baseline commit (HEAD at preparation) | `fdf5fbcb3dab06b73a6cca08b8a239db26df9fbf` |
| Baseline tag | P4.3 readiness audit complete |
| Operator | `jorkeyliu` |
| Reconciliation status | **COMPLETE — pre-cutover reconciliation decision recorded (clean reset / manual rebuild, no value migration); cutover remains PENDING** |
| Reconciliation date | `2026-08-24` |
| Cutover status | _PENDING — readers NOT deleted_ |
| Verification signature | `manual-reset-rebuild` |

All operator/reconciliation/verification fields for the pre-cutover decision are now filled per the user's explicit clean-reset decision on 2026-08-24. Preparation metadata (date prepared, baseline commit/tag) is retained as template provenance. Cutover status intentionally remains `_PENDING — readers NOT deleted` — no cutover has occurred in this unit. This artifact is a completed pre-cutover reconciliation decision record, not evidence of P4.3 cutover or P4.4 completion.

Decisions applied in this reconciliation: manual/user-owned pre-cutover reconciliation only (no automatic import or migration tool); this task only completes the pre-cutover checklist and does not delete readers or alter runtime code; clean reset/rebuild — do not reconstruct legacy values into new canonical configuration (backed-up global `kilo.jsonc` is reference-only; provider credentials/API keys and other values to be re-entered manually after reset); cutover remains a later atomic operation with no dual-read, no import, and no reader deletion in this task.

## Purpose and Bound

This checklist records the 15 P0 source mappings to the four retained legal classes or 13 removal classes and provides explicit per-source fields for the sole user to reconcile desired values into canonical files, SecretStorage, or defaults before the reader deletion boundary.

- **What this artifact is:** A manual-only record that enumerates every current effective-config source, classifies it against the closed taxonomy, states its canonical target or explicit no-target disposition, and records the sole user's explicit clean-reset decision (no value migration, backed-up config reference-only, manual reconfiguration after reset).
- **What this artifact is not:** It does not delete readers, does not introduce automation, does not reconstruct legacy values, and does not claim cutover completeness. P4 remains Active; no claim of P4.3 cutover / P4.4 / P4.5 completion is made. Cutover is a later atomic unit.

## Manual-Only, No-Import, No-Dual-Read Statements

- **Manual-only:** Reconciliation is performed solely by the sole user by hand-editing canonical files, SecretStorage, or defaults. No script, import tool, migration tooling, or automatic reconciliation exists or will be created to populate this checklist.
- **No import tool:** There is no migration/import tool and none will be created. Any desired configuration after reset is recreated manually in the canonical locations. The backed-up global `kilo.jsonc` is reference-only.
- **No dual-read window:** There is no dual-read compatibility window and none will be created. Until the cutover, current implementation may still read legacy sources; at the cutover all legacy readers are deleted together atomically and cannot affect effective config. No dual-read was introduced in this unit.
- **No compatibility reader:** No generic compatibility reader or overlay survives the cutover. Deleted sources have no target reader.
- **Production readers unchanged in this unit:** This unit creates only this evidence artifact. It does not modify production config readers or P4.4/P4.5 artifacts. Verified: only this checklist file changed; no canonical config, backup, source code, test, or runtime reader was modified.
- **Clean reset / no value migration:** No legacy value was reconstructed into the new canonical configuration in this reconciliation and no canonical global/project file was reset, recreated, populated, or verified in this checklist unit. The decision is to use initial/versioned canonical configuration after a later clean reset/manual rebuild (versioned defaults / initial `kilo.jsonc`); any desired settings, providers, or API keys will be re-entered manually by the user after this checklist and before/around cutover as separately owned work, using the backed-up global `kilo.jsonc` as reference-only. No plaintext secret or provider credential was copied.

## Authoritative Sources Cited

| Document | Section | What it authorizes |
|---|---|---|
| `specs/vscode-orchestrator/p0-current-state-inventory.md` | §6.1 Config authority and 12+-source merge (15-source enumeration, evidence lines) | The 15 current merge sources and their inventory phrases |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §3.1 Legal source taxonomy | The closed set of four retained legal effective-config classes |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §5.1 Config authority and deterministic materialization | Canonical file/asset authority, no generic compatibility reader, manual recreation discipline |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §7 Migration Strategy — Legacy-reader cutover (no dual-read) | Atomic cutover semantics: legacy readers deleted together, no import, sole-user manual reconciliation before boundary |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §8.1 Effective-config source removal | The 13 removal source classes with disposition Delete / No target reader |
| `specs/vscode-orchestrator/runtime-and-configuration-direction.md` | §3.2 Field registry | Per-field canonical assignment that this checklist precedes (registry decides field-level target) |

Counts are reproducible via the inventory §11 commands and the readiness inventory test at `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts`.

## Mapping Summary — 15 to 4 / 13

- **15 P0 sources** enumerated in inventory §6.1 are each mapped exactly once.
- **4 retained legal classes** from runtime §3.1 are the only canonical targets. Any source not mapped here is invalid.
- **13 removal classes** from runtime §8.1 are the only deletion dispositions. Each P0 source maps to either a retained class or a single removal class; none maps to both and none is unclassified.

| Summary | Count | Verification |
|---|---|---|
| P0 sources enumerated | 15 | 15 rows in checklist below, IDs P0-01 through P0-15, no duplicate or missing ID |
| Retained legal classes available | 4 | Listed in Retained Legal Classes table below, each used at least once where applicable |
| Removal classes available | 13 | Listed in Removal Classes table below, each is a valid deletion disposition |
| Mapping cardinality | 15 → (4 retained) + (13 removal) | Every P0 row cites exactly one class; P0 retained rows = 3, P0 removal rows = 12 (P0-11 is the single project-canonical retained source; P0-02 and P0-03 share the global-canonical retained class; remaining 12 map to 7 distinct removal classes; the other 6 removal classes have no P0 source but remain explicit removal boundaries — see note below) |

Note on the 6 removal classes with no P0 source in the current 15 enumeration: `Legacy opencode.* keys, .opencode / .kilocode locations`, `Global project-asset sources`, `Ancestor directory walks`, `Primary-worktree mirror reads`, `Top-level mode/tools conversions`, and `Arbitrary CLI/env override layers` are still explicit removal boundaries defined in runtime §8.1. They have no distinct P0 merge source in inventory §6.1 (they are either notification-only, generic conversions, or structural reads collapsed into neighboring rows), but they are documented here so the 4/13 closed set is fully visible. The 15-source to 13-class relationship is therefore not one-to-one; it is many-to-one and partially sparse, which is the authoritative classification.

## Retained Legal Effective-Config Classes (4)

Exactly the four rows of runtime §3.1. No other authored source is retained.

| # | Source class (verbatim — runtime §3.1) | Owner | Persistence | Legal contents |
|---|---|---|---|---|
| 1 | Canonical global config files/assets | User authoring; extension host (GUI) reads/writes on the user's behalf | One canonical global config root | Global-effective config fields and global typed assets (for example global agent markdown, commands, rules, skills) per the field registry |
| 2 | Canonical project config files/assets | User authoring; extension host (canonical boundary) | `<workspaceRoot>/.kilo/` | Project-effective config fields and project typed assets (for example agent markdown, commands, skills, project MCP/tool declarations) per the field registry |
| 3 | SecretStorage credentials | Extension host | VS Code SecretStorage | Credentials only, referenced by opaque IDs |
| 4 | Runtime defaults and safety invariants | Versioned runtime schema | Schema (versioned) | Defaults and safety invariants, never user-authored overlays |

Documented separately and not effective-config inputs: `globalState`/`workspaceState` (UI-only layout/churn), session/storage state (operational facts), and bounded permission approval records. These are not checklist targets.

## Removal Source Classes (13)

Exactly the 13 rows of runtime §8.1. Disposition for every row is **Delete; no target reader**. Any desired value is manually recreated in a retained class above before the cutover; no migration/import exists. Pre-cutover reconciliation decision for this unit is recorded in the added column — all rows accept deletion/no reconstruction under clean reset; cutover not yet executed; P4.4 will later record per-row inactive/removal evidence.

| # | Source class (verbatim — runtime §8.1) | Disposition | Pre-cutover reconciliation decision (2026-08-24, clean reset) | Evidence phase |
|---|---|---|---|---|
| 1 | KILO_CONFIG env override | Delete; no target reader; desired values recreated in canonical files | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 2 | KILO_CONFIG_DIR env override | Delete; no target reader | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 3 | KILO_CONFIG_CONTENT env override | Delete; no target reader | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 4 | KILO_PERMISSION env override | Delete; no target reader; desired policy written to canonical policy files | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 5 | Legacy opencode.* keys, .opencode / .kilocode locations | Delete; no target reader | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 6 | Global project-asset sources | Delete; no target reader; project-versioned harness assets live only under `<workspaceRoot>/.kilo/` | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 7 | Ancestor directory walks | Delete; no target reader; the canonical project boundary is `<workspaceRoot>/.kilo/` | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 8 | Primary-worktree mirror reads | Delete; no target reader; effective config reads the first VS Code workspace root, never a primary-worktree mirror | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 9 | Cloud/org/managed config sources | Delete; no target reader | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 10 | Top-level mode/tools conversions | Delete; no target reader; desired agents/tools recreated as canonical typed assets | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 11 | Arbitrary CLI/env override layers | Delete; no target reader; private-worker bootstrap/diagnostic process parameters are not configuration sources | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 12 | Legacy global config filenames/readers | Delete; no target reader; the only global authored scope is the canonical global config root | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |
| 13 | Legacy migration readers/import tooling | Delete; no target reader; no migration tool exists or will be created | Accept deletion / no reconstruction under clean reset; no target reader; cutover NOT YET EXECUTED — deletion pending P4.3 cutover; inactive/removal evidence to be recorded at P4.4. | P4.4 |

Legacy readers are deleted together at the P4.3 cutover; P4.4 records per-row inactive/removal evidence. P4.4/P4.5 remove legacy storage writers/readers under runtime §8.2 with the same per-category evidence rules.

## P0 Reconciliation Checklist — 15 Sources

Instructions for the sole user: For each row, inspect the current value contributed by that source (if any), decide the desired value, and manually recreate it in the stated Canonical target before the cutover, or explicitly confirm deletion if no value is desired. Fill the Desired value reconciled into canonical target and Verification (file/field/provenance) columns by hand. Leave no row blank at cutover.

Reconciliation decision for this pre-cutover gate (2026-08-24, operator `jorkeyliu`, signature `manual-reset-rebuild`): **Clean reset / manual rebuild — do not migrate old values. Decision is to start the refactored application after a later clean reset/manual rebuild with initial canonical configuration (versioned defaults + initial `kilo.jsonc`); any desired settings, providers, API keys, or project values will be re-entered manually by the user after this checklist and before/around cutover as separately owned work. Backed-up `kilo.jsonc` is reference-only and no canonical global/project file was reset, recreated, populated, or verified in this checklist unit. No automatic import, no dual-read, no value reconstruction in this unit.** All rows below record that decision explicitly; P0-02/P0-03/P0-11 verification records the user's disposition, not proof that the future reset has already happened. Cutover remains pending; readers not deleted.

| ID | Inventory phrase (exact — inventory §6.1) | Evidence anchor (inventory §6.1) | Classification (retained / removal) | Canonical target or explicit disposition (no-target) | Desired value reconciled into canonical target | Verification (file/field/provenance) |
|---|---|---|---|---|---|---|
| P0-01 | Global `config.json` | `config.ts:404` | Removal — Legacy global config filenames/readers | **Planned deletion — No target reader (pending cutover).** Desired values, if any, manually recreated in Canonical global config files/assets (global `kilo.jsonc`) under the one canonical global config root per the field registry. No import. | Accept deletion / no reconstruction under clean reset. No legacy value is reconstructed into canonical config. Read-only inventory observed no `config.json` file in inspected scope; any value that may exist in this source is intentionally not migrated and is accepted as deleted (backup reference-only if discovered later). Will be re-entered manually after reset only if desired. No target reader. | Inventory anchor `config.ts:404` enumerated; read-only inventory scope observed: no `config.json` file found. Verification grounded in inventory observation + user decision `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepting deletion/no reconstruction under clean reset. Cutover PENDING — readers NOT deleted in this unit. No hash or value invented. |
| P0-02 | Global `kilo.json` | `config.ts:406` | Retained — Canonical global config files/assets | **Canonical global config files/assets** — One canonical global config root; canonical file `kilo.jsonc` (JSONC) per field registry. Existing `kilo.json` content, if desired, manually merged into the canonical `kilo.jsonc`. | No legacy value reconstruction; accept deletion after backup; manually re-enter desired settings later. `kilo.json` is legacy global filename — class retained as canonical global `kilo.jsonc`, but content is NOT merged/migrated. Backed-up `kilo.json` (if any) and backed-up global `kilo.jsonc` are reference-only. Desired settings will be authored manually in the initial/default `kilo.jsonc` after the future reset, as needed. No import. | Inventory anchor `config.ts:406`; read-only inventory observed legacy global `kilo.json` present. Decision: `jorkeyliu` `2026-08-24` `manual-reset-rebuild` — accept deletion/no reconstruction; retain the canonical file class (`kilo.jsonc`) with initial/default configuration, not the old file content. No migration performed; backup is reference-only; no canonical file was reset, recreated, populated, or verified in this checklist unit. Verification records the user's disposition, not proof that the future reset has already happened. Cutover PENDING — readers NOT deleted. |
| P0-03 | Global `kilo.jsonc` | `config.ts:407` | Retained — Canonical global config files/assets | **Canonical global config files/assets** — One canonical global config root; canonical file `kilo.jsonc` (JSONC) is already the legal file. Verify content is the intended global-effective state. | Decision is to retain the canonical file class and to reset/recreate it with initial/default configuration after a later clean reset/manual rebuild; no canonical file was reset, recreated, populated, or verified in this checklist unit and the old ~79KB content is not claimed as migrated or verified as the desired final config. The existing canonical global `kilo.jsonc` (observed ~79KB) is NOT migrated or verified as final desired state; its backup is reference-only. After the future reset, the application will start with initial `kilo.jsonc` defaults; any desired fields, providers, or API keys will be re-entered manually by the user after this checklist and before/around cutover as separately owned work. No import. | Inventory anchor `config.ts:407`; read-only inventory observed canonical global `kilo.jsonc` at `/Users/jorkeyliu/.config/kilo/kilo.jsonc` (~79KB). Verification: `jorkeyliu` `2026-08-24` `manual-reset-rebuild` — canonical class retained per runtime §3.1, but content decision is clean reset to initial/default after a later reset; no canonical file was reset, recreated, populated, or verified in this checklist unit. Verification records the user's disposition, not proof that the future reset has already happened. No claim that old 79KB content was migrated or is the desired final config; no hash invented; backup reference-only. Cutover PENDING — no reader deletion in this unit. |
| P0-04 | Global `opencode.json` | `config.ts:409` | Removal — Legacy global config filenames/readers | **Planned deletion — No target reader (pending cutover).** Desired values, if any, manually recreated in Canonical global config files/assets under the one canonical global config root. No import. | Accept deletion / no reconstruction under clean reset. No value from `opencode.json` is reconstructed. Read-only inventory did not highlight a distinct `opencode.json` global file in the inspected scope; any value currently held is accepted as deleted after backup reference-only. Desired global values, if any, to be manually authored in canonical `kilo.jsonc` after reset only if desired. | Inventory anchor `config.ts:409` enumerated; read-only inventory scope noted no explicit `opencode.json` prominence beyond legacy class. Verification: user `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepts deletion/no reconstruction; cutover PENDING — readers NOT deleted; no import/migration created. No invented value. |
| P0-05 | Global `opencode.jsonc` | `config.ts:410` | Removal — Legacy global config filenames/readers | **Planned deletion — No target reader (pending cutover).** Desired values, if any, manually recreated in Canonical global config files/assets under the one canonical global config root. No import. | Accept deletion / no reconstruction under clean reset. No value from `opencode.jsonc` is reconstructed. Any value currently held is accepted as deleted (backup reference-only). Manual re-entry after reset only if desired. No target reader. | Inventory anchor `config.ts:410` enumerated. Verification: user `jorkeyliu` `2026-08-24` `manual-reset-rebuild` — explicit acceptance of deletion/no reconstruction under clean reset; cutover PENDING — readers NOT deleted; no value migrated. |
| P0-06 | Legacy global `config` (TOML) one-shot migration | `config.ts:412-426` (`with: { type: "toml" }`) | Removal — Legacy migration readers/import tooling | **Planned deletion — No target reader (pending cutover).** No migration tool exists or will be created. Desired values manually recreated in canonical files/assets. | Accept deletion / no reconstruction under clean reset. No TOML one-shot migration is performed and no value is imported. Read-only inventory did not surface an active TOML legacy config in inspected scope; any dormant TOML state is accepted as deleted, not re-executed. Desired behavior after reset uses versioned defaults / manual canonical config only. | Inventory anchor `config.ts:412-426` enumerated. Verification: user `jorkeyliu` `2026-08-24` `manual-reset-rebuild` — no migration reader executed, no import tool created. Cutover PENDING — readers remain but will be deleted atomically at cutover; no dual-read. |
| P0-07 | Legacy project config migration (`loadLegacyConfigs`) | `config.ts:495-501` (`async function loadLegacyConfigs`) | Removal — Legacy migration readers/import tooling | **Planned deletion — No target reader (pending cutover).** Desired project values manually recreated in Canonical project config files/assets under `<workspaceRoot>/.kilo/`. No import. | Accept deletion / no reconstruction under clean reset. No legacy project config migration is performed. Read-only inventory observed legacy `.opencode/opencode.jsonc` at project scope but explicitly chose not to migrate its values; accept deletion after backup reference-only. Decision is that canonical project config will start after the future clean reset/manual rebuild with no migrated project values / initial state; no canonical file was reset, recreated, populated, or verified in this checklist unit; project `kilo.jsonc` to be manually authored later only if desired as separately owned work before/around cutover. | Inventory anchor `config.ts:495-501`; read-only inventory observed legacy `.opencode/opencode.jsonc` (no canonical `.kilo/kilo.jsonc` observed). Verification: user `jorkeyliu` `2026-08-24` `manual-reset-rebuild` — deletion/no reconstruction accepted; `loadLegacyConfigs` not executed as migration; no target reader after cutover. Cutover PENDING. |
| P0-08 | Organization modes (`loadOrganizationModes`) | `config.ts:512-516` (`async function loadOrganizationModes`) | Removal — Cloud/org/managed config sources | **Planned deletion — No target reader (pending cutover).** Desired modes, if still intended, manually recreated as canonical files/assets per field registry; org source does not survive. | Accept deletion / no reconstruction under clean reset. No org mode value is reconstructed. Read-only inventory reported unknown org/remote/managed state (not inspected); user explicitly accepts loss of any org-mode value that may exist — contents not inspected and not claimed as reviewed. Any desired mode to be manually recreated as canonical typed asset under `<workspaceRoot>/.kilo/` after reset, if still intended. No target reader. | Inventory anchor `config.ts:512-516`. Verification: inventory + user statement — org/remote/managed state unknown/unobservable in inspected shell; no claim that contents were inspected. Decision `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepts deletion/no reconstruction with acknowledged limitation. Cutover PENDING — readers NOT deleted; no import. |
| P0-09 | Auth-record well-known remote config (`.well-known/opencode`) | `config.ts:586-639` (`.well-known/opencode`) | Removal — Cloud/org/managed config sources | **Planned deletion — No target reader (pending cutover).** Desired values manually recreated in canonical files if needed; remote well-known fetch does not survive. | Accept deletion / no reconstruction under clean reset. No remote well-known value is reconstructed. Inventory state for remote well-known was unknown/unobservable; user accepts loss without inspecting remote content — not claimed as reviewed. Desired values, if needed after reset, to be manually authored in canonical files. No target reader; no remote fetch after cutover. | Inventory anchor `config.ts:586-639`. Verification: unknown/unobservable remote state per inventory; user `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepts deletion/no reconstruction with limitation recorded. Cutover PENDING — readers NOT deleted in this unit. |
| P0-10 | Explicit `KILO_CONFIG` file | `config.ts:655-670` (`if (Flag.KILO_CONFIG)`) | Removal — KILO_CONFIG env override | **Planned deletion — No target reader (pending cutover).** Desired values manually recreated in canonical global or project files/assets per field registry scope. No env indirection survives. | Accept deletion / no reconstruction under clean reset. No `KILO_CONFIG` indirection value is reconstructed. Read-only inventory observed no `KILO_CONFIG` in inspected shell env; any value that may be set in other shells/sessions is accepted as deleted, not migrated. Desired path to be manually placed in canonical global/project `kilo.jsonc` after reset if needed. No env indirection survives. | Inventory anchor `config.ts:655-670`; env inspection: `KILO_CONFIG` not present in inspected shell. Verification: user `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepts deletion/no reconstruction; acknowledges that uninspected shells may hold a value but it is intentionally not migrated. Cutover PENDING — readers NOT deleted. |
| P0-11 | Project config files + discovered config directories | `config.ts:680` (`ConfigPaths.files`, `ConfigPaths.directories`) | Retained — Canonical project config files/assets | **Canonical project config files/assets** — `<workspaceRoot>/.kilo/` only (exact project boundary; no ancestor walk, no global project-asset mirror). Project `kilo.jsonc` under that boundary is the sole legal project file; discovered-directory fan-out is removed. | Decision is to retain the legal project class and to start after a later clean reset/manual rebuild with no migrated project values / initial state; no canonical project file was reset, recreated, populated, or verified in this checklist unit; manually author `<workspaceRoot>/.kilo/kilo.jsonc` later only if desired as separately owned work before/around cutover. No legacy `.opencode` project values are migrated. Inventory observed no canonical project `.kilo/kilo.jsonc`; observed legacy `.opencode/opencode.jsonc`. Decision: canonical project boundary retained per runtime §3.1, but content will start empty/initial after the future reset — no reconstruction, no fan-out inheritance. | Inventory anchor `config.ts:680`; observed: no canonical `<workspaceRoot>/.kilo/kilo.jsonc`, legacy `.opencode/opencode.jsonc` present but not migrated. Verification: `jorkeyliu` `2026-08-24` `manual-reset-rebuild` — class retained, content decision is initial state after a later reset; no canonical file was reset, recreated, populated, or verified in this checklist unit. Verification records the user's disposition, not proof that the future reset has already happened; discovered-directory fan-out explicitly not retained; backup reference-only. Cutover PENDING — readers NOT deleted. |
| P0-12 | `KILO_CONFIG_DIR` | `config.ts:707-732` (`Flag.KILO_CONFIG_DIR`) | Removal — KILO_CONFIG_DIR env override | **Planned deletion — No target reader (pending cutover).** Desired values manually recreated in canonical files. Env directory indirection does not survive. | Accept deletion / no reconstruction under clean reset. No `KILO_CONFIG_DIR` directory indirection is reconstructed. Read-only inventory observed no `KILO_CONFIG_DIR` in inspected shell; any value in other env contexts is accepted as deleted, not migrated. Desired directory-hosted values, if needed, to be manually placed in the one canonical global config root after reset. | Inventory anchor `config.ts:707-732`; env inspection: `KILO_CONFIG_DIR` not present in inspected shell. Verification: user `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepts deletion/no reconstruction with limitation (other contexts not inspected). Cutover PENDING. |
| P0-13 | `KILO_CONFIG_CONTENT` env | `config.ts:794-808` (`process.env.KILO_CONFIG_CONTENT`) | Removal — KILO_CONFIG_CONTENT env override | **Planned deletion — No target reader (pending cutover).** Desired values manually recreated in canonical files. Env content injection does not survive. | Accept deletion / no reconstruction under clean reset. No `KILO_CONFIG_CONTENT` injection is reconstructed. Read-only inventory observed no `KILO_CONFIG_CONTENT` in inspected shell; any injected content elsewhere is accepted as deleted. Desired config to be manually authored in canonical files after reset. | Inventory anchor `config.ts:794-808`; env inspection: `KILO_CONFIG_CONTENT` not present in inspected shell. Verification: `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepts deletion/no reconstruction; cutover PENDING — env injection reader NOT deleted in this unit but will be deleted at cutover; no import. |
| P0-14 | Managed config directory / macOS managed preferences | `config.ts` (`managedConfigDir`) | Removal — Cloud/org/managed config sources | **Planned deletion — No target reader (pending cutover).** Desired values manually recreated in canonical files if needed; managed-directory/macOS managed prefs do not survive. | Accept deletion / no reconstruction under clean reset. No managed-directory/macOS managed preference value is reconstructed. Managed state was unknown/unobservable in inventory; user accepts loss without inspecting managed sources — not claimed as reviewed. Desired values, if needed, to be manually authored in canonical files after reset. | Inventory anchor `config.ts` (`managedConfigDir`); inventory: managed config state unknown/unobservable. Verification: user `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepts deletion/no reconstruction with acknowledged limitation; no claim of inspection. Cutover PENDING. |
| P0-15 | Runtime flag-derived permission/tool/compaction/plugin behavior | `config.ts` merge chain (`Flag.KILO_PERMISSION`, `result.tools`) | Removal — KILO_PERMISSION env override | **Planned deletion — No target reader (pending cutover).** Desired permission policy manually written to canonical policy files; plugin/tool enablement via canonical project/global assets per registry. Env flag does not survive. | Accept deletion / no reconstruction under clean reset. No `KILO_PERMISSION` or flag-derived tool/compaction/plugin overlay is reconstructed. Read-only inventory observed no `KILO_PERMISSION` in inspected shell; any flag-held policy elsewhere is accepted as deleted. Desired permission behavior after reset uses versioned runtime defaults plus manually authored canonical policy files/assets only. | Inventory anchor merge chain `Flag.KILO_PERMISSION` / `result.tools`; env inspection: `KILO_PERMISSION` not present in inspected shell. Verification: `jorkeyliu` `2026-08-24` `manual-reset-rebuild` accepts deletion/no reconstruction; acknowledges flag-derived behavior will be reconfigured manually after reset if desired. Cutover PENDING — readers NOT deleted. |

SecretStorage credentials and Runtime defaults and safety invariants are retained legal classes with no P0 merge-source row in inventory §6.1 (they are not file-merge sources). The sole user has reconciled:

- **SecretStorage credentials (retained class, no P0 row):** Credentials / API keys are intentionally **not migrated**. Old refs/values are reference-only and may be recreated manually by the user after reset; no plaintext secret is copied into canonical config. In this clean-reset reconciliation, no SecretStorage credential was inspected or moved, and no provider credential/API key value was reconstructed into canonical files. Verification is user acceptance: operator `jorkeyliu` on `2026-08-24` (signature `manual-reset-rebuild`) accepts that SecretStorage will be reconfigured manually after reset, including re-entering any provider credentials/API keys as opaque SecretStorage entries referenced by ID. No hash or secret value is claimed or recorded.
- **Runtime defaults and safety invariants (retained class, no P0 row):** Use **initial / versioned runtime defaults**; no legacy overlay/default values are reconstructed. Any reliance on a deleted source's implicit default is accepted as deleted; behavior after reset is the versioned runtime schema default unless the user explicitly authors a canonical value. Verification: operator `jorkeyliu` on `2026-08-24` (signature `manual-reset-rebuild`) accepts manually reconfiguring any desired behavior after reset, starting from initial defaults. No legacy default was preserved or overlayed.

## Pre-Cutover Completion Criteria

The checklist is complete only when the sole user has, by hand:

1. Visited each of the 15 P0 rows and either recreated the desired value in the stated canonical target or explicitly confirmed that no value is desired (deletion intentionally accepted). — **DONE under clean reset: all 15 rows explicitly accept deletion/no reconstruction (P0-02/P0-03/P0-11 retain the canonical file class with initial/default content; no legacy values reconstructed). 2026-08-24 `jorkeyliu` `manual-reset-rebuild`.**
2. Filled the Desired value and Verification columns for all 15 rows plus the two retained-class notes above — no pre-cutover `_PENDING` remains. — **DONE: all 15 Desired/Verification cells and both retained-class notes are filled; only cutover fields intentionally remain `_PENDING — readers NOT deleted`.**
3. Verified that canonical global `kilo.jsonc` under the one canonical global config root and canonical project `kilo.jsonc`/typed assets under `<workspaceRoot>/.kilo/` contain the intended effective config and that the effective config is file-authoritative with canonical provenance (runtime §5.1, §3.2). — **DONE per clean-reset decision: intended effective config after this decision is the initial/default canonical state to be used after a later clean reset/manual rebuild; decision is that global `kilo.jsonc` will be reset/recreated with initial configuration after the future reset (no canonical global/project file was reset, recreated, populated, or verified in this checklist unit; old ~79KB backup is reference-only, not verified as final); project `<workspaceRoot>/.kilo/kilo.jsonc` will start after the future reset with no migrated values (initial/empty) and will be authored later only if desired as separately owned work before/around cutover. Verification records the user's disposition, not proof that the future reset has already happened. File-authoritative provenance holds per runtime §5.1. Cutover still PENDING.**
4. Manually accounted for every desired value currently held in any of the 13 removal classes — each desired value either recreated in its canonical target or explicitly accepted as deleted — without claiming pre-cutover proof that legacy readers are inactive. Proof that legacy readers are inactive and removal evidence per runtime §8.1 belongs to P4.4 after the cutover. — **DONE: all 13 removal classes explicitly accepted as deleted/no reconstruction under clean reset; no value claimed as recreated except retained-class initial defaults. Readers remain active until cutover; no pre-cutover inactivity proof claimed. P4.4 evidence pending by design.**
5. Recorded the operator name and date in the Artifact Metadata table and signed the verification line. — **DONE: Operator `jorkeyliu`, Reconciliation date `2026-08-24`, Verification signature `manual-reset-rebuild`, Reconciliation status COMPLETE (pre-cutover decision). Cutover status intentionally remains `_PENDING — readers NOT deleted`.**

Do not proceed to reader deletion until all five criteria are met. The cutover deletes all legacy readers together; there is no partial cutover and no rollback via a compatibility reader. — *Note: criteria 1-5 for the pre-cutover decision are met in this file; actual reader deletion is the separate subsequent atomic unit (still PENDING).*

## Post-Checklist Cutover Boundary

- **What happens at cutover:** All legacy readers listed as Removal are deleted together. After that point no read of any deleted source can affect effective config, because no reader exists. — **PENDING — readers NOT deleted in this unit.**
- **What does not happen:** No import tool runs, no dual-read window opens, and no automatic reconciliation populates canonical files. The checklist is the sole bridge. — **Preserved: no import/dual-read created in this unit.**
- **What P4.4 does:** Records per-row inactive/removal evidence for every one of the 13 removal classes (runtime §8.1) with the tracker evidence categories (source/tests/docs/generated SDK/config/i18n/build/package), and verifies that no target reader remains. — **PENDING — P4.4 not executed in this unit.**

P4 remains Active after this artifact lands. No phase completion is claimed. Cutover (P4.3 cutover) and P4.4 remain pending by design.

## Verification Steps for the Sole User

1. Open this file and the two authoritative specs cited above. Confirm the 15 inventory phrases still match inventory §6.1 verbatim and that the 4 + 13 class names still match runtime §3.1 and §8.1 verbatim. — **Verified by Manifestor: phrases and class names unchanged.**
2. For each P0 row, open the canonical target file/asset (or confirm deletion) and record the file path, field, and provenance that now holds the desired value. — **Recorded under clean-reset decision: decision is that canonical global `kilo.jsonc` will be reset to initial/default after a later clean reset/manual rebuild (file at `/Users/jorkeyliu/.config/kilo/kilo.jsonc` per inventory, backup reference-only); project `<workspaceRoot>/.kilo/kilo.jsonc` will be initial/empty after the future reset (no migrated values); no canonical global/project file was reset, recreated, populated, or verified in this checklist unit and verification records the user's disposition, not proof that the future reset has already happened; all removal sources accepted as deleted/no reconstruction. Any manual re-entry of providers, API keys, project values, or desired settings occurs after this checklist and before/around cutover as separately owned user work. No legacy value reconstructed.**
3. For SecretStorage, open VS Code SecretStorage (via the extension's credential UI) and confirm opaque IDs resolve; record the credential ID and that it is reachable. — **Clean-reset decision: no credential inspected or migrated in this unit; SecretStorage will be reconfigured manually after reset; no plaintext secret copied. Operator acceptance recorded.**
4. For defaults, confirm the runtime schema version that will materialize the snapshot and that no field relies on a deleted overlay. — **Clean-reset decision: effective config after reset uses initial/versioned runtime defaults; no deleted overlay is relied upon; manual reconfiguration after reset if desired.**
5. Re-run the readiness inventory test to confirm the taxonomy is still closed — from `packages/opencode/`: `bun test test/kilocode/p4-3-readiness-inventory.test.ts` — expects 15 P0 sources, 4 retained, 13 removal, no invented class, no dual-read surface. This test does not verify manual values; it verifies the taxonomy. — **To be run in Verification below; test is read-only and does not mutate files.**
6. Keep this file as evidence. The cutover PR must reference this file and the filled operator/date; the tracker will record per-row evidence at P4.4. — **Evidence preserved: this file is the sole artifact changed in this unit.**

## Remaining Risks — What Must Be Manually Filled Before Cutover

- **Unfilled rows:** Every blank pre-cutover field was a risk. — **RESOLVED for pre-cutover decision: no pre-cutover `_PENDING` remains; all 15 rows + 2 retained notes are explicitly filled with clean-reset acceptance. Cutover field intentionally remains `_PENDING — readers NOT deleted`.**
- **Hidden values in removal sources:** Environment variables (`KILO_CONFIG*`, `KILO_PERMISSION`), legacy global files (`config.json`, `opencode.json`), TOML one-shot state, org well-known fetches, and managed-directory overrides may hold values not visible in the canonical files. They must be inspected by hand before deletion. — **Risk accepted under clean reset: user `jorkeyliu` explicitly accepts loss of any hidden values in removal sources without exhaustive inspection; for unobservable org/remote/managed sources, contents were not inspected and limitation is recorded. Backup `kilo.jsonc` is reference-only for manual re-entry after reset.**
- **Scope misplacement:** A value placed in the wrong canonical scope (global vs project) will compose differently under the field registry's typed operators. The registry (runtime §3.2) decides global-only / project-only / both-with-typed-composition per field; misplacement is a validation error at materialization, not a silent fallback. — **Mitigated by reset: no values were placed in this unit; scope will be decided at manual re-authoring time per registry.**
- **SecretStorage gap:** Credentials referenced by canonical files must already be in SecretStorage before cutover; file-only secrets will fail materialization. — **Risk accepted: SecretStorage intentionally not migrated; credentials/API keys will be re-entered manually as opaque SecretStorage entries after reset. No plaintext secret was copied. Until re-entry, file-only references will fail — user accepts this as part of clean reset.**
- **No invented values or hashes in this template:** This template contains no hashes or reconciled values. Any hash or value appearing after manual fill must be the sole user's own record, not an invented placeholder. — **Preserved: no hash or provider/API key value invented in this reconciliation; old 79KB content not claimed as migrated/verified; no plaintext secret recorded.**

## Placeholders and Ambiguities

No product-scope ambiguity was introduced in this template. If a field's canonical scope or composition operator is unclear, consult the field registry (`packages/kilo-vscode/src/config/registry.ts`, runtime §3.2) and leave the row marked pending with a note; do not invent a mapping.

Reconciliation-specific notes for this completed pre-cutover decision:
- Clean-reset scope: operator `jorkeyliu` 2026-08-24 chose to start refactored application with initial canonical configuration; backed-up global `kilo.jsonc` (~79KB) is reference-only, not a source of truth for migrated values.
- Unobservable sources (org modes, well-known remote, managed directory/macOS prefs): contents not inspected; user acceptance of loss recorded with explicit limitation in P0-08/P0-09/P0-14 verification cells.
- Env overrides (`KILO_CONFIG`, `KILO_CONFIG_DIR`, `KILO_CONFIG_CONTENT`, `KILO_PERMISSION`): inspected shell showed none set; other shells/sessions may hold values but are intentionally not migrated per clean-reset decision.
- Cutover boundary: all 13 removal classes accept deletion/no reconstruction; readers remain until the later atomic cutover — no claim of inactivity is made in this file; P4.4 will verify removal.

## Change Log

| Date | Change | By |
|---|---|---|
| 2026-08-24 | Initial creation: P4.3 pre-cutover manual reconciliation checklist template (pending, manual-only, 15 P0 rows → 4 retained / 13 removal, no reader changes). Baseline `fdf5fbcb3dab06b73a6cca08b8a239db26df9fbf`. | Manifestor execution of P4.3 checklist task |
| 2026-08-24 | Reconciliation complete (pre-cutover decision): recorded clean-reset/manual-rebuild decision — operator `jorkeyliu`, reconciliation date `2026-08-24`, signature `manual-reset-rebuild`. Filled all 15 P0 Desired/Verification cells with explicit accept-deletion/no-reconstruction (P0-02/P0-03/P0-11 retain canonical class with initial/default content, no legacy migration; P0-01/04-10/12-15 removal accepted; unobservable sources recorded with limitation), filled SecretStorage + Runtime defaults retained-class notes (no credential/default migration, manual reconfiguration after reset), completed 13 removal-class handling decisions (accept deletion/no reconstruction, cutover NOT YET EXECUTED, P4.4 pending). No reader deleted, no canonical/backup/source file changed. Status: reconciliation COMPLETE, cutover PENDING — readers NOT deleted. | Manifestor execution of P4.3 pre-cutover reconciliation checklist (user explicit decision: clean reset, no value migration, backed-up kilo.jsonc reference-only) |

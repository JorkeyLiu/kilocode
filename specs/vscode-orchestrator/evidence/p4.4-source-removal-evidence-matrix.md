# P4.4 Source-Removal Evidence Matrix — Static 13-Row Inventory

> **Unit status: COMPLETE — static evidence inventory recorded on 2026-08-25. P4.4 remains Active.**
> This artifact records effective-config inactivity after the P4.3 cutover and
> the source, diagnostic, TUI, SDK-wrapper, i18n, build/package, and transport
> residues that remain open. It does not claim physical deletion of those
> residues, transport narrowing, provider-catalog removal, storage removal, or
> P4.4 phase completion.
> **P4.4 bounded — reference-only `.opencode` filesystem scanner and synthetic migration notification removed (2026-08-28, no phase/row closure; P4.4 remains Active/residual)** — Bounded P4.4 removal deletes the remaining reference-only `.opencode` filesystem diagnostic scanner and synthetic migration notification: `packages/opencode/src/kilocode/config/config.ts` `detectOpencodeConfig`/`opencodeConfigNotification`/`OPENCODE_NOTIFICATION_ID`/`CONFIG_DOCS_URL`/`isConfigDir` `flagDir` param and `existsSync` import deleted (`isConfigDir` remains `dir.endsWith(".kilo")` canonical only), `packages/opencode/src/kilocode/server/httpapi/handlers/kilo-gateway.ts` gateway notice append and now-unused `KilocodeConfig`/`Flag`/`Instance` imports deleted (cloud `fetchKilocodeNotifications` remains). Scoped caller/test inspection proves no extension/webview consumer or public contract required them. Tests updated to assert absence/no legacy scanner while preserving canonical behavior. Canonical global/project config loading, `KILO_DISABLE_PROJECT_CONFIG` semantics where still used, `Global.Path.config` + `KILO_CONFIG_DIR` override and sandbox deny, `.kilocode` theme compatibility, generated SDK, and P4.3 cutover remain untouched per LOCK-001/002/003/004/009. Does not close P4.4, P4.5, P5, or unrelated source/storage rows.
> **P4.4 bounded correction — permission config-paths parity (2026-08-28, no phase/row closure; P4.4 remains Active/residual)** — Bounded correction narrows `packages/opencode/src/kilocode/permission/config-paths.ts:23` `CONFIG_ROOT_FILES` to canonical `kilo.json`, `kilo.jsonc`, `AGENTS.md` only (`opencode.json`/`opencode.jsonc` removed) for parity with `packages/opencode/src/permission/evaluator.ts:232` `rootFiles`. Prior `config-paths.ts:23` contained 5 entries while `evaluator.ts:232` already canonical-only (verified gap). Caller inspection: `ConfigProtection.isProtectedPath` via `packages/opencode/src/kilocode/permission/protected-files.ts`, `ConfigProtection.isRelative`/`isAbsolute` via `packages/opencode/src/kilocode/config-validation.ts`, `ConfigProtection.isRequest` no production caller found in scoped search (test-covered); no sync/warp/provider/catalog/transport/SDK/generated consumers; `packages/opencode/src/kilocode/sandbox/policy.ts:169-173` sandbox deny literals and `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` `.kilocode` theme handling preserved per LOCK-003/004. Tests: `packages/opencode/test/kilocode/permission/config-paths.test.ts` new case `returns false for legacy opencode root files (canonical kilo.json/kilo.jsonc/AGENTS.md only)` proving `opencode.json`/`opencode.jsonc` not protected via `isRequest`/`isRelative`/`isProtectedPath` while `kilo.json`/`kilo.jsonc`/`AGENTS.md` and `.kilo`/`plans` remain protected; existing `p4-4-legacy-filesystem-discovery-removal.test.ts` parity retained. Verification: focused permission tests pass, `bun run typecheck` pass. Does NOT close P4.4, the full 13-row source row, P4.5, or P5; no provider/catalog, transport, SDK, P4.3 reader, or theme change. Tracker: `specs/vscode-orchestrator/migration-tracker.md` (P4.4 bounded correction 2026-08-28).
>
> **P4.4 residual package — audit correction (2026-08-28, legacy filesystem discovery fallbacks removed + evaluator/MCP canonical narrowing; P4.4 remains Active/residual; no phase/row closure)** — Bounded P4.4 residual-removal package removes inert legacy filesystem discovery fallbacks from effective-config, project identity, permission, and MCP paths while preserving canonical `.kilo` behavior and `KILO_CONFIG_DIR -> Global.Path.config` + sandbox deny per LOCK-002/003/004/005. Source: `packages/opencode/src/kilocode/paths.ts:32-76` `globalDirs()` → `~/.kilo` only and `skillDirectories()` → `targets: [".kilo"]` only; `packages/opencode/src/kilocode/project-id.ts:41-47,83-89` `getProjectIdFromConfig()` → `.kilo/config.json` only and `resolveProjectId()` → `.kilo -> git` only; `packages/opencode/src/kilocode/permission/config-paths.ts:9-11,62,93` `CONFIG_DIRS` → `[".kilo/"]` only and `isAbsolute()` → `~/.kilo` only; **audit correction: `packages/opencode/src/permission/evaluator.ts:208-231` `isGlobalProtectedPath` now `Global.Path.config` + `~/.kilo` + `~/.config/kilo` only (no `~/.kilocode`) and lexical `.kilo` only (no `.kilocode`) without broad `.kilo/plans` exemption, `isProtectedPath` `rootFiles` now `kilo.json[kilo.jsonc], AGENTS.md` only (no `opencode.*`) and workspace `.kilo/plans` exemption evaluated before global matching (workspace `.kilo/plans` exempt only when beneath workspace root, global `~/.kilo/plans` remains protected when workspace elsewhere, overlap `workspace==$HOME` remains exempt while canonical `~/.kilo` and workspace `.kilo` stay protected); `packages/opencode/src/cli/cmd/mcp.ts:405-436` `resolveConfigPath` now canonical `.kilo/kilo.jsonc|json` (project) and `kilo.jsonc|json` (global) only (no `.kilocode`, no `opencode.*`) and exported**. Preserves `theme.tsx:564` `[".kilocode",".kilo"]` per LOCK-004. Tests: `paths.test.ts`/`project-id.test.ts`/`permission/config-paths.test.ts` legacy rewritten to absence, `p4-4-legacy-filesystem-discovery-removal.test.ts` (static absence + canonical preservation + runtime canonical-vs-legacy `false`/`true` + behavioral evaluator canonical `ask-ceiling` vs legacy `ask`, MCP resolver canonical choice vs legacy ignore, `KILO_CONFIG_DIR -> Global.Path.config` live `Flag`/`Global.make()` override, sandbox `profile` denyWrite/env) with env restoration, sorted profile `global-health < legacy < managed`. No `Config.Service`/loader/server/SDK/provider/transport change; **row 5 updated to record fallback + evaluator/MCP canonical narrowing (rows 5,7,12,13 remain residual/open per LOCK-001)**.

> **P4.4 residual package (2026-08-27, Open Config legacy diagnostic cleanup; P4.4 remains Active/residual)** — Bounded P4.4 residual-removal package removes obsolete VS Code Open Config diagnostics while preserving canonical opening/creation and `KILO_CONFIG_DIR` resolved-root compatibility. Source: `packages/kilo-vscode/src/kilo-provider/config-file.ts` now enumerates only `kilo.jsonc` at resolved global root (`KILO_CONFIG_DIR` → `Global.Path.config` override) and `<workspaceRoot>/.kilo/kilo.jsonc` (`Source = sourceGlobal|sourceLocal`, no `virtual`/`legacy`, no `.kilocode`/`.opencode`/`config.json`/`opencode.json` alternatives, no home/ancestor scans, no `KILO_CONFIG`/`KILO_CONFIG_CONTENT` labels); `packages/kilo-vscode/src/kilo-provider/open-config.ts` narrowed `Labels` and removed `statusLoadedLegacy`; `webview-ui/src/types/messages/webview-messages.ts` narrowed `OpenConfigFileRequest.labels` to `sourceGlobal`/`sourceLocal`; `webview-ui/src/components/settings/Settings.tsx` posts only canonical labels; 20 i18n dictionaries removed legacy `status.loadedLegacy`/`source.xdg|home*|env*|project*` and added `source.global`/`local`. Tests: `tests/unit/open-config.test.ts` rewritten (legacy absence, canonical global/local, `KILO_CONFIG_DIR` resolved root, disabled semantics, creation/error); new `tests/unit/p4-4-open-config-legacy-absence.test.ts` (file/message/i18n/sandbox preservation). No `Config.Service`/loader/server route/SDK/OpenAPI/transport/`Flag.KILO_CONFIG_DIR` change; `KILO_CONFIG_DIR` represented via resolved global root, not env taxonomy.

> **P4.4 residual package (2026-08-27) — dead TUI migration helper and unused KILO_TUI_CONFIG getter physically removed (one cohesive unit; P4.4 remains Active/residual).** Bounded residual-removal package deletes `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` (`migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`/`Filesystem.findUp`/`opencodeFiles` — zero active callers since T27) and `packages/core/src/flag/flag.ts` `get KILO_TUI_CONFIG()` getter (no production reader; canonical TUI loader preserved via T27/T30 `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` global→direct→`.kilo`, LOCK-SOURCE/LOCK-R6). Preserves `KILO_CONFIG_DIR` bridge, `KILO_DISABLE_PROJECT_CONFIG`, `ConfigPaths.files`/`fileInDirectory`, `theme.tsx` `[".kilocode",".kilo"]`, and HTTP/SSE/SDK `/global/event`/`/global/config` bridge per LOCK-002/003/004. Tests: `p4-4-tui-migrate-kilo-config-removal.test.ts` rewritten (helper absent + Flag absent + canonical preserved), `p4-4-flag-legacy-getter-removal.test.ts` updated (KILO_TUI_CONFIG absent), `p4-4-tui-canonical-source-removal.test.ts` updated (migrate absent, Flag absent). Rows 5/12/13 updated to record physical helper deletion (rows remain residual/open; no broader completion); row 2 updated for KILO_TUI_CONFIG deletion while KILO_CONFIG_DIR retained; source-links `https://app.kilo.ai/tui.json` removed via `script/extract-source-links.ts`.

> **P4.4-T1b update (2026-08-25) — legacy source-inventory reader and
> `/config/sources` physically removed.** A bounded P4.4 unit deleted
> `packages/opencode/src/kilocode/config/sources.ts` (the 12-source diagnostic
> inventory reader), removed the `KilocodeConfigSources` type/echo from the
> overlay, and removed the `/config/sources` schema/path/endpoint/handler from
> the config-console group and the `sources` echo field from the overlay
> response. The checked-in generated SDK and OpenAPI were regenerated and no
> `config.sources`/`ConfigSourcesResponse` contract remains. The rows below
> that cited `sources.ts` line ranges were recorded against the historical
> `8167992b` baseline where that file existed; the row-level delta is recorded
> in the P4.4-T1b note below. No row is marked complete: each affected row
> keeps its other retained residues (TUI/instruction/`ConfigPaths`, sandbox
> policy, SDK wrapper forwarding, managed/primary-worktree helpers) and P4.4
> remains Active.
>
> **P4.4-T2 update (2026-08-25) — orphaned primary-worktree mirror-read helper physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/kilocode/primary-worktree.ts` (helper with only test consumers, no production call site), deleted its direct test `packages/opencode/test/kilocode/primary-worktree.test.ts`, and removed the glob entry from `packages/opencode/script/kilocode/test-profile.ts`. The `ConfigPaths` module and canonical-root handling remain untouched per scope. Rows 6 and 8 are updated for this deletion; all other rows keep their prior T1b delta. P4.4 remains Active.
>
> **P4.4-T3 update (2026-08-25) — unreachable legacy managed/MDM configuration helper physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/config/managed.ts` (unreachable helper: `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences`, `MANAGED_PLIST_DOMAIN`, no production call site after the P4.3 cutover), removed its direct tests (`ConfigManaged` import and 5 `parseManagedPlist` unit tests) from `packages/opencode/test/config/config.test.ts`, and added a focused absence regression `packages/opencode/test/kilocode/p4-4-managed-removal.test.ts` plus its glob entry in `packages/opencode/script/kilocode/test-profile.ts`. No `Config.Service` behavior, `ConfigPaths`, daemon/transport, generated SDK/OpenAPI, provider catalog, TUI/instruction, or sandbox residue is changed. Row 9 is updated for this deletion; rows 1-8 and 10-13 keep their prior T2 delta. P4.4 remains Active.
>
> **P4.4-T4 update (2026-08-25) — legacy CLI `.well-known/opencode` provider-auth discovery URL-login branch physically removed.** A bounded P4.4 unit deleted the `args.url` branch at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`fetch` `<url>/.well-known/opencode`, `Process.spawn` of `auth.command`, `auth.set(url, { type: "wellknown", … })` persistence), removed the `login [url]` positional and its `Process`/`text` imports, and changed the command to `login` without URL discovery. Stale well-known intercept helpers (`remoteConfigClient`, `wellKnownAuth`, `json` helper, `wellKnown` helper) were removed from `packages/opencode/test/config/config.test.ts`. Added focused absence regression `packages/opencode/test/kilocode/p4-4-wellknown-provider-auth-removal.test.ts` plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (managed < primary < wellknown). Generic provider login (api-key/plugin/custom-provider, `kilo auth login` selection, `handlePluginAuth`, `resolvePluginProviders`) and canonical effective config remain untouched; legacy HTTP/SSE/generated-SDK bridge remains per LOCK-009; no SDK regeneration is required (no public server endpoint or OpenAPI change). Row 9 is updated for this CLI discovery deletion; rows 1-8 and 10-13 keep their prior T3 delta. P4.4 remains Active.
>
> **P4.4-T5 update (2026-08-25) — preset provider display metadata enrichment physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/kilocode/provider/metadata.ts` (preset-only `providerMetadata` helper — `noteKey`/`icon`/`priority` mapping for preset provider IDs) and removed only its import and `metadata: providerMetadata(item.id)` response enrichment from `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts`, retaining the provider listing aggregation (`all`/`connected`/`default`/`failed`), auth/model/config dependencies (`Config.Service`, `Provider.Service`, `ProviderAuth.Service`, `ModelCache.Service`, `ModelsDev.Service`), and endpoint wiring (`list`/`auth`/`authorize`/`callback`). Deleted the direct obsolete test `packages/opencode/test/kilocode/provider-metadata.test.ts` and added focused absence/preservation regression `packages/opencode/test/kilocode/p4-4-provider-metadata-removal.test.ts` plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (managed < primary < provider < wellknown). The optional `providerMetadata` field on `Provider.Info` (`Provider` OpenAPI schema, generated SDK `types.gen.ts`) remains optional and contract-compatible when omitted; webview `webview-ui/src/components/settings/provider-catalog.ts` fallback (`synthetic` icon, priority fallback, noteKey fallback) is preserved. Generic provider adapters, `KILO_BUNDLED_PROVIDERS`, `models-api.json` (3.0 MB checked-in snapshot), provider model/catalog loaders, custom-provider save/delete/validation, `custom-provider.ts`/`provider.ts`, auth lifecycle, and HTTP/SSE/generated-SDK bridge remain untouched per LOCK-006/009 (historical `ModelCache` mention at T5 is superseded 2026-08-27 by file deletion — see P4.4 ModelCache residual removal); preset catalogs, models.dev snapshot remain open. Row 9/provider residue is updated for this preset-metadata deletion; rows 1-8 and 10-13 keep their prior T4 delta except for the provider-residue clarification. No new reconstruction-candidate entry is required: preset display metadata is an explicitly removed preset surface (LOCK-014/015 inherent permanent removal) with no displaced behavior needing evidence preservation. P4.4 remains Active.
>
> **P4.4-T6 update (2026-08-26) — preset `@kilocode/kilo-gateway` bundled provider loader physically removed.** A bounded P4.4 unit deleted the sole `KILO_BUNDLED_PROVIDERS` preset identity loader (`"@kilocode/kilo-gateway": async () => createKilo`) and its `createKilo` import plus `BundledSDK` helper from `packages/opencode/src/kilocode/provider/provider.ts`, and removed its import/spread injection (`...KILO_BUNDLED_PROVIDERS`) from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS`. Generic SDK loader map (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`, etc.), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`/`patchConfigModel`/`kiloCustomLoaders`/`patchCustomLoaderResult`, `models-api.json` (3.0 MB checked-in snapshot), `ModelsDev` catalog service (`provider/models.ts`) — `ModelCache` (`provider/model-cache.ts`) is historical at T6 and superseded 2026-08-27 by file deletion, custom-provider save/delete/validation/auth lifecycle (`kilocode/custom-provider.ts`, `custom-provider-save.ts`, `custom-provider-delete.ts`, `provider-auth-lifecycle.ts`), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) are retained per LOCK-006/009 — static source-presence preservation only; dynamic Kilo model resolution through fallback (models.dev/custom lifecycle) is intentionally not runtime-proven (historical `ModelCache` fallback at T6 superseded 2026-08-27 by file deletion) in this static unit (no network/package installation; see tracker row 9 open residue). No SDK regeneration is required (no public endpoint/OpenAPI change) and no performance claim is made (LOCK-PERF-6). Added focused static absence/preservation regression `packages/opencode/test/kilocode/p4-4-bundled-provider-loader-removal.test.ts` (source presence only; dynamic fallback intentionally unproven) plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (bundled < managed < primary < provider < wellknown). Row 9 is updated for this preset-loader deletion; rows 1-8 and 10-13 keep their prior T5 delta. P4.4 remains Active; no phase completion claimed.
>
> **P4.4-T7 update (2026-08-26) — kilo/apertis dynamic model-fetch/cache physically removed.** A bounded P4.4 unit removed the `kilo` and `apertis` dynamic fetch/injection paths and their preset-only auth/network helpers: `packages/opencode/src/provider/model-cache.ts` is historical invalidation-only `clear` at T7 and superseded 2026-08-27 by file deletion (historical stubbed `get`/`fetch`/`refresh`/`getFailure`/`failedProviders` without network — no `fetchKiloModels`, `@kilocode/kilo-gateway`, `KiloModelsService`/`kiloModelsLayer`, `APERTIS_BASE_URL`/`api.apertis.ai`, `fetchApertisModels`/`authOptions`, `KILO_API_KEY`/`APERTIS_API_KEY`, `kilocodeToken`, `HttpClient`/`FetchHttpClient`, `Auth.Service`/`Config.Service` at that date); `packages/opencode/src/provider/models.ts` removed `ModelCache`/`Config`/`Auth` dependencies and `kilo`/`apertis` provider injection (`cache.fetch`/`refresh`, `baseURL`, `KILO_OPENROUTER_BASE`, `providers.kilo`/`apertis`) — now static `Core.Service` overlay only (`core.get` with defect catch); `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` removed `ModelCache.Service` import and `failedProviders()` call — now `failed: string[] = []` with `validProviders` guard `Object.keys(item.models).length > 0 || id in connected` (failedSet removed). Deleted obsolete kilo/apertis cache tests (`model-cache-effect.test.ts`, `model-cache-org.test.ts`, `provider-list-failed-state.test.ts`, `kilo-models-401-fallback.test.ts`) and simplified `kilo-loader-auth.test.ts` to `kiloCustomLoaders` helpers only; updated `p4-4-bundled-provider-loader-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts` expectations (handler no longer contains `ModelCache.Service`, `models.ts` no longer contains `ModelCache`/`KILO_OPENROUTER_BASE`/`fetchKiloModels`). Added focused absence/preservation/runtime regression `packages/opencode/test/kilocode/p4-4-model-cache-removal.test.ts` (historical at T7: kilo/apertis network-absence, handler/models absence, `models-api.json`/`models-dev.ts`/`BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge preservation, `clear`/`get`/`failedProviders` runtime invalidation; current 2026-08-27: file absent, no production `ModelCache`/`model-cache` reference, seams removed, catalog preserved) plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (bundled < managed < model-cache < primary < provider < wellknown). Generic adapters (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache, fallback fetch, 60-minute scoped background refresh, explicit `kilo models --refresh` — `provider/models.ts` wrapper retained as static `Core` overlay), custom-provider save/delete/auth lifecycle (historical `ModelCache.clear` invalidation at T7 is superseded 2026-08-27 by file deletion), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) are retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed at T7 and `ModelCache` boundary is deleted 2026-08-27 (`bun run refresh:models` only refreshes checked-in snapshot); dynamic `kilo`/`apertis` resolution intentionally not runtime-proven beyond invalidation (no `kilo`/`apertis` network). No SDK regeneration is required (no endpoint/OpenAPI change) and no performance claim is made (LOCK-PERF-6). Row 9 is updated for this dynamic-cache deletion; rows 1-8 and 10-13 keep their prior T6 delta. P4.4 remains Active; no phase completion claimed. No new reconstruction-candidate entry is required per LOCK-014/015: kilo/apertis dynamic cache is an explicitly removed preset/org source (LOCK-006 permanent removal).
>
> **P4.4-T8 update (2026-08-26) — preset provider login selector identity physically removed.** A bounded P4.4 unit removed hardcoded preset ordering/recommendation/alias metadata from CLI provider login selectors: `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` priority map (`kilo: 0`, `anthropic: 2`, `github-copilot: 3`, `openai: 4`, `google: 5`, `openrouter: 6`, `vercel: 7`), `kilo: "recommended"` / `openai: "ChatGPT login or API key"` hints, and `kilocode_change` `codex` -> `openai` alias; `packages/opencode/src/cli/cmd/github.handler.ts` `githubInstall`/`promptProvider()` priority/recommended map (`kilo: 0`, `anthropic: 1`, `openai: 2`, `google: 3`, `hint: priority[x.id] === 0 ? "recommended"`). Deterministic generic ordering retained as alphabetical `sortBy((x) => x.name ?? x.id)` via `buildProviderOptions` (`providers.ts:236-257`) / `buildGithubProviderOptions` (`github.handler.ts:156-166`) and `matchProviderInput` (`providers.ts:258-274`) without preset identity; plugin/custom-provider auth (`handlePluginAuth`, `resolvePluginProviders`, `other` path with `Enter provider id`), generic catalog lookup (`ModelsDev.Service` with `modelsDev.get()`/`refresh`), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) retained per LOCK-006/009. Core `ModelsDev` (`packages/core/src/models-dev.ts`) and `models-api.json` (3.0 MB checked-in snapshot) retained per scope (not removed in this unit); webview `webview-ui/src/components/settings/provider-catalog.ts` Kilo fallback identity out of scope and untouched; generated SDK/OpenAPI and package APIs unchanged (no endpoint/OpenAPI change, no regeneration). Added focused regression `packages/opencode/test/kilocode/p4-4-provider-login-preset-removal.test.ts` (static absence plus executable ordering/prompt/codex/plugin dispatch via `buildProviderOptions`/`buildGithubProviderOptions`/`matchProviderInput`/`resolvePluginProviders`) plus sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (bundled < managed < model-cache < primary < provider-login < provider < wellknown). Row 9 updated for this selector-identity deletion; rows 1-8 and 10-13 keep their prior T7 delta. P4.4 remains Active; no phase completion or performance claim. No new reconstruction-candidate entry is required per LOCK-014/015: preset selector ordering/hint/alias is an explicitly removed preset identity surface (LOCK-006 permanent removal).

> **P4.4-T9 update (2026-08-26) — handwritten SDK `KILO_CONFIG_CONTENT` wrapper forwarding physically removed.** A bounded P4.4 unit physically removed the obsolete `KILO_CONFIG_CONTENT` env-forwarding machinery from the handwritten SDK server wrappers (LOCK-010 canonical file authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014 bounded removal; no performance claim per LOCK-PERF-6). Source: `packages/sdk/js/src/server.ts` and `packages/sdk/js/src/v2/server.ts` deleted `mergeConfig`, `parseExistingConfig`, `buildConfigEnv` helpers and `env: { ...process.env, KILO_CONFIG_CONTENT: buildConfigEnv(...) }` forwarding (retaining `...process.env` and `logLevel` arg passthrough, `createKiloServer`/`createKiloTui` spawning, `Config` type for `ServerOptions`/`TuiOptions`). No production call site uses the removed helpers (only the wrappers themselves). Generated SDK (`packages/sdk/js/src/gen/*`, `v2/gen/*`) and OpenAPI (`packages/sdk/openapi.json`) unchanged — verified no `KILO_CONFIG_CONTENT`/`buildConfigEnv` in generated surface; HTTP/SSE transport, custom-provider/plugin/auth, sandbox `KILO_CONFIG_CONTENT` deny list, canonical `Config.Service` loader/cutover proofs untouched. Tests: `packages/sdk/js/test/server.test.ts` obsolete `buildConfigEnv` suite removed and replaced with wrapper-absence regression (handwritten wrappers contain no merge helpers/`KILO_CONFIG_CONTENT`, `...process.env` preserved, `createKiloServer`/`createKiloTui` still exposed, generated SDK preserved); `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts` added (handwritten wrapper absence, generated SDK/OpenAPI unchanged, canonical `Config.Service` authority absence, sandbox deny preservation, custom-provider/plugin/auth preservation, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < sdk < wellknown`); retained `p4-3-cutover.test.ts`, `sandbox/policy.test.ts` cutover/deny anchors. Row delta: row 3 source/test/generated/build/inactive-proof updated to record handwritten wrapper forwarding deletion with generated-SDK/OpenAPI unchanged and sandbox deny preserved; rows 10/11 SDK wrapper residues updated to reflect forwarding deleted while `...process.env` and `logLevel` passthrough remain; rows 1-2,4-9,12-13 keep prior T8 delta. No new reconstruction-candidate entry required per LOCK-014/015: `KILO_CONFIG_CONTENT` env override is a permanent removal class (runtime §8.1) with no target reader after P4.3 cutover; handwritten forwarding is a displaced preset-env surface with no target value. P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).


> **P4.4-T12 update (2026-08-26) — dead synthetic Kilo provider factory and orphaned i18n residues physically removed.** A bounded P4.4-T12 unit deleted the dead synthetic Kilo provider factory `createKiloFallbackProvider()` and its synthetic metadata literals (`Kilo Gateway`, `settings.providers.note.kilo`, `KILO_API_KEY` env, `noteKey`/`icon`/`priority`/`custom` source) from `packages/kilo-vscode/src/shared/provider-model.ts` (now exports `KILO_PROVIDER_ID`, `KILO_AUTO`, `PROVIDER_PRIORITY`, `providerOrderIndex` plus `CUSTOM_PROVIDER_PACKAGES`/`CUSTOM_PROVIDER_PACKAGE`, `PROVIDER_ID_PATTERN`, `isCustomProviderPackage`, `parseModelString` — `createKiloFallbackProvider` and its synthetic literals deleted; no imports/types made unused beyond that deletion per scope) and removed orphaned locale keys `settings.providers.tag.gateway` and `dialog.provider.tag.recommended` from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` dictionary (20 locales) and `settings.providers.note.kilo` from every `packages/kilo-i18n/src/*.ts` dictionary (20 locales) per repository dictionary conventions — no production consumer remains for those keys after T10/T11 settings-surface synthesis/login branches were removed. Retained protected surfaces untouched: `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`/`CUSTOM_PROVIDER_PACKAGES`, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), speech-to-text Kilo guard (`availability.ts` `KILO_PROVIDER_ID`), `PROVIDER_PRIORITY`/`ModelSelector` Kilo visibility, provider auth stitching/`goToLogin`/`KILO_AUTO` fallback, persisted auth/state, backend `/provider` API, generated SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider UI, locale schema/key typing, formatting conventions. Tests: `packages/kilo-vscode/tests/unit/p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to prove factory/synthetic literal absence and orphaned locale key absence across all dictionaries (20 + 20 locales) while retaining `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex` and speech/session guards — reusing P4.4 settings-surface static test style without weakening coverage. No 13-row matrix row marked complete; P4.4 remains Active (dead-factory/i18n residue only, not full LOCK-006; LOCK-009/010/013/PERF-6 as above).

> **P4.4-T10 update (2026-08-26) — synthetic Kilo provider identity removed from settings surfaces.** A bounded P4.4 unit removed settings-side synthesis of the Kilo Gateway provider and its Kilo-specific display/login branches so provider settings render only records supplied by existing provider state (LOCK-006 bounded settings-surface removal; not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; LOCK-PERF-6 no performance claim). Source: `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` simplified `visibleConnectedIds` to identity `[...connected]` and `providersWithKiloFallback` to identity `return providers` (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` import); `provider-catalog.ts` removed Kilo-specific branches `providerIcon` `KILO_PROVIDER_ID` `validIcon("kilo")` fallback and `providerNoteKey` `settings.providers.note.kilo` and deleted `kiloFallbackProvider()` export (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` imports; retained `isPopularProvider`/`popularProviderIndex`/`sortProviders` generic sorting and `providerIcon`/`providerNoteKey` generic paths); `ProvidersTab.tsx` removed `providersWithKiloFallback` injection, `KILO_PROVIDER_ID` import, `canonicalMode` Kilo filter, `sourceTag` `settings.providers.tag.gateway`, `connectProvider` `server.goToLogin()` Kilo branch, add-list `common.signIn` Kilo label (now always `settings.providers.action.configure`), and `showAccountButton` Kilo routing (now always `false` with preserved `server.goToProfile` dead slot); `ProviderSelectDialog.tsx` removed `kiloFallbackProvider()` injection (`withKilo` synthesis), `KILO_PROVIDER_ID` import, `server.goToLogin()` Kilo branch, and `dialog.provider.tag.recommended` Kilo tag. Retained out-of-scope surfaces untouched: `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` (with `settings.providers.note.kilo` metadata), speech-to-text `availability.ts` `KILO_PROVIDER_ID` guard, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), persisted state/auth storage, backend `/provider` APIs, SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider sorting/icons/notes/configure/auth/delete flows. Tests: updated `provider-visibility.test.ts` and `provider-catalog.test.ts` to prove no synthetic injection and generic behavior; added `p4-4-settings-surface-no-synthetic-kilo.test.ts` (static absence of Kilo synthesis/login/icon/note/recommended/gateway branches in the 4 allowed files plus preservation of shared constants/speech/model fallback). No changeset per spec (internal migration). Row delta: no 13-row matrix row marked complete; this is a bounded settings-surface step, not full preset catalog/org removal.


> **P4.4-T13 update (2026-08-26) — legacy `KILO_CONFIG` TUI migration helper input physically removed.** A bounded P4.4 unit physically removed the sole legacy `KILO_CONFIG` effective-config input residue from the TUI migration helper (LOCK-010 canonical file authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014 bounded removal; no performance claim per LOCK-PERF-6). Source: `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` deleted `if (Flag.KILO_CONFIG) files.push(Flag.KILO_CONFIG)` from `opencodeFiles` (lines 148-158 scope); `Flag` import retained for `Flag.KILO_DISABLE_PROJECT_CONFIG` (still used) so no unused import remains. No `ConfigPaths`, `session/instruction`, transport/server routes, generated SDK/OpenAPI, provider code, or storage/convergence artifact was changed per scope. Retained TUI theme/keybind migration (`migrateTuiConfig`, `normalizeTui`, `backupAndStripLegacy`, `TUI_SCHEMA_URL`, theme/keybinds/tui extraction, `tui.json` materialization), `ConfigPaths` discovery (`Filesystem.findUp` `["kilo.json", "kilo.jsonc"]`, `ConfigPaths.fileInDirectory(Global.Path.config, "kilo")`, `unique` directories, `Filesystem.exists` filtering), `Flag.KILO_DISABLE_PROJECT_CONFIG` gate, `ConfigPaths` `KILO_CONFIG_DIR` profile and instruction `KILO_CONFIG_DIR` behavior (historical at T13 — instruction removed in P4.4-T17), server health, HTTP/SSE/generated SDK, provider/catalog, custom-provider lifecycle preserved. Tests: `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` added (tui-migrate absence, retained migration/canonical-loader boundaries, `ConfigPaths`/`instruction` preservation (historical at T13), generated SDK/OpenAPI unchanged, sandbox deny preservation, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < sdk < tui-migrate < wellknown`); retained `p4-3-cutover.test.ts`, `p4-3-readiness-inventory.test.ts` cutover anchors. Row delta: row 1 source/test/docs updated to record TUI migration helper `Flag.KILO_CONFIG` injection deletion with `ConfigPaths`/instruction (historical at T13) and TUI migration preservation; rows 2-13 keep prior T12 delta; row 1 remains `residual retained` (open) for remaining `ConfigPaths`/TUI residues; no row/phase/P4/LOCK-006 complete claimed.

> **P4.4-T14 update (2026-08-26) — `ConfigPaths.directories()` `.kilocode` directory targets physically removed.** A bounded P4.4 unit narrowed `packages/opencode/src/config/paths.ts:29,35` `ConfigPaths.directories()` from `targets: [".kilocode", ".kilo"]` to `targets: [".kilo"]` in both the project-ancestor walk and the home walk (LOCK-010 exactly two canonical scopes — `.kilocode` not a target canonical source; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014/015 bounded removal without shim; no performance claim per LOCK-PERF-6). Ancestor/home walk topology, `Global.Path.config`/`Global.Path.home`, `Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`, `ConfigPaths.files()`/`fileInDirectory()`, `tui.ts` filtering, plugin install, instruction profile, canonical `Config.Service`, provider/transport/storage/convergence, generated SDK/OpenAPI/source-links untouched per scope; all other `.kilocode` occurrences outside `config/paths.ts` preserved (e.g. `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` retains `[".kilocode", ".kilo"]` per scope). Tests: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:100,165` and `p4-4-tui-migrate-kilo-config-removal.test.ts:145`/`p4-4-managed-removal.test.ts:114`/`p4-4-primary-worktree-removal.test.ts:83` updated to assert `targets: [".kilo"]` while preserving `KILO_CONFIG_DIR`/`Global.Path.home`/`Global.Path.config` and non-canonical loader boundary checks. Row delta: rows 5 and 7 source/test/docs updated to record `.kilocode` target removal with residual retained; rows 1-4,6,8-13 keep prior T13 delta; no row/phase/P4.4/LOCK-006 complete claimed.

> **P4.4-T15 update (2026-08-26) — `ConfigPaths.directories()` home `Global.Path.home` `.kilo` discovery block physically removed.** A bounded P4.4 unit deleted only the home-directory `.kilo` discovery block from `packages/opencode/src/config/paths.ts:34-38` (`...(yield* afs.up({ targets: [".kilo"], start: Global.Path.home, stop: Global.Path.home }))`) (LOCK-010 exactly one canonical global root and workspace `.kilo`; arbitrary home `.kilo` discovery not target authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014/015 bounded principle-first removal without shim; no performance claim per LOCK-PERF-6). `ConfigPaths.directories()` now contains exactly one `targets: [".kilo"]` via the project-ancestor walk (`Global.Path.home` absent, `Global.Path.config`/`Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`ConfigPaths.files()`/`fileInDirectory()` preserved per scope); `tui.ts` filtering, plugin install, `session/instruction.ts` `KILO_CONFIG_DIR` profile intentionally retained/deferred per scope — not removed in this unit — canonical `Config.Service`, provider/transport/storage/convergence, generated SDK/OpenAPI/source-links unchanged; all other `.kilocode` occurrences outside `config/paths.ts` preserved (e.g. `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` retains `[".kilocode", ".kilo"]` per scope). Tests: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:97-104,154-171` and `p4-4-tui-migrate-kilo-config-removal.test.ts`/`p4-4-managed-removal.test.ts`/`p4-4-primary-worktree-removal.test.ts` now assert home walk absent (`not.toContain("Global.Path.home")`) and single `targets: [".kilo"]` while preserving `Global.Path.config`/`KILO_CONFIG_DIR`/`KILO_DISABLE_PROJECT_CONFIG`/`files`/`fileInDirectory` and instruction `KILO_CONFIG_DIR` profile. Row delta: row 7 source/test/docs updated to record home `.kilo` discovery deletion with residual retained (ancestor walk remains); rows 1-6,8-13 keep prior T14 delta; no row/phase/P4.4/LOCK-006 complete claimed. P4.4 remains Active.


> **P4.4-T16 update (2026-08-26) — `ConfigPaths.directories()` workspace-ancestor `.kilo` discovery walk and dead no-op guard/void references physically removed; temporary TUI-local legacy discovery preserved.** A bounded P4.4 unit deleted the sole remaining ancestor directory walk and dead no-op `...(!Flag.KILO_DISABLE_PROJECT_CONFIG ? [] : [])` plus `void directory`/`void worktree` from `packages/opencode/src/config/paths.ts:23-27` `ConfigPaths.directories()` (`...(!Flag.KILO_DISABLE_PROJECT_CONFIG ? yield* afs.up({ targets: [".kilo"], start: directory, stop: worktree }) : [])`) and narrowed the signature to `directories(): Effect<string[]>` (LOCK-010 target authored scopes are one global root plus `<workspaceRoot>/.kilo/` — no ancestor walk is a target configuration source; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; no performance claim per LOCK-PERF-6). `ConfigPaths.directories()` now returns only `Global.Path.config` plus optional `Flag.KILO_CONFIG_DIR` (`Global.Path.config`/`Flag.KILO_CONFIG_DIR` preserved, `Flag.KILO_DISABLE_PROJECT_CONFIG`/`void`/`afs.up`/`targets: [".kilo"]`/`start: directory`/`stop: worktree` absent, `Global.Path.home` remains absent; `ConfigPaths.files()`/`fileInDirectory()` untouched). Temporary TUI-local legacy discovery is preserved in `packages/opencode/src/cli/cmd/tui/config/tui.ts:211-225` — `yield* afs.up({ targets: [".kilocode", ".kilo"], start: ctx.directory })` gated by `Flag.KILO_DISABLE_PROJECT_CONFIG`, merged as `unique([global-filtered baseDirectories, ...legacyProjectDirs, ...KILO_CONFIG_DIR-filtered baseDirectories])` so merged order is exactly global → enabled legacy project discovery → KILO_CONFIG_DIR (last wins, preserving prior precedence); `KILO_CONFIG_DIR` instruction profile (`packages/opencode/src/session/instruction.ts` `Flag.KILO_CONFIG_DIR`/`prefer KILO_CONFIG_DIR profile`) preserved at this baseline and deferred TUI `.kilocode`/`.kilo` (`packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` `[".kilocode", ".kilo"]`, `tui.ts:211-255` gated `dirs` filter) remain deferred/intact per scope (P4.5-adjacent, not modified). `Config.Service` canonical loader (`packages/opencode/src/config/config.ts` `canonicalRoot`/`globalConfigFile`/`projectFile`), provider/transport/generated SDK/OpenAPI/source-links, sandbox, storage/convergence unchanged. Tests: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:97-108,158-182` and `p4-4-t16-tui-legacy-discovery.test.ts` (12 behavior tests: workspace/ancestor .kilo/.kilocode, global, KILO_CONFIG_DIR, disable-gating, plus precedence regressions proving KILO_CONFIG_DIR wins setting and plugin-spec/origin collisions against both .kilo and .kilocode with merged order global → legacy → KILO_CONFIG_DIR last-wins) plus `p4-4-tui-migrate-kilo-config-removal.test.ts`/`p4-4-managed-removal.test.ts`/`p4-4-primary-worktree-removal.test.ts` now assert `afs.up`/`targets: [".kilo"]` absent with `Global.Path.config`/`KILO_CONFIG_DIR`/`KILO_DISABLE_PROJECT_CONFIG`/`files`/`fileInDirectory` preserved and `Global.Path.home`/`canonicalRoot` absent as before. Row delta: row 7 source/test/docs updated to record ancestor walk deletion with residual retained (`ConfigPaths.files` ancestor walk and TUI/instruction `KILO_CONFIG_DIR`/`.kilocode` residues remain, P4.5-adjacent); row 5 source/test updated to reflect `ConfigPaths.directories()` ancestor removal; rows 1-4,6,8-13 keep prior T15 delta; no row, phase, P4.5, or P5 complete claimed. P4.4 remains Active.

> **P4.4-T17 update (2026-08-26) — `session/instruction.ts` `KILO_CONFIG_DIR` profile fallback physically removed; `ConfigPaths`/`TUI` retained.** A bounded P4.4 unit deleted the two `Flag.KILO_CONFIG_DIR` reads from `packages/opencode/src/session/instruction.ts` — `...(Flag.KILO_CONFIG_DIR ? [path.join(Flag.KILO_CONFIG_DIR, "AGENTS.md")] : [])` from `globalFiles` and `Flag.KILO_CONFIG_DIR ?? global.config` from `relative()` — now the `KILO_CONFIG_DIR` `AGENTS.md` candidate is removed while `globalFiles` retains the canonical `path.join(global.config, "AGENTS.md")` plus the conditional `path.join(global.home, ".claude", "CLAUDE.md")` candidate, and `relative()` fallback is `global.config` only when `KILO_DISABLE_PROJECT_CONFIG` is set; project instruction discovery when enabled, existing `KILO_DISABLE_PROJECT_CONFIG` gating, and trusted-origin semantics preserved; `ConfigPaths.directories()` (`Global.Path.config` plus optional `Flag.KILO_CONFIG_DIR`) and TUI precedence (global → legacy project directories → `KILO_CONFIG_DIR` last wins) retained per LOCK-001/002; `Flag.KILO_CONFIG_DIR` getter, sandbox deny, and broader P4.4/P4.5 surfaces untouched per LOCK-003/005. Tests: `packages/opencode/test/kilocode/instruction.test.ts` rewritten to prove `KILO_CONFIG_DIR` does not override canonical global `AGENTS.md` and relative instruction with project disabled resolves from `Global.Path.config` not `KILO_CONFIG_DIR`; `p4-3-cutover.test.ts` and `p4-4-tui-migrate-kilo-config-removal.test.ts` now absence-guard `instruction.ts` for `Flag.KILO_CONFIG_DIR` while affirming `ConfigPaths`/`TUI`. Row delta: row 2 source/test updated — instruction profile deleted, `ConfigPaths`/`TUI` residual retained; rows 1,3-13 keep prior T16 delta; no row/phase/P4.4 complete claimed.

> **P4.4-T18 update (2026-08-26) — `ConfigPaths.directories()` `KILO_CONFIG_DIR` import/reader physically removed; TUI-owned explicit compatibility preserved (Historical at T18 — superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30 clarification).** A bounded P4.4 unit removed the sole `KILO_CONFIG_DIR` reader from `packages/opencode/src/config/paths.ts:4,22-24` — `Flag` import deleted and `directories()` narrowed to `unique([Global.Path.config])` (LOCK-001 effective-config ownership remains canonical; no ConfigPaths fan-out; LOCK-003 Global override and sandbox deny untouched). `packages/opencode/src/cli/cmd/tui/config/tui.ts:207-221` at T18 explicitly appended `Flag.KILO_CONFIG_DIR` after global + enabled legacy dirs (`unique([...baseDirectories.filter(dir !== Flag.KILO_CONFIG_DIR), ...legacyProjectDirs, ...(Flag.KILO_CONFIG_DIR ? [Flag.KILO_CONFIG_DIR] : [])])`) preserving exact prior precedence global → legacy `.kilocode`/`.kilo` → `KILO_CONFIG_DIR` last wins, including deduplication when `Global.Path.config` equals `KILO_CONFIG_DIR` and theme/plugin-origin precedence and `KILO_DISABLE_PROJECT_CONFIG` gating. `Flag.KILO_CONFIG_DIR` getter, `Global.Path.config` override (`packages/core/src/global.ts:79`), sandbox deny-list, instruction absence, Config.Service absence, `ConfigPaths.files`/`fileInDirectory`, legacy TUI discovery targets, transport/SDK/provider surfaces untouched per LOCK-003/004. Tests: `packages/opencode/test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` added (ConfigPaths absence, TUI explicit last-wins, sole retained reader distinction, Global/sandbox preservation, precedence); `p4-3-cutover.test.ts` and `p4-4-tui-migrate-kilo-config-removal.test.ts`/`p4-4-managed-removal.test.ts`/`p4-4-primary-worktree-removal.test.ts` updated to assert `ConfigPaths` has no `Flag.KILO_CONFIG_DIR` (`return unique([Global.Path.config])`) while `tui.ts` retains `Flag.KILO_CONFIG_DIR` explicit append; `p4-4-t16-tui-legacy-discovery.test.ts` behavior still proves last-wins. Row delta: row 2 source/test updated — ConfigPaths `KILO_CONFIG_DIR` deleted, TUI retained as sole direct reader; rows 5/7 source/test updated to record ConfigPaths `KILO_CONFIG_DIR` absence with TUI legacy discovery preserved; rows 1,3-4,6,8-13 keep prior T17 delta; no row/phase/P4.4/LOCK-006 complete claimed.

> **P4.4-T19 update (2026-08-26) — cumulative T13-T18 runtime/source-removal evidence unit (no new deletion; evidence only).** A bounded P4.4 evidence unit after T18 produces reproducible proof that no removed reader initializes in the effective-config path (proven by focused tests, not by benchmark scenarios) and records descriptive backend HTTP/SSE/AppLayer runtime observations where the existing harness can produce reliable results — scenarios 6/12 are backend-only observations and do not execute TUI/ConfigPaths; focused tests, not benchmark scenarios, are the inactive-reader/compatibility proof; no causal attribution to T13-T18 or T18 is made (LOCK-001 P4.4 remains Active; LOCK-002 no P4.5/transport/provider work; LOCK-003 ConfigPaths global-only, historical T18 TUI sole explicit `KILO_CONFIG_DIR` last-wins superseded in P4.4-T27 — now no TUI `KILO_CONFIG_DIR` reader (canonical global → root direct → root `.kilo` only, see P4.4-T30 clarification); LOCK-004 Global.Path.config override and sandbox deny-list untouched (not effective source); LOCK-005 descriptive med/p95/sample/provenance, no threshold, descriptive only). Effective-config inactivity for all T13-T18 removals is runtime-proven via focused tests without weakening compatibility locks: `packages/opencode/test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` proves `ConfigPaths.directories()` is global-only even with `KILO_CONFIG_DIR` set (`[Global.Path.config]`); `packages/opencode/test/kilocode/p4-3-cutover.test.ts` proves `Config.Service` has no legacy reader anchors and nested canonical-root convergence; `packages/opencode/test/kilocode/p4-4-t16-tui-legacy-discovery.test.ts` proves TUI gated legacy discovery and `KILO_CONFIG_DIR` last-wins **historical at T16 — superseded in T27** (no TUI reader remains); `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` proves `tui-migrate.ts` has no `Flag.KILO_CONFIG`; no new focused test was added per the "only when needed" rule — all claims already have runtime/behavioral proof via focused tests. Benchmark harness selection: scenarios 6 `first-prompt` and 12 `cold-during-generation` are backend `Server.listen`/`AppLayer` HTTP/SSE observations only — they do not execute TUI/ConfigPaths and do not cover or prove reader removal; they provide descriptive startup/resource timing context only per LOCK-005 (scenario 6 observes cold boot → first text via backend HTTP/SSE + loopback LLM; scenario 12 observes cold PATCH during held generation via backend HTTP/SSE + held gate); scenarios 7,8,9,11,13 and Extension Host/private-worker tiers are deliberately not observed and not claimed — focused tests, not benchmark scenarios, are the inactive-reader/compatibility proof. Evidence: `specs/vscode-orchestrator/evidence/p4.4-t19-runtime-evidence.md` with exact six-file command (`bun test test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts test/kilocode/p4-3-cutover.test.ts test/kilocode/p4-4-t16-tui-legacy-discovery.test.ts test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts test/kilocode/p4-4-managed-removal.test.ts test/kilocode/p4-4-primary-worktree-removal.test.ts` — 54 pass / 0 fail / 493 expects) + `bun run script/p0-benchmark.ts --scenarios 6,12 --samples 5 --warmup 1 --out /tmp/p4-4-t19-backend-6-12.jsonl` (raw versioned at `specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` — 36 lines, 59430 bytes, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`, byte-identical via `cmp`), environment/provenance (`darwin/arm64/node 24.3.0/bun 1.3.14/git 98a9d1d6e6 clean`, boot `Server.listen/AppLayer` HTTP/SSE — backend observations only; does not execute TUI/ConfigPaths), samples (10 measured +2 warmup, status ok), medians/p95 (scenario 6: `totalMs` med 604 p95 629, `firstConfigLoadMs` med 2 p95 3, `firstInstanceBootstrapMs` med 3 p95 4; scenario 12: `patchMs` med 19 p95 28, `commitToConvergedMs` med 284 p95 554), drift-noted comparison vs P0 baseline `2026-08-12T06-08-27-792Z/backend.jsonl` (git 7a768502dd dirty) — descriptive only, no threshold, no causal attribution per LOCK-005, limitations (backend-harness tier only; Extension Host/private-worker/storage not observed), and explicit inactive-reader conclusion (focused tests, not benchmark scenarios) with P4.4 still Active. P4.4 remains Active; no row/phase marked complete; no production code changed except harness execution.

> **P4.4-T20 update (2026-08-26) — extension webview preset-provider priority/catalog residue physically removed (LOCK-006 bounded preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved).** A bounded P4.4 unit removes the legacy preset-provider priority/fallback ordering path from the VS Code extension webview while preserving generic provider discovery and custom-provider behavior (no P4.5/Config.Service/instruction/transport/daemon/SDK work; no URL change; no SDK regeneration). Source: `packages/kilo-vscode/src/shared/provider-model.ts:9-17,30-33` deleted `PROVIDER_PRIORITY` (`kilo`/`anthropic`/`deepseek`/`openai`/`google`/`openrouter`/`vercel`) and `providerOrderIndex` (now exports only `KILO_PROVIDER_ID`, `KILO_AUTO`, `CUSTOM_PROVIDER_PACKAGES`/`CUSTOM_PROVIDER_PACKAGE`, `PROVIDER_ID_PATTERN`, `isCustomProviderPackage`, `parseModelString`); `webview-ui/src/components/settings/provider-catalog.ts:1-10,12-22,44-50` deleted `PROVIDER_PRIORITY as FALLBACK_PROVIDER_IDS` import, `fallback` Set, `isPopularProvider`, `popularProviderIndex`, `providerOrderIndex` dependence — `sortProviders` now `a.name.localeCompare(b.name) || a.id.localeCompare(b.id)` deterministic alphabetical; `webview-ui/src/components/shared/model-selector-utils.ts:1-9,77,145-147` deleted `PROVIDER_PRIORITY as PROVIDER_ORDER` import, `providerOrderIndex` import, `export { PROVIDER_ORDER }`, `providerSortKey` — `buildModelGroups` provider-group sort now `aLabel.localeCompare(bLabel) || aId.localeCompare(bId)` (display name with id tie-break; no preset priority); `webview-ui/src/components/settings/provider-tab-helpers.ts:10,113-118` removed `isPopularProvider` import and `&& isPopularProvider(item)` filter — `buildAddList` now `sortProviders(Object.values(allProviders).filter(!configuredIds.has(id)))` generic alphabetical; `webview-ui/src/components/settings/ProviderSelectDialog.tsx:12-17,70-89` removed `isPopularProvider`/`popularProviderIndex` imports and priority `groupBy` (`recommended`/`other`) and `sortBy` rank logic and `sortGroupsBy` recommended-first — now `groupBy` single `dialog.provider.group.other`, `sortBy` alphabetical with `CUSTOM_PROVIDER_ID` first (`localeCompare` + id tie-break), `sortGroupsBy` alphabetical. Retained protected surfaces untouched: `KILO_PROVIDER_ID`/`KILO_AUTO`/`CUSTOM_PROVIDER_PACKAGES`, `PROVIDER_ID_PATTERN`/`isCustomProviderPackage`/`parseModelString`, `providerIcon`/`providerNoteKey` generic paths, `KILO_GATEWAY_ID`, `buildModelGroups` favorites/recommendedIndex/auto alphabetical, `provider-tab-helpers` Kilo reconstruction guard (`if (id === KILO_PROVIDER_ID) return undefined`) and generic custom-provider reconstruction, session `KILO_AUTO` fallback, speech-to-text Kilo guard, backend `/provider` APIs, generated SDK/OpenAPI, HTTP/SSE bridge. Tests: updated `packages/kilo-vscode/tests/unit/provider-catalog.test.ts` (sortProviders alphabetical with tie-break + generic icon/note), `model-selector-utils.test.ts` (buildModelGroups alphabetical ordering + existing helpers), `provider-tab-helpers.test.ts` (buildAddList generic alphabetical includes `groq`/`obscure` + determinism), updated `p4-4-settings-surface-no-synthetic-kilo.test.ts` to assert T20 priority-symbol absence (`PROVIDER_PRIORITY`/`providerOrderIndex`/`FALLBACK_PROVIDER_IDS`/`isPopularProvider`/`popularProviderIndex`/`PROVIDER_ORDER`/`providerSortKey` absent, `localeCompare` present, `KILO_PROVIDER_ID`/`KILO_AUTO`/`CUSTOM_PROVIDER_PACKAGES` preserved) while preserving T12 i18n/factory/speech/session checks, added `p4-4-provider-catalog-priority-removal.test.ts` (static absence across 5 scoped files, protected-symbol presence, deterministic alphabetical for `sortProviders`/`buildAddList`/`buildModelGroups`, custom-provider reconstruction preserved). No row/phase/P4.4/LOCK-006 complete claimed; P4.4 remains Active; no `PROVIDER_PRIORITY`/`isPopularProvider`/`providerOrderIndex` consumer remains in scoped extension path.

> **P4.4-T21 update (2026-08-26) — dead legacy `KILO_CONFIG`, `KILO_CONFIG_CONTENT`, `KILO_PERMISSION` Flag entries physically removed (LOCK-001 P4.4 Active; LOCK-010 KILO_CONFIG_DIR last-wins; LOCK-011 convergence; no P4.5/transport/SDK).** A bounded P4.4 unit physically removed the three dead legacy effective-config Flag entries from `packages/core/src/flag/flag.ts` — `KILO_CONFIG: process.env["KILO_CONFIG"]`, `KILO_CONFIG_CONTENT: process.env["KILO_CONFIG_CONTENT"]`, and `get KILO_PERMISSION() { return process.env["KILO_PERMISSION"] }` — after post-T20 inspection proving no production consumers (last direct Flag definitions for three source classes whose actual readers/forwarders were already removed in P4.3/T9/T13; repository independently governs compatibility). Preserved exactly `get KILO_CONFIG_DIR()`, `Flag.KILO_CONFIG_DIR ?? Path.config` in `packages/core/src/global.ts:79` (historical at T21 — TUI explicit `unique([...baseDirectories.filter(dir !== Flag.KILO_CONFIG_DIR), ...legacyProjectDirs, ...(Flag.KILO_CONFIG_DIR ? [Flag.KILO_CONFIG_DIR] : [])])` last-wins in `packages/opencode/src/cli/cmd/tui/config/tui.ts:207-221` superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30 clarification), and sandbox literal deny entries `"KILO_CONFIG"`, `"KILO_CONFIG_CONTENT"`, `"KILO_CONFIG_DIR"` in `packages/opencode/src/kilocode/sandbox/policy.ts:168-175`; `ConfigPaths.directories()` remains `unique([Global.Path.config])` global-only, extension direct diagnostic `process.env` reads untouched per scope, no SDK regeneration or fixtures changed. Tests: `packages/opencode/test/kilocode/p4-4-flag-legacy-getter-removal.test.ts` added (Flag definitions/readers absent, protected KILO_CONFIG_DIR/Global/TUI/sandbox/ConfigPaths preservation, SDK/OpenAPI unchanged, sorted `bundled < flag < managed < model-cache < primary < provider-login < provider < sdk < t16 < t18 < tui-migrate < wellknown`); updated `p4-4-sdk-config-forwarding-removal.test.ts`/`p4-4-tui-migrate-kilo-config-removal.test.ts` sorted assertions and `script/kilocode/test-profile.ts` insertion. Changeset `p44-flag-legacy-getter-removal.md` patch for `@kilocode/cli`. Row delta: rows 1,3,4 source/test updated to record Flag deletion with residual/open retained; row 2 and rows 5-13 keep prior T20 delta; no row/phase/P4.4/LOCK complete, no performance claim, no P4.5/transport/SDK expansion.

> **P4.4-T22 update (2026-08-26) — canonical-first selector readiness decoupling (LOCK-009 / R1, LOCK-012 / selector readiness, LOCK-013, LOCK-014; one bounded P4.4 unit; P4.4, P5, all rows, LOCK-009 remain open; T22 replaces abandoned metadata-removal proposal; never modifies metadata paths).** Ensure provider, agent, and configuration selectors publish from extension-owned canonical materialization/indexes without waiting for `KiloProvider.initializeConnection()` HTTP/SSE/generated-SDK connectivity or global `extensionDataReady`; HTTP/SSE/SDK remain background reconciliation; noncanonical/SDK-only categories unchanged. Source: `packages/kilo-vscode/src/KiloProvider.ts` `webviewReady` canonical publish before `syncWebviewState` and `doInitializeConnection` canonical fetches as background `void Promise.all` not blocking `extensionDataReady` (SDK-only `fetchAndSendSkills`/`fetchAndSendCommands`/`seedSessionStatusMap` still gate `extensionDataReady`); `webview-ui/src/context/provider.tsx` and `config.tsx` remove global `extensionDataReady` retry for provider/config; `webview-ui/src/context/session.tsx` removes `extensionDataReady` for agents while retaining it for SDK-only `mcpStatus`. Preserved: default noncanonical mode, `KiloConnectionService` singleton/lifecycle, `ServerManager` ownership, `SdkSSEAdapter` heartbeat/reconnect, config revision ownership, `extensionDataReady` for SDK-only domains, full fallback reconcile, canonical `ready:false` before materialization and last-known valid after. Not modified: `packages/opencode`, SDK generated files, private-worker protocol, transport endpoints, provider model metadata (`KILO_MODEL_SCHEMA_EXTENSIONS`, `patchModelsDevModel`, etc.), TUI/CLI, config-convergence. Tests: `packages/kilo-vscode/tests/unit/p4-4-canonical-first-selector-readiness.test.ts` (7 tests — canonical publish while connection unresolved/failed, webviewReady ordering, doInitializeConnection decoupling, webview no longer depends on global data-ready, noncanonical SDK bridge retained). Retained-surface: SDK/SSE bridge remains for noncanonical/background; `extensionDataReady` remains for SDK-only (skills, commands, MCP, session status). No P4.4/P5/row/LOCK completion; no performance claim (LOCK-013/LOCK-PERF-6).

> **P4.4-T23 update (2026-08-26) — obsolete preset-Kilo presence invalidation helper physically removed (LOCK-008 bounded removal; LOCK-001 P4.4 Active, P5 Not started, all rows/matrix/LOCK remain open/residual; LOCK-002/005/006/007 preserved).** Delete no-longer-owned preset `providerID === "kilo"` presence branch while preserving generic auth invalidation and KiloViewers ownership. Source: `packages/opencode/src/kilocode/server/provider-auth-lifecycle.ts` deleted `KiloViewers` import and `invalidatePresence` helper (`export const invalidatePresence = Effect.fn("KiloServer.invalidatePresence")` with `KiloViewers.Service`/`invalidateAuth()`); `packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts` removed `invalidatePresence` import and the two guarded `if (ctx.params.providerID === "kilo") yield* invalidatePresence()` calls (authSet/authRemove) while retaining `invalidateAfterProviderAuthChange` with `cleanupDisabled:true` for authSet and without for authRemove; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` removed `invalidatePresence` import and the single guarded `if (ctx.params.providerID === "kilo") yield* invalidatePresence()` call (callback) while retaining `invalidateAfterProviderAuthChange` with `cleanupDisabled:true`. Preserved: `invalidateAfterProviderAuthChange` canonical coordinator (fence/snapshot/compensation), rollback/fence and disabled-provider semantics (historical `ModelCache.clear` under fence at T23 is superseded 2026-08-27 by file deletion — no ModelCache remains) (`cleanupDisabled`, `disabled_providers`, `ConfigRollbackFailed`, `withColdMutation`), `KiloViewers` service/layer and AppLayer provisioning (`packages/opencode/src/kilocode/presence/service.ts` with `Service`/`layer`/`defaultLayer`/`update`/`invalidateAuth`/`viewed`/`EventServiceClient`; `packages/opencode/src/effect/app-runtime.ts` with `KiloViewers.defaultLayer`), all other KILO identifier logic (`KILO_PROVIDER_ID`/`KILO_AUTO`, `auth.get("kilo")`, `KILO_API_KEY`, `KILO_MODEL_SCHEMA_EXTENSIONS`, custom-provider save/delete, `models-api.json`), and existing `providerID === "kilo"` uses outside the three removed branches (none removed per LOCK-008). Not modified: `presence/service.ts`, `effect/app-runtime.ts`, session handlers, SDK/OpenAPI/generated clients, KiloProvider/webview readiness, models snapshot/catalog, global health, TUI files, or other `providerID === "kilo"` logic; no endpoint/contract/transport/daemon change (LOCK-005); no TUI migration/discovery change (LOCK-007). Tests: `packages/opencode/test/kilocode/p4-4-presence-invalidation-removal.test.ts` added (7 tests — helper/import absence in lifecycle + control/provider, no production `invalidatePresence` surface, generic lifecycle primitives preservation, KiloViewers service/layer and AppLayer preservation, other KILO identifier preservation, presence file untouched, sorted test-profile `bundled < flag < managed < model-cache < presence < primary < provider-login < provider < sdk < t16 < t18 < tui-migrate < wellknown`); retained focused suites (`provider-auth-lifecycle`, presence/service, custom-provider-save/delete) continue to pass. Row delta: row 9 provider residue updated — preset presence helper deleted with generic lifecycle preserved; rows 1-8,10-13 keep prior T22 delta; no row/phase/P4.4/LOCK complete claimed. P4.4 remains Active; no performance claim (LOCK-PERF-6).

> **P4.4-T25 update (2026-08-27) — CLI TUI provider dialog preset display residue physically removed (T25-SCOPE P4.4 CLI TUI display-only; T25-BEHAVIOR generic name+id ordering, unified Providers category, no preset Popular/recommendation/title/description — generic deterministic name+id order; LOCK-006 generic provider lifecycle/catalog/auth preserved; generic credential/error guidance retained without stale recommended copy; no P4.4/P5 completion — all remain open/residual; P5 product deletion out of scope).** `packages/opencode/src/kilocode/cli/cmd/tui/component/dialog-provider.tsx:45-56` deleted `PROVIDER_PRIORITY` (`kilo: -1`, `anthropic: 0`, `github-copilot: 1`, `openai: 2`, `google: 3`, `anaconda-desktop: 4`), `PROVIDER_DESCRIPTIONS` (`kilo: "(Recommended)"`, `anthropic: "(Claude Max or API key)"`, `openai: "(ChatGPT login or API key)"`, `anaconda-desktop: "(Local models)"`), `PROVIDER_TITLES` (`openai: "OpenAI / Codex"`) and Kilo Gateway recommended `renderApiDescription` branch (`Kilo Gateway gives you access… https://kilo.ai/gateway`) — `atomic-chat` local guidance retained (`Connect to Atomic Chat on this machine… Leave API key empty`); `LOCAL_OPTIONAL_API_KEY`/`isLocalOptionalApiKey`/`LOCAL_API_KEY_PLACEHOLDER`, `renderGutter`/`failedDescription` (`connection error — click to reconnect`), `renderAutoMethod`/`apiKeyPlaceholder`/`selectProvider` retained per LOCK-006. `packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx:21,42-54,120-132` removed `PROVIDER_PRIORITY` import/usage and preset `sortBy((x) => PROVIDER_PRIORITY[x.id] ?? 99)` with `Popular` vs `Providers` category branching and `PROVIDER_DESCRIPTIONS`/`PROVIDER_TITLES` special cases — `providerOptions` now generic deterministic `sortBy((x) => x.name.toLowerCase() + "\0" + x.id.toLowerCase())` (display name case-insensitive with id tie-break per T8/T20) and constant `category: "Providers"`; `createDialogProviderOptions` now generic `title: provider.title` and `description: failedDesc ?? provider.description` (failedDesc preserved, baseDesc/title branch removed). Preserved: `failed`/`connected`/`consoleManaged`/`onboarded` gutter, `selectProvider`/`renderAutoMethod`, `isLocalOptionalApiKey`/`apiKeyPlaceholder`/`LOCAL_API_KEY_PLACEHOLDER`, `CUSTOM_PROVIDER_OPTION_VALUE`/`CUSTOM_PROVIDER_ID`/`normalizeCustomProviderID`, `DialogProvider`/`DialogModel`/`ApiMethod`/`AutoMethod`/`CodeMethod`/`PromptsMethod`, custom-provider `Other` flow, `ModelCache.clear`/fence/rollback/disabled cleanup, `KiloViewers`/`AppLayer`/session viewer, `KILO` identifiers, provider catalog/`models-api.json`/`ModelsDev`, transport/storage/SDK/ConfigPaths/Global/sandbox per T25-SCOPE (display-only, no catalog/transport/storage change). Not modified: `packages/kilo-vscode` and completed T20 artifacts, server/API/SDK/generated files, provider packages/auth support, P5 scope. Tests: `packages/opencode/test/kilocode/p4-4-tui-provider-dialog-preset-removal.test.ts` added (kilocode Absence + generic-presence, consumer absence + generic-presence, production-source preset-symbol absence, test-profile sorted insertion `tui-migrate < tui-provider-dialog < wellknown`, executable generic ordering with name+id tie-break and Popular/category absence) plus `packages/opencode/test/cli/cmd/tui/provider-options.test.ts` retained; `packages/opencode/script/kilocode/test-profile.ts` insertion at `tui-migrate < tui-provider-dialog < wellknown` sorted; retained `p4-4-provider-login-preset-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts` T8/T20 anchors and `p4-3-cutover.test.ts`. No row or phase marked complete — rows 5/12/13 and overall P4.4/P5/matrix/LOCK remain residual/open.

> **P4.4-T26 update (2026-08-27) — CLI TUI model dialog preset recommendation/popularity presentation physically removed (T26-SCOPE P4.4 CLI TUI display-only; T26-BEHAVIOR generic provider display-name category, no Kilo Recommended / Popular providers / kiloRank — generic deterministic provider-name category; T26-SCOPE generic provider lifecycle/auth/catalog/model selection, fence/rollback/disabled cleanup/KiloViewers (historical `ModelCache` at T26 superseded 2026-08-27 by file deletion), Kilo auto routing/auth, KILO identifiers, transport/storage/SDK, provider data schema including recommendedIndex outside dialog preserved; FreeModelDisclosure, BYOK/May-train/Free footer construction, ModelInfoPanel, footer-based free-first ordering, model actions, favorites/recents, provider-scoped newest-first ordering, generic DialogProvider flow preserved; no P4.4/P5 completion — all remain open/residual; P5 product deletion out of scope; dialog-provider, Kilo auto-auth, provider catalog/schema, SDK/generated surfaces, FreeModelDisclosure/ModelInfoPanel, completion row statuses not modified).** `packages/opencode/src/cli/cmd/tui/component/dialog-model.tsx:5,8,28-33,133-138,145-171` removed `kiloRank` memo (`Map` of `recommendedIndex`), `Recommended` category/rank (`provider.id === "kilo" && info.recommendedIndex !== undefined ? "Recommended" : provider.name`), disconnected `Popular providers` synthesis (`pipe(providers(), map(category: "Popular providers"), take(6))`) and related unused imports/bindings (`take`, `createDialogProviderOptions` `providers`) — category collapsed to `connected() ? provider.name : undefined` generic provider display-name; `sortModelOptions` call narrowed to `sortModelOptions(options, props.providerID !== undefined)` and `sortModelOptions` signature narrowed to `(options, newestFirst)` removing `rank: ReadonlyMap<string, number>` and `recommended` sort key (`option.value?.providerID === "kilo" ? rank.get(...) : 0`) while retaining footer `free-first` (`(option) => option.footer === undefined`) and `newestFirst` (`releaseDate` desc + title) ordering. Preserved: `FreeModelDisclosure` (`hasByok`/`collectsData`/`byok`/`label`), `ModelInfoPanel` preview (`wide()`/`lookup`/`setPreview`), footer BYOK/May-train/Free disclosure, `favorites`/`recents` (`Favorites`/`Recent` categories, `showExtra`, deduping), provider-scoped `newestFirst` (providerOptions `sortBy` newestFirst then title), generic `DialogProvider` (`DialogProvider`/`DialogVariant`, `model.dialog.provider`/`model.dialog.favorite` actions, `useConnected`/`useSync`/`useLocal`, `skipFilter`/`onMove`/`current`), `KILO` identifiers, provider catalog/`models-api.json`/`ModelsDev`, `ModelCache`/`KiloViewers`/`AppLayer`, transport/storage/SDK/ConfigPaths/Global/sandbox per T26-SCOPE (display-only, no catalog/transport/storage change). Not modified: `dialog-provider`, Kilo auto-auth, provider catalog/schema, SDK/generated surfaces, `FreeModelDisclosure`/`ModelInfoPanel`, completion row statuses per scope. Tests: `packages/opencode/test/kilocode/p4-4-tui-model-dialog-preset-removal.test.ts` added (dialog-model absence + generic-presence, category collapse, data-layer `recommendedIndex` preservation, test-profile sorted insertion `tui-migrate < tui-model-dialog < tui-provider-dialog < wellknown`, executable generic ordering with rank independence, `FreeModelDisclosure`/`ModelInfoPanel` preservation) plus `packages/opencode/test/cli/cmd/tui/model-options.test.ts` updated for rank independence (footer free-first + newestFirst preserved, extra rank arg ignored, signature length 2); `packages/opencode/script/kilocode/test-profile.ts` insertion at `tui-migrate < tui-model-dialog < tui-provider-dialog < wellknown` sorted; retained `p4-4-tui-provider-dialog-preset-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts`/`provider-options.test.ts` and `p4-3-cutover.test.ts`. No row or phase marked complete — rows 5/12/13 and overall P4.4/P5/matrix/LOCK remain residual/open.

> **P4.4 ModelCache residual removal (2026-08-27) — ModelCache service boundary deleted (LOCK-MODELCACHE-001, LOCK-006/009 preserved).** `packages/opencode/src/provider/model-cache.ts` is physically deleted (boundary deleted, not replaced). `packages/opencode/src/effect/app-runtime.ts` no longer exposes `ModelCache`/`ModelCacheLayer` or `modelCache` seam (`buildCoreLayer` → `makeAppLayer` only), `packages/opencode/src/server/routes/instance/httpapi/server.ts` `AppOptions` no longer exposes it, and callers `packages/opencode/src/kilocode/server/provider-auth-lifecycle.ts`, `packages/opencode/src/kilocode/server/custom-provider-save.ts`, `packages/opencode/src/kilocode/server/custom-provider-delete.ts`, `packages/opencode/src/kilocode/anaconda-desktop/service.ts` no longer reference `ModelCache` (`ModelCache.clear`/`cache.clear` absent). Generic catalog `packages/opencode/src/kilocode/provider/models-api.json` (3.0 MB), `packages/core/src/models-dev.ts` (disk cache, fallback fetch, 60-minute refresh), `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS`, and `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel` remain per LOCK-006; HTTP/SSE/generated-SDK transport unchanged per LOCK-009. Evidence: `packages/opencode/test/kilocode/p4-4-model-cache-removal.test.ts` (file absent, no production `ModelCache`/`model-cache` reference, seams removed, catalog preserved, sorted test-profile `bundled < model-cache < provider`). P4.4 remains Active/residual; no row/phase closure claimed. Prior T7 (2026-08-26) description of `provider/model-cache.ts` as invalidation-only `clear` is historical and superseded by this deletion.

> **P4.4-T30 update (2026-08-27) — CLI TUI daemon and explicit-attach canonical Git worktree root (canonical file-authoritative, canonical label, no legacy fallback; supersedes residual nested-as-root on active attach lifecycles; supersedes stale T18/T19 TUI sole-reader and ConfigPaths.files API-removal claims).** Final audit found `packages/opencode/src/kilocode/cli/cmd/tui/thread.ts` `KiloTuiThreadDaemon.attach()` still called `TuiConfig.get()` with raw `process.cwd()` (via `CurrentWorkingDirectory` default) and explicit `packages/opencode/src/cli/cmd/tui/attach.ts` `AttachCommand` did the same after `chdir` — nested Git directory could therefore become effective TUI config root on daemon-attached and explicit-attach lifecycles. T30 threads the existing canonical root to `TuiConfig` in `KiloTuiThreadDaemon.attach` (`Effect.provide(TuiConfig.defaultLayer.pipe(Layer.provide(Layer.succeed(CurrentWorkingDirectory, input.cwd))))`) and resolves/provides canonical root in explicit `AttachCommand` after `chdir` using the existing minimal mechanism (`resolveWorktree`/`canonicalRoot` with `Filesystem.resolve`, non-Git fallback to directory/`process.cwd()`): `const base = directory ?? Filesystem.resolve(process.cwd()); const worktree = await resolveWorktree(base); const canonicalForConfig = canonicalRoot(base, worktree)` then `Effect.provide(... CurrentWorkingDirectory, canonicalForConfig)` for `TuiConfig.get` **(Historical T30 attach base — superseded in final-audit correction #2; current (LOCK-SOURCE) uses `selectedLocalDir ?? localBase` via `Filesystem.resolve(process.cwd())` after successful local chdir, retaining caller local root only for unavailable remote --dir)**. Preserved canonical global→root direct→root `.kilo` precedence, normal non-daemon T29 path, config-console T28 alignment, attach/daemon lifecycle behavior beyond root passed to config, no ancestor discovery/`.kilocode`/env/migration fallback, theme/global/provider/transport. **Clarification:** `ConfigPaths.files` API is retained (`files` and `fileInDirectory` remain for other consumers; only active TUI call-site `ConfigPaths.files("tui", ctx.directory)` ancestor walk removed in T27 and no active TUI call remains — TUI now uses direct `ConfigPaths.fileInDirectory(root, "tui")` and `ConfigPaths.fileInDirectory(root/.kilo, "tui")`); `ConfigPaths.directories` remains global-only `unique([Global.Path.config])`. **Supersedes** historical T18/T19 claims that TUI was the sole `KILO_CONFIG_DIR` reader with last-wins — no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate` reader remains after T27-correction; only `Global.Path.config` `Flag.KILO_CONFIG_DIR ?? Path.config` override and sandbox deny-list retained per compatibility (not effective TUI source). Regression: `packages/opencode/test/kilocode/p4-4-tui-attach-canonical.test.ts` (6 tests — daemon attach canonical root wins, daemon non-Git fallback, explicit attach canonical via shared `resolveWorktree`/`canonicalRoot` boundary, explicit non-Git fallback, static entrypoint canonical, retained `ConfigPaths.files` vs no active TUI call). Provenance now through T30 (T29 + T30); historical nested-as-root on attach lifecycles and T18/T19 sole-reader / `files` API removal claims superseded while provenance preserved; T30 attach base superseded in correction #2 but T30 daemon canonical and history preserved. No P4.4 completion claimed; P4.4 remains Active (residual).

> **Final-audit correction (2026-08-27, P4.4 — remote attach local canonical isolation, non-destructive test isolation, reload coverage):** Audit found `attach.ts` derived canonical local TUI config root from remote `--dir` when `chdir` failed, and new canonical TUI suites unlinked `${Global.Path.config}/tui.json[c]` without restoring user content, and nested config-console suite omitted POST reload. Correction preserves distinct local configuration base captured before attempting remote directory `chdir` (`const localBase = Filesystem.resolve(process.cwd())` before `process.chdir(args.dir)`; `resolveWorktree(localBase)` / `canonicalRoot(localBase, worktree)` only for config, `directory` remains session-context for `validateSession`/SDK), proves unavailable remote `--dir` cannot displace local canonical root via `p4-4-tui-attach-canonical.test.ts` remote-displacement test; replaces destructive global-config cleanup with isolated config root (`fs.mkdtemp` → `Global.Path.config = tmp` → `fs.rm(tmp)` only for test-created dirs, owned temp-dir lifecycle; safe content/existence restoration sufficient as original user file metadata never touched); updates T19/P4.3/matrix/tracker historical T18/T19 claims to superseded and states T27-T30 current behavior (canonical `resolveWorktree`/`canonicalRoot`, `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only, global → direct → `.kilo`, non-Git fallback to directory); adds nested PATCH → POST `/instance/reload` → GET coverage in default `Server.Default` app and production `Server.listen` path with owned `Listener` + `tmpdir`/`disposeAllInstances` cleanup. No source fallback, no provider/theme/global/sandbox change, no historical deletion, no untracked resources.

> **Final-audit correction #2 (2026-08-27, P4.4 — explicit attach valid local --dir, docs historical, plugin/test lifecycle):** Supersedes residual current-looking T18/T19/T27 passages and stale T30 attach description (`const base = directory ?? Filesystem.resolve(process.cwd())`) which always used pre-chdir localBase even after valid local --dir succeeded. Current (LOCK-SOURCE) explicit attach honors successfully selected local directory as local TUI configuration root (`let selectedLocalDir; try { process.chdir(args.dir); selectedLocalDir = Filesystem.resolve(process.cwd()) } catch { return args.dir } ; const configBase = selectedLocalDir ?? localBase; await resolveAttachConfigRoot(configBase)`) while unavailable remote --dir remains session-only and retains caller local root for configuration (directory forwarding unchanged, no source fallback, global -> root direct -> root .kilo precedence, non-Git fallback). Updates tracker/matrix T18/T19/T27 current-looking old behavior to explicitly historical/superseded while preserving history and P4.4 Active. For plugin-bearing focused configs uses existing `markPluginDependenciesReady` on exact loaded canonical directory or awaits `TuiConfig.waitForDependencies` before disposal to prove no plugin install outlives fixture. Replaces empty cleanup catches with failure-safe observable strategy and replaces unsafe `unknown as AttachArgs` double-cast with valid typed minimal args fixture. Removes unused production `dirs` alias constant in `cli/cmd/tui/config/tui.ts`. Preserves remote session/SDK directory forwarding, canonical precedence/non-Git fallback, config-console, no user global file mutations, no source fallback, generated artifacts/source-links, no protocol/SDK break. P4.4 remains Active.

> **P4.4-T31 update (2026-08-27) — server-side `/global/health` deprecated transport surface physically removed (LOCK-002 transport-narrowing, LOCK-003 bridge preserved).** A bounded P4.4 unit deletes the server-side deprecated health endpoint `GET /global/health` and every generated SDK/OpenAPI exposure while retaining SSE-based reconnect/liveness: `packages/opencode/src/server/routes/instance/httpapi/groups/global.ts` deletes `GlobalHealth` schema, `health: "/global/health"` path, and `global.health` endpoint (now only `/global/event`, `/global/config`, `/global/dispose`, `/global/upgrade` remain); `packages/opencode/src/server/routes/instance/httpapi/handlers/global.ts` deletes health handler, `InstallationVersion` import, and `handle("health")` wiring (retain `event`/`configGet`/`configUpdate`/`dispose`/`upgrade` plus SSE `eventResponse` with `server.connected`/`server.heartbeat` and `Stream.interruptWhen(disconnect)`); generated contracts regenerated via repo-prescribed `./script/generate.ts` (run twice idempotently) — `packages/sdk/openapi.json` no longer exposes `/global/health`/`global.health`, `packages/sdk/js/src/v2/gen/sdk.gen.ts`/`types.gen.ts` no longer expose `GlobalHealth`/`global.health`/`/global/health` while retaining `global.event`/`global.config`. Daemon liveness retargeted from authenticated `fetch .../global/health` with zod version parse to `fetch .../global/config` returning `{healthy:true, version:state.version}` and `status()` version-mismatch compares stored `state.version` vs `InstallationVersion` (probe validates liveness only) — no replacement health poll introduced (SSE `GET /global/event` heartbeat/reconnect remains sole liveness path per LOCK-002); debug workspace plugin `waitForHealth` retargeted to `/global/event` (accepts 200 or 401). Tests/docs updated: `httpapi-exercise` health scenario removed, `httpapi-instance` no longer expects `/global/health`, `serve-process` smoke now probes `/global/config`, `daemon.test` probes `/global/config`, `httpapi-sdk` uses `global.config.get`/`global.event`, `TESTING.md` curl examples and `cli-runtime.md` Health row now reference `/global/config`/`/global/event`. Evidence: `packages/opencode/test/kilocode/p4-4-global-health-removal.test.ts` (14 tests — absence across group/handler/OpenAPI/SDK/exercise/daemon/plugin plus SSE heartbeat/reconnect presence and retained `GET /api/health` v2 distinctness) and `packages/opencode/script/kilocode/test-profile.ts` sorted glob insertion `flag < global-health < managed`. P4.4 remains Active (residual transport/retained bridge/provider/storage rows stay open; no row marked complete), P4.5/P5 remain open, no phase completion or numeric performance claim (LOCK-PERF-6).

> **P4.4-T24 superseding clarification (2026-08-27, P4.4 — TUI migration helper historical, superseded by T27-T30 canonical loader):** Clarifies P4.4-T24 (bounded TUI migration source rewrite/backup removal, `backupAndStripLegacy` deletion) as retained materialization only — `migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`/`Filesystem.findUp` still present in `cli/cmd/tui/config/tui-migrate.ts` but has **no active TUI caller after P4.4-T27** (`cli/cmd/tui/config/tui.ts` no longer imports/calls `migrateTuiConfig`, no `Flag.KILO_CONFIG`/`Flag.KILO_TUI_CONFIG`/ancestor/`.kilocode` readers). Historical T24 passages that describe migration as current effective TUI source are superseded; current (T27-T30) is canonical file-authoritative loader only — `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` with precedence global → direct → `.kilo`, non-Git fallback to directory, `ConfigPaths.files` ancestor walk removed from active TUI path (LOCK-SOURCE, LOCK-R6). Rows 12/13 residues that cite "TUI migration helper remains" are retained as historical open residue (dead helper, not effective source) per P4.4 Active residual, not as current reader. No source fallback, no file deletion, no P4.4 completion claim.

> **P4.4-T13-T17/T19/T24 historical snapshot clarification and plugin-origin scope fix (2026-08-27, LOCK-SOURCE/LOCK-R6, no active legacy dual-read/import fallback):** Dated P4.4-T13 through T17, T24, and T19 passages stating legacy discovery (`yield* afs.up({ targets: [".kilocode", ".kilo"] })`, `ConfigPaths.directories()` ancestor/home walks), `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG` last-wins, or `migrateTuiConfig`/`.kilocode`/`.kilo` migration as current active TUI behavior are **historical snapshots at those dates**, explicitly superseded by T27-T30 current canonical behavior: canonical global -> root direct -> root `.kilo` only (`resolveWorktree`/`canonicalRoot` via `Filesystem.resolve`, `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")`, no ancestor walk, no `.kilocode`, no `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig` readers, LOCK-SOURCE, LOCK-R6, no active legacy dual-read/import fallback). The TUI migration helper `cli/cmd/tui/config/tui-migrate.ts` remains as **physical/dead residue with no active TUI caller after P4.4-T27** (no import/call in `cli/cmd/tui/config/tui.ts`), not current effective source. Plugin origin assembly now uses explicit source identity at merge time: global config entries are explicitly `scope: "global"` regardless of `Global.Path.config` location (even when beneath worktree), direct root and `.kilo` remain `scope: "local"`; source path, plugin directory derivation, dedup/overrides and precedence preserved (LOCK-SOURCE). P4.4 remains Active.


>>**Provenance split.** The 13-row matrix originated as a
> documentation-only residue inventory recorded against the historical working
> tree at HEAD `8167992b` on 2026-08-25 (rows 1–12 doc-only; row 13
> extension importer/Roo removal and its assertions in
> `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` were post-removal
> working-tree evidence dated 2026-08-25 from a later unit in the current
> working tree, not part of the `8167992b` baseline). The current worktree
> amends that baseline with bounded physical removals P4.4-T1b through
> P4.4-T31 (see notes above) — P4.4-T1b through P4.4-T18 plus P4.4-T19 evidence-only unit (no new deletion) plus P4.4-T20 webview preset-provider priority/catalog residue, P4.4-T21 Flag `KILO_CONFIG`/`KILO_CONFIG_CONTENT`/`KILO_PERMISSION` entries, P4.4-T22 canonical-first selector readiness decoupling, P4.4-T23 preset-Kilo presence invalidation helper, P4.4-T25 CLI TUI provider dialog preset display, P4.4-T26 CLI TUI model dialog preset recommendation/popularity presentation, and P4.4-T27 TUI canonical source removal (no `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig`/ancestor/`.kilocode` readers), P4.4-T28 config-console canonical root, P4.4-T29 standalone CLI canonical root, P4.4-T30 daemon/attach canonical root (see P4.4-T30 note — supersedes T18/T19 TUI sole-reader and `ConfigPaths.files` API-removal claims; `ConfigPaths.files` retained with no active TUI call), and P4.4-T31 server-side `/global/health` deprecated transport surface (no `GlobalHealth`/health endpoint; SDK/OpenAPI regenerated; daemon now probes `/global/config`; SSE heartbeat/reconnect retained) — including P4.4-T17 removal of the `KILO_CONFIG_DIR` profile from `packages/opencode/src/session/instruction.ts`, P4.4-T18 removal of the `Flag.KILO_CONFIG_DIR` reader from `packages/opencode/src/config/paths.ts` (ConfigPaths is now global-only; historical T18 claim that TUI explicitly appended `KILO_CONFIG_DIR` superseded in T27 — no TUI `KILO_CONFIG_DIR` reader remains), P4.4-T13 deletion of the `Flag.KILO_CONFIG` injection from `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` and its test replacement `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts`, and P4.4-T9 deletion of the handwritten SDK `KILO_CONFIG_CONTENT` wrapper forwarding and its test replacement in `packages/sdk/js/src/server.ts`, `src/v2/server.ts`, `packages/sdk/js/test/server.test.ts`, and `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts` (generated SDK/OpenAPI unchanged per LOCK-009), and the P4.4-T19 reproducible runtime proof that no removed reader initializes in the effective-config path plus descriptive backend-harness medians/p95 (`p4.4-t19-runtime-evidence.md`). Historical baseline and current-worktree amendments are not conflated; per-row/per-note deltas below record the current implemented reality.

## Purpose, scope, and non-goals

This is the first bounded P4.4 execution unit. It establishes an auditable
static matrix for every effective-config source class in runtime spec §8.1 and
maps each row to current source, test, documentation, generated-SDK, config,
i18n, build/package, and inactive-proof evidence.

This artifact also records the extension-side legacy-migration/importer and
Roo wiring removal as a distinct current working-tree implemented change, not
yet committed as of 2026-08-25. Row 13 catalogues that removal; rows 1–12 were
originally the documentation-only static inventory at the `8167992b` baseline
and have since been amended by bounded physical removals P4.4-T1b through
P4.4-T31 (including P4.4-T17 instruction `KILO_CONFIG_DIR` profile removal, P4.4-T18 ConfigPaths `KILO_CONFIG_DIR` removal — ConfigPaths is now global-only, historical T18 TUI `KILO_CONFIG_DIR` last-wins superseded in T27; no TUI `KILO_CONFIG_DIR` reader remains —, P4.4-T13 `Flag.KILO_CONFIG` TUI migration helper deletion at `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` / `p4-4-tui-migrate-kilo-config-removal.test.ts`, P4.4-T9 handwritten SDK wrapper `KILO_CONFIG_CONTENT` forwarding deletion with retained `...process.env`/`logLevel` and unchanged generated SDK/OpenAPI per LOCK-009/010/014, plus P4.4-T20 webview preset-provider priority/catalog residue, P4.4-T21 Flag `KILO_CONFIG`/`KILO_CONFIG_CONTENT`/`KILO_PERMISSION` legacy entries, P4.4-T22 canonical-first selector readiness decoupling, P4.4-T23 preset-Kilo presence invalidation helper, P4.4-T25 CLI TUI provider dialog preset display, and P4.4-T27 TUI canonical source removal / P4.4-T28 config-console / P4.4-T29 standalone / P4.4-T30 daemon-attach canonical Git worktree root (`ConfigPaths.files` API retained with no active TUI call, see P4.4-T30 clarification) / P4.4-T31 server-side `/global/health` deprecated transport surface (no `GlobalHealth`/health endpoint; SDK/OpenAPI regenerated; daemon now probes `/global/config`; SSE retained) — see notes above). This artifact does not claim completion of remaining open P4.4/P4.5 residues.

In scope:

- the exact 13-row taxonomy from
  `specs/vscode-orchestrator/runtime-and-configuration-direction.md` §8.1;
- post-P4.3 effective-config inactivity in the canonical `Config.Service` path;
- retained source inventories and non-effective consumers that must not be
  mistaken for deleted runtime/product surfaces;
- concrete open work and unknowns for later P4.4 units.

Non-goals:

- no transport narrowing, private-worker selector/startup work, or P5 work;
- no CLI/TUI/Console deletion, which remains P4.5 scope;
- no provider/preset/catalog/onboarding/org removal, which remains the separate
  LOCK-006 P4.4 residual task;
- no storage reader/writer removal;
- no edits to runtime specs, ADRs, canonical architecture docs, or the
  historical P4.3 reconciliation checklist;
- as originally recorded at the `8167992b` baseline, this
  evidence-documentation unit made no additional source-code, generated-SDK,
  config, or product implementation changes beyond the single current
  working-tree extension importer/Roo removal dated 2026-08-25 (row 13);
  the other 12 rows were then a documentation-only static inventory.
  Bounded amendments P4.4-T1b through P4.4-T26 in the current worktree
   subsequently added physical source/test deletions — including P4.4-T9
   handwritten SDK wrapper forwarding deletion (`packages/sdk/js/src/server.ts`,
   `src/v2/server.ts`, `packages/sdk/js/test/server.test.ts`,
   `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts`;
   generated SDK/OpenAPI unchanged) plus P4.4-T20 webview preset-provider priority/catalog residue, P4.4-T21 Flag `KILO_CONFIG`/`KILO_CONFIG_CONTENT`/`KILO_PERMISSION` entries, P4.4-T22 canonical-first selector readiness decoupling, and P4.4-T23 preset-Kilo presence invalidation helper, P4.4-T25 CLI TUI provider dialog preset display, and P4.4-T26 CLI TUI model dialog preset recommendation/popularity presentation (see notes above) — with per-row/per-note deltas below that
  describe current implemented reality; rows 1–12 are no longer purely
  documentation-only.

## Taxonomy and active legal source set

The taxonomy is closed by runtime spec §8.1: four retained legal effective
sources and 13 deletion classes. The active effective-config source set after
P4.3 is only:

1. the canonical global config root and its canonical files/assets;
2. `<workspaceRoot>/.kilo/` and its project-versioned files/assets;
3. VS Code SecretStorage credentials, referenced by opaque identifiers; and
4. versioned runtime defaults and safety invariants.

UI `globalState`/`workspaceState` is derived UI state, not effective-config
authority. Session/storage state and bounded approval records are operational
state, not effective-config sources. `KILO_CONFIG_DIR` instruction profile
fallback was removed from `session/instruction.ts` in P4.4-T17,
`ConfigPaths` reader was removed in P4.4-T18 (ConfigPaths is now global-only),
and TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig`/ancestor
`.kilocode`/`.kilo` readers were removed in P4.4-T27 (T27-correction) — no TUI
`KILO_CONFIG_DIR` reader remains (historical T18 sole-reader claim superseded,
see P4.4-T30 clarification); `Global.Path.config` override and sandbox
deny-list remain as compatibility/safety per LOCK-003, not an effective TUI
source.

## Static method and status vocabulary

The initial inventory was performed against the working tree at HEAD
`8167992b` on 2026-08-25. The method was:

> **Provenance note.** The `8167992b` HEAD is the historical baseline for the
> original documentation-only rows 1–12. Row 13 importer/Roo assertions and
> bounded amendments P4.4-T1b through P4.4-T26 — including P4.4-T17 instruction `KILO_CONFIG_DIR` profile removal, P4.4-T18 ConfigPaths `KILO_CONFIG_DIR` removal (ConfigPaths is now global-only; historical T18 claim that TUI explicitly appends `KILO_CONFIG_DIR` — superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30/final-audit clarification), P4.4-T13 `Flag.KILO_CONFIG` TUI migration helper deletion (`tui-migrate.ts` / `p4-4-tui-migrate-kilo-config-removal.test.ts`), P4.4-T9 handwritten SDK wrapper `KILO_CONFIG_CONTENT` forwarding deletion (source `packages/sdk/js/src/server.ts`, `src/v2/server.ts`; tests `packages/sdk/js/test/server.test.ts`, `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts`; generated SDK/OpenAPI unchanged) plus P4.4-T20 webview preset-provider priority/catalog residue, P4.4-T21 Flag `KILO_CONFIG`/`KILO_CONFIG_CONTENT`/`KILO_PERMISSION` entries, P4.4-T22 canonical-first selector readiness decoupling, P4.4-T23 preset-Kilo presence invalidation helper, P4.4-T25 CLI TUI provider dialog preset display, and P4.4-T26 CLI TUI model dialog preset recommendation/popularity presentation (see notes above) — are post-baseline current-worktree evidence with per-row/per-note deltas below. They reflect the tree after those deletions and are not part of the `8167992b` baseline. Do not reclassify post-baseline rows/amendments as `8167992b`-era claims; rows now describe current implemented reality. Current canonical TUI configuration is T27-T30/final-audit: canonical `resolveWorktree`/`canonicalRoot` via `Filesystem.resolve` (non-Git fallback to directory) with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only (global → direct → `.kilo`, no `KILO_CONFIG_DIR`/ancestor/`.kilocode` reader).

1. verify the 13 row names and dispositions against runtime spec §8.1 and the
   placeholder rows in tracker §7;
2. inspect the canonical loader and P4.3 absence anchors in
   `packages/opencode/src/config/config.ts` and
   `packages/opencode/test/kilocode/p4-3-cutover.test.ts`;
3. inspect retained diagnostic/config-console, TUI/instruction, sandbox,
   migration, SDK-wrapper, provider/catalog, i18n, and build/package paths;
4. record only path-level static evidence. No dynamic call-graph or transport
   deletion claim is inferred from a string absence.

The matrix uses these statuses:

- **effective inactive** — the P4.3 canonical-loader evidence proves that the
  source no longer affects effective `Config.Service` output; no relevant
  non-effective residue was found in the inspected paths;
- **residual retained** — effective use is inactive, but source inventory,
  diagnostic, TUI/instruction, SDK-wrapper, safety, migration, catalog, or
  transport machinery remains and is open P4.4 work;
- **unknown** — the inspected evidence does not prove either classification.

`residual retained` does not mean that the residue is an effective-config
reader. It means that physical source removal or adjacent-surface narrowing is
not complete and must not be reported as complete.

## 13-row source-removal evidence matrix

| # | Source class (runtime §8.1) | Status | Source | Tests | Docs | Generated SDK | Config | i18n | Build/package | Inactive proof |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `KILO_CONFIG` env override | residual retained; evidence date: 2026-08-26 (P4.4-T21) | deleted: `sources.ts` diagnostic listing removed (P4.4-T1b); `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts:148-158` TUI migration helper `Flag.KILO_CONFIG` injection physically deleted (P4.4-T13) — `if (Flag.KILO_CONFIG) files.push(Flag.KILO_CONFIG)` removed; `Flag` import retained for `Flag.KILO_DISABLE_PROJECT_CONFIG` (still used); `packages/core/src/flag/flag.ts` `KILO_CONFIG: process.env["KILO_CONFIG"]` Flag entry physically deleted (P4.4-T21) — no `Flag.KILO_CONFIG` definition or reader remains; `Flag.KILO_CONFIG_DIR` getter, `Global.Path.config` override (historical T18 TUI `KILO_CONFIG_DIR` last-wins claim superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30/final-audit — canonical global→root direct→root `.kilo`), sandbox deny preserved per LOCK-003/010 | test: `p4-3-cutover.test.ts:13-78`; `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` (tui-migrate absence); `packages/opencode/test/kilocode/p4-4-flag-legacy-getter-removal.test.ts` (Flag `KILO_CONFIG` definition/reader absence, `KILO_CONFIG_DIR`/`Global`/`TUI`/`sandbox`/`ConfigPaths` preservation, generated SDK/OpenAPI unchanged, sorted `bundled < flag < managed < model-cache < primary < provider-login < provider < sdk < t16 < t18 < tui-migrate < wellknown`) | doc: runtime §8.1; P4.3 cutover evidence §6; this matrix P4.4-T21 (Flag entry deleted, row remains residual/open per LOCK-001) | - | residual: TUI helper `Flag.KILO_CONFIG` injection deleted (P4.4-T13); Flag entry `KILO_CONFIG` deleted (P4.4-T21); `sources.ts` already removed (P4.4-T1b); canonical `config/config.ts` has no `Flag.KILO_CONFIG` reader; historical T18 TUI `KILO_CONFIG_DIR` sole-reader last-wins superseded (T27-correction removes all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/migrate readers) — now `Flag.KILO_CONFIG_DIR` has no effective TUI reader (T27-T30), `Global.Path.config` override and sandbox deny retained per LOCK-003 (not effective source) | - | residual retained: TUI migration helper `tui-migrate.ts` (`migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`) and `Flag.KILO_TUI_CONFIG` physically deleted 2026-08-27 (zero active callers since T27; canonical `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` global→direct→`.kilo`; `ConfigPaths.files` retained with no active TUI call); `Flag.KILO_DISABLE_PROJECT_CONFIG` gate preserved | effective inactive: `Flag.KILO_CONFIG` absent from `Flag` and `Config.Service`; no effective-config reader (T13 + T21); sandbox `KILO_CONFIG` deny remains as safety (not effective source); row remains residual/open — not complete per LOCK-001 |
| 2 | `KILO_CONFIG_DIR` env override | residual retained; evidence date: 2026-08-27 (P4.4 residual package) | deleted: `sources.ts:135-159,184-194` removed (P4.4-T1b); `packages/opencode/src/session/instruction.ts:60-88` `Flag.KILO_CONFIG_DIR` profile removed (P4.4-T17) — `...(Flag.KILO_CONFIG_DIR ? [path.join(Flag.KILO_CONFIG_DIR, "AGENTS.md")] : [])` and `Flag.KILO_CONFIG_DIR ?? global.config` deleted; `packages/opencode/src/config/paths.ts:4,22-24` `Flag.KILO_CONFIG_DIR` import/reader physically removed (P4.4-T18) — `Flag` import deleted, `directories()` now `unique([Global.Path.config])` only; historical T18 sole TUI reader superseded — P4.4-T27 (T27-correction) physically removed all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig`/`.kilocode`/ancestor readers from `cli/cmd/tui/config/tui.ts` (deleted `migrateTuiConfig` import/call, `Flag.KILO_CONFIG_DIR`/`Flag.KILO_TUI_CONFIG` env reads, `yield* afs.up({ targets: [".kilocode", ".kilo"] })` walk); **current (T27-T30)**: TUI uses canonical Git worktree root (`resolveWorktree`/`canonicalRoot` via `Filesystem.resolve`, non-Git fallback to `directory`) — `ConfigPaths.fileInDirectory(root, "tui")` and `ConfigPaths.fileInDirectory(path.join(root, ".kilo"), "tui")` only (`ConfigPaths.files` ancestor walk deleted; no active `ConfigPaths.files("tui", ctx.directory)` call remains); `kilocode/tui/config.ts` dirs `[".kilo"]` only; `Global.Path.config` override in `packages/core/src/global.ts:79` and sandbox deny-list retained per LOCK-003 (not effective TUI source); **`Flag.KILO_TUI_CONFIG` getter physically deleted (2026-08-27 residual package — `packages/core/src/flag/flag.ts` `get KILO_TUI_CONFIG()` removed, no production reader; `Flag.KILO_CONFIG_DIR` getter retained; `ConfigPaths.files` API retained with no active TUI call, T18/T19 sole-reader claims superseded)** | test: `p4-3-cutover.test.ts:152-168` (amended T27 — instruction/ConfigPaths have no `KILO_CONFIG_DIR`, TUI has no `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate`); `packages/opencode/test/kilocode/instruction.test.ts` (P4.4-T17); `packages/opencode/test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` (ConfigPaths global-only); `packages/opencode/test/kilocode/p4-4-tui-canonical-source-removal.test.ts` (TUI canonical global→root direct→root `.kilo`, no ancestor/`.kilocode`/env/migrate, canonical root via `resolveWorktree`/`canonicalRoot`); `p4-4-tui-migrate-kilo-config-removal` (amended T27) | doc: runtime §8.1; P4.3 cutover evidence §10 (amended P4.4-T30 — T18/T19 sole-reader superseded, no TUI `KILO_CONFIG_DIR` reader) | - | deleted: instruction profile removed (P4.4-T17) and ConfigPaths reader removed (P4.4-T18) and TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate`/ancestor readers removed (P4.4-T27); historical T18 sole-reader superseded — no TUI `KILO_CONFIG_DIR` reader remains; `Global.Path.config` override and sandbox deny retained per LOCK-003, not `Config.Service` authority | - | residual: no TUI `KILO_CONFIG_DIR` reader remains (T27-correction); `ConfigPaths` global-only; instruction no longer uses `KILO_CONFIG_DIR`; `Global`/`sandbox` compatibility retained per LOCK-003 (not effective source) | effective inactive: canonical loader ignores flag; instruction now also ignores `KILO_CONFIG_DIR`; `ConfigPaths` now also ignores `KILO_CONFIG_DIR` — historical T18 TUI sole-reader last-wins superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains (T27-T30 canonical global→root direct→root `.kilo`, see final-audit); `Global.Path.config` override and sandbox deny retained per LOCK-003 (not effective source) (row remains open/residual; P4.4 not complete) |
| 3 | `KILO_CONFIG_CONTENT` env override | residual retained; evidence date: 2026-08-26 (P4.4-T21) | deleted: `packages/sdk/js/src/server.ts:5-35` helpers and `KILO_CONFIG_CONTENT` env forwarding (`68-73,155-163`) and equivalent `packages/sdk/js/src/v2/server.ts` wrappers physically removed (P4.4-T9); `sources.ts:171-183` already removed (P4.4-T1b); `packages/core/src/flag/flag.ts` `KILO_CONFIG_CONTENT: process.env["KILO_CONFIG_CONTENT"]` Flag entry physically deleted (P4.4-T21) — no `Flag.KILO_CONFIG_CONTENT` definition or reader remains; `Flag.KILO_CONFIG_DIR` getter, `Global.Path.config` override (historical T18 TUI `KILO_CONFIG_DIR` last-wins claim superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30/final-audit — canonical global→root direct→root `.kilo`), sandbox deny preserved per LOCK-003/010 | test: `p4-3-cutover.test.ts:39-47`; `packages/sdk/js/test/server.test.ts` wrapper-absence regression; `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts` (handwritten wrapper absence, generated SDK/OpenAPI unchanged); `packages/opencode/test/kilocode/p4-4-flag-legacy-getter-removal.test.ts` (Flag `KILO_CONFIG_CONTENT` definition/reader absence, protected paths preservation, SDK/OpenAPI unchanged, sorted profile) | doc: runtime §8.1; P4.3 cutover evidence §6; this matrix P4.4-T21 | SDK/server-wrapper evidence (handwritten): wrappers now contain no `mergeConfig`/`parseExistingConfig`/`buildConfigEnv`/`KILO_CONFIG_CONTENT` (verified); Actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`, no `buildConfigEnv`; `packages/sdk/openapi.json` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009 HTTP/SSE bridge preserved) | residual: Flag entry `KILO_CONFIG_CONTENT` deleted (P4.4-T21); handwritten wrapper forwarding deleted (P4.4-T9); source inventory removed (P4.4-T1b); canonical `config/config.ts` has no `KILO_CONFIG_CONTENT` reader; `Flag.KILO_CONFIG_DIR`/`Global`/`TUI`/`sandbox` preserved | - | residual retained: handwritten SDK wrapper forwarding deleted; Flag entry deleted; generated SDK/OpenAPI and HTTP/SSE transport unchanged (LOCK-009) | effective inactive: `Flag.KILO_CONFIG_CONTENT` absent from `Flag` and `Config.Service`; handwritten forwarding cannot affect canonical config; sandbox `KILO_CONFIG_CONTENT` deny remains as safety (not effective source); row remains residual/open — not complete per LOCK-001 |
| 4 | `KILO_PERMISSION` env override | residual retained; evidence date: 2026-08-26 (P4.4-T21) | deleted: `sources.ts:275-291` removed (P4.4-T1b); `packages/core/src/flag/flag.ts` `get KILO_PERMISSION() { return process.env["KILO_PERMISSION"] }` Flag entry physically deleted (P4.4-T21) — no `Flag.KILO_PERMISSION` definition or reader remains; sandbox environment deny-list at `packages/opencode/src/kilocode/sandbox/policy.ts:168-175` retained with literals `"KILO_CONFIG"`, `"KILO_CONFIG_CONTENT"`, `"KILO_CONFIG_DIR"` exactly per LOCK-003; `Flag.KILO_CONFIG_DIR` getter and `Global.Path.config` override preserved | test: `p4-3-cutover.test.ts:39-51`; `packages/opencode/test/kilocode/p4-4-flag-legacy-getter-removal.test.ts` (Flag `KILO_PERMISSION` definition/reader absence, protected KILO_CONFIG_DIR/Global/TUI/sandbox preservation, SDK/OpenAPI unchanged, sorted profile); `packages/opencode/test/permission/r18-service.test.ts` (permission operational, no Flag reader) | doc: runtime §8.1; P4.3 cutover evidence §6; this matrix P4.4-T21 | - | residual: Flag entry `KILO_PERMISSION` deleted (P4.4-T21); sandbox safety deny preserved with exact literals; no `Flag.KILO_PERMISSION` effective overlay | - | - | effective inactive: `Flag.KILO_PERMISSION` absent from `Flag` and `Config.Service` (T21 + P4.3 `result.tools` absent); sandbox `KILO_PERMISSION` not an effective source — deny remains as safety; row remains residual/open — not complete per LOCK-001 |
| 5 | Legacy `opencode.*` keys, `.opencode` / `.kilocode` locations | residual retained; evidence date: 2026-08-28 (P4.4 residual package + audit correction) | deleted: `sources.ts:59-60,121-132` removed (P4.4-T1b); `packages/opencode/src/config/paths.ts:29,35` `.kilocode` targets physically removed (P4.4-T14), home `.kilo` removed (P4.4-T15), ancestor `.kilo` walk + dead guard removed (P4.4-T16), `KILO_CONFIG_DIR` removed from `ConfigPaths` (P4.4-T18) — now `unique([Global.Path.config])` only (`afs.up`/`targets: [".kilo"]`/`Global.Path.home`/`Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`void` absent; `ConfigPaths.files()`/`fileInDirectory()` retained for other consumers); **historical T18 TUI legacy-directory last-wins superseded** — P4.4-T27 (T27-correction) physically removed all TUI legacy readers (`migrateTuiConfig`, `Flag.KILO_TUI_CONFIG`, `Flag.KILO_CONFIG_DIR`, `yield* afs.up({ targets: [".kilocode", ".kilo"] })` walk, `.kilocode` handling) and narrowed TUI to canonical Git worktree root (`resolveWorktree`/`canonicalRoot`, `ConfigPaths.fileInDirectory(root, "tui")` + `ConfigPaths.fileInDirectory(path.join(root, ".kilo"), "tui")` only; `ConfigPaths.files` active call removed, `config/tui.ts:211-255` legacy merge deleted); **P4.4 residual package (2026-08-27) physically deleted dead helper `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` (no active caller since T27) and `Flag.KILO_TUI_CONFIG` getter (no production reader; canonical T27/T30 loader `resolveWorktree`/`canonicalRoot` preserved);** `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` `[".kilocode",".kilo"]` preserved per scope (P4.5-adjacent, not TUI effective source); **audit correction (2026-08-28): `packages/opencode/src/permission/evaluator.ts:208-231` `isGlobalProtectedPath` `Global.Path.config`/`~/.kilo`/`~/.config/kilo` only (no `~/.kilocode`) and lexical `.kilo` only (no `.kilocode`) without broad `.kilo/plans` exemption, `isProtectedPath` `rootFiles` `kilo.json[kilo.jsonc], AGENTS.md` only (no `opencode.*`) and workspace `.kilo/plans` exemption evaluated before global matching (workspace `.kilo/plans` exempt only when beneath workspace root, global `~/.kilo/plans` remains protected when workspace elsewhere, overlap `workspace==$HOME` remains exempt while canonical `~/.kilo` and workspace `.kilo` stay protected); `packages/opencode/src/cli/cmd/mcp.ts:405-436` `resolveConfigPath` canonical `.kilo/kilo.jsonc|json` (project) and `kilo.jsonc|json` (global) only (no `.kilocode`, no `opencode.*`) exported**; **bounded 2026-08-28: `packages/opencode/src/kilocode/config/config.ts` `detectOpencodeConfig`/`opencodeConfigNotification`/`OPENCODE_NOTIFICATION_ID`/`CONFIG_DOCS_URL`/`flagDir` param and `existsSync` deleted (`isConfigDir` remains `dir.endsWith(".kilo")` canonical only), `packages/opencode/src/kilocode/server/httpapi/handlers/kilo-gateway.ts` gateway synthetic notice and `KilocodeConfig`/`Flag`/`Instance` imports deleted (cloud `fetchKilocodeNotifications` remains; does not close P4.4/P4.5/P5)** | test: `p4-3-cutover.test.ts:97-108,161-184` now assert `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void`/`KILO_CONFIG_DIR` absent from `ConfigPaths` with `Global.Path.home` absent and `Global.Path.config` preserved (historical T18 claim of TUI `targets: [".kilocode", ".kilo"]` gated and `Flag.KILO_CONFIG_DIR` explicit last-wins — superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR`/`.kilocode`/ancestor reader remains, see P4.4-T30/final-audit canonical `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` only); `p4-4-t16-tui-legacy-discovery.test.ts` behavior (historical at T16 — superseded in T27, see T30); `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` (ConfigPaths global-only; historical T18 TUI sole-reader superseded in T27, see T30); `packages/opencode/test/kilocode/config/config.test.ts:430-502` preserved; `packages/opencode/test/kilocode/p4-4-legacy-filesystem-discovery-removal.test.ts` (static `.kilocode/.opencode` absence + canonical `.kilo` preservation + runtime `ConfigProtection`/`project-id`/`globalDirs`/`skillDirectories` + behavioral evaluator canonical `.kilo` `ask-ceiling` vs legacy `.kilocode`/`opencode.json` `ask`, `KILO_CONFIG_DIR -> Global.Path.config` live `Flag`/`Global.make()` override with env restoration, sandbox `profile` denyWrite/env, evaluator workspace `~$HOME` overlap `.kilo/plans` exemption (`isProtectedForCeiling` false vs `.kilo/foo` true + `evaluate` `ask` vs `ask-ceiling`), plus global `~/.kilo/plans` protected when workspace elsewhere vs workspace exempt (`isProtectedForCeiling` true vs false + `evaluate` `ask-ceiling` vs `ask`), MCP resolver canonical `.kilo/kilo.jsonc|json` choice vs legacy ignore with global `kilo.json` fallback and local `.kilo/kilo.json` fallback asserted) | doc: runtime §8.1; P4.3 cutover evidence §6; this matrix P4.4-T18 (historical T18 — superseded in T27, see T30; current canonical T27-T30) | - | residual: source inventory removed (P4.4-T1b); `ConfigPaths.directories()` `.kilocode` removed (P4.4-T14), home walk removed (P4.4-T15), ancestor+dead guard removed (P4.4-T16) and `KILO_CONFIG_DIR` reader removed (P4.4-T18) — now `Global.Path.config` only; historical T18 TUI-local `.kilo`/`.kilocode` with explicit `KILO_CONFIG_DIR` last-wins superseded in P4.4-T27 — no TUI `KILO_CONFIG_DIR`/`.kilocode`/ancestor reader remains (T27-T30 canonical `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only); canonical loader is `.kilo/kilo.jsonc` only | - | residual: `theme.tsx:564` `[".kilocode", ".kilo"]` preserved per scope (P4.5-adjacent, not TUI effective source); no active TUI legacy-directory reader remains (T27-T30) | effective inactive: canonical loader absence anchors cover `.opencode`, `.kilocode`, and legacy filenames; `ConfigPaths.directories()` ancestor+dead guard removal verified by absence of `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void`/`Flag.KILO_CONFIG_DIR` and `Global.Path.home` absence (P4.4-T16/T18); historical T18 TUI-local discovery with explicit `KILO_CONFIG_DIR` last-wins verified as historical (superseded in T27, see T30); current canonical TUI is T27-T30 global→root direct→root `.kilo` only |
| 6 | Global project-asset sources | residual retained; evidence date: 2026-08-25 (P4.4-T2) | residual: `sources.ts:135-159` removed (P4.4-T1b); `packages/opencode/src/kilocode/primary-worktree.ts:7-37` helper physically deleted (P4.4-T2) | test: `p4-3-cutover.test.ts:54-60`; `packages/opencode/test/kilocode/config/config.test.ts:563-590`; `p4-4-primary-worktree-removal.test.ts`; `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` | doc: runtime §8.1/R5; P4.3 cutover evidence §7 | - | residual: helper deleted (P4.4-T2); source inventory removed (P4.4-T1b); canonical project assets are restricted to `<workspaceRoot>/.kilo/` | - | - | effective inactive: `primaryPaths(` and legacy discovery are absent from `Config.Service`; canonical-root tests reject mirror/legacy values; helper file and import surface absent |
| 7 | Ancestor directory walks | residual retained; evidence date: 2026-08-26 (P4.4-T18) | residual: `packages/opencode/src/config/paths.ts:23-27` workspace-ancestor `afs.up({ targets: [".kilo"], start: directory, stop: worktree })` walk plus dead guard/void removed (P4.4-T16) — `ConfigPaths.directories()` now `unique([Global.Path.config])` only (`afs.up`/`targets: [".kilo"]`/`start: directory`/`stop: worktree`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`void`/`Flag.KILO_CONFIG_DIR` absent, `Global.Path.home` remains absent, `Global.Path.config` preserved, signature `directories(): Effect<string[]>`; `ConfigPaths.files()`/`fileInDirectory()` retained for other consumers; `.kilocode` removed P4.4-T14, home `.kilo` removed P4.4-T15; `sources.ts:121-159` removed P4.4-T1b; `session/instruction.ts` `KILO_CONFIG_DIR` removed P4.4-T17); **historical T18 TUI-local legacy discovery superseded** — P4.4-T27 (T27-correction) removed `tui.ts:207-221` `yield* afs.up({ targets: [".kilocode", ".kilo"] })` gated walk and `KILO_CONFIG_DIR` last-wins merge, `migrateTuiConfig` and `KILO_TUI_CONFIG` readers, and active `ConfigPaths.files("tui", ctx.directory)` ancestor walk — now TUI uses canonical Git worktree root (`resolveWorktree`/`canonicalRoot`) with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only; `theme.tsx:564` `[".kilocode", ".kilo"]` preserved per scope (P4.5-adjacent, not TUI effective source) | test: `p4-3-cutover.test.ts:97-108,161-184` now assert `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void`/`KILO_CONFIG_DIR` absent from `ConfigPaths` with `Global.Path.config`/`files`/`fileInDirectory` preserved (historical T18 claim of TUI `targets: [".kilocode", ".kilo"]` gated and `Flag.KILO_CONFIG_DIR` explicit last-wins — superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR`/`.kilocode`/ancestor reader remains, see P4.4-T30/final-audit); `packages/opencode/test/kilocode/config/config.test.ts:563-590`; `p4-4-t16-tui-legacy-discovery.test.ts` behavior (historical at T16 — superseded in T27, see T30); `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` (ConfigPaths global-only; historical T18 TUI sole-reader superseded in T27, see T30); `p4-4-tui-migrate-kilo-config-removal`/`p4-4-managed-removal`/`p4-4-primary-worktree-removal` now assert `ConfigPaths` global-only (historical T18 TUI gated presence superseded in T27) | doc: runtime §8.1/R5; P4.3 cutover evidence §7; this matrix P4.4-T18 (historical T18 — superseded in T27, see T30; current canonical T27-T30) | - | residual: `ConfigPaths.directories()` ancestor+dead guard removed (P4.4-T16) and `KILO_CONFIG_DIR` reader removed (P4.4-T18) — now `Global.Path.config` only; historical T18 temporary TUI-local `.kilo`/`.kilocode` discovery gated with explicit `KILO_CONFIG_DIR` last-wins superseded in P4.4-T27 — no TUI `KILO_CONFIG_DIR`/`.kilocode`/ancestor reader remains (T27-T30 canonical `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` only); `ConfigPaths.files()` ancestor walk for `tui` direct files removed (P4.4-T27, no active call); instruction `KILO_CONFIG_DIR` profile removed in P4.4-T17 (historical), `ConfigPaths` is global-only per P4.4-T18 | - | residual: no active TUI legacy-directory reader remains (T27-T30 canonical); `ConfigPaths.files` API retained with no active TUI call per P4.4-T30 clarification; `ConfigPaths.directories()` is global-only; instruction `KILO_CONFIG_DIR` removed in P4.4-T17 | effective inactive: `ConfigPaths.files`/`directories` absence anchors apply to `config/config.ts`, while retained consumers are outside effective loading; `ConfigPaths.directories()` ancestor+dead guard deletion verified by absence of `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void`/`Flag.KILO_CONFIG_DIR` and `Global.Path.home` absence (P4.4-T16/T18); historical T18 TUI-local legacy discovery verified as historical (superseded in T27, see T30); current canonical TUI is T27-T30 global→root direct→root `.kilo` only; residual retained (effective-config inactivity already established P4.3) |
| 8 | Primary-worktree mirror reads | removed (helper deleted); evidence date: 2026-08-25 (P4.4-T2) | deleted: `packages/opencode/src/kilocode/primary-worktree.ts:7-64` helper physically deleted (P4.4-T2); `test/kilocode/primary-worktree.test.ts` deleted; `script/kilocode/test-profile.ts` glob entry removed; no canonical-loader call site | test: `p4-3-cutover.test.ts:54-60`; `p4-4-primary-worktree-removal.test.ts`; `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` | doc: runtime §8.1/R5; P4.3 cutover evidence §7 | - | helper deleted; canonical `canonicalRoot` path is the effective boundary; `ConfigPaths` preserved | - | - | effective inactive: `primaryPaths(` absent from `config/config.ts`; helper file absent and no production import remains; canonical tests prove primary/mirror values do not load |
| 9 | Cloud/org/managed config sources | residual retained; evidence date: 2026-08-26 (P4.4-T23) | deleted: `sources.ts:82-95,210-271` removed (P4.4-T1b); `packages/opencode/src/config/managed.ts` helper physically deleted (P4.4-T3) — `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences` absent; CLI `.well-known/opencode` provider-auth discovery URL-login branch deleted at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`login [url]` positional and `args.url` `fetch`/`Process.spawn`/`wellknown` persistence) (P4.4-T4) — `args.url` fetch, `Process.spawn`, `wellknown` persistence removed; `login [url]` → `login`; preset provider display metadata helper `packages/opencode/src/kilocode/provider/metadata.ts` physically deleted (P4.4-T5) — `providerMetadata` preset `noteKey`/`icon`/`priority` mapping absent; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` import and `metadata: providerMetadata(item.id)` enrichment removed (P4.4-T5) — listing now `all: Object.values(validProviders).map(Provider.toPublicInfo)` without preset enrichment; preset `@kilocode/kilo-gateway` bundled provider loader `KILO_BUNDLED_PROVIDERS` (`"@kilocode/kilo-gateway": async () => createKilo`) deleted from `packages/opencode/src/kilocode/provider/provider.ts` and its `...KILO_BUNDLED_PROVIDERS` spread removed from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS` (P4.4-T6) — generic SDK loaders (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`, etc.), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache, fallback fetch, background refresh, explicit CLI refresh; `bun run refresh:models` only refreshes checked-in snapshot), custom-provider/auth lifecycle, and `/provider` bridge retained per LOCK-006/009 (`ModelCache` `clear` is historical at T6/T7 and superseded 2026-08-27 by file deletion) — only `kilo`/`apertis` dynamic fetching was removed; `ModelCache` service boundary deleted; dynamic `kilo`/`apertis` model resolution through removed injection intentionally not runtime-proven in this static unit; kilo/apertis dynamic fetch/cache physically removed (P4.4-T7) — `provider/model-cache.ts` simplified to invalidation-only `clear` (stubbed `get`/`fetch` without network; `fetchKiloModels`/`KiloModelsService`/`APERTIS_BASE_URL`/`fetchApertisModels`/`authOptions`/`HttpClient`/`KILO_API_KEY` deleted), `provider/models.ts` removed `ModelCache`/`Config`/`Auth`/`kilo`/`apertis` injection (`cache.fetch`/`KILO_OPENROUTER_BASE`/`providers.kilo`/`apertis`) — static `Core` overlay only, handler removed `ModelCache.Service`/`failedProviders()` — `failed: string[] = []` with `validProviders` guard without failedSet; committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache, fallback fetch, 60-minute background refresh, explicit `kilo models --refresh`; `bun run refresh:models` only refreshes checked-in snapshot) plus static `Core` wrapper, `BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed; CLI preset provider login selector identity physically removed (P4.4-T8) — `packages/opencode/src/cli/cmd/providers.ts` priority map (`kilo: 0`, `anthropic: 2`, `github-copilot: 3`, `openai: 4`, `google: 5`, `openrouter: 6`, `vercel: 7`), `kilo`/`openai` hints, `codex` -> `openai` alias removed; `packages/opencode/src/cli/cmd/github.handler.ts` priority/recommended map (`kilo: 0`, `anthropic: 1`, `openai: 2`, `google: 3`) removed; deterministic generic ordering retained as `sortBy((x) => x.name ?? x.id)` without preset identity; plugin/custom-provider auth, `ModelsDev.Service`, provider HTTP/SSE bridge, Core `ModelsDev`, `models-api.json` retained per LOCK-006/009 — `models-api.json` 3.0 MB committed build-time snapshot and generic `Core.ModelsDev` runtime (disk cache, fallback fetch, 60-minute background refresh, explicit `kilo models --refresh`; `bun run refresh:models` only refreshes checked-in snapshot) remain open P4.4 residues; dead synthetic Kilo provider factory `createKiloFallbackProvider` and synthetic literals `Kilo Gateway`/`settings.providers.note.kilo`/`KILO_API_KEY`/`noteKey`/`icon`/`priority`/`custom` deleted from `packages/kilo-vscode/src/shared/provider-model.ts` — file now exports `KILO_PROVIDER_ID`, `KILO_AUTO`, `PROVIDER_PRIORITY`, `providerOrderIndex` plus `CUSTOM_PROVIDER_PACKAGES`/`PROVIDER_ID_PATTERN`/`parseModelString`/`isCustomProviderPackage` with no unused imports/types beyond that deletion (P4.4-T12); orphaned locale keys `settings.providers.tag.gateway`/`dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` (20 locales) and `settings.providers.note.kilo` from every `packages/kilo-i18n/src/*.ts` (20 locales) — retained `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`, session `KILO_AUTO` fallback, speech guard, generic custom-provider UI per LOCK-006/009/010; config-console no longer reads the inventory (handler call sites removed P4.4-T1b); extension webview preset-provider priority/catalog residue physically removed (P4.4-T20) — `packages/kilo-vscode/src/shared/provider-model.ts:9-17,30-33` deleted `PROVIDER_PRIORITY` (`kilo`/`anthropic`/`deepseek`/`openai`/`google`/`openrouter`/`vercel`) and `providerOrderIndex` (now exports only `KILO_PROVIDER_ID`, `KILO_AUTO`, `CUSTOM_PROVIDER_PACKAGES`/`CUSTOM_PROVIDER_PACKAGE`, `PROVIDER_ID_PATTERN`, `isCustomProviderPackage`, `parseModelString`); `webview-ui/src/components/settings/provider-catalog.ts:1-10,12-22,44-50` deleted `PROVIDER_PRIORITY as FALLBACK_PROVIDER_IDS` import, `fallback` Set, `isPopularProvider`, `popularProviderIndex`, `providerOrderIndex` dependence — `sortProviders` now `a.name.localeCompare(b.name) || a.id.localeCompare(b.id)` deterministic alphabetical; `webview-ui/src/components/shared/model-selector-utils.ts:1-9,77,145-147` deleted `PROVIDER_PRIORITY as PROVIDER_ORDER` import, `providerOrderIndex` import, `export { PROVIDER_ORDER }`, `providerSortKey` — `buildModelGroups` provider-group sort now `aLabel.localeCompare(bLabel) || aId.localeCompare(bId)` alphabetical by display name with id tie-break; `webview-ui/src/components/settings/provider-tab-helpers.ts:10,113-118` removed `isPopularProvider` import and `&& isPopularProvider(item)` filter — `buildAddList` now `sortProviders(Object.values(allProviders).filter(!configuredIds.has(id)))` generic alphabetical; `webview-ui/src/components/settings/ProviderSelectDialog.tsx:12-17,70-89` removed `isPopularProvider`/`popularProviderIndex` imports and priority `groupBy`/`sortBy`/`sortGroupsBy` — now single `dialog.provider.group.other` alphabetical with `CUSTOM_PROVIDER_ID` first; generic provider discovery, custom-provider save/delete/validation, provider model/catalog loaders, auth lifecycle, and HTTP/SSE/generated-SDK bridge retained per LOCK-006/009 (still open/residual; no row/phase complete) | test: `packages/opencode/test/kilocode/p4-4-presence-invalidation-removal.test.ts` (preset presence helper/branch absence with generic `invalidateAfterProviderAuthChange`/`ModelCache.clear`/KiloViewers preserved per LOCK-008); `p4-3-cutover.test.ts:35-38` (canonical-loader absence); `p4-4-managed-removal.test.ts` (helper file/import-surface absence, direct-test removal, canonical-loader preservation); `p4-4-wellknown-provider-auth-removal.test.ts` (CLI well-known discovery branch absence, stale config.test helpers removed, retained generic provider-login guarded); `p4-4-provider-metadata-removal.test.ts` (preset helper file/import-surface absence, handler enrichment absence, `all`/`connected`/`default`/`failed` aggregation and auth/model/config dependency preservation, `Provider.Info` optional `metadata` contract unchanged, generated OpenAPI/SDK unchanged, generic adapters/custom-provider/bridge preservation, sorted test-profile `managed < primary < provider < wellknown`); `p4-4-bundled-provider-loader-removal.test.ts` (static source presence only — preset loader `KILO_BUNDLED_PROVIDERS`/`createKilo`/`"@kilocode/kilo-gateway"` loader absence, `BUNDLED_PROVIDERS` generic adapters/`models-api.json`/`ModelsDev`/`ModelCache`/custom-provider/`/provider` bridge static preservation, dynamic fallback intentionally unproven, sorted test-profile `bundled < managed < primary < provider < wellknown`); `p4-4-model-cache-removal.test.ts` (kilo/apertis network-absence, handler/models absence, `models-api.json`/`models-dev.ts`/`BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge preservation, `clear`/`get`/`failedProviders` runtime invalidation, sorted test-profile `bundled < managed < model-cache < primary < provider < wellknown`); `p4-4-provider-login-preset-removal.test.ts` (preset priority/hint/codex alias absence in `cli/cmd/providers.ts` and `cli/cmd/github.handler.ts`, deterministic generic `sortBy((x) => x.name ?? x.id)` ordering, `ModelsDev.Service`/plugin/custom-provider/`other` path and `/provider` bridge preservation, Core `ModelsDev`/`models-api.json` retained, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < wellknown`); P4.4-T20: `packages/kilo-vscode/tests/unit/model-grouping.test.ts` alphabetical `Anthropic < Kilo Gateway < OpenAI` (`aLabel.localeCompare(bLabel) || aId.localeCompare(bId)`), `p4-4-provider-metadata-removal.test.ts` updated to assert `provider-catalog.ts` generic `icon`/`noteKey`/`synthetic`/`validIcon` preservation plus `FALLBACK_PROVIDER_IDS`/`PROVIDER_PRIORITY`/`provider.metadata?.priority`/`isPopularProvider`/`providerOrderIndex` absence and alphabetical `sortProviders` — generic/custom discovery retained, no preset priority | doc: runtime §8.1; P4.3 cutover evidence §6 | - | helper deleted (P4.4-T3); source inventory removed (P4.4-T1b); CLI well-known discovery deleted (P4.4-T4); preset metadata enrichment deleted (P4.4-T5) — `Provider.Info` optional `metadata` remains contract-compatible when omitted; config-console source reporting removed; canonical loader has no managed/org read | orphaned locale keys `settings.providers.tag.gateway`/`dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` (20 locales) and `settings.providers.note.kilo` removed from every `packages/kilo-i18n/src/*.ts` (20 locales) (P4.4-T12) — no production consumer; other provider/org/catalog labels remain residual but not proof of an effective reader; `Auth.WellKnown` storage type remains in `packages/opencode/src/auth/index.ts` for persisted credential compatibility; `KILO_BUNDLED_PROVIDERS`, `models-api.json`, `models.dev` snapshot, `ModelCache`, and `providerMetadata` schema optionality remain open P4.4 residues (LOCK-006) | deleted: CLI `.well-known/opencode` fetch at `packages/opencode/src/cli/cmd/providers.ts:327` removed (P4.4-T4); preset display metadata enrichment at `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts:85` removed (P4.4-T5) — `Provider.toPublicInfo` without preset enrichment; CLI preset selector priority/hint/alias at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`buildProviderOptions` `providers.ts:236-257` / `matchProviderInput` `providers.ts:258-274`) and `cli/cmd/github.handler.ts` `githubInstall`/`promptProvider()` (`buildGithubProviderOptions` `github.handler.ts:156-166` / `promptProvider()` `github.handler.ts:240-250`) removed (P4.4-T8) — deterministic generic `sortBy((x) => x.name ?? x.id)` ordering; no public server endpoint or generated SDK/OpenAPI regeneration required (LOCK-009 bridge preserved; optional `metadata` field unchanged; `models-api.json` retained) | effective inactive: managed/cloud anchors are absent from `Config.Service`; CLI well-known provider discovery is absent from `cli/cmd/providers.ts`; preset provider display metadata enrichment is absent from the provider handler (`providerMetadata` import and call absent; listing is `Provider.toPublicInfo` only); `effective` is `config.get()`; managed helper and preset metadata helper files absent and no production import remains; provider endpoint still serves `all`/`connected`/`default`/`failed` via generic/custom-provider paths; CLI preset selector ordering/hint/alias is absent from `cli/cmd/providers.ts` (`priority` map, `kilo`/`openai` hints, `codex` alias absent; ordering is `sortBy((x) => x.name ?? x.id)`) and `cli/cmd/github.handler.ts` (`priority`/`recommended` absent; ordering is `sortBy((x) => x.name ?? x.id)`); generic `ModelsDev.Service`/plugin/custom paths and HTTP/SSE bridge remain; Core `ModelsDev`/`models-api.json` retained (preset selector removal only); canonical tests prove managed values do not load; extension webview preset priority/catalog removed (P4.4-T20) — `PROVIDER_PRIORITY`/`providerOrderIndex`/`FALLBACK_PROVIDER_IDS`/`isPopularProvider`/`popularProviderIndex`/`providerOrderIndex`/`providerSortKey`/`PROVIDER_ORDER` absent from `provider-model.ts`/`provider-catalog.ts`/`model-selector-utils.ts`/`provider-tab-helpers.ts`/`ProviderSelectDialog.tsx`; `sortProviders` is `a.name.localeCompare(b.name) || a.id.localeCompare(b.id)` and `buildModelGroups` is `aLabel.localeCompare(bLabel) || aId.localeCompare(bId)` deterministic alphabetical; generic provider discovery and custom-provider lifecycle retained; still open/residual, P4.4 Active, no row/phase complete |
| 10 | Top-level `mode`/`tools` conversions | residual retained; evidence date: 2026-08-26 (P4.4-T9) | residual: no current `Config.Service` conversion found; `sources.ts` diagnostic taxonomy removed (P4.4-T1b); handwritten SDK wrapper `mergeConfig` that preserved `mode`/plugin/config for child env deleted (P4.4-T9) — wrappers now retain only `...process.env` and `logLevel` arg passthrough | test: exact P4.3 absence assertions in `packages/opencode/test/kilocode/p4-3-cutover.test.ts:48-51`; `p4-4-sdk-config-forwarding-removal.test.ts` verifies no `mergeConfig`/`mode` forwarding residue in wrappers and `logLevel` passthrough preserved | doc: runtime §8.1; P4.3 cutover evidence §7 | SDK/server-wrapper evidence (handwritten): wrappers now contain no `mergeConfig`/`mode` preservation for child env (verified); Actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009) | residual: no effective top-level conversion in canonical config; handwritten wrapper forwarding deleted; typed assets are canonical | - | removed: handwritten SDK wrapper no longer merges `mode`/plugin/config via `KILO_CONFIG_CONTENT`; package path remains with `...process.env` | effective inactive: `result.mode`, `result.tools`, and `loadMode` conversion anchors are absent from canonical config/agent paths; wrapper no longer forwards `mode` via env |
| 11 | Arbitrary CLI/env override layers | residual retained; evidence date: 2026-08-26 (P4.4-T9) | residual: sandbox safety environment policy at `packages/opencode/src/kilocode/sandbox/policy.ts:168-181` (retained; `KILO_CONFIG_CONTENT` deny preserved); handwritten SDK wrapper `KILO_CONFIG_CONTENT` env construction at `packages/sdk/js/src/v2/server.ts:68-74,155-163` and `src/server.ts` equivalent deleted (P4.4-T9) — wrappers now retain only `env: { ...process.env }` and `logLevel` arg | test: `p4-3-cutover.test.ts:39-51`; `p4-4-sdk-config-forwarding-removal.test.ts` verifies wrapper forwarding deleted and sandbox deny preserved; `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts:168-190` | doc: runtime §8.1; P4.3 cutover evidence §7 | SDK/server-wrapper evidence (handwritten): wrapper now forwards only `...process.env` without `KILO_CONFIG_CONTENT` (verified); actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009) | residual: safety policy remains (environment deny `KILO_CONFIG_CONTENT`/`KILO_CONFIG`/`KILO_CONFIG_DIR` preserved, not an effective-config overlay); handwritten wrapper env forwarding deleted | - | removed: handwritten SDK wrapper `KILO_CONFIG_CONTENT` forwarding deleted; SDK/server bridge machinery remains for transport narrowing with `...process.env` | effective inactive: no generic override layer is applied by `Config.Service`; sandbox denial remains as safety but is not an effective source read; diagnostic SDK wrapper forwarding deleted |
| 12 | Legacy global config filenames/readers | residual retained; evidence date: 2026-08-27 (P4.4 residual package) | deleted: `sources.ts:59-60,99-105` filename inventory removed (P4.4-T1b); **TUI migration helper `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` physically deleted (2026-08-27 residual package — had zero active TUI callers since T27); `Flag.KILO_TUI_CONFIG` getter physically deleted from `packages/core/src/flag/flag.ts` (no production reader);** canonical TUI loader is `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only (global → direct → `.kilo`, LOCK-SOURCE, LOCK-R6); legacy discovery/`KILO_TUI_CONFIG`/migrate readers absent per T27/T30 and residual package | test: `p4-3-cutover.test.ts:20-27,64-78` (canonical-only, no legacy filenames); `packages/opencode/test/kilocode/config/config.test.ts:610-667`; **residual package: `p4-4-tui-migrate-kilo-config-removal.test.ts` (helper file absent + Flag absent + canonical preserved), `p4-4-flag-legacy-getter-removal.test.ts` (KILO_TUI_CONFIG absent), `p4-4-tui-canonical-source-removal.test.ts` (migrate absent, Flag absent)** | doc: runtime §8.1; P4.3 cutover evidence §6; this matrix P4.4 residual package (helper + getter deleted; row remains residual/open per LOCK-001, no broader completion) | - | deleted: TUI migration helper file and `Flag.KILO_TUI_CONFIG` getter deleted (2026-08-27); source filename inventory removed (P4.4-T1b); canonical stamp/loader is `kilo.jsonc` only | - | residual: TUI/diagnostic dead helper removed; `ConfigPaths.files`/`theme.tsx` preserved per LOCK-003 (not TUI source) | effective inactive: all legacy filename/readers are absent from `config/config.ts`; `KilocodeGlobalConfigStamp` is canonical-only; TUI migration helper no longer exists on disk and `Flag.KILO_TUI_CONFIG` has no production reader (row remains residual/open per LOCK-001) |
| 13 | Legacy migration readers/import tooling | residual retained; evidence date: 2026-08-27 (P4.4 residual package) | deleted: extension legacy-migration and Roo import source removed (pre-2026-08-25); **TUI migration helper `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` physically deleted (2026-08-27 residual package — had zero active callers since T27; `migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`/`Filesystem.findUp` removed); `Flag.KILO_TUI_CONFIG` getter physically deleted from `packages/core/src/flag/flag.ts` (no production reader);** current canonical TUI loader is `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only (global → direct → `.kilo`, LOCK-SOURCE, LOCK-R6); legacy discovery/`KILO_TUI_CONFIG`/migrate readers absent per T27/T30 and residual package | test: `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts:420-444` (extension importer absent); `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts:107-114`; **residual package: `p4-4-tui-migrate-kilo-config-removal.test.ts` (helper file absent + Flag absent + canonical preserved), `p4-4-flag-legacy-getter-removal.test.ts` (KILO_TUI_CONFIG absent), `p4-4-tui-canonical-source-removal.test.ts` (migrate absent, Flag absent)** | doc: runtime §8.1; `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md:264-266`; this matrix P4.4 residual package (helper + getter deleted; row remains residual/open per LOCK-001, no broader completion) | - | deleted: TUI migration helper file and `Flag.KILO_TUI_CONFIG` getter deleted (2026-08-27); extension importer/Roo already removed | residual: TUI migration dead surface removed; `ConfigPaths.files`/`theme.tsx` preserved per LOCK-003 (not TUI source) | extension importer/Roo wiring is removed and cannot read legacy data or write canonical config; CLI/TUI migration helper file is physically removed (not just dead) and `Flag.KILO_TUI_CONFIG` has no production reader — no active TUI migration import remains. Row remains open/residual per LOCK-001 because broader migration/import tooling surface and P4.5-adjacent TUI/theme residues remain; no P4.4 phase completion claimed |

## Residual machinery and open P4.4 work

### `KilocodeConfigSources` and config-console

The legacy `KilocodeConfigSources` diagnostic inventory reader
(`packages/opencode/src/kilocode/config/sources.ts`, the 12-source listing of
env, legacy filename, project, cloud, managed, and runtime sources) was
**physically removed on 2026-08-25 (P4.4-T1b)**. The `KilocodeConfigSources`
type/echo is gone from `packages/opencode/src/kilocode/config/overlay.ts`
(`Result`/`Input`/`resolve` no longer carry `sources`), and the config-console
handler
(`packages/opencode/src/kilocode/server/httpapi/handlers/config-console.ts`)
no longer acquires auth/account state for an inventory and no longer exposes a
`sources` handler. The `/config/sources` schema/path/endpoint/handler and the
`ConfigSourcesResponse` schema were removed from the config-console group, and
the `sources` echo field was removed from `ConfigOverlayResponse`. The
config-console handler's effective value remains `config.get()`; the retained
overlay resolution is unchanged. No replacement shim or import/migration
behavior was added (LOCK-014/015); the behavior is recorded as a dropped
reconstruction candidate. This unit does not remove the remaining config-console
bridge endpoints (`/config/overlay`, `/config/effective`, `/config/transaction`,
rules, model-state, TUI config/keybinds).

### Canonical merge versus the deleted multi-source merge

P4.3 deleted the legacy reader fan-out, migration readers, and `loadMode` path
from retained effective-config loading. The canonical loader and canonical
overlay now use the global/project `kilo.jsonc` roots and typed assets. P4.4-T9
physically removed the handwritten SDK `mergeConfig`/`parseExistingConfig`/
`buildConfigEnv` child-process helper and its `KILO_CONFIG_CONTENT` env
forwarding from `packages/sdk/js/src/server.ts` and
`packages/sdk/js/src/v2/server.ts` (retaining only `...process.env` and
`logLevel` arg passthrough per LOCK-009/010/014; generated SDK/OpenAPI and
HTTP/SSE bridge unchanged). The diagnostic overlay and TUI readers remain
separately inventoried and are not claimed gone. No 12-source effective merge
is treated as active based solely on those retained residues.

### Convergence

`packages/opencode/src/kilocode/server/config-convergence.ts:1-43` remains the
active canonical save/convergence coordinator. This unit records its presence
as residual P4.4 machinery only; it does not decide or implement any narrowing
of convergence, admission fences, disposal, or boot behavior.

### Provider catalog and LOCK-006

Preset/provider catalog, onboarding, and organization residues remain a separate
P4.4 task under LOCK-006. Existing provider catalog and organization/i18n
surfaces are not counted as removed by this matrix except for the bounded
P4.4-T5 preset display metadata enrichment removal (`kilocode/provider/metadata.ts`
and its handler `metadata: providerMetadata(item.id)` enrichment deleted;
optional `Provider.Info` `metadata` field and `Provider` OpenAPI/SDK schema remain
optional/contract-compatible, webview `provider-catalog.ts` fallback preserved)
and the bounded P4.4-T6 preset `@kilocode/kilo-gateway` bundled provider loader
removal (`KILO_BUNDLED_PROVIDERS` ` "@kilocode/kilo-gateway": async () => createKilo`
and its `...KILO_BUNDLED_PROVIDERS` spread from `provider/provider.ts`
`BUNDLED_PROVIDERS` deleted; `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`,
generic adapters, `models-api.json` 3.0 MB committed build-time snapshot, `models.dev`
catalog, custom-provider save/delete/validation, and auth lifecycle
remain open per LOCK-006/009; `ModelCache` deleted 2026-08-27) and the bounded P4.4-T7 kilo/apertis dynamic model-fetch/cache removal (`provider/model-cache.ts` is historical invalidation-only `clear` at T7 and superseded 2026-08-27 by file deletion — `fetchKiloModels`/`KiloModelsService`/`kiloModelsLayer`/`APERTIS_BASE_URL`/`api.apertis.ai`/`fetchApertisModels`/`authOptions`/`KILO_API_KEY`/`APERTIS_API_KEY`/`kilocodeToken`/`HttpClient` deleted at that later date; `provider/models.ts` removed `ModelCache`/`Config`/`Auth` and `kilo`/`apertis` injection — now static `Core` wrapper only; handler `ModelCache.Service`/`failedProviders()` removed — `failed: []`; retained generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with on-disk cache at `${cache}/models.json`, fallback `GET ${source}/api.json` when cache and snapshot are both unavailable, 5-minute TTL, 60-minute scoped background refresh, explicit `kilo models --refresh` / `ModelsDev.refresh`; `models-api.json` is a build-time snapshot and `bun run refresh:models` only refreshes the checked-in snapshot and is not the sole runtime refresh) plus `BUNDLED_PROVIDERS` generic adapters/`KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`/bridge retained per LOCK-006/009 (`ModelCache` `clear` is historical and superseded by file deletion) — only `kilo`/`apertis` dynamic fetching was removed and `ModelCache` boundary is deleted not replaced) and the bounded P4.4-T8 preset provider login selector identity removal, bounded P4.4-T10–T12 synthetic Kilo settings-surface/factory/i18n residue removals, and bounded P4.4-T23 obsolete preset-Kilo presence invalidation helper removal (`kilocode/server/provider-auth-lifecycle.ts` `KiloViewers` import and `invalidatePresence` helper deleted; `server/routes/instance/httpapi/handlers/control.ts` `invalidatePresence` import and two `providerID === "kilo"` calls deleted; `server/routes/instance/httpapi/handlers/provider.ts` `invalidatePresence` import and single `providerID === "kilo"` call deleted — `invalidateAfterProviderAuthChange`/`ModelCache.clear`/rollback/fence/`disabled_providers`/`KiloViewers` service/layer and AppLayer provisioning preserved per LOCK-008) and bounded P4.4-T20 preset priority/popularity fallback removal (webview `provider-catalog.ts`/`model-selector-utils.ts`/`provider-tab-helpers.ts`/`ProviderSelectDialog.tsx` preset `PROVIDER_PRIORITY`/`providerOrderIndex`/`isPopularProvider`/`FALLBACK_PROVIDER_IDS` ordering removed; generic `icon`/`noteKey`/`synthetic` fallback and alphabetical sorting plus custom-provider behavior preserved per LOCK-006) — full provider catalog removal remains open and is not claimed complete. The matrix row for cloud/org/
managed sources records only effective-config inactivity and the retained
diagnostic/provider residue, now updated through the T23 obsolete preset-Kilo presence helper deletion (plus prior T20 bounded preset priority/popularity fallback deletion).

### Backend fetch and public transport chain

The SDK/server wrappers still launch `kilo` with `...process.env` and `logLevel` arg passthrough but no longer forward `KILO_CONFIG_CONTENT` (`packages/sdk/js/src/v2/server.ts` and `src/server.ts` handlers deleted P4.4-T9; generated SDK/OpenAPI unchanged per LOCK-009). The CLI/server still exposes HTTP/SSE, health, and provider fetch surfaces. This unit does not narrow or delete them. P4.4 transport narrowing remains open, and P5 structural absence of the selector startup fetch chain has not started.

> **P4.4-T1a note (2026-08-25) — extension-side periodic `/global/health` poll removed.**
> `KiloConnectionService` no longer polls `/global/health`: `HEALTH_POLL_INTERVAL_MS`,
> `startHealthPoll`/`stopHealthPoll`/`checkHealth`, the `healthPollTimer` field,
> and all call sites are gone from
> `packages/kilo-vscode/src/services/cli-backend/connection-service.ts` (source).
> Evidence: test: `packages/kilo-vscode/tests/unit/connection-service-health-poll-absence.test.ts`
> (6 tests / 22 expect calls — poll-surface absence plus retained-transport presence
> guard: `SdkSSEAdapter` heartbeat/reconnect remains the sole liveness path,
> LOCK-009); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`
> (health-poll row removed from the connection liveness/disposal table). Only the
> extension-side periodic poll is removed: the server-side `/global/health`
> route, the HTTP/SSE/generated-SDK bridge, provider/catalog (LOCK-006),
> convergence, storage/sync, CLI/TUI, and this back-end-gated fetch chain remain
> open; P4.4 transport narrowing, P4.5, and P5 remain open. No matrix row is
> marked complete and no numeric performance claim is made (LOCK-PERF-6).
> Working-tree evidence, not yet committed as of 2026-08-25.

> **P4.4-T1b note (2026-08-25) — legacy source-inventory reader and `/config/sources` removed.**
> This bounded unit physically removed the diagnosis-only source inventory from
> effective-config surfaces (LOCK-010/014/015).
> Source: `packages/opencode/src/kilocode/config/sources.ts` (the 12-source
> `KilocodeConfigSources` inventory reader) deleted; `KilocodeConfigSources`
> import/type/echo removed from
> `packages/opencode/src/kilocode/config/overlay.ts` (`Result`/`Input`/`resolve`
> no longer carry `sources`); `/config/sources` schema/path/endpoint/handler and
> `ConfigSourcesResponse` removed from
> `packages/opencode/src/kilocode/server/httpapi/groups/config-console.ts` and
> `handlers/config-console.ts`, and the `sources` echo field removed from
> `ConfigOverlayResponse`. The overlay endpoint still resolves effective/global/
> project via `config.get()`/`config.getGlobal()`/`KilocodeConfigOverlay.resolve`
> with no behavioral change to canonical resolution.
> Tests: `packages/opencode/test/kilocode/p4-4-source-inventory-removal.test.ts`
> (static absence of the reader, the `/config/sources` contract in production
> source, generated v2 SDK, and `packages/sdk/openapi.json`, plus retained
> overlay/effective/transaction/rules/model-state/TUI bridge presence);
> `config-sources.test.ts` deleted (it exercised the removed endpoint);
> `httpapi-exercise-scenarios.ts` and `httpapi-public.test.ts` drop the removed
> route entries; `p4-3-overlay-mode.test.ts` and `config-overlay.test.ts`
> updated for the `sources`-less overlay `Input`/`Result`.
> Docs: `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md`
> (source-inventory removal, not a surviving diagnostic); skill
> `packages/opencode/src/kilocode/skills/kilo-config.md`; P4.3 cutover evidence
> P4.4 row.
> Generated SDK: `packages/sdk/js/src/v2/gen/sdk.gen.ts` and `types.gen.ts` and
> `packages/sdk/openapi.json` regenerated via `bun ./script/generate.ts` from the
> repo root (twice, idempotent); no `config.sources`/`ConfigSourcesResponse`
> remains.
> Row delta: the `sources.ts` diagnostic-inventory residual cited in rows 1-7,
> 9, 10, and 12 is physically removed; each such row remains `residual retained`
> for its other retained residues (TUI migration/`ConfigPaths`, instruction
> profile, sandbox policy, SDK wrapper forwarding, managed/primary-worktree
> helpers) and no row is marked complete. Rows 8 and 11 carried no `sources.ts`
> residual and are unchanged. The retained config-console bridge endpoints
> (`/config/overlay`, `/config/effective`, `/config/transaction`, rules,
> model-state, TUI config/keybinds) and the overlay/transaction handlers are
> preserved (LOCK-009). Evidence-class reproduction of this unit's own row-level
> partial/closed delta is recorded per row above and in the tracker §7 rows; the
> behavior is recorded as a dropped reconstruction candidate (RC-P4.4-001,
> tracker section 7 registry) per LOCK-015. No risk-area code was removed beyond
> the inventory and its endpoint; P4.4 remains Active; no performance claim is
> made (LOCK-PERF-6). Working-tree evidence, not yet committed as of 2026-08-25.

> **P4.4-T2 note (2026-08-25) — orphaned primary-worktree mirror-read helper physically removed.**
> This bounded unit physically removed the unused primary-worktree discovery helper
> that had only test consumers after the P4.3 canonical cutover (managed worktree
> infrastructure permanently removed; canonical project assets live only under the
> first workspace root `.kilo` boundary; no mirror-read fallback).
> Source: `packages/opencode/src/kilocode/primary-worktree.ts` (64 lines, `primaryPaths`/`primaryWorktree`) deleted; direct test `packages/opencode/test/kilocode/primary-worktree.test.ts` deleted; glob entry `primary-worktree` removed from `packages/opencode/script/kilocode/test-profile.ts`; no production consumer existed (`config/config.ts`, `kilocode/config/config.ts`, `skill/index.ts`, `config/paths.ts` contain no `primary-worktree`/`primaryPaths`/`primaryWorktree` import). Canonical-root handling (`canonicalRoot`) and `ConfigPaths` (`packages/opencode/src/config/paths.ts` unchanged per scope) plus all legacy-reader cutover proofs, retained config-console endpoints, private worker bridge, and existing HTTP/SSE bridge remain untouched.
> Tests: `packages/opencode/test/kilocode/p4-4-primary-worktree-removal.test.ts` (static file-deletion, helper-import absence across CLI source, test-profile glob absence, canonical-root/`ConfigPaths` preservation); `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` updated to assert helper file/test absence and zero `primaryWorktree`/`primaryPaths`/`primary-worktree` surface in CLI source (P4.4 exception closed); retained `p4-3-cutover.test.ts` cutover anchors continue to prove canonical-only loading.
> Row delta: row 6 source/config columns updated to record `primary-worktree.ts:7-37` deletion; row 8 status moved from `residual retained` to `removed (helper deleted)` with source/test/docs evidence for physical deletion and import-surface absence; rows 1-5,7,9-13 unchanged except for row 6 config clarification. No new reconstruction-candidate entry is required: primary-worktree mirror reads are a permanent removal class (runtime §8.1) recorded in the tracker §7 removal inventory, not a displaced behavior needing evidence preservation; inherent permanent removals are not candidate rows. `ConfigPaths`, TUI/instruction, SDK wrapper forwarding, sandbox policy, and transport narrowing remain open P4.4 residues; no phase completion or performance claim is made. Working-tree evidence, not yet committed as of 2026-08-25.
>
> **P4.4-T3 note (2026-08-25) — unreachable legacy managed/MDM configuration helper physically removed.**
> This bounded unit physically removed the dead managed configuration reader that cannot affect effective configuration after the P4.3 cutover (canonical authored config is only one global root and `<workspaceRoot>/.kilo/`; no generic managed/legacy overlay is a target source; permanent removal is direct with no shim).
> Source: `packages/opencode/src/config/managed.ts` (77 lines, `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences`, `MANAGED_PLIST_DOMAIN`) deleted; direct tests `ConfigManaged` import and 5 `parseManagedPlist` unit tests removed from `packages/opencode/test/config/config.test.ts` (helper-only tests; `p4-3-cutover.test.ts` preservation and `Config.Service` canonical loader behavior unchanged); supporting `managedConfigDir`/`writeManagedSettingsEffect` harness removed. No production import remains — `config/config.ts`, `kilocode/config/config.ts`, `kilocode/config/overlay.ts`, `config/paths.ts` contain no `ConfigManaged`/`managedConfigDir`/`readManagedPreferences`/`parseManagedPlist`/`MANAGED_PLIST`/`ai.opencode.managed` reference; `ConfigPaths` and canonical-root handling preserved per scope.
> Tests: `packages/opencode/test/kilocode/p4-4-managed-removal.test.ts` (static file-deletion, helper-import absence across CLI source including `KILO_TEST_MANAGED_CONFIG_DIR`, test-profile glob presence, canonical-loader managed-absence preservation, direct-test removal, `ConfigPaths`/`canonicalRoot` preservation); retained `p4-3-cutover.test.ts:35-38` cutover anchors continue to prove `managedConfigDir()`/`readManagedPreferences` absent from canonical loader.
> Row delta: row 9 source/test/config/inactive-proof columns updated to record `config/managed.ts` deletion and new absence regression; rows 1-8 and 10-13 unchanged except for row 9. No new reconstruction-candidate entry is required: cloud/org/managed config sources are a permanent removal class (runtime §8.1) recorded in the §7 removal inventory; the unreachable helper has no target value and is not a displaced behavior needing evidence preservation; inherent permanent removals are not candidate rows. Provider/org/catalog labels, `sources.ts` already deleted (P4.4-T1b), `primary-worktree` already deleted (P4.4-T2), `ConfigPaths`, TUI/instruction, SDK wrapper forwarding, sandbox policy, and transport narrowing remain open P4.4 residues; no phase completion or performance claim is made. Working-tree evidence, not yet committed as of 2026-08-25.
>
> **P4.4-T4 note (2026-08-25) — legacy CLI `.well-known/opencode` provider-auth discovery URL-login branch physically removed.**
> This bounded unit physically removed the `args.url` branch at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`fetch` `<url>/.well-known/opencode`, `Process.spawn` of `auth.command`, `auth.set(url, { type: "wellknown", … })` persistence), removed the `login [url]` positional and its `Process`/`text` imports, and changed the command to `login` without URL discovery. Stale well-known intercept helpers (`remoteConfigClient`, `wellKnownAuth`, `json`/`wellKnown` helpers) were removed from `packages/opencode/test/config/config.test.ts`. No production import remains for the well-known fetch path; canonical loader unchanged.
> Tests: `packages/opencode/test/kilocode/p4-4-wellknown-provider-auth-removal.test.ts` (CLI well-known branch absence, stale helpers removed, retained generic provider-login guarded: `ProvidersLoginCommand`, `handlePluginAuth`, `resolvePluginProviders`, api-key/plugin/custom-provider paths preserved; P4.3 cutover anchors preserved); `packages/opencode/script/kilocode/test-profile.ts` sorted glob `managed < primary < wellknown` updated. Generic provider login and `Config.Service`/`Plugin`/`ModelsDev` remain; HTTP/SSE/generated-SDK bridge preserved (LOCK-009); no SDK regeneration required (no public endpoint change).
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record CLI well-known discovery deletion; rows 1-8 and 10-13 keep prior T3 delta. No reconstruction-candidate entry required (cloud/org/managed permanent removal class, LOCK-014/015). All other provider residues (`KILO_BUNDLED_PROVIDERS`, `models-api.json`, `metadata.ts` still present at that date; `ModelCache` is historical at that date and superseded 2026-08-27 by file deletion) remain open. P4.4 remains Active.
>
> **P4.4-T5 note (2026-08-25) — preset provider display metadata enrichment physically removed.**
> This bounded unit physically removed the preset-only display metadata enrichment that added `noteKey`/`icon`/`priority` to provider-list responses (LOCK-006 preset surface, direct removal per LOCK-014/015 — no reconstruction candidate needed for an explicitly removed preset surface).
> Source: `packages/opencode/src/kilocode/provider/metadata.ts` (41 lines, `ProviderMetadata` type, `notes`/`order`/`priority`/`icons`/`key()`/`providerMetadata()`) deleted; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` import `providerMetadata` and `metadata: providerMetadata(item.id)` enrichment removed — listing now `all: Object.values(validProviders).map((item) => Provider.toPublicInfo(item))` without preset enrichment; `all`/`connected`/`default`/`failed` aggregation, `Config.Service`/`Provider.Service`/`ProviderAuth.Service`/`ModelCache.Service`/`ModelsDev.Service` dependencies, `filterPromptTrainingModels`/`overlayAnacondaDesktop`/`pickBy` valid-providers guard, and `/provider` endpoint wiring (`list`/`auth`/`authorize`/`callback`) retained. No production source references `kilocode/provider/metadata` after removal. Direct obsolete test `packages/opencode/test/kilocode/provider-metadata.test.ts` deleted.
> Tests: `packages/opencode/test/kilocode/p4-4-provider-metadata-removal.test.ts` (preset helper file/test deletion, `kilocode/provider/metadata` and `providerMetadata(item.id)` import-surface absence across CLI source, handler enrichment absence with `all`/`connected`/`default`/`failed` and dependency/endpoint preservation, `Provider.Info` optional `metadata` contract unchanged — `ProviderMetadata` struct with `noteKey`/`icon`/`priority` remains `optionalOmitUndefined`, OpenAPI `Provider` `metadata` not in `required` and contract-compatible when omitted, generated SDK `metadata?:` preserved, webview `provider-catalog.ts` fallback (`synthetic`/`FALLBACK_PROVIDER_IDS`/`validIcon`) preserved, generic adapters `KILO_BUNDLED_PROVIDERS`/`models-api.json`/`models.ts`/custom-provider save/delete/auth lifecycle preserved (historical `ModelCache` at that date superseded 2026-08-27 by file deletion), sorted test-profile `managed < primary < provider < wellknown`); retained provider handler/provider tests and config/cutover suites plus `p4-3-cutover.test.ts` anchors.
> Row delta: row 9 source/test/config/i18n/build/inactive-proof updated to record `metadata.ts` and handler enrichment deletion with contract-preservation evidence; rows 1-8 and 10-13 keep prior T4 delta except for provider-residue clarification; provider catalog section updated. No new reconstruction-candidate entry is required per LOCK-014/015: preset display metadata is an explicitly removed preset display surface; preset catalogs (`KILO_BUNDLED_PROVIDERS`, `models-api.json` 3.0 MB), `models.dev` catalog, generic adapters, custom-provider paths, HTTP/SSE/generated-SDK bridge, and provider `metadata` optional schema all remain open P4.4 residues (`ModelCache` is historical at that date and superseded 2026-08-27 by file deletion). P4.4 remains Active; no phase completion or performance claim.
>
> **P4.4-T6 note (2026-08-26) — preset `@kilocode/kilo-gateway` bundled provider loader physically removed.**
> This bounded unit physically removed the sole preset identity loader `KILO_BUNDLED_PROVIDERS` (`"@kilocode/kilo-gateway": async () => createKilo`) and its `createKilo` import plus `BundledSDK` helper from `packages/opencode/src/kilocode/provider/provider.ts`, and removed its import/spread (`...KILO_BUNDLED_PROVIDERS`) from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS` (LOCK-006 preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change).
> Source: `kilocode/provider/provider.ts` now imports `{ type KiloProvider, AI_SDK_PROVIDERS, PROMPTS }` without `createKilo`; `provider/provider.ts` `BUNDLED_PROVIDERS` retains generic SDK loaders (`@ai-sdk/openai`, `@ai-sdk/anthropic`, `@ai-sdk/google`, `@ai-sdk/azure`, `@ai-sdk/openai-compatible`, `@openrouter/ai-sdk-provider`, etc.) without `KILO_BUNDLED_PROVIDERS`.
> Tests: `packages/opencode/test/kilocode/p4-4-bundled-provider-loader-removal.test.ts` (preset loader `KILO_BUNDLED_PROVIDERS`/`createKilo`/`"@kilocode/kilo-gateway"` absence, `KILO_BUNDLED_PROVIDERS` import/spread absence, generic adapters/`models-api.json`/`ModelsDev`/custom-provider/`/provider` bridge preservation (historical `ModelCache` at that date superseded 2026-08-27 by file deletion), sorted test-profile `bundled < managed < primary < provider < wellknown`); retained `p4-3-cutover.test.ts` and provider handler tests.
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record `KILO_BUNDLED_PROVIDERS` and loader spread deletion with generic-adapter/catalog/bridge preservation; rows 1-8 and 10-13 keep prior T5 delta. No new reconstruction-candidate entry is required per LOCK-014/015: preset bundled loader is an explicitly removed preset identity surface; `models-api.json` 3.0 MB, `models.dev` catalog, generic adapters, custom-provider paths, HTTP/SSE/generated-SDK bridge, and provider `metadata` optional schema all remain open P4.4 residues (`ModelCache` is historical at that date and superseded 2026-08-27 by file deletion). P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).
>
> **P4.4-T7 note (2026-08-26) — kilo/apertis dynamic model-fetch/cache physically removed.**
> This bounded unit physically removed the `kilo` and `apertis` dynamic model-fetch/cache subsystem (LOCK-006 preset/org source removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change).
> Source: `provider/model-cache.ts` simplified to invalidation-only `clear` (with stubbed `get`/`fetch`/`refresh`/`getFailure`/`failedProviders` without network — `fetchKiloModels`/`KiloModelsService`/`kiloModelsLayer`/`APERTIS_BASE_URL`/`api.apertis.ai`/`fetchApertisModels`/`authOptions`/`HttpClient`/`KILO_API_KEY`/`APERTIS_API_KEY` deleted); `provider/models.ts` removed `ModelCache`/`Config`/`Auth` and `kilo`/`apertis` injection (`cache.fetch`/`KILO_OPENROUTER_BASE`/`providers.kilo`/`apertis`/`baseURL`) — now static `Core` overlay only; `server/routes/instance/httpapi/handlers/provider.ts` removed `ModelCache.Service`/`failedProviders()` — now `failed: string[] = []` with `validProviders` guard without failedSet. Deleted obsolete kilo/apertis cache tests (`model-cache-effect.test.ts`, `model-cache-org.test.ts`, `provider-list-failed-state.test.ts`, `kilo-models-401-fallback.test.ts`) and simplified `kilo-loader-auth.test.ts` to `kiloCustomLoaders` helpers only; updated `p4-4-bundled-provider-loader-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts` expectations to reflect handler/models absence.
> Tests: `packages/opencode/test/kilocode/p4-4-model-cache-removal.test.ts` (kilo/apertis network-absence, handler/models absence, `models-api.json`/`models-dev.ts`/`BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge preservation, `clear`/`get`/`failedProviders` runtime invalidation with injectable defect, sorted test-profile `bundled < managed < model-cache < primary < provider < wellknown`); retained `p4-3-cutover.test.ts`, provider handler, and custom-provider save/delete lifecycle tests.
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record dynamic-cache deletion with static-catalog/adapter/bridge preservation; rows 1-8 and 10-13 keep prior T6 delta. No new reconstruction-candidate entry is required per LOCK-014/015: kilo/apertis dynamic cache is an explicitly removed preset/org source (LOCK-006 permanent removal); `models-api.json` 3.0 MB, `models.dev` catalog, generic adapters, custom-provider paths, HTTP/SSE bridge, and optional `metadata` schema all remain open P4.4 residues (`ModelCache` `clear` is historical at T7 and superseded 2026-08-27 by file deletion). P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).
>
> **P4.4-T8 note (2026-08-26) — preset provider login selector identity physically removed.**
> This bounded unit physically removed hardcoded preset ordering/recommendation/alias metadata from CLI provider login selectors (LOCK-006 preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change; Core `ModelsDev` and `models-api.json` retained per scope).
> Source: `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` removed `priority: Record<string, number> = { kilo: 0, anthropic: 2, "github-copilot": 3, openai: 4, google: 5, openrouter: 6, vercel: 7 }` and `hint: { kilo: "recommended", openai: "ChatGPT login or API key" }[x.id]` and `codex` -> `openai` alias (`const alias = ... "codex" ? ... "openai" ...` / `byID ?? byName ?? alias`), replaced with deterministic generic helpers `buildProviderOptions` (`providers.ts:236-257`) and `matchProviderInput` (`providers.ts:258-274`) using `sortBy((x) => x.name ?? x.id)` and `map((x) => ({ label: x.name, value: x.id }))` without preset hint; `packages/opencode/src/cli/cmd/github.handler.ts` `githubInstall`/`promptProvider()` removed `priority: Record<string, number> = { kilo: 0, anthropic: 1, openai: 2, google: 3 }` and `hint: priority[x.id] === 0 ? "recommended"` with same alphabetical `sortBy` via `buildGithubProviderOptions` (`github.handler.ts:156-166`) and `promptProvider()` (`github.handler.ts:240-250`). No dead imports/helpers required removal beyond the priority maps/hints/alias (remeda `map`/`pipe`/`sortBy`/`values` retained for generic ordering); `handlePluginAuth`/`resolvePluginProviders`/`Other` custom path and `ModelsDev.Service`/`Plugin.Service`/`Config.Service` preserved.
> Tests: `packages/opencode/test/kilocode/p4-4-provider-login-preset-removal.test.ts` — static absence of preset priority/hint/codex alias in `cli/cmd/providers.ts` (`ProvidersLoginCommand`/`buildProviderOptions`/`matchProviderInput`) and `cli/cmd/github.handler.ts` (`buildGithubProviderOptions`/`promptProvider()`), plus executable coverage via real implementation (`buildProviderOptions` ordering/prompt contents/hint absence with insertion-order tie preservation for same-name collisions, `buildGithubProviderOptions` ordering without hint, `matchProviderInput` codex rejection and exact id plus case-insensitive name dispatch with distinct-id assertions, `resolvePluginProviders` plugin/custom/`other` dispatch), `ModelsDev.Service`/plugin/custom-provider/`other` path and `/provider` `list`/`auth`/`authorize`/`callback` bridge preservation, Core `ModelsDev`/`models-api.json` retained, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < wellknown`; retained `p4-3-cutover.test.ts`, provider handler, and custom-provider lifecycle tests.
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record selector-identity deletion with generic ordering/bridge/catalog preservation; rows 1-8 and 10-13 keep prior T7 delta. No new reconstruction-candidate entry is required per LOCK-014/015: preset selector ordering/hint/alias is an explicitly removed preset identity surface (LOCK-006 permanent removal); `models-api.json` 3.0 MB, `models.dev` catalog, generic adapters, custom-provider paths, HTTP/SSE bridge, webview Kilo fallback, and optional `metadata` schema all remain open P4.4 residues (`ModelCache` `clear` is historical and superseded 2026-08-27 by file deletion). P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).

> **P4.4-T10 note (2026-08-26) — synthetic Kilo provider identity removed from VS Code settings surfaces.**
> This bounded unit removed settings-side synthesis of the Kilo Gateway provider and its Kilo-specific display/login branches so provider settings render only records supplied by existing provider state (LOCK-006 bounded settings-surface removal; not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; LOCK-PERF-6 no performance claim).
> Source: `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` simplified `visibleConnectedIds` to identity `[...connected]` and `providersWithKiloFallback` to identity `return providers` (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` import; retained as identity exports for compatibility); `provider-catalog.ts` removed Kilo-specific `providerIcon` `KILO_PROVIDER_ID` branch and `providerNoteKey` `settings.providers.note.kilo` branch and deleted `kiloFallbackProvider()` export (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` imports; retained `isPopularProvider`/`popularProviderIndex`/`sortProviders` and generic `providerIcon`/`providerNoteKey` with `validIcon` fallback); `ProvidersTab.tsx` removed `providersWithKiloFallback` injection, `KILO_PROVIDER_ID` import, `canonicalMode` Kilo filter, `sourceTag` `settings.providers.tag.gateway`, `connectProvider` `server.goToLogin()` Kilo branch, add-list `common.signIn` Kilo label (now always `settings.providers.action.configure`), and `showAccountButton` Kilo routing (now always `false`); `ProviderSelectDialog.tsx` removed `kiloFallbackProvider()` injection (`withKilo` synthesis), `KILO_PROVIDER_ID` import, `server.goToLogin()` Kilo branch, and `dialog.provider.tag.recommended` Kilo tag. Out-of-scope preserved: `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` (with `settings.providers.note.kilo` metadata), speech-to-text `availability.ts` `KILO_PROVIDER_ID` guard, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), persisted state/auth storage, backend `/provider` APIs, SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider sorting/icons/notes/configure/auth/delete flows.
> Tests: updated `packages/kilo-vscode/tests/unit/provider-visibility.test.ts` (visibleConnectedIds identity, providersWithKiloFallback identity without synthesis) and `provider-catalog.test.ts` (generic icon/note, no Kilo branches) to prove no synthetic injection and generic behavior; added `packages/kilo-vscode/tests/unit/p4-4-settings-surface-no-synthetic-kilo.test.ts` (static absence of `KILO_PROVIDER_ID`/`createKiloFallbackProvider`/`kiloFallbackProvider`/`settings.providers.note.kilo`/`validIcon("kilo")`/`providersWithKiloFallback` injection/`settings.providers.tag.gateway`/`common.signIn`/`server.goToLogin`/`dialog.provider.tag.recommended` in the 4 allowed files plus preservation of shared constants and speech-to-text guard). No changeset per spec (internal migration).
> Row delta: no 13-row matrix row marked complete; rows 1-8,10-13 keep prior T9 delta; row 9 and provider catalog section updated to record bounded settings-surface Kilo synthesis removal with generic custom-provider preservation and out-of-scope `KILO_PROVIDER_ID`/`KILO_AUTO`/`createKiloFallbackProvider` retention. No new reconstruction-candidate entry required per LOCK-014/015: synthetic Kilo settings display is an explicitly removed preset display surface (LOCK-006 permanent removal class); preset catalogs (`models-api.json` 3.0 MB), `models.dev` catalog, generic adapters, and full preset org/catalog removal remain open P4.4 residues (`ModelCache` clear is historical and superseded 2026-08-27 by file deletion); this is not full LOCK-006 completion. P4.4 remains Active.

> **P4.4-T11 audit correction (2026-08-26) — suppress Kilo reconstruction and delete dead helpers.**
> This audit tightening corrects T10 to also suppress configured-row reconstruction and remove dead identity helpers (LOCK-006 bounded settings-surface removal, LOCK-009 bridge preserved, LOCK-013 docs to reality).
> Source: `packages/kilo-vscode/webview-ui/src/components/settings/provider-tab-helpers.ts` now guards `KILO_PROVIDER_ID` — `resolveConfiguredProvider` returns `undefined` when backend omits Kilo (before config fallback) and `buildConfiguredList` filters `undefined`, so a `kilo` entry in `disabled_providers`/`config.provider`/`authStates`/`connected` cannot synthesize a settings row when backend omits Kilo; generic custom-provider IDs still reconstruct from auth/config. `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` deleted dead exports `visibleConnectedIds`/`providersWithKiloFallback` (no production callers; file retained as placeholder with no exports, no `KILO_PROVIDER_ID`/`createKiloFallbackProvider`/`kilo`). `provider-catalog.ts`/`ProvidersTab.tsx`/`ProviderSelectDialog.tsx` T10 removals retained.
> Tests: `provider-tab-helpers.test.ts` adds blocker-path coverage (missing-backend-Kilo + auth/config + connected/disabled cannot produce Kilo row; generic custom still reconstructs; backend-present Kilo still renders); `p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to assert helper deletion and guard presence (`if (id === KILO_PROVIDER_ID) return undefined`); `provider-visibility.test.ts` deleted as obsolete (no production consumer). No changeset per spec.
> Row delta: row 9 source/test/docs updated to record reconstruction-guard addition and dead-helper deletion with generic custom-provider preservation and out-of-scope `KILO_PROVIDER_ID`/`KILO_AUTO`/`createKiloFallbackProvider` retention; rows 1-8,10-13 keep prior T10 delta. Still no 13-row matrix row marked complete; not full LOCK-006. P4.4 remains Active.

> **P4.4-T10 re-audit correction (2026-08-26) — empty placeholder and dead Kilo account/profile slot removed (style-only cleanup, no new phase).** This correction applies the two accepted T10 re-audit style fixes within the existing settings-surface cleanup boundary (LOCK-006 bounded settings-surface removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs to implemented reality). Source: `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` deleted (empty placeholder with no production exports — no imports remain, no production responsibility); `webview-ui/src/components/settings/provider-tab-helpers.ts` removed unreachable Kilo `account` primary-slot path — deleted `isKiloProvider` predicate, removed `"account"` from `PrimarySlot` type, removed `isKilo` from `resolvePrimarySlot` (priority now `edit → apiKey → chatgpt → anaconda → placeholder`); `webview-ui/src/components/settings/ProvidersTab.tsx` removed dead Kilo `account` branch — deleted `showAccountButton` (always `false`), removed `useServer` import and `server` const, deleted `primary() === "account"` `Match` with `IconButton person` and `server.goToProfile()` navigation. Generic custom-provider configuration, auth, API-key, edit, delete, sorting, add-list behavior and backend-present Kilo row rendering (as existing record only, via helpers guard) remain preserved; shared `KILO_PROVIDER_ID`/`KILO_AUTO`/`createKiloFallbackProvider` and speech-to-text Kilo guard remain out of scope. Tests: `provider-tab-helpers.test.ts` removed `isKiloProvider` suite and `account` slot case; `provider-connect-dialog-arch.test.ts` replaced account-slot presence asserts with absence asserts; `p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to assert module/file absence (`existsSync` false) and absence of `provider-visibility`/`isKiloProvider`/`showAccountButton`/`server.goToProfile`/`"account"` imports/references. No changeset, no new phase, not full LOCK-006 completion.
> Row delta: none — correction within T10/T11 settings-surface scope; row 9 and provider catalog section previously recorded for T10/T11 remain accurate with placeholder now recorded as deleted and account slot now recorded as removed; still no 13-row matrix row marked complete.

> **P4.4-T12 note (2026-08-26) — dead synthetic Kilo provider factory and orphaned i18n residues physically removed (dead-factory/i18n residue only).** This bounded unit deletes the dead synthetic factory and its orphaned locale residues only (LOCK-006 bounded dead-factory/i18n residue removal; not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-010 canonical config ownership unchanged; LOCK-013 docs to implemented reality; LOCK-PERF-6 no performance claim).
> Source: `packages/kilo-vscode/src/shared/provider-model.ts` deleted `createKiloFallbackProvider()` and synthetic literals (`Kilo Gateway`, `settings.providers.note.kilo`, `KILO_API_KEY` env, `noteKey`/`icon`/`priority`/`custom` source) — file now exports `KILO_PROVIDER_ID`, `KILO_AUTO`, `PROVIDER_PRIORITY`, `providerOrderIndex` plus `CUSTOM_PROVIDER_PACKAGES`/`CUSTOM_PROVIDER_PACKAGE`, `PROVIDER_ID_PATTERN`, `isCustomProviderPackage`, `parseModelString` — `createKiloFallbackProvider` deleted with no imports/types made unused beyond that deletion; locale keys `settings.providers.tag.gateway` and `dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` dictionary (20 locales) and `settings.providers.note.kilo` from every `packages/kilo-i18n/src/*.ts` dictionary (20 locales) per repository dictionary conventions — no production consumer remains (settings-surface synthesis/login/icon/note/recommended/gateway branches already removed in T10/T11, factory had no remaining call site since b780e5df9b). Retained protected surfaces untouched: `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`/`CUSTOM_PROVIDER_PACKAGES`, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), speech-to-text Kilo guard (`availability.ts` `KILO_PROVIDER_ID`), `PROVIDER_PRIORITY`/`ModelSelector` Kilo visibility, provider auth stitching/`goToLogin`/`KILO_AUTO` fallback, persisted auth/state, backend `/provider` API, generated SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider UI/sorting/icons/notes/add/configure/auth/delete, locale schema/key typing, formatting conventions.
> Tests: `packages/kilo-vscode/tests/unit/p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to prove factory/synthetic literal absence (`createKiloFallbackProvider`/`Kilo Gateway`/`settings.providers.note.kilo`/`noteKey`/`custom` source) and orphaned locale key absence across all dictionaries (20 + 20 locales) while proving retained `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`, speech guard, and session fallback present — reusing P4.4 settings-surface static test style without weakening coverage. No changeset, no new phase, not full LOCK-006 completion.
> Row delta: no 13-row matrix row marked complete; rows 1-8,10-13 keep prior T11 re-audit delta; row 9 and provider catalog / i18n residues updated to record dead-factory/i18n residue removal with generic custom-provider and protected-bridge preservation. P4.4 remains Active.

> **P4.4-T23 note (2026-08-26) — obsolete preset-Kilo presence invalidation helper physically removed (LOCK-008).**
> This bounded unit physically removed the obsolete preset-Kilo presence socket invalidation that was invoked only for `providerID === "kilo"` after a Kilo auth change (LOCK-008 bounded removal; LOCK-001 P4.4 Active; LOCK-002/005/006/007 preserved).
> Source: `packages/opencode/src/kilocode/server/provider-auth-lifecycle.ts` deleted `KiloViewers` import and `export const invalidatePresence = Effect.fn("KiloServer.invalidatePresence")` helper (which yielded `KiloViewers.Service`/`invalidateAuth()`); `packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts` removed `invalidatePresence` from `kilocode/server/provider-auth-lifecycle` import and deleted the two guarded `if (ctx.params.providerID === "kilo") yield* invalidatePresence()` calls (authSet before `cleanupDisabled:true`, authRemove without cleanup); `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` removed `invalidatePresence` import and deleted the single guarded `if (ctx.params.providerID === "kilo") yield* invalidatePresence()` call (callback before `cleanupDisabled:true`). No other file required change.
> Preserved: `invalidateAfterProviderAuthChange` canonical coordinator with `withColdMutation` fence, `Auth.snapshotFile`/`restoreFile`, `ConfigRollbackFailed`/`restoreTarget`, `cleanupDisabled`/`disabled_providers` semantics, `KiloViewers` (historical `ModelCache.Service`/`clear(providerID)` under fence at T23 is superseded 2026-08-27 by file deletion) service/layer and AppLayer provisioning (`kilocode/presence/service.ts` with `Service`/`layer`/`defaultLayer`/`update`/`invalidateAuth`/`viewed`/`visibleUnion`/`EventServiceClient`; `effect/app-runtime.ts` with `KiloViewers.defaultLayer`), all other KILO identifier logic (`KILO_PROVIDER_ID`/`KILO_AUTO`, `auth.get("kilo")`, `KILO_API_KEY`, `KILO_MODEL_SCHEMA_EXTENSIONS`, custom-provider save/delete, `models-api.json`), and existing HTTP/SSE/generated-SDK/transport/daemon/session/TUI/global-health surfaces per LOCK-005/007. Not modified: `presence/service.ts`, `effect/app-runtime.ts`, session handlers, SDK/OpenAPI/generated clients, KiloProvider/webview readiness, models snapshot/catalog, global health, TUI files, or other `providerID === "kilo"` logic per scope.
> Tests: `packages/opencode/test/kilocode/p4-4-presence-invalidation-removal.test.ts` (7 tests — lifecycle helper/import absence, handler import/guard absence with generic `invalidateAfterProviderAuthChange`/`cleanupDisabled` preservation, no production `invalidatePresence` surface, generic lifecycle primitives preservation, `KiloViewers` service/layer and AppLayer preservation, other KILO identifier preservation, presence file untouched, sorted test-profile `bundled < flag < managed < model-cache < presence < primary < provider-login < provider < sdk < t16 < t18 < tui-migrate < wellknown`); retained `provider-auth-lifecycle`, `presence/service`, `custom-provider-save/delete` suites.
> Row delta: row 9 provider residue updated to record preset presence helper deletion with generic lifecycle preserved; rows 1-8,10-13 keep prior T22 delta. No new reconstruction-candidate entry required per LOCK-008/015: preset presence invalidation is an explicitly removed preset-only branch with no displaced target behavior. No row/phase/P4.4/LOCK complete claimed; no performance claim (LOCK-PERF-6).

> **2026-08-27 — bounded TUI migration source rewrite/backup removal.** `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` no longer creates `*.tui-migration.bak` files or strips `theme`, `keybinds`, and `tui` fields from legacy `kilo.json`/`kilo.jsonc` inputs. It retains materialization of a missing `tui.json` from those fields — `$schema`, `theme`, `keybinds`, normalized `tui` payload (`scroll_speed`, `scroll_acceleration`, `diff_style`), target-existence guard, `Filesystem.findUp`/`ConfigPaths.fileInDirectory` discovery, and write of a missing `tui.json` — without replacing an existing `tui.json`. Removed helper `backupAndStripLegacy` and its call path, plus imports made unused by its removal (`access`/`constants` from `fs/promises`, `applyEdits`/`modify` from `jsonc-parser`). The sole caller `packages/opencode/src/cli/cmd/tui/config/tui.ts:221` discovery/loading semantics unchanged, `ConfigPaths`/`Global.Path.config` override with `KILO_CONFIG_DIR` last-wins, sandbox deny-list, provider/auth/runtime-convergence/storage/SDK surfaces, and generated artifacts unchanged. Tests: `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` now proves backup/rewrite helper absence while retained materialization and guard remain; `packages/opencode/test/config/tui.test.ts` updated to assert source retention and no backup creation while verifying payload and target-exists guard; retained `p4-3-cutover.test.ts`, `p4-4-t16-tui-legacy-discovery.test.ts`, and `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` remain passing. No row or phase marked complete — rows 5/12/13 and overall P4.4/P5 remain residual/open.

## Storage and transport inventory summary — open, not closed

The following are recorded as open P4.4/P4.5 work rather than completion claims:

- SDK/server wrappers: handwritten `KILO_CONFIG_CONTENT` forwarding deleted (P4.4-T9) — wrappers now retain only `...process.env` and `logLevel` arg; generated SDK/OpenAPI unchanged (LOCK-009); HTTP/SSE/transport narrowing remains open;
- HTTP/SSE, global health/port, provider fetch, and config-console bridge
  surfaces (config-console group retained; only the source-inventory reader and
  `/config/sources` endpoint are removed by the P4.4-T1b unit);
- `ConfigPaths`, TUI/instruction, and CLI/TUI migration
  residues (the `KilocodeConfigSources` diagnostic inventory is removed by the
  P4.4-T1b unit; the orphaned `primary-worktree` helper is deleted by the
  P4.4-T2 unit; the unreachable `config/managed.ts` helper is deleted by the
  P4.4-T3 unit; the CLI `.well-known/opencode` provider-auth discovery branch is deleted by the
  P4.4-T4 unit; the preset provider display metadata helper `kilocode/provider/metadata.ts` and its handler enrichment are deleted by the P4.4-T5 unit — only preset `noteKey`/`icon`/`priority` enrichment is removed, the optional `Provider.Info` `metadata` field and catalog/model adapters remain; the preset `@kilocode/kilo-gateway` bundled provider loader `KILO_BUNDLED_PROVIDERS` and its `...KILO_BUNDLED_PROVIDERS` spread are deleted by the P4.4-T6 unit — `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, generic SDK loaders, `models-api.json` 3.0 MB, `models.dev` catalog (ModelCache deleted 2026-08-27 — boundary deleted, no ModelCache remains), and custom-provider/auth lifecycle remain per LOCK-006/009; the kilo/apertis dynamic fetch/cache is removed by the P4.4-T7 unit — `provider/model-cache.ts` physically deleted (LOCK-MODELCACHE-001 — boundary deleted, no ModelCache/clear remains), `provider/models.ts` static `Core` wrapper only without `ModelCache`/`kilo`/`apertis`, handler `ModelCache`/`failedProviders` removed — committed `models-api.json` 3.0 MB build-time snapshot and generic `Core.ModelsDev` runtime (`core/src/models-dev.ts` with disk cache, fallback fetch, 60-minute background refresh, explicit `kilo models --refresh`; `bun run refresh:models` only refreshes checked-in snapshot) plus `BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider paths/bridge retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed and ModelCache boundary is deleted not replaced; the preset provider login selector identity is removed by the P4.4-T8 unit — `cli/cmd/providers.ts` priority map/hints/alias and `cli/cmd/github.handler.ts` priority/recommended map removed with deterministic generic `sortBy((x) => x.name ?? x.id)` ordering retained — plugin/custom-provider, `ModelsDev.Service`, HTTP/SSE bridge, Core `ModelsDev`, `models-api.json` retained per LOCK-006/009 (webview fallback out of scope) — `ConfigPaths` itself is preserved per scope; the synthetic Kilo provider identity is removed from settings surfaces by the P4.4-T10 unit (`provider-visibility.ts`/`provider-catalog.ts`/`ProvidersTab.tsx`/`ProviderSelectDialog.tsx` synthesis/login/icon/note/recommended/gateway branches removed — `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` and speech-to-text `availability.ts` retained per scope)); the extension
  legacy-migration/importer and Roo wiring are removed by the row-13 unit;
- provider/preset/catalog/onboarding/org cleanup under LOCK-006 (bounded P4.4-T5 preset display metadata enrichment removed; bounded P4.4-T6 preset `@kilocode/kilo-gateway` bundled provider loader removed; bounded P4.4-T7 kilo/apertis dynamic fetch/cache removed — `provider/model-cache.ts` physically deleted (LOCK-MODELCACHE-001 — boundary deleted, no ModelCache/clear remains), `provider/models.ts` static `Core` wrapper, handler `ModelCache` removed — committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (disk cache, fallback fetch, 60-minute background refresh, explicit CLI refresh; `bun run refresh:models` only refreshes checked-in snapshot), generic adapters and custom-provider paths remain open (no ModelCache/clear remains) — only `kilo`/`apertis` dynamic fetching was removed and ModelCache boundary is deleted not replaced; bounded P4.4-T8 preset provider login selector identity removed — `cli/cmd/providers.ts` priority map (kilo/anthropic/github-copilot/openai/google/openrouter/vercel), kilo/openai hints, codex->openai alias, and `cli/cmd/github.handler.ts` priority/recommended map removed — deterministic generic `sortBy((x) => x.name ?? x.id)` ordering retained; plugin/custom-provider, `ModelsDev.Service`, HTTP/SSE bridge, Core `ModelsDev`, `models-api.json` retained per LOCK-006/009; bounded P4.4-T10 synthetic Kilo settings-surface identity removed — `provider-visibility.ts`/`provider-catalog.ts`/`ProvidersTab.tsx`/`ProviderSelectDialog.tsx` synthesis/login/icon/note/recommended/gateway branches removed — `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY` retained per scope (factory `createKiloFallbackProvider` still present at T10), generic custom-provider sorting/icons/notes/configure/auth/delete preserved; bounded P4.4-T11 audit correction tightened to suppress `KILO_PROVIDER_ID` reconstruction via `provider-tab-helpers.ts` (`resolveConfiguredProvider` → `undefined` when backend omits Kilo, `buildConfiguredList` filters `undefined`; generic custom reconstruction preserved) and to delete dead `provider-visibility.ts` exports `visibleConnectedIds`/`providersWithKiloFallback`; bounded P4.4-T10 re-audit correction deleted empty `provider-visibility.ts` placeholder and dead Kilo `account` slot/profile branch (`isKiloProvider`/`account`/`server.goToProfile`); bounded P4.4-T12 dead-factory/i18n residue removal deleted `src/shared/provider-model.ts` `createKiloFallbackProvider` and synthetic literals (`Kilo Gateway`/`settings.providers.note.kilo`/`noteKey`/`custom` source) and orphaned locale keys `settings.providers.tag.gateway`/`dialog.provider.tag.recommended` (20 webview locales) + `settings.providers.note.kilo` (20 kilo-i18n locales) — retained `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`/`CUSTOM_PROVIDER_PACKAGES`, session `KILO_AUTO` fallback, speech guard, `PROVIDER_PRIORITY`/`ModelSelector` Kilo visibility, auth stitching, persisted auth/state, backend `/provider` API, generated SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider UI per LOCK-006/009/010 — bounded P4.4-T23 obsolete preset-Kilo presence invalidation helper removed — `kilocode/server/provider-auth-lifecycle.ts` `KiloViewers`/`invalidatePresence` and `control.ts`/`provider.ts` `invalidatePresence`/`providerID === "kilo"` branches deleted with `invalidateAfterProviderAuthChange`/KiloViewers preserved per LOCK-008 (no ModelCache/clear — boundary deleted) — still no row marked complete);
- legacy storage reader/writer and sync/warp removal rows in tracker §7;
- convergence machinery review and removed-feature startup/resource evidence;
- P5 selector/startup structural absence and worker-unavailable scenarios.

No storage or transport row is marked complete by this artifact.

## Unknowns and next actions

Unknowns intentionally left open:

1. A static source scan does not prove the complete runtime call graph for every
   diagnostic or TUI path. The next unit must trace and/or remove each retained
   adjacent consumer before physical deletion is claimed.
2. The complete generated-SDK/public-server surface affected by transport
   narrowing is not enumerated here; only the inspected server wrappers and
   their tests are cited.
3. Locale-key ownership for provider/catalog/org and migration residues needs a
   dedicated LOCK-006/P4.4 inventory; this unit does not remove or reclassify
   those keys.
4. Convergence/startup/resource deltas against P0 remain unmeasured for this
   source-removal unit.

Next actions are bounded to later P4.4 units: narrow/delete retained diagnostic
and TUI source inventories, reconcile SDK/server transport wrappers, complete
the separate LOCK-006 provider catalog inventory, review convergence and
startup/resource evidence, and then update the tracker only with verified
per-row results. P4.4 remains Active; P4.5 and P5 remain open.
# P4.4 Source-Removal Evidence Matrix — Static 13-Row Inventory

> **Unit status: COMPLETE — static evidence inventory recorded on 2026-08-25. P4.4 remains Active.**
> This artifact records effective-config inactivity after the P4.3 cutover and
> the source, diagnostic, TUI, SDK-wrapper, i18n, build/package, and transport
> residues that remain open. It does not claim physical deletion of those
> residues, transport narrowing, provider-catalog removal, storage removal, or
> P4.4 phase completion.
>
> **P4.4-T1b update (2026-08-25) — legacy source-inventory reader and
> `/config/sources` physically removed.** A bounded P4.4 unit deleted
> `packages/opencode/src/kilocode/config/sources.ts` (the 12-source diagnostic
> inventory reader), removed the `KilocodeConfigSources` type/echo from the
> overlay, and removed the `/config/sources` schema/path/endpoint/handler from
> the config-console group and the `sources` echo field from the overlay
> response. The checked-in generated SDK and OpenAPI were regenerated and no
> `config.sources`/`ConfigSourcesResponse` contract remains. The rows below
> that cited `sources.ts` line ranges were recorded against the historical
> `8167992b` baseline where that file existed; the row-level delta is recorded
> in the P4.4-T1b note below. No row is marked complete: each affected row
> keeps its other retained residues (TUI/instruction/`ConfigPaths`, sandbox
> policy, SDK wrapper forwarding, managed/primary-worktree helpers) and P4.4
> remains Active.
>
> **P4.4-T2 update (2026-08-25) — orphaned primary-worktree mirror-read helper physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/kilocode/primary-worktree.ts` (helper with only test consumers, no production call site), deleted its direct test `packages/opencode/test/kilocode/primary-worktree.test.ts`, and removed the glob entry from `packages/opencode/script/kilocode/test-profile.ts`. The `ConfigPaths` module and canonical-root handling remain untouched per scope. Rows 6 and 8 are updated for this deletion; all other rows keep their prior T1b delta. P4.4 remains Active.
>
> **P4.4-T3 update (2026-08-25) — unreachable legacy managed/MDM configuration helper physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/config/managed.ts` (unreachable helper: `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences`, `MANAGED_PLIST_DOMAIN`, no production call site after the P4.3 cutover), removed its direct tests (`ConfigManaged` import and 5 `parseManagedPlist` unit tests) from `packages/opencode/test/config/config.test.ts`, and added a focused absence regression `packages/opencode/test/kilocode/p4-4-managed-removal.test.ts` plus its glob entry in `packages/opencode/script/kilocode/test-profile.ts`. No `Config.Service` behavior, `ConfigPaths`, daemon/transport, generated SDK/OpenAPI, provider catalog, TUI/instruction, or sandbox residue is changed. Row 9 is updated for this deletion; rows 1-8 and 10-13 keep their prior T2 delta. P4.4 remains Active.
>
> **P4.4-T4 update (2026-08-25) — legacy CLI `.well-known/opencode` provider-auth discovery URL-login branch physically removed.** A bounded P4.4 unit deleted the `args.url` branch at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`fetch` `<url>/.well-known/opencode`, `Process.spawn` of `auth.command`, `auth.set(url, { type: "wellknown", … })` persistence), removed the `login [url]` positional and its `Process`/`text` imports, and changed the command to `login` without URL discovery. Stale well-known intercept helpers (`remoteConfigClient`, `wellKnownAuth`, `json` helper, `wellKnown` helper) were removed from `packages/opencode/test/config/config.test.ts`. Added focused absence regression `packages/opencode/test/kilocode/p4-4-wellknown-provider-auth-removal.test.ts` plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (managed < primary < wellknown). Generic provider login (api-key/plugin/custom-provider, `kilo auth login` selection, `handlePluginAuth`, `resolvePluginProviders`) and canonical effective config remain untouched; legacy HTTP/SSE/generated-SDK bridge remains per LOCK-009; no SDK regeneration is required (no public server endpoint or OpenAPI change). Row 9 is updated for this CLI discovery deletion; rows 1-8 and 10-13 keep their prior T3 delta. P4.4 remains Active.
>
> **P4.4-T5 update (2026-08-25) — preset provider display metadata enrichment physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/kilocode/provider/metadata.ts` (preset-only `providerMetadata` helper — `noteKey`/`icon`/`priority` mapping for preset provider IDs) and removed only its import and `metadata: providerMetadata(item.id)` response enrichment from `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts`, retaining the provider listing aggregation (`all`/`connected`/`default`/`failed`), auth/model/config dependencies (`Config.Service`, `Provider.Service`, `ProviderAuth.Service`, `ModelCache.Service`, `ModelsDev.Service`), and endpoint wiring (`list`/`auth`/`authorize`/`callback`). Deleted the direct obsolete test `packages/opencode/test/kilocode/provider-metadata.test.ts` and added focused absence/preservation regression `packages/opencode/test/kilocode/p4-4-provider-metadata-removal.test.ts` plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (managed < primary < provider < wellknown). The optional `providerMetadata` field on `Provider.Info` (`Provider` OpenAPI schema, generated SDK `types.gen.ts`) remains optional and contract-compatible when omitted; webview `webview-ui/src/components/settings/provider-catalog.ts` fallback (`synthetic` icon, priority fallback, noteKey fallback) is preserved. Generic provider adapters, `KILO_BUNDLED_PROVIDERS`, `models-api.json` (3.0 MB checked-in snapshot), provider model/catalog loaders, `ModelCache`, custom-provider save/delete/validation, `custom-provider.ts`/`provider.ts`, auth lifecycle, and HTTP/SSE/generated-SDK bridge remain untouched per LOCK-006/009; preset catalogs, models.dev snapshot, and model caches remain open. Row 9/provider residue is updated for this preset-metadata deletion; rows 1-8 and 10-13 keep their prior T4 delta except for the provider-residue clarification. No new reconstruction-candidate entry is required: preset display metadata is an explicitly removed preset surface (LOCK-014/015 inherent permanent removal) with no displaced behavior needing evidence preservation. P4.4 remains Active.
>
> **P4.4-T6 update (2026-08-26) — preset `@kilocode/kilo-gateway` bundled provider loader physically removed.** A bounded P4.4 unit deleted the sole `KILO_BUNDLED_PROVIDERS` preset identity loader (`"@kilocode/kilo-gateway": async () => createKilo`) and its `createKilo` import plus `BundledSDK` helper from `packages/opencode/src/kilocode/provider/provider.ts`, and removed its import/spread injection (`...KILO_BUNDLED_PROVIDERS`) from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS`. Generic SDK loader map (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`, etc.), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`/`patchConfigModel`/`kiloCustomLoaders`/`patchCustomLoaderResult`, `models-api.json` (3.0 MB checked-in snapshot), `ModelsDev`/`ModelCache` catalog services (`provider/models.ts`/`provider/model-cache.ts`), custom-provider save/delete/validation/auth lifecycle (`kilocode/custom-provider.ts`, `custom-provider-save.ts`, `custom-provider-delete.ts`, `provider-auth-lifecycle.ts`), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) are retained per LOCK-006/009 — static source-presence preservation only; dynamic Kilo model resolution through fallback (models.dev/ModelCache/custom lifecycle) is intentionally not runtime-proven in this static unit (no network/package installation; see tracker row 9 open residue). No SDK regeneration is required (no public endpoint/OpenAPI change) and no performance claim is made (LOCK-PERF-6). Added focused static absence/preservation regression `packages/opencode/test/kilocode/p4-4-bundled-provider-loader-removal.test.ts` (source presence only; dynamic fallback intentionally unproven) plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (bundled < managed < primary < provider < wellknown). Row 9 is updated for this preset-loader deletion; rows 1-8 and 10-13 keep their prior T5 delta. P4.4 remains Active; no phase completion claimed.
>
> **P4.4-T7 update (2026-08-26) — kilo/apertis dynamic model-fetch/cache physically removed.** A bounded P4.4 unit removed the `kilo` and `apertis` dynamic fetch/injection paths and their preset-only auth/network helpers: `packages/opencode/src/provider/model-cache.ts` simplified to invalidation-only `clear` (with stubbed `get`/`fetch`/`refresh`/`getFailure`/`failedProviders` without network — no `fetchKiloModels`, `@kilocode/kilo-gateway`, `KiloModelsService`/`kiloModelsLayer`, `APERTIS_BASE_URL`/`api.apertis.ai`, `fetchApertisModels`/`authOptions`, `KILO_API_KEY`/`APERTIS_API_KEY`, `kilocodeToken`, `HttpClient`/`FetchHttpClient`, `Auth.Service`/`Config.Service`); `packages/opencode/src/provider/models.ts` removed `ModelCache`/`Config`/`Auth` dependencies and `kilo`/`apertis` provider injection (`cache.fetch`/`refresh`, `baseURL`, `KILO_OPENROUTER_BASE`, `providers.kilo`/`apertis`) — now static `Core.Service` overlay only (`core.get` with defect catch); `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` removed `ModelCache.Service` import and `failedProviders()` call — now `failed: string[] = []` with `validProviders` guard `Object.keys(item.models).length > 0 || id in connected` (failedSet removed). Deleted obsolete kilo/apertis cache tests (`model-cache-effect.test.ts`, `model-cache-org.test.ts`, `provider-list-failed-state.test.ts`, `kilo-models-401-fallback.test.ts`) and simplified `kilo-loader-auth.test.ts` to `kiloCustomLoaders` helpers only; updated `p4-4-bundled-provider-loader-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts` expectations (handler no longer contains `ModelCache.Service`, `models.ts` no longer contains `ModelCache`/`KILO_OPENROUTER_BASE`/`fetchKiloModels`). Added focused absence/preservation/runtime regression `packages/opencode/test/kilocode/p4-4-model-cache-removal.test.ts` (kilo/apertis network-absence, handler/models absence, `models-api.json`/`models-dev.ts`/`BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge preservation, `clear`/`get`/`failedProviders` runtime invalidation) plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (bundled < managed < model-cache < primary < provider < wellknown). Generic adapters (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache, fallback fetch, 60-minute scoped background refresh, explicit `kilo models --refresh` — `provider/models.ts` wrapper retained as static `Core` overlay), custom-provider save/delete/auth lifecycle (`ModelCache.clear` invalidation without network), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) are retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed (`bun run refresh:models` only refreshes checked-in snapshot); dynamic `kilo`/`apertis` resolution intentionally not runtime-proven beyond invalidation (no `kilo`/`apertis` network). No SDK regeneration is required (no endpoint/OpenAPI change) and no performance claim is made (LOCK-PERF-6). Row 9 is updated for this dynamic-cache deletion; rows 1-8 and 10-13 keep their prior T6 delta. P4.4 remains Active; no phase completion claimed. No new reconstruction-candidate entry is required per LOCK-014/015: kilo/apertis dynamic cache is an explicitly removed preset/org source (LOCK-006 permanent removal).
>
> **P4.4-T8 update (2026-08-26) — preset provider login selector identity physically removed.** A bounded P4.4 unit removed hardcoded preset ordering/recommendation/alias metadata from CLI provider login selectors: `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` priority map (`kilo: 0`, `anthropic: 2`, `github-copilot: 3`, `openai: 4`, `google: 5`, `openrouter: 6`, `vercel: 7`), `kilo: "recommended"` / `openai: "ChatGPT login or API key"` hints, and `kilocode_change` `codex` -> `openai` alias; `packages/opencode/src/cli/cmd/github.handler.ts` `githubInstall`/`promptProvider()` priority/recommended map (`kilo: 0`, `anthropic: 1`, `openai: 2`, `google: 3`, `hint: priority[x.id] === 0 ? "recommended"`). Deterministic generic ordering retained as alphabetical `sortBy((x) => x.name ?? x.id)` via `buildProviderOptions` (`providers.ts:236-257`) / `buildGithubProviderOptions` (`github.handler.ts:156-166`) and `matchProviderInput` (`providers.ts:258-274`) without preset identity; plugin/custom-provider auth (`handlePluginAuth`, `resolvePluginProviders`, `other` path with `Enter provider id`), generic catalog lookup (`ModelsDev.Service` with `modelsDev.get()`/`refresh`), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) retained per LOCK-006/009. Core `ModelsDev` (`packages/core/src/models-dev.ts`) and `models-api.json` (3.0 MB checked-in snapshot) retained per scope (not removed in this unit); webview `webview-ui/src/components/settings/provider-catalog.ts` Kilo fallback identity out of scope and untouched; generated SDK/OpenAPI and package APIs unchanged (no endpoint/OpenAPI change, no regeneration). Added focused regression `packages/opencode/test/kilocode/p4-4-provider-login-preset-removal.test.ts` (static absence plus executable ordering/prompt/codex/plugin dispatch via `buildProviderOptions`/`buildGithubProviderOptions`/`matchProviderInput`/`resolvePluginProviders`) plus sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (bundled < managed < model-cache < primary < provider-login < provider < wellknown). Row 9 updated for this selector-identity deletion; rows 1-8 and 10-13 keep their prior T7 delta. P4.4 remains Active; no phase completion or performance claim. No new reconstruction-candidate entry is required per LOCK-014/015: preset selector ordering/hint/alias is an explicitly removed preset identity surface (LOCK-006 permanent removal).

> **P4.4-T9 update (2026-08-26) — handwritten SDK `KILO_CONFIG_CONTENT` wrapper forwarding physically removed.** A bounded P4.4 unit physically removed the obsolete `KILO_CONFIG_CONTENT` env-forwarding machinery from the handwritten SDK server wrappers (LOCK-010 canonical file authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014 bounded removal; no performance claim per LOCK-PERF-6). Source: `packages/sdk/js/src/server.ts` and `packages/sdk/js/src/v2/server.ts` deleted `mergeConfig`, `parseExistingConfig`, `buildConfigEnv` helpers and `env: { ...process.env, KILO_CONFIG_CONTENT: buildConfigEnv(...) }` forwarding (retaining `...process.env` and `logLevel` arg passthrough, `createKiloServer`/`createKiloTui` spawning, `Config` type for `ServerOptions`/`TuiOptions`). No production call site uses the removed helpers (only the wrappers themselves). Generated SDK (`packages/sdk/js/src/gen/*`, `v2/gen/*`) and OpenAPI (`packages/sdk/openapi.json`) unchanged — verified no `KILO_CONFIG_CONTENT`/`buildConfigEnv` in generated surface; HTTP/SSE transport, custom-provider/plugin/auth, sandbox `KILO_CONFIG_CONTENT` deny list, canonical `Config.Service` loader/cutover proofs untouched. Tests: `packages/sdk/js/test/server.test.ts` obsolete `buildConfigEnv` suite removed and replaced with wrapper-absence regression (handwritten wrappers contain no merge helpers/`KILO_CONFIG_CONTENT`, `...process.env` preserved, `createKiloServer`/`createKiloTui` still exposed, generated SDK preserved); `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts` added (handwritten wrapper absence, generated SDK/OpenAPI unchanged, canonical `Config.Service` authority absence, sandbox deny preservation, custom-provider/plugin/auth preservation, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < sdk < wellknown`); retained `p4-3-cutover.test.ts`, `sandbox/policy.test.ts` cutover/deny anchors. Row delta: row 3 source/test/generated/build/inactive-proof updated to record handwritten wrapper forwarding deletion with generated-SDK/OpenAPI unchanged and sandbox deny preserved; rows 10/11 SDK wrapper residues updated to reflect forwarding deleted while `...process.env` and `logLevel` passthrough remain; rows 1-2,4-9,12-13 keep prior T8 delta. No new reconstruction-candidate entry required per LOCK-014/015: `KILO_CONFIG_CONTENT` env override is a permanent removal class (runtime §8.1) with no target reader after P4.3 cutover; handwritten forwarding is a displaced preset-env surface with no target value. P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).


> **P4.4-T12 update (2026-08-26) — dead synthetic Kilo provider factory and orphaned i18n residues physically removed.** A bounded P4.4-T12 unit deleted the dead synthetic Kilo provider factory `createKiloFallbackProvider()` and its synthetic metadata literals (`Kilo Gateway`, `settings.providers.note.kilo`, `KILO_API_KEY` env, `noteKey`/`icon`/`priority`/`custom` source) from `packages/kilo-vscode/src/shared/provider-model.ts` (now exports `KILO_PROVIDER_ID`, `KILO_AUTO`, `PROVIDER_PRIORITY`, `providerOrderIndex` plus `CUSTOM_PROVIDER_PACKAGES`/`CUSTOM_PROVIDER_PACKAGE`, `PROVIDER_ID_PATTERN`, `isCustomProviderPackage`, `parseModelString` — `createKiloFallbackProvider` and its synthetic literals deleted; no imports/types made unused beyond that deletion per scope) and removed orphaned locale keys `settings.providers.tag.gateway` and `dialog.provider.tag.recommended` from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` dictionary (20 locales) and `settings.providers.note.kilo` from every `packages/kilo-i18n/src/*.ts` dictionary (20 locales) per repository dictionary conventions — no production consumer remains for those keys after T10/T11 settings-surface synthesis/login branches were removed. Retained protected surfaces untouched: `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`/`CUSTOM_PROVIDER_PACKAGES`, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), speech-to-text Kilo guard (`availability.ts` `KILO_PROVIDER_ID`), `PROVIDER_PRIORITY`/`ModelSelector` Kilo visibility, provider auth stitching/`goToLogin`/`KILO_AUTO` fallback, persisted auth/state, backend `/provider` API, generated SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider UI, locale schema/key typing, formatting conventions. Tests: `packages/kilo-vscode/tests/unit/p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to prove factory/synthetic literal absence and orphaned locale key absence across all dictionaries (20 + 20 locales) while retaining `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex` and speech/session guards — reusing P4.4 settings-surface static test style without weakening coverage. No 13-row matrix row marked complete; P4.4 remains Active (dead-factory/i18n residue only, not full LOCK-006; LOCK-009/010/013/PERF-6 as above).

> **P4.4-T10 update (2026-08-26) — synthetic Kilo provider identity removed from settings surfaces.** A bounded P4.4 unit removed settings-side synthesis of the Kilo Gateway provider and its Kilo-specific display/login branches so provider settings render only records supplied by existing provider state (LOCK-006 bounded settings-surface removal; not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; LOCK-PERF-6 no performance claim). Source: `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` simplified `visibleConnectedIds` to identity `[...connected]` and `providersWithKiloFallback` to identity `return providers` (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` import); `provider-catalog.ts` removed Kilo-specific branches `providerIcon` `KILO_PROVIDER_ID` `validIcon("kilo")` fallback and `providerNoteKey` `settings.providers.note.kilo` and deleted `kiloFallbackProvider()` export (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` imports; retained `isPopularProvider`/`popularProviderIndex`/`sortProviders` generic sorting and `providerIcon`/`providerNoteKey` generic paths); `ProvidersTab.tsx` removed `providersWithKiloFallback` injection, `KILO_PROVIDER_ID` import, `canonicalMode` Kilo filter, `sourceTag` `settings.providers.tag.gateway`, `connectProvider` `server.goToLogin()` Kilo branch, add-list `common.signIn` Kilo label (now always `settings.providers.action.configure`), and `showAccountButton` Kilo routing (now always `false` with preserved `server.goToProfile` dead slot); `ProviderSelectDialog.tsx` removed `kiloFallbackProvider()` injection (`withKilo` synthesis), `KILO_PROVIDER_ID` import, `server.goToLogin()` Kilo branch, and `dialog.provider.tag.recommended` Kilo tag. Retained out-of-scope surfaces untouched: `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` (with `settings.providers.note.kilo` metadata), speech-to-text `availability.ts` `KILO_PROVIDER_ID` guard, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), persisted state/auth storage, backend `/provider` APIs, SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider sorting/icons/notes/configure/auth/delete flows. Tests: updated `provider-visibility.test.ts` and `provider-catalog.test.ts` to prove no synthetic injection and generic behavior; added `p4-4-settings-surface-no-synthetic-kilo.test.ts` (static absence of Kilo synthesis/login/icon/note/recommended/gateway branches in the 4 allowed files plus preservation of shared constants/speech/model fallback). No changeset per spec (internal migration). Row delta: no 13-row matrix row marked complete; this is a bounded settings-surface step, not full preset catalog/org removal.


> **P4.4-T13 update (2026-08-26) — legacy `KILO_CONFIG` TUI migration helper input physically removed.** A bounded P4.4 unit physically removed the sole legacy `KILO_CONFIG` effective-config input residue from the TUI migration helper (LOCK-010 canonical file authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014 bounded removal; no performance claim per LOCK-PERF-6). Source: `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` deleted `if (Flag.KILO_CONFIG) files.push(Flag.KILO_CONFIG)` from `opencodeFiles` (lines 148-158 scope); `Flag` import retained for `Flag.KILO_DISABLE_PROJECT_CONFIG` (still used) so no unused import remains. No `ConfigPaths`, `session/instruction`, transport/server routes, generated SDK/OpenAPI, provider code, or storage/convergence artifact was changed per scope. Retained TUI theme/keybind migration (`migrateTuiConfig`, `normalizeTui`, `backupAndStripLegacy`, `TUI_SCHEMA_URL`, theme/keybinds/tui extraction, `tui.json` materialization), `ConfigPaths` discovery (`Filesystem.findUp` `["kilo.json", "kilo.jsonc"]`, `ConfigPaths.fileInDirectory(Global.Path.config, "kilo")`, `unique` directories, `Filesystem.exists` filtering), `Flag.KILO_DISABLE_PROJECT_CONFIG` gate, `ConfigPaths` `KILO_CONFIG_DIR` profile and instruction `KILO_CONFIG_DIR` behavior (historical at T13 — instruction removed in P4.4-T17), server health, HTTP/SSE/generated SDK, provider/catalog, custom-provider lifecycle preserved. Tests: `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` added (tui-migrate absence, retained migration/canonical-loader boundaries, `ConfigPaths`/`instruction` preservation (historical at T13), generated SDK/OpenAPI unchanged, sandbox deny preservation, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < sdk < tui-migrate < wellknown`); retained `p4-3-cutover.test.ts`, `p4-3-readiness-inventory.test.ts` cutover anchors. Row delta: row 1 source/test/docs updated to record TUI migration helper `Flag.KILO_CONFIG` injection deletion with `ConfigPaths`/instruction (historical at T13) and TUI migration preservation; rows 2-13 keep prior T12 delta; row 1 remains `residual retained` (open) for remaining `ConfigPaths`/TUI residues; no row/phase/P4/LOCK-006 complete claimed.

> **P4.4-T14 update (2026-08-26) — `ConfigPaths.directories()` `.kilocode` directory targets physically removed.** A bounded P4.4 unit narrowed `packages/opencode/src/config/paths.ts:29,35` `ConfigPaths.directories()` from `targets: [".kilocode", ".kilo"]` to `targets: [".kilo"]` in both the project-ancestor walk and the home walk (LOCK-010 exactly two canonical scopes — `.kilocode` not a target canonical source; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014/015 bounded removal without shim; no performance claim per LOCK-PERF-6). Ancestor/home walk topology, `Global.Path.config`/`Global.Path.home`, `Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`, `ConfigPaths.files()`/`fileInDirectory()`, `tui.ts` filtering, plugin install, instruction profile, canonical `Config.Service`, provider/transport/storage/convergence, generated SDK/OpenAPI/source-links untouched per scope; all other `.kilocode` occurrences outside `config/paths.ts` preserved (e.g. `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` retains `[".kilocode", ".kilo"]` per scope). Tests: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:100,165` and `p4-4-tui-migrate-kilo-config-removal.test.ts:145`/`p4-4-managed-removal.test.ts:114`/`p4-4-primary-worktree-removal.test.ts:83` updated to assert `targets: [".kilo"]` while preserving `KILO_CONFIG_DIR`/`Global.Path.home`/`Global.Path.config` and non-canonical loader boundary checks. Row delta: rows 5 and 7 source/test/docs updated to record `.kilocode` target removal with residual retained; rows 1-4,6,8-13 keep prior T13 delta; no row/phase/P4.4/LOCK-006 complete claimed.

> **P4.4-T15 update (2026-08-26) — `ConfigPaths.directories()` home `Global.Path.home` `.kilo` discovery block physically removed.** A bounded P4.4 unit deleted only the home-directory `.kilo` discovery block from `packages/opencode/src/config/paths.ts:34-38` (`...(yield* afs.up({ targets: [".kilo"], start: Global.Path.home, stop: Global.Path.home }))`) (LOCK-010 exactly one canonical global root and workspace `.kilo`; arbitrary home `.kilo` discovery not target authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014/015 bounded principle-first removal without shim; no performance claim per LOCK-PERF-6). `ConfigPaths.directories()` now contains exactly one `targets: [".kilo"]` via the project-ancestor walk (`Global.Path.home` absent, `Global.Path.config`/`Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`ConfigPaths.files()`/`fileInDirectory()` preserved per scope); `tui.ts` filtering, plugin install, `session/instruction.ts` `KILO_CONFIG_DIR` profile intentionally retained/deferred per scope — not removed in this unit — canonical `Config.Service`, provider/transport/storage/convergence, generated SDK/OpenAPI/source-links unchanged; all other `.kilocode` occurrences outside `config/paths.ts` preserved (e.g. `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` retains `[".kilocode", ".kilo"]` per scope). Tests: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:97-104,154-171` and `p4-4-tui-migrate-kilo-config-removal.test.ts`/`p4-4-managed-removal.test.ts`/`p4-4-primary-worktree-removal.test.ts` now assert home walk absent (`not.toContain("Global.Path.home")`) and single `targets: [".kilo"]` while preserving `Global.Path.config`/`KILO_CONFIG_DIR`/`KILO_DISABLE_PROJECT_CONFIG`/`files`/`fileInDirectory` and instruction `KILO_CONFIG_DIR` profile. Row delta: row 7 source/test/docs updated to record home `.kilo` discovery deletion with residual retained (ancestor walk remains); rows 1-6,8-13 keep prior T14 delta; no row/phase/P4.4/LOCK-006 complete claimed. P4.4 remains Active.


> **P4.4-T16 update (2026-08-26) — `ConfigPaths.directories()` workspace-ancestor `.kilo` discovery walk and dead no-op guard/void references physically removed; temporary TUI-local legacy discovery preserved.** A bounded P4.4 unit deleted the sole remaining ancestor directory walk and dead no-op `...(!Flag.KILO_DISABLE_PROJECT_CONFIG ? [] : [])` plus `void directory`/`void worktree` from `packages/opencode/src/config/paths.ts:23-27` `ConfigPaths.directories()` (`...(!Flag.KILO_DISABLE_PROJECT_CONFIG ? yield* afs.up({ targets: [".kilo"], start: directory, stop: worktree }) : [])`) and narrowed the signature to `directories(): Effect<string[]>` (LOCK-010 target authored scopes are one global root plus `<workspaceRoot>/.kilo/` — no ancestor walk is a target configuration source; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; no performance claim per LOCK-PERF-6). `ConfigPaths.directories()` now returns only `Global.Path.config` plus optional `Flag.KILO_CONFIG_DIR` (`Global.Path.config`/`Flag.KILO_CONFIG_DIR` preserved, `Flag.KILO_DISABLE_PROJECT_CONFIG`/`void`/`afs.up`/`targets: [".kilo"]`/`start: directory`/`stop: worktree` absent, `Global.Path.home` remains absent; `ConfigPaths.files()`/`fileInDirectory()` untouched). Temporary TUI-local legacy discovery is preserved in `packages/opencode/src/cli/cmd/tui/config/tui.ts:211-225` — `yield* afs.up({ targets: [".kilocode", ".kilo"], start: ctx.directory })` gated by `Flag.KILO_DISABLE_PROJECT_CONFIG`, merged as `unique([global-filtered baseDirectories, ...legacyProjectDirs, ...KILO_CONFIG_DIR-filtered baseDirectories])` so merged order is exactly global → enabled legacy project discovery → KILO_CONFIG_DIR (last wins, preserving prior precedence); `KILO_CONFIG_DIR` instruction profile (`packages/opencode/src/session/instruction.ts` `Flag.KILO_CONFIG_DIR`/`prefer KILO_CONFIG_DIR profile`) preserved at this baseline and deferred TUI `.kilocode`/`.kilo` (`packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` `[".kilocode", ".kilo"]`, `tui.ts:211-255` gated `dirs` filter) remain deferred/intact per scope (P4.5-adjacent, not modified). `Config.Service` canonical loader (`packages/opencode/src/config/config.ts` `canonicalRoot`/`globalConfigFile`/`projectFile`), provider/transport/generated SDK/OpenAPI/source-links, sandbox, storage/convergence unchanged. Tests: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:97-108,158-182` and `p4-4-t16-tui-legacy-discovery.test.ts` (12 behavior tests: workspace/ancestor .kilo/.kilocode, global, KILO_CONFIG_DIR, disable-gating, plus precedence regressions proving KILO_CONFIG_DIR wins setting and plugin-spec/origin collisions against both .kilo and .kilocode with merged order global → legacy → KILO_CONFIG_DIR last-wins) plus `p4-4-tui-migrate-kilo-config-removal.test.ts`/`p4-4-managed-removal.test.ts`/`p4-4-primary-worktree-removal.test.ts` now assert `afs.up`/`targets: [".kilo"]` absent with `Global.Path.config`/`KILO_CONFIG_DIR`/`KILO_DISABLE_PROJECT_CONFIG`/`files`/`fileInDirectory` preserved and `Global.Path.home`/`canonicalRoot` absent as before. Row delta: row 7 source/test/docs updated to record ancestor walk deletion with residual retained (`ConfigPaths.files` ancestor walk and TUI/instruction `KILO_CONFIG_DIR`/`.kilocode` residues remain, P4.5-adjacent); row 5 source/test updated to reflect `ConfigPaths.directories()` ancestor removal; rows 1-4,6,8-13 keep prior T15 delta; no row, phase, P4.5, or P5 complete claimed. P4.4 remains Active.

> **P4.4-T17 update (2026-08-26) — `session/instruction.ts` `KILO_CONFIG_DIR` profile fallback physically removed; `ConfigPaths`/`TUI` retained.** A bounded P4.4 unit deleted the two `Flag.KILO_CONFIG_DIR` reads from `packages/opencode/src/session/instruction.ts` — `...(Flag.KILO_CONFIG_DIR ? [path.join(Flag.KILO_CONFIG_DIR, "AGENTS.md")] : [])` from `globalFiles` and `Flag.KILO_CONFIG_DIR ?? global.config` from `relative()` — now the `KILO_CONFIG_DIR` `AGENTS.md` candidate is removed while `globalFiles` retains the canonical `path.join(global.config, "AGENTS.md")` plus the conditional `path.join(global.home, ".claude", "CLAUDE.md")` candidate, and `relative()` fallback is `global.config` only when `KILO_DISABLE_PROJECT_CONFIG` is set; project instruction discovery when enabled, existing `KILO_DISABLE_PROJECT_CONFIG` gating, and trusted-origin semantics preserved; `ConfigPaths.directories()` (`Global.Path.config` plus optional `Flag.KILO_CONFIG_DIR`) and TUI precedence (global → legacy project directories → `KILO_CONFIG_DIR` last wins) retained per LOCK-001/002; `Flag.KILO_CONFIG_DIR` getter, sandbox deny, and broader P4.4/P4.5 surfaces untouched per LOCK-003/005. Tests: `packages/opencode/test/kilocode/instruction.test.ts` rewritten to prove `KILO_CONFIG_DIR` does not override canonical global `AGENTS.md` and relative instruction with project disabled resolves from `Global.Path.config` not `KILO_CONFIG_DIR`; `p4-3-cutover.test.ts` and `p4-4-tui-migrate-kilo-config-removal.test.ts` now absence-guard `instruction.ts` for `Flag.KILO_CONFIG_DIR` while affirming `ConfigPaths`/`TUI`. Row delta: row 2 source/test updated — instruction profile deleted, `ConfigPaths`/`TUI` residual retained; rows 1,3-13 keep prior T16 delta; no row/phase/P4.4 complete claimed.


>**Provenance split.** The 13-row matrix originated as a
> documentation-only residue inventory recorded against the historical working
> tree at HEAD `8167992b` on 2026-08-25 (rows 1–12 doc-only; row 13
> extension importer/Roo removal and its assertions in
> `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` were post-removal
> working-tree evidence dated 2026-08-25 from a later unit in the current
> working tree, not part of the `8167992b` baseline). The current worktree
> amends that baseline with bounded physical removals P4.4-T1b through
> P4.4-T13 — including P4.4-T13 deletion of the `Flag.KILO_CONFIG` injection from
> `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` and its test replacement
> `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` and
> P4.4-T9 deletion of the handwritten SDK
> `KILO_CONFIG_CONTENT` wrapper forwarding and its test replacement in
> `packages/sdk/js/src/server.ts`, `src/v2/server.ts`,
> `packages/sdk/js/test/server.test.ts`, and
> `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts`
> (generated SDK/OpenAPI unchanged per LOCK-009). Historical baseline and
> current-worktree amendments are not conflated; per-row/per-note deltas below
> record the current implemented reality.

## Purpose, scope, and non-goals

This is the first bounded P4.4 execution unit. It establishes an auditable
static matrix for every effective-config source class in runtime spec §8.1 and
maps each row to current source, test, documentation, generated-SDK, config,
i18n, build/package, and inactive-proof evidence.

This artifact also records the extension-side legacy-migration/importer and
Roo wiring removal as a distinct current working-tree implemented change, not
yet committed as of 2026-08-25. Row 13 catalogues that removal; rows 1–12 were
originally the documentation-only static inventory at the `8167992b` baseline
and have since been amended by bounded physical removals P4.4-T1b through
P4.4-T13 (including P4.4-T13 `Flag.KILO_CONFIG` TUI migration helper deletion at
`packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` / `p4-4-tui-migrate-kilo-config-removal.test.ts` and
P4.4-T9 handwritten SDK wrapper `KILO_CONFIG_CONTENT`
forwarding deletion with retained `...process.env`/`logLevel` and unchanged
generated SDK/OpenAPI per LOCK-009/010/014). This artifact does not remove the
remaining CLI/TUI migration helper or any other open P4.4/P4.5 residual.

In scope:

- the exact 13-row taxonomy from
  `specs/vscode-orchestrator/runtime-and-configuration-direction.md` §8.1;
- post-P4.3 effective-config inactivity in the canonical `Config.Service` path;
- retained source inventories and non-effective consumers that must not be
  mistaken for deleted runtime/product surfaces;
- concrete open work and unknowns for later P4.4 units.

Non-goals:

- no transport narrowing, private-worker selector/startup work, or P5 work;
- no CLI/TUI/Console deletion, which remains P4.5 scope;
- no provider/preset/catalog/onboarding/org removal, which remains the separate
  LOCK-006 P4.4 residual task;
- no storage reader/writer removal;
- no edits to runtime specs, ADRs, canonical architecture docs, or the
  historical P4.3 reconciliation checklist;
- as originally recorded at the `8167992b` baseline, this
  evidence-documentation unit made no additional source-code, generated-SDK,
  config, or product implementation changes beyond the single current
  working-tree extension importer/Roo removal dated 2026-08-25 (row 13);
  the other 12 rows were then a documentation-only static inventory.
  Bounded amendments P4.4-T1b through P4.4-T13 in the current worktree
  subsequently added physical source/test deletions — including P4.4-T9
  handwritten SDK wrapper forwarding deletion (`packages/sdk/js/src/server.ts`,
  `src/v2/server.ts`, `packages/sdk/js/test/server.test.ts`,
  `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts`;
  generated SDK/OpenAPI unchanged) — with per-row/per-note deltas below that
  describe current implemented reality; rows 1–12 are no longer purely
  documentation-only.

## Taxonomy and active legal source set

The taxonomy is closed by runtime spec §8.1: four retained legal effective
sources and 13 deletion classes. The active effective-config source set after
P4.3 is only:

1. the canonical global config root and its canonical files/assets;
2. `<workspaceRoot>/.kilo/` and its project-versioned files/assets;
3. VS Code SecretStorage credentials, referenced by opaque identifiers; and
4. versioned runtime defaults and safety invariants.

UI `globalState`/`workspaceState` is derived UI state, not effective-config
authority. Session/storage state and bounded approval records are operational
state, not effective-config sources. `KILO_CONFIG_DIR` instruction profile
fallback was removed from `session/instruction.ts` in P4.4-T17 — only
`ConfigPaths`/`TUI` `KILO_CONFIG_DIR` remains (LOCK-001/002), not
`Config.Service` authority.

## Static method and status vocabulary

The initial inventory was performed against the working tree at HEAD
`8167992b` on 2026-08-25. The method was:

> **Provenance note.** The `8167992b` HEAD is the historical baseline for the
> original documentation-only rows 1–12. Row 13 importer/Roo assertions and
> bounded amendments P4.4-T1b through P4.4-T13 — including P4.4-T13 `Flag.KILO_CONFIG`
> TUI migration helper deletion (`tui-migrate.ts` / `p4-4-tui-migrate-kilo-config-removal.test.ts`) and
> P4.4-T9 handwritten
> SDK wrapper `KILO_CONFIG_CONTENT` forwarding deletion (source
> `packages/sdk/js/src/server.ts`, `src/v2/server.ts`; tests
> `packages/sdk/js/test/server.test.ts`,
> `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts`;
> generated SDK/OpenAPI unchanged) — are post-baseline current-worktree
> evidence with per-row/per-note deltas below. They reflect the tree after
> those deletions and are not part of the `8167992b` baseline. Do not
> reclassify post-baseline rows/amendments as `8167992b`-era claims; rows
> now describe current implemented reality.

1. verify the 13 row names and dispositions against runtime spec §8.1 and the
   placeholder rows in tracker §7;
2. inspect the canonical loader and P4.3 absence anchors in
   `packages/opencode/src/config/config.ts` and
   `packages/opencode/test/kilocode/p4-3-cutover.test.ts`;
3. inspect retained diagnostic/config-console, TUI/instruction, sandbox,
   migration, SDK-wrapper, provider/catalog, i18n, and build/package paths;
4. record only path-level static evidence. No dynamic call-graph or transport
   deletion claim is inferred from a string absence.

The matrix uses these statuses:

- **effective inactive** — the P4.3 canonical-loader evidence proves that the
  source no longer affects effective `Config.Service` output; no relevant
  non-effective residue was found in the inspected paths;
- **residual retained** — effective use is inactive, but source inventory,
  diagnostic, TUI/instruction, SDK-wrapper, safety, migration, catalog, or
  transport machinery remains and is open P4.4 work;
- **unknown** — the inspected evidence does not prove either classification.

`residual retained` does not mean that the residue is an effective-config
reader. It means that physical source removal or adjacent-surface narrowing is
not complete and must not be reported as complete.

## 13-row source-removal evidence matrix

| # | Source class (runtime §8.1) | Status | Source | Tests | Docs | Generated SDK | Config | i18n | Build/package | Inactive proof |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `KILO_CONFIG` env override | residual retained; evidence date: 2026-08-26 (P4.4-T13) | deleted: `sources.ts` diagnostic listing removed (P4.4-T1b); `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts:148-158` TUI migration helper `Flag.KILO_CONFIG` injection physically deleted (P4.4-T13) — `if (Flag.KILO_CONFIG) files.push(Flag.KILO_CONFIG)` removed; `Flag` import retained for `Flag.KILO_DISABLE_PROJECT_CONFIG` (still used) | test: `p4-3-cutover.test.ts:13-78`; `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` (tui-migrate absence, retained TUI theme/keybind migration, `ConfigPaths` discovery, canonical-loader absence, `ConfigPaths`/`instruction` `KILO_CONFIG_DIR` preservation, generated SDK/OpenAPI unchanged, sandbox deny preservation, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < sdk < tui-migrate < wellknown`) | doc: runtime §8.1; P4.3 cutover evidence §6; this matrix P4.4-T13 | - | residual: TUI helper `Flag.KILO_CONFIG` injection deleted (P4.4-T13); `sources.ts` already removed (P4.4-T1b); canonical `config/config.ts` has no `Flag.KILO_CONFIG` reader; `ConfigPaths` `KILO_CONFIG_DIR` profile retained separately | - | residual retained: packaged TUI migration helper remains for theme/keybind/`tui` migration and `ConfigPaths` discovery (`Filesystem.findUp` `["kilo.json", "kilo.jsonc"]`, `ConfigPaths.fileInDirectory(Global.Path.config, "kilo")`, `unique` directories); `Flag.KILO_DISABLE_PROJECT_CONFIG` gate preserved; package impact narrowed to helper injection only | effective inactive: P4.3 absence anchors; no `Config.Service` read remains (`p4-3-cutover.test.ts:40-45`; `p4-4-tui-migrate-kilo-config-removal.test.ts` verifies `tui-migrate.ts` has no `Flag.KILO_CONFIG`); row remains open/residual for remaining `ConfigPaths`/TUI residues; no P4.4 completion claim |
| 2 | `KILO_CONFIG_DIR` env override | residual retained; evidence date: 2026-08-26 (P4.4-T17) | deleted: `sources.ts:135-159,184-194` removed (P4.4-T1b); `packages/opencode/src/session/instruction.ts:60-88` `Flag.KILO_CONFIG_DIR` profile removed (P4.4-T17) — `...(Flag.KILO_CONFIG_DIR ? [path.join(Flag.KILO_CONFIG_DIR, "AGENTS.md")] : [])` and `Flag.KILO_CONFIG_DIR ?? global.config` deleted, now the `KILO_CONFIG_DIR` `AGENTS.md` candidate is removed while `globalFiles` retains the canonical `path.join(global.config, "AGENTS.md")` plus the conditional `path.join(global.home, ".claude", "CLAUDE.md")` candidate, and `relative()` fallback is `global.config` only; residual: `packages/opencode/src/config/paths.ts:23-28` (`Global.Path.config` plus optional `Flag.KILO_CONFIG_DIR`) and `packages/opencode/src/cli/cmd/tui/config/tui.ts:211-225` legacy discovery with `Flag.KILO_CONFIG_DIR` last-wins retained (LOCK-001/002) | test: `p4-3-cutover.test.ts:152-168`; `packages/opencode/test/kilocode/instruction.test.ts` (P4.4-T17 — canonical global only, `KILO_CONFIG_DIR` does not override, relative fallback from `Global.Path.config` when disabled); `p4-4-tui-migrate-kilo-config-removal` | doc: runtime §8.1; P4.3 cutover evidence §10 (amended P4.4-T17) | - | deleted: instruction profile removed (P4.4-T17); residual: `ConfigPaths`/`TUI` `KILO_CONFIG_DIR` retained (LOCK-001/002), not `Config.Service` authority | - | residual: `ConfigPaths`/`TUI` consumers remain; instruction no longer uses `KILO_CONFIG_DIR` | effective inactive: canonical loader ignores flag; instruction now also ignores `KILO_CONFIG_DIR` — only `ConfigPaths`/`TUI` residual retained (row remains open/residual; P4.4 not complete) |
| 3 | `KILO_CONFIG_CONTENT` env override | removed (handwritten wrapper forwarding deleted); evidence date: 2026-08-26 (P4.4-T9) | deleted: `packages/sdk/js/src/server.ts:5-35` helpers and `KILO_CONFIG_CONTENT` env forwarding (`68-73,155-163`) and equivalent `packages/sdk/js/src/v2/server.ts` wrappers physically removed (P4.4-T9); `sources.ts:171-183` already removed (P4.4-T1b) | test: `p4-3-cutover.test.ts:39-47`; `packages/sdk/js/test/server.test.ts` forwarding suite removed and replaced with wrapper-absence regression (`p4-4-sdk-config-forwarding-removal`); `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts` (handwritten wrapper absence, generated SDK/OpenAPI unchanged, canonical loader absence, sandbox deny preservation) | doc: runtime §8.1; P4.3 cutover evidence §6 | SDK/server-wrapper evidence (handwritten): wrappers now contain no `mergeConfig`/`parseExistingConfig`/`buildConfigEnv`/`KILO_CONFIG_CONTENT` (verified); Actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`, no `buildConfigEnv`; `packages/sdk/openapi.json` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009 HTTP/SSE bridge preserved) | removed: handwritten wrapper forwarding deleted; source inventory removed (P4.4-T1b); canonical `config/config.ts` has no `KILO_CONFIG_CONTENT` reader (cutover) | - | removed: handwritten SDK wrapper package no longer emits the env; generated SDK/OpenAPI and HTTP/SSE transport unchanged (LOCK-009) | effective inactive: `config/config.ts` contains no content injection; handwritten forwarding deleted and cannot affect canonical effective config; sandbox `KILO_CONFIG_CONTENT` deny remains as safety (not effective-config source) |
| 4 | `KILO_PERMISSION` env override | residual retained; evidence date: 2026-08-25 | residual: `sources.ts:275-291` removed (P4.4-T1b); sandbox environment deny-list at `packages/opencode/src/kilocode/sandbox/policy.ts:168-175` | test: `p4-3-cutover.test.ts:39-51`; `packages/opencode/test/permission/r18-service.test.ts` | doc: runtime §8.1; P4.3 cutover evidence §6 | - | residual: sandbox safety handling remains; no `Flag.KILO_PERMISSION` effective overlay | - | - | effective inactive: P4.3 proves `Flag.KILO_PERMISSION` and `result.tools` absent from `Config.Service` |
| 5 | Legacy `opencode.*` keys, `.opencode` / `.kilocode` locations | residual retained; evidence date: 2026-08-26 (P4.4-T16) | residual: `sources.ts:59-60,121-132` removed (P4.4-T1b); `packages/opencode/src/config/paths.ts:29,35` `.kilocode` targets physically removed (P4.4-T14) — `ConfigPaths.directories()` workspace-ancestor `.kilo` walk plus dead guard/void removed (P4.4-T16) — now `Global.Path.config` plus `KILO_CONFIG_DIR` only (`afs.up`/`targets: [".kilo"]` absent, `Global.Path.home` absent, `Flag.KILO_DISABLE_PROJECT_CONFIG` absent from `ConfigPaths` — gated TUI-local `tui.ts:211-225` `yield* afs.up({ targets: [".kilocode", ".kilo"] })` preserved; `Global.Path.config`/`KILO_CONFIG_DIR` preserved; `ConfigPaths.files()` untouched); TUI legacy-directory handling in `packages/opencode/src/cli/cmd/tui/config/tui.ts:211-255` (merged order global → enabled legacy `legacyProjectDirs` → KILO_CONFIG_DIR last-wins via `unique([global-filtered baseDirectories, ...legacyProjectDirs, ...KILO_CONFIG_DIR-filtered baseDirectories])`, `dirs` filter `.kilo`/`.kilocode`/`KILO_CONFIG_DIR`) retained and `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` `[".kilocode", ".kilo"]` preserved per scope (P4.5-adjacent) | test: `p4-3-cutover.test.ts:97-108,161-184` now assert `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void` absent from `ConfigPaths` with `Global.Path.home` absent and `Global.Path.config`/`KILO_CONFIG_DIR` preserved plus TUI `targets: [".kilocode", ".kilo"]` gated; `p4-4-t16-tui-legacy-discovery.test.ts` behavior (`.kilo`/`.kilocode`/global/`KILO_CONFIG_DIR`/disable, plus KILO_CONFIG_DIR wins setting and plugin-spec/origin vs both .kilo and .kilocode with order global → legacy → KILO_CONFIG_DIR last-wins); `packages/opencode/test/kilocode/config/config.test.ts:430-502` preserved | doc: runtime §8.1; P4.3 cutover evidence §6; this matrix P4.4-T16 (see row 7/T16) | - | residual: TUI compatibility paths; source inventory removed (P4.4-T1b); `ConfigPaths.directories()` `.kilocode` removed (P4.4-T14), home walk removed (P4.4-T15) and ancestor+dead guard removed (P4.4-T16) — now `Global.Path.config` plus `KILO_CONFIG_DIR` only; TUI-local `.kilo`/`.kilocode` retained; canonical loader is `.kilo/kilo.jsonc` only | - | residual: TUI package still contains legacy-directory handling (TUI-local gated) | effective inactive: canonical loader absence anchors cover `.opencode`, `.kilocode`, and legacy filenames; `ConfigPaths.directories()` ancestor+dead guard removal verified by absence of `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void` and `Global.Path.home` absence (P4.4-T16); TUI-local discovery verified |
| 6 | Global project-asset sources | residual retained; evidence date: 2026-08-25 (P4.4-T2) | residual: `sources.ts:135-159` removed (P4.4-T1b); `packages/opencode/src/kilocode/primary-worktree.ts:7-37` helper physically deleted (P4.4-T2) | test: `p4-3-cutover.test.ts:54-60`; `packages/opencode/test/kilocode/config/config.test.ts:563-590`; `p4-4-primary-worktree-removal.test.ts`; `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` | doc: runtime §8.1/R5; P4.3 cutover evidence §7 | - | residual: helper deleted (P4.4-T2); source inventory removed (P4.4-T1b); canonical project assets are restricted to `<workspaceRoot>/.kilo/` | - | - | effective inactive: `primaryPaths(` and legacy discovery are absent from `Config.Service`; canonical-root tests reject mirror/legacy values; helper file and import surface absent |
| 7 | Ancestor directory walks | residual retained; evidence date: 2026-08-26 (P4.4-T16) | residual: `packages/opencode/src/config/paths.ts:23-27` workspace-ancestor `afs.up({ targets: [".kilo"], start: directory, stop: worktree })` walk plus dead guard/void removed (P4.4-T16) — `ConfigPaths.directories()` now `Global.Path.config` plus `KILO_CONFIG_DIR` only (`afs.up`/`targets: [".kilo"]`/`start: directory`/`stop: worktree`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`void` absent, `Global.Path.home` remains absent, `Global.Path.config`/`KILO_CONFIG_DIR` preserved, signature `directories()`; `ConfigPaths.files()`/`fileInDirectory()` untouched; `.kilocode` already removed P4.4-T14, home `.kilo` already removed P4.4-T15; `sources.ts:121-159` already removed P4.4-T1b; `session/instruction.ts` `KILO_CONFIG_DIR` profile removed in P4.4-T17 (historically retained at T16 baseline); temporary TUI-local legacy discovery `packages/opencode/src/cli/cmd/tui/config/tui.ts:211-225` (`afs.up` `[ ".kilo", ".kilocode"]` gated by `Flag.KILO_DISABLE_PROJECT_CONFIG`, merged as `unique([global-filtered baseDirectories, ...legacyProjectDirs, ...KILO_CONFIG_DIR-filtered baseDirectories])` preserving order global → legacy → KILO_CONFIG_DIR last-wins) plus `ConfigPaths.files` ancestor for `tui` direct files remain; TUI `theme.tsx:564` `[".kilocode", ".kilo"]` preserved per scope (P4.5-adjacent) | test: `p4-3-cutover.test.ts:97-108,161-184` now assert `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void` absent from `ConfigPaths` with `Global.Path.config`/`KILO_CONFIG_DIR`/`files`/`fileInDirectory` preserved and TUI `targets: [".kilocode", ".kilo"]` gated; `packages/opencode/test/kilocode/config/config.test.ts:563-590`; `p4-4-t16-tui-legacy-discovery.test.ts` behavior (precedence regressions: KILO_CONFIG_DIR wins setting and plugin-spec/origin vs both .kilo and .kilocode, order global → legacy → KILO_CONFIG_DIR); `p4-4-tui-migrate-kilo-config-removal`/`p4-4-managed-removal`/`p4-4-primary-worktree-removal` now assert `ConfigPaths` absence and TUI `tui.ts` gated presence | doc: runtime §8.1/R5; P4.3 cutover evidence §7; this matrix P4.4-T16 | - | residual: `ConfigPaths.directories()` ancestor+dead guard removed — now `Global.Path.config` plus `KILO_CONFIG_DIR` only; temporary TUI-local `.kilo`/`.kilocode` discovery gated; `ConfigPaths.files()` ancestor walk for `tui` direct files remains P4.5-adjacent; instruction `KILO_CONFIG_DIR` profile removed in P4.4-T17 | - | residual: CLI/TUI path machinery remains packaged (TUI-local gated discovery, `ConfigPaths.files` ancestor walk and TUI `.kilocode`/`KILO_CONFIG_DIR` remain; instruction `KILO_CONFIG_DIR` removed in P4.4-T17, P4.5-adjacent) | effective inactive: `ConfigPaths.files`/`directories` absence anchors apply to `config/config.ts`, while retained consumers are outside effective loading; `ConfigPaths.directories()` ancestor+dead guard deletion verified by absence of `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void` and `Global.Path.home` absence (P4.4-T16); TUI-local legacy discovery verified via behavior test; residual retained (effective-config inactivity already established P4.3) |
| 8 | Primary-worktree mirror reads | removed (helper deleted); evidence date: 2026-08-25 (P4.4-T2) | deleted: `packages/opencode/src/kilocode/primary-worktree.ts:7-64` helper physically deleted (P4.4-T2); `test/kilocode/primary-worktree.test.ts` deleted; `script/kilocode/test-profile.ts` glob entry removed; no canonical-loader call site | test: `p4-3-cutover.test.ts:54-60`; `p4-4-primary-worktree-removal.test.ts`; `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` | doc: runtime §8.1/R5; P4.3 cutover evidence §7 | - | helper deleted; canonical `canonicalRoot` path is the effective boundary; `ConfigPaths` preserved | - | - | effective inactive: `primaryPaths(` absent from `config/config.ts`; helper file absent and no production import remains; canonical tests prove primary/mirror values do not load |
| 9 | Cloud/org/managed config sources | residual retained; evidence date: 2026-08-26 (P4.4-T12) | deleted: `sources.ts:82-95,210-271` removed (P4.4-T1b); `packages/opencode/src/config/managed.ts` helper physically deleted (P4.4-T3) — `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences` absent; CLI `.well-known/opencode` provider-auth discovery URL-login branch deleted at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`login [url]` positional and `args.url` `fetch`/`Process.spawn`/`wellknown` persistence) (P4.4-T4) — `args.url` fetch, `Process.spawn`, `wellknown` persistence removed; `login [url]` → `login`; preset provider display metadata helper `packages/opencode/src/kilocode/provider/metadata.ts` physically deleted (P4.4-T5) — `providerMetadata` preset `noteKey`/`icon`/`priority` mapping absent; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` import and `metadata: providerMetadata(item.id)` enrichment removed (P4.4-T5) — listing now `all: Object.values(validProviders).map(Provider.toPublicInfo)` without preset enrichment; preset `@kilocode/kilo-gateway` bundled provider loader `KILO_BUNDLED_PROVIDERS` (`"@kilocode/kilo-gateway": async () => createKilo`) deleted from `packages/opencode/src/kilocode/provider/provider.ts` and its `...KILO_BUNDLED_PROVIDERS` spread removed from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS` (P4.4-T6) — generic SDK loaders (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`, etc.), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache, fallback fetch, background refresh, explicit CLI refresh; `bun run refresh:models` only refreshes checked-in snapshot), `ModelCache` `clear`, custom-provider/auth lifecycle, and `/provider` bridge retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed; dynamic `kilo`/`apertis` model resolution through removed injection intentionally not runtime-proven in this static unit; kilo/apertis dynamic fetch/cache physically removed (P4.4-T7) — `provider/model-cache.ts` simplified to invalidation-only `clear` (stubbed `get`/`fetch` without network; `fetchKiloModels`/`KiloModelsService`/`APERTIS_BASE_URL`/`fetchApertisModels`/`authOptions`/`HttpClient`/`KILO_API_KEY` deleted), `provider/models.ts` removed `ModelCache`/`Config`/`Auth`/`kilo`/`apertis` injection (`cache.fetch`/`KILO_OPENROUTER_BASE`/`providers.kilo`/`apertis`) — static `Core` overlay only, handler removed `ModelCache.Service`/`failedProviders()` — `failed: string[] = []` with `validProviders` guard without failedSet; committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache, fallback fetch, 60-minute background refresh, explicit `kilo models --refresh`; `bun run refresh:models` only refreshes checked-in snapshot) plus static `Core` wrapper, `BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed; CLI preset provider login selector identity physically removed (P4.4-T8) — `packages/opencode/src/cli/cmd/providers.ts` priority map (`kilo: 0`, `anthropic: 2`, `github-copilot: 3`, `openai: 4`, `google: 5`, `openrouter: 6`, `vercel: 7`), `kilo`/`openai` hints, `codex` -> `openai` alias removed; `packages/opencode/src/cli/cmd/github.handler.ts` priority/recommended map (`kilo: 0`, `anthropic: 1`, `openai: 2`, `google: 3`) removed; deterministic generic ordering retained as `sortBy((x) => x.name ?? x.id)` without preset identity; plugin/custom-provider auth, `ModelsDev.Service`, provider HTTP/SSE bridge, Core `ModelsDev`, `models-api.json` retained per LOCK-006/009 — `models-api.json` 3.0 MB committed build-time snapshot and generic `Core.ModelsDev` runtime (disk cache, fallback fetch, 60-minute background refresh, explicit `kilo models --refresh`; `bun run refresh:models` only refreshes checked-in snapshot) remain open P4.4 residues; dead synthetic Kilo provider factory `createKiloFallbackProvider` and synthetic literals `Kilo Gateway`/`settings.providers.note.kilo`/`KILO_API_KEY`/`noteKey`/`icon`/`priority`/`custom` deleted from `packages/kilo-vscode/src/shared/provider-model.ts` — file now exports `KILO_PROVIDER_ID`, `KILO_AUTO`, `PROVIDER_PRIORITY`, `providerOrderIndex` plus `CUSTOM_PROVIDER_PACKAGES`/`PROVIDER_ID_PATTERN`/`parseModelString`/`isCustomProviderPackage` with no unused imports/types beyond that deletion (P4.4-T12); orphaned locale keys `settings.providers.tag.gateway`/`dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` (20 locales) and `settings.providers.note.kilo` from every `packages/kilo-i18n/src/*.ts` (20 locales) — retained `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`, session `KILO_AUTO` fallback, speech guard, generic custom-provider UI per LOCK-006/009/010; config-console no longer reads the inventory (handler call sites removed P4.4-T1b) | test: `p4-3-cutover.test.ts:35-38` (canonical-loader absence); `p4-4-managed-removal.test.ts` (helper file/import-surface absence, direct-test removal, canonical-loader preservation); `p4-4-wellknown-provider-auth-removal.test.ts` (CLI well-known discovery branch absence, stale config.test helpers removed, retained generic provider-login guarded); `p4-4-provider-metadata-removal.test.ts` (preset helper file/import-surface absence, handler enrichment absence, `all`/`connected`/`default`/`failed` aggregation and auth/model/config dependency preservation, `Provider.Info` optional `metadata` contract unchanged, generated OpenAPI/SDK unchanged, generic adapters/custom-provider/bridge preservation, sorted test-profile `managed < primary < provider < wellknown`); `p4-4-bundled-provider-loader-removal.test.ts` (static source presence only — preset loader `KILO_BUNDLED_PROVIDERS`/`createKilo`/`"@kilocode/kilo-gateway"` loader absence, `BUNDLED_PROVIDERS` generic adapters/`models-api.json`/`ModelsDev`/`ModelCache`/custom-provider/`/provider` bridge static preservation, dynamic fallback intentionally unproven, sorted test-profile `bundled < managed < primary < provider < wellknown`); `p4-4-model-cache-removal.test.ts` (kilo/apertis network-absence, handler/models absence, `models-api.json`/`models-dev.ts`/`BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge preservation, `clear`/`get`/`failedProviders` runtime invalidation, sorted test-profile `bundled < managed < model-cache < primary < provider < wellknown`); `p4-4-provider-login-preset-removal.test.ts` (preset priority/hint/codex alias absence in `cli/cmd/providers.ts` and `cli/cmd/github.handler.ts`, deterministic generic `sortBy((x) => x.name ?? x.id)` ordering, `ModelsDev.Service`/plugin/custom-provider/`other` path and `/provider` bridge preservation, Core `ModelsDev`/`models-api.json` retained, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < wellknown`) | doc: runtime §8.1; P4.3 cutover evidence §6 | - | helper deleted (P4.4-T3); source inventory removed (P4.4-T1b); CLI well-known discovery deleted (P4.4-T4); preset metadata enrichment deleted (P4.4-T5) — `Provider.Info` optional `metadata` remains contract-compatible when omitted; config-console source reporting removed; canonical loader has no managed/org read | orphaned locale keys `settings.providers.tag.gateway`/`dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` (20 locales) and `settings.providers.note.kilo` removed from every `packages/kilo-i18n/src/*.ts` (20 locales) (P4.4-T12) — no production consumer; other provider/org/catalog labels remain residual but not proof of an effective reader; `Auth.WellKnown` storage type remains in `packages/opencode/src/auth/index.ts` for persisted credential compatibility; `KILO_BUNDLED_PROVIDERS`, `models-api.json`, `models.dev` snapshot, `ModelCache`, and `providerMetadata` schema optionality remain open P4.4 residues (LOCK-006) | deleted: CLI `.well-known/opencode` fetch at `packages/opencode/src/cli/cmd/providers.ts:327` removed (P4.4-T4); preset display metadata enrichment at `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts:85` removed (P4.4-T5) — `Provider.toPublicInfo` without preset enrichment; CLI preset selector priority/hint/alias at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`buildProviderOptions` `providers.ts:236-257` / `matchProviderInput` `providers.ts:258-274`) and `cli/cmd/github.handler.ts` `githubInstall`/`promptProvider()` (`buildGithubProviderOptions` `github.handler.ts:156-166` / `promptProvider()` `github.handler.ts:240-250`) removed (P4.4-T8) — deterministic generic `sortBy((x) => x.name ?? x.id)` ordering; no public server endpoint or generated SDK/OpenAPI regeneration required (LOCK-009 bridge preserved; optional `metadata` field unchanged; `models-api.json` retained) | effective inactive: managed/cloud anchors are absent from `Config.Service`; CLI well-known provider discovery is absent from `cli/cmd/providers.ts`; preset provider display metadata enrichment is absent from the provider handler (`providerMetadata` import and call absent; listing is `Provider.toPublicInfo` only); `effective` is `config.get()`; managed helper and preset metadata helper files absent and no production import remains; provider endpoint still serves `all`/`connected`/`default`/`failed` via generic/custom-provider paths; CLI preset selector ordering/hint/alias is absent from `cli/cmd/providers.ts` (`priority` map, `kilo`/`openai` hints, `codex` alias absent; ordering is `sortBy((x) => x.name ?? x.id)`) and `cli/cmd/github.handler.ts` (`priority`/`recommended` absent; ordering is `sortBy((x) => x.name ?? x.id)`); generic `ModelsDev.Service`/plugin/custom paths and HTTP/SSE bridge remain; Core `ModelsDev`/`models-api.json` retained (preset selector removal only); canonical tests prove managed values do not load |
| 10 | Top-level `mode`/`tools` conversions | residual retained; evidence date: 2026-08-26 (P4.4-T9) | residual: no current `Config.Service` conversion found; `sources.ts` diagnostic taxonomy removed (P4.4-T1b); handwritten SDK wrapper `mergeConfig` that preserved `mode`/plugin/config for child env deleted (P4.4-T9) — wrappers now retain only `...process.env` and `logLevel` arg passthrough | test: exact P4.3 absence assertions in `packages/opencode/test/kilocode/p4-3-cutover.test.ts:48-51`; `p4-4-sdk-config-forwarding-removal.test.ts` verifies no `mergeConfig`/`mode` forwarding residue in wrappers and `logLevel` passthrough preserved | doc: runtime §8.1; P4.3 cutover evidence §7 | SDK/server-wrapper evidence (handwritten): wrappers now contain no `mergeConfig`/`mode` preservation for child env (verified); Actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009) | residual: no effective top-level conversion in canonical config; handwritten wrapper forwarding deleted; typed assets are canonical | - | removed: handwritten SDK wrapper no longer merges `mode`/plugin/config via `KILO_CONFIG_CONTENT`; package path remains with `...process.env` | effective inactive: `result.mode`, `result.tools`, and `loadMode` conversion anchors are absent from canonical config/agent paths; wrapper no longer forwards `mode` via env |
| 11 | Arbitrary CLI/env override layers | residual retained; evidence date: 2026-08-26 (P4.4-T9) | residual: sandbox safety environment policy at `packages/opencode/src/kilocode/sandbox/policy.ts:168-181` (retained; `KILO_CONFIG_CONTENT` deny preserved); handwritten SDK wrapper `KILO_CONFIG_CONTENT` env construction at `packages/sdk/js/src/v2/server.ts:68-74,155-163` and `src/server.ts` equivalent deleted (P4.4-T9) — wrappers now retain only `env: { ...process.env }` and `logLevel` arg | test: `p4-3-cutover.test.ts:39-51`; `p4-4-sdk-config-forwarding-removal.test.ts` verifies wrapper forwarding deleted and sandbox deny preserved; `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts:168-190` | doc: runtime §8.1; P4.3 cutover evidence §7 | SDK/server-wrapper evidence (handwritten): wrapper now forwards only `...process.env` without `KILO_CONFIG_CONTENT` (verified); actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009) | residual: safety policy remains (environment deny `KILO_CONFIG_CONTENT`/`KILO_CONFIG`/`KILO_CONFIG_DIR` preserved, not an effective-config overlay); handwritten wrapper env forwarding deleted | - | removed: handwritten SDK wrapper `KILO_CONFIG_CONTENT` forwarding deleted; SDK/server bridge machinery remains for transport narrowing with `...process.env` | effective inactive: no generic override layer is applied by `Config.Service`; sandbox denial remains as safety but is not an effective source read; diagnostic SDK wrapper forwarding deleted |
| 12 | Legacy global config filenames/readers | residual retained; evidence date: 2026-08-25 | residual: `sources.ts:59-60,99-105` filename inventory removed (P4.4-T1b); TUI migration helper remains | test: `p4-3-cutover.test.ts:20-27,64-78`; `packages/opencode/test/kilocode/config/config.test.ts:610-667` | doc: runtime §8.1; P4.3 cutover evidence §6 | - | residual: TUI migration filename handling remains; source filename inventory removed (P4.4-T1b); canonical stamp/loader is `kilo.jsonc` only | - | residual: TUI/diagnostic paths remain packaged | effective inactive: all legacy filename/readers are absent from `config/config.ts`; `KilocodeGlobalConfigStamp` is canonical-only |
| 13 | Legacy migration readers/import tooling | residual retained; evidence date: 2026-08-25 (post-removal working-tree evidence, not the `8167992b` baseline) | extension legacy-migration and Roo import source removed; TUI migration helper remains at `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts:33-80,148-166` | test: `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts:420-444`; `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts:107-114` | doc: runtime §8.1; `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md:264-266`; this matrix | - | residual: intentional migration/Roo-specific locale keys remain removed; generic settings import/export remains | residual: TUI migration surface remains P4.5-adjacent | extension importer/Roo wiring is removed and cannot read legacy data or write canonical config; CLI/TUI migration helper remains open residual work. Row remains open/residual; no P4.4 completion claim |

## Residual machinery and open P4.4 work

### `KilocodeConfigSources` and config-console

The legacy `KilocodeConfigSources` diagnostic inventory reader
(`packages/opencode/src/kilocode/config/sources.ts`, the 12-source listing of
env, legacy filename, project, cloud, managed, and runtime sources) was
**physically removed on 2026-08-25 (P4.4-T1b)**. The `KilocodeConfigSources`
type/echo is gone from `packages/opencode/src/kilocode/config/overlay.ts`
(`Result`/`Input`/`resolve` no longer carry `sources`), and the config-console
handler
(`packages/opencode/src/kilocode/server/httpapi/handlers/config-console.ts`)
no longer acquires auth/account state for an inventory and no longer exposes a
`sources` handler. The `/config/sources` schema/path/endpoint/handler and the
`ConfigSourcesResponse` schema were removed from the config-console group, and
the `sources` echo field was removed from `ConfigOverlayResponse`. The
config-console handler's effective value remains `config.get()`; the retained
overlay resolution is unchanged. No replacement shim or import/migration
behavior was added (LOCK-014/015); the behavior is recorded as a dropped
reconstruction candidate. This unit does not remove the remaining config-console
bridge endpoints (`/config/overlay`, `/config/effective`, `/config/transaction`,
rules, model-state, TUI config/keybinds).

### Canonical merge versus the deleted multi-source merge

P4.3 deleted the legacy reader fan-out, migration readers, and `loadMode` path
from retained effective-config loading. The canonical loader and canonical
overlay now use the global/project `kilo.jsonc` roots and typed assets. P4.4-T9
physically removed the handwritten SDK `mergeConfig`/`parseExistingConfig`/
`buildConfigEnv` child-process helper and its `KILO_CONFIG_CONTENT` env
forwarding from `packages/sdk/js/src/server.ts` and
`packages/sdk/js/src/v2/server.ts` (retaining only `...process.env` and
`logLevel` arg passthrough per LOCK-009/010/014; generated SDK/OpenAPI and
HTTP/SSE bridge unchanged). The diagnostic overlay and TUI readers remain
separately inventoried and are not claimed gone. No 12-source effective merge
is treated as active based solely on those retained residues.

### Convergence

`packages/opencode/src/kilocode/server/config-convergence.ts:1-43` remains the
active canonical save/convergence coordinator. This unit records its presence
as residual P4.4 machinery only; it does not decide or implement any narrowing
of convergence, admission fences, disposal, or boot behavior.

### Provider catalog and LOCK-006

Preset/provider catalog, onboarding, and organization residues remain a separate
P4.4 task under LOCK-006. Existing provider catalog and organization/i18n
surfaces are not counted as removed by this matrix except for the bounded
P4.4-T5 preset display metadata enrichment removal (`kilocode/provider/metadata.ts`
and its handler `metadata: providerMetadata(item.id)` enrichment deleted;
optional `Provider.Info` `metadata` field and `Provider` OpenAPI/SDK schema remain
optional/contract-compatible, webview `provider-catalog.ts` fallback preserved)
and the bounded P4.4-T6 preset `@kilocode/kilo-gateway` bundled provider loader
removal (`KILO_BUNDLED_PROVIDERS` ` "@kilocode/kilo-gateway": async () => createKilo`
and its `...KILO_BUNDLED_PROVIDERS` spread from `provider/provider.ts`
`BUNDLED_PROVIDERS` deleted; `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`,
generic adapters, `models-api.json` 3.0 MB committed build-time snapshot, `models.dev`
catalog, `ModelCache`, custom-provider save/delete/validation, and auth lifecycle
remain open per LOCK-006/009) and the bounded P4.4-T7 kilo/apertis dynamic model-fetch/cache removal (`provider/model-cache.ts` simplified to invalidation-only `clear` without network — `fetchKiloModels`/`KiloModelsService`/`kiloModelsLayer`/`APERTIS_BASE_URL`/`api.apertis.ai`/`fetchApertisModels`/`authOptions`/`KILO_API_KEY`/`APERTIS_API_KEY`/`kilocodeToken`/`HttpClient` deleted; `provider/models.ts` removed `ModelCache`/`Config`/`Auth` and `kilo`/`apertis` injection — now static `Core` wrapper only; handler `ModelCache.Service`/`failedProviders()` removed — `failed: []`; retained generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with on-disk cache at `${cache}/models.json`, fallback `GET ${source}/api.json` when cache and snapshot are both unavailable, 5-minute TTL, 60-minute scoped background refresh, explicit `kilo models --refresh` / `ModelsDev.refresh`; `models-api.json` is a build-time snapshot and `bun run refresh:models` only refreshes the checked-in snapshot and is not the sole runtime refresh) plus `BUNDLED_PROVIDERS` generic adapters/`KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`/custom-provider `clear`/bridge retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed). The matrix row for cloud/org/
managed sources records only effective-config inactivity and the retained
diagnostic/provider residue, now updated for the T7 dynamic-cache deletion.

### Backend fetch and public transport chain

The SDK/server wrappers still launch `kilo` with `...process.env` and `logLevel` arg passthrough but no longer forward `KILO_CONFIG_CONTENT` (`packages/sdk/js/src/v2/server.ts` and `src/server.ts` handlers deleted P4.4-T9; generated SDK/OpenAPI unchanged per LOCK-009). The CLI/server still exposes HTTP/SSE, health, and provider fetch surfaces. This unit does not narrow or delete them. P4.4 transport narrowing remains open, and P5 structural absence of the selector startup fetch chain has not started.

> **P4.4-T1a note (2026-08-25) — extension-side periodic `/global/health` poll removed.**
> `KiloConnectionService` no longer polls `/global/health`: `HEALTH_POLL_INTERVAL_MS`,
> `startHealthPoll`/`stopHealthPoll`/`checkHealth`, the `healthPollTimer` field,
> and all call sites are gone from
> `packages/kilo-vscode/src/services/cli-backend/connection-service.ts` (source).
> Evidence: test: `packages/kilo-vscode/tests/unit/connection-service-health-poll-absence.test.ts`
> (6 tests / 22 expect calls — poll-surface absence plus retained-transport presence
> guard: `SdkSSEAdapter` heartbeat/reconnect remains the sole liveness path,
> LOCK-009); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`
> (health-poll row removed from the connection liveness/disposal table). Only the
> extension-side periodic poll is removed: the server-side `/global/health`
> route, the HTTP/SSE/generated-SDK bridge, provider/catalog (LOCK-006),
> convergence, storage/sync, CLI/TUI, and this back-end-gated fetch chain remain
> open; P4.4 transport narrowing, P4.5, and P5 remain open. No matrix row is
> marked complete and no numeric performance claim is made (LOCK-PERF-6).
> Working-tree evidence, not yet committed as of 2026-08-25.

> **P4.4-T1b note (2026-08-25) — legacy source-inventory reader and `/config/sources` removed.**
> This bounded unit physically removed the diagnosis-only source inventory from
> effective-config surfaces (LOCK-010/014/015).
> Source: `packages/opencode/src/kilocode/config/sources.ts` (the 12-source
> `KilocodeConfigSources` inventory reader) deleted; `KilocodeConfigSources`
> import/type/echo removed from
> `packages/opencode/src/kilocode/config/overlay.ts` (`Result`/`Input`/`resolve`
> no longer carry `sources`); `/config/sources` schema/path/endpoint/handler and
> `ConfigSourcesResponse` removed from
> `packages/opencode/src/kilocode/server/httpapi/groups/config-console.ts` and
> `handlers/config-console.ts`, and the `sources` echo field removed from
> `ConfigOverlayResponse`. The overlay endpoint still resolves effective/global/
> project via `config.get()`/`config.getGlobal()`/`KilocodeConfigOverlay.resolve`
> with no behavioral change to canonical resolution.
> Tests: `packages/opencode/test/kilocode/p4-4-source-inventory-removal.test.ts`
> (static absence of the reader, the `/config/sources` contract in production
> source, generated v2 SDK, and `packages/sdk/openapi.json`, plus retained
> overlay/effective/transaction/rules/model-state/TUI bridge presence);
> `config-sources.test.ts` deleted (it exercised the removed endpoint);
> `httpapi-exercise-scenarios.ts` and `httpapi-public.test.ts` drop the removed
> route entries; `p4-3-overlay-mode.test.ts` and `config-overlay.test.ts`
> updated for the `sources`-less overlay `Input`/`Result`.
> Docs: `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md`
> (source-inventory removal, not a surviving diagnostic); skill
> `packages/opencode/src/kilocode/skills/kilo-config.md`; P4.3 cutover evidence
> P4.4 row.
> Generated SDK: `packages/sdk/js/src/v2/gen/sdk.gen.ts` and `types.gen.ts` and
> `packages/sdk/openapi.json` regenerated via `bun ./script/generate.ts` from the
> repo root (twice, idempotent); no `config.sources`/`ConfigSourcesResponse`
> remains.
> Row delta: the `sources.ts` diagnostic-inventory residual cited in rows 1-7,
> 9, 10, and 12 is physically removed; each such row remains `residual retained`
> for its other retained residues (TUI migration/`ConfigPaths`, instruction
> profile, sandbox policy, SDK wrapper forwarding, managed/primary-worktree
> helpers) and no row is marked complete. Rows 8 and 11 carried no `sources.ts`
> residual and are unchanged. The retained config-console bridge endpoints
> (`/config/overlay`, `/config/effective`, `/config/transaction`, rules,
> model-state, TUI config/keybinds) and the overlay/transaction handlers are
> preserved (LOCK-009). Evidence-class reproduction of this unit's own row-level
> partial/closed delta is recorded per row above and in the tracker §7 rows; the
> behavior is recorded as a dropped reconstruction candidate (RC-P4.4-001,
> tracker section 7 registry) per LOCK-015. No risk-area code was removed beyond
> the inventory and its endpoint; P4.4 remains Active; no performance claim is
> made (LOCK-PERF-6). Working-tree evidence, not yet committed as of 2026-08-25.

> **P4.4-T2 note (2026-08-25) — orphaned primary-worktree mirror-read helper physically removed.**
> This bounded unit physically removed the unused primary-worktree discovery helper
> that had only test consumers after the P4.3 canonical cutover (managed worktree
> infrastructure permanently removed; canonical project assets live only under the
> first workspace root `.kilo` boundary; no mirror-read fallback).
> Source: `packages/opencode/src/kilocode/primary-worktree.ts` (64 lines, `primaryPaths`/`primaryWorktree`) deleted; direct test `packages/opencode/test/kilocode/primary-worktree.test.ts` deleted; glob entry `primary-worktree` removed from `packages/opencode/script/kilocode/test-profile.ts`; no production consumer existed (`config/config.ts`, `kilocode/config/config.ts`, `skill/index.ts`, `config/paths.ts` contain no `primary-worktree`/`primaryPaths`/`primaryWorktree` import). Canonical-root handling (`canonicalRoot`) and `ConfigPaths` (`packages/opencode/src/config/paths.ts` unchanged per scope) plus all legacy-reader cutover proofs, retained config-console endpoints, private worker bridge, and existing HTTP/SSE bridge remain untouched.
> Tests: `packages/opencode/test/kilocode/p4-4-primary-worktree-removal.test.ts` (static file-deletion, helper-import absence across CLI source, test-profile glob absence, canonical-root/`ConfigPaths` preservation); `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` updated to assert helper file/test absence and zero `primaryWorktree`/`primaryPaths`/`primary-worktree` surface in CLI source (P4.4 exception closed); retained `p4-3-cutover.test.ts` cutover anchors continue to prove canonical-only loading.
> Row delta: row 6 source/config columns updated to record `primary-worktree.ts:7-37` deletion; row 8 status moved from `residual retained` to `removed (helper deleted)` with source/test/docs evidence for physical deletion and import-surface absence; rows 1-5,7,9-13 unchanged except for row 6 config clarification. No new reconstruction-candidate entry is required: primary-worktree mirror reads are a permanent removal class (runtime §8.1) recorded in the tracker §7 removal inventory, not a displaced behavior needing evidence preservation; inherent permanent removals are not candidate rows. `ConfigPaths`, TUI/instruction, SDK wrapper forwarding, sandbox policy, and transport narrowing remain open P4.4 residues; no phase completion or performance claim is made. Working-tree evidence, not yet committed as of 2026-08-25.
>
> **P4.4-T3 note (2026-08-25) — unreachable legacy managed/MDM configuration helper physically removed.**
> This bounded unit physically removed the dead managed configuration reader that cannot affect effective configuration after the P4.3 cutover (canonical authored config is only one global root and `<workspaceRoot>/.kilo/`; no generic managed/legacy overlay is a target source; permanent removal is direct with no shim).
> Source: `packages/opencode/src/config/managed.ts` (77 lines, `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences`, `MANAGED_PLIST_DOMAIN`) deleted; direct tests `ConfigManaged` import and 5 `parseManagedPlist` unit tests removed from `packages/opencode/test/config/config.test.ts` (helper-only tests; `p4-3-cutover.test.ts` preservation and `Config.Service` canonical loader behavior unchanged); supporting `managedConfigDir`/`writeManagedSettingsEffect` harness removed. No production import remains — `config/config.ts`, `kilocode/config/config.ts`, `kilocode/config/overlay.ts`, `config/paths.ts` contain no `ConfigManaged`/`managedConfigDir`/`readManagedPreferences`/`parseManagedPlist`/`MANAGED_PLIST`/`ai.opencode.managed` reference; `ConfigPaths` and canonical-root handling preserved per scope.
> Tests: `packages/opencode/test/kilocode/p4-4-managed-removal.test.ts` (static file-deletion, helper-import absence across CLI source including `KILO_TEST_MANAGED_CONFIG_DIR`, test-profile glob presence, canonical-loader managed-absence preservation, direct-test removal, `ConfigPaths`/`canonicalRoot` preservation); retained `p4-3-cutover.test.ts:35-38` cutover anchors continue to prove `managedConfigDir()`/`readManagedPreferences` absent from canonical loader.
> Row delta: row 9 source/test/config/inactive-proof columns updated to record `config/managed.ts` deletion and new absence regression; rows 1-8 and 10-13 unchanged except for row 9. No new reconstruction-candidate entry is required: cloud/org/managed config sources are a permanent removal class (runtime §8.1) recorded in the §7 removal inventory; the unreachable helper has no target value and is not a displaced behavior needing evidence preservation; inherent permanent removals are not candidate rows. Provider/org/catalog labels, `sources.ts` already deleted (P4.4-T1b), `primary-worktree` already deleted (P4.4-T2), `ConfigPaths`, TUI/instruction, SDK wrapper forwarding, sandbox policy, and transport narrowing remain open P4.4 residues; no phase completion or performance claim is made. Working-tree evidence, not yet committed as of 2026-08-25.
>
> **P4.4-T4 note (2026-08-25) — legacy CLI `.well-known/opencode` provider-auth discovery URL-login branch physically removed.**
> This bounded unit physically removed the `args.url` branch at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`fetch` `<url>/.well-known/opencode`, `Process.spawn` of `auth.command`, `auth.set(url, { type: "wellknown", … })` persistence), removed the `login [url]` positional and its `Process`/`text` imports, and changed the command to `login` without URL discovery. Stale well-known intercept helpers (`remoteConfigClient`, `wellKnownAuth`, `json`/`wellKnown` helpers) were removed from `packages/opencode/test/config/config.test.ts`. No production import remains for the well-known fetch path; canonical loader unchanged.
> Tests: `packages/opencode/test/kilocode/p4-4-wellknown-provider-auth-removal.test.ts` (CLI well-known branch absence, stale helpers removed, retained generic provider-login guarded: `ProvidersLoginCommand`, `handlePluginAuth`, `resolvePluginProviders`, api-key/plugin/custom-provider paths preserved; P4.3 cutover anchors preserved); `packages/opencode/script/kilocode/test-profile.ts` sorted glob `managed < primary < wellknown` updated. Generic provider login and `Config.Service`/`Plugin`/`ModelsDev` remain; HTTP/SSE/generated-SDK bridge preserved (LOCK-009); no SDK regeneration required (no public endpoint change).
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record CLI well-known discovery deletion; rows 1-8 and 10-13 keep prior T3 delta. No reconstruction-candidate entry required (cloud/org/managed permanent removal class, LOCK-014/015). All other provider residues (`KILO_BUNDLED_PROVIDERS`, `models-api.json`, `ModelCache`, `metadata.ts` still present at that date) remain open. P4.4 remains Active.
>
> **P4.4-T5 note (2026-08-25) — preset provider display metadata enrichment physically removed.**
> This bounded unit physically removed the preset-only display metadata enrichment that added `noteKey`/`icon`/`priority` to provider-list responses (LOCK-006 preset surface, direct removal per LOCK-014/015 — no reconstruction candidate needed for an explicitly removed preset surface).
> Source: `packages/opencode/src/kilocode/provider/metadata.ts` (41 lines, `ProviderMetadata` type, `notes`/`order`/`priority`/`icons`/`key()`/`providerMetadata()`) deleted; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` import `providerMetadata` and `metadata: providerMetadata(item.id)` enrichment removed — listing now `all: Object.values(validProviders).map((item) => Provider.toPublicInfo(item))` without preset enrichment; `all`/`connected`/`default`/`failed` aggregation, `Config.Service`/`Provider.Service`/`ProviderAuth.Service`/`ModelCache.Service`/`ModelsDev.Service` dependencies, `filterPromptTrainingModels`/`overlayAnacondaDesktop`/`pickBy` valid-providers guard, and `/provider` endpoint wiring (`list`/`auth`/`authorize`/`callback`) retained. No production source references `kilocode/provider/metadata` after removal. Direct obsolete test `packages/opencode/test/kilocode/provider-metadata.test.ts` deleted.
> Tests: `packages/opencode/test/kilocode/p4-4-provider-metadata-removal.test.ts` (preset helper file/test deletion, `kilocode/provider/metadata` and `providerMetadata(item.id)` import-surface absence across CLI source, handler enrichment absence with `all`/`connected`/`default`/`failed` and dependency/endpoint preservation, `Provider.Info` optional `metadata` contract unchanged — `ProviderMetadata` struct with `noteKey`/`icon`/`priority` remains `optionalOmitUndefined`, OpenAPI `Provider` `metadata` not in `required` and contract-compatible when omitted, generated SDK `metadata?:` preserved, webview `provider-catalog.ts` fallback (`synthetic`/`FALLBACK_PROVIDER_IDS`/`validIcon`) preserved, generic adapters `KILO_BUNDLED_PROVIDERS`/`models-api.json`/`models.ts`/`ModelCache`/custom-provider save/delete/auth lifecycle preserved, sorted test-profile `managed < primary < provider < wellknown`); retained provider handler/provider tests and config/cutover suites plus `p4-3-cutover.test.ts` anchors.
> Row delta: row 9 source/test/config/i18n/build/inactive-proof updated to record `metadata.ts` and handler enrichment deletion with contract-preservation evidence; rows 1-8 and 10-13 keep prior T4 delta except for provider-residue clarification; provider catalog section updated. No new reconstruction-candidate entry is required per LOCK-014/015: preset display metadata is an explicitly removed preset display surface; preset catalogs (`KILO_BUNDLED_PROVIDERS`, `models-api.json` 3.0 MB), `models.dev` catalog, `ModelCache`, generic adapters, custom-provider paths, HTTP/SSE/generated-SDK bridge, and provider `metadata` optional schema all remain open P4.4 residues. P4.4 remains Active; no phase completion or performance claim.
>
> **P4.4-T6 note (2026-08-26) — preset `@kilocode/kilo-gateway` bundled provider loader physically removed.**
> This bounded unit physically removed the sole preset identity loader `KILO_BUNDLED_PROVIDERS` (`"@kilocode/kilo-gateway": async () => createKilo`) and its `createKilo` import plus `BundledSDK` helper from `packages/opencode/src/kilocode/provider/provider.ts`, and removed its import/spread (`...KILO_BUNDLED_PROVIDERS`) from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS` (LOCK-006 preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change).
> Source: `kilocode/provider/provider.ts` now imports `{ type KiloProvider, AI_SDK_PROVIDERS, PROMPTS }` without `createKilo`; `provider/provider.ts` `BUNDLED_PROVIDERS` retains generic SDK loaders (`@ai-sdk/openai`, `@ai-sdk/anthropic`, `@ai-sdk/google`, `@ai-sdk/azure`, `@ai-sdk/openai-compatible`, `@openrouter/ai-sdk-provider`, etc.) without `KILO_BUNDLED_PROVIDERS`.
> Tests: `packages/opencode/test/kilocode/p4-4-bundled-provider-loader-removal.test.ts` (preset loader `KILO_BUNDLED_PROVIDERS`/`createKilo`/`"@kilocode/kilo-gateway"` absence, `KILO_BUNDLED_PROVIDERS` import/spread absence, generic adapters/`models-api.json`/`ModelsDev`/`ModelCache`/custom-provider/`/provider` bridge preservation, sorted test-profile `bundled < managed < primary < provider < wellknown`); retained `p4-3-cutover.test.ts` and provider handler tests.
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record `KILO_BUNDLED_PROVIDERS` and loader spread deletion with generic-adapter/catalog/bridge preservation; rows 1-8 and 10-13 keep prior T5 delta. No new reconstruction-candidate entry is required per LOCK-014/015: preset bundled loader is an explicitly removed preset identity surface; `models-api.json` 3.0 MB, `models.dev` catalog, `ModelCache`, generic adapters, custom-provider paths, HTTP/SSE/generated-SDK bridge, and provider `metadata` optional schema all remain open P4.4 residues. P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).
>
> **P4.4-T7 note (2026-08-26) — kilo/apertis dynamic model-fetch/cache physically removed.**
> This bounded unit physically removed the `kilo` and `apertis` dynamic model-fetch/cache subsystem (LOCK-006 preset/org source removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change).
> Source: `provider/model-cache.ts` simplified to invalidation-only `clear` (with stubbed `get`/`fetch`/`refresh`/`getFailure`/`failedProviders` without network — `fetchKiloModels`/`KiloModelsService`/`kiloModelsLayer`/`APERTIS_BASE_URL`/`api.apertis.ai`/`fetchApertisModels`/`authOptions`/`HttpClient`/`KILO_API_KEY`/`APERTIS_API_KEY` deleted); `provider/models.ts` removed `ModelCache`/`Config`/`Auth` and `kilo`/`apertis` injection (`cache.fetch`/`KILO_OPENROUTER_BASE`/`providers.kilo`/`apertis`/`baseURL`) — now static `Core` overlay only; `server/routes/instance/httpapi/handlers/provider.ts` removed `ModelCache.Service`/`failedProviders()` — now `failed: string[] = []` with `validProviders` guard without failedSet. Deleted obsolete kilo/apertis cache tests (`model-cache-effect.test.ts`, `model-cache-org.test.ts`, `provider-list-failed-state.test.ts`, `kilo-models-401-fallback.test.ts`) and simplified `kilo-loader-auth.test.ts` to `kiloCustomLoaders` helpers only; updated `p4-4-bundled-provider-loader-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts` expectations to reflect handler/models absence.
> Tests: `packages/opencode/test/kilocode/p4-4-model-cache-removal.test.ts` (kilo/apertis network-absence, handler/models absence, `models-api.json`/`models-dev.ts`/`BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge preservation, `clear`/`get`/`failedProviders` runtime invalidation with injectable defect, sorted test-profile `bundled < managed < model-cache < primary < provider < wellknown`); retained `p4-3-cutover.test.ts`, provider handler, and custom-provider save/delete lifecycle tests.
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record dynamic-cache deletion with static-catalog/adapter/bridge preservation; rows 1-8 and 10-13 keep prior T6 delta. No new reconstruction-candidate entry is required per LOCK-014/015: kilo/apertis dynamic cache is an explicitly removed preset/org source (LOCK-006 permanent removal); `models-api.json` 3.0 MB, `models.dev` catalog, generic adapters, `ModelCache` `clear`, custom-provider paths, HTTP/SSE bridge, and optional `metadata` schema all remain open P4.4 residues. P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).
>
> **P4.4-T8 note (2026-08-26) — preset provider login selector identity physically removed.**
> This bounded unit physically removed hardcoded preset ordering/recommendation/alias metadata from CLI provider login selectors (LOCK-006 preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change; Core `ModelsDev` and `models-api.json` retained per scope).
> Source: `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` removed `priority: Record<string, number> = { kilo: 0, anthropic: 2, "github-copilot": 3, openai: 4, google: 5, openrouter: 6, vercel: 7 }` and `hint: { kilo: "recommended", openai: "ChatGPT login or API key" }[x.id]` and `codex` -> `openai` alias (`const alias = ... "codex" ? ... "openai" ...` / `byID ?? byName ?? alias`), replaced with deterministic generic helpers `buildProviderOptions` (`providers.ts:236-257`) and `matchProviderInput` (`providers.ts:258-274`) using `sortBy((x) => x.name ?? x.id)` and `map((x) => ({ label: x.name, value: x.id }))` without preset hint; `packages/opencode/src/cli/cmd/github.handler.ts` `githubInstall`/`promptProvider()` removed `priority: Record<string, number> = { kilo: 0, anthropic: 1, openai: 2, google: 3 }` and `hint: priority[x.id] === 0 ? "recommended"` with same alphabetical `sortBy` via `buildGithubProviderOptions` (`github.handler.ts:156-166`) and `promptProvider()` (`github.handler.ts:240-250`). No dead imports/helpers required removal beyond the priority maps/hints/alias (remeda `map`/`pipe`/`sortBy`/`values` retained for generic ordering); `handlePluginAuth`/`resolvePluginProviders`/`Other` custom path and `ModelsDev.Service`/`Plugin.Service`/`Config.Service` preserved.
> Tests: `packages/opencode/test/kilocode/p4-4-provider-login-preset-removal.test.ts` — static absence of preset priority/hint/codex alias in `cli/cmd/providers.ts` (`ProvidersLoginCommand`/`buildProviderOptions`/`matchProviderInput`) and `cli/cmd/github.handler.ts` (`buildGithubProviderOptions`/`promptProvider()`), plus executable coverage via real implementation (`buildProviderOptions` ordering/prompt contents/hint absence with insertion-order tie preservation for same-name collisions, `buildGithubProviderOptions` ordering without hint, `matchProviderInput` codex rejection and exact id plus case-insensitive name dispatch with distinct-id assertions, `resolvePluginProviders` plugin/custom/`other` dispatch), `ModelsDev.Service`/plugin/custom-provider/`other` path and `/provider` `list`/`auth`/`authorize`/`callback` bridge preservation, Core `ModelsDev`/`models-api.json` retained, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < wellknown`; retained `p4-3-cutover.test.ts`, provider handler, and custom-provider lifecycle tests.
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record selector-identity deletion with generic ordering/bridge/catalog preservation; rows 1-8 and 10-13 keep prior T7 delta. No new reconstruction-candidate entry is required per LOCK-014/015: preset selector ordering/hint/alias is an explicitly removed preset identity surface (LOCK-006 permanent removal); `models-api.json` 3.0 MB, `models.dev` catalog, generic adapters, `ModelCache` `clear`, custom-provider paths, HTTP/SSE bridge, webview Kilo fallback, and optional `metadata` schema all remain open P4.4 residues. P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).

> **P4.4-T10 note (2026-08-26) — synthetic Kilo provider identity removed from VS Code settings surfaces.**
> This bounded unit removed settings-side synthesis of the Kilo Gateway provider and its Kilo-specific display/login branches so provider settings render only records supplied by existing provider state (LOCK-006 bounded settings-surface removal; not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; LOCK-PERF-6 no performance claim).
> Source: `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` simplified `visibleConnectedIds` to identity `[...connected]` and `providersWithKiloFallback` to identity `return providers` (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` import; retained as identity exports for compatibility); `provider-catalog.ts` removed Kilo-specific `providerIcon` `KILO_PROVIDER_ID` branch and `providerNoteKey` `settings.providers.note.kilo` branch and deleted `kiloFallbackProvider()` export (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` imports; retained `isPopularProvider`/`popularProviderIndex`/`sortProviders` and generic `providerIcon`/`providerNoteKey` with `validIcon` fallback); `ProvidersTab.tsx` removed `providersWithKiloFallback` injection, `KILO_PROVIDER_ID` import, `canonicalMode` Kilo filter, `sourceTag` `settings.providers.tag.gateway`, `connectProvider` `server.goToLogin()` Kilo branch, add-list `common.signIn` Kilo label (now always `settings.providers.action.configure`), and `showAccountButton` Kilo routing (now always `false`); `ProviderSelectDialog.tsx` removed `kiloFallbackProvider()` injection (`withKilo` synthesis), `KILO_PROVIDER_ID` import, `server.goToLogin()` Kilo branch, and `dialog.provider.tag.recommended` Kilo tag. Out-of-scope preserved: `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` (with `settings.providers.note.kilo` metadata), speech-to-text `availability.ts` `KILO_PROVIDER_ID` guard, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), persisted state/auth storage, backend `/provider` APIs, SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider sorting/icons/notes/configure/auth/delete flows.
> Tests: updated `packages/kilo-vscode/tests/unit/provider-visibility.test.ts` (visibleConnectedIds identity, providersWithKiloFallback identity without synthesis) and `provider-catalog.test.ts` (generic icon/note, no Kilo branches) to prove no synthetic injection and generic behavior; added `packages/kilo-vscode/tests/unit/p4-4-settings-surface-no-synthetic-kilo.test.ts` (static absence of `KILO_PROVIDER_ID`/`createKiloFallbackProvider`/`kiloFallbackProvider`/`settings.providers.note.kilo`/`validIcon("kilo")`/`providersWithKiloFallback` injection/`settings.providers.tag.gateway`/`common.signIn`/`server.goToLogin`/`dialog.provider.tag.recommended` in the 4 allowed files plus preservation of shared constants and speech-to-text guard). No changeset per spec (internal migration).
> Row delta: no 13-row matrix row marked complete; rows 1-8,10-13 keep prior T9 delta; row 9 and provider catalog section updated to record bounded settings-surface Kilo synthesis removal with generic custom-provider preservation and out-of-scope `KILO_PROVIDER_ID`/`KILO_AUTO`/`createKiloFallbackProvider` retention. No new reconstruction-candidate entry required per LOCK-014/015: synthetic Kilo settings display is an explicitly removed preset display surface (LOCK-006 permanent removal class); preset catalogs (`models-api.json` 3.0 MB), `models.dev` catalog, `ModelCache` clear, generic adapters, and full preset org/catalog removal remain open P4.4 residues; this is not full LOCK-006 completion. P4.4 remains Active.

> **P4.4-T11 audit correction (2026-08-26) — suppress Kilo reconstruction and delete dead helpers.**
> This audit tightening corrects T10 to also suppress configured-row reconstruction and remove dead identity helpers (LOCK-006 bounded settings-surface removal, LOCK-009 bridge preserved, LOCK-013 docs to reality).
> Source: `packages/kilo-vscode/webview-ui/src/components/settings/provider-tab-helpers.ts` now guards `KILO_PROVIDER_ID` — `resolveConfiguredProvider` returns `undefined` when backend omits Kilo (before config fallback) and `buildConfiguredList` filters `undefined`, so a `kilo` entry in `disabled_providers`/`config.provider`/`authStates`/`connected` cannot synthesize a settings row when backend omits Kilo; generic custom-provider IDs still reconstruct from auth/config. `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` deleted dead exports `visibleConnectedIds`/`providersWithKiloFallback` (no production callers; file retained as placeholder with no exports, no `KILO_PROVIDER_ID`/`createKiloFallbackProvider`/`kilo`). `provider-catalog.ts`/`ProvidersTab.tsx`/`ProviderSelectDialog.tsx` T10 removals retained.
> Tests: `provider-tab-helpers.test.ts` adds blocker-path coverage (missing-backend-Kilo + auth/config + connected/disabled cannot produce Kilo row; generic custom still reconstructs; backend-present Kilo still renders); `p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to assert helper deletion and guard presence (`if (id === KILO_PROVIDER_ID) return undefined`); `provider-visibility.test.ts` deleted as obsolete (no production consumer). No changeset per spec.
> Row delta: row 9 source/test/docs updated to record reconstruction-guard addition and dead-helper deletion with generic custom-provider preservation and out-of-scope `KILO_PROVIDER_ID`/`KILO_AUTO`/`createKiloFallbackProvider` retention; rows 1-8,10-13 keep prior T10 delta. Still no 13-row matrix row marked complete; not full LOCK-006. P4.4 remains Active.

> **P4.4-T10 re-audit correction (2026-08-26) — empty placeholder and dead Kilo account/profile slot removed (style-only cleanup, no new phase).** This correction applies the two accepted T10 re-audit style fixes within the existing settings-surface cleanup boundary (LOCK-006 bounded settings-surface removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs to implemented reality). Source: `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` deleted (empty placeholder with no production exports — no imports remain, no production responsibility); `webview-ui/src/components/settings/provider-tab-helpers.ts` removed unreachable Kilo `account` primary-slot path — deleted `isKiloProvider` predicate, removed `"account"` from `PrimarySlot` type, removed `isKilo` from `resolvePrimarySlot` (priority now `edit → apiKey → chatgpt → anaconda → placeholder`); `webview-ui/src/components/settings/ProvidersTab.tsx` removed dead Kilo `account` branch — deleted `showAccountButton` (always `false`), removed `useServer` import and `server` const, deleted `primary() === "account"` `Match` with `IconButton person` and `server.goToProfile()` navigation. Generic custom-provider configuration, auth, API-key, edit, delete, sorting, add-list behavior and backend-present Kilo row rendering (as existing record only, via helpers guard) remain preserved; shared `KILO_PROVIDER_ID`/`KILO_AUTO`/`createKiloFallbackProvider` and speech-to-text Kilo guard remain out of scope. Tests: `provider-tab-helpers.test.ts` removed `isKiloProvider` suite and `account` slot case; `provider-connect-dialog-arch.test.ts` replaced account-slot presence asserts with absence asserts; `p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to assert module/file absence (`existsSync` false) and absence of `provider-visibility`/`isKiloProvider`/`showAccountButton`/`server.goToProfile`/`"account"` imports/references. No changeset, no new phase, not full LOCK-006 completion.
> Row delta: none — correction within T10/T11 settings-surface scope; row 9 and provider catalog section previously recorded for T10/T11 remain accurate with placeholder now recorded as deleted and account slot now recorded as removed; still no 13-row matrix row marked complete.

> **P4.4-T12 note (2026-08-26) — dead synthetic Kilo provider factory and orphaned i18n residues physically removed (dead-factory/i18n residue only).** This bounded unit deletes the dead synthetic factory and its orphaned locale residues only (LOCK-006 bounded dead-factory/i18n residue removal; not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-010 canonical config ownership unchanged; LOCK-013 docs to implemented reality; LOCK-PERF-6 no performance claim).
> Source: `packages/kilo-vscode/src/shared/provider-model.ts` deleted `createKiloFallbackProvider()` and synthetic literals (`Kilo Gateway`, `settings.providers.note.kilo`, `KILO_API_KEY` env, `noteKey`/`icon`/`priority`/`custom` source) — file now exports `KILO_PROVIDER_ID`, `KILO_AUTO`, `PROVIDER_PRIORITY`, `providerOrderIndex` plus `CUSTOM_PROVIDER_PACKAGES`/`CUSTOM_PROVIDER_PACKAGE`, `PROVIDER_ID_PATTERN`, `isCustomProviderPackage`, `parseModelString` — `createKiloFallbackProvider` deleted with no imports/types made unused beyond that deletion; locale keys `settings.providers.tag.gateway` and `dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` dictionary (20 locales) and `settings.providers.note.kilo` from every `packages/kilo-i18n/src/*.ts` dictionary (20 locales) per repository dictionary conventions — no production consumer remains (settings-surface synthesis/login/icon/note/recommended/gateway branches already removed in T10/T11, factory had no remaining call site since b780e5df9b). Retained protected surfaces untouched: `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`/`CUSTOM_PROVIDER_PACKAGES`, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), speech-to-text Kilo guard (`availability.ts` `KILO_PROVIDER_ID`), `PROVIDER_PRIORITY`/`ModelSelector` Kilo visibility, provider auth stitching/`goToLogin`/`KILO_AUTO` fallback, persisted auth/state, backend `/provider` API, generated SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider UI/sorting/icons/notes/add/configure/auth/delete, locale schema/key typing, formatting conventions.
> Tests: `packages/kilo-vscode/tests/unit/p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to prove factory/synthetic literal absence (`createKiloFallbackProvider`/`Kilo Gateway`/`settings.providers.note.kilo`/`noteKey`/`custom` source) and orphaned locale key absence across all dictionaries (20 + 20 locales) while proving retained `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`, speech guard, and session fallback present — reusing P4.4 settings-surface static test style without weakening coverage. No changeset, no new phase, not full LOCK-006 completion.
> Row delta: no 13-row matrix row marked complete; rows 1-8,10-13 keep prior T11 re-audit delta; row 9 and provider catalog / i18n residues updated to record dead-factory/i18n residue removal with generic custom-provider and protected-bridge preservation. P4.4 remains Active.

## Storage and transport inventory summary — open, not closed

The following are recorded as open P4.4/P4.5 work rather than completion claims:

- SDK/server wrappers: handwritten `KILO_CONFIG_CONTENT` forwarding deleted (P4.4-T9) — wrappers now retain only `...process.env` and `logLevel` arg; generated SDK/OpenAPI unchanged (LOCK-009); HTTP/SSE/transport narrowing remains open;
- HTTP/SSE, global health/port, provider fetch, and config-console bridge
  surfaces (config-console group retained; only the source-inventory reader and
  `/config/sources` endpoint are removed by the P4.4-T1b unit);
- `ConfigPaths`, TUI/instruction, and CLI/TUI migration
  residues (the `KilocodeConfigSources` diagnostic inventory is removed by the
  P4.4-T1b unit; the orphaned `primary-worktree` helper is deleted by the
  P4.4-T2 unit; the unreachable `config/managed.ts` helper is deleted by the
  P4.4-T3 unit; the CLI `.well-known/opencode` provider-auth discovery branch is deleted by the
  P4.4-T4 unit; the preset provider display metadata helper `kilocode/provider/metadata.ts` and its handler enrichment are deleted by the P4.4-T5 unit — only preset `noteKey`/`icon`/`priority` enrichment is removed, the optional `Provider.Info` `metadata` field and catalog/model adapters remain; the preset `@kilocode/kilo-gateway` bundled provider loader `KILO_BUNDLED_PROVIDERS` and its `...KILO_BUNDLED_PROVIDERS` spread are deleted by the P4.4-T6 unit — `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, generic SDK loaders, `models-api.json` 3.0 MB, `models.dev` catalog, `ModelCache`, and custom-provider/auth lifecycle remain per LOCK-006/009; the kilo/apertis dynamic fetch/cache is removed by the P4.4-T7 unit — `provider/model-cache.ts` invalidation-only `clear` without `fetchKiloModels`/`APERTIS_BASE_URL`/`HttpClient`, `provider/models.ts` static `Core` wrapper only without `ModelCache`/`kilo`/`apertis`, handler `ModelCache`/`failedProviders` removed — committed `models-api.json` 3.0 MB build-time snapshot and generic `Core.ModelsDev` runtime (`core/src/models-dev.ts` with disk cache, fallback fetch, 60-minute background refresh, explicit `kilo models --refresh`; `bun run refresh:models` only refreshes checked-in snapshot) plus `BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed; the preset provider login selector identity is removed by the P4.4-T8 unit — `cli/cmd/providers.ts` priority map/hints/alias and `cli/cmd/github.handler.ts` priority/recommended map removed with deterministic generic `sortBy((x) => x.name ?? x.id)` ordering retained — plugin/custom-provider, `ModelsDev.Service`, HTTP/SSE bridge, Core `ModelsDev`, `models-api.json` retained per LOCK-006/009 (webview fallback out of scope) — `ConfigPaths` itself is preserved per scope; the synthetic Kilo provider identity is removed from settings surfaces by the P4.4-T10 unit (`provider-visibility.ts`/`provider-catalog.ts`/`ProvidersTab.tsx`/`ProviderSelectDialog.tsx` synthesis/login/icon/note/recommended/gateway branches removed — `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` and speech-to-text `availability.ts` retained per scope)); the extension
  legacy-migration/importer and Roo wiring are removed by the row-13 unit;
- provider/preset/catalog/onboarding/org cleanup under LOCK-006 (bounded P4.4-T5 preset display metadata enrichment removed; bounded P4.4-T6 preset `@kilocode/kilo-gateway` bundled provider loader removed; bounded P4.4-T7 kilo/apertis dynamic fetch/cache removed — `provider/model-cache.ts` invalidation-only, `provider/models.ts` static `Core` wrapper, handler `ModelCache` removed — committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (disk cache, fallback fetch, 60-minute background refresh, explicit CLI refresh; `bun run refresh:models` only refreshes checked-in snapshot), generic adapters, `ModelCache` `clear`, and custom-provider paths remain open — only `kilo`/`apertis` dynamic fetching was removed; bounded P4.4-T8 preset provider login selector identity removed — `cli/cmd/providers.ts` priority map (kilo/anthropic/github-copilot/openai/google/openrouter/vercel), kilo/openai hints, codex->openai alias, and `cli/cmd/github.handler.ts` priority/recommended map removed — deterministic generic `sortBy((x) => x.name ?? x.id)` ordering retained; plugin/custom-provider, `ModelsDev.Service`, HTTP/SSE bridge, Core `ModelsDev`, `models-api.json` retained per LOCK-006/009; bounded P4.4-T10 synthetic Kilo settings-surface identity removed — `provider-visibility.ts`/`provider-catalog.ts`/`ProvidersTab.tsx`/`ProviderSelectDialog.tsx` synthesis/login/icon/note/recommended/gateway branches removed — `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY` retained per scope (factory `createKiloFallbackProvider` still present at T10), generic custom-provider sorting/icons/notes/configure/auth/delete preserved; bounded P4.4-T11 audit correction tightened to suppress `KILO_PROVIDER_ID` reconstruction via `provider-tab-helpers.ts` (`resolveConfiguredProvider` → `undefined` when backend omits Kilo, `buildConfiguredList` filters `undefined`; generic custom reconstruction preserved) and to delete dead `provider-visibility.ts` exports `visibleConnectedIds`/`providersWithKiloFallback`; bounded P4.4-T10 re-audit correction deleted empty `provider-visibility.ts` placeholder and dead Kilo `account` slot/profile branch (`isKiloProvider`/`account`/`server.goToProfile`); bounded P4.4-T12 dead-factory/i18n residue removal deleted `src/shared/provider-model.ts` `createKiloFallbackProvider` and synthetic literals (`Kilo Gateway`/`settings.providers.note.kilo`/`noteKey`/`custom` source) and orphaned locale keys `settings.providers.tag.gateway`/`dialog.provider.tag.recommended` (20 webview locales) + `settings.providers.note.kilo` (20 kilo-i18n locales) — retained `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`/`CUSTOM_PROVIDER_PACKAGES`, session `KILO_AUTO` fallback, speech guard, `PROVIDER_PRIORITY`/`ModelSelector` Kilo visibility, auth stitching, persisted auth/state, backend `/provider` API, generated SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider UI per LOCK-006/009/010 — still no row marked complete);
- legacy storage reader/writer and sync/warp removal rows in tracker §7;
- convergence machinery review and removed-feature startup/resource evidence;
- P5 selector/startup structural absence and worker-unavailable scenarios.

No storage or transport row is marked complete by this artifact.

## Unknowns and next actions

Unknowns intentionally left open:

1. A static source scan does not prove the complete runtime call graph for every
   diagnostic or TUI path. The next unit must trace and/or remove each retained
   adjacent consumer before physical deletion is claimed.
2. The complete generated-SDK/public-server surface affected by transport
   narrowing is not enumerated here; only the inspected server wrappers and
   their tests are cited.
3. Locale-key ownership for provider/catalog/org and migration residues needs a
   dedicated LOCK-006/P4.4 inventory; this unit does not remove or reclassify
   those keys.
4. Convergence/startup/resource deltas against P0 remain unmeasured for this
   source-removal unit.

Next actions are bounded to later P4.4 units: narrow/delete retained diagnostic
and TUI source inventories, reconcile SDK/server transport wrappers, complete
the separate LOCK-006 provider catalog inventory, review convergence and
startup/resource evidence, and then update the tracker only with verified
per-row results. P4.4 remains Active; P4.5 and P5 remain open.
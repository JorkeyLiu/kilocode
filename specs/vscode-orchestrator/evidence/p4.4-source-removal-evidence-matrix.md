# P4.4 Source-Removal Evidence Matrix — Static 13-Row Inventory

> **Unit status: COMPLETE — static evidence inventory recorded on 2026-08-25. P4.4 remains Active.**
> This artifact records effective-config inactivity after the P4.3 cutover and
> the source, diagnostic, TUI, SDK-wrapper, i18n, build/package, and transport
> residues that remain open. It does not claim physical deletion of those
> residues, transport narrowing, provider-catalog removal, storage removal, or
> P4.4 phase completion.
>
> **P4.4-T1b update (2026-08-25) — legacy source-inventory reader and
> `/config/sources` physically removed.** A bounded P4.4 unit deleted
> `packages/opencode/src/kilocode/config/sources.ts` (the 12-source diagnostic
> inventory reader), removed the `KilocodeConfigSources` type/echo from the
> overlay, and removed the `/config/sources` schema/path/endpoint/handler from
> the config-console group and the `sources` echo field from the overlay
> response. The checked-in generated SDK and OpenAPI were regenerated and no
> `config.sources`/`ConfigSourcesResponse` contract remains. The rows below
> that cited `sources.ts` line ranges were recorded against the historical
> `8167992b` baseline where that file existed; the row-level delta is recorded
> in the P4.4-T1b note below. No row is marked complete: each affected row
> keeps its other retained residues (TUI/instruction/`ConfigPaths`, sandbox
> policy, SDK wrapper forwarding, managed/primary-worktree helpers) and P4.4
> remains Active.
>
> **P4.4-T2 update (2026-08-25) — orphaned primary-worktree mirror-read helper physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/kilocode/primary-worktree.ts` (helper with only test consumers, no production call site), deleted its direct test `packages/opencode/test/kilocode/primary-worktree.test.ts`, and removed the glob entry from `packages/opencode/script/kilocode/test-profile.ts`. The `ConfigPaths` module and canonical-root handling remain untouched per scope. Rows 6 and 8 are updated for this deletion; all other rows keep their prior T1b delta. P4.4 remains Active.
>
> **P4.4-T3 update (2026-08-25) — unreachable legacy managed/MDM configuration helper physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/config/managed.ts` (unreachable helper: `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences`, `MANAGED_PLIST_DOMAIN`, no production call site after the P4.3 cutover), removed its direct tests (`ConfigManaged` import and 5 `parseManagedPlist` unit tests) from `packages/opencode/test/config/config.test.ts`, and added a focused absence regression `packages/opencode/test/kilocode/p4-4-managed-removal.test.ts` plus its glob entry in `packages/opencode/script/kilocode/test-profile.ts`. No `Config.Service` behavior, `ConfigPaths`, daemon/transport, generated SDK/OpenAPI, provider catalog, TUI/instruction, or sandbox residue is changed. Row 9 is updated for this deletion; rows 1-8 and 10-13 keep their prior T2 delta. P4.4 remains Active.
>
> **P4.4-T4 update (2026-08-25) — legacy CLI `.well-known/opencode` provider-auth discovery URL-login branch physically removed.** A bounded P4.4 unit deleted the `args.url` branch at `packages/opencode/src/cli/cmd/providers.ts:324-347` (fetch of `<url>/.well-known/opencode`, `Process.spawn` of the returned `auth.command`, and `auth.set(url, { type: "wellknown", … })` persistence), removed the `login [url]` positional and its `Process`/`text` imports, and changed the command to `login` without URL discovery. Stale well-known intercept helpers (`remoteConfigClient`, `wellKnownAuth`, `json` helper, `wellKnown` helper) were removed from `packages/opencode/test/config/config.test.ts`. Added focused absence regression `packages/opencode/test/kilocode/p4-4-wellknown-provider-auth-removal.test.ts` plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (managed < primary < wellknown). Generic provider login (api-key/plugin/custom-provider, `kilo auth login` selection, `handlePluginAuth`, `resolvePluginProviders`) and canonical effective config remain untouched; legacy HTTP/SSE/generated-SDK bridge remains per LOCK-009; no SDK regeneration is required (no public server endpoint or OpenAPI change). Row 9 is updated for this CLI discovery deletion; rows 1-8 and 10-13 keep their prior T3 delta. P4.4 remains Active.
>
> **P4.4-T5 update (2026-08-25) — preset provider display metadata enrichment physically removed.** A bounded P4.4 unit deleted `packages/opencode/src/kilocode/provider/metadata.ts` (preset-only `providerMetadata` helper — `noteKey`/`icon`/`priority` mapping for preset provider IDs) and removed only its import and `metadata: providerMetadata(item.id)` response enrichment from `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts`, retaining the provider listing aggregation (`all`/`connected`/`default`/`failed`), auth/model/config dependencies (`Config.Service`, `Provider.Service`, `ProviderAuth.Service`, `ModelCache.Service`, `ModelsDev.Service`), and endpoint wiring (`list`/`auth`/`authorize`/`callback`). Deleted the direct obsolete test `packages/opencode/test/kilocode/provider-metadata.test.ts` and added focused absence/preservation regression `packages/opencode/test/kilocode/p4-4-provider-metadata-removal.test.ts` plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (managed < primary < provider < wellknown). The optional `providerMetadata` field on `Provider.Info` (`Provider` OpenAPI schema, generated SDK `types.gen.ts`) remains optional and contract-compatible when omitted; webview `webview-ui/src/components/settings/provider-catalog.ts` fallback (`synthetic` icon, priority fallback, noteKey fallback) is preserved. Generic provider adapters, `KILO_BUNDLED_PROVIDERS`, `models-api.json` (3.0 MB checked-in snapshot), provider model/catalog loaders, `ModelCache`, custom-provider save/delete/validation, `custom-provider.ts`/`provider.ts`, auth lifecycle, and HTTP/SSE/generated-SDK bridge remain untouched per LOCK-006/009; preset catalogs, models.dev snapshot, and model caches remain open. Row 9/provider residue is updated for this preset-metadata deletion; rows 1-8 and 10-13 keep their prior T4 delta except for the provider-residue clarification. No new reconstruction-candidate entry is required: preset display metadata is an explicitly removed preset surface (LOCK-014/015 inherent permanent removal) with no displaced behavior needing evidence preservation. P4.4 remains Active.
>
> **P4.4-T6 update (2026-08-26) — preset `@kilocode/kilo-gateway` bundled provider loader physically removed.** A bounded P4.4 unit deleted the sole `KILO_BUNDLED_PROVIDERS` preset identity loader (`"@kilocode/kilo-gateway": async () => createKilo`) and its `createKilo` import plus `BundledSDK` helper from `packages/opencode/src/kilocode/provider/provider.ts`, and removed its import/spread injection (`...KILO_BUNDLED_PROVIDERS`) from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS`. Generic SDK loader map (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`, etc.), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`/`patchConfigModel`/`kiloCustomLoaders`/`patchCustomLoaderResult`, `models-api.json` (3.0 MB checked-in snapshot), `ModelsDev`/`ModelCache` catalog services (`provider/models.ts`/`provider/model-cache.ts`), custom-provider save/delete/validation/auth lifecycle (`kilocode/custom-provider.ts`, `custom-provider-save.ts`, `custom-provider-delete.ts`, `provider-auth-lifecycle.ts`), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) are retained per LOCK-006/009 — static source-presence preservation only; dynamic Kilo model resolution through fallback (models.dev/ModelCache/custom lifecycle) is intentionally not runtime-proven in this static unit (no network/package installation; see tracker row 9 open residue). No SDK regeneration is required (no public endpoint/OpenAPI change) and no performance claim is made (LOCK-PERF-6). Added focused static absence/preservation regression `packages/opencode/test/kilocode/p4-4-bundled-provider-loader-removal.test.ts` (source presence only; dynamic fallback intentionally unproven) plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (bundled < managed < primary < provider < wellknown). Row 9 is updated for this preset-loader deletion; rows 1-8 and 10-13 keep their prior T5 delta. P4.4 remains Active; no phase completion claimed.
>
> **Provenance split.** The 13-row static matrix (rows 1-12) is a
> documentation-only residue inventory recorded against the historical working
> tree at HEAD `8167992b` on 2026-08-25. Row 13's extension importer/Roo removal
> claim, and its removal assertions in
> `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts`, are post-removal
> working-tree evidence dated 2026-08-25 from a later implementation unit in
> the current working tree that is not yet committed — not part of the
> `8167992b` baseline. The two evidence bases are not conflated here.

## Purpose, scope, and non-goals

This is the first bounded P4.4 execution unit. It establishes an auditable
static matrix for every effective-config source class in runtime spec §8.1 and
maps each row to current source, test, documentation, generated-SDK, config,
i18n, build/package, and inactive-proof evidence.

This artifact also records the extension-side legacy-migration/importer and
Roo wiring removal as a distinct current working-tree implemented change, not
yet committed as of 2026-08-25. Row 13 catalogues that removal; rows 1-12 are
the documentation-only
static inventory. This artifact does not remove the remaining CLI/TUI migration
helper or any other open P4.4/P4.5 residual.

In scope:

- the exact 13-row taxonomy from
  `specs/vscode-orchestrator/runtime-and-configuration-direction.md` §8.1;
- post-P4.3 effective-config inactivity in the canonical `Config.Service` path;
- retained source inventories and non-effective consumers that must not be
  mistaken for deleted runtime/product surfaces;
- concrete open work and unknowns for later P4.4 units.

Non-goals:

- no transport narrowing, private-worker selector/startup work, or P5 work;
- no CLI/TUI/Console deletion, which remains P4.5 scope;
- no provider/preset/catalog/onboarding/org removal, which remains the separate
  LOCK-006 P4.4 residual task;
- no storage reader/writer removal;
- no edits to runtime specs, ADRs, canonical architecture docs, or the
  historical P4.3 reconciliation checklist;
- this evidence-documentation unit makes no additional source-code,
  generated-SDK, config, or product implementation changes beyond the single
  current working-tree extension importer/Roo removal, not yet committed as of
  2026-08-25. That removal is a separate implemented unit dated 2026-08-25
  (row 13) recorded here with its removal
  evidence; the other 12 rows are a documentation-only static inventory and
  make no implementation change.

## Taxonomy and active legal source set

The taxonomy is closed by runtime spec §8.1: four retained legal effective
sources and 13 deletion classes. The active effective-config source set after
P4.3 is only:

1. the canonical global config root and its canonical files/assets;
2. `<workspaceRoot>/.kilo/` and its project-versioned files/assets;
3. VS Code SecretStorage credentials, referenced by opaque identifiers; and
4. versioned runtime defaults and safety invariants.

UI `globalState`/`workspaceState` is derived UI state, not effective-config
authority. Session/storage state and bounded approval records are operational
state, not effective-config sources. `KILO_CONFIG_DIR` instruction profile
fallback is separately retained instruction behavior, not `Config.Service`
authority.

## Static method and status vocabulary

The inventory was performed against the working tree at HEAD `8167992b` on
2026-08-25. The method was:

> **Provenance note.** The `8167992b` HEAD is the historical baseline for the
> documentation-only row 1-12 inventory. Row 13 and the importer-removal
> assertions it cites are post-removal working-tree evidence dated 2026-08-25 —
> they reflect the tree after the extension legacy-migration/importer and Roo
> wiring were deleted, and are not part of the `8167992b` baseline. Do not
> reclassify row 13 as a `8167992b`-era source claim.

1. verify the 13 row names and dispositions against runtime spec §8.1 and the
   placeholder rows in tracker §7;
2. inspect the canonical loader and P4.3 absence anchors in
   `packages/opencode/src/config/config.ts` and
   `packages/opencode/test/kilocode/p4-3-cutover.test.ts`;
3. inspect retained diagnostic/config-console, TUI/instruction, sandbox,
   migration, SDK-wrapper, provider/catalog, i18n, and build/package paths;
4. record only path-level static evidence. No dynamic call-graph or transport
   deletion claim is inferred from a string absence.

The matrix uses these statuses:

- **effective inactive** — the P4.3 canonical-loader evidence proves that the
  source no longer affects effective `Config.Service` output; no relevant
  non-effective residue was found in the inspected paths;
- **residual retained** — effective use is inactive, but source inventory,
  diagnostic, TUI/instruction, SDK-wrapper, safety, migration, catalog, or
  transport machinery remains and is open P4.4 work;
- **unknown** — the inspected evidence does not prove either classification.

`residual retained` does not mean that the residue is an effective-config
reader. It means that physical source removal or adjacent-surface narrowing is
not complete and must not be reported as complete.

## 13-row source-removal evidence matrix

| # | Source class (runtime §8.1) | Status | Source | Tests | Docs | Generated SDK | Config | i18n | Build/package | Inactive proof |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `KILO_CONFIG` env override | residual retained; evidence date: 2026-08-25 | residual: `sources.ts` diagnostic listing removed (P4.4-T1b); `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts:33-80,148-158` TUI migration input | test: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:13-78` | doc: runtime §8.1; P4.3 cutover evidence §6 | - | residual: TUI paths only; source inventory removed (P4.4-T1b); canonical `config/config.ts` has no `Flag.KILO_CONFIG` reader | - | residual: packaged TUI migration helper remains; package impact not yet inventoried | effective inactive: P4.3 absence anchors; no `Config.Service` read remains (`p4-3-cutover.test.ts:40-45`) |
| 2 | `KILO_CONFIG_DIR` env override | residual retained; evidence date: 2026-08-25 | residual: `sources.ts:135-159,184-194` removed (P4.4-T1b); `packages/opencode/src/config/paths.ts:23-40`; `packages/opencode/src/session/instruction.ts:60-91` | test: `p4-3-cutover.test.ts:97-102,152-168`; `packages/opencode/test/kilocode/instruction.test.ts:85-132` | doc: runtime §8.1; P4.3 cutover evidence §10 | - | residual: TUI/config-directory and instruction profile behavior; not `Config.Service` authority | - | residual: TUI/CLI config path consumers remain | effective inactive: canonical loader ignores the flag; retained instruction/profile behavior is explicitly separate |
| 3 | `KILO_CONFIG_CONTENT` env override | residual retained; evidence date: 2026-08-25 | residual: `sources.ts:171-183` removed (P4.4-T1b); nested-process env forwarding residue remains in SDK wrapper | test: `p4-3-cutover.test.ts:39-47`; `packages/sdk/js/test/server.test.ts:1-109` | doc: runtime §8.1; P4.3 cutover evidence §6 | SDK/server-wrapper evidence (handwritten): `packages/sdk/js/src/v2/server.ts:5-35,68-74,155-163`; `packages/sdk/js/src/server.ts` equivalent wrapper. Actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts`; row-specific generated-surface inventory open/not yet inventoried | residual: nested-process env forwarding remains outside canonical loader; source inventory removed (P4.4-T1b) | - | residual: SDK server package still emits the env | effective inactive: `config/config.ts` contains no content injection; forwarding residue cannot affect canonical effective config after P4.3 |
| 4 | `KILO_PERMISSION` env override | residual retained; evidence date: 2026-08-25 | residual: `sources.ts:275-291` removed (P4.4-T1b); sandbox environment deny-list at `packages/opencode/src/kilocode/sandbox/policy.ts:168-175` | test: `p4-3-cutover.test.ts:39-51`; `packages/opencode/test/permission/r18-service.test.ts` | doc: runtime §8.1; P4.3 cutover evidence §6 | - | residual: sandbox safety handling remains; no `Flag.KILO_PERMISSION` effective overlay | - | - | effective inactive: P4.3 proves `Flag.KILO_PERMISSION` and `result.tools` absent from `Config.Service` |
| 5 | Legacy `opencode.*` keys, `.opencode` / `.kilocode` locations | residual retained; evidence date: 2026-08-25 | residual: `sources.ts:59-60,121-132` removed (P4.4-T1b); TUI legacy-directory handling in `packages/opencode/src/cli/cmd/tui/config/tui.ts:239-248` | test: `p4-3-cutover.test.ts:54-78`; `packages/opencode/test/kilocode/config/config.test.ts:430-502` | doc: runtime §8.1; P4.3 cutover evidence §6 | - | residual: TUI compatibility paths; source inventory removed (P4.4-T1b); canonical loader is `.kilo/kilo.jsonc` only | - | residual: TUI package still contains legacy-directory handling | effective inactive: canonical loader absence anchors cover `.opencode`, `.kilocode`, and legacy filenames |
| 6 | Global project-asset sources | residual retained; evidence date: 2026-08-25 (P4.4-T2) | residual: `sources.ts:135-159` removed (P4.4-T1b); `packages/opencode/src/kilocode/primary-worktree.ts:7-37` helper physically deleted (P4.4-T2) | test: `p4-3-cutover.test.ts:54-60`; `packages/opencode/test/kilocode/config/config.test.ts:563-590`; `p4-4-primary-worktree-removal.test.ts`; `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` | doc: runtime §8.1/R5; P4.3 cutover evidence §7 | - | residual: helper deleted (P4.4-T2); source inventory removed (P4.4-T1b); canonical project assets are restricted to `<workspaceRoot>/.kilo/` | - | - | effective inactive: `primaryPaths(` and legacy discovery are absent from `Config.Service`; canonical-root tests reject mirror/legacy values; helper file and import surface absent |
| 7 | Ancestor directory walks | residual retained; evidence date: 2026-08-25 | residual: `packages/opencode/src/config/paths.ts:10-40`; `sources.ts:121-159` removed (P4.4-T1b); TUI `ConfigPaths.files/directories` consumers | test: `p4-3-cutover.test.ts:54-60,97-102`; `packages/opencode/test/kilocode/config/config.test.ts:563-590` | doc: runtime §8.1/R5; P4.3 cutover evidence §7 | - | residual: paths service and TUI discovery still walk; canonical loader does not | - | residual: CLI/TUI path machinery remains packaged | effective inactive: `ConfigPaths.files`/`directories` absence anchors apply to `config/config.ts`, while retained consumers are outside effective loading |
| 8 | Primary-worktree mirror reads | removed (helper deleted); evidence date: 2026-08-25 (P4.4-T2) | deleted: `packages/opencode/src/kilocode/primary-worktree.ts:7-64` helper physically deleted (P4.4-T2); `test/kilocode/primary-worktree.test.ts` deleted; `script/kilocode/test-profile.ts` glob entry removed; no canonical-loader call site | test: `p4-3-cutover.test.ts:54-60`; `p4-4-primary-worktree-removal.test.ts`; `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` | doc: runtime §8.1/R5; P4.3 cutover evidence §7 | - | helper deleted; canonical `canonicalRoot` path is the effective boundary; `ConfigPaths` preserved | - | - | effective inactive: `primaryPaths(` absent from `config/config.ts`; helper file absent and no production import remains; canonical tests prove primary/mirror values do not load |
| 9 | Cloud/org/managed config sources | residual retained; evidence date: 2026-08-26 (P4.4-T6) | deleted: `sources.ts:82-95,210-271` removed (P4.4-T1b); `packages/opencode/src/config/managed.ts` helper physically deleted (P4.4-T3) — `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences` absent; CLI `.well-known/opencode` provider-auth discovery URL-login branch deleted at `packages/opencode/src/cli/cmd/providers.ts:324-347` (P4.4-T4) — `args.url` fetch, `Process.spawn`, `wellknown` persistence removed; `login [url]` → `login`; preset provider display metadata helper `packages/opencode/src/kilocode/provider/metadata.ts` physically deleted (P4.4-T5) — `providerMetadata` preset `noteKey`/`icon`/`priority` mapping absent; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` import and `metadata: providerMetadata(item.id)` enrichment removed (P4.4-T5) — listing now `all: Object.values(validProviders).map(Provider.toPublicInfo)` without preset enrichment; preset `@kilocode/kilo-gateway` bundled provider loader `KILO_BUNDLED_PROVIDERS` (`"@kilocode/kilo-gateway": async () => createKilo`) deleted from `packages/opencode/src/kilocode/provider/provider.ts` and its `...KILO_BUNDLED_PROVIDERS` spread removed from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS` (P4.4-T6) — generic SDK loaders (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`, etc.), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, `models-api.json`, `ModelsDev`/`ModelCache`, custom-provider/auth lifecycle, and `/provider` bridge retained per LOCK-006/009 — static source-presence preservation only; dynamic Kilo model resolution through fallback intentionally not runtime-proven in this static unit; config-console no longer reads the inventory (handler call sites removed P4.4-T1b) | test: `p4-3-cutover.test.ts:35-38` (canonical-loader absence); `p4-4-managed-removal.test.ts` (helper file/import-surface absence, direct-test removal, canonical-loader preservation); `p4-4-wellknown-provider-auth-removal.test.ts` (CLI well-known discovery branch absence, stale config.test helpers removed, retained generic provider-login guarded); `p4-4-provider-metadata-removal.test.ts` (preset helper file/import-surface absence, handler enrichment absence, `all`/`connected`/`default`/`failed` aggregation and auth/model/config dependency preservation, `Provider.Info` optional `metadata` contract unchanged, generated OpenAPI/SDK unchanged, generic adapters/custom-provider/bridge preservation, sorted test-profile `managed < primary < provider < wellknown`); `p4-4-bundled-provider-loader-removal.test.ts` (static source presence only — preset loader `KILO_BUNDLED_PROVIDERS`/`createKilo`/`"@kilocode/kilo-gateway"` loader absence, `BUNDLED_PROVIDERS` generic adapters/`models-api.json`/`ModelsDev`/`ModelCache`/custom-provider/`/provider` bridge static preservation, dynamic fallback intentionally unproven, sorted test-profile `bundled < managed < primary < provider < wellknown`) | doc: runtime §8.1; P4.3 cutover evidence §6 | - | helper deleted (P4.4-T3); source inventory removed (P4.4-T1b); CLI well-known discovery deleted (P4.4-T4); preset metadata enrichment deleted (P4.4-T5) — `Provider.Info` optional `metadata` remains contract-compatible when omitted; config-console source reporting removed; canonical loader has no managed/org read | residual: provider/org/catalog labels remain in `packages/kilo-i18n/src/*`; not proof of an effective reader; `Auth.WellKnown` storage type remains in `packages/opencode/src/auth/index.ts` for persisted credential compatibility; `KILO_BUNDLED_PROVIDERS`, `models-api.json`, `models.dev` snapshot, `ModelCache`, and `providerMetadata` schema optionality remain open P4.4 residues (LOCK-006) | deleted: CLI `.well-known/opencode` fetch at `packages/opencode/src/cli/cmd/providers.ts:327` removed (P4.4-T4); preset display metadata enrichment at `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts:85` removed (P4.4-T5) — `Provider.toPublicInfo` without preset enrichment; no public server endpoint or generated SDK/OpenAPI regeneration required (LOCK-009 bridge preserved; optional `metadata` field unchanged) | effective inactive: managed/cloud anchors are absent from `Config.Service`; CLI well-known provider discovery is absent from `cli/cmd/providers.ts`; preset provider display metadata enrichment is absent from the provider handler (`providerMetadata` import and call absent; listing is `Provider.toPublicInfo` only); `effective` is `config.get()`; managed helper and preset metadata helper files absent and no production import remains; provider endpoint still serves `all`/`connected`/`default`/`failed` via generic/custom-provider paths; canonical tests prove managed values do not load |
| 10 | Top-level `mode`/`tools` conversions | residual retained; evidence date: 2026-08-25 | residual: no current `Config.Service` conversion found; `sources.ts` diagnostic taxonomy removed (P4.4-T1b); nested-process merge preserves `mode` in SDK wrapper | test: exact P4.3 absence assertions in `packages/opencode/test/kilocode/p4-3-cutover.test.ts:48-51` | doc: runtime §8.1; P4.3 cutover evidence §7 | SDK/server-wrapper evidence (handwritten): `packages/sdk/js/src/v2/server.ts:13-18` and `src/server.ts` preserve `mode`/plugin/config fields for child env construction. Actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts`; row-specific generated-surface inventory open/not yet inventoried | residual: no effective top-level conversion in canonical config; typed assets are canonical | - | residual: SDK wrapper package path remains | effective inactive: `result.mode`, `result.tools`, and `loadMode` conversion anchors are absent from canonical config/agent paths |
| 11 | Arbitrary CLI/env override layers | residual retained; evidence date: 2026-08-25 | residual: sandbox safety environment policy at `packages/opencode/src/kilocode/sandbox/policy.ts:168-181`; SDK child-process env construction in `packages/sdk/js/src/v2/server.ts:68-74,155-163` | test: `p4-3-cutover.test.ts:39-51`; `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts:168-190` | doc: runtime §8.1; P4.3 cutover evidence §7 | SDK/server-wrapper evidence (handwritten): wrapper forwards process configuration; actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts`; row-specific generated-surface inventory open/not yet inventoried | residual: safety policy remains, but it is not an effective-config overlay | - | residual: SDK/server bridge machinery remains open for transport narrowing | effective inactive: no generic override layer is applied by `Config.Service`; sandbox denial and diagnostic forwarding are not effective source reads |
| 12 | Legacy global config filenames/readers | residual retained; evidence date: 2026-08-25 | residual: `sources.ts:59-60,99-105` filename inventory removed (P4.4-T1b); TUI migration helper remains | test: `p4-3-cutover.test.ts:20-27,64-78`; `packages/opencode/test/kilocode/config/config.test.ts:610-667` | doc: runtime §8.1; P4.3 cutover evidence §6 | - | residual: TUI migration filename handling remains; source filename inventory removed (P4.4-T1b); canonical stamp/loader is `kilo.jsonc` only | - | residual: TUI/diagnostic paths remain packaged | effective inactive: all legacy filename/readers are absent from `config/config.ts`; `KilocodeGlobalConfigStamp` is canonical-only |
| 13 | Legacy migration readers/import tooling | residual retained; evidence date: 2026-08-25 (post-removal working-tree evidence, not the `8167992b` baseline) | extension legacy-migration and Roo import source removed; TUI migration helper remains at `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts:33-80,148-166` | test: `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts:420-444`; `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts:107-114` | doc: runtime §8.1; `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md:264-266`; this matrix | - | residual: intentional migration/Roo-specific locale keys remain removed; generic settings import/export remains | residual: TUI migration surface remains P4.5-adjacent | extension importer/Roo wiring is removed and cannot read legacy data or write canonical config; CLI/TUI migration helper remains open residual work. Row remains open/residual; no P4.4 completion claim |

## Residual machinery and open P4.4 work

### `KilocodeConfigSources` and config-console

The legacy `KilocodeConfigSources` diagnostic inventory reader
(`packages/opencode/src/kilocode/config/sources.ts`, the 12-source listing of
env, legacy filename, project, cloud, managed, and runtime sources) was
**physically removed on 2026-08-25 (P4.4-T1b)**. The `KilocodeConfigSources`
type/echo is gone from `packages/opencode/src/kilocode/config/overlay.ts`
(`Result`/`Input`/`resolve` no longer carry `sources`), and the config-console
handler
(`packages/opencode/src/kilocode/server/httpapi/handlers/config-console.ts`)
no longer acquires auth/account state for an inventory and no longer exposes a
`sources` handler. The `/config/sources` schema/path/endpoint/handler and the
`ConfigSourcesResponse` schema were removed from the config-console group, and
the `sources` echo field was removed from `ConfigOverlayResponse`. The
config-console handler's effective value remains `config.get()`; the retained
overlay resolution is unchanged. No replacement shim or import/migration
behavior was added (LOCK-014/015); the behavior is recorded as a dropped
reconstruction candidate. This unit does not remove the remaining config-console
bridge endpoints (`/config/overlay`, `/config/effective`, `/config/transaction`,
rules, model-state, TUI config/keybinds).

### Canonical merge versus the deleted multi-source merge

P4.3 deleted the legacy reader fan-out, migration readers, and `loadMode` path
from retained effective-config loading. The canonical loader and canonical
overlay now use the global/project `kilo.jsonc` roots and typed assets. This
matrix does not claim that every adjacent merge-like helper is physically gone:
the diagnostic overlay, TUI readers, and SDK `mergeConfig` child-process helper
remain separately inventoried. No 12-source effective merge is treated as
active based solely on those residues.

### Convergence

`packages/opencode/src/kilocode/server/config-convergence.ts:1-43` remains the
active canonical save/convergence coordinator. This unit records its presence
as residual P4.4 machinery only; it does not decide or implement any narrowing
of convergence, admission fences, disposal, or boot behavior.

### Provider catalog and LOCK-006

Preset/provider catalog, onboarding, and organization residues remain a separate
P4.4 task under LOCK-006. Existing provider catalog and organization/i18n
surfaces are not counted as removed by this matrix except for the bounded
P4.4-T5 preset display metadata enrichment removal (`kilocode/provider/metadata.ts`
and its handler `metadata: providerMetadata(item.id)` enrichment deleted;
optional `Provider.Info` `metadata` field and `Provider` OpenAPI/SDK schema remain
optional/contract-compatible, webview `provider-catalog.ts` fallback preserved)
and the bounded P4.4-T6 preset `@kilocode/kilo-gateway` bundled provider loader
removal (`KILO_BUNDLED_PROVIDERS` ` "@kilocode/kilo-gateway": async () => createKilo`
and its `...KILO_BUNDLED_PROVIDERS` spread from `provider/provider.ts`
`BUNDLED_PROVIDERS` deleted; `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`,
generic adapters, `models-api.json` 3.0 MB checked-in snapshot, `models.dev`
catalog, `ModelCache`, custom-provider save/delete/validation, and auth lifecycle
remain open per LOCK-006/009). The matrix row for cloud/org/
managed sources records only effective-config inactivity and the retained
diagnostic/provider residue, now updated for the T6 preset-loader deletion.

### Backend fetch and public transport chain

The SDK/server wrappers still launch `kilo` and forward
`KILO_CONFIG_CONTENT` (`packages/sdk/js/src/v2/server.ts` and `src/server.ts`).
The CLI/server still exposes HTTP/SSE, health, and provider fetch surfaces. This
unit does not narrow or delete them. P4.4 transport narrowing remains open, and
P5 structural absence of the selector startup fetch chain has not started.

> **P4.4-T1a note (2026-08-25) — extension-side periodic `/global/health` poll removed.**
> `KiloConnectionService` no longer polls `/global/health`: `HEALTH_POLL_INTERVAL_MS`,
> `startHealthPoll`/`stopHealthPoll`/`checkHealth`, the `healthPollTimer` field,
> and all call sites are gone from
> `packages/kilo-vscode/src/services/cli-backend/connection-service.ts` (source).
> Evidence: test: `packages/kilo-vscode/tests/unit/connection-service-health-poll-absence.test.ts`
> (6 tests / 22 expect calls — poll-surface absence plus retained-transport presence
> guard: `SdkSSEAdapter` heartbeat/reconnect remains the sole liveness path,
> LOCK-009); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`
> (health-poll row removed from the connection liveness/disposal table). Only the
> extension-side periodic poll is removed: the server-side `/global/health`
> route, the HTTP/SSE/generated-SDK bridge, provider/catalog (LOCK-006),
> convergence, storage/sync, CLI/TUI, and this back-end-gated fetch chain remain
> open; P4.4 transport narrowing, P4.5, and P5 remain open. No matrix row is
> marked complete and no numeric performance claim is made (LOCK-PERF-6).
> Working-tree evidence, not yet committed as of 2026-08-25.

> **P4.4-T1b note (2026-08-25) — legacy source-inventory reader and `/config/sources` removed.**
> This bounded unit physically removed the diagnosis-only source inventory from
> effective-config surfaces (LOCK-010/014/015).
> Source: `packages/opencode/src/kilocode/config/sources.ts` (the 12-source
> `KilocodeConfigSources` inventory reader) deleted; `KilocodeConfigSources`
> import/type/echo removed from
> `packages/opencode/src/kilocode/config/overlay.ts` (`Result`/`Input`/`resolve`
> no longer carry `sources`); `/config/sources` schema/path/endpoint/handler and
> `ConfigSourcesResponse` removed from
> `packages/opencode/src/kilocode/server/httpapi/groups/config-console.ts` and
> `handlers/config-console.ts`, and the `sources` echo field removed from
> `ConfigOverlayResponse`. The overlay endpoint still resolves effective/global/
> project via `config.get()`/`config.getGlobal()`/`KilocodeConfigOverlay.resolve`
> with no behavioral change to canonical resolution.
> Tests: `packages/opencode/test/kilocode/p4-4-source-inventory-removal.test.ts`
> (static absence of the reader, the `/config/sources` contract in production
> source, generated v2 SDK, and `packages/sdk/openapi.json`, plus retained
> overlay/effective/transaction/rules/model-state/TUI bridge presence);
> `config-sources.test.ts` deleted (it exercised the removed endpoint);
> `httpapi-exercise-scenarios.ts` and `httpapi-public.test.ts` drop the removed
> route entries; `p4-3-overlay-mode.test.ts` and `config-overlay.test.ts`
> updated for the `sources`-less overlay `Input`/`Result`.
> Docs: `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md`
> (source-inventory removal, not a surviving diagnostic); skill
> `packages/opencode/src/kilocode/skills/kilo-config.md`; P4.3 cutover evidence
> P4.4 row.
> Generated SDK: `packages/sdk/js/src/v2/gen/sdk.gen.ts` and `types.gen.ts` and
> `packages/sdk/openapi.json` regenerated via `bun ./script/generate.ts` from the
> repo root (twice, idempotent); no `config.sources`/`ConfigSourcesResponse`
> remains.
> Row delta: the `sources.ts` diagnostic-inventory residual cited in rows 1-7,
> 9, 10, and 12 is physically removed; each such row remains `residual retained`
> for its other retained residues (TUI migration/`ConfigPaths`, instruction
> profile, sandbox policy, SDK wrapper forwarding, managed/primary-worktree
> helpers) and no row is marked complete. Rows 8 and 11 carried no `sources.ts`
> residual and are unchanged. The retained config-console bridge endpoints
> (`/config/overlay`, `/config/effective`, `/config/transaction`, rules,
> model-state, TUI config/keybinds) and the overlay/transaction handlers are
> preserved (LOCK-009). Evidence-class reproduction of this unit's own row-level
> partial/closed delta is recorded per row above and in the tracker §7 rows; the
> behavior is recorded as a dropped reconstruction candidate (RC-P4.4-001,
> tracker section 7 registry) per LOCK-015. No risk-area code was removed beyond
> the inventory and its endpoint; P4.4 remains Active; no performance claim is
> made (LOCK-PERF-6). Working-tree evidence, not yet committed as of 2026-08-25.

> **P4.4-T2 note (2026-08-25) — orphaned primary-worktree mirror-read helper physically removed.**
> This bounded unit physically removed the unused primary-worktree discovery helper
> that had only test consumers after the P4.3 canonical cutover (managed worktree
> infrastructure permanently removed; canonical project assets live only under the
> first workspace root `.kilo` boundary; no mirror-read fallback).
> Source: `packages/opencode/src/kilocode/primary-worktree.ts` (64 lines, `primaryPaths`/`primaryWorktree`) deleted; direct test `packages/opencode/test/kilocode/primary-worktree.test.ts` deleted; glob entry `primary-worktree` removed from `packages/opencode/script/kilocode/test-profile.ts`; no production consumer existed (`config/config.ts`, `kilocode/config/config.ts`, `skill/index.ts`, `config/paths.ts` contain no `primary-worktree`/`primaryPaths`/`primaryWorktree` import). Canonical-root handling (`canonicalRoot`) and `ConfigPaths` (`packages/opencode/src/config/paths.ts` unchanged per scope) plus all legacy-reader cutover proofs, retained config-console endpoints, private worker bridge, and existing HTTP/SSE bridge remain untouched.
> Tests: `packages/opencode/test/kilocode/p4-4-primary-worktree-removal.test.ts` (static file-deletion, helper-import absence across CLI source, test-profile glob absence, canonical-root/`ConfigPaths` preservation); `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` updated to assert helper file/test absence and zero `primaryWorktree`/`primaryPaths`/`primary-worktree` surface in CLI source (P4.4 exception closed); retained `p4-3-cutover.test.ts` cutover anchors continue to prove canonical-only loading.
> Row delta: row 6 source/config columns updated to record `primary-worktree.ts:7-37` deletion; row 8 status moved from `residual retained` to `removed (helper deleted)` with source/test/docs evidence for physical deletion and import-surface absence; rows 1-5,7,9-13 unchanged except for row 6 config clarification. No new reconstruction-candidate entry is required: primary-worktree mirror reads are a permanent removal class (runtime §8.1) recorded in the tracker §7 removal inventory, not a displaced behavior needing evidence preservation; inherent permanent removals are not candidate rows. `ConfigPaths`, TUI/instruction, SDK wrapper forwarding, sandbox policy, and transport narrowing remain open P4.4 residues; no phase completion or performance claim is made. Working-tree evidence, not yet committed as of 2026-08-25.
>
> **P4.4-T3 note (2026-08-25) — unreachable legacy managed/MDM configuration helper physically removed.**
> This bounded unit physically removed the dead managed configuration reader that cannot affect effective configuration after the P4.3 cutover (canonical authored config is only one global root and `<workspaceRoot>/.kilo/`; no generic managed/legacy overlay is a target source; permanent removal is direct with no shim).
> Source: `packages/opencode/src/config/managed.ts` (77 lines, `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences`, `MANAGED_PLIST_DOMAIN`) deleted; direct tests `ConfigManaged` import and 5 `parseManagedPlist` unit tests removed from `packages/opencode/test/config/config.test.ts` (helper-only tests; `p4-3-cutover.test.ts` preservation and `Config.Service` canonical loader behavior unchanged); supporting `managedConfigDir`/`writeManagedSettingsEffect` harness removed. No production import remains — `config/config.ts`, `kilocode/config/config.ts`, `kilocode/config/overlay.ts`, `config/paths.ts` contain no `ConfigManaged`/`managedConfigDir`/`readManagedPreferences`/`parseManagedPlist`/`MANAGED_PLIST`/`ai.opencode.managed` reference; `ConfigPaths` and canonical-root handling preserved per scope.
> Tests: `packages/opencode/test/kilocode/p4-4-managed-removal.test.ts` (static file-deletion, helper-import absence across CLI source including `KILO_TEST_MANAGED_CONFIG_DIR`, test-profile glob presence, canonical-loader managed-absence preservation, direct-test removal, `ConfigPaths`/`canonicalRoot` preservation); retained `p4-3-cutover.test.ts:35-38` cutover anchors continue to prove `managedConfigDir()`/`readManagedPreferences` absent from canonical loader.
> Row delta: row 9 source/test/config/inactive-proof columns updated to record `config/managed.ts` deletion and new absence regression; rows 1-8 and 10-13 unchanged except for row 9. No new reconstruction-candidate entry is required: cloud/org/managed config sources are a permanent removal class (runtime §8.1) recorded in the §7 removal inventory; the unreachable helper has no target value and is not a displaced behavior needing evidence preservation; inherent permanent removals are not candidate rows. Provider/org/catalog labels, `sources.ts` already deleted (P4.4-T1b), `primary-worktree` already deleted (P4.4-T2), `ConfigPaths`, TUI/instruction, SDK wrapper forwarding, sandbox policy, and transport narrowing remain open P4.4 residues; no phase completion or performance claim is made. Working-tree evidence, not yet committed as of 2026-08-25.
>
> **P4.4-T4 note (2026-08-25) — legacy CLI `.well-known/opencode` provider-auth discovery URL-login branch physically removed.**
> This bounded unit physically removed the `args.url` branch at `packages/opencode/src/cli/cmd/providers.ts:324-347` (fetch of `<url>/.well-known/opencode`, `Process.spawn` of the returned `auth.command`, and `auth.set(url, { type: "wellknown", … })` persistence), removed the `login [url]` positional and its `Process`/`text` imports, and changed the command to `login` without URL discovery. Stale well-known intercept helpers (`remoteConfigClient`, `wellKnownAuth`, `json`/`wellKnown` helpers) were removed from `packages/opencode/test/config/config.test.ts`. No production import remains for the well-known fetch path; canonical loader unchanged.
> Tests: `packages/opencode/test/kilocode/p4-4-wellknown-provider-auth-removal.test.ts` (CLI well-known branch absence, stale helpers removed, retained generic provider-login guarded: `ProvidersLoginCommand`, `handlePluginAuth`, `resolvePluginProviders`, api-key/plugin/custom-provider paths preserved; P4.3 cutover anchors preserved); `packages/opencode/script/kilocode/test-profile.ts` sorted glob `managed < primary < wellknown` updated. Generic provider login and `Config.Service`/`Plugin`/`ModelsDev` remain; HTTP/SSE/generated-SDK bridge preserved (LOCK-009); no SDK regeneration required (no public endpoint change).
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record CLI well-known discovery deletion; rows 1-8 and 10-13 keep prior T3 delta. No reconstruction-candidate entry required (cloud/org/managed permanent removal class, LOCK-014/015). All other provider residues (`KILO_BUNDLED_PROVIDERS`, `models-api.json`, `ModelCache`, `metadata.ts` still present at that date) remain open. P4.4 remains Active.
>
> **P4.4-T5 note (2026-08-25) — preset provider display metadata enrichment physically removed.**
> This bounded unit physically removed the preset-only display metadata enrichment that added `noteKey`/`icon`/`priority` to provider-list responses (LOCK-006 preset surface, direct removal per LOCK-014/015 — no reconstruction candidate needed for an explicitly removed preset surface).
> Source: `packages/opencode/src/kilocode/provider/metadata.ts` (41 lines, `ProviderMetadata` type, `notes`/`order`/`priority`/`icons`/`key()`/`providerMetadata()`) deleted; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` import `providerMetadata` and `metadata: providerMetadata(item.id)` enrichment removed — listing now `all: Object.values(validProviders).map((item) => Provider.toPublicInfo(item))` without preset enrichment; `all`/`connected`/`default`/`failed` aggregation, `Config.Service`/`Provider.Service`/`ProviderAuth.Service`/`ModelCache.Service`/`ModelsDev.Service` dependencies, `filterPromptTrainingModels`/`overlayAnacondaDesktop`/`pickBy` valid-providers guard, and `/provider` endpoint wiring (`list`/`auth`/`authorize`/`callback`) retained. No production source references `kilocode/provider/metadata` after removal. Direct obsolete test `packages/opencode/test/kilocode/provider-metadata.test.ts` deleted.
> Tests: `packages/opencode/test/kilocode/p4-4-provider-metadata-removal.test.ts` (preset helper file/test deletion, `kilocode/provider/metadata` and `providerMetadata(item.id)` import-surface absence across CLI source, handler enrichment absence with `all`/`connected`/`default`/`failed` and dependency/endpoint preservation, `Provider.Info` optional `metadata` contract unchanged — `ProviderMetadata` struct with `noteKey`/`icon`/`priority` remains `optionalOmitUndefined`, OpenAPI `Provider` `metadata` not in `required` and contract-compatible when omitted, generated SDK `metadata?:` preserved, webview `provider-catalog.ts` fallback (`synthetic`/`FALLBACK_PROVIDER_IDS`/`validIcon`) preserved, generic adapters `KILO_BUNDLED_PROVIDERS`/`models-api.json`/`models.ts`/`ModelCache`/custom-provider save/delete/auth lifecycle preserved, sorted test-profile `managed < primary < provider < wellknown`); retained provider handler/provider tests and config/cutover suites plus `p4-3-cutover.test.ts` anchors.
> Row delta: row 9 source/test/config/i18n/build/inactive-proof updated to record `metadata.ts` and handler enrichment deletion with contract-preservation evidence; rows 1-8 and 10-13 keep prior T4 delta except for provider-residue clarification; provider catalog section updated. No new reconstruction-candidate entry is required per LOCK-014/015: preset display metadata is an explicitly removed preset display surface; preset catalogs (`KILO_BUNDLED_PROVIDERS`, `models-api.json` 3.0 MB), `models.dev` catalog, `ModelCache`, generic adapters, custom-provider paths, HTTP/SSE/generated-SDK bridge, and provider `metadata` optional schema all remain open P4.4 residues. P4.4 remains Active; no phase completion or performance claim.
>
> **P4.4-T6 note (2026-08-26) — preset `@kilocode/kilo-gateway` bundled provider loader physically removed.**
> This bounded unit physically removed the sole preset identity loader `KILO_BUNDLED_PROVIDERS` (`"@kilocode/kilo-gateway": async () => createKilo`) and its `createKilo` import plus `BundledSDK` helper from `packages/opencode/src/kilocode/provider/provider.ts`, and removed its import/spread (`...KILO_BUNDLED_PROVIDERS`) from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS` (LOCK-006 preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change).
> Source: `kilocode/provider/provider.ts` now imports `{ type KiloProvider, AI_SDK_PROVIDERS, PROMPTS }` without `createKilo`; `provider/provider.ts` `BUNDLED_PROVIDERS` retains generic SDK loaders (`@ai-sdk/openai`, `@ai-sdk/anthropic`, `@ai-sdk/google`, `@ai-sdk/azure`, `@ai-sdk/openai-compatible`, `@openrouter/ai-sdk-provider`, etc.) without `KILO_BUNDLED_PROVIDERS`.
> Tests: `packages/opencode/test/kilocode/p4-4-bundled-provider-loader-removal.test.ts` (preset loader `KILO_BUNDLED_PROVIDERS`/`createKilo`/`"@kilocode/kilo-gateway"` absence, `KILO_BUNDLED_PROVIDERS` import/spread absence, generic adapters/`models-api.json`/`ModelsDev`/`ModelCache`/custom-provider/`/provider` bridge preservation, sorted test-profile `bundled < managed < primary < provider < wellknown`); retained `p4-3-cutover.test.ts` and provider handler tests.
> Row delta: row 9 source/test/docs/build/inactive-proof updated to record `KILO_BUNDLED_PROVIDERS` and loader spread deletion with generic-adapter/catalog/bridge preservation; rows 1-8 and 10-13 keep prior T5 delta. No new reconstruction-candidate entry is required per LOCK-014/015: preset bundled loader is an explicitly removed preset identity surface; `models-api.json` 3.0 MB, `models.dev` catalog, `ModelCache`, generic adapters, custom-provider paths, HTTP/SSE/generated-SDK bridge, and provider `metadata` optional schema all remain open P4.4 residues. P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).

## Storage and transport inventory summary — open, not closed

The following are recorded as open P4.4/P4.5 work rather than completion claims:

- SDK/server wrappers and `KILO_CONFIG_CONTENT` forwarding;
- HTTP/SSE, global health/port, provider fetch, and config-console bridge
  surfaces (config-console group retained; only the source-inventory reader and
  `/config/sources` endpoint are removed by the P4.4-T1b unit);
- `ConfigPaths`, TUI/instruction, and CLI/TUI migration
  residues (the `KilocodeConfigSources` diagnostic inventory is removed by the
  P4.4-T1b unit; the orphaned `primary-worktree` helper is deleted by the
  P4.4-T2 unit; the unreachable `config/managed.ts` helper is deleted by the
  P4.4-T3 unit; the CLI `.well-known/opencode` provider-auth discovery branch is deleted by the
  P4.4-T4 unit; the preset provider display metadata helper `kilocode/provider/metadata.ts` and its handler enrichment are deleted by the P4.4-T5 unit — only preset `noteKey`/`icon`/`priority` enrichment is removed, the optional `Provider.Info` `metadata` field and catalog/model adapters remain; the preset `@kilocode/kilo-gateway` bundled provider loader `KILO_BUNDLED_PROVIDERS` and its `...KILO_BUNDLED_PROVIDERS` spread are deleted by the P4.4-T6 unit — `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, generic SDK loaders, `models-api.json` 3.0 MB, `models.dev` catalog, `ModelCache`, and custom-provider/auth lifecycle remain per LOCK-006/009 — `ConfigPaths` itself is preserved per scope); the extension
  legacy-migration/importer and Roo wiring are removed by the row-13 unit;
- provider/preset/catalog/onboarding/org cleanup under LOCK-006 (bounded P4.4-T5 preset display metadata enrichment removed; bounded P4.4-T6 preset `@kilocode/kilo-gateway` bundled provider loader removed; `models-api.json` 3.0 MB, `models.dev` snapshot, generic adapters, `ModelCache`, and custom-provider paths remain open);
- legacy storage reader/writer and sync/warp removal rows in tracker §7;
- convergence machinery review and removed-feature startup/resource evidence;
- P5 selector/startup structural absence and worker-unavailable scenarios.

No storage or transport row is marked complete by this artifact.

## Unknowns and next actions

Unknowns intentionally left open:

1. A static source scan does not prove the complete runtime call graph for every
   diagnostic or TUI path. The next unit must trace and/or remove each retained
   adjacent consumer before physical deletion is claimed.
2. The complete generated-SDK/public-server surface affected by transport
   narrowing is not enumerated here; only the inspected server wrappers and
   their tests are cited.
3. Locale-key ownership for provider/catalog/org and migration residues needs a
   dedicated LOCK-006/P4.4 inventory; this unit does not remove or reclassify
   those keys.
4. Convergence/startup/resource deltas against P0 remain unmeasured for this
   source-removal unit.

Next actions are bounded to later P4.4 units: narrow/delete retained diagnostic
and TUI source inventories, reconcile SDK/server transport wrappers, complete
the separate LOCK-006 provider catalog inventory, review convergence and
startup/resource evidence, and then update the tracker only with verified
per-row results. P4.4 remains Active; P4.5 and P5 remain open.

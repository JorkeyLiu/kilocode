# P4.4-G3 — Live file-backed retention/high-low watermark proof (G3 residual, logical simulation only)

- **Status:** Evidence-only toward P4-G8/Gate D preparation. P4-G8 remains **Active**, G3 remains **Active**, Gate C/D/P4.4 remain open. No live `Global.Path.data` cutover, no physical 8/6 GiB pressure proof, no crash/lifecycle proof, no transport change, no Linux/Windows fd3/fd4 claim.
- **Bound:** Run-owned temporary file-backed `Database.layerFromPath` with WAL (`mkdtempSync(join(os.tmpdir(), "s2-g3-*"))`, `join(dir,"storage")` via per-test `Global.Path.data = dir` mutation captured/restored inside `Semaphore.makeUnsafe(1) globalPathGuard withPermits(1)` file-local guard covering root creation, assignment, layer/service execution, restoration, and `safeRemoveOwned` deletion via `try/finally` after `Effect.scoped` close, `expect(dir).not.toBe(origData)` + `expect(Database.path()).toBe(":memory:")` + `assertOwned` `storageDir.startsWith(dir)` + `existsSync(join(storageDir,...))` + `!existsSync(join(origData,"storage",...))` in-test). Logical high/low watermark via existing injectable `Accounting` service (`Retention.HIGH_BYTES=8 GiB`, `Retention.LOW_BYTES=6 GiB`) — **no 8 GiB allocation in CI** and **not described as physical disk-pressure proof**. No production code, retention semantics, transport, or crash hooks added. No fixed `Effect.sleep` except intentional `Duration.millis(30/15)` in concurrency-guard probe; no dead imports/helpers, no avoidable `any` tautology; every `Effect.scoped` closes before `rmSync` and `existsSync(dir)` false is asserted; 3 sequential runs leave no `s2-g3-*` under `$TMPDIR` (`os.tmpdir()`).
- **Isolation (product vocabulary):** Run-owned temp file-backed only via per-test `Global.Path.data` mutation to `s2-g3-*` (never original user data), logical watermark via injectable `Accounting` (no 8 GiB, labeled logical simulation), no production code/semantics/transport/crash, SDK PATCH authoritative and title-only gates remain open (no Linux/Windows claim). Repository-wide product locks unchanged.
- **Scope lock:** Title/`result_snapshot` rows created only as retention fixtures via `SessionOperation.insertSessionUpdateSucceededTx` (`sessionUpdate:<id>:<token>` + `result_snapshot` JSON); no broad `metadata`/`permission`/`archive` redesign, no second transport, no `Promise` facade in shared `packages/opencode/src`.

## Requirement mapping — G3 live retention/high-low watermark (P4.4-G3 toward P4-G8)

| # | Acceptance criterion (spec Scope Must do) | Test evidence | Evidence boundary |
|---|---|---|---|
| 1 | B2 `session_update` title + `result_snapshot` family cascade through **live** `Maintenance.runOnce` (file-backed WAL) | [test: packages/opencode/test/storage/s2-live-retention.test.ts](../../../packages/opencode/test/storage/s2-live-retention.test.ts) `B2 session_update title+result_snapshot family cascade through live Maintenance.runOnce (file-backed WAL)` | Creates session, inserts durable `sessionUpdate` via `insertSessionUpdateSucceededTx` (one `title`+`revision`+`changefeed` `changed` + `result_snapshot` JSON), writes `session_diff`/`session_diff_base` via `Storage` under `join(dir,"storage")` with `existsSync` under owned root and `!existsSync` under `origData`, sets `time_updated=0`, runs `Maintenance.runOnce` with logical HIGH→LOW `Accounting`, asserts `selected=1` `deleted=1` `rowsReclaimed=1` `checkpoint=ok` `vacuum=ok`, verifies session row gone, `SessionOperation` rows cascade-deleted, `session_changefeed` retains `changed`+`deleted` (deleted `revision=before+1`), `retention_obligation` empty, family artifacts removed (`!existsSync` under owned root), WAL `journal_mode=wal` `auto_vacuum=2` on file-backed DB, `physicalBytesWith(file,storageDir)===safeStat(main)+safeStat(wal)+artifactBytes(storageDir)`, run-owned temp cleaned after `Effect.scoped` close (`existsSync(dir)` false). Logical watermark only. |
| 2 | High→low hysteresis with injected `Accounting` and **real file-backed DB/artifact accounting where possible** | [test: packages/opencode/test/storage/s2-live-retention.test.ts](../../../packages/opencode/test/storage/s2-live-retention.test.ts) `high->low hysteresis with file-backed DB and logical Accounting (WAL mode and WAL-inclusive formula, artifact bytes via real FS)` | Two old families (`time_updated=0`) with `session_diff` under `join(dir,"storage")` (both `existsSync` under owned root, not under `origData`), logical `Accounting` returns `HIGH+2048` for `before`/`cur` then `LOW`, asserts `beforeBytes=HIGH+2048` `afterBytes=LOW` `selected=2` `deleted=1` `checkpoint=ok` `vacuum=ok`, verifies `Accounting.physicalBytesWith(file, storageDir) === safeStat(main)+safeStat(wal)+artifactBytes(storageDir)` WAL-inclusive formula with size-if-present on file-backed `kilo.db`, one artifact remains under owned root, `run-owned` temp not `origData`. Logical HIGH/LOW; no physical 8 GiB fill; no tautology. |
| 3 | 7-day/active/leased protection and **descriptive skipped diagnostics when LOW cannot be reached** | [test: packages/opencode/test/storage/s2-live-retention.test.ts](../../../packages/opencode/test/storage/s2-live-retention.test.ts) `global busy short-circuit and 7-day protection with LOW-unreachable diagnostics are descriptive (file-backed)` | Four families: recent `fresh` (within 7 days), old, `active` (`Ownership.acquireActive`), `leased` (`Lease.acquire`) all with `session_diff` under `join(dir,"storage")`. First `runOnce` with stuck `HIGH+512` returns global busy idle `selected=0` `deleted=0` `skipped=4` `skipReasons={busy:4}` `checkpoint=skipped-busy` `vacuum=skipped-busy` (global `activeLeased` guard — whole run short-circuits before per-family checks). After releasing active/leased, second `runOnce` still `HIGH+512` with `fresh` still recent returns `selected=3` `deleted=3` `skipped=1` `skipReasons={within-7days:1}` `checkpoint=ok` `vacuum=ok` `afterBytes=HIGH+512` `failures=[]` and one family (`fresh`) remains. Proves global busy short-circuit, 7-day `within-7days` protection, and LOW-unreachable descriptive diagnostics (deleted all eligible yet still HIGH). Per-family `active`/`leased` revalidation (`active-revalidated`/`leased-revalidated`) is structurally unreachable while any busy exists (maintenance.ts global guard) and is documented as not exercised, not claimed. Logical stuck HIGH; no physical pressure. |
| 4 | `retention_obligation` **replay after an artifact cleanup failure** (attempts bump, then success) | [test: packages/opencode/test/storage/s2-live-retention.test.ts](../../../packages/opencode/test/storage/s2-live-retention.test.ts) `retention_obligation replay after artifact cleanup failure (file-backed) increments attempts then succeeds` | File-backed `deleteFamilyTransaction` under `join(dir,"storage")` creates obligation `attempts=0` with artifact under owned root, `replayObligations` with failing deleter bumps `attempts=1` and retains row, `Maintenance.replay()` (real `Storage.remove` under owned root) clears obligation, idempotent second replay leaves `0` rows. Also covers direct `replayObligations(() => void)` success path. Run-owned file-backed, no user data, `Effect.scoped` closes before `rmSync`. |
| 5 | **WAL-inclusive physical byte accounting / checkpoint / vacuum outcomes** | [test: packages/opencode/test/storage/s2-live-retention.test.ts](../../../packages/opencode/test/storage/s2-live-retention.test.ts) `WAL-inclusive physical byte accounting, checkpoint TRUNCATE and incremental_vacuum outcomes (file-backed)` | File-backed `kilo.db` under `join(dir,"storage")` asserts `PRAGMA journal_mode=wal` and `PRAGMA auto_vacuum=2` (incremental), creates sessions + `session_diff` artifacts under owned root, verifies `Accounting.physicalBytesWith(file, storageDir) === manual safeStat(main)+safeStat(wal)+artifactBytes` and `>= artifactBytes`, `PRAGMA wal_checkpoint(TRUNCATE)` succeeds and `wal` size not increased (`walAfter <= walStat`), `PRAGMA incremental_vacuum(10)` succeeds, `Maintenance.runOnce` with `LOW` returns descriptive `checkpoint`/`vacuum` (`ok`/`skipped-below-high`/`skipped-no-delete`). Proves WAL-mode configuration, WAL-inclusive accounting formula, and checkpoint/vacuum plumbing on file-backed store; does **not** claim nonzero WAL sidecar contribution (portably nondeterministic — narrowed to mode+formula). |
| 6 | **Cleanup ownership/idempotency** (Storage.remove, obligation, lease/active, run-owned temp) | [test: packages/opencode/test/storage/s2-live-retention.test.ts](../../../packages/opencode/test/storage/s2-live-retention.test.ts) `cleanup ownership/idempotency: Storage.remove, obligation, lease, active are idempotent and run-owned temp is cleaned` | `Storage.remove` on existing key then second remove succeeds with `!existsSync` under owned root, `read` after fails, remove on never-existed key succeeds; `retention_obligation` replay idempotent (`0` rows after double replay); `Ownership.acquireActive`/`Lease.acquire` double-acquire and double-release idempotent (`isActive`/`isLeased` reflect refcount, second release no throw); `mkdtemp` dir starts with `os.tmpdir()` contains `s2-g3-`, `storageDir` and `file` start with `dir`, `existsSync(file)` true during test and `safeRemoveOwned` after `Effect.scoped` leaves `existsSync(dir)` false; 3 sequential runs leave no `s2-g3-*` under `os.tmpdir()`. No `any` cast, no sleep except probe. |
| 7 | **File-local `Global.Path.data` ownership guard and concurrent probe** | [test: packages/opencode/test/storage/s2-live-retention.test.ts](../../../packages/opencode/test/storage/s2-live-retention.test.ts) `concurrency guard serializes Global.Path.data overrides (no cross-contamination)` | File-local `Semaphore.makeUnsafe(1) globalPathGuard withPermits(1)` serializes every `Global.Path.data` override lifetime (root `mkdtempSync`, assignment, `Storage.defaultLayer` via `join(dir,"storage")`, layer/service execution, `try/finally` restoration, `safeRemoveOwned` deletion) and releases on every failure path; deliberately concurrent `Effect.all(..., { concurrency: 2 })` probe with two guarded `runOwned` tasks each capturing `origData` inside guard, assigning `dir`, `Duration.millis(30/15)` hold, asserting `captured===orig` and `Global.Path.data===dir` during hold, then `seen=[a-enter,a-exit,b-enter,b-exit]` or `[b-enter,b-exit,a-enter,a-exit]` (no interleaving), `Global.Path.data===orig` after, `!existsSync(dir)` for each. Proves no cross-contamination. |

## Verification

| Command | Result |
|---|---|
| `bun test ./test/storage/s2-live-retention.test.ts` (packages/opencode, 7 live) | PASS — 7 pass 0 fail 186 expects (verified 2026-08-30) |
| `bun test ./test/storage/s2-live-retention.test.ts` x3 sequential + `ls -d $(node -e "console.log(require('os').tmpdir())")/s2-g3-*` | PASS — 7 pass each, no `s2-g3-*` after each run and final (verified 2026-08-30, `os.tmpdir()` + `/tmp` checked) |
| `bun test ./test/storage/s2-live-retention.test.ts ./test/storage/s2-production.test.ts` (packages/opencode) | PASS — 20 pass across 2 files (7 G3 + 13 S2 production) (verified 2026-08-30) |
| `bun test ./test/storage/s2-live-retention.test.ts ./test/storage/s2-production.test.ts ./test/storage/s2-retention-engine.test.ts` | PASS — 26 pass across 3 files (verified 2026-08-30) |
| `bun test` (packages/core) | PASS — 15 fail is known baseline (verified 2026-08-30) |
| `bun turbo typecheck` (root) | PASS — 17 tasks ok (verified) |
| `git diff --check` | PASS — no whitespace errors (verified) |
| `bun run script/check-md-table-padding.ts` | PASS — no padded tables (verified) |
| `bun run script/check-architecture-impact.ts --worktree` (root) | PASS — no high signal for test-only change (test files exempt) — reported per completion gate |

## Scenario matrix — what is proven vs not proven

| Scenario | Transport / Store | Provenance | Status | Notes |
|---|---|---|---|---|
| File-backed WAL `kilo.db` with `journal_mode=wal` `auto_vacuum=2`, `wal_checkpoint(TRUNCATE)` + `incremental_vacuum`, WAL-inclusive `physicalBytesWith` formula | `Database.layerFromPath(mkdtemp/kilo.db)` inside `os.tmpdir()` run-owned `s2-g3-*` with `join(dir,"storage")` via `Global.Path.data` mutation, `safeRemoveOwned` after `Effect.scoped` | `s2-live-retention.test.ts` `WAL-inclusive` (verified PASS 3 runs no leak) | PASS — logical file-backed proof on Darwin (WAL/incremental are SQLite invariants). | WAL-mode and `physicalBytesWith===safeStat(main)+safeStat(wal)+artifactBytes` proven; nonzero WAL sidecar not claimed (nondeterministic, narrowed). |
| B2 `session_update` durable lane retention cascade via live `Maintenance.runOnce` | File-backed `s2-g3-*` + `join(dir,"storage")` + logical `HIGH→LOW` `Accounting` injection | `s2-live-retention.test.ts` `B2 session_update title+result_snapshot family cascade` | PASS — verified | One `title`+`revision`+`changed`+`result_snapshot` + `deleted` tombstone; cascade deletes `session_operation` via `ON DELETE CASCADE`; artifacts under owned root removed; `LOW` is logical. |
| High→low hysteresis with injected `Accounting` and real FS stats where possible | File-backed `s2-g3-*` + `join(dir,"storage")` + logical `Accounting` | `s2-live-retention.test.ts` `high->low hysteresis` | PASS — verified | `HIGH=8 GiB` `LOW=6 GiB` constants asserted; `physicalBytesWith` manual equality proven; `LOW` is injected, not physical 8 GiB fill. |
| Global busy short-circuit + 7-day protection + LOW-unreachable descriptive diagnostics | File-backed `s2-g3-*` + stuck `HIGH` injection | `s2-live-retention.test.ts` `global busy short-circuit and 7-day protection with LOW-unreachable diagnostics` | PASS — verified | Global `busy` guard (`skipped-busy`) proven separately; `within-7days` proven with `fresh` recent; LOW-unreachable `afterBytes` still `HIGH` after deleting all eligible; per-family `active-revalidated`/`leased-revalidated` structurally unreachable while any busy (maintenance.ts global guard) — documented not claimed. |
| `retention_obligation` replay after artifact failure (attempts bump) | File-backed `s2-g3-*` | `s2-live-retention.test.ts` `retention_obligation replay` | PASS — verified | Failing deleter `attempts 0→1`, `Maintenance.replay()` clears via `Storage.remove` under owned root, idempotent. |
| Cleanup ownership/idempotency + run-owned temp exact cleanup | File-backed `s2-g3-*` `mkdtemp` + `Storage`/`Ownership`/`Lease` | `s2-live-retention.test.ts` `cleanup ownership/idempotency` | PASS — verified | `Storage.remove` idempotent, obligation/lease/active refcount idempotent, `safeRemoveOwned` after `Effect.scoped`, 3 runs no `s2-g3-*`. |
| File-local `Global.Path.data` guard + concurrent probe | `Semaphore.makeUnsafe(1) globalPathGuard withPermits(1)` file-local guard | `s2-live-retention.test.ts` `concurrency guard serializes ...` (verified PASS 3 runs) | PASS — logical file-local proof (no prod code). | `Effect.all` concurrency 2 with guarded `runOwned` probes proves `captured===orig`, `Global.Path.data===dir` during hold, `seen` paired serialization, `!existsSync(dir)` and `Global.Path.data===orig` after. |
| Linux/Windows fd3/fd4 production | `kilo serve` extra-fd | None | Not proven — no claim | Product isolation forbids Linux/Windows claim (Darwin-only logical proof). |
| Physical 8/6 GiB disk-pressure proof (real 8 GiB fill, `df` pressure) | Real `Global.Path.data` with 8 GiB file | None | Not proven — explicitly not claimed (logical simulation only) | Logical simulation only; `HIGH+1024`/`LOW` are injected constants, not `fallocate`/`df`. |
| Crash recovery / durable retry accounting across process kill | Live crash | None | Not proven — residual | No `kill -9` + replay proof; `retention_obligation` replay is in-process only. |
| Live `Global.Path.data` cutover / production retention on real user data | `Global.Path.data` | None | Not proven — residual | Test asserts `dir !== origData` and `Database.path() === ":memory:"` for parent process; every artifact path `startsWith(dir)` and not under `origData`. |

## Isolation and explicit non-claims

- Run-owned: every `kilo.db` is `mkdtempSync(join(os.tmpdir(), "s2-g3-*"))` with `storageDir=join(dir,"storage")` via file-local `Semaphore.makeUnsafe(1) globalPathGuard withPermits(1)` guard around per-test `Global.Path.data = dir` mutation (captured `origData` inside guard, restored in `finally` after `Effect.scoped`), `try { yield* Effect.scoped } finally { safeRemoveOwned(dir) }` with `assertOwned` (`startsWith(os.tmpdir())`, `includes("s2-g3-")`, `startsWith(dir)` for file/storage) and `existsSync(join(storageDir, kind, id+".json"))` true during test and `!existsSync(join(origData,"storage",...))` true, `expect(dir).not.toBe(origData)` and `expect(file).not.toBe(Database.path())` in-test; parent process `Database.path() === ":memory:"` via preload `KILO_DB=:memory:`; `safeRemoveOwned` verifies `startsWith(os.tmpdir())` and `includes("s2-g3-")` before `rmSync`; concurrency probe proves two guarded `runOwned` tasks serialize (`seen` paired, no interleaving, `Global.Path.data===orig` after); 3 sequential runs leave no `s2-g3-*` under `os.tmpdir()` (checked `ls -d $(node -e "console.log(require('os').tmpdir())")/s2-g3-*`).
- Logical watermark: `HIGH_BYTES`/`LOW_BYTES` are constants (`8*1024*1024*1024` / `6*1024*1024*1024`) injected via `Accounting.Service` layer; tests never allocate 8 GiB, never `fallocate`, never check `df`; results labeled **logical high/low watermark simulation**.
- WAL-inclusive accounting is proven via `safeStat(main)+safeStat(wal)+artifactBytes(storageDir)` equality on the file-backed temp store under `join(dir,"storage")`, not on `Global.Path.data` original; `journal_mode=wal` and `auto_vacuum=2` proven, `wal_checkpoint(TRUNCATE)` not increasing WAL proven, nonzero WAL sidecar contribution explicitly **not** claimed (portably nondeterministic, narrowed to mode+formula).
- Protection: global `busy` short-circuit (`selected=0` `skipReasons.busy`) proven separately; 7-day `within-7days` proven with `fresh` recent; LOW-unreachable `afterBytes` still `HIGH` after deleting all eligible proven; per-family `active-revalidated`/`leased-revalidated` is structurally unreachable while any `active`/`leased` exists (maintenance.ts `activeLeased` global guard returns before per-family loop) — documented as not exercised, not claimed.
- No production code, retention semantics, transport, or crash hooks added; `packages/core/src` and `packages/opencode/src` unchanged; no `Effect.sleep("80 millis")` fixed sleeps (boot readiness via `pollWithTimeout`; only intentional `Duration.millis(30/15)` in concurrency guard probe), no dead `memLayer`/`AppLayer`/`memoMap`/`tmpdirScoped` imports, no avoidable `any` casts (typed `Database.Interface["db"]` and direct `k`).
- No Gate C/D/P4.4 closure, no G3 Complete, no `Global.Path.data` cutover, no crash/lifecycle, no Linux/Windows production proof beyond Darwin SQLite invariants.

## Explicit residuals (still unproven)

| Deferred | Status |
|---|---|
| Live physical 8/6 GiB disk-pressure proof with real `Global.Path.data` fill + `df` / `ENOSPC` + `wal_checkpoint` under pressure | Not proven — logical simulation only (no 8 GiB allocation) |
| Crash recovery (kill -9 after `deleteFamilyTransaction` before `artifact deleter`, then `replayObligations` across restart) | Not proven — in-process replay only |
| Real mid-flight lease/epoch ambiguous / live `transportUnknown` | Not claimed beyond unit |
| Linux/Windows fd3/fd4 production | Not claimed — fail-closed only |
| Per-family `active-revalidated`/`leased-revalidated` mid-run protection (active acquired during `eligible` loop) | Not proven — structurally unreachable while any busy at start; not exercised (documented) |
| Gates C/D/E/F/P4.4 closure, G3 Complete, live `Global.Path.data` retention trimming on real user data | Not claimed — P4-G8 remains Active, G3 remains Active |

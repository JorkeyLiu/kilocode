# P4.3 Cutover Evidence — Atomic Legacy-Reader Cutover Complete

> **Status: CUTOVER COMPLETE — atomic legacy-reader deletion executed; no dual-read, no import; P4.3 gate complete. P4.4/P4.5 remain open.**
> This artifact is the dedicated post-cutover evidence for P4.3. The pre-cutover reconciliation checklist at `specs/vscode-orchestrator/evidence/p4.3-pre-cutover-reconciliation-checklist.md` (reconciled 2026-08-24, operator `jorkeyliu`, signature `manual-reset-rebuild`) is preserved as historical record and is not modified here.

## Artifact Metadata

| Field | Value |
|---|---|
| Title | P4.3 Cutover Evidence — Atomic Legacy-Reader Cutover Complete |
| Date | 2026-08-25 |
| Operator | `jorkeyliu` |
| Baseline commit (HEAD at evidence creation) | `39a706316d3834412d3003b6659e85298d0ed34e` |
| Implementation state | working-tree validation before commit — cutover implementation validated against the working tree based on HEAD `39a706316d`; evidence formalizes the validated cutover without code changes |
| Pre-cutover reconciliation | `specs/vscode-orchestrator/evidence/p4.3-pre-cutover-reconciliation-checklist.md` — reconciled 2026-08-24, 15→4/13 mapping complete, clean reset / manual rebuild |
| Cutover status | **COMPLETE — atomic deletion executed 2026-08-25; legacy readers absent from retained CLI effective-config paths** |
| P4.3 gate | **COMPLETE** — legacy-reader cutover checklist item MET (tracker section 5, P4 exit checklist) |
| P4.4 status | **OPEN — per-row inactive/removal evidence and transport narrowing not claimed complete** |
| P4.5 status | **OPEN — old CLI/TUI/Console surface deletion not claimed complete** |
| Verification signature | `p4.3-cutover-complete-2026-08-25` |

Historical pre-cutover wording (`CUTOVER PENDING — readers NOT deleted`) remains in the pre-cutover checklist file intentionally. This artifact supersedes it for cutover completion; the checklist file is preserved unchanged as historical decision record.

## Purpose and Bound

- Records the atomic legacy-reader cutover that makes the repository's migration state match the validated implementation.
- Applies no implementation code changes. Evidence and tracker only.
- Canonical-only effective config after cutover: global/project `kilo.jsonc` plus canonical assets, SecretStorage, runtime defaults. No dual-read/import. Instruction `KILO_CONFIG_DIR` profile removed in P4.4-T17 and `ConfigPaths` `KILO_CONFIG_DIR` reader removed in P4.4-T18 and neither is effective `Config.Service` authority; TUI `KILO_CONFIG_DIR` behavior was retained at T18 as sole direct reader but removed in P4.4-T27/T27-correction — no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig` reader remains (only `Global.Path.config` override and sandbox deny-list per compatibility, not an effective TUI source); historical T18 observation preserved with this additive superseding clarification. T28 ensures config-console TUI GET/PATCH/reload all use same trusted `canonicalRoot(directory, worktree)` — no ancestor walk, no `.kilocode`, no legacy env source — with canonical precedence global → direct → `<workspaceRoot>/.kilo` (historical read/write mismatch superseded). T29 ensures standalone CLI TUI startup uses same trusted canonical Git worktree root via `resolveWorktree`/`canonicalRoot` (nested `tui.json[c]`/`.kilo` ignored, `root` direct + `.kilo` canonical, plugin install and `fileScope` rooted at `root`, non-Git fallback to `cwd`; historical nested-as-root and legacy-discovery last-wins claims superseded, see T29 clarification). T30 ensures daemon-attached and explicit attach lifecycles load from same trusted canonical root as standalone and config-console (`resolveWorktree`/`canonicalRoot`, non-Git fallback; `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/ancestor/`.kilocode`/migration ignored, `ConfigPaths.files` API retained with no active TUI call); historical attach raw-cwd and T18/T19 sole-reader claims superseded, see T30 clarification.

## Cutover Decisions Applied

Translated from session coordination into product vocabulary (no coordination tokens in artifact):

| Decision | Statement |
|---|---|
| P4.3 completion; P5 not started | P4.3 is complete after this cutover; P5 has not started. |
| Canonical-only, no dual-read/import | Retired readers/migrators/`loadMode` are deleted from retained CLI effective-config paths. Canonical sources are global/project `kilo.jsonc` + canonical assets, SecretStorage, runtime defaults. Instruction `KILO_CONFIG_DIR` profile removed in P4.4-T17 and `ConfigPaths` `KILO_CONFIG_DIR` reader removed in P4.4-T18 and neither is effective `Config.Service` authority; historical at T18 only TUI (`tui.ts` global → legacy → `KILO_CONFIG_DIR` last wins) retained `KILO_CONFIG_DIR` as sole direct reader per LOCK-001/002 — superseded in P4.4-T27 (no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/ancestor/`.kilocode`/migration reader remains); current (T27-T30) TUI uses canonical Git worktree root `resolveWorktree`/`canonicalRoot` with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only, precedence global → direct → `.kilo`; `Global.Path.config` override and sandbox deny-list remain per LOCK-003 (not effective TUI source). |
| P4.4 remains open | Per-row inactive/removal evidence/transport narrowing remains open; do not mark it complete. |
| P4.5 remains open | CLI/TUI/Console old-surface deletion remains open; do not mark it complete. No TUI/CLI source edits in this cutover. |
| Canonical ownership | Canonical loader is read-only; canonical mutations use `canonicalRoot`, shared discovery locks, atomic JSONC writes, and lifecycle ownership. |
| Rules gap deferred | `rules` typed-asset materialization gap is explicitly deferred; no rules runtime added. |

## Canonical Retained Sources (4)

Exactly the four rows of runtime spec §3.1. No other authored source is retained for effective `Config.Service`.

| # | Source class (runtime §3.1) | Owner | Persistence | Legal contents |
|---|---|---|---|---|
| 1 | Canonical global config files/assets | User authoring; extension host (GUI) reads/writes on the user's behalf | One canonical global config root | Global-effective config fields and global typed assets per field registry |
| 2 | Canonical project config files/assets | User authoring; extension host (canonical boundary) | `<workspaceRoot>/.kilo/` | Project-effective config fields and project typed assets (for example agent markdown, commands, skills, project MCP/tool declarations) per field registry |
| 3 | SecretStorage credentials | Extension host | VS Code SecretStorage | Credentials only, referenced by opaque IDs |
| 4 | Runtime defaults and safety invariants | Versioned runtime schema | Schema (versioned) | Defaults and safety invariants, never user-authored overlays |

Separate and not effective-config inputs: `globalState`/`workspaceState` (UI-only layout/churn), session/storage state (operational facts), bounded permission approval records. Instruction profile directory fallback (`KILO_CONFIG_DIR` in `packages/opencode/src/session/instruction.ts`) was removed in P4.4-T17 and `ConfigPaths` reader removed in P4.4-T18 (historically outside `Config.Service` effective-config authority before removal) — historical at T18 only TUI retained `KILO_CONFIG_DIR` as sole direct reader per LOCK-001/002 — superseded in P4.4-T27 (no TUI `KILO_CONFIG_DIR` reader remains; current T27-T30 TUI uses canonical root `resolveWorktree`/`canonicalRoot` with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only); `Global.Path.config` override and sandbox deny-list remain per LOCK-003 — see Instruction vs Effective Config below.

## 15→4/13 Mapping Pointer

- Pre-cutover checklist (`p4.3-pre-cutover-reconciliation-checklist.md`) reconciled 2026-08-24 enumerates all 15 P0 sources (inventory §6.1) and classifies each exactly once against the closed taxonomy: 3 retained (P0-02/P0-03/P0-11 to canonical `kilo.jsonc` global/project) and 12 removal mappings to 7 distinct removal classes; the remaining 6 removal classes have no distinct P0 source but remain explicit removal boundaries — see `p4.3-readiness-inventory.test.ts` `REMOVAL_WITHOUT_DISTINCT_READER`.
- This cutover executes the `Planned deletion — No target reader (pending cutover)` rows atomically. After cutover every P0 removal row is `Deleted; no target reader` with no import.

Verification of taxonomy before cutover: `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts` proves closed taxonomy (15 sources, 4 retained, 13 removal, no invented class) without claiming live reader presence. That test is historical taxonomy proof; post-cutover absence is proven by `p4-3-cutover.test.ts` (see Verification).

- Authoritative inventory: `specs/vscode-orchestrator/p0-current-state-inventory.md` §6.1 (15 sources)
- Authoritative taxonomy: `specs/vscode-orchestrator/runtime-and-configuration-direction.md` §3.1 (4 retained), §8.1 (13 removal)

## Atomic Deletion Scope

All legacy readers/migrators deleted together; no dual-read window, no import tool, no compatibility reader survives in retained CLI effective-config paths. Deletion is verified by `p4-3-cutover.test.ts` absence anchors plus `git diff --stat` (49 files, 5966 deletions vs 778 insertions).

| Scope class (runtime §8.1) | Deleted readers / migrators / code paths | Verified absent (retained paths) |
|---|---|---|
| `KILO_CONFIG` env override | `Flag.KILO_CONFIG` file indirection in `packages/opencode/src/config/config.ts` (`if (Flag.KILO_CONFIG)`) | `p4-3-cutover.test.ts` `Flag.KILO_CONFIG` absent from `config/config.ts` |
| `KILO_CONFIG_DIR` env override | `Flag.KILO_CONFIG_DIR` overlay in `config/config.ts:707-732` | `Flag.KILO_CONFIG_DIR` absent from `config/config.ts` (historical at T18: retained in `tui.ts` only as sole direct reader per LOCK-001/002 since P4.4-T18 — superseded in P4.4-T27 (T27-correction removes all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig`/`.kilocode`/ancestor readers; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30 clarification); removed from `config/paths.ts` in P4.4-T18 and from `session/instruction.ts` in P4.4-T17 — see Instruction note; `Global.Path.config` override and sandbox deny-list remain per LOCK-003, not an effective TUI source) |
| `KILO_CONFIG_CONTENT` env override | `process.env.KILO_CONFIG_CONTENT` injection `config/config.ts:794-808` | `KILO_CONFIG_CONTENT` absent from `config/config.ts` (single occurrence check) |
| `KILO_PERMISSION` env override | `Flag.KILO_PERMISSION` + `result.tools` derived permission overlay | `Flag.KILO_PERMISSION`, `result.tools` absent from `config/config.ts` |
| Legacy `opencode.*` keys, `.opencode` / `.kilocode` locations | `.opencode`/`.kilocode` discovery, `opencode.json`/`opencode.jsonc`/`kilo.json` global filename readers, `.kilocode` path joins | `.opencode`, `.kilocode`, `opencode.json` absent from `config/config.ts`; `KILO_DIR_SUFFIXES = [".kilo"]` only; `ALL_CONFIG_FILES = ["kilo.jsonc"]` |
| Global project-asset sources | `primaryPaths(` fan-out crossing workspace boundaries | `primaryPaths(` absent from `config/config.ts` |
| Ancestor directory walks | `ConfigPaths.files` / `ConfigPaths.directories` walks scanning ancestors | `ConfigPaths.files`, `ConfigPaths.directories` absent from `config/config.ts` |
| Primary-worktree mirror reads | primary-worktree mirror-read logic via `primaryPaths` | same `primaryPaths(` deletion; overlay uses `canonicalRoot(directory, worktree)` only |
| Cloud/org/managed config sources | `managedConfigDir()`, `readManagedPreferences`, `.well-known/opencode` fetch, `loadOrganizationModes` | `managedConfigDir()`, `readManagedPreferences`, `.well-known/opencode`, `loadOrganizationModes` absent from `config/config.ts` and `kilocode/config/config.ts` |
| Top-level `mode`/`tools` conversions | `result.mode` conversion, `result.tools` aggregation, `config/agent.ts:loadMode` | `result.mode` absent from `config/config.ts`; `loadMode` absent from `config/agent.ts` |
| Arbitrary CLI/env override layers | Generic catch-all env-derived permission conversion (`result.permission` derived overlay) | Verified via `KILO_PERMISSION`/`result.tools` anchors removed; no generic env overlay remains |
| Legacy global config filenames/readers | `path.join(Global.Path.config, "config.json")`, `"kilo.json"`, `"opencode.json"`, `"opencode.jsonc"`, TOML `with: { type: "toml" }` | Each absent from `config/config.ts`; `global-stamp.ts` narrowed to `["kilo.jsonc"]` |
| Legacy migration readers/import tooling | `loadLegacyConfigs` (`config.ts:495-501`), `migrateBashPermission`, `substituteWellKnownRemoteConfig`, per-asset migrators (`config-injector.ts`, `ignore-migrator.ts`, `mcp-migrator.ts`, `modes-migrator.ts`, `rules-migrator.ts`, `workflows-migrator.ts`) | `loadLegacyConfigs`, `migrateBashPermission` absent; 6 migrator files deleted (`D packages/opencode/src/kilocode/*-migrator.ts` plus `config-injector.ts`); tests deleted accordingly |

Supporting narrowed surfaces verified present (canonical-only retained path):

- `path.join(Global.Path.config, "kilo.jsonc")` and `".kilo", "kilo.jsonc"` remain in `config/config.ts`
- `ConfigAgent.load`, `ConfigCommand.load`, `ConfigPlugin.load`, `KilocodeGlobalConfigStamp.read`, `KilocodeDefaultPlugins.apply`, `canonicalRoot` remain
- `KilocodeConfig.KILO_CONFIG_FILES = ["kilo.jsonc"]`, `ALL_CONFIG_FILES = ["kilo.jsonc"]`, `KILO_DIR_SUFFIXES = [".kilo"]`, `AGENT_PATTERNS`/`COMMAND_PATTERNS` canonical `.kilo` only
- `KilocodeConfigOverlay.globalTarget()` / `projectTarget` / `projectConfigUpdateTarget` use `canonicalRoot` only; `files = ["kilo.jsonc"]`, `dirs = [".kilo"]`
- `permission/index.ts` global candidates `["kilo.jsonc"]` only; no legacy `".kilocode/kilo.json"` variants
- `skill/index.ts` narrowed to `config.directories()` with `KILO_SKILL_PATTERN`; no `primaryPaths` / `Flag.KILO_CONFIG_DIR`

Full file-level deletion list (working-tree `git status` vs HEAD `39a706316d`):

- Deleted: `packages/opencode/src/kilocode/config-injector.ts`, `packages/opencode/src/kilocode/ignore-migrator.ts`, `packages/opencode/src/kilocode/mcp-migrator.ts`, `packages/opencode/src/kilocode/modes-migrator.ts`, `packages/opencode/src/kilocode/rules-migrator.ts`, `packages/opencode/src/kilocode/workflows-migrator.ts`, plus their test counterparts `test/kilocode/config-injector.test.ts`, `test/kilocode/ignore-migrator.test.ts`, `test/kilocode/mcp-migrator.test.ts`, `test/kilocode/modes-migrator.test.ts`, `test/kilocode/rules-migrator.test.ts`, `test/kilocode/workflows-migrator.test.ts`
- Modified (readers removed, canonical retained): `packages/opencode/src/config/config.ts` (−~538 lines), `packages/opencode/src/config/agent.ts` (60 lines loadMode removed), `packages/opencode/src/kilocode/config/config.ts` (−~268 lines), `packages/opencode/src/kilocode/config/overlay.ts`, `packages/opencode/src/kilocode/config/global-stamp.ts`, `packages/opencode/src/permission/index.ts`, `packages/opencode/src/project/instance-context.ts` (canonicalRoot added), `packages/opencode/src/skill/index.ts`, `packages/opencode/src/kilocode/agent/index.ts`, handlers/docs

`ConfigPaths` retention (post-T18, superseded T27): `packages/opencode/src/config/paths.ts` no longer retains `Flag.KILO_CONFIG_DIR` — now `unique([Global.Path.config])` only (`Flag` import deleted, `KILO_CONFIG_DIR` absent; `targets: [".kilo"]` removed in P4.4-T14, home walk removed in P4.4-T15, ancestor walk removed in P4.4-T16 — now `directories(): Effect<string[]>`; `ConfigPaths.files()`/`fileInDirectory()` retained for other consumers); **historical T18 TUI `KILO_CONFIG_DIR` last-wins superseded** — P4.4-T27 (T27-correction) removes all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig`/`.kilocode`/ancestor readers and active `ConfigPaths.files("tui", ctx.directory)` walk — now TUI uses canonical Git worktree root (`resolveWorktree`/`canonicalRoot` via `Filesystem.resolve`, non-Git fallback to `directory`) with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only (`ConfigPaths.files` API retained with no active TUI call, see P4.4-T30 clarification); `session/instruction.ts` no longer retains `Flag.KILO_CONFIG_DIR` (removed in P4.4-T17). `Global.Path.config` override and sandbox deny-list remain per LOCK-003 (not effective source). All isolated from `Config.Service` canonical effective config per `p4-3-cutover.test.ts`.

## Canonical Loader Read-Only + Mutation Ownership

| Property | Evidence |
|---|---|
| Loader read-only | No loader-side persistence outside lock+atomic: `config/config.ts` contains `P4.3 canonical-only: in-memory normalization only; no loader-side persistence` and `no loader-side seeding outside lock+atomic (audit finding 1)` |
| Mutations use `canonicalRoot` | `project/instance-context.ts:canonicalRoot` converges root vs nested `directory`/`worktree` to one identical canonical root; `overlay.ts` and `kilocode/config/config.ts` use it exclusively |
| Shared discovery locks | `KilocodeConfig.configDiscoveryProjectKey(canonicalRoot)` is identical for root and nested contexts; `EffectFlock` serializes concurrent `remove` vs `updateProjectConfig` on same key — proven by `p4-3-canonical-lock-regression.test.ts` (3 tests: key identity, concurrent agent removal vs project config mutation, markdown deletion serialized) |
| Atomic JSONC writes | `kilocode/agent/index.ts` uses `KilocodeAtomicWrite` (jsonc `modify`+`applyEdits` + atomic write) and `KilocodeConfigOverlay.globalTarget()/projectTarget()`; no `Bun.write` direct in agent mutation path |
| Lifecycle ownership | Verified by lifecycle tests: removal helpers acquire `EffectFlock` via `configDiscoveryProjectKey(canonicalRoot)` before scanning/unlinking/writing |

## Instruction `KILO_CONFIG_DIR` Profile Behavior — Separate From Effective Config

Instruction loading in `packages/opencode/src/session/instruction.ts` is not effective `Config.Service` authority. Effective config after cutover ignores `KILO_CONFIG_DIR`/`KILO_CONFIG`/`KILO_PERMISSION`/`KILO_CONFIG_CONTENT`.

- At cutover (2026-08-25) `session/instruction.ts` retained `Flag.KILO_CONFIG_DIR` / `KILO_CONFIG_DIR` / `path.join(global.config, "AGENTS.md")` / `prefer KILO_CONFIG_DIR profile`. See historical `p4-3-cutover.test.ts` `instruction retains KILO_CONFIG_DIR profile fallback; effective config does not`.
- **P4.4-T17 amendment (2026-08-26):** `session/instruction.ts` `KILO_CONFIG_DIR` profile removed — both `Flag.KILO_CONFIG_DIR` reads deleted, now the `KILO_CONFIG_DIR` `AGENTS.md` candidate is removed while `globalFiles` retains the canonical `path.join(global.config, "AGENTS.md")` plus the conditional `path.join(global.home, ".claude", "CLAUDE.md")` candidate, and `relative()` fallback is `global.config` only when `KILO_DISABLE_PROJECT_CONFIG` is set; project instruction discovery when enabled and `KILO_DISABLE_PROJECT_CONFIG` gating preserved. `ConfigPaths` (`Global.Path.config` plus optional `Flag.KILO_CONFIG_DIR`) and TUI (`tui.ts` global → legacy → `KILO_CONFIG_DIR` last wins) remained retained at that baseline per LOCK-001/002.
- **P4.4-T18 amendment (2026-08-26) — Historical at T18; superseded in P4.4-T27/T27-correction (no TUI `KILO_CONFIG_DIR` reader remains):** `packages/opencode/src/config/paths.ts` `KILO_CONFIG_DIR` reader removed — `Flag` import deleted, `directories()` now `unique([Global.Path.config])` only; **historical at T18** `tui.ts` explicitly appended `Flag.KILO_CONFIG_DIR` after global + enabled legacy dirs (`unique([...baseDirectories.filter(dir !== Flag.KILO_CONFIG_DIR), ...legacyProjectDirs, ...(Flag.KILO_CONFIG_DIR ? [Flag.KILO_CONFIG_DIR] : [])])`) preserving exact prior precedence global → legacy → `KILO_CONFIG_DIR` last wins — **superseded in P4.4-T27 (T27-correction removes all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate`/`ancestor`/`.kilocode` readers; current T27-T30 TUI uses canonical `resolveWorktree`/`canonicalRoot` with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only, see P4.4-T30). `Flag.KILO_CONFIG_DIR` getter, `Global.Path.config` override, sandbox deny, and broader P4.4/P4.5 surfaces untouched per LOCK-003/005; historical T18 TUI sole-reader claim superseded.

Verified (post-T17):

| Path | Expectation |
|---|---|
| `session/instruction.ts` | `not.toContain("Flag.KILO_CONFIG_DIR")`, `not.toContain("KILO_CONFIG_DIR")`, `not.toContain("prefer KILO_CONFIG_DIR profile")`, contains `path.join(global.config, "AGENTS.md")` |
| `config/config.ts` (Config.Service) | `not.toContain("Flag.KILO_CONFIG_DIR")`, no `KILO_CONFIG_CONTENT` |
| `config/paths.ts` | `not.toContain("Flag.KILO_CONFIG_DIR")`/`not.toContain("KILO_CONFIG_DIR")`, contains `return unique([Global.Path.config])`, `not.toContain("canonicalRoot")` (Historical at T18 TUI retained `targets: [".kilocode", ".kilo"]` with explicit `Flag.KILO_CONFIG_DIR` last wins — superseded T27; current T27-T30 TUI uses canonical `resolveWorktree`/`canonicalRoot` with no `KILO_CONFIG_DIR` reader) |
| `packages/core/src/global.ts` / `Flag` / `sandbox/policy.ts` | Retains `Flag.KILO_CONFIG_DIR` getter, `Global.Path.config` `Flag.KILO_CONFIG_DIR ?? Path.config` override and sandbox `KILO_CONFIG_DIR` deny-list per LOCK-003 |

This separation is now explicit in `p4-3-cutover.test.ts` tests `instruction no longer retains KILO_CONFIG_DIR profile fallback; effective config does not (P4.4-T17)` and `canonical global/project config and asset loading remain` (amended T18 — `config/paths.ts` no `Flag.KILO_CONFIG_DIR`, **historical at T18** TUI retained explicit append — superseded T27, current T27-T30 no TUI `KILO_CONFIG_DIR` reader) and `packages/opencode/test/kilocode/instruction.test.ts` (P4.4-T17) plus `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` (P4.4-T18).

## Verification

### Test / Verification Commands and Results (authoritative validated run)

| Command / scope | Result |
|---|---|
| `bun test` — core config tests (`packages/opencode/test/config/config.test.ts`, `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts`, `packages/opencode/test/kilocode/config/config.test.ts` suites) | 65 pass |
| `bun test` — `packages/opencode/test/kilocode/config/` suites (including `config-validation.test.ts`, `config.test.ts`, `global-noop.test.ts`) | 104 pass, 2 skipped |
| `bun test` — instruction/loader/R18 suites (`packages/opencode/test/kilocode/config-gitignore.test.ts`, `canonical-loader-side-effect.test.ts`, `permission/r18-service.test.ts`, `permission/r18-blockers.test.ts`) | 30 pass |
| `bun test` — P4.3/agent suites (`packages/opencode/test/kilocode/p4-3-cutover.test.ts`, `p4-3-canonical-lock-regression.test.ts`, `p4-3-overlay-mode.test.ts`, `agent-remove-lock.test.ts`, `agent-remove-mode-untouched.test.ts`, `agent-unlink-visible.test.ts`, `agent-scan.test.ts`) | 31 pass, 251 expects |
| `bun turbo typecheck` | PASS |
| Repository guards | All pass with explicit 60s timeout: `script/check-md-table-padding.ts`, `script/check-architecture-impact.ts --worktree` (PASS; docs updated), `script/extract-source-links.ts` (idempotent), `script/check-opencode-promise-facades.ts`, `script/check-workflows.ts` |
| Architecture-impact | PASS; docs updated (`packages/kilo-docs/pages/contributing/architecture/cli-runtime.md`, `packages/opencode/src/kilocode/skills/kilo-config.md`, `packages/kilo-docs/pages/customize/*`, `specs/vscode-orchestrator/p0-current-state-inventory.md`) |
| TUI/CLI source changes | None — no `packages/opencode/src/cli`, `src/kilocode/tui`, or TUI handlers modified |
| Raw validator output | Preserved in working-tree validation run; formal evidence is the 65 + 104 (2 skips) + 30 + 31 pass, 251 expects gate above |

Post-cutover taxonomy still closed: `p4-3-readiness-inventory.test.ts` 15 P0 sources, 4 retained, 13 removal, no invented class; notification helper `detectOpencodeConfig` states `Kilo no longer reads .opencode` (reference-only detection).

### Focused Cutover Test Anchors (representative assertions)

| Test | Key assertions |
|---|---|
| `p4-3-cutover.test.ts` `effective-config code has no legacy reader anchors` | No legacy global filename readers, TOML, `loadLegacyConfigs`, `loadOrganizationModes`, `.well-known/opencode`, `managedConfigDir`, env flags, `result.tools`/`result.mode`, loadMode, `ConfigPaths.files/directories`, `.kilocode`/`.opencode` in effective config |
| `p4-3-cutover.test.ts` `canonical global/project config and asset loading remain` | `kilo.jsonc`, `.kilo/kilo.jsonc`, `ConfigAgent.load`/`ConfigCommand.load`/`ConfigPlugin.load` present; constants narrowed to `["kilo.jsonc"]` / `[".kilo"]` |
| `p4-3-cutover.test.ts` `no dual-read or import helper surface exists` | No `dualRead`/`DualRead`/`importLegacyConfig`/`MigrationTool` in combined `config/config.ts` + `kilocode/config/config.ts` |
| `p4-3-cutover.test.ts` `instruction no longer retains KILO_CONFIG_DIR profile fallback; effective config does not (P4.4-T17)` (amended T18 — historical, superseded T27) | `session/instruction.ts` has no `Flag.KILO_CONFIG_DIR`/`KILO_CONFIG_DIR`/`prefer KILO_CONFIG_DIR profile` and retains `path.join(global.config, "AGENTS.md")` plus conditional `path.join(global.home, ".claude", "CLAUDE.md")`; `config/config.ts` has no `Flag.KILO_CONFIG_DIR`/`KILO_CONFIG_CONTENT`; `config/paths.ts` has no `Flag.KILO_CONFIG_DIR`/`KILO_CONFIG_DIR` and returns `unique([Global.Path.config])`; **historical at T18** TUI retained `targets: [".kilocode", ".kilo"]` with explicit `Flag.KILO_CONFIG_DIR` last wins (`...(Flag.KILO_CONFIG_DIR ? [Flag.KILO_CONFIG_DIR] : [])`) — **superseded T27** (current T27-T30 TUI has no `KILO_CONFIG_DIR` reader, canonical `resolveWorktree`/`canonicalRoot` only); `Global.Path.config` override and sandbox deny-list retained per LOCK-003 |
| `p4-3-cutover.test.ts` `nested workspace-root canonical behavior preserved` | Nested `directory`/`worktree` resolves to single canonical `root/.kilo/kilo.jsonc`; nested `.kilo` ignored; Config.Service same-convergence verified |
| `p4-3-canonical-lock-regression.test.ts` | Shared discovery lock identity and serialization under `EffectFlock` for nested vs root contexts |

## Rules Contract Gap (Explicitly Deferred)

`rules` is a normative canonical typed-asset class registered in the extension registry/spec (`packages/kilo-vscode/src/config/types.ts:169` `ASSET_DIRECTORIES` includes `"rules"`), validated in `packages/kilo-vscode/src/config/validate.ts`, and normative in runtime spec R10 — exactly as `agent`/`command`/`skill`/`tool`/`plugin` are. The current opencode effective snapshot / materialization path (`packages/opencode/src/config/config.ts` + `packages/opencode/src/kilocode/config/config.ts` + `packages/opencode/src/kilocode/config/overlay.ts`) does **not** currently load `rules` assets into its effective `Config.Info`. No `rules` loader/composition operator participates in this path.

- P4.3 does not add a rules loader, composition operator, or runtime subsystem.
- The asset class is preserved in the registry/spec and remains the normative definition.
- Follow-up to define opencode consumption/composition is deferred beyond P4.3.

Evidence: `specs/vscode-orchestrator/evidence/p4.3-rules-contract-gap.md`; referenced in `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md` and `packages/opencode/src/kilocode/skills/kilo-config.md` and `packages/opencode/src/kilocode/docs/rules-migration.md`.

## Explicit P4.4 / P4.5 Remaining-Open Scope

| Phase | Status | Remaining scope (not claimed complete) |
|---|---|---|
| P4.4 | **OPEN** | Per-row inactive/removal evidence for the 13 removal classes (tracker section 7 `Effective-config source removal evidence` table — each row records `source`/`tests`/`docs`/`generated SDK`/`config`/`i18n`/`build/package` plus `Inactive proof`: no read of that source affects effective config because reader is deleted at P4.3 cutover; the legacy `KilocodeConfigSources` diagnostic inventory reader was physically removed in P4.4, so no deferred source inventory remains) and transport narrowing (private transport, SDK/public server surface ceases to be public) and residual legacy machinery removal (12-source merge remnants, convergence machinery narrowing). Tracker gates `[ ] P4.4 source removal proves ...` and `[ ] Each legacy effective-config source ...` and `[ ] Legacy storage reader/writer removal` rows remain unchecked. |
| P4.5 | **OPEN** | Deletion of old CLI/TUI/Console products and public interfaces (`packages/opencode/src/cli`, `src/kilocode/tui`, and TUI handlers). No TUI/CLI source edits in P4.3. Tracker gate `[ ] CLI/TUI/Console products and public interfaces deleted` remains unchecked. |
| P5 | **NOT STARTED** | Startup and selector readiness — not started. |

Do not claim P4.4 inactive/removal proof, P4.5 deletion, P5 readiness, or rules runtime completion. This cutover completes only the atomic deletion boundary; the per-row verification and surface deletions are the next gated work.

## What Changed vs What Is Preserved

| Concern | Change |
|---|---|
| Pre-cutover checklist | Preserved unchanged as historical decision record; its `CUTOVER PENDING — readers NOT deleted` wording is historical only — this artifact supersedes it for completion |
| Normative runtime spec | Not rewritten in this unit (separate docs update already applied: `cli-runtime.md`, `skills/kilo-config.md`, `migration.md`, `rules-migration.md`, `p0-current-state-inventory.md`) |
| New evidence artifact | This file `p4.3-cutover-evidence.md` is the dedicated post-cutover evidence |
| Tracker | Bounded lines updated to mark P4.3 gate complete, keep P4.4/P4.5 open, advance next actions to P4.4 |

## Verification Steps for Reviewer

1. Open `p4.3-pre-cutover-reconciliation-checklist.md` — confirm reconciled 2026-08-24, 15→4/13 mapping, and `manual-reset-rebuild` signature; confirm file unchanged from pre-cutover.
2. Open this file — confirm atomic deletion scope table covers all 13 removal classes and instruction separation.
3. Run: `bun test ./test/kilocode/p4-3-cutover.test.ts ./test/kilocode/p4-3-readiness-inventory.test.ts ./test/kilocode/p4-3-canonical-lock-regression.test.ts` from `packages/opencode/` — expect pass.
4. Run: `bun run script/check-md-table-padding.ts`, `bun run script/check-architecture-impact.ts --worktree`, `bun run script/extract-source-links.ts` from repo root — expect pass/idempotent.
5. `git diff --stat` — confirm only evidence/tracker files changed in this formalization diff; implementation cutover diff is the prior working-tree validation against the working tree based on HEAD `39a706316d` (778 insertions vs 5966 deletions, 6 migrator files deleted).
> **P4.4-T27-correction superseding clarification (2026-08-27):** Independent audit after T27 found `tui.ts` still executed `migrateTuiConfig` (legacy `kilo.json`/`kilo.jsonc` import) and `Flag.KILO_TUI_CONFIG` arbitrary file injection, and `kilocode/tui/config.ts` `projectTarget` still resolved/wrote `.kilocode` or ancestor via `Filesystem.findUp`. T27-correction removes them — no effective TUI reader remains for `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`.kilocode`/ancestor/`migrate`; only `Global.Path.config` override and sandbox deny per compatibility (not effective source). Historical T27 statement that TUI retained `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate` is superseded while historical record preserved.

> **P4.4-T28 superseding clarification (2026-08-27):** Re-audit found `config-console.ts` TUI GET passed only `instance.directory` while PATCH passed `directory` + `worktree` and `KilocodeTuiConfig.get` loaded from `directory` while `projectTarget` wrote from `worktree` — nested requests had read/write mismatch. T28 threads trusted `canonicalRoot(directory, worktree)` through `KilocodeTuiConfig.get({ directory, worktree? })`, `config-console.ts` GET, PATCH, and response reload, with fallback to `directory` when `worktree` absent (`/`), preserving global target and canonical precedence global → direct → `<workspaceRoot>/.kilo` (historical read/write mismatch superseded).

> **P4.4-T29 superseding clarification (2026-08-27):** Final audit found `packages/opencode/src/cli/cmd/tui/thread.ts` initialized `TuiConfig` with `CurrentWorkingDirectory = process.cwd()` (raw `process.cwd()`), while `tui.ts` read directly from `ctx.directory` without worktree resolution — nested Git invocation could therefore load `tui.json[c]` and `.kilo/tui.json[c]` from the nested directory as effective config. T29 resolves/passes the trusted Git worktree root via the minimal existing mechanism (`Bun.spawn` `git rev-parse --show-toplevel` plus `canonicalRoot(directory, worktree)` with `Filesystem.resolve`, non-Git fallback to current directory): `project/instance-context.ts` adds `resolveWorktree(directory)` and `resolveCanonicalRoot(directory)` helpers, `cli/cmd/tui/config/tui.ts` canonicalizes `loadState` via `resolveWorktree`/`canonicalRoot` and uses `root` consistently for `workspaceKiloDir` (`path.join(root, ".kilo")`), direct `ConfigPaths.fileInDirectory(root, "tui")`, `.kilo` `ConfigPaths.fileInDirectory(root/.kilo)`, `pluginScope` and `fileScope` (plugin install path and source metadata), `cli/cmd/tui/thread.ts` resolves `canonicalCwd = canonicalRoot(cwd, await resolveWorktree(cwd))` and provides it as `CurrentWorkingDirectory` for `TuiConfig.get`, plus uses `canonicalCwd` for `KiloTuiThreadDaemon.attach`, `checkUpgrade`, `validateSession` and `start`. Historical nested-as-root and legacy-discovery last-wins claims superseded while provenance preserved. No P4.4 completion claimed.

> **P4.4-T30 superseding clarification (2026-08-27):** Final audit found `packages/opencode/src/kilocode/cli/cmd/tui/thread.ts` `KiloTuiThreadDaemon.attach()` still called `TuiConfig.get()` with raw `process.cwd()` and explicit `packages/opencode/src/cli/cmd/tui/attach.ts` did the same after `chdir` — nested Git directories could become effective TUI config roots on active attach lifecycles. T30 threads the existing canonical root to `TuiConfig` in `KiloTuiThreadDaemon.attach` and resolves/provides canonical root in explicit `AttachCommand` after `chdir` using the existing `resolveWorktree`/`canonicalRoot` with non-Git fallback, preserving canonical global→root direct→root `.kilo` precedence and no ancestor/`.kilocode`/env/migration source. `ConfigPaths.files` API is retained (`files`/`fileInDirectory` remain for other consumers; only active TUI call-site `ConfigPaths.files("tui", ctx.directory)` removed in T27 and no active TUI call remains). Supersedes historical T18/T19 claims that TUI was the sole `KILO_CONFIG_DIR` reader — no TUI `KILO_CONFIG_DIR` reader remains after T27-correction; only `Global.Path.config` override and sandbox deny. Historical attach raw-cwd and T18/T19 sole-reader / `files` API removal claims preserved with this additive superseding clarification. No P4.4 completion claimed.

> **Final-audit correction (2026-08-27, P4.4 — remote attach local canonical isolation, non-destructive test isolation, reload coverage):** Audit found `attach.ts` derived canonical local TUI config root from remote `--dir` when `chdir` failed, and new canonical TUI suites unlinked `${Global.Path.config}/tui.json[c]` without restoring user content, and nested config-console suite omitted POST reload. Correction preserves distinct local configuration base captured before attempting remote directory `chdir` (`const localBase = Filesystem.resolve(process.cwd())` before `process.chdir(args.dir)`; `resolveWorktree(localBase)` / `canonicalRoot(localBase, worktree)` only for config, `directory` (remote path or `process.cwd()` after chdir) remains session-context for `validateSession`/SDK), proves unavailable remote `--dir` cannot displace local canonical root via `p4-4-tui-attach-canonical.test.ts` remote-displacement test; replaces destructive global-config cleanup with isolated config root (`fs.mkdtemp` → `Global.Path.config = tmp` → `fs.rm(tmp)` only for test-created dirs, using owned temp-dir lifecycle; safe content/existence restoration sufficient as original user file metadata (perms/mtime) is never touched and isolation removes the need for metadata replay); updates T19/P4.3/matrix/tracker historical claims to superseded and states T27-T30 current behavior (canonical `resolveWorktree`/`canonicalRoot`, `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only, global → direct → `.kilo`, non-Git fallback to directory); adds nested PATCH → POST `/instance/reload` → GET coverage in default `Server.Default` app and production `Server.listen` path with owned `Listener` + `tmpdir`/`disposeAllInstances` cleanup. No source fallback, no provider/theme/global/sandbox change, no historical deletion, no untracked resources.

> **P4.4-T13-T17/T19/T24 historical snapshot clarification and plugin-origin scope fix (2026-08-27, LOCK-SOURCE/LOCK-R6):** Dated T13-T17, T24, and T19 passages herein stating legacy discovery, `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG` last-wins, or `migrateTuiConfig`/`.kilocode` migration as current active TUI behavior are **historical snapshots at those dates**, explicitly superseded by T27-T30 current canonical behavior: canonical global -> root direct -> root `.kilo` only (`resolveWorktree`/`canonicalRoot`, `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")`, no ancestor walk, no `.kilocode`, no `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig` readers, LOCK-SOURCE, LOCK-R6). Migration helper `cli/cmd/tui/config/tui-migrate.ts` is **physical/dead residue with no active TUI caller after P4.4-T27**. Plugin origin assembly now uses explicit source identity at merge time: global entries are explicitly `scope: "global"` regardless of `Global.Path.config` location (even when beneath worktree), direct/`.kilo` remain `scope: "local"`; source path, plugin dir derivation, dedup/overrides and precedence preserved. P4.4 remains Active.

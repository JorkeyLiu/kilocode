# Private Request-Schema Draft — Prospective Transport Prerequisite

> **Status: PROSPECTIVE DRAFT — documentation only**, with one narrow B1 exception.
> This document describes a future private request protocol sufficient to guide
> implementation review. It does not define production behavior, report parity,
> authorize a cutover, or make any current SDK/HTTP/SSE bridge removable.
> **B1 amendment (2026-08-30):** `session/cancelQueued` over `kilo serve` fd3/fd4
> into the same `AppRuntime`/`CancelQueuedDispatch` is now implemented as an
> SDK-authoritative parity observation only
> ([doc: p4.4-g3-b1-private-cancel-queued-carrier.md](p4.4-g3-b1-private-cancel-queued-carrier.md)).
> G3 remains Active and the SDK HTTP/SSE bridge remains authoritative. All other
> provider/session/config families remain prospective and deferred per §4.4.

## 1. Purpose and boundary

The current transport inventory records that the private worker is an
observation side path and that the extension still relies on the CLI server,
SDK client, and SSE adapter for retained request semantics. This draft fills
only the missing documentation prerequisite: a bounded vocabulary and contract
for a future private request path.

The associated migration work remains Active/residual. This draft does not
complete that phase or its transport gate. The current SDK HTTP/SSE bridge and
all documented bridge responsibilities remain retained until the deletion gates
in §10 are met.

The draft covers the request and mutation classes that must be implemented and
verified before a transport cutover can be considered:

- provider records, provider backend actions, credential checks, and custom
  model discovery;
- session creation, reads, message hydration, prompt submission, lifecycle
  mutations, cancellation, and child-session routing;
- canonical configuration reads, selector materialization, and configuration
  mutations;
- success, failure, cancellation, acknowledgement, retry, reconnect, and
  lifecycle-convergence envelopes shared by those classes.

It does not add a second configuration authority, a second session store, a
credential store in the worker, a compatibility reader, or a replacement for
the existing observation protocol. Observation remains a distinct snapshot /
revision / changefeed surface. The schemas below are wire-shape proposals, not
TypeScript declarations or an implementation commitment.

## 2. Current-state constraints that the draft must preserve

The following are constraints derived from the current inventory and direction
documents, not claims that the draft has been implemented.

| Constraint | Evidence and consequence for this draft |
|---|---|
| The CLI request bridge remains production-required | [Transport bridge inventory](p4.4-g3-transport-bridge-inventory.md) §2 records SDK client construction, server startup/port discovery, SSE delivery, and the retained `KiloProvider` request classes as retained. The proposed protocol cannot be described as their replacement. |
| Canonical selector authority is service-owned | The inventory §3 records `CanonicalConfigService` as authoritative for canonical provider, agent, and configuration selector publication and canonical mutations. Private requests must not create a competing selector/config authority. |
| The private worker currently provides observation only (B1 narrow exception) | The inventory §4 cites `observation/snapshot`, `observation/read`, `observation/ack`, `observation/subscribe`, and `observation/changed`. Before B1, no provider/session/config request handler existed. As of B1, only `session/cancelQueued` over `kilo serve` fd3/fd4 into the same `AppRuntime` dispatch is implemented, SDK-authoritative and parity-only ([doc: p4.4-g3-b1-private-cancel-queued-carrier.md](p4.4-g3-b1-private-cancel-queued-carrier.md)); all other provider/session/config/private-query families remain absent and prospective. |
| Credentials are extension-host-owned | The runtime direction §3/§3.1 assigns credentials to VS Code `SecretStorage`, with opaque references in canonical files. A worker request must never turn the worker or webview into a credential store. |
| Runtime facts are not client-authored | The runtime direction §7.1 makes the runtime authoritative for session existence, lifecycle, message ordering, revisions, and operation outcomes. Clients may observe and request; they may not invent or revise those facts. |
| Config versions are immutable and generation-pinned | The runtime direction §5/§5.1 requires atomic version creation, validation before commit, no active-generation interruption, and version-scoped resource ownership. Config mutation acknowledgements must express those semantics. |
| Current health absence is narrower than transport absence | The inventory §5 records that `/global/health` removal preserves the request bridge and SSE heartbeat/reconnect. No health-absence result is a private request parity result. |

## 3. Carrier and version envelope

The future path may continue to use the existing private JSON-RPC 2.0 stdio
carrier and Content-Length framing. That carrier is intentionally generic; the
semantic contract is the versioned envelope below. Existing `initialize`,
`ping`, `echo`, and observation methods are not silently redefined by this
draft.

### 3.1 Initialization

The worker and extension negotiate the semantic protocol before any domain
request. A worker must reject an unsupported major version and must not accept a
domain request before a successful handshake.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "initialize",
  "params": {
    "protocol": { "name": "kilo-private", "major": 1, "minor": 0 },
    "client": { "name": "kilo-vscode", "version": "<extension-version>" },
    "capabilities": ["provider", "session", "config"]
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "protocol": { "name": "kilo-private", "major": 1, "minor": 0 },
    "worker": { "name": "kilo-private-worker", "version": "<worker-version>" },
    "capabilities": {
      "provider": ["read", "mutate", "discover-models"],
      "session": ["read", "mutate", "prompt", "cancel"],
      "config": ["read", "mutate"]
    }
  }
}
```

Minor additions must be capability-advertised. A missing capability is a
bounded unsupported operation, not permission to fall back to a different
semantic request. Major-version mismatch is a protocol failure and includes no
domain side effect.

### 3.2 Common request envelope

Every domain request uses a stable operation identity and explicit routing
context. `requestId` identifies one delivery attempt; `opId` identifies the
accepted semantic operation and remains stable across a safe retry or
re-observation.

```json
{
  "v": 1,
  "requestId": "req_<uuid>",
  "opId": "prompt:<message-id>",
  "op": "session/prompt",
  "context": {
    "directory": "/absolute/workspace/path",
    "sessionId": "ses_<id>",
    "parentSessionId": "ses_<id-or-null>",
    "configVersion": 42,
    "sessionRevision": 17
  },
  "idempotencyKey": "idem_<uuid>",
  "payload": {}
}
```

Rules:

1. `v` is the domain-envelope major version. Unknown major versions fail
   before dispatch; unknown fields are ignored only where the operation says
   they are extension-safe.
2. `requestId` is unique for the request delivery. It is not a session ID,
   message ID, config version, or retry identity.
3. `opId` is required for a semantic mutation or generation operation. Reads
   may omit it or use a request-scoped read identity. The operation identity
   must conform to the runtime operation identity rules once the runtime
   implementation is wired.
4. `idempotencyKey` is required for mutations and prompt submission. Reusing
   it with different operation facts is a protocol error. An exact duplicate
   may return the previously recorded outcome without dispatching a second
   side effect.
5. `context.directory` is an absolute, normalized directory identity selected
   by the extension. The worker must reject an absent, relative, or malformed
   directory rather than infer a current working directory.
6. `sessionId` is required for session-scoped operations. A child operation
   carries both the child session and, where relevant, the parent session; the
   worker resolves the directory from the explicit session binding and rejects
   a conflicting directory/session pair.
7. `configVersion` and `sessionRevision` are optimistic freshness guards, not
   client-authored replacements. A stale guard produces a typed stale result;
   it never silently applies against a newer identity.
8. Secret values, access tokens, authorization headers, and serialized SDK
   auth objects are not valid values in `payload`, `context`, or diagnostic
   data. Credential references and bounded credential intents are handled by
   §7.

### 3.3 Common result and event envelopes

JSON-RPC transport success/error remains the carrier-level result. The domain
result is always one of these explicitly discriminated outcomes so a client
can distinguish a successful operation, a rejected operation, cancellation,
and an unknown delivery state.

```json
{
  "v": 1,
  "requestId": "req_<uuid>",
  "opId": "<operation-id>",
  "status": "succeeded",
  "accepted": true,
  "outcome": {
    "type": "succeeded",
    "time": 1730000000000
  },
  "revision": { "session": 18, "config": 42 },
  "data": {}
}
```

The other terminal status shapes are:

```json
{
  "v": 1,
  "requestId": "req_<uuid>",
  "opId": "<operation-id>",
  "status": "failed",
  "accepted": true,
  "outcome": {
    "type": "failed",
    "time": 1730000000000,
    "failure": {
      "code": "provider.unavailable",
      "message": "Redacted bounded message",
      "retryable": true
    }
  }
}
```

```json
{
  "v": 1,
  "requestId": "req_<uuid>",
  "opId": "<operation-id>",
  "status": "cancelled",
  "accepted": true,
  "outcome": {
    "type": "abandoned",
    "time": 1730000000000,
    "cancelledBy": "user_stop"
  }
}
```

`cancelledBy` is a closed initial set: `user_stop`, `steering`, `timeout`,
`network_disconnect`, and `unknown`. A request that was not accepted has
`accepted: false` and no semantic side effect. If delivery is lost after
acceptance and before a result, the client receives an `unknown` delivery
state, not a fabricated failure or success; it must re-observe by `opId`.

Failure data is runtime-normalized and redacted before persistence, projection,
or transport. The draft does not freeze a complete failure taxonomy. At
minimum, failure data has a stable code, bounded message, occurrence time,
retryability decision, and optional bounded diagnostic reference; stack traces,
headers, prompts, and credential material are never panel-visible.

Notifications are revisioned runtime facts, not implicit request responses:

```json
{
  "jsonrpc": "2.0",
  "method": "runtime/changed",
  "params": {
    "v": 1,
    "scope": { "directory": "/absolute/workspace/path", "sessionId": "ses_<id>" },
    "revision": 18,
    "cursor": 204,
    "events": [{ "type": "message.updated", "id": "msg_<id>" }]
  }
}
```

The event payload is bounded and may identify changed records without carrying
full transcript or credential payloads. Snapshot and cursor hydration remain
the authority after a gap, reconnect, or restart; a client never fabricates a
missing event from a request result.

## 4. Operation namespace and routing rules

Names are prospective and intentionally map to current responsibilities. They
do not indicate that a corresponding private handler exists today.

### 4.1 Provider operations

| Operation | Kind | Required context/payload | Result and acknowledgement |
|---|---|---|---|
| `provider/list` | Read | Directory; optional canonical config version | Provider records, connected state, default selection, and an index revision. In canonical mode, selector publication remains derived from canonical service state. |
| `provider/get` | Read | Directory, provider ID | One user-defined provider record or typed not-found. No preset catalog or organization source is implied. |
| `provider/connect` | Mutation | Provider ID, canonical mutation stamp, credential intent or credential ref, provider metadata allowed by the canonical schema | Accepted mutation acknowledgement includes committed config version and provenance; provider readiness is a separate outcome, not a fake config success. |
| `provider/disconnect` | Mutation | Provider ID, canonical mutation stamp, idempotency key | Acknowledgement includes whether the canonical record changed and the resulting config version. |
| `provider/save-custom` | Mutation | Provider ID, canonical provider record without plaintext credential, credential intent/ref as applicable | Validates before commit; success returns the committed version and redacted record projection. |
| `provider/delete-custom` | Mutation | Provider ID, canonical mutation stamp, idempotency key | Success is acknowledged only after the canonical deletion is committed. |
| `provider/authorize` / `provider/oauth-complete` | Mutation | Provider ID, method, bounded OAuth state/code intent; no access token in client payload | The extension-owned credential boundary stores the resulting secret; the wire receives only a redacted outcome and opaque reference metadata. |
| `provider/credential-status` | Read | Provider ID, canonical config version | `available`/`missing`/`invalid` status only. It never returns the credential value. |
| `provider/discover-models` | Read/action | Provider ID or endpoint identity, canonical stamp, credential intent if required | Model records or a typed failure. Discovery does not mutate provider config unless a separately acknowledged mutation is requested. |

Provider listing must not reintroduce preset identities, preset catalogs,
organization config, or cloud onboarding. Generic protocol adapters remain
implementation details of a user-defined provider.

### 4.2 Session operations

| Operation | Kind | Required context/payload | Result and acknowledgement |
|---|---|---|---|
| `session/create` | Mutation | Directory, optional agent/provider/model selection, config version, metadata | Returns the runtime-created session identity, initial session revision, and directory binding. The client does not choose a conflicting session ID. |
| `session/get` | Read | Directory and session ID | Session metadata plus its authoritative revision and lifecycle state. |
| `session/status` | Read | Directory; optional session set | Status records keyed by session ID, each carrying occurrence time and revision. |
| `session/messages` | Read | Directory, session ID, page cursor/limit, optional `before` | Ordered message page with cursor and revision. A stale cursor requests rehydration rather than fabricated deltas. |
| `session/children` | Read | Directory and parent session ID | Child-session identities and parent bindings. |
| `session/prompt` | Mutation/generation | Directory, session ID, config version, message/op identity, prompt payload, idempotency key | First acknowledgement says whether the semantic operation was accepted. Later outcome/events report running, succeeded, failed, abandoned, or crash disposition. Accepted prompts are not client-replayed. |
| `session/abort` | Mutation | Directory, session ID, cancellation reason, idempotency key | Acknowledges one session cancellation request (receipt, not cancellation proof) and eventually reports its aggregated authoritative outcome after runtime/SSE/persistence convergence. `opId` is the cancellation-request identity per §3.2 (not a single-generation identity) bound to directory + sessionId per §8; the abort scope is the session cancellation scope including its aggregated generation, queued-work, descendant, and event-publication impact per §6, not HTTP status alone. An already terminal cancellation scope is an idempotent no-op returning its existing outcome; missing session is typed `session.not_found`; cancel miss is `not_affected`. An existing generation operation identity, if cited, is an outcome association only and never the request identity. B9 evidence-only: current `session.abort` remains volatile `200 true` with no operation identity, outcome aggregation, or scope validation; no private abort capability exists; Gate B remains open. |
| `session/revert` | Mutation | Directory, session ID, message/part checkpoint identity, config version, idempotency key | Returns the newly committed session revision and checkpoint result. It does not mutate an old revision in place. |
| `session/unrevert` | Mutation | Directory, session ID, config version, idempotency key | Returns the newly committed session revision and restored result. |
| `session/delete` | Mutation | Directory, session ID, idempotency key | Acknowledges only after runtime deletion/tombstone ordering is committed; subsequent events identify the deleted session without requiring the client to retain facts. |
| `session/fork` | Mutation | Directory (target), source `sessionID` + optional `messageID` checkpoint (`ForkInput` `session/session.ts:275` `sessionID: SessionID, messageID?: MessageID`), `configVersion`/`sessionRevision` guards, `idempotencyKey`/`opId` per §3.2, prospective explicit target-directory routing required per §8 (current `forkTargetDirectory` `kilocode/server/routes/fork-routing.ts:8` is path-dependent — `workspace-routing.ts:178-186` may resolve via workspace before reaching `forkTargetDirectory`; `Session.fork` `session/session.ts:878`) — consumer `KiloProvider:76` `fork-session.ts:37` `AgentManagerProvider:609` `session.fork` | Creates new session copying transcript up to checkpoint; title derived `"<base> (fork #N)"`, `model`/`variant` pinned at fork point, `sandboxFallback` carried from source directory; returns runtime-created `Session.Info` with new revision/directory binding; stale/`not-found`/`scope_mismatch` map to §6 `rejected`/`failed` per §3.3 envelope; follows §5 two-stage `accepted` then terminal `outcome`. |
| `session/update` (rename) | Mutation | Directory, `sessionID`, `title` via `UpdatePayload` `session.ts:49` (`title?: string`) validated by `parseSessionTitle` `kilo-provider/rename-session.ts:13`, `configVersion`/`sessionRevision` guards, `idempotencyKey`/`opId` per §3.2 — consumer `kilo-provider/rename-session.ts:13` `session.update` | Returns committed `Session.Info` with updated title and new revision; validation/stale/missing-title map to typed `rejected`/`failed` per §6 and §3.3 `failure` (redacted, `retryable`); acknowledgement follows §5 immediate commit without follower fence; no directory/session identity creation. |
| `session/cancelQueued` | Mutation (drain-control) | Directory, `sessionID`, `messageID` of queued slot (`DELETE /session/:sessionID/queue/:messageID` `session.ts:113` / `openapi session.cancelQueued`), `idempotencyKey`/`opId` per §3.2; classified by `classifyDrainControl` `drain-control.ts:105` — consumer `KiloProvider.ts:4847` `session.cancelQueued` | Removes only not-yet-started queued slot and its persisted message (`KiloSessionPromptQueue.cancelOne` + `session.removeMessage` `handlers/session.ts:398`); running slot is never interrupted; returns `boolean` `whether cancelled`; remains available during cold-config convergence fence per §4.3/§5.1 and drain-control lane `drain-control.ts:43` (§6 `InstanceUnavailableDuringConfigRebuild` 409 when no snapshot); typed `not-found`/`rejected` map to §6. |

`session/command` is not a distinct lifecycle mutation; it is a `session/prompt` variant that carries a slash-command envelope (`command`, `arguments`, optional `messageID`, `model`/`agent`/`variant`/`parts`) per `SessionPrompt.CommandInput` `session/prompt.ts:2367` and consumer `KiloProvider.ts:4734` `session.command`. It reuses `session/prompt`'s §3.2 versioned envelope (`v`, `requestId`, `opId`, `idempotencyKey`, `context.directory`/`sessionId`/`configVersion`/`sessionRevision`), §5 two-stage acknowledgement, §6 failure/cancellation envelope, and §8 directory/session routing and idempotency; no separate operation identity, versioned operation, or lifecycle contract is introduced beyond the prompt variant.

Prompt streaming is event delivery associated with the accepted `opId`, not a
series of independently replayable prompt requests. A stream can be resumed by
observation/hydration or marked ambiguous/abandoned by the runtime after a
worker crash; the extension must not silently submit the prompt again.

### 4.3 Configuration operations

| Operation | Kind | Required context/payload | Result and acknowledgement |
|---|---|---|---|
| `config/snapshot` | Read | Directory, optional requested version | Immutable effective snapshot metadata, schema version, config version, provenance, and selector indexes. The snapshot is derived, not a second persisted authority. |
| `config/selectors` | Read | Directory, optional index revision | Provider/model/agent selector indexes from canonical materialization. It is safe to render before worker readiness and does not imply runtime action readiness. |
| `config/read` | Read | Scope (`global`/`project`/`effective`), directory, optional version | Canonical file/asset projection with provenance. Secret values are excluded. |
| `config/write` | Mutation | Scope, field/asset patch expressed in schema vocabulary, base content/version stamp, idempotency key | Validation result, conflict details or committed immutable config version. Stale drafts fail visibly; no silent overwrite. |
| `config/unset` | Mutation | Scope, canonical field/asset identity, base stamp, idempotency key | Schema-defined unset/default result and new version when committed. |
| `config/validate` | Read/action | Candidate canonical records/assets, never legacy overlays | Validation issues with field/file provenance. Validation alone does not commit or alter runtime state. |
| `config/reconcile` | Internal action/event | Canonical file stamp and observed change | New valid version or explicit invalid/stale state. Invalid input preserves the prior valid version and never falls back to legacy values. |

Canonical configuration writes are routed through `CanonicalConfigService` (or
its future private-runtime integration) and must preserve its ownership of
canonical selectors and mutations. The private transport is a carrier, not a
new source class. A cold mutation validates, persists, and acknowledges
immediately after the backend transaction and synchronous side effects. It never
waits for active-generation drain and never interrupts active generations:
affected directories receive a generation-admission fence before persistence,
in-flight generations keep the config snapshot they started with, and new
generations and readers wait until the latest committed-version convergence
lands. Later writes, write-intent operations, and drain-control operations
remain available during convergence and do not queue behind an earlier
convergence. Cold obligations coalesce and are versioned: the pass drains
readers and write/control leases, disposes the exact pre-fence identities,
boots the latest disk state, and releases the fence only after the latest
committed version converges. Reload/load during a fence and runtime shutdown
ownership follow the same fenced model.

## 4.4 Retained-path coverage matrix (inventory-tied, explicit)

This matrix is the only coverage statement for Gate B. Broad wording elsewhere
in this draft does not constitute coverage. This matrix covers the enumerated
inventory in `p4.4-g3-transport-bridge-inventory.md` and the bounded audit scan
of SDK-backed routes cited in this draft; it does not claim exhaustive discovery
of every SDK call or to enumerate every known consumer. Status `named` means the
draft defines a prospective operation and parity expectation; `deferred` means
the class is retained but has no schema/contract in this draft and is an explicit
Gate B blocker. No deferred row is implicitly satisfied by generic wording. Any
SDK-backed route discovered beyond the rows below is an explicit Gate B blocker
and a required matrix addition before Gate B can be met.

| Retained class | Inventory / consumer evidence | Draft operation (prospective) | Coverage status |
|---|---|---|---|
| `session/create` | inventory §2 `KiloProvider.ts:2470` | `session/create` §4.2 | named — Gate B open |
| `session/get` | inventory §2 `KiloProvider.ts:2503` | `session/get` §4.2 | named — Gate B open |
| `session/status` | inventory §2 + `session/status` via `KiloProvider`/`AgentManagerProvider` | `session/status` §4.2 | named — Gate B open |
| `session/messages` | inventory §2 `KiloProvider.ts:2585` | `session/messages` §4.2 | named — Gate B open |
| `session/children` (child sync) | inventory §2 `KiloProvider.ts:2650` + `AgentManagerProvider` children | `session/children` §4.2 | named — Gate B open |
| `session/prompt` (promptAsync) | inventory §2 `KiloProvider.ts:4634` + `AgentManagerProvider:545` | `session/prompt` §4.2 | named — Gate B open |
| `session/abort` | `packages/kilo-vscode/src/kilo-provider/abort.ts:63-65` `session.abort` | `session/abort` §4.2 (B9: `opId` = cancellation-request identity, receipt ≠ terminal convergence, typed `session.not_found`/terminal no-op/`not_affected`, per-target dispositions, terminal outcome aggregates generation/queued-work/descendant/event-publication disposition) | named — Gate B open (B9 evidence-only; current volatile `200 true` gap; no private abort capability) |
| `session/revert` | `KiloProvider.ts:4811` `session.revert` | `session/revert` §4.2 | named — Gate B open |
| `session/unrevert` | `KiloProvider.ts:4826` `session.unrevert` | `session/unrevert` §4.2 | named — Gate B open |
| `session/delete` | `KiloProvider.ts:2855` `session.delete` | `session/delete` §4.2 | named — Gate B open |
| `session/command` (slash command) | `KiloProvider.ts:4734` `session.command` (`SessionPrompt.CommandInput` `session/prompt.ts:2367`) | `session/prompt` variant — command envelope (§4.2) | named — Gate B open (variant, no dedicated lifecycle operation) |
| `session/fork` | `KiloProvider:76` `fork-session.ts:37` `AgentManagerProvider:609` `session.fork` (`Session.ForkInput` `session/session.ts:275`) | `session/fork` §4.2 | named — Gate B open |
| `session/update` (rename) | `kilo-provider/rename-session.ts:13` `session.update` (`UpdatePayload` `session.ts:49`) | `session/update` §4.2 | named — Gate B open |
| `session/cancelQueued` | `KiloProvider.ts:4847` `session.cancelQueued` (`DELETE /session/:sessionID/queue/:messageID` `session.ts:113`) | `session/cancelQueued` §4.2 | named — Gate B open (drain-control, available during convergence fence) |
| `session/share` / export | `kilo-provider/export-transcript.ts` `session.get` + share/next | not defined | deferred — Gate B blocking |
| `sessionModelUsage` (session-usage/image-model metering) | `KiloProvider.ts:2543` `kilocode.sessionModelUsage` | not defined | deferred — Gate B blocking |
| provider `list` / `get` | inventory §2 `KiloProvider.ts:2931` + `AgentManagerProvider:816` | `provider/list`, `provider/get` §4.1 | named — Gate B open |
| provider backend/auth (`provider.auth`, `auth.set/remove`, `oauth`) | inventory §2 `KiloProvider.ts:3018` + `provider-actions.ts` `auth.*` / `provider.oauth.*` | `provider/connect`, `provider/authorize` §4.1 | named (narrow) — Gate B open, full backend semantics deferred |
| `customProvider/save` / `delete` | `provider-actions.ts:428,486` `customProvider.save/delete` | `provider/save-custom`, `provider/delete-custom` §4.1 | named — Gate B open |
| `provider/credential-status` | `KiloProvider.ts:3092` credential read | `provider/credential-status` §4.1 | named — Gate B open |
| `provider/discover-models` | `KiloProvider.ts:3168` custom model discovery | `provider/discover-models` §4.1 | named — Gate B open |
| image-model | inventory §2 additional + `kilocode.sessionModelUsage` / custom model discovery | not defined | deferred — Gate B blocking |
| `config/get` + `global.config.get` | inventory §2 `KiloProvider.ts:3640,3692` `config.get` / `global.config.get` | `config/read`, `config/snapshot` §4.3 | named — Gate B open |
| `config/overlay` | `KiloProvider.ts:3670,4286` `config.overlay` | not defined — overlay as derived snapshot | deferred — Gate B blocking |
| `config/warnings` (diagnostics) | `KiloProvider.ts:3753` `config.warnings` | not defined | deferred — Gate B blocking |
| `config/transaction` (write/unset atomic) | `KiloProvider.ts:4105` `config.transaction` | `config/write`, `config/unset` §4.3 | named (partial) — Gate B open, transaction batch semantics deferred |
| `sandbox` config (sandbox-session) | inventory §2 additional `shared/sandbox-session.ts:20` `config.get` sandbox default | not defined | deferred — Gate B blocking |
| `mcp/status` / `connect` / `disconnect` / `auth` / `add` | inventory §2 additional `AgentManagerProvider` `mcp.status` / `KiloProvider` `mcp-oauth` / `browser-automation-service` `mcp.add` | not defined | deferred — Gate B blocking |
| `command/list` (commands) | `kilo-provider/commands.ts:14` `command.list` | not defined — distinct from `session/command` | deferred — Gate B blocking |
| `skill` (removeSkill) | `KiloProvider.ts:3272` `kilocode.removeSkill` | not defined | deferred — Gate B blocking |
| `agent` (removeAgent) | `KiloProvider.ts:3350` `kilocode.removeAgent` | not defined | deferred — Gate B blocking |
| `notebook/list` | `KiloProvider.ts:access` `kilocode.notebook.list` | not defined | deferred — Gate B blocking |
| `find/files` | `kilo-provider/file-search.ts:36` `find.files` | not defined | deferred — Gate B blocking |
| `project/git-status` | `kilo-provider/git-status.ts:4` `project` | not defined | deferred — Gate B blocking |
| `permission` (list/save/reply) | `KiloProvider/handlers/permission-handler.ts` `permission.*` + `commands/toggle-auto-approve` | not defined | deferred — Gate B blocking (uses §6 provenance expectation only) |
| `question` (list/reply/reject) | `kilo-provider/handlers/question.ts` `question.*` | not defined | deferred — Gate B blocking |
| `suggestion` (list/accept/dismiss) | `kilo-provider/handlers/suggestion.ts` `suggestion.*` | not defined | deferred — Gate B blocking |
| `kilo/profile` / `kilo/organization` / `kilo/marketplace` (marketplace) | inventory §2 additional + `kilo-provider/handlers/auth.ts` `kilo.profile` / `services/marketplace/actions.ts` | not defined | deferred — Gate B blocking |
| `backgroundProcess` | `kilo-provider/background-process.ts:9` `backgroundProcess` | not defined | deferred — Gate B blocking |
| `experimental/session/list` | `KiloProvider.ts:2699` `experimental.session.list` (`GET /experimental/session` with `SessionListQuery`, handler `limit + 1`/cursor, SDK `client.experimental.session.list`, consumer `sessionRefreshContext.listSessions`) | `experimental/session/list` §4.2 read-family/§4.6 (strict v1 envelope, routing-only directory/workspace, safe summary projection, redacted failure, detached parity helper) | named (contract-only) — Gate B open, no parity/cutover |
| `enhancePrompt.enhance` | `KiloProvider.ts:2172` `sdkClient.enhancePrompt.enhance` (handler `enhance-prompt.ts:13`) | not defined | deferred — Gate B blocking |
| `instance.reload` | `KiloProvider.ts:5060` `extension.ts:634` `client.instance.reload` (endpoint `instance-reload.ts:26`) | not defined | deferred — Gate B blocking |
| `path.get` | `kilo-provider/model-state.ts:62` `client.path.get` (route `instance/httpapi/groups/instance.ts:77`) | not defined | deferred — Gate B blocking |
| `kilocode.agentRequirements` | `kilo-provider/agent-requirements-controller.ts:199` `client.kilocode.agentRequirements` (path `kilocode.ts:100,126`) | not defined | deferred — Gate B blocking |
| `remote.status` | `services/RemoteStatusService.ts:52,63` `client.remote.status` (endpoint `remote.ts:54`) | `remote/status` contract-only candidate §4.5 (strict v1 envelope, routing-only directory/workspace, process-global `{enabled,connected}`, redacted failure, detached parity helper) | named (contract-only) — Gate B open, no parity/cutover |
| `global.config.update` | `provider-actions.ts:236` `kilo-provider/work-style-apply-handler.ts:30` `services/marketplace/actions.ts:121` `client.global.config.update` (openapi `global.config.update`) | not defined | deferred — Gate B blocking |
| `instance.dispose` | `services/marketplace/actions.ts:125` `client.instance.dispose` (route `instance/httpapi/groups/instance.ts:67`) | not defined | deferred — Gate B blocking |
| `pty.create` / `pty.update` / `pty.remove` | `agent-manager/terminal-manager.ts:77` `pty.create` / `:105` `pty.update` / `:127,191` `pty.remove` (openapi `pty.create`/`pty.update`/`pty.remove`) | not defined | deferred — Gate B blocking |
| `pty.list` / `pty.get` / `pty.shells` / `pty.connect` (co-located retained PTY routes) | `sdk` `pty.list`/`pty.get`/`pty.shells`/`pty.connect` — co-located with `pty.create`/`update`/`remove` in `sdk.gen.ts` / `openapi.json` `pty.*`; no extension SDK consumer enumerated in bounded scan | not defined | deferred — Gate B blocking (co-located; no private contract) |

Gate B is not met until every `deferred` row has a versioned operation,
identity/routing/error/cancellation contract, and real-production parity
evidence per §10 Gate B-D. The presence of `KiloProvider` SDK calls beside a
row does not imply that the draft covers that row.

### 4.5 `remote.status` contract-only candidate evidence (2026-09-05, Gate B open)

Contract/evidence only for deferred `remote.status`; no private carrier
implementation, no production parity, no cutover or Gate closure. SDK
`client.remote.status` (`GET /remote/status`) remains the sole authority.
`remote.enable`, `remote.disable`, and `RemoteStatusChanged` event-stream
parity are explicitly out of scope.

| Contract fact | Pointer |
|---|---|
| Strict v1 envelope `remote/status` with absolute routing-only `directory`, optional `workspace`, empty payload, and `idempotencyKey === opId` (`remote-status:<token>`) | `packages/kilo-vscode/src/services/cli-backend/serve-private-remote-status-contract.ts` (`validateRemoteStatusContractRequest`) |
| Routing-only scope: directory/workspace conflicts are `scope_mismatch` on request-routing identity only and say nothing about payload ownership | `serve-private-remote-status-contract.ts` (`checkRemoteStatusScope`) |
| Process-global payload `{enabled,connected}` (both booleans); shape-only validation asserts no directory binding, and cross-directory equality of the two booleans is expected, not a divergence | `serve-private-remote-status-contract.ts` (`validateRemoteStatusPayload`, `isRemoteStatusProcessGlobalField`) |
| Redacted failure shape `{code,message,retryable}` only; raw status/session/directory echo keys rejected | `serve-private-remote-status-contract.ts` (`validateRemoteStatusFailure`) |
| Detached parity helper compares only the two process-global booleans exactly and never the request directory | `serve-private-remote-status-contract.ts` (`compareRemoteStatusParity`) |
| Focused contract tests for envelope, scope, process-global semantics, payload, redacted failure, wire normalization, and detached parity | `packages/kilo-vscode/src/services/cli-backend/serve-private-remote-status-contract.test.ts` (9 pass 56 expects) |

Non-claim: tests prove shape only, not production parity. No private carrier
or capability, no production behavior change, no cutover. Unresolved:
`remote.enable`/`remote.disable`/event-stream semantics, freshness/
observation semantics, and transport behavior; busy/error/timeout/late-SSE
recovery for this path is unobserved. Gate B remains open, G3 Active, Gates
C/D unchanged.

### 4.6 `experimental/session/list` contract-only candidate evidence (2026-09-05, Gate B open)

Contract/evidence only for deferred `experimental/session/list`; no private
carrier implementation, no production parity, no cutover or Gate closure. SDK
`client.experimental.session.list` (`GET /experimental/session`) remains the
sole authority. This operation is distinct from `session/status`,
`session/get`, `session/messages`, `session/children`, and non-experimental
session routes, and from `command/list`. No pagination, ordering, freshness,
deleted/archived lifecycle, or cross-directory semantics are invented; they are
recorded as unknowns below.

| Contract fact | Pointer |
|---|---|
| Strict v1 envelope `experimental/session/list` with absolute routing-only `directory`, optional `workspace`, `filter` payload (`projectID?`, `roots?`, `start?`, `cursor?`, `search?`, `limit?`, `archived?`), and `idempotencyKey === opId` (`experimental-session-list:<token>`) | `packages/kilo-vscode/src/services/cli-backend/serve-private-session-list-contract.ts` (`validateSessionListContractRequest`) |
| Routing-only scope: directory/workspace/request conflicts are `scope_mismatch` on routing identity only and say nothing about payload ownership; cross-directory payload comparison is out of scope | `serve-private-session-list-contract.ts` (`checkSessionListScope`) |
| Safe summary projection `{id,directory,title,updated}` only (`updated` carries `time.updated` as bare number with no freshness claim); all other `GlobalInfo` fields excluded by design | `serve-private-session-list-contract.ts` (`validateSessionListSummary`, `validateSessionListSummaries`) |
| Redacted failure shape `{code,message,retryable}` only; session/payload/cursor-echo keys rejected | `serve-private-session-list-contract.ts` (`validateSessionListFailure`) |
| Detached parity helper compares only shared-id `id`/`directory`/`title` projection; order never compared; length/membership gaps reported as `session-list-membership-unknown`, not match; `updated` shape-only, never compared | `serve-private-session-list-contract.ts` (`compareSessionListParity`) |
| Focused contract tests for opId grammar, v1 routing/filter validation, scope, summary projection, redacted failure, result/wire normalization, and detached parity | `packages/kilo-vscode/src/services/cli-backend/serve-private-session-list-contract.test.ts` (7 pass 49 expects) |

Non-claim: tests prove shape only, not production parity. No private carrier
or capability, no production behavior change, no cutover. Explicit unknowns:
pagination (`limit`/`cursor`/`x-next-cursor` truncation policy), ordering
(production "most recently updated" description not proven here), freshness
(`time.updated` staleness), deleted/archived inclusion/exclusion (`archived`
default-excluded description-only), and cross-directory ownership. Gate B
remains open, G3 Active, Gates C/D unchanged.

## 5. Acknowledgements, ordering, and idempotency

### 5.1 Two-stage mutation acknowledgement

Mutations use a small explicit distinction:

1. **Accepted acknowledgement** — the runtime has accepted the semantic
   operation, assigned/confirmed `opId`, and recorded enough durable state to
   account for it. This does not mean the provider call or generation has
   completed.
2. **Terminal outcome** — the operation is `succeeded`, `failed`, `abandoned`,
   `ambiguous`, or another runtime-approved terminal disposition, with
   occurrence time, redacted failure facts where relevant, and final revision.

Config commits are the exception in timing, not authority: a committed cold
config mutation returns its new immutable config version in the same
acknowledgement without waiting for convergence, but resource convergence and
generation use obey version pinning and fenced latest-version convergence. See
§4.3 for admission-fence, coalescing/versioning, and no-interruption semantics.

`session/cancelQueued` (and `session/abort`) remain drain-control available during that fence via the snapshot-first lane (`drain-control.ts:43,105` `classifyDrainControl` `DELETE /session/:sessionID/queue/:messageID`) and return deterministic `InstanceUnavailableDuringConfigRebuild` 409 when no cached snapshot exists; they operate on the pre-fence runtime, never boot a new instance, never wait for latest-version convergence, and never queue behind an earlier cold write — preserving the established cold-config acknowledgement and convergence contract.

For `session/abort` (B9 prospective, Gate B open): the two-stage acknowledgement applies with `opId` as one session cancellation-request identity bound to directory + sessionId per §§3.2/8; receipt (`accepted`/`accepted-unresolved` transport receipt, non-terminal) is not cancellation proof — the terminal outcome aggregates the actual outcome set per §6 and is confirmed via runtime/SSE/persistence convergence, re-observed by `opId` after `transport-unknown` (§9.1 delivery uncertainty only; `ambiguous` remains §5.1/§6 runtime semantic terminal outcome) per §9.1 with no automatic replay. Missing session maps to typed `session.not_found` (no side effect); an already terminal scope is an idempotent terminal no-op returning its existing outcome; a cancel miss is `not_affected`, never success. Root/descendant/queued/intake/background/followup/event-publication dispositions must be separately observable. SDK HTTP remains the sole abort authority; any future private observation is detached parity only. Current `session.abort` remains volatile `200 true` with no operation identity or outcome aggregation; no private abort capability exists; busy HTTP→SSE/persistence ordering remains explicit unknown.

### 5.2 Ordering and stale results

- Every session mutation advances the session revision in the same runtime
  transaction as its durable state change.
- Every config commit advances the config version atomically with canonical
  file/asset persistence and materialization metadata.
- Events carry their scope and revision. The receiver accepts duplicate events
  idempotently, rejects or rehydrates on gaps, and never applies an older
  revision over a newer one.
- A response includes the revision it observed. The extension drops a response
  that no longer matches its request context and rehydrates from the runtime;
  it does not merge stale response data into current UI state.
- A mutation with an already-used idempotency key must return the original
  accepted/terminal facts, or a typed conflict if the supplied operation facts
  differ. It must not dispatch a second provider call, prompt, deletion, or
  config write.

## 6. Failure and cancellation contract

The semantic failure envelope is deliberately smaller than a complete error
taxonomy. It must provide enough information to distinguish:

| State | Meaning |
|---|---|
| `rejected` | The request was invalid, unauthorized for its scope, stale, unsupported, or not accepted. No semantic side effect occurred. |
| `accepted` / `running` | The operation is runtime-owned and may produce events/outcome later. |
| `succeeded` | The operation completed successfully; result data and final revision are authoritative. |
| `failed` | The operation completed unsuccessfully; normalized redacted failure facts are authoritative. |
| `cancelled` / `abandoned` | The runtime recorded cancellation provenance and no client replay is implied. |
| `ambiguous` | The runtime cannot prove the external side effect after a crash/disconnect; the client must re-observe and must not replay automatically. |
| `superseded` | A newer operation or version owns the resulting state; the prior operation remains recorded. |

Cancellation is a request against a runtime operation, not transport disposal
alone. Closing a panel, losing a connection, or killing a worker may trigger a
runtime disposition, but only the runtime records whether the operation was
cancelled, abandoned, ambiguous, or already succeeded. A client-side abort
controller may stop waiting; it cannot rewrite the outcome.

For `session/abort` (B9 prospective): the abort `opId` identifies one session cancellation request, not a single generation; an existing generation operation identity, if cited, is an affected-set association only and never the request identity; its terminal outcome must account for the actual generation, queued-work, descendant, and event-publication disposition, never HTTP status alone. Missing session is typed `session.not_found` with no side effect; terminal scope is an idempotent no-op returning the existing outcome; cancel miss is `not_affected`, not success. Timeout, epoch change, descriptor mismatch, or private-transport `unknown` never advances client state except by re-observation; diagnostics stay redacted per §3.3.

The private runtime owns recovery policy and retry accounting. Nested provider,
SDK, task, or transport attempts must be visible under the owning `opId` and
consume its budget. The client can ask for cancellation and re-observe, but it
cannot schedule a second semantic operation merely because a response timed
out.

`session/fork`, `session/update`, and `session/cancelQueued` reuse the §3.3 versioned envelope (`v`, `requestId`, `opId`, `status`, `outcome`, `revision`, `failure.code`/`retryable`) and §6 distinctions above: a `session/fork` source-not-found or `scope_mismatch` maps to `rejected`, `session/update` `parseSessionTitle` validation maps to `rejected`, and `session/cancelQueued` missing/already-promoted slot maps to idempotent `succeeded false` rather than `failed`; all persist redacted `failure` facts and never echo secret material. `session/command` inherits the same envelope as `session/prompt` variant per §4.2.

## 7. Credential and authorization boundary

The extension host remains the owner of VS Code `SecretStorage`. The following
boundary is required for any implementation of this draft:

1. Canonical provider records contain only an exact opaque credential reference
   and never plaintext keys, headers, OAuth tokens, or environment-variable
   values.
2. Webview messages and persisted selector/config indexes contain no secret
   value. A credential read returns presence/status only.
3. A request may carry a credential reference, a bounded user-intent marker
   such as `credentialRequested: true`, or a provider identity. It may not carry
   an arbitrary secret-storage key or ask the worker to scan SecretStorage.
4. When a provider operation genuinely needs secret material, the extension
   host validates the exact owned reference and resolves it at the narrowest
   host-to-runtime boundary. The value is ephemeral, excluded from logs,
   outcomes, events, persistence, and error messages, and is not echoed in a
   response. An implementation may replace this with an equivalent scoped
   capability/token, but may not move credential ownership to the worker.
5. OAuth authorization codes, callback state, and user-entered credentials are
   single-operation inputs. They are consumed or rejected by the extension-host
   auth boundary and never become general request metadata.
6. Invalid, missing, stale, or unavailable references fail closed with a
   redacted typed failure. No derived key reconstruction, prefix scan, or
   fallback to a legacy credential source is permitted.

Provider authorization is distinct from config mutation authorization. A
successful secret write must be acknowledged separately from provider
connectivity/model discovery, and a failed later provider action must not erase
the prior valid canonical config without an explicit mutation.

## 8. Directory and session identity

The request router must make identity explicit because current extension paths
share one backend while routing directory-scoped state and child sessions.

```json
{
  "directory": "/absolute/canonical/workspace",
  "sessionId": "ses_child",
  "parentSessionId": "ses_parent",
  "configVersion": 42,
  "sessionRevision": 17
}
```

- `directory` is the canonical workspace/request identity, not the panel title,
  process current directory, or a value inferred from a session ID.
- A session binding is established by `session/create` or a runtime-owned
  child-session record. Every later request supplies or resolves exactly one
  binding; mismatches are rejected as `scope_mismatch`.
- Parent-child requests carry the child as the operation target. The parent is
  context for inheritance and provenance only; a parent request cannot mutate
  a child by omission or by using the parent's directory accidentally.
- `session/abort` (B9 prospective) binds directory + sessionId + cancellation-request `opId` as one scope; a conflicting directory/session pair or unbound cancellation identity is rejected as `scope_mismatch` per §§3.2/6.
- Provider/config reads use the requested directory and config version. A
  session prompt additionally pins the config version used by that generation.
- `session/fork` (prospective) must carry an explicit target `directory` for the new session (never inferred from the source); current `forkTargetDirectory` `kilocode/server/routes/fork-routing.ts:8` is path-dependent — `workspace-routing.ts:178-186` workspace middleware may resolve before `forkTargetDirectory` handling — with `sourceID`/`sourceDirectory`/`sandboxFallback` inheritance per `Session.fork` `session/session.ts:878`; `Session.ForkInput` `session/session.ts:275` is `sessionID: SessionID, messageID?: MessageID`. `session/update` (rename) targets `sessionID` bound to its `directory` via `UpdatePayload` `session.ts:49`. `session/cancelQueued` targets `sessionID` + `messageID` of the queued slot (`session.ts:113`) and is an `opId`/`idempotencyKey` mutation per §3.2/§5. `session/command` reuses `session/prompt` routing. All four share §3.2 versioned envelope and idempotency guard rules.
- Reconnect and panel/session switch must rehydrate by directory plus session
  identity. No UI-held session fact is authoritative across those boundaries.

## 9. Retry, reconnect, and worker lifecycle

### 9.1 Request delivery states

The client tracks delivery separately from semantic outcome:

| Delivery state | Client action |
|---|---|
| `not-sent` | It may send the request once. |
| `sent-unaccepted` | It may retry the same idempotency key only after the transport implementation proves the request was not accepted, or may re-observe if that cannot be proven. |
| `accepted-unresolved` | It must not replay. Reconnect and query the operation by `opId`. |
| `terminal` | It may display the recorded outcome; an exact duplicate returns the same facts. |
| `transport-unknown` | It must treat semantic state as unknown and rehydrate/query; timeout alone is not failure proof. |

Retry policy is runtime-owned. The private client reconnects the carrier and
performs initialize plus observation hydration; it does not replay accepted
prompts, mutations, provider actions, or cancellations. A request retry must
be bounded, carry the same idempotency key, and be rejected if operation facts
or scope differ.

### 9.2 Restart and convergence handshake

After worker restart, transport reconnect, extension reload, panel close/reopen,
or session switch, the extension performs this conceptual sequence:

1. establish and negotiate the private carrier;
2. obtain the runtime identity/epoch and supported capabilities;
3. hydrate canonical selector/config indexes from the extension-owned canonical
   materialization path, independently of worker action readiness;
4. hydrate runtime observations from a snapshot and the last acknowledged
   cursor/revision;
5. read unresolved operation outcomes by `opId`;
6. subscribe to revisioned events and acknowledge the cursor only after the
   derived read model has applied the snapshot/deltas.

An epoch change invalidates in-memory transport handles, not semantic operation
identities. In-flight operations at a crash receive an explicit runtime
disposition and cleanup fact; they are never silently replayed. A changefeed
gap causes full hydration, not guessed deltas. Reconnect must not erase the
last-known valid selector index or make selector availability depend on worker
readiness.

## 10. Implementation and parity gates

This draft is a prerequisite checklist, not evidence that any gate is met. A
future implementation must record objective evidence for every gate below.

### Gate A — schema and ownership review

- The private protocol version, operation registry, envelope discriminators,
  identity rules, and compatibility policy are reviewed as one bounded schema.
- Each operation maps to one current inventory responsibility and one runtime
  owner; no operation creates a second config/session/credential store.
- Canonical provider/config/agent selector ownership and canonical mutation
  routing remain service-owned. Any proposed change to that ownership returns
  to architecture review rather than being inferred from this draft.

### Gate B — complete retained-path coverage

Production consumers are enumerated against the documented inventory in
`p4.4-g3-transport-bridge-inventory.md` and the bounded audit scan of SDK-backed
routes cited in §4.4 and mapped to prospective private operations. The
authoritative enumeration is the retained-path coverage matrix in §4.4, which
covers only that enumerated inventory and bounded scan; it explicitly does not
claim exhaustive SDK discovery or to enumerate every known retained consumer.
Any SDK-backed route discovered beyond the rows in §4.4 is an explicit Gate B
blocker and requires a matrix row addition before Gate B can be met. The summary
below is not a substitute for that matrix and does not constitute coverage:

- session create/get/status/messages/children, prompt, abort/cancellation,
  revert/unrevert, delete, parent-child synchronization, and stream/event
  hydration;
- canonical provider listing and mutations, credential status, retained
  backend/auth actions, and custom-provider model discovery;
- canonical config/effective snapshot reads, selector indexes, global/project
  reads, validated writes/unset, conflict reporting, and external-file
  reconciliation;
- SDK-only or adjacent retained request paths whose removal would otherwise
  regress session status, permissions/questions, skills, commands, MCP, image
  models, sandbox, usage, export, or other inventory-listed responsibilities.

Every `deferred` row in §4.4 — including inventory-listed `sandbox`,
`mcp/*`, `image-model`, `marketplace` (`kilo/profile`/`organization`),
`session-usage` (`kilocode.sessionModelUsage`), plus `command/list`,
`skill`, `agent`, `notebook`, `find/files`, `permission`, `question`,
`suggestion`, and the audit-identified `enhancePrompt.enhance`, `instance.reload`,
`path.get`, `kilocode.agentRequirements`, `global.config.update`,
`instance.dispose`, and `pty.create`/`pty.update`/`pty.remove` (with co-located
`pty.list`/`pty.get`/`pty.shells`/`pty.connect` where documented) — is an explicit
unresolved Gate B blocker until it has a versioned operation, identity/routing/
error/cancellation contract, and real-production parity evidence per Gates C-D.
Additionally, the four session rows `session/command` (prompt variant), `session/fork`, `session/update`, and `session/cancelQueued` are now `named — Gate B open` with prospective §4.2 contracts — versioned envelope §3.2, identity/routing §8, error/cancellation §6, and `cancelQueued` drain-control fence availability §5.1/`drain-control.ts:43,105` — and the `remote.status` row is now `named (contract-only) — Gate B open` with a pure contract-evidence candidate (§4.5: strict v1 envelope, routing-only scope, process-global payload, redacted failure, detached parity helper; tests prove shape only, not production parity) and the `experimental/session/list` row is now `named (contract-only) — Gate B open` with a pure contract-evidence candidate (§4.6: strict v1 routing identity, safe summary projection, typed failures/scope mismatch, detached parity helper, explicit pagination/order/freshness/lifecycle/cross-directory unknowns; tests prove shape only, not production parity) — but all remain Gate B open pending implementation and Gates C-D parity; they are not cutover and do not satisfy Gates C-F. Additionally, any SDK-backed route discovered beyond the enumerated inventory and
bounded audit scan is an explicit Gate B blocker and must be added to §4.4 as a
`deferred` row before Gate B can be met. No broad wording, typed union, or
inventory phrase satisfies a deferred or named-open row.

The private worker's existing observation methods do not count as coverage for
these request classes. A generic request passthrough, test bridge, or type-only
union does not count as an implementation. This gate remains Active/residual;
this draft does not imply completion or cutover and creates no product
compatibility decision.

### Gate C — real production wiring

- Extension consumers use the private implementation on a real activation and
  real worker lifecycle, not only unit fakes or a fixture bridge.
- The worker owns runtime facts and generation/recovery decisions; the extension
  owns canonical files, SecretStorage, selector read models, and transport
  lifecycle.
- The implementation proves directory/session isolation and exact operation
  identity under concurrent Agent Manager and editor-tab requests.
- Private-worker unavailable, restart, reconnect, reload, close/reopen, and
  session-switch paths have explicit behavior and do not silently change the
  current SDK bridge status.

### Gate D — behavioral parity

Using real current production paths as the comparison oracle, tests cover:

- successful and failed reads/mutations for every mapped operation;
- provider auth, missing credentials, credential cancellation, redaction, and
  model discovery outcomes;
- prompt acceptance, streaming ordering, message hydration, parent-child
  routing, revert/unrevert, delete, abort, and permission/question waits;
- validation, stale drafts, external edits, canonical version commits,
  generation pinning, no active-generation interruption, admission-fence
  latest-version convergence, coalescing/versioning of subsequent cold writes,
  and reload/load-during-fence behavior;
- duplicate requests, stale responses, event duplicates/gaps, cancellation
  provenance, transport loss before/after acceptance, worker crash, and
  explicit in-flight disposition;
- reconnect and restart convergence with no lost/duplicated presentation facts.

Static presence tests, health-absence tests, observation-only tests, or tests
that duplicate the implementation algorithm do not establish this gate.

### Gate E — cutover decision

A separately reviewed cutover record must identify the exact consumers being
moved, the release/rollback boundary, and the evidence from Gates A-D. It must
prove that the private path is authoritative for every mapped request before
any SDK client, server discovery, HTTP route, SSE adapter, or generated SDK
consumer is proposed for deletion. Canonical config has no dual-read/import
exception; transport migration must not use this draft to introduce one.

### Gate F — deletion authorization

Bridge deletion is prohibited until all of the following are true and linked to
evidence:

1. Gate E explicitly approves private authority for the complete retained
   inventory, including fallback/reconciliation and SDK-only paths.
2. Production static reachability and runtime traces show no remaining bridge
   consumer for the deleted boundary; observation-only private wiring is not
   counted.
3. Private and extension lifecycle tests pass for unavailable, reconnect,
   restart, close/reopen, reload, and concurrent directory/session cases.
4. Credential and redaction review proves no secret value is persisted,
   projected, logged, or returned by the private protocol.
5. A rollback path exists that does not replay accepted semantic operations or
   create a second authority, and the deletion batch has its own evidence and
   tracker update.

Until this gate is met, the current SDK HTTP/SSE bridge, server startup/port
discovery, and SSE event/liveness path remain retained exactly as recorded in
the inventory. No phase completion or cutover claim follows from this draft.

## 11. Explicit non-claims and remaining risks

This artifact does not claim (updated for B1 narrow exception):

- that any provider, session, or config private request schema beyond B1’s single `session/cancelQueued` fd3/fd4 parity carrier is implemented — B1 implements only `session/cancelQueued` via `kilo serve` extra-fd into the same `AppRuntime` dispatch, SDK-authoritative and parity-only ([doc: p4.4-g3-b1-private-cancel-queued-carrier.md](p4.4-g3-b1-private-cancel-queued-carrier.md)), G3 remains Active and no cutover is claimed;
- that the private worker handles provider/session/config requests beyond that single B1 operation;
- that the private worker is a replacement for the CLI server, SDK client, server discovery, or SSE adapter;
- that canonical selector authority supplies missing session, prompt,
  message-history, provider-auth/backend, or SDK-only semantics;
- that observation parity is request parity, or that health-route absence is
  transport cutover;
- that P4.4, the G3 transport gate, old CLI/server deletion, or selector
  readiness work is complete.
- that `session/abort` (B9) is implemented, closed, or privately executable — B9 is contract investigation only (evidence-only); current `session.abort` remains volatile `200 true` with no operation identity, outcome aggregation, or scope validation; SDK HTTP remains the sole abort authority; no private abort capability exists and Gate B remains open. HTTP receipt (including any future `accepted`/`accepted-unresolved` transport receipt status, non-terminal; `ambiguous` remains §5.1/§6 runtime semantic terminal outcome, `transport-unknown` is §9.1 delivery uncertainty only) is not cancellation proof; terminal convergence is confirmed separately via runtime/SSE/persistence.
- B9-P2 (2026-09-04, docs-only): no new abort fd capability/private carrier, no `handleAbort`/state-machine change; busy HTTP→runner→persistence→SSE ordering and SDK timeout/late-SSE recovery remain explicit unknowns, not proven facts.
- B9-P2.1 TCP oracle (2026-09-04, plain TCP observed, docs-only): plain TCP abort sequence is evidence only (receipt/busy/persistence/SSE/shell layers recorded separately; `200 true` receipt, not cancellation proof); invalid `agent:build` 500 is malformed probe input; env-only/bogus-fd is probe limitation, not VS Code production evidence; real Extension Host fd3/fd4 remains unknown; Gate B open, G3 Active, Gates C-D unchanged; no contract field changed.
- B9-P2.1 (2026-09-04, docs-only, in-process scope only): one ephemeral external in-process probe (real AppLayer/typed HTTP routes/GlobalBus/SSE, `:memory:`, test provider, permission allow; temp file deleted, no repo artifact) observed busy `shell sleep 30` → 409 second-shell busy, busy `prompt_async` 204 with messages 0→3 and LLM calls 0→0, abort `200 true` as receipt only, shell completed ~552ms with `User aborted the command`, queue internal state cleared, status map `{}`/absent, SSE `turn.open`/`turn.close`/`session.status idle`/`session.idle`/shell end, and missing/idle/completed re-abort `200 true` as receipt. Direct `runState.cancel` descendant evidence is `packages/opencode/test/tool/task.test.ts` (`cancelling a parent run recursively cancels descendant background tasks`, 1 pass 26 filtered) — not cancelTree/HTTP full-tree. `packages/opencode/test/kilocode/server/listener-runtime.test.ts` stays unresolved/harness-or-product unknown (HTTP 200 expect passed, parent runner not interrupted, ~7474ms timeout; no product-bug attribution). Workspace TCP liveness is unstable/blocked (connectable, zero response bytes; `us_socket_t kind=invalid` SIGTRAP on some attempts; prod control fast 401) — never PASS, no real HTTP abort timeline. Queued slot identity, descendant HTTP outcome, followup live cancel, new intake race, and partial aggregation stay unproven; no field semantics changed, Gate B remains open, G3 remains Active.
- B9-P2.1 Extension Host oracle (2026-09-04, real Extension Host observed, docs-only): real `KILO_E2E_SCENARIO=real-lifecycle` Node-launcher Host composition is evidence only (ExtHost pid 99779 / backend pid 190 / PPID 99779 / port 62964 / epoch 1; backend `lsof -a -p 190 -d 3,4 -n -P` fd3/fd4 unix endpoints kernel-side only; Extension Host peer fd number unobserved, no dual-end fd mapping claim; `privatePeerStatus` available/open, `kilo-private/1.0`, 8 capabilities, `hasSessionUpdate` true; PrivatePeer negotiated pid 190 epoch 1; SSE opened/server connected; `real-lifecycle` passed; summary only, external temp evidence, no repo artifact). Raw handoff malformed is validator drift only (stale 2-cap allowlist rejected `session/fork`; fixed separately in `packages/kilo-vscode/script/e2e-evidence.ts` + `packages/kilo-vscode/tests/unit/e2e-evidence.test.ts`), not peer failure and not Gate closure. Read-path 37 divergences (Get 13 / Messages 9 / Children 15) carry no per-call failure code/status/opId and stay unknown, not a failure rate and not product-attributed. This Host run executed no abort; `200 true` in prior plain TCP/in-process observations is receipt only, not cancellation proof, and not an abort observation of this Host run. Gate B open, G3 Active, Gates C-D unchanged; no contract field changed.
- B9-P2.1 Host abort oracle (2026-09-04, native CLI override, docs-only for this doc update): `KILO_P0_BACKEND_CLI` native override `real-session` Host A/B abort convergence is evidence only (exit 0 / `validated true`; backend pid 17930 / private peer pid 17930 epoch 1 / port 51055 / hang server 51004 / SSE connected; A/B busy → Stop A → A idle / B busy with backend `service=session.processor ... error=Aborted process` → Stop B → both idle; snapshots real-snap1..8 / DOM evidence / LLM 4 records all `e2e-local`/`e2e-model` `violations[]`; `real-session lifecycle passed` lifecycle only; summary only, external temp evidence; this doc update itself added no repo artifact). Real UI Stop is the sole abort path; no abort receipt raw body captured, any `200 true` is receipt only. This is native override evidence, not default source-wrapper production path proof. Default source-wrapper run remains failed startup timeout (pid 81735 no port in 30s) as independent limitation; owned direct source startup matrix succeeds outside Host, not wrapper health. Prior Host composition boundary retained (backend fd3/fd4 backend-only, validator drift fixed separately, read 37 unknown, previous Host run no abort). Queued/partial/timeout/late-SSE/operation aggregation stay unknown; LLM records are not SDK abort call count; no exact SSE event order beyond captured facts. Gate B open, G3 Active, Gates C-D unchanged; no contract field changed.
- B9-P2.1 Default source-wrapper real-session Host abort oracle (2026-09-04, docs-only for this doc update): default-path evidence only, distinct from the prior native override delta (HEAD `0343a02b1b8d9814dd40766b316d21916ac69de4`, `KILO_P0_BACKEND_CLI` absent, default `packages/kilo-vscode/bin/kilo` via `KILO_E2E_SCENARIO=real-session ... node script/e2e-probe-launch.mjs`, 34986ms / `validated true` / `status complete`; external `/tmp/kilo-p0-default-realsession-20260904-155200/` not committed; this doc update itself added no repo artifact). Layered facts are backend pid 83832 / port 53573 / PrivatePeer pid 83832 epoch 1 / SSE stream opened, A/B busy → Stop A → A idle / B busy → Stop B → both idle via real UI Stop only, backend `session.prompt ... cancel` plus `session.processor ... error=Aborted process` with first Stop A followed by a B-session Aborted line so attribution stays ambiguous (backend-side signal only, not HTTP receipt), snapshots real-snap-1..7 / DOM / closed-A persists / B rehydrates + persists / `real-session lifecycle passed` as lifecycle only, LLM 4 generation requests (`e2e-local`/`e2e-model`, `violations[]`, matrix only), `sdk=succeeded priv=failed` plus `InstanceRef not provided` observed without root-cause attribution, and clean teardown (no owned VS Code process, CDP 53550 released, scratch removed, exit 0). Real UI Stop is the sole abort path; UI Stop convergence and backend signal are recorded as separate observations, with backend attribution remaining ambiguous; the SDK abort receipt was not independently observed, and exact SSE ordering and durable terminal outcome remain unobserved. Exact SDK `session.abort` body, abort call count, exact SSE order, queued/partial/timeout/late-SSE/aggregation stay unobserved; no wrapper-health, production-wide, aggregate-outcome, Gate-closure, or readiness claim. Prior native delta (pid 17930 / port 51055 / hang 51004) stays separate; The prior source-wrapper 30s timeout remains historical evidence; this run adds one successful default-path observation and does not establish wrapper-wide health or repeatability. Gate B open, G3 Active, Gates C-D unchanged; no contract field changed.
- B9-P2.1 Default-path live A/B abort evidence delta (2026-09-04, docs-only for this doc update): default-path live evidence only, distinct from both the prior native override delta and the prior default source-wrapper delta (HEAD `88eba1812186876969ad635f57e78553da35e48f`, unset `KILO_P0_BACKEND_CLI` env, default `packages/kilo-vscode/bin/kilo` via `KILO_E2E_SCENARIO=real-session ... node script/e2e-probe-launch.mjs`, fixture `e2e-probe-a1f137a6`, 43700ms / `validated true` / launcher 57s; external `/tmp/kilo-p0-abort-20260904-185800-evidence/` not committed; this doc update itself added no repo artifact). Layered facts are backend pid 4971 / port 63829 / PrivatePeer pid 4971 epoch 1 / SSE connected, sessions A `ses_f93ee292bffeyQs0GAf47wCG5q` / B `ses_f93ee1f84ffeNiNINPmH1SNl8f`, `abort-attempts.json` total 4 (two records per session, attempts 1/2, all ok true / 200 / data true, A 94/105ms, B 82/91ms, receipt only), A/B timelines 10 entries seq 0-9 with no drops, backend prompt cancel + `Aborted process` with first Stop A attribution ambiguous, A idle / B busy → both idle via real UI Stop only, closed-A persists / B rehydrates + persists / canonical archive stable / lifecycle passed as lifecycle only, LLM 4 generation requests as matrix only, and clean teardown. Real UI Stop is the sole abort path; SDK receipt, backend signal, SSE ordering, and durable outcome stay separate. Two directory records per session are observed fan-out only, not retry. Exact logical SDK abort call count, cross-layer HTTP→backend→SSE order, durable aggregate outcome, queued/partial/timeout/late-SSE/aggregation stay unobserved; no wrapper-health, production-wide, aggregate-outcome, Gate-closure, readiness, or root-cause claim; prior native (pid 17930) and prior default (pid 83832) deltas stay separate. Gate B open, G3 Active, Gates C-D unchanged; no contract field changed.

- `remote.status` contract-only candidate (2026-09-05, Gate B open): pure contract helpers plus focused tests are shape evidence only (`serve-private-remote-status-contract.ts`, 9 pass 56 expects) — strict v1 envelope with routing-only directory/workspace and empty payload, process-global `{enabled,connected}` payload with no directory binding, redacted `{code,message,retryable}` failure, and a detached parity helper comparing only the two booleans. No private carrier or capability, no production behavior change, no production parity, no cutover. `remote.enable`, `remote.disable`, and `RemoteStatusChanged` event-stream parity are explicitly out of scope; freshness/observation semantics and transport behavior (including busy/error/timeout/late-SSE recovery for this path) remain unresolved and unobserved. SDK `client.remote.status` remains the sole authority. Gate B remains open, G3 Active, Gates C-D unchanged.

- `experimental/session/list` contract-only candidate (2026-09-05, Gate B open): pure contract helpers plus focused tests are shape evidence only (`serve-private-session-list-contract.ts`, 7 pass 49 expects) — strict v1 `experimental/session/list` envelope with routing-only directory/workspace and `filter` payload, `experimental-session-list:<token>` opId with `idempotencyKey === opId`, `scope_mismatch` routing-only scope, safe `{id,directory,title,updated}` summary projection with `updated` shape-only, redacted `{code,message,retryable}` failure, and a detached parity helper comparing only shared-id projection with membership gaps as `session-list-membership-unknown`. No private carrier or capability, no production behavior change, no production parity, no cutover. Distinct from `session/status`/`session/get`/`session/messages`/`session/children`, non-experimental session routes, and `command/list`. Pagination (`limit`/`cursor`/`x-next-cursor`), ordering, freshness, deleted/archived lifecycle, and cross-directory ownership are explicit unknowns. SDK `client.experimental.session.list` remains the sole authority. Gate B remains open, G3 Active, Gates C-D unchanged.

Remaining risks are concrete and bounded:

1. The matrix in §4.4 covers the enumerated inventory and the bounded audit scan
    of SDK-backed routes cited in this draft (including audit-identified
    `enhancePrompt.enhance`, `instance.reload`, `path.get`,
    `kilocode.agentRequirements`, `global.config.update`,
    `instance.dispose`, and `pty.create`/`pty.update`/`pty.remove` with co-located
    `pty.list`/`pty.get`/`pty.shells`/`pty.connect`, plus now-named but Gate B open prospective rows `session/command` (prompt variant `session/prompt.ts:2367`), `session/fork` (`ForkInput` `session/session.ts:275`), `session/update` (`UpdatePayload` `session.ts:49`), `session/cancelQueued` (`DELETE /queue` `session.ts:113` drain-control `drain-control.ts:43,105`), plus the now-named (contract-only) but Gate B open `remote.status` candidate (§4.5: strict v1 envelope, routing-only scope, process-global payload, redacted failure, detached parity helper; tests prove shape only) and the now-named (contract-only) but Gate B open `experimental/session/list` candidate (§4.6: strict v1 routing identity, safe summary projection, typed failures/scope mismatch, detached parity helper, explicit pagination/order/freshness/lifecycle/cross-directory unknowns; tests prove shape only), and remaining deferred `sandbox`, `mcp/*`, `image-model`, `marketplace`, `session-usage`,
    `command/list`, `skill`, `agent`, `notebook`, `find/files`, `permission`,
    `question`, `suggestion`, and others). It does not claim exhaustive discovery
    of every SDK call. Any additional SDK-backed route discovered beyond the rows
    below is an explicit Gate B blocker and required matrix addition before Gate B
    can be met; no broad wording satisfies a deferred or named-open row.
2. The exact host-to-worker credential handoff and provider resource ownership
   need security and lifecycle review before implementation.
3. Stream resumability, operation lookup after restart, and runtime storage of
   accepted outcomes need production-path evidence; this draft only specifies
   the required distinctions.
4. Compatibility behavior for minor/major protocol evolution needs an
   implementation decision at Gate A; this draft does not authorize a public
   compatibility contract.
5. Cold-config fence convergence, coalescing/versioning, and reload-during-fence
   semantics (§4.3, §5.1, Gate D) are specification only and need
   implementation and parity evidence before any cutover.
6. B9 `session/abort` contract direction (cancellation-request identity with aggregated generation/queued-work/descendant/event-publication outcome) is prospective and evidence-only; the current volatile `200 true` gap means no abort outcome, scope, or parity claim can be inferred until Gates C-D evidence exists. B9-P2 (2026-09-04): receipt vs terminal convergence split, typed `session.not_found`/terminal no-op/`not_affected`, and per-target observability are contract only; busy ordering and timeout/late-SSE recovery stay explicit unknowns. B9-P2.1 (2026-09-04, in-process scope only): the ephemeral probe proves handler/runtime/persistence/Bus behavior only — receipt/queue-clear/SSE facts above — not TCP or Gates C-D; descendant HTTP failure cause, followup live cancel, new intake race, and partial aggregation stay unknown; the listener-runtime failure is not attributed to a product bug. B9-P2.1 TCP oracle (2026-09-04, plain TCP observed, docs-only): plain current-workspace TCP sequence (HEAD `f92690fe6b`, Bun 1.3.14/macOS, isolated XDG/temp DB, plain env cleared; `curl /global/config` 200 ~49ms, `/event` 200 SSE + `server.connected`, create 200 ~95ms, corrected `agent:code` + test model `sleep 30` busy, second shell 409, abort 200 true ~143ms as receipt, blocked shell 200 ~50ms completed with `User aborted the command`, SSE message/part → idle, `{}` by design, messages persisted, missing/idle re-abort 200 true, post-abort echo 200 reusable) is plain TCP evidence only — HTTP receipt, runner busy/interrupt, persistence, SSE, shell response recorded separately; invalid `agent:build` 500 is malformed probe input, not product fault; env-only/bogus-fd (`KILO_CLIENT=vscode`/`KILO_PARENT_PID`, fake-parent watchdog expected, real-parent zero-byte/reset + child 94–98% CPU, non-production supervisor gate true) is environment/probe limitation only; real Extension Host fd3/fd4 remains unknown; `accepted-unresolved`/`transport-unknown`/`ambiguous` unchanged; Gate B open, G3 Active, Gates C-D unchanged; no field semantics changed.
7. B9-P2.1 Extension Host oracle (2026-09-04, real Host observed, docs-only): true peer evidence stops at unix endpoint/pid/epoch layer (ExtHost 99779 / backend 190 / port 62964 / epoch 1, backend fd3/fd4 kernel endpoints captured, Host-side fd number unobserved, no dual-end fd mapping claim); validator drift (stale 2-cap allowlist rejected `session/fork`) is fixed separately and is not Gate closure; read-path 37 divergences (Get 13 / Messages 9 / Children 15) stay failure-code unknown with no product attribution; no abort was executed in this Host run; Gate B open, G3 Active, Gates C-D unchanged.
8. B9-P2.1 Host abort oracle (2026-09-04, native CLI override, docs-only for this doc update): native override evidence only (`KILO_P0_BACKEND_CLI` native binary + `KILO_E2E_SCENARIO=real-session` via `node script/e2e-probe-launch.mjs`, exit 0 / `validated true`); layered facts are backend `service=session.processor ... error=Aborted process`, A idle / B busy after Stop A, both idle after Stop B, snapshots real-snap1..8 / DOM / LLM 4 records (`e2e-local`/`e2e-model`, `violations[]`), peer pid 17930 epoch 1 / port 51055 / hang 51004 / SSE connected, and `real-session lifecycle passed` as lifecycle only. Real UI Stop is the sole abort path; no abort receipt raw body captured. Default source-wrapper startup timeout (pid 81735 no port in 30s) stays an independent limitation; owned direct source startup matrix outside Host is not wrapper health. Queued/partial/timeout/late-SSE/operation aggregation stay unknown; no new carrier/capability; Gate B open, G3 Active, Gates C-D unchanged.
9. B9-P2.1 Default source-wrapper real-session Host abort oracle (2026-09-04, docs-only for this doc update): default-path evidence only, distinct from the prior native override delta (HEAD `0343a02b1b8d9814dd40766b316d21916ac69de4`, `KILO_P0_BACKEND_CLI` absent, default `packages/kilo-vscode/bin/kilo`, 34986ms / `validated true`; external `/tmp/kilo-p0-default-realsession-20260904-155200/` not committed). Layered facts are pid 83832 / port 53573 / epoch 1 / SSE stream opened, A/B busy → Stop A → A idle / B busy → Stop B → both idle via real UI Stop only, backend cancel plus Aborted-process signal with B-session line after first Stop A so attribution stays ambiguous, snapshots real-snap-1..7 / DOM / closed-A persists / B rehydrates + persists / lifecycle passed as lifecycle only, LLM 4 generation requests as matrix only, `sdk=succeeded priv=failed` plus `InstanceRef not provided` without root-cause attribution, and clean teardown (no owned process, CDP 53550 released, scratch removed, exit 0). Exact SDK abort body, abort call count, exact SSE order, queued/partial/timeout/late-SSE/aggregation stay unobserved; no wrapper-health, production-wide, aggregate-outcome, Gate-closure, or readiness claim; Gate B open, G3 Active, Gates C-D unchanged.
10. B9-P2.1 Default-path live A/B abort evidence delta (2026-09-04, docs-only for this doc update): default-path live evidence only, distinct from both prior deltas (HEAD `88eba1812186876969ad635f57e78553da35e48f`, pid 4971 / port 63829 / epoch 1, sessions A `ses_f93ee292bffeyQs0GAf47wCG5q` / B `ses_f93ee1f84ffeNiNINPmH1SNl8f`, `abort-attempts.json` total 4 as receipt only, A/B timelines 10 entries seq 0-9, backend cancel + `Aborted process` with ambiguous first Stop A attribution, A idle / B busy → both idle, closed-A persists / B rehydrates + persists / archive stable, LLM 4 as matrix only, clean teardown; external `/tmp/kilo-p0-abort-20260904-185800-evidence/` not committed). SDK receipt, backend signal, SSE ordering, and durable outcome stay separate; two directory records per session are observed fan-out only, not retry; no logical abort call count, cross-layer order, durable aggregate outcome, wrapper-health, production-wide, or root-cause claim; prior native (pid 17930) and prior default (pid 83832) deltas stay separate; Gate B open, G3 Active, Gates C-D unchanged.

## 12. Verification record

This document was reviewed as a static cross-reference artifact. No private
request implementation, cutover, or live parity run was performed.

| Check | Command or review | Result |
|---|---|---|
| Direction cross-reference (cold semantics) | Read `runtime-and-configuration-direction.md` §§3, 5, 5.1, 5.4, 6, 7.1, 7.2 and `AGENTS.md` config update lifecycle | PASS — acknowledgement is immediate after commit without interrupting active generations; admission fence, latest-version convergence, coalescing/versioning, and reload-during-fence are preserved without claiming implementation. |
| Current transport cross-reference | Read `evidence/p4.4-g3-transport-bridge-inventory.md` §§2-6 and current private-worker sources cited there | PASS — retained SDK/SSE status and observation-only boundary are preserved. |
| Routing / retained-path cross-reference | Static review of `KiloProvider.ts`, `AgentManagerProvider.ts`, `kilo-provider/*`, `provider-actions.ts`, `shared/sandbox-session.ts`, `services/marketplace/actions.ts`, `agent-manager/terminal-manager.ts`, `kilo-provider/model-state.ts`, `kilo-provider/agent-requirements-controller.ts`, `services/RemoteStatusService.ts`, `extension.ts`, `kilo-provider/work-style-apply-handler.ts` against §4.4 matrix (enumerated inventory + bounded audit scan) | PASS — matrix covers enumerated inventory and bounded audit scan only, not exhaustive SDK discovery; §4.4 now lists four named but Gate B open prospective session rows `session/command` (variant), `session/fork` (`ForkInput`), `session/update`, `session/cancelQueued` (drain-control, fence-available via `drain-control.ts:43,105`) plus the named (contract-only) but Gate B open `remote.status` candidate (§4.5) and the named (contract-only) but Gate B open `experimental/session/list` candidate (§4.6) plus audit-identified `enhancePrompt.enhance`, `instance.reload`, `path.get`, `kilocode.agentRequirements`, `global.config.update`, `instance.dispose`, `pty.create`/`update`/`remove` plus co-located `pty` routes and remaining deferred `sandbox`/`mcp`/`image-model`/`marketplace`/`session-usage` and other rows; `named — Gate B open` remains open per §10 Gates C-D with no implementation/cutover claimed; any unlisted discovered SDK call is an explicit Gate B blocker requiring matrix addition. No broad wording is a coverage claim. Canonical selector authority remains distinct from the bridge. |
| B9 abort contract cross-reference | Read `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` §§ Locked direction / Current CLI-VS Code evidence / Lifecycle matrix / Route governance scan + `runtime-and-configuration-direction.md` §7.2.1 | PASS — `opId` is one session cancellation-request identity bound to directory + sessionId; abort scope is the session cancellation scope with aggregated generation/queued-work/descendant/event-publication impact; an existing generation identity is an outcome association only, never the request identity; no `target op ID` / single-generation wording remains in the abort contract. |
| B9 route/path evidence | Count `HttpApiEndpoint.*` entries in `packages/opencode/src/server/routes/instance/httpapi/groups/session.ts`; read `packages/kilo-vscode/src/kilo-provider/abort.ts:63-65` | PASS — 28 endpoint entries (29 raw string hits include the import line); abort citation is the full repo-relative path `packages/kilo-vscode/src/kilo-provider/abort.ts:63-65`. |
| B9-P2 wire closure (2026-09-04) | Read B9-P2 section in `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` + §4.2/§4.4/§5.1/§6/§11 deltas in this draft | PASS — receipt ≠ convergence, request-identity binding with generation-association-only, typed `session.not_found`/terminal no-op/`not_affected`, per-target observability prerequisite, SDK/SSE/private authority split, no new carrier/state-machine change, busy/timeout unknowns retained; Gate B open, G3 Active. |
| B9-P2.1 dynamic oracle delta (2026-09-04, docs-only, in-process scope) | Read `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` B9-P2.1 delta + §11 deltas in this draft | PASS — ephemeral probe recorded as receipt/queue/SSE timeline summary only (no repo artifact); repo anchors `packages/opencode/test/tool/task.test.ts` descendant pass and `packages/opencode/test/kilocode/server/listener-runtime.test.ts` unresolved failure cited without product-bug attribution; TCP liveness recorded as unstable/blocked, never PASS; queued/descendant/followup/intake/partial unknowns retained; Gate B open, G3 Active, no field semantics changed. |
| B9-P2.1 TCP oracle delta (2026-09-04, plain TCP observed, docs-only) | Read `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` TCP oracle delta + §11 risk 6 delta | PASS — plain TCP timeline recorded layered (receipt/busy/persistence/SSE/shell); `200 true` receipt only; `agent:build` 500 as malformed input; bogus-fd as probe limitation, real peer unknown; Gate B open, G3 Active, Gates C-D unchanged, no field semantics changed. |
| B9-P2.1 Extension Host oracle delta (2026-09-04, real Host observed, docs-only) | Read `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` Extension Host oracle delta + §11 non-claim/risk 7 deltas in this draft | PASS — Host composition layered (ancestry/backend fds/private peer/HTTP-SSE/lifecycle); backend fd3/fd4 kernel-side only, Host-side fd unobserved; raw handoff malformed as validator drift fixed separately, not peer failure; 37 read divergences failure-code unknown; no abort executed in this Host run, `200 true` from prior plain TCP/in-process observations as receipt only (not a Host-run abort observation); Gate B open, G3 Active, Gates C-D unchanged, no field semantics changed. |
| B9-P2.1 Host abort oracle delta (2026-09-04, native CLI override, docs-only for this doc update) | Read `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` Host abort oracle delta + §11 non-claim/risk 8 deltas in this draft | PASS — native `KILO_P0_BACKEND_CLI` override recorded (not default source-wrapper proof); exit 0 / validated true; A/B busy → Stop A → A idle / B busy + backend Aborted process → Stop B → both idle via real UI Stop only; snapshots real-snap1..8 / DOM / LLM 4 records matrix only; no abort receipt body quoted; source-wrapper 30s timeout retained as independent limitation; queued/partial/timeout/late-SSE/aggregation unknowns retained; Gate B open, G3 Active, Gates C-D unchanged, no field semantics changed. |
| B9-P2.1 Default source-wrapper real-session Host abort oracle delta (2026-09-04, docs-only for this doc update) | Read `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` Default source-wrapper delta + §11 non-claim/risk 9 deltas in this draft | PASS — default `packages/kilo-vscode/bin/kilo` recorded distinct from native override (HEAD 0343a02b1b, KILO_P0_BACKEND_CLI absent, 34986ms / validated true); pid 83832 / port 53573 / epoch 1 / SSE opened; A/B Stop convergence layered; backend cancel + Aborted signal with ambiguous attribution; snapshots 1..7 / DOM / persist + rehydrate / lifecycle-passed as lifecycle only; LLM 4 matrix only; divergence without attribution; clean teardown; exact abort body / call count / SSE order / queued-partial-timeout-late-SSE-aggregation unobserved; Gate B open, G3 Active, Gates C-D unchanged, no field semantics changed. |
| B9-P2.1 Default-path live A/B abort evidence delta (2026-09-04, docs-only for this doc update) | Read `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` Default-path live delta + §11 non-claim/risk 10 deltas in this draft | PASS — default-path live run recorded distinct from both prior deltas (HEAD 88eba18121, unset `KILO_P0_BACKEND_CLI` envs, 43700ms / validated true / 57s); pid 4971 / port 63829 / epoch 1 / SSE connected; A/B Stop convergence layered; `abort-attempts.json` 4 records as receipt only (fan-out, not retry); timelines 10 entries each; backend cancel + Aborted signal with ambiguous first Stop A attribution; persist + rehydrate + archive stable / lifecycle-passed as lifecycle only; LLM 4 matrix only; clean teardown; call count / cross-layer order / durable aggregate outcome unobserved; Gate B open, G3 Active, Gates C-D unchanged, no field semantics changed. |
| `remote.status` contract-only candidate (2026-09-05) | Read `packages/kilo-vscode/src/services/cli-backend/serve-private-remote-status-contract.ts` + `.test.ts` and §4.5 | PASS — strict v1 envelope, routing-only scope, process-global payload, redacted failure, detached parity helper recorded; focused tests prove shape only (9 pass 56 expects), no production parity; `remote.enable`/`remote.disable`/event/freshness/transport unresolved; Gate B open, G3 Active, Gates C-D unchanged. |
| `experimental/session/list` contract-only candidate (2026-09-05) | Read `packages/kilo-vscode/src/services/cli-backend/serve-private-session-list-contract.ts` + `.test.ts` and §4.6 | PASS — strict v1 routing identity, safe summary projection, typed failures/scope mismatch, detached parity helper recorded; focused tests prove shape only (7 pass 49 expects), no production parity; pagination/order/freshness/lifecycle/cross-directory explicit unknowns; distinct from session/status/get/messages/children, non-experimental routes, and command/list; Gate B open, G3 Active, Gates C-D unchanged. |
| Markdown whitespace | `git diff --check` | PASS — no whitespace errors. |
| Markdown table padding | `bun run script/check-md-table-padding.ts specs/vscode-orchestrator/evidence/p4.4-g3-b9-session-abort-contract-investigation.md specs/vscode-orchestrator/evidence/p4.4-g3-private-request-schema-draft.md specs/vscode-orchestrator/runtime-and-configuration-direction.md specs/vscode-orchestrator/migration-tracker.md` | PASS — no padded tables found. |
| Untracked B9 evidence file | Read back `evidence/p4.4-g3-b9-session-abort-contract-investigation.md` separately from the tracked diff | PASS — `B9-LOCK-001..008` namespace only (no repository-wide `LOCK-001..008` reuse), route count 28, full abort path, timeout wording limited to static evidence, investigation-only with no implementation/cutover claim. |

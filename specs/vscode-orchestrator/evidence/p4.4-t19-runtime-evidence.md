# P4.4-T19 Runtime / Source-Removal Evidence — Cumulative T13-T18

> **Status: EVIDENCE ONLY — P4.4 remains Active per LOCK-001. No phase completion claimed.**

> **P4.4-T27-T30 superseding clarification (2026-08-27): Historical T18/T19 claims below are preserved as T18 evidence; T27-T30 supersedes the TUI KILO_CONFIG_DIR sole-reader and legacy-discovery last-wins model — current (T27-T30) TUI uses canonical Git worktree root (`resolveWorktree`/`canonicalRoot` via `Filesystem.resolve`, non-Git fallback to directory) with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only, no `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig`/`.kilocode`/ancestor readers, precedence global → direct → `.kilo`; `Global.Path.config` override and sandbox deny-list remain per LOCK-003 (not effective TUI source); `ConfigPaths.files` API retained with no active TUI call. This file’s T18-era “sole TUI reader” language is historical; see P4.4-T27-T30 clarifications in `p4.3-cutover-evidence.md` and `p4.4-source-removal-evidence-matrix.md`.**

> **P4.4-T13-T17/T19/T24 historical snapshot clarification and plugin-origin scope fix (2026-08-27, LOCK-SOURCE/LOCK-R6, no active legacy dual-read/import fallback):** Dated T13-T17, T24, and T19 passages herein stating legacy discovery (`yield* afs.up({ targets: [".kilocode", ".kilo"] })`, `ConfigPaths.directories()` ancestor/home walks), `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG` last-wins, or `migrateTuiConfig`/`.kilocode`/`.kilo` migration as current active TUI behavior are **historical snapshots at those dates**, explicitly superseded by T27-T30 current canonical behavior: canonical global -> root direct -> root `.kilo` only (`resolveWorktree`/`canonicalRoot`, `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")`, no ancestor walk, no `.kilocode`, no `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig` readers, LOCK-SOURCE, LOCK-R6, no active legacy dual-read/import fallback). The migration helper `cli/cmd/tui/config/tui-migrate.ts` remains as **physical/dead residue with no active TUI caller after P4.4-T27** (no import/call in `cli/cmd/tui/config/tui.ts`). Plugin origin assembly now uses explicit source identity at merge time: global entries are explicitly `scope: "global"` regardless of `Global.Path.config` location (even when beneath worktree), direct/`.kilo` remain `scope: "local"`; source path, plugin directory derivation, dedup/overrides and precedence preserved.

## Artifact Metadata

| Field | Value |
|---|---|
| Title | P4.4-T19 runtime/source-removal evidence — cumulative T13-T18 |
| Date | 2026-08-26 |
| Operator | `jorkeyliu` |
| Baseline commit (HEAD) | `98a9d1d6e6` (`refactor(opencode): make ConfigPaths global-only`) |
| Worktree | clean at collection (`gitDirty=false` in benchmark provenance) |
| P4.4 status | **Active** — per LOCK-001; zero-active exit not met |
| P4.5 status | Open — out of scope per LOCK-002 |

## Purpose and Bound

Produce reproducible P4.4 evidence for the cumulative T13-T18 removed configuration readers:

- prove that no removed reader initializes in the effective-config path (`Config.Service` canonical loader) — proven by focused static/runtime tests, not by benchmark scenarios
- record descriptive backend HTTP/SSE/AppLayer runtime observations where the existing harness can produce reliable results — scenarios 6/12 are backend-only observations and do not execute TUI or ConfigPaths code paths
- preserve LOCK-002/003/004/005 and all T18 production behavior
- benchmark scenarios provide backend startup/resource timing context only; they do not cover or prove TUI/ConfigPaths reader removal and do not attribute values to T13-T18 or T18 removal

Non-goals: no Config.Service, instruction, transport, SDK, provider, daemon, P4.5, generated-SDK, or P5 work; no numeric threshold; no phase-completion claim.

## Locked Decisions Honored

| ID | Statement |
|---|---|
| LOCK-001 | P4.4 remains Active; T19 does not mark P4.4 or any 13-row source class complete unless zero-active exit is met |
| LOCK-002 | No expansion into P4.5, Config.Service behavior, instruction behavior, transport, SDK, provider, daemon |
| LOCK-003 | Historical at T19: `ConfigPaths.directories()` global-only, TUI sole `KILO_CONFIG_DIR` reader last-wins — superseded in P4.4-T27. Current (T27-T30): `ConfigPaths.directories()` global-only; no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/ancestor/`.kilocode`/migration reader; TUI canonical Git worktree root (`resolveWorktree`/`canonicalRoot`) with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only, precedence global → direct → `.kilo` (see T27-T30 clarification) |
| LOCK-004 | `Global.Path.config` existing `KILO_CONFIG_DIR` override and sandbox deny-list remain untouched |
| LOCK-005 | P4 performance comparison is descriptive, same-environment, with med/p95/sample/provenance and no numeric pass threshold |

## Cumulative T13-T18 Removal Inventory (effective-config perspective)

| Unit | Removed reader / code path | Retained compatibility | Inactive proof location |
|---|---|---|---|
| T13 | `Flag.KILO_CONFIG` injection `if (Flag.KILO_CONFIG) files.push(Flag.KILO_CONFIG)` in `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts:148-158` | TUI theme/keybind/`tui.json` migration, `ConfigPaths` discovery, `Flag.KILO_DISABLE_PROJECT_CONFIG` gate | `p4-4-tui-migrate-kilo-config-removal.test.ts` static absence + `p4-3-cutover.test.ts` canonical `Config.Service` absence |
| T14 | `ConfigPaths.directories()` `targets: [".kilocode", ".kilo"]` narrowed to `targets: [".kilo"]` (both ancestor and home walks) | `ConfigPaths.files()`/`fileInDirectory()` unchanged; TUI-local `tui.ts:211-225` `targets: [".kilocode", ".kilo"]` preserved per P4.5-adjacent scope | `p4-3-cutover.test.ts:97-108,161-184` static `targets: [".kilo"]` only |
| T15 | Home `Global.Path.home` `.kilo` discovery block `...(yield* afs.up({ targets: [".kilo"], start: Global.Path.home, stop: Global.Path.home }))` removed | `Global.Path.config`/`Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG` preserved | `p4-3-cutover.test.ts` `not.toContain("Global.Path.home")` + single `targets: [".kilo"]` |
| T16 | Workspace-ancestor `afs.up({ targets: [".kilo"], start: directory, stop: worktree })` plus dead `...(!Flag.KILO_DISABLE_PROJECT_CONFIG ? [] : [])` and `void directory`/`void worktree` removed; signature `directories(): Effect<string[]>` | TUI-local `tui.ts:211-225` gated `yield* afs.up({ targets: [".kilocode", ".kilo"], start: ctx.directory })` preserved | `p4-3-cutover.test.ts` `not.toContain("afs.up")`/`not.toContain("targets: [")` in `config/paths.ts` |
| T17 | `session/instruction.ts` `Flag.KILO_CONFIG_DIR` profile fallback removed (`...(Flag.KILO_CONFIG_DIR ? [path.join(Flag.KILO_CONFIG_DIR, "AGENTS.md")] : [])` and `Flag.KILO_CONFIG_DIR ?? global.config`) | `path.join(global.config, "AGENTS.md")` + `path.join(global.home, ".claude", "CLAUDE.md")` preserved; `KILO_DISABLE_PROJECT_CONFIG` gating preserved | `instruction.test.ts` runtime + `p4-3-cutover.test.ts` `instruction no longer retains KILO_CONFIG_DIR` |
| T18 | `config/paths.ts:4,22-24` `Flag` import and `Flag.KILO_CONFIG_DIR` reader removed; `directories()` now `unique([Global.Path.config])` | Historical at T18: `tui.ts:207-221` `unique([...baseDirectories.filter(dir !== Flag.KILO_CONFIG_DIR), ...legacyProjectDirs, ...(Flag.KILO_CONFIG_DIR ? [Flag.KILO_CONFIG_DIR] : [])])` last-wins preserved — superseded in P4.4-T27; Current (T27-T30): no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/ancestor/`.kilocode`/migration reader; TUI canonical root `resolveWorktree`/`canonicalRoot` with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only; `Global.Path.config` override and sandbox deny-list untouched (not effective TUI source) | `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` runtime proof `ConfigPaths.directories()` global-only even with `KILO_CONFIG_DIR` set |

Historical at T19: All six removals together leave `ConfigPaths.directories()` global-only; the only `KILO_CONFIG_DIR` direct reader was `cli/cmd/tui/config/tui.ts` (last-wins). Superseded in P4.4-T27 — Current (T27-T30): `ConfigPaths.directories()` global-only; no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/ancestor/`.kilocode`/migration reader remains; TUI uses canonical Git worktree root (`resolveWorktree`/`canonicalRoot`) with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only. `Global.Path.config` override in `packages/core/src/global.ts:79` (`Flag.KILO_CONFIG_DIR ?? Path.config`) and sandbox deny-list in `packages/opencode/src/kilocode/sandbox/policy.ts` remain as compatibility/safety per LOCK-003/004; neither is an effective-config source read (TUI retained reader claim historical, superseded T27).

## Inactive-Reader Conclusion (explicit)

No removed reader initializes in the effective-config path after T18:

- `Config.Service` canonical loader (`packages/opencode/src/config/config.ts` + `kilocode/config/config.ts` + `overlay.ts`) contains no `Flag.KILO_CONFIG`, `Flag.KILO_CONFIG_DIR`, `Flag.KILO_PERMISSION`, `KILO_CONFIG_CONTENT`, `ConfigPaths.files`/`directories`, `primaryPaths`, `.kilocode`/`.opencode`, or legacy filename readers — proven by `p4-3-cutover.test.ts` absence anchors (13 assertions) and runtime canonical-constants check (`KILO_CONFIG_FILES = ["kilo.jsonc"]`, `KILO_DIR_SUFFIXES = [".kilo"]`).
- `ConfigPaths.directories()` contains no `Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`afs.up`/`targets:`/`Global.Path.home` — proven by static absence plus **runtime** `ConfigPaths.directories()` with `KILO_CONFIG_DIR` set returns exactly `[Global.Path.config]` (`p4-4-t18-config-paths-kilo-config-dir-removal.test.ts:167-198`, 9 pass/75 expects).
- `session/instruction.ts` contains no `Flag.KILO_CONFIG_DIR`/`KILO_CONFIG_DIR` — proven by `p4-3-cutover.test.ts` and `instruction.test.ts` behavioral suites.
- `cli/cmd/tui/config/tui-migrate.ts` contains no `Flag.KILO_CONFIG` — proven by `p4-4-tui-migrate-kilo-config-removal.test.ts`.

Historical at T19: TUI retained `Flag.KILO_CONFIG_DIR` as sole explicit compatibility reader with precedence global → legacy → `KILO_CONFIG_DIR` last-wins; isolated from `Config.Service`. Superseded in P4.4-T27 — Current (T27-T30): no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/ancestor/`.kilocode`/migration reader; TUI canonical root `resolveWorktree`/`canonicalRoot` with global → direct → `.kilo` precedence; `Global.Path.config` override and sandbox deny-list retained per LOCK-003 (not effective TUI source).

## Benchmark Harness Selection

Scenarios 6 and 12 are backend `Server.listen`/`AppLayer` HTTP/SSE observations only. They do not execute TUI or ConfigPaths code paths and do not prove inactive-reader or compatibility claims; those claims are proven by focused tests (see Inactive-Reader Conclusion and Durable Test Coverage Check). Scenarios were selected as the only existing backend harness scenarios that provide descriptive startup/resource timing context without modification; all performance results remain descriptive only per LOCK-005. No causal attribution to T13-T18 or T18 is made and no coverage of TUI/ConfigPaths reader removal is claimed.

| Selected | What it observes (backend HTTP/SSE/AppLayer only) |
|---|---|
| Scenario 6 `first-prompt` — cold instance boot → first assistant text | Observes cold boot to first assistant text via production `Server.listen`/`AppLayer` HTTP/SSE + loopback LLM in a run-owned git project dir with `test` provider fixture; records `config_load` + `instance_bootstrap` + `provider_state_init` timing as backend startup observations |
| Scenario 12 `cold-during-generation` — cold provider PATCH during held generation | Observes cold provider PATCH during held generation via production `Server.listen`/`AppLayer` HTTP/SSE + held LLM gate; records `patchMs` + `commitToConvergedMs` + `instance_bootstrap` reboot span (expects 2 spans: boot + reboot) as backend convergence observations |

Not selected and why:

| Not selected | Reason |
|---|---|
| Scenario 7 `large-stream` | Streaming bytes throughput, not config/startup |
| Scenario 8 `parallel-sessions` | Parallelism, not config path |
| Scenario 9 `permission-heavy` | Permission/tool round-trip, not config path |
| Scenario 11 `hot-config` | Hot PATCH never triggers cold rebuild; less relevant to removed cold path |
| Scenario 13 `burst-cold` | Burst coalescing already covered by scenario 12 single-cold; omitted to keep descriptive scope bounded |
| Extension Host / private-worker harnesses | Not backend-harness tier; not executed in this unit and not claimed — focused tests, not benchmark scenarios, are the inactive-reader/compatibility proof per LOCK-003/LOCK-005 |

## Commands (reproducible)

| Step | Command | CWD | Provenance captured |
|---|---|---|---|
| Targeted tests | `bun test test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts test/kilocode/p4-3-cutover.test.ts test/kilocode/p4-4-t16-tui-legacy-discovery.test.ts test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts test/kilocode/p4-4-managed-removal.test.ts test/kilocode/p4-4-primary-worktree-removal.test.ts` | `packages/opencode/` | — |
| Backend benchmark (selected) | `bun run script/p0-benchmark.ts --scenarios 6,12 --samples 5 --warmup 1 --out /tmp/p4-4-t19-backend-6-12.jsonl` | `packages/opencode/` | frozen before artifact creation; `env` on every `run`/`sample` line; raw JSONL versioned at `specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` (36 lines, 59430 bytes, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`, byte-identical to `/tmp` output) |
| Table padding | `bun run script/check-md-table-padding.ts` | repo root | — |
| Whitespace | `git diff --check` | repo root | — |
| Typecheck (spot) | `bun turbo typecheck` | repo root | — |

## Environment / Provenance

| Field | Value |
|---|---|
| OS | `darwin` 24.6.0 ARM64 |
| Arch | `arm64` |
| Node | `24.3.0` |
| Bun | `1.3.14` |
| Git HEAD (benchmark campaign) | `98a9d1d6e6` (`refactor(opencode): make ConfigPaths global-only`) |
| Git dirty (campaign) | `false` (clean worktree) |
| Git HEAD (at evidence write time) | `98a9d1d6e6` clean (`git status` empty per spec) |
| Boot description (harness) | `in-process production Server.listen/AppLayer HTTP/SSE path (same as kilo serve); process-level stages (listener) emit once per process; serve_cli_entry is CLI-process-tier and not emitted here` — backend HTTP/SSE/AppLayer observations only; does not execute TUI/ConfigPaths |
| Comparison baseline | `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` (git `7a768502dd` dirty, same harness/tech, samples=5 warmup=1) — descriptive same-harness comparison; drift `7a768502dd` dirty → `98a9d1d6e6` clean, same `darwin/arm64/bun 1.3.14` class noted explicitly; no threshold |
| Versioned raw output | `specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` — 36 lines, 59430 bytes, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`, byte-identical to `/tmp/p4-4-t19-backend-6-12.jsonl` via `cmp` |

## Benchmark Results (backend harness tier only)

Raw output versioned at `specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` (36 lines, 59430 bytes, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`; byte-identical to `/tmp/p4-4-t19-backend-6-12.jsonl`; 10 measured + 2 warmup samples, status `ok`, 0 failures). Summaries below are `kind: "summary"` lines (descriptive per-metric `n/min/median/p95/max/mean` with truthful unit). These are backend HTTP/SSE/AppLayer observations only and do not execute TUI/ConfigPaths; focused tests, not benchmark scenarios, are the inactive-reader/compatibility proof per LOCK-003. No causal attribution to T13-T18 or T18 is made.

### Scenario 6 `first-prompt` (cold boot → first text) — backend HTTP/SSE/AppLayer observation only

| Metric | Unit | n | min | median | p95 | max | mean | P0 baseline median/p95 (same harness, git 7a768502dd dirty) |
|---|---|---|---|---|---|---|---|---|
| `totalMs` | ms | 5 | 575 | 604 | 629 | 629 | 603.2 | 2628 / 2691 |
| `startToFirstDeltaMs` | ms | 5 | 398 | 416 | 436 | 436 | 413.8 | 2067 / 2166 |
| `firstDeltaToIdleMs` | ms | 5 | 173 | 193 | 200 | 200 | 189.4 | 490 / 594 |
| `processorEntryToIdleMs` | ms | 5 | 492 | 511 | 533 | 533 | 508.2 | 1740 / 1886 |
| `firstConfigLoadMs` | ms | 5 | 1 | 2 | 3 | 3 | 2 | 177 / 310 |
| `firstInstanceBootstrapMs` | ms | 5 | 3 | 3 | 4 | 4 | 3.4 | 209 / 318 |
| `firstProviderStateInitMs` | ms | 5 | 1 | 1 | 1 | 1 | 1 | 15 / 16 |
| `config_load` | count | 5 | 2 | 2 | 2 | 2 | 2 | 2 / 2 |
| `instance_bootstrap` | count | 5 | 1 | 1 | 1 | 1 | 1 | 1 / 1 |
| `provider_state_init` | count | 5 | 2 | 2 | 2 | 2 | 2 | 2 / 2 |

### Scenario 12 `cold-during-generation` (cold PATCH during held generation) — backend HTTP/SSE/AppLayer observation only

| Metric | Unit | n | min | median | p95 | max | mean | P0 baseline median/p95 |
|---|---|---|---|---|---|---|---|---|
| `totalMs` | ms | 5 | 929 | 998 | 1023 | 1023 | 986.6 | — (no direct prior single-metric; see baseline `commitToConvergedMs`) |
| `patchMs` | ms | 5 | 15 | 19 | 28 | 28 | 21 | 501 / 659 |
| `commitToConvergedMs` | ms | 5 | 258 | 284 | 554 | 554 | 332.2 | 1125 / 1833 |
| `convergedToFollowUpMs` | ms | 5 | 232 | 237 | 320 | 320 | 265.8 | 1379 / 1461 |
| `heldToReleaseMs` | ms | 5 | 143 | 440 | 480 | 480 | 388.6 | — |
| `instance_bootstrap` | count | 5 | 2 | 2 | 2 | 2 | 2 | 2 / 2 (boot + reboot) |
| `config_load` | count | 5 | 5 | 5 | 5 | 5 | 5 | — |
| `convergence_complete` | count | 5 | 1 | 1 | 1 | 1 | 1 | 1 / 1 |

Descriptive notes: all values are n=5 nearest-rank p95=max descriptive sample statistics per `test/benchmark/statistics.ts` — not a tail-latency SLA, no threshold, no causal attribution to T13-T18 or T18. Values are backend-only observations and do not prove TUI/ConfigPaths removal; same-environment comparison vs P0 baseline (`firstConfigLoadMs` 2 ms vs 177 ms median, `firstInstanceBootstrapMs` 3 ms vs 209 ms median, `totalMs` 604 ms vs 2628 ms) is descriptive only — drift (`7a768502dd` dirty → `98a9d1d6e6` clean), project fixture, and machine scheduling are uncontrolled — no improvement/regression or coverage claim is made per LOCK-005/LOCK-PERF-6/R7.

## Limitations and Explicit Not-Observed Dimensions

| Dimension | Status | Reason |
|---|---|---|
| Backend harness tier `Server.listen`/`AppLayer` HTTP/SSE | **Observed** | Scenarios 6,12 ran to completion with 0 failures, 10 measured samples, per-sample P0 capture isolation — backend observations only; does not execute TUI/ConfigPaths |
| `Extension Host` cold-start / warm-view / many-agent-MCP startup | **Not observed** | Not executed in this bounded unit; requires VS Code extension host harness (`packages/kilo-vscode/script/p0-bench/`) — not claimed |
| Private-worker / persisted-selector paint / `extensionDataReady` chain | **Not observed** | Out of scope per LOCK-002; no private worker measurement claimed |
| Model/network vs transport attribution | **Not separately observed** | Loopback LLM only; no per-request wire duration |
| Storage / retention metrics | **Not observed** | Storage cutover already evidenced at P4.2a; not rerun |
| Resource initialization counts beyond `config_load`/`instance_bootstrap`/`provider_state_init`/`processor_entry` | **Observed as counts** | Counts above; structural absence of removed-feature init remains proved by focused tests, not by benchmark scenarios |

## Durable Test Coverage Check

| Asserted inactive-reader claim | Existing runtime proof (focused tests, not benchmark scenarios) | New test needed? |
|---|---|---|
| `ConfigPaths.directories()` never initializes removed `.kilocode`/home/ancestor/`KILO_CONFIG_DIR` readers | `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` runtime test with `KILO_CONFIG_DIR` set → `[Global.Path.config]` | No |
| `Config.Service` never initializes legacy readers | `p4-3-cutover.test.ts` cutover + nested canonical-root + canonical constants runtime | No |
| `session/instruction.ts` never initializes `KILO_CONFIG_DIR` | `instruction.test.ts` behavioral + cutover absence | No |
| `tui-migrate.ts` never initializes `Flag.KILO_CONFIG` | Static `not.toContain("Flag.KILO_CONFIG")` — not in effective-config path; no runtime effective-config initialization to prove | No — bounded T13 scope is migration helper, not `Config.Service` |
| TUI `tui.ts` precedence global → legacy → `KILO_CONFIG_DIR` last-wins (historical at T18) | `p4-4-t16-tui-legacy-discovery.test.ts` 12 behavioral cases (historical T16-T18) — superseded T27; Current (T27-T30): canonical root `resolveWorktree`/`canonicalRoot`, global → direct → `.kilo`, no ancestor/`.kilocode`/`KILO_CONFIG_DIR` reader — see `p4-4-tui-canonical-source-removal.test.ts` + `p4-4-tui-cli-nested-canonical.test.ts` + `p4-4-tui-config-console-nested.test.ts` + `p4-4-tui-attach-canonical.test.ts` | No — historical |

No new regression test was added — all inactive-reader claims already have executable runtime or behavioral proof via focused tests; benchmark scenarios do not prove inactive-reader claims and adding a synthetic test would duplicate existing coverage — not required by the task's "only when needed" rule.

## Verification

| Command | Result |
|---|---|
| `bun test test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts test/kilocode/p4-3-cutover.test.ts test/kilocode/p4-4-t16-tui-legacy-discovery.test.ts test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts test/kilocode/p4-4-managed-removal.test.ts test/kilocode/p4-4-primary-worktree-removal.test.ts` (from `packages/opencode/`) | PASS — 54 pass / 0 fail / 493 expects |
| `bun run script/p0-benchmark.ts --scenarios 6,12 --samples 5 --warmup 1 --out /tmp/p4-4-t19-backend-6-12.jsonl` (from `packages/opencode/`) | PASS — status `ok` samples 10 failures 0 warmupFailures 0 — see `kind: "run"` provenance lines above; versioned copy `specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` byte-identical (36 lines, 59430 bytes, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`) via `cmp`/`shasum` |
| `bun run script/check-md-table-padding.ts` (from repo root) | PASS |
| `git diff --check` (from repo root) | PASS — no whitespace errors |
| `cmp /tmp/p4-4-t19-backend-6-12.jsonl specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl && shasum -a 256 specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` | PASS — byte-identical, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`, 59430 bytes, 36 lines |
| `bun turbo typecheck` (from repo root) | PASS (opencode + kilo-vscode; pre-existing only if any) |

## Remaining Risks

| Risk | Detail |
|---|---|
| Same-environment drift | P0 baseline vs T19 run differ by git commit/dirty (`7a768502dd` dirty → `98a9d1d6e6` clean); OS/bun class is same but wall-clock contention and `bun` scheduling remain uncontrolled — deltas are descriptive, not causal |
| Harness tier scope | Only backend `Server.listen`/`AppLayer` tier observed; Extension Host/private-worker evidence remains distinct and is not claimed here |
| Small n | n=5 medians with p95=max (nearest-rank) — descriptive only; no tail claim; burst/parallel dimensions not sampled |

## Commit-Ready Artifact Status

| Artifact | Commit-ready? | Notes |
|---|---|---|
| `specs/vscode-orchestrator/evidence/p4.4-t19-runtime-evidence.md` (this file) | Yes | Reproducible commands, provenance, samples, med/p95, limits, explicit inactive-reader conclusion (focused tests, not benchmark scenarios), no phase claim, backend-only observation scope |
| `specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` | Yes | Versioned raw JSONL — 36 lines, 59430 bytes, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`, byte-identical to `/tmp` output via `cmp` |
| `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` + `specs/vscode-orchestrator/migration-tracker.md` updates for T19 | Yes — to be applied in same local work unit | P4.4 remains Active (LOCK-001); no row/phase newly marked complete |
| Production code | No change | Per scope — only evidence/tracker; harness executed without production mutation |

Unverified dimensions: Extension Host cold-start/warm-view/many-agent-MCP startup, private-worker lifecycle, model/network wire attribution, storage retention — all intentionally not measured in this bounded unit and must not be claimed as observed.

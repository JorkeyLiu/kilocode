# P4.4-G3-B9 — Session abort contract investigation (B9, G3 Active, investigation-only)

- **Scope:** Contract/evidence only for `session/abort`. No abort implementation, no new fd capability, no `handleSyncSession` change, no cutover.
- **Status:** G3 remains **Active**, Gate B remains **open**, SDK HTTP remains sole authority. This is B9 investigation-only.
- **Locked semantic:** Operation identity names one session cancellation request (canonical directory + sessionId + cancellation-request identity), not one generation. Authoritative outcome aggregates actually-affected generations, queued work, descendants, and event publication result; HTTP status alone does not decide.

## Locked direction

B9-local contract locks (`B9-LOCK-001..008`) below are investigation-only labels for this evidence pack. They do not override or reuse the repository-wide `LOCK-001..015` / `LOCK-PERF-*` meanings in `migration-tracker.md` §2.

| ID | B9 invariant (concise) |
|---|---|
| B9-LOCK-001 | SDK HTTP is sole abort authority; private never executes a second abort |
| B9-LOCK-002 | Private observer never auto-replays |
| B9-LOCK-003 | Request binds canonical directory + sessionId + cancellation-request identity |
| B9-LOCK-004 | After SDK completes, private is detached parity observation only |
| B9-LOCK-005 | Timeout/epoch/descriptor/private-transport-unknown never mutates client stream/session state |
| B9-LOCK-006 | Outcome aggregates generation, queued work, descendants, event publication; HTTP status insufficient |
| B9-LOCK-007 | Diagnostics scrubbed; no session/operation/prompt/tool/raw-error output |
| B9-LOCK-008 | This phase is contract/evidence only; no abort build, no fd capability, no `handleSyncSession` change, no cutover |

## Current CLI evidence

| Fact | Pointer |
|---|---|
| Abort handler is volatile `promptSvc.cancel` + `return true` (`Schema.Boolean`); no op identity, durable outcome, or scope check | `packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts:504-507` |
| Route `POST /session/:sessionID/abort`, success `Schema.Boolean`, error `BadRequest` only | `packages/opencode/src/server/routes/instance/httpapi/groups/session.ts:126`, `packages/opencode/src/server/routes/instance/httpapi/groups/session.ts:289-300` |
| SDK surface `POST /session/{sessionID}/abort` (path sessionID + query directory/workspace) | `packages/sdk/js/src/v2/gen/sdk.gen.ts:4376-4397` |
| `SessionPrompt.cancel` delegates to `KiloSessionPrompt.cancelTree` (stop queued work + subagents) | `packages/opencode/src/session/prompt.ts:194-197` |
| Cancel tree fans out to session + descendants via `sessions.children`: `KiloSessionPromptQueue.cancel` + `PlanFollowup.abort` + `abortIntakes` + per-session `state.cancel` | `packages/opencode/src/kilocode/session/prompt.ts:142-167` |
| Per-session intake interruption only (`entry.cancelled` + fiber interrupt, unbounded) | `packages/opencode/src/kilocode/session/prompt.ts:66-76` |
| Per-session runner cancel: background jobs + runner cancel, missing runner → set `idle` | `packages/opencode/src/session/run-state.ts:78-87` |
| Busy/idle/interrupt wiring is runner-local (`onIdle`/`onBusy`/`onInterrupt`); no durable abort outcome | `packages/opencode/src/session/run-state.ts:60-66` |
| Background-job cancel matches running jobs by session/parent session only; no generation/queue/event aggregation | `packages/opencode/src/session/run-state.ts:120-152` |

## Current VS Code evidence

| Fact | Pointer |
|---|---|
| Client abort is SDK-only `client.session.abort({ sessionID, directory })` | `packages/kilo-vscode/src/kilo-provider/abort.ts:63-65` |
| `SessionAbort.stop` fans out SDK abort across known + fallback directories (`Promise.allSettled`), returns `known`; failure logs only | `packages/kilo-vscode/src/kilo-provider/abort.ts:25-39` |
| `SessionAbort.observe` tracks busy/idle per directory only; no outcome, no replay | `packages/kilo-vscode/src/kilo-provider/abort.ts:7-23` |
| `abortSessions`/`stopSession` route through `aborts.stop(client, sid, workspaceDir)`; no private carrier | `packages/kilo-vscode/src/KiloProvider.ts:5290-5313` |
| `handleAbort` sets `idle` + flushes streams + posts `sessionTurnClosed`/`sessionStatus` on stop success only | `packages/kilo-vscode/src/KiloProvider.ts:5315-5322` |
| `handleSyncSession` is SDK-first get/messages sync; unchanged by abort work (no abort logic added) | `packages/kilo-vscode/src/KiloProvider.ts:2872-2890` |
| No private `session/abort` fd3/fd4 carrier exists; B5-B8 carriers cover status/get/messages/children only | `packages/opencode/test/kilocode/server/fd-carrier-children.test.ts`, `packages/kilo-vscode/src/services/cli-backend/server-manager-b8-integration.test.ts` |

## Gate B status

| Fact | Pointer |
|---|---|
| `session/abort` is `named — Gate B open` prospective row; current tree stays volatile `200 true` with no identity/outcome/scope; no private abort capability; Gate B open | `specs/vscode-orchestrator/evidence/p4.4-g3-private-request-schema-draft.md:288` |
| `session/abort` matrix row binds `packages/kilo-vscode/src/kilo-provider/abort.ts:63-65` to `session/abort` §4.2 as `named — Gate B open` | `specs/vscode-orchestrator/evidence/p4.4-g3-private-request-schema-draft.md:353` |
| Any SDK-backed route beyond the matrix is an explicit Gate B blocker requiring a matrix row | `specs/vscode-orchestrator/evidence/p4.4-g3-private-request-schema-draft.md:397-399` |

## Lifecycle matrix (current tree vs contract)

| Case | Current-tree behavior | Contract requirement (B9-LOCK) |
|---|---|---|
| success | `cancelTree` interrupts intakes/runners/jobs, returns volatile `true`; no per-target disposition | Aggregate actually-interrupted generations + queued work + descendants + event publication into one terminal outcome |
| already terminal (session cancellation scope) | Missing runner → set `idle`, return `true`; indistinguishable from real cancel | Idempotent no-op returning the existing terminal outcome, not fresh `true` |
| missing session | No `requireSession` in abort handler; cancel proceeds on unknown id | `session.not_found`-class typed failure with scope binding, no side effect |
| timeout | No timeout/typed outcome defined; the static investigation does not prove the exact caller-visible symptom | Typed timeout outcome per B9-LOCK-005; never mutates stream/session state |
| cancel miss (busy race) | `assertNotBusy`/`RunnerBusy` path is prompt-side; abort reports `true` regardless | Outcome reports the missed generation as not-affected, not success |
| epoch drift | No epoch check in abort path | Stale-epoch request fails closed without state change |
| descriptor close | No descriptor/lease concept in abort path | Closed-descriptor request fails closed without state change |
| peer replacement | Client fans out to all known dirs; replacement backend gets a fresh abort, not identity-bound | Cancellation-request identity binds one backend epoch; replacement needs a new request identity |
| private transport unknown | No private abort path today, so nothing to misreport | When a carrier exists: warn-only detached observation per B9-LOCK-002/B9-LOCK-004/B9-LOCK-005 |

## Open blockers

- No cancellation-request identity, durable outcome record, or scope validation exists in the abort path.
- Busy event ordering and partial-cancellation outcome are not proven by the current tree (runner-local busy/idle only).
- Multi-directory fan-out has no single authoritative outcome aggregation point.
- Private abort carrier is undefined by design in this phase (B9-LOCK-001/B9-LOCK-004).

## Non-claims

- No claim of busy HTTP/SSE parity passing for abort.
- No new fd capability or wire schema.
- No `handleSyncSession` behavior change.
- No cutover and no Gate B/C/D or P4.4-G3 closure.
- P2 runtime evidence is historical only, not current-tree abort PASS.
- B9-P2 (2026-09-04): HTTP receipt is not cancellation proof; `session.not_found` / terminal no-op / `not_affected` typed dispositions and per-target observability above are contract only, not implemented behavior.

## B9-P2 wire outcome contract closure (2026-09-04, docs-only)

B9-P2 只做 abort oracle 与合同闭合，不新增 private carrier，不修改 `handleAbort`、FD capabilities、现有 `cancelTree` 行为（B9-LOCK-001..008 保持不变；Gate B 仍 open，G3 仍 Active）。

| Wire decision | 合同语义（prospective，未实现） |
|---|---|
| Request receipt ≠ terminal convergence | HTTP response（当前 volatile `200 true`，未来 typed ack）只是 request receipt，不是取消成功证明；receipt 不声称取消已完成。Terminal outcome 需经 runtime/SSE/persistence convergence 另行确认。Receipt 可先返回 transport receipt `accepted`/`accepted-unresolved` 非终端状态（`ambiguous` 仅 runtime semantic terminal outcome；`transport-unknown` 仅 delivery uncertainty），最终 outcome 以 runtime 收敛为准。 |
| opId / request identity | Cancellation-request `opId` 标识一次 directory + session + request identity 的取消请求（canonical directory + sessionId + cancellation-request identity）。既有 generation operation records（R11 identity 形式）只作为 affected-set association，不新增 generation kind，不作为 request identity。 |
| Missing session | Typed `session.not_found`-class failure，无 side effect；scope 绑定失败不扩散。当前 handler 无 `requireSession`、直接 cancel（`handlers/session.ts:504-507`），属实现差距，非目标语义。 |
| Terminal session | Idempotent terminal no-op：返回既有 terminal outcome，不创建新的 cancellation success。当前 tree 的 missing-runner → `idle` + `true`（`run-state.ts:78-87`）与真实取消不可区分，属实现差距。 |
| Cancel miss | Affected-set 中记为 `not_affected`，不能计为 success。当前 abort 无论 prompt 侧 `assertNotBusy`/`RunnerBusy` 如何都报 `true`，属实现差距。 |
| Per-target observability | Root / descendant / queued / intake / background / followup / event publication 必须分别可观察，各自 disposition 进入聚合 outcome。当前 `cancelTree`（`kilocode/session/prompt.ts:142-167`）为 `unbounded` + `discard` 无序扇出、无 per-target aggregation；queue cancel 是 version-based，intake 是 snapshot，followup sibling 可不在 descendants、新 runner 可重新准入—— race / late SSE / partial result 的顺序与聚合是实现前置证据要求（Gates C-D），不是已证明事实。 |
| Authority 不重叠 | SDK 是 abort command 的唯一 authority；SSE `session.status idle`（`KiloProvider.ts:5945-5959` 观察侧）只做收敛观察；private observer（如未来存在）仅 detached parity observation，不得发起第二次 abort、不得 replay。`KiloProvider.ts:5315-5322` 的 optimistic idle/flush/TurnClosed 只在 stop success 后执行，partial fan-out failure 保持 busy；无 abort timeout envelope；pending prompt recovery 与 abort 解耦。 |

Dynamic unknown（显式保留，不得当作已证明）：busy 下 HTTP → runner → persistence → SSE 时序 unknown（本环境真实 `bun dev serve` probe HTTP 无响应，busy HTTP→SSE/persistence 顺序保持 unknown）；SDK timeout / late SSE recovery 语义 unknown；multi-directory `allSettled`（`abort.ts:25-39`）partial failure 的权威聚合点 unknown。Volatile test 证明 idle/missing/sibling 均 `true` 且无 durable write——该静态证据不得伪装成 busy 实证。

## Route governance scan

| Check | Result |
|---|---|
| SDK route additions since B8 | None; last `groups/session.ts` change is `67ea9107ad` 2026-09-03 (B4 lane), before B8 parity work |
| B8-prior legacy routes as B9 additions | Excluded; no pre-B8 route counted as new |
| Declared session endpoints | 28 `HttpApiEndpoint.*` entries in `groups/session.ts`, unchanged by this docs-only phase |

## Verification

- Citation paths above read back present (handlers, groups, prompt, run-state, abort, KiloProvider, SDK gen, schema draft, B8 tests). `packages/kilo-vscode/src/kilo-provider/abort.ts:63-65` is the SDK-only abort call; `groups/session.ts` holds 28 `HttpApiEndpoint.*` entries (29 raw `HttpApiEndpoint` string hits include the import line).
- `git diff --check` — PASS, no whitespace errors (run at audit-fix time; see tracker provenance).
- B9-P2 provenance (2026-09-04, docs-only): wire closure rows above; volatile `handlers/session.ts:504-507` + `groups/session.ts:289-300` receipt, `cancelTree` `prompt.ts:142-167` unbounded/discard fan-out, `abort.ts:25-39,63-65` + `KiloProvider.ts:5315-5322` optimistic path + `KiloProvider.ts:5945-5959` SSE-idle observation read back present; busy HTTP→SSE/persistence ordering retained as explicit unknown.
- Markdown table padding check on all four changed docs — PASS via `bun run script/check-md-table-padding.ts` on this pack, the schema draft, `runtime-and-configuration-direction.md`, and `migration-tracker.md` (see tracker provenance).
- This untracked evidence file was checked separately from the tracked diff: `git diff --no-index --check /dev/null specs/vscode-orchestrator/evidence/p4.4-g3-b9-session-abort-contract-investigation.md` produced no whitespace diagnostics (empty output); exit 1 only signals the expected content difference between `/dev/null` and the target file, not a whitespace failure — so the whitespace check passes. Also read back for B9-LOCK namespace (no repository-wide `LOCK-001..008` reuse), no `target op ID` / single-generation wording, route count 28, full abort citation path, and no overclaim beyond static evidence.

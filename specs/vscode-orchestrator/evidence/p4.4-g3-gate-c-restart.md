# P4.4 G3 Gate C — Run-bound E2E seam and Darwin Host proof — P3.5 Complete, pure Agent Manager /2

> Status: **P3.5 Complete (2026-09-01) — historical /1 complete (Darwin, valid) + current /2 Agent Manager-only complete (2026-09-01 validated); Gates C/D/P4.4/G3 remain Active (preparation only, no cutover)**

Owner: Kilo VS Code Extension (`ServerManager`/`KiloConnectionService`/`ServePrivatePeer`) + CLI provider seam (`kilocode/config/e2e-provider.ts`).

One authority: `SessionUpdateDispatch` + `SessionOperation`; carrier `fd-carrier.ts` is replay-only. Historical /1 evidence (`kilo-gc-proof/1`) remains valid for SDK-authoritative title / SSE / worker-restart facts; its shared-backend / `openTab` part no longer constitutes current acceptance. Current product is Agent Manager-only (`kilo-gc-proof/2`), host in Primary/Secondary Sidebar or editor group does not change judgment.

## Gate

| Gate | Scope | Required artifact | Status |
|---|---|---|---|
| Gate C restart — historical /1 | Durable title via SDK authoritative + private replay, worker-restart and SSE epoch proof — shared-backend (Agent Manager + editor tab) | `rr-gc-proof.json` (redacted, schema `kilo-gc-proof/1`, scope `real-restart Gate C: shared-backend + SDK-authoritative title + SSE same-epoch + worker-restart new-epoch`) | Complete (historical) — preparation only; Gates remain Active |
| Gate C restart — current /2 (P3.5 Complete 2026-09-01) | SDK-authoritative title + SSE same-epoch + worker-restart new-epoch — Agent Manager-only (no TabPanel/`openTab`) | `rr-gc-proof.json` (redacted, schema `kilo-gc-proof/2` v2, sha256 `cd26b5f06e1e3a3cfdd70f3f8e8570486fa573dfde8f54000f902737e60e3bc8`) — `/tmp/kilo-rr-20260901-181450-8a3f7c9e` manifest `complete validated:true files:44 required:22 missing:0 malformed:0` | Complete 2026-09-01 — validated; Gates C/D/P4.4/G3 remain Active |

## E2E seam (internal, not config scope)

| Aspect | Behavior |
|---|---|
| Gate | `KILO_E2E_FIXTURE === "1"` exact, two package-local behaviorally equivalent predicates `isE2EFixtureEnabled()` — extension `packages/kilo-vscode/src/util/e2e-fixture.ts` and CLI `packages/opencode/src/kilocode/config/e2e-provider.ts`, behaviorally identical, not a cross-package import — in all fixture paths: extension command registration, `PrivateObservationService` testBridge, `ServerManager` collector/env, `KiloConnectionService` fixture methods, CLI provider seam |
| Scratch shape | Absolute, basename `kilo-e2e-*`, never `/tmp` itself, no `../` or null byte |
| Run binding | Launcher creates `scratch/e2e-marker.json` (`{v:1, fixtureId}`) before VS Code launch, `fixtureId = e2e-probe-<rand>`; `KILO_E2E_FIXTURE_ID` env carries same ID; `ServerManager.validatedE2EProviderEnv()` and `getE2EProviderFragment()` require marker exists as regular file, bounded read (<2 KiB), `realpath` relative check stays inside scratch, JSON `fixtureId` equals env, loopback `/v1` exact |
| Lifetime | Marker created by harness, survives child restarts and `reloadWindow`, cleaned by scratch owner (`rm -rf scratch`) only, no global glob |
| Public surface | No flag, no schema, no legacy discovery, no config file field; synthetic provider fragment is lowest-priority global that project canonical metadata merges over |

## Proof artifact

| Field | Source | Redaction | Historical /1 | Current /2 |
|---|---|---|---|---|
| `schema`, `version`, `scope`, `fixtureIdHash` (16 hex), `sessionIdHash`, `titleHash` | launcher fixtureId, `createHash(sha256).slice(16)` | no raw title, no path | `kilo-gc-proof/1`, `1`, `shared-backend + SDK-authoritative title + SSE same-epoch + worker-restart new-epoch` | `kilo-gc-proof/2`, `2`, `SDK-authoritative title + SSE same-epoch + worker-restart new-epoch` — Agent Manager-only, no `shared-backend` |
| `pre` backend+private `pid/port/epoch/protocol/capabilities` | `rr-private-status` | no secret | present | present — retained |
| `openTab` before/after (historical /1 only) | `rr-private-status` before/after `rr-open-tab` | no secret | present — shared-backend identity proof | absent — Agent Manager-only /2 has no `openTab`/`TabPanel`; validator rejects `openTab` key |
| `titleOp` `opIdHash`/`idempotencyKeyHash`/`requestIdHash`, `order: [sdk,private]`, `sdk.status`/`httpStatus`, `private.status`, `parity`, `revision` | `fixtureSessionUpdate` redacted hashes | no raw payload | present | present — retained |
| `replay` initial `revision` | `fixturePrivateReplay` | no mutation | present | present — retained |
| `sse` `pre`/`conn`/`post` `pid/port/epoch` `states` `connectedEvents` | `fixtureSseReconnect` + `privatePeerStatus` | — | present — SSE same-epoch | present — retained |
| `killed` `pid/port/epoch` , `postRestart` backend+private | `fixtureKillServer` + `privatePeerStatus` | exact owned kill | present | present — retained |
| `replayAfterRestart` `revision` same as initial | same-key replay after restart | proves replay-only | present | present — retained |
| `parity` top-level equals `titleOp.parity` | titleOp parity | — | present | present — retained |
| `collectedAt` | harness time | — | present | present |

Historical /1 staged via `e2e-evidence.ts` (`rr-gc-proof.json` required for `real-restart`); `rr-title-request` transient excluded from durable manifest; `parseFailure` validates schema/version/hashes and fails if absent/malformed. Current /2 validator (`kilo-gc-proof/2` v2) retains title/SSE/worker-restart/replay/parity/identity checks and drops `openTab` validation — validated 2026-09-01 with hashes below.

## Run and destination — current /2 (Agent Manager-only, P3.5 Complete 2026-09-01)

| Aspect | Value |
|---|---|
| Command | `KILO_E2E_SCENARIO=real-restart KILO_E2E_EVIDENCE_DIR=/tmp/kilo-rr-20260901-181450-8a3f7c9e bun run test:e2e:real-restart` (Node, not Bun, macOS Darwin) |
| Destination | `/tmp/kilo-rr-20260901-181450-8a3f7c9e` (run-owned handoff) |
| Started | `2026-09-01T10:14:55.400Z` |
| Completed | `2026-09-01T10:16:26.683Z` (91283 ms, probePid 88556, fixture `e2e-probe-922f071e`, scenario `real-restart`) |
| Manifest | `status: complete` `validated: true` `missing: 0` `malformed: 0` `finalizedAt: 2026-09-01T10:16:29.764Z` — 44 files, required 22 |
| `rr-gc-proof.json` | `cd26b5f06e1e3a3cfdd70f3f8e8570486fa573dfde8f54000f902737e60e3bc8` (3485 bytes, schema `kilo-gc-proof/2` v2, scope `SDK-authoritative title + SSE same-epoch + worker-restart new-epoch`) |
| `run.log` | `00609a194480f75296522d28e2b4e95b2d0384c33263ecf57a0bd53a119df752` (70082 bytes) |
| `canonical-archive-before/after.json` | `e35d1a85683cd99beeec9e08f739fdae45270e19135ae9c35974788814fda67c` (348 bytes each, 1 archive unchanged) |
| `canonical-gate.json` | `fc1ef1fb945382fd597211e9123fc4f8b610bf1e6d5576dcea1026284c90e364` (1056 bytes) |

Proof validates: pre `pid:89820 port:51036 epoch:1` (`kilo-private/1` `session/update`) → `killed` same → post `pid:90959 port:51147 epoch:2` (`kilo-private/1`), SSE same-epoch `connectedEvents:1`, `titleOp`/`replay`/`replayAfterRestart` `revision {session:32, config:0}` `parity:null`, `scope` Agent Manager-only no `openTab`. Historical /1 remains valid; this /2 appends pointer per LOCK-005.

## Darwin Extension Host proof boundary — historical /1 + current /2 result (P3.5 Complete)

| Boundary | What is proven (historical /1) | How (/1) | Current /2 result (P3.5 Complete 2026-09-01, validated) |
|---|---|---|---|
| Shared backend identity (historical) | Agent Manager + editor-tab `openInTab` share same `pid/port/epoch`/`kilo-private/1` + `session/update` | `requestPrivateStatus` before/after `requestOpenTab` | Not part of /2 — Agent Manager is sole surface; `pre` backend+private identity retained, no `openTab` |
| Backend identity (Agent Manager) | — | — | Proven — pre `pid:89820/port:51036/epoch:1` `kilo-private/1` `session/update` via `requestPrivateStatus` |
| SDK authoritative | `PATCH /session/:id` `sessionUpdate:<id>:<token>` → `succeeded` with `revision` | `fixtureSessionUpdate` SDK then private | Proven — SDK `PATCH` authoritative `revision 32/0` private replay-only |
| Private replay-only | same `opId`/`idempotencyKey`/`requestId` replays same `revision` without new mutation | `fixturePrivateReplay` pre- and post-restart | Proven — same-key pre `epoch:1` and post `epoch:2` both `succeeded` `revision 32/0` |
| SSE epoch | `SseReconnect` keeps same `pid/port/epoch`, ≥1 `server.connected` | `restartPhaseA` + `privatePeerStatus` | Proven — pre `pid:89820/port:51036/epoch:1` → conn `pid:89820/port:51036/epoch:1` `connectedEvents:1` |
| Worker restart | `SIGTERM` exact `-pid` via `ServerManager.killProcess`, new `pid/port/epoch` strictly greater, private re-negotiates `kilo-private/1`, durable title persists | `restartPhaseB` + `privatePeerStatus` + snapshot wait | Proven — killed `pid:89820` → post `pid:90959/port:51147/epoch:2` greater, `kilo-private/1` re-established |
| Title persistence | backend snapshot title equals `gcTitle` after each boundary | `snapshotClient.waitFor` | Proven — title `b4a7b5078c840366` persists across SSE and worker restart (`revision 32/0` both replays) |

Run via `KILO_E2E_SCENARIO=real-restart KILO_E2E_EVIDENCE_DIR=<absent empty dir> bun run test:e2e:real-restart` (Node, not Bun, macOS/Linux).

## Explicit non-goals / remains open

| Item | Status | Note |
|---|---|---|
| Panel close/reopen title convergence | Open | not proven in Gate C proof (lifecycle /2 covers pure AM; restart /2 does not) |
| Webview reload title convergence | Open | `reloadWindow` Phase C proven for session/artifact, not for title lane |
| Session switch title convergence | Open | not exercised in restart |
| Cross-directory isolation | Open | all Gate C ops use single `canonicalRoot` |
| Live mid-flight epoch drift `transportUnknown` | Open | unit epoch drift → ambiguous only |
| Physical crash recovery / `kill -9` | Open | only SIGTERM exact group |
| Linux/Windows fd3/fd4 production | Open | Darwin `server-manager-b2-integration` PASS only, others fail-closed |
| Gate C/D closure | Open | B2 preparation only, no cutover |
| Current /2 runtime evidence | Complete 2026-09-01 | `/tmp/kilo-rr-20260901-181450-8a3f7c9e` `rr-gc-proof.json cd26b5f0…` `run.log 00609a…` pre `89820/51036/1` → post `90959/51147/2`; Gates C/D/P4.4/G3 remain Active (see LOCK-004) |
| P3.5 bounded product phase | Complete 2026-09-01 | Lifecycle `b2dc5002…` + restart `cd26b5f0…` both validated; Gates C/D/P4.4/G3 remain Active (no transport cutover/cross-dir/crash/retention/epoch-drift/Linux/Windows closure) |

No second listener, no `Promise` facade, no new DB authority.

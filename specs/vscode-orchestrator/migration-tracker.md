# VS Code Agent Orchestrator - Migration Tracker

Current-state amendment (2026-08-27, P4.4 ModelCache residual removal — ModelCache service boundary deleted, LOCK-MODELCACHE-001) — Bounded P4.4 residual-removal package physically deletes `packages/opencode/src/provider/model-cache.ts` (boundary deleted, not replaced). `packages/opencode/src/effect/app-runtime.ts` no longer exposes `ModelCache`/`ModelCacheLayer` seam, `packages/opencode/src/server/routes/instance/httpapi/server.ts` `AppOptions` no longer exposes it, and callers `packages/opencode/src/kilocode/server/provider-auth-lifecycle.ts`, `packages/opencode/src/kilocode/server/custom-provider-save.ts`, `packages/opencode/src/kilocode/server/custom-provider-delete.ts`, `packages/opencode/src/kilocode/anaconda-desktop/service.ts` no longer reference `ModelCache`. Generic catalog `packages/opencode/src/kilocode/provider/models-api.json` (3.0 MB), `packages/core/src/models-dev.ts` (disk cache, fallback fetch, 60-minute refresh), `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS`, and `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel` remain per LOCK-006; HTTP/SSE/generated-SDK transport unchanged per LOCK-009. Evidence: `packages/opencode/test/kilocode/p4-4-model-cache-removal.test.ts` (file absent, no production `ModelCache`/`model-cache` reference, seams removed, catalog preserved, sorted test-profile `bundled < model-cache < provider`). P4.4 remains Active/residual; no row/phase closure claimed. Prior T7 (2026-08-26) description of `provider/model-cache.ts` as invalidation-only `clear` is historical and superseded by this deletion.

Current-state amendment (2026-08-27, P4.4 residual package — VS Code Open Config legacy diagnostic inventory cleanup, one cohesive unit) — Bounded P4.4 residual-removal package removes obsolete VS Code legacy configuration-source diagnostics from the Open Config workflow while preserving canonical config-opening behavior and `KILO_CONFIG_DIR` compatibility (P4.4 remains Active/residual; KILO_CONFIG_DIR → Global.Path.config compatibility and sandbox safety preserved; diagnostic inventory now shows only resolved global kilo.jsonc and <workspaceRoot>/.kilo/kilo.jsonc). Source: `packages/kilo-vscode/src/kilo-provider/config-file.ts` rewritten to enumerate only canonical `kilo.jsonc` at resolved global root (`process.env.KILO_CONFIG_DIR || XDG`/`XDG_CONFIG_HOME`) and workspace `.kilo/kilo.jsonc` (`Source = sourceGlobal|sourceLocal`, no `virtual`/`legacy`, no `.kilocode`/`.opencode`/`config.json`/`opencode.json`/`kilo.json` alternative filenames, no home/project ancestor scans, no `KILO_CONFIG`/`KILO_CONFIG_CONTENT`/`KILO_CONFIG_DIR` environment-source taxonomy beyond resolved global root); `packages/kilo-vscode/src/kilo-provider/open-config.ts` narrowed `Labels` to `sourceGlobal`/`sourceLocal` and removed `statusLoadedLegacy`/`legacy`/`virtual` paths; `packages/kilo-vscode/webview-ui/src/types/messages/webview-messages.ts` `OpenConfigFileRequest.labels` narrowed to `sourceGlobal`/`sourceLocal` plus `statusLoaded`/`statusNotLoaded`/`statusCreate`; `packages/kilo-vscode/webview-ui/src/components/settings/Settings.tsx` now posts only `sourceGlobal`/`sourceLocal` labels via `settings.config.source.global|local`; all 20 webview i18n dictionaries (`packages/kilo-vscode/webview-ui/src/i18n/*.ts`) removed `settings.config.status.loadedLegacy` and `settings.config.source.xdg|homeKilo|homeKilocode|homeOpencode|envFile|envDir|envContent|projectKilo|projectRoot|projectKilocode|projectOpencode` and added `settings.config.source.global`/`local` (e.g. en `Global config`/`Workspace .kilo config`). Tests: `packages/kilo-vscode/tests/unit/open-config.test.ts` rewritten to prove legacy sources absent, canonical global (XDG and `KILO_CONFIG_DIR` resolved root) and local `.kilo/kilo.jsonc` discoverable/creatable, `KILO_DISABLE_PROJECT_CONFIG` still marks not-loaded, and open/creation/error paths; new `packages/kilo-vscode/tests/unit/p4-4-open-config-legacy-absence.test.ts` proves file/message/i18n/sandbox preservation absence. No runtime `Config.Service`/`ConfigPaths`/`Global` loader, server routes, SDK/OpenAPI, transport, or Flag.KILO_CONFIG_DIR semantics changed. Docs: this tracker and `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` and `specs/vscode-orchestrator/p0-current-state-inventory.md` updated to record this cleanup; P4.4 and P5 remain Active/residual; no matrix row closure claimed.

Current-state amendment (2026-08-27, P4.4 residual package — dead TUI migration helper and unused KILO_TUI_CONFIG getter physically removed, one cohesive unit) — Bounded P4.4 residual-removal package physically removes the dead TUI legacy migration helper `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` (`migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`/`Filesystem.findUp`/`opencodeFiles`) which has had zero active callers since P4.4-T27 (`cli/cmd/tui/config/tui.ts` no longer imports/calls it) and the now-unused `Flag.KILO_TUI_CONFIG` getter (`get KILO_TUI_CONFIG() { return process.env["KILO_TUI_CONFIG"] }`) from `packages/core/src/flag/flag.ts` after inspection proving no production reader remains (canonical TUI loader is T27/T30 `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` global→direct→`.kilo`, non-Git fallback, LOCK-SOURCE/LOCK-R6). Preserves exactly canonical `KILO_CONFIG_DIR` compatibility bridge (`Flag.KILO_CONFIG_DIR` + `Global.Path.config` override + sandbox deny), `KILO_DISABLE_PROJECT_CONFIG` gating, `ConfigPaths.files`/`fileInDirectory` APIs, `theme.tsx` `[".kilocode",".kilo"]` literals, and HTTP/SSE/SDK global transport (`/global/event` and `/global/config` bridge) per LOCK-002/003/004. Tests: `p4-4-tui-migrate-kilo-config-removal.test.ts` rewritten to prove helper file absent (`existsSync` false) and `Flag.KILO_TUI_CONFIG` absent in Flag and in `opencode/src`/`core/src` while preserving canonical-loader and ConfigPaths/instruction/sandbox preservation; `p4-4-flag-legacy-getter-removal.test.ts` updated to prove `KILO_TUI_CONFIG` definition/reader absence (including flag file absence after `KILO_CONFIG_DIR` strip) and `tui-migrate.ts` absent; `p4-4-tui-canonical-source-removal.test.ts` updated to prove migrate file absent via `existsSync` and Flag getter absent; retained `config/tui.test.ts` `KILO_TUI_CONFIG` ignored canaries still pass (env ignored by canonical loader). Docs: matrix rows 5/12/13 updated to record physical helper deletion (rows remain residual/open per LOCK-001, no broader row completion); row 2 updated to record `KILO_TUI_CONFIG` getter deletion while `KILO_CONFIG_DIR` bridge retained; tracker §7 rows updated; source-links `https://app.kilo.ai/tui.json` entry removed via `script/extract-source-links.ts`. Changeset `p44-tui-migrate-residual-removal.md` patch for `@kilocode/cli` and `kilo-code`. P4.4 remains Active (residual); no phase or all-rows complete claimed; no new sources, no ancestor/`.kilocode`/env fallback.

Current-state amendment (2026-08-27, P4.4-T31) — server-side `/global/health` deprecated transport surface physically removed (LOCK-002 transport-narrowing, LOCK-003 bridge preserved). Bounded unit deletes `packages/opencode/src/server/routes/instance/httpapi/groups/global.ts` `GlobalHealth` schema, `health: "/global/health"` path, and `global.health` endpoint (now only `/global/event`, `/global/config`, `/global/dispose`, `/global/upgrade` remain); `packages/opencode/src/server/routes/instance/httpapi/handlers/global.ts` deletes health handler, `InstallationVersion` import, and `handle("health")` wiring (retain `event`/`configGet`/`configUpdate`/`dispose`/`upgrade` plus SSE `eventResponse` heartbeat/reconnect). Generated contracts regenerated via repo-prescribed `./script/generate.ts` (run twice idempotently): `packages/sdk/openapi.json` no longer exposes `/global/health`/`global.health`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`/`types.gen.ts` no longer expose `GlobalHealth`/`global.health`/`/global/health` while retaining `global.event`/`global.config`. Daemon liveness retargeted from authenticated `fetch .../global/health` with version parse to `fetch .../global/config` returning `{healthy:true, version:state.version}` and `status()` version-mismatch now compares stored `state.version` vs `InstallationVersion` (probe validates liveness only) — no replacement health poll introduced (SSE `GET /global/event` heartbeat/reconnect remains sole liveness path per LOCK-002). Debug workspace plugin `waitForHealth` retargeted from `/global/health` to `/global/event` (accepts 200 or 401). Tests/docs updated: `httpapi-exercise` health scenario removed, `httpapi-instance` no longer expects `/global/health`, `serve-process` smoke now probes `/global/config`, `daemon.test` probes `/global/config`, `httpapi-sdk` uses `global.config.get`/`global.event` not `global.health`, `TESTING.md` curl examples and `cli-runtime.md` Health row now reference `/global/config`/`/global/event`. Evidence: `packages/opencode/test/kilocode/p4-4-global-health-removal.test.ts` (14 tests proving absence across group/handler/OpenAPI/SDK/exercise/daemon/plugin plus SSE heartbeat/reconnect presence and retained `GET /api/health` v2 distinctness) and `packages/opencode/script/kilocode/test-profile.ts` sorted glob insertion `flag < global-health < managed`. P4.4 remains **Active** (residual transport/retained bridge/provider/storage rows stay open; no row marked complete), P4.5/P5 remain open, no phase completion or numeric performance claim (LOCK-PERF-6).

Current-state amendment (2026-08-27, P4.4-T30) — CLI TUI daemon and explicit-attach canonical Git worktree root (canonical file-authoritative, canonical label, no legacy fallback; supersedes residual nested-as-root on active attach lifecycles; supersedes stale T18/T19 TUI sole-reader and ConfigPaths.files API-removal claims). Final audit found `packages/opencode/src/kilocode/cli/cmd/tui/thread.ts` `KiloTuiThreadDaemon.attach()` still called `TuiConfig.get()` with raw `process.cwd()` (via `CurrentWorkingDirectory` default) and explicit `packages/opencode/src/cli/cmd/tui/attach.ts` `AttachCommand` did the same after `chdir` — nested Git directory could therefore become effective TUI config root on daemon-attached and explicit-attach lifecycles. T30 threads the existing canonical root to `TuiConfig` in `KiloTuiThreadDaemon.attach` (`Effect.provide(TuiConfig.defaultLayer.pipe(Layer.provide(Layer.succeed(CurrentWorkingDirectory, input.cwd))))`) and resolves/provides canonical root in explicit `AttachCommand` after `chdir` using the existing minimal mechanism (`resolveWorktree`/`canonicalRoot` with `Filesystem.resolve`, non-Git fallback to directory/`process.cwd()`): `const base = directory ?? Filesystem.resolve(process.cwd()); const worktree = await resolveWorktree(base); const canonicalForConfig = canonicalRoot(base, worktree)` then `Effect.provide(... CurrentWorkingDirectory, canonicalForConfig)` for `TuiConfig.get`. Preserved canonical global→root direct→root `.kilo` precedence, normal non-daemon T29 path, config-console T28 alignment, attach/daemon lifecycle behavior beyond root passed to config, no ancestor discovery/`.kilocode`/env/migration fallback, theme/global/provider/transport. Clarification: `ConfigPaths.files` API is retained (`files` and `fileInDirectory` remain for other consumers; only active TUI call-site `ConfigPaths.files("tui", ctx.directory)` ancestor walk removed in T27 and no active TUI call remains — TUI now uses direct `ConfigPaths.fileInDirectory(root, "tui")` and `ConfigPaths.fileInDirectory(root/.kilo, "tui")`); `ConfigPaths.directories` remains global-only `unique([Global.Path.config])`. Supersedes historical T18/T19 claims that TUI was the sole `KILO_CONFIG_DIR` reader with last-wins — no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate` reader remains after T27-correction; only `Global.Path.config` `Flag.KILO_CONFIG_DIR ?? Path.config` override and sandbox deny-list retained per compatibility (not effective TUI source). Regression: `packages/opencode/test/kilocode/p4-4-tui-attach-canonical.test.ts` (6 tests — daemon attach canonical root wins, daemon non-Git fallback, explicit attach canonical via shared `resolveWorktree`/`canonicalRoot` boundary, explicit non-Git fallback, static entrypoint canonical, retained `ConfigPaths.files` vs no active TUI call). Provenance now through T30 (T29 + T30); historical nested-as-root on attach lifecycles and T18/T19 sole-reader / `files` API removal claims superseded while provenance preserved. No P4.4 completion claimed; P4.4 remains Active (residual).


Current-state amendment (2026-08-27, P4.4 — final-audit correction: remote attach local canonical isolation, non-destructive test isolation, reload coverage) — Final audit found `attach.ts` derived canonical local TUI config root from remote `--dir` when `chdir` failed, new canonical TUI suites unlinked `${Global.Path.config}/tui.json[c]` without restoring user content, and nested config-console suite omitted POST reload. Correction preserves distinct local configuration base captured before attempting remote directory `chdir` (`const localBase = Filesystem.resolve(process.cwd())` before `process.chdir(args.dir)`; `resolveWorktree(localBase)` / `canonicalRoot(localBase, worktree)` only for config, `directory` remains session-context for `validateSession`/SDK), proves unavailable remote `--dir` cannot displace local canonical root via `p4-4-tui-attach-canonical.test.ts` remote-displacement test; replaces destructive global-config cleanup with isolated config root (`fs.mkdtemp` → `Global.Path.config = tmp` → `fs.rm(tmp)` only for test-created dirs, owned temp-dir lifecycle; safe content/existence restoration sufficient as original user file metadata never touched); updates T19/P4.3/matrix/tracker historical T18/T19 claims to superseded and states T27-T30 current behavior (canonical `resolveWorktree`/`canonicalRoot`, `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only, global → direct → `.kilo`, non-Git fallback to directory); adds nested PATCH → POST `/instance/reload` → GET coverage in default `Server.Default` app and production `Server.listen` path with owned `Listener` + `tmpdir`/`disposeAllInstances` cleanup. No source fallback, no provider/theme/global/sandbox change, no historical deletion, no untracked resources. P4.4 remains Active (residual).

Current-state amendment (2026-08-27, P4.4 — final-audit correction #2: explicit attach valid local --dir, docs historical, plugin/test lifecycle) — Supersedes residual current-looking T18/T19/T27 passages and stale T30 attach description (`const base = directory ?? Filesystem.resolve(process.cwd())`) which always used pre-chdir localBase even after valid local --dir succeeded. Current (LOCK-SOURCE) explicit attach honors successfully selected local directory as local TUI configuration root (`let selectedLocalDir; try { process.chdir(args.dir); selectedLocalDir = Filesystem.resolve(process.cwd()) } catch { return args.dir } ; const configBase = selectedLocalDir ?? localBase; await resolveAttachConfigRoot(configBase)`) while unavailable remote --dir remains session-only and retains caller local root for configuration (`directory` forwarding unchanged, no source fallback, global -> root direct -> root .kilo precedence, non-Git fallback to directory). Updates tracker/matrix T18/T19/T27 current-looking old behavior to explicitly historical/superseded while preserving history and P4.4 Active state. For plugin-bearing focused configs uses existing `markPluginDependenciesReady` on exact loaded canonical directory or awaits `TuiConfig.waitForDependencies` before disposal to prove no plugin install outlives fixture. Replaces empty cleanup catches with failure-safe observable strategy (logs via console/Log without masking assertions) and replaces unsafe `unknown as AttachArgs` double-cast with valid typed minimal args fixture. Removes unused production `dirs` alias constant in `cli/cmd/tui/config/tui.ts` (now direct `workspaceDirs` usage, `dirs: result.plugin?.length ? workspaceDirs : []`). Preserves remote session/SDK directory forwarding, canonical precedence/non-Git fallback, config-console, no user global file mutations, no source fallback, generated artifacts/source-links, no protocol/SDK break. P4.4 remains Active.

Current-state amendment (2026-08-27, P4.4 — T24 superseding clarification: TUI migration helper historical, superseded by T27-T30 canonical loader) — Clarifies P4.4-T24 (bounded TUI migration `backupAndStripLegacy` removal) as retained materialization only — `migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`/`Filesystem.findUp` still present in `cli/cmd/tui/config/tui-migrate.ts` but has **no active TUI caller after P4.4-T27** (`cli/cmd/tui/config/tui.ts` no longer imports/calls `migrateTuiConfig`, no `Flag.KILO_CONFIG`/`Flag.KILO_TUI_CONFIG`/ancestor/`.kilocode` readers). Historical T24 passages that describe migration as current effective TUI source are superseded; current (T27-T30) is canonical file-authoritative loader only — `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` with precedence global → direct → `.kilo`, non-Git fallback to directory, `ConfigPaths.files` ancestor walk removed from active TUI path (LOCK-SOURCE, LOCK-R6). Matrix rows 12/13 residues that cite "TUI migration helper remains" are retained as historical open residue (dead helper, not effective source) per P4.4 Active residual. No source fallback, no file deletion, no P4.4 completion claim.

Current-state amendment (2026-08-27, P4.4 — T13-T17/T19/T24 historical snapshot clarification and plugin-origin scope fix, LOCK-SOURCE/LOCK-R6) — Clarifies that dated P4.4-T13 through T17, T24, and T19 passages stating legacy discovery (`yield* afs.up({ targets: [".kilocode", ".kilo"] })`, `ConfigPaths.directories()` ancestor/home walks), `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG` last-wins, or `migrateTuiConfig`/`.kilocode`/`.kilo` migration as current active TUI behavior are **historical snapshots at those dates**, explicitly superseded by T27-T30 current canonical behavior: canonical global -> root direct -> root `.kilo` only (`resolveWorktree`/`canonicalRoot` via `Filesystem.resolve`, `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")`, no ancestor walk, no `.kilocode`, no `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig` readers, LOCK-SOURCE, LOCK-R6, no active legacy dual-read/import fallback). The TUI migration helper `cli/cmd/tui/config/tui-migrate.ts` (`migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`/`Filesystem.findUp`) remains as **physical/dead residue with no active TUI caller after P4.4-T27** (no import/call in `cli/cmd/tui/config/tui.ts`), not as current effective source. Plugin origin assembly now uses explicit source identity at merge time: plugin entries merged from global config are explicitly `scope: "global"` regardless of `Global.Path.config` physical location (even when `Global.Path.config` lies beneath the worktree), direct root and `.kilo` files keep `scope: "local"`; source path, plugin directory derivation, dedup/overrides and precedence preserved (LOCK-SOURCE). P4.4 remains Active (residual), no new sources, no old reader restoration.

Mutable progress and evidence tracker for the VS Code Agent Orchestrator
migration. Durable decisions stay in [ADR-0002](../adr/0002-focus-vscode-on-agent-orchestration.md)
and [ADR-0003](../adr/0003-replace-cli-configuration-with-private-gui-runtime.md);
product target and acceptance semantics stay in
[`agent-orchestration-direction.md`](agent-orchestration-direction.md); runtime
and configuration target and acceptance semantics stay in
[`runtime-and-configuration-direction.md`](runtime-and-configuration-direction.md).
This tracker owns changing phase status, evidence, blockers, issue/PR links, and
next actions.

## Session Bootstrap

A fresh session on this work continues from the durable artifacts, not from this
tracker alone: read [ADR-0002](../adr/0002-focus-vscode-on-agent-orchestration.md),
[ADR-0003](../adr/0003-replace-cli-configuration-with-private-gui-runtime.md),
[ADR-0004](../adr/0004-architecture-first-direct-reconstruction.md), and
[ADR-0005](../adr/0005-bounded-private-runtime-storage.md) (bounded
private-runtime canonical storage foundation; supersedes ADR-0001's
checkpoint + resync / multi-client transport target) for the locked decisions,
the [direction spec](agent-orchestration-direction.md) for product target,
ownership, and acceptance semantics, the
[runtime spec](runtime-and-configuration-direction.md) for runtime/config
semantics including the file-authoritative hybrid — the legal source taxonomy
(two canonical authored scopes: one global config root and
`<workspaceRoot>/.kilo/`), field registry, typed composition, materialization,
the bidirectional file-editing/WYSIWYG contract, permission composition, and
source-removal disposition (runtime spec sections 3, 5.1-5.4, 8.1) — plus the six normative startup acceptance criteria (runtime spec section 6) and the
runtime observation and hydration contract (runtime spec section 7.1), the
bounded Failure/Outcome/Recovery target (runtime spec section 7.2 — an
independently valuable P4.2 foundation, with post-reconstruction error maturity
separately scoped in section 7.3 and the R11-R14 bounded implementation
decisions in section 9), the storage foundation integration (ADR-0005; the
[storage spec](../storage/session-storage-rewriting.md) — current state,
operational containment, target model, P4.2a storage work units S0..S5,
cutover, gates/tests; R15-R17 bounded in runtime spec section 9), the
no-threshold performance comparison policy (R7
resolved 2026-08-14; runtime spec sections 9-10), and the
atomic legacy-reader cutover at P4.3 (no dual-read window, no import; runtime
spec sections 7, 9 R6). This tracker then supplies current progress truth: P0 is
Complete (2026-08-12) with recorded evidence; P1 is Complete (2026-08-14) with
recorded evidence (sections 4, 5, 8); P2 is Complete (2026-08-15) with
recorded evidence (sections 4, 5, 6, 8); P3 is Complete (2026-08-17) — P3.1
Complete (2026-08-15) with recorded evidence (sections 4, 5, 7, 8) — the
ordinary single-chat Activity Bar sidebar is removed (LOCK-001) with no numeric
adoption threshold, because P1/P2 supplied the migration affordances before
removal; P3.2 Complete (2026-08-16) with recorded evidence (sections 4, 5, 7,
8) — the managed worktree infrastructure and custom Diff Viewer/Diff Virtual
surfaces are permanently removed (LOCK-002), the orphaned `agent_manager`
list/prompt request-reply service is removed, root-local start delegation, the
H paths, H-12, and generic worktree/project/snapshot semantics are retained,
and P4.4's primary-worktree mirror-read removal explicitly remains out of P3.2
scope; P3.3 Complete (2026-08-16) with recorded evidence (sections 4, 5, 7, 8)
— cloud sessions, JetBrains, the local Console, and KiloClaw are permanently
removed (LOCK-003); P3.4 Complete (2026-08-17) with recorded evidence
(sections 4, 5, 6, 7, 8) — indexing/semantic search, project
memory/injection/tools, user-visible context management/compaction
(CLI/TUI/ACP/HTTP/SDK summarize+compact), and autocomplete/next-edit/chat
autocomplete/FIM/edit are permanently removed (LOCK-004); the invisible
automatic internal context-overflow safeguard (H-13) is retained (LOCK-005);
P4 is Active with P4.1 Complete (2026-08-18), P4.2a Active with S0 Complete
(2026-08-19), S1 Complete (2026-08-19), S2 Complete (2026-08-20 corrected), S3 Complete (2026-08-20), S4 Complete (2026-08-20), S5 Complete (2026-08-21) for the offline cutover work unit (archive integrity, fresh DB identity/zero gate, rollback rehearsal/recovery, lease/marker safety, focused production-path evidence), P4.2 H-10/H-11 post-cutover Extension Host five-boundary convergence Complete (2026-08-22) as the conjunction of real-restart transport reconnect + worker restart + window/reload and real-session session switch + panel close/reopen (validated=true/success=true), P4.2b-1 R1 scaffold Complete (2026-08-22) — additive private transport carrier (JSON-RPC 2.0 over stdio Content-Length, initialize handshake, bounded stderr, EOF/exit lifecycle; scaffold-only [Historical scaffold-only — superseded 2026-08-23 by production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease observer, managed bridge canonical KILO_DB via resolveCanonicalDbPath, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant)], no production wiring at that date — LOCK-009 bridge remains; R9, R11-R14 remain open, required for P4.2 exit per runtime spec section 9), P4.2b R9 wire foundation Complete (2026-08-22) — additive protocol foundation only over private carrier (versioned observation envelope without storage integration/private-worker lifecycle convergence; R9 Resolved 2026-08-23 pending that convergence; R11/R12/R13/R14 Resolved 2026-08-23; no production wiring at that date — Historical wire foundation, superseded 2026-08-23 by production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease observer, managed bridge canonical KILO_DB via resolveCanonicalDbPath, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant)), P4.2b standalone lease bootstrap Complete (2026-08-22; corrected 2026-08-22; packaging corrigendum 2026-08-22) — additive gated file-backed canonical DB via leased Database.layerFromPath + createChangefeedDeps [Historical leased — superseded 2026-08-23 by production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease observer, managed bridge canonical KILO_DB via resolveCanonicalDbPath, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant)]; P4.2b live canonical changefeed notification delivery Complete (2026-08-23) — bounded live `observation/changed` via test-only bridge reusing real Changefeed APIs over file-backed DB for test proof [Historical leased test bridge — superseded 2026-08-23; current production is enabled:true no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed, legacy bridge sole lease owner], isolated from production HTTP/SSE, lifetime cleanup on worker dispose/EOF/SIGINT/SIGTERM (process 'exit' not awaited; stale-PID recovery via Database.layerFromPath on next acquire [Historical leased recovery — superseded 2026-08-23; current production no-lease has no stale-lease recovery, legacy kilo serve remains sole lease owner]), idempotent cleanup with signal/stdin/interval listener removal, real-child lease/observation proof (direct rehydrate:true on cursor-ahead, sequential reacquire; lease exclusivity proven via Database.layerFromPath: sequential reacquire proven 2026-08-22, concurrent-owner contention and stale-PID crash-recovery proven 2026-08-23 via real child (second concurrent worker fails observably with peer closed while winner lease/PID and observation remain healthy; abrupt SIGKILL of exact-PID worker leaves stale lease recoverable, next real child reacquires same DB and observes valid empty/current cursor with rehydrate:true on cursor-ahead)); P4.2b R9-C1 private observation reconnect/reacquire bounded proof Complete (2026-08-23) — `PrivateObservationService.reconnect()` (dispose+reinit same canonical env, fail-closed, idempotent, exact cleanup; no production wiring, no full R9 claim); P4.2b R12 Failure/Outcome normalization foundation Complete and R12 Resolved (2026-08-23) — additive pure private-runtime failure module (CLI ↔ VS Code parity): sole runtime-owned normalization boundary, minimal 9-field schema without taxonomy freeze, field tiers (durable/diagnostic/panel-visible), runtime-side redaction inside normalize before persistence/projection, five-source cancellation provenance, versioned 1.0 panel envelope via runtime projection; no production wiring, no new store, no HTTP/SSE/SDK/session change; tests packages/opencode/test/private-worker/failure.test.ts + packages/kilo-vscode/tests/unit/failure.test.ts (39+39 tests, 146+146 assertions); audited 2026-08-23 ACCEPT (0 blockers); R11/R12/R13/R14 Resolved 2026-08-23; R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); packaging: default CJS lightweight (no `Effect`/`Database`/`bun:sqlite`) and standalone ESM `dist/private-worker/standalone-worker.mjs` (`conditions: ["node","import"]`, `node:sqlite`, bundled) with compile proof; R9 Resolved 2026-08-23 pending five-boundary lifecycle convergence; R11/R12/R13/R14 Resolved 2026-08-23, P4.3/P4.4/P4.5, legacy reader deletion/import, HTTP/SSE/SDK removal, real-user DB cutover remain pending, and P4.2b beyond R9 wire — standalone lease bootstrap [Historical leased, superseded], live canonical changefeed [Historical test bridge], private observation service with production enabled:true via resolveCanonicalDbPath/Database.layerNoLease (historical gate-off superseded), R9-C1 reconnect, R9-C2 cursor persistence, R9-C3 lifecycle triggers Complete 2026-08-23 as above [Historical gated phases now production no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed, legacy bridge sole lease owner]; R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); and P4.2b-P4.5 beyond P4.2b (P4.3/P4.4/P4.5) remain Not started, P4.3/P4.4/P4.5 pending; P4.2b private observation service Complete (2026-08-23) — additive extension-owned `PrivateObservationService` (`enabled:true` production no-lease via `resolveCanonicalDbPath`/`Database.layerNoLease` (historical `enabled:false` gate-off superseded 2026-08-23), one `PrivateWorkerHost`, `observation/snapshot|read|ack|subscribe` + `observation/changed` forwarding, `isAbsolute` + canonical `KILO_DB`); test: `private-observation-service.test.ts` 5 pass/98 assertions; R9 Resolved 2026-08-23; P4/P4.2 remain Active; R9 Resolved 2026-08-23; five-boundary convergence proven
Not started except those protocol foundations;
P5 remains Not started; R2 and R6 were revised by a
post-P0 user clarification on 2026-08-13 (section 9); R7 resolved 2026-08-14
(section 9); R10 resolved 2026-08-18 (section 9); R15-R17 resolved 2026-08-20, required by P4.2 and not P1-P3
blockers (section 9); Q1 (topic meaning) resolved 2026-08-14 (section 9);
Q2/Q4/R4 resolved 2026-08-15 (sidebar deprecation timing and no adoption
threshold; section 9); H-1..H-13 target-surface parity is proven at P2 on
the current migration bridge for the in-scope current-behavior capability
(section 6) — the P4-gated target contracts (typed-manifest validation,
restrictive policy-stack semantics, WYSIWYG editing) and final parity under
the private runtime remain P4 gates, and P2 proves no P3 removal or
performance improvement/regression. No
phase claims completion without objective evidence recorded in the sections
below. P3.4 no-new-candidate rationale: indexing UX, memory UX/tools, manual
compact/context UX, autocomplete/ghost text/next-edit, and commit-message UX
are inherent observable surfaces of the LOCK-004 closed-set permanent
removals; recording them as reconstruction candidates would incorrectly
reclassify permanent removal scope. Retained H-13 is not disabled.
Lifecycle-boundary convergence evidence is a per-phase acceptance input
(direction spec section 11), observed under the runtime observation contract
(runtime spec section 7.1): P1 records the extension-owned view boundaries
(panel close/reopen, reload, session switch) on the migration bridge; P2
records the complete five-boundary behavior (those three plus transport
reconnect and worker restart) on harness-parity flows — recorded 2026-08-15 in
the final durable P2 campaign (section 6, H-11); P3.4 re-proves H-13
retained parity on the post-removal surface (section 6); P4 records all five
against the private-worker observation surface. R9 (runtime spec section 9) is
the open bounded implementation decision required by P4.2 only — not a P1-P3
blocker; testing current
behavior at P1/P2 does not require deciding future private handshake mechanics.

## 1. Purpose And Maintenance Rules

Purpose:

- Record, per phase, the scope, status, exit checklist, and the objective evidence
  that gates completion (issue/PR/test/doc links).
- Keep the ADRs and the direction specs durable: they are not edited to record
  progress.
- Make the migration auditable over time: any reader can see what is done, what is
  proven, what is blocked, and what is next, including the removal inventory and
  the runtime/config migration evidence.

Maintenance rules:

- This tracker is the source of truth for phase status, evidence, blockers, issue/PR
  links, and next actions. The ADRs and the direction specs are not updated to
  record progress.
- No phase is marked `Complete` without objective exit evidence recorded here. A
  checkbox without a link/ID is not evidence.
- Status changes, evidence additions, metric updates, risk log entries, and change
  log entries for a given phase ship in the same change that the phase ships.
- Every entry records a date. Evidence fields use the formats: `issue: #NNN`,
  `PR: #NNN`, `test: <path>`, `doc: <path>`.
- When evidence is a link, the field holds the concrete issue/PR/test/doc
  reference; a bare "TBD" or "-" means no evidence exists.
- Open decisions carry an owner and a required-by phase (section 9); they stay open
  until the hub decides. No phase may exit while a decision it requires is open.
- Do not claim capability parity, baseline counts, removals, or reductions that
  have no recorded evidence.
- No performance claim is accepted without runtime evidence (LOCK-PERF-6); all
  performance metrics stay `Not proven` until measurements with evidence links
  are recorded here. Estimates are hypotheses, never acceptance claims.
- Removed features are never reclassified as deferred; a removal row's evidence
  categories must be filled for every category that exists for the item.
- The migration is principle-first, not inventory-first (LOCK-014): no
  exhaustive pre-migration feature list and no complete suspension registry are
  required before P1/P2/P4 begin; legacy features/behaviors are disposed just in
  time when implementation first touches them, and this tracker never pretends a
  feature is already disabled.
- The two closed sets are non-negotiable (LOCK-014): final required capabilities
  (ADR-0002 LOCK-005/007/008 via H-1..H-13 — LOCK-005's minimal internal
  overflow safeguard remains in the final-required set through H-13) and
  permanent removals (ADR-0002 LOCK-001/002/003/004/006 plus the
  non-preservation clause of LOCK-005 for the existing compaction
  implementation; runtime spec sections 8/8.1 legacy sources). Anything else has
  no default compatibility entitlement; old implementations may be directly
  removed, disabled, or discarded when they obstruct the target, and every
  intentional non-carry-forward — including a clearly-obsolete discard
  immediately assigned `drop` — records a reconstruction-candidate entry
  preserving user value/provenance/decision evidence.
- Reconstruction-candidate entries are evidence preservation only (LOCK-015):
  they never create a compatibility promise, implementation backlog, phase
  gate, or removal classification, and they never block P1-P4 exits. Before P5
  completion every pending entry receives `rebuild` or `drop`; optional rebuild
  implementation is separately scoped and does not block P5 unless promoted to
  an H invariant. Final H-1..H-13 parity is never waived by an entry or an
  observed-candidate note (LOCK-014).
- This tracker records progress and evidence; it does not restate spec content.
  Exit checklists link to their owning spec sections (direction spec, runtime
  spec) for criterion text and acceptance semantics.

## 2. Status Vocabulary

| Status | Meaning |
|---|---|
| Not started | No work and no evidence recorded for this phase |
| Active | Work is in progress; evidence accrues here as it lands |
| Blocked | Work is stalled on a decision, dependency, or issue recorded in section 10 |
| Complete | All exit criteria met and objective exit evidence recorded in this tracker |
| Deferred | Scope moved out of the current migration per a recorded decision |
| Reconstruction candidate | A registry entry recording a legacy feature/behavior actually disabled, removed, or intentionally not carried forward (LOCK-015). Evidence preservation only — never a compatibility promise, implementation backlog, phase gate, or removal classification |

## 3. Decision Locks

These are non-negotiable for every phase (full text in the direction spec, section
1.1, the runtime spec performance section (section 10) for LOCK-PERF-1..7, and
ADR-0002/0003).

| ID | Decision |
|---|---|
| LOCK-001 | The only product is the VS Code Agent Orchestrator. The ordinary single-chat sidebar is not co-equal and remains on the deprecation/removal path. |
| LOCK-002 | Remove all worktree infrastructure and all custom Diff Viewer surfaces. Native VS Code diff APIs may still be used for checkpoint review where needed. |
| LOCK-003 | Remove cloud sessions, JetBrains, Console, and KiloClaw completely; they are not deferred. |
| LOCK-004 | Remove indexing, semantic indexing/search integration, project memory, memory tools/system-prompt injection, user-visible context management/compaction settings, and autocomplete completely. |
| LOCK-005 | Retain a minimal internal context-overflow safeguard for long-running agents. It is an invisible harness reliability mechanism, not a user-facing context-management product. Do not require preserving the existing compaction implementation. |
| LOCK-006 | Retain only user-defined/custom providers. Remove preset provider identities/catalogs, bundled gateway/provider onboarding/auth flows, the models.dev catalog dependency, and organization/cloud provider sources. Generic protocol adapters required to connect a user-defined provider may remain. |
| LOCK-007 | Retain checkpoint behavior defined as SessionRevert + Snapshot semantics: withdrawing/reverting a message restores affected code, with unrevert/cleanup and lifecycle correctness. Do not conflate this with the ADR-0005 canonical storage foundation (or ADR-0001's historical storage checkpoint/resync). |
| LOCK-008 | Preserve core harness capabilities: custom agents, sub-task delegation, extensible tools, skills, MCP, permissions/questions, parent-child sessions, background/parallel execution, user-selected custom-provider models, persistence, lifecycle correctness, checkpoint rollback, and internal context-overflow reliability. Worktrees are not a harness invariant. |
| LOCK-009 | Eliminate CLI/TUI/Console as products and public interfaces. Keep the agent runtime out of the VS Code Extension Host as an extension-owned private headless worker process for crash/resource/lifecycle isolation. The existing `kilo serve` HTTP/SSE/generated-SDK path may be a migration bridge, but it is not a target compatibility contract. The private transport remains an internal implementation choice. |
| LOCK-010 | GUI-managed file authority is authoritative (revised 2026-08-13: file-authoritative hybrid). All user-authored effective configuration is file-authoritative and WYSIWYG through the UI under exactly two canonical authored scopes — one global config root and `<workspaceRoot>/.kilo/` — with the field registry deciding global-only/project-only/both-with-typed-composition; the UI is a bidirectional editor/read model over canonical files/assets, not a separate config store. Product/UI configuration and persisted selector indexes are extension-owned (VS Code state is UI-local/derived only, never effective-config authority); secrets use VS Code SecretStorage; project-versioned harness assets use the one canonical explicit project boundary with no multi-source precedence merge; the runtime consumes immutable versioned snapshots. No migration/import tool and no dual-read compatibility window (runtime spec sections 3, 5.4, 7, 9). |
| LOCK-011 | A generation keeps the exact config/runtime snapshot it starts with. A configuration update atomically creates a new version for later generations and must not interrupt active generations. Resource replacement (provider/MCP/tool resources) is version-scoped/lazy and old resources are disposed only after owners release them; avoid process-global rebuild/convergence as the target model. |
| LOCK-012 | Model and agent selectors render from extension-owned persisted indexes before the private worker is ready. Runtime connection/validation is separate readiness and must not globally disable selection. Startup gates are action-specific, not one global `extensionDataReady` barrier. |
| LOCK-013 | Canonical architecture docs continue to describe implemented reality and are updated only as implementation lands. The mutable tracker records current migration evidence truthfully. |
| LOCK-014 | The migration is principle-first, not inventory-first (ADR-0004): no exhaustive pre-migration feature list and no complete suspension registry are required before P1/P2/P4 begin. Two closed sets are fixed: final required capabilities (ADR-0002 LOCK-005/007/008 operationalized by H-1..H-13 — user-level semantics hold at final target acceptance; existing implementations and uninterrupted intermediate availability are not invariants unless a phase explicitly requires them) and permanent removals (ADR-0002 LOCK-001/002/003/004/006, plus the non-preservation clause of LOCK-005 for the existing compaction implementation — LOCK-005's minimal internal overflow safeguard remains in the final-required set through H-13; runtime spec sections 8/8.1 legacy sources — never reclassified as deferred/suspended/rebuild candidates without a superseding ADR). Everything outside them has no default compatibility entitlement: when implementation first touches a legacy feature/behavior, the responsible phase directly migrates it, removes/disables the old implementation and records it as a reconstruction candidate, or discards it if clearly obsolete — every intentional non-carry-forward, including a clearly-obsolete discard immediately assigned `drop`, records a reconstruction-candidate entry; decided just in time, never through advance enumeration. H-1..H-13 are final target/parity gates, not a per-phase preservation obligation; intermediate phases keep the workspace testable, keep migration evidence truthful, and avoid corrupting persisted state, and may record temporary capability absence. No compatibility shim/adapter/dual implementation/old state owner is added solely to preserve a reconstruction candidate; existing compatibility code may be deleted when it obstructs the target. |
| LOCK-015 | A reconstruction-candidate entry is created only when an implemented behavior is actually disabled/removed or intentionally not carried forward (ADR-0004). It records user value/observable behavior, provenance, architectural obstruction, date/phase, whether it maps to a final invariant, post-foundation decision point, and disposition (`pending`, `rebuild`, or `drop`). Entries are evidence preservation — never a compatibility promise, implementation backlog, phase gate, or removal classification — and never block P1-P4 exits. Before P5 completion every pending entry receives `rebuild` or `drop`; optional rebuild implementation is separately scoped and does not block P5 unless promoted to an H invariant. Known examples may be recorded as observed candidates only when confirmed likely to be touched, never marked disabled today, and stay separate from the actual registry. |
| LOCK-PERF-1 | Performance simplification is a primary objective alongside product coherence. Remove redundant architecture from the hot path as a structural objective — eliminating redundant architecture, not per-phase optimization or a numeric improvement demand. |
| LOCK-PERF-2 | CLI/TUI/Console are not products. A private headless worker may remain for isolation; its startup and runtime costs must be measured. |
| LOCK-PERF-3 | Removed features must not contribute to startup/readiness: worktree/Diff Viewer, cloud sessions, JetBrains, Console, KiloClaw, indexing, memory, user-visible context management, autocomplete, preset provider catalog/onboarding. |
| LOCK-PERF-4 | Persisted custom-provider/model/agent choices must be renderable before worker readiness; action-specific readiness replaces global `extensionDataReady` gating. |
| LOCK-PERF-5 | Preserve harness semantics and performance correctness: custom agents, delegation, tools, skills, MCP, permissions/questions, parent-child/background/parallel sessions, persistence, SessionRevert+Snapshot rollback, invisible internal context-overflow safeguard. |
| LOCK-PERF-6 | No performance claim is accepted without runtime evidence. Static reachability identifies candidates; benchmarks/profiles establish magnitude. |
| LOCK-PERF-7 | Prompt submission/streaming transport on localhost is not presumed to be the dominant generation latency. Model/network/tool/user approval costs must be measured separately from transport/event overhead. |

## 4. Phase Overview

Current phase: **P4 Active (P4.1 Complete 2026-08-18; P4.2a Active — S0 Complete 2026-08-19, S1 Complete 2026-08-19, S2 Complete (2026-08-20 corrected), S3 Complete (2026-08-20), S4 Complete (2026-08-20), S5 Complete (2026-08-21) offline cutover work unit; P4.2 H-10/H-11 post-cutover Extension Host five-boundary convergence Complete (2026-08-22) as the conjunction of real-restart transport reconnect + worker restart + window/reload and real-session session switch + panel close/reopen (validated=true/success=true); P4.2b-1 R1 scaffold Complete (2026-08-22) scaffold-only — additive private transport carrier, no production wiring, LOCK-009 bridge remains, R9 remains Open (R11/R12/R13/R14 Resolved 2026-08-23); P4.2b R9 wire foundation Complete (2026-08-22) — protocol foundation only: versioned `1.0` `observation/snapshot|read|ack|subscribe` + `observation/changed` payload-free cursor/gap→rehydrate; R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); R11/R12/R13/R14 Resolved 2026-08-23; no production wiring at that date — Historical wire foundation, superseded 2026-08-23 by production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease observer, managed bridge canonical KILO_DB via resolveCanonicalDbPath, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant); no P4.2 complete claimed (still true, R9 Resolved 2026-08-23); P4.2b R9-C2 cursor persistence Complete (2026-08-23) — bounded, testable persisted-cursor abstraction, ack-persisted single integer, survives dispose/reinitialize, gap→rehydrate deterministic; R9 Resolved 2026-08-23 at that date — Historical R9-C2, superseded 2026-08-23 by R9-C3 production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant), full R9 still Open pending five-boundary convergence); P4.2b R9-C3 lifecycle triggers Complete (2026-08-23) — debounced singleflight coalesced panel/window/config/session/peer-closed → reconnect+read(persisted) gap→rehydrate, trailing ~150ms, no polling, production enabled:true no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed initialization, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant), R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); remaining private-runtime work (P4.3/P4.4/P4.5) remains pending)**; **P5 Not started** (P0 Complete 2026-08-12; P1 Complete 2026-08-14; P2 Complete 2026-08-15; P3.1 Complete 2026-08-15; P3.2 Complete 2026-08-16; P3.3 Complete 2026-08-16; P3.4 Complete 2026-08-17; P3 overall is Complete; P4.1 Complete 2026-08-18; P4.2b-1 scaffold Complete 2026-08-22; P4.2b R9 wire foundation Complete (protocol foundation only; R9 Resolved 2026-08-23) 2026-08-22; P4.2b live canonical changefeed notification delivery Complete (2026-08-23) — bounded live `observation/changed` via test-only bridge reusing real Changefeed APIs over file-backed canonical DB for test proof [Historical leased test bridge — superseded 2026-08-23; current production is enabled:true no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed, legacy bridge sole lease owner] (payload-free, monotonic cursor, read/ack, stale→rehydrate after truncation/eviction, sequential reacquire, no duplicate from prior host; run-owned temp XDG, exact PID/handle cleanup; CJS lightweight preserved, ESM standalone gated by KILO_PRIVATE_WORKER_TEST_BRIDGE=1 + KILO_PRIVATE_WORKER_STANDALONE=1 + absolute KILO_DB; historical leased bridge was test-only — current production is no-lease observer, LOCK-009 bridge remains for legacy; R9 Resolved 2026-08-23 (R11/R12/R13/R14 Resolved 2026-08-23); does not claim P4.2 complete); P4.2b R12 Failure/Outcome normalization foundation Complete and R12 Resolved (2026-08-23) — additive pure private-runtime failure module (CLI ↔ VS Code parity): sole runtime-owned normalization boundary, minimal 9-field schema without taxonomy freeze, field tiers (durable/diagnostic/panel-visible), runtime-side redaction inside normalize before persistence/projection, five-source cancellation provenance, versioned 1.0 panel envelope via runtime projection; no production wiring, no new store, no HTTP/SSE/SDK/session change; tests packages/opencode/test/private-worker/failure.test.ts + packages/kilo-vscode/tests/unit/failure.test.ts (39+39 tests, 146+146 assertions); audited 2026-08-23 ACCEPT (0 blockers); R11/R12/R13/R14 Resolved 2026-08-23; R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); P4.2b private observation service Complete (2026-08-23) — additive `PrivateObservationService` (`enabled:true` production no-lease via `resolveCanonicalDbPath`/`Database.layerNoLease` (historical `enabled:false` gate-off superseded 2026-08-23), one `PrivateWorkerHost`, `observation/snapshot|read|ack|subscribe` + `observation/changed` forwarding, `isAbsolute` + canonical `KILO_DB`); test: `private-observation-service.test.ts` 5 pass/98 assertions; P4.2b R9-C2 cursor persistence Complete (2026-08-23) — bounded `ObservationCursorStore` abstraction, ack-persisted single integer, survives dispose/reinitialize, gap→rehydrate deterministic; `private-observation-cursor.test.ts` 5 pass; P4.2b R9-C3 lifecycle triggers Complete (2026-08-23) — debounced singleflight triggers (panel visibility, window focus, config change, session switch, peer-closed) coalesced trailing ~150ms → reconnect+read(persisted) gap→rehydrate, no polling, production enabled:true no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant), R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); `private-observation-lifecycle.test.ts` 5 pass; `extension.ts` now `enabled:true` no-lease production (historical `enabled:false` superseded 2026-08-23); additive only, R9 Resolved 2026-08-23; P4/P4.2 remain Active; R9 Resolved 2026-08-23; five-boundary convergence proven; P4.3-P4.5 Not started).

| Phase | Title | Status | Exit evidence recorded | Notes |
|---|---|---|---|---|
| P0 | Baseline inventory | Complete (2026-08-12) | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md`; test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` (all H-1..H-13 executed as live tests; no gap records remain), `packages/opencode/test/kilocode/p0-instrument.test.ts`, `packages/opencode/test/kilocode/p0-bench-seed-config-schema.test.ts`, `packages/opencode/test/benchmark/`, `packages/kilo-vscode/script/p0-bench/`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/` (six formal accepted campaigns: Extension Host cold-start `2026-08-11T11-51-16-883Z/benchmark.jsonl`, Extension Host many-agent-MCP `many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl`, historical backend `2026-08-11T04-46-19-926Z/backend.jsonl`, Extension Host warm-view/no-provider/custom-provider `2026-08-12T05-52-07-672Z/benchmark.jsonl`, current-tier backend `2026-08-12T06-08-27-792Z/backend.jsonl`, Extension Host session-switch `2026-08-12T07-21-22-201Z/benchmark.jsonl`) | Static inventory, H-1..H-13 baseline fixture executing all 13 flows as live tests (parity still unproven), opt-in `KILO_P0_PERF` instrumentation, backend/extension benchmark harnesses, and six formal repeated baseline campaigns recorded as objective evidence with exact paths and scope caveats (two pre-fix partial artifacts retained but excluded: `2026-08-12T04-30-39-069Z/benchmark.jsonl`, `2026-08-12T06-05-12-356Z/backend.jsonl`); Q3 and R1/R2/R5/R6/R8 resolved and recorded (section 9); target-surface parity remains unproven, no performance threshold/SLA is claimed, and R7 was resolved on 2026-08-14 (no numeric performance thresholds; section 9); P0 marked Complete 2026-08-12 |
| P1 | Orchestration-first navigation | Complete (2026-08-14) | test: `packages/kilo-vscode/tests/unit/topics.test.ts`, `packages/kilo-vscode/tests/unit/agent-manager-arch.test.ts`, `packages/kilo-vscode/tests/unit/agent-manager-i18n.test.ts`, `packages/kilo-vscode/tests/unit/agent-manager-i18n-split.test.ts`, `packages/kilo-vscode/tests/topic-navigation.spec.ts` (Storybook Playwright interaction spec, 7 passed), `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (Extension Host `test:e2e:topic-navigation` and full `test:e2e` passed); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-14T11-45-17-512Z/benchmark.jsonl` | Gated on P0; Q1 resolved 2026-08-14 (derived Topic concept; section 9); Topic grouping/navigation implemented over the migration bridge as a derived read-model (`packages/kilo-vscode/webview-ui/agent-manager/topics.ts`) with no worktree dependency (LOCK-002); descriptive scenario-10 session-switch comparison recorded vs the P0 baseline (section 4); lifecycle-boundary convergence evidence recorded for the extension-owned view boundaries (session navigation, panel close/reopen, reload); no ordinary single-chat sidebar capability change; H-1..H-13 parity was Not proven at P1 (2026-08-14; proven at P2 2026-08-15, section 6); removal inventory unchanged (section 7); canonical architecture docs unchanged (LOCK-013); P1 marked Complete 2026-08-14 |
| P2 | Harness surface parity | Complete (2026-08-15) | test: `packages/kilo-vscode/script/e2e-probe.ts` (focused Extension Host E2E harness, fixture-gated via `KILO_E2E_FIXTURE`), `packages/kilo-vscode/script/e2e-evidence.ts`, `packages/kilo-vscode/script/e2e-pin.ts`, `packages/kilo-vscode/script/e2e-probe-dom.ts`, `packages/kilo-vscode/script/e2e-probe-restart.ts`, `packages/kilo-vscode/script/e2e-llm-matrix.ts`, `packages/kilo-vscode/script/e2e-completed-seed.ts`, `packages/kilo-vscode/script/e2e-restart-seed.ts`, `packages/kilo-vscode/script/e2e-scripted-model.ts`, `packages/kilo-vscode/src/agent-manager/fixture-backend.ts`, `packages/kilo-vscode/src/services/cli-backend/llm-request-collector.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/` (four complete validated manifests — 135 manifest entries covering 127 unique files, all 127 hash-verified — 28/28 requests e2e-local/e2e-model, 0 violations, 12/12 UI pins pass), `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T06-18-28-167Z/backend.jsonl` (scenarios 6/7/9, 18/18 ok) | Gated on P1; establishes target-surface product/harness behavior on the current migration bridge for the in-scope H-1..H-13 current-behavior capability (section 6), with the target-contract split keeping typed-manifest/permission-stack/WYSIWYG and final private-runtime parity at P4; all five lifecycle boundaries recorded on harness-parity flows (panel close/reopen, reload, session switch, SSE transport reconnect, worker restart/Extension Host restart); descriptive affected-path performance comparison recorded vs the P0 baseline with no threshold and no regression/improvement claim (R7; section 8); two pre-final P2-era evidence directories retained but excluded (contamination: external gateway title generation; copy-race: final-window artifacts not physically captured); no P3 removal, P4 private-runtime parity, LOCK-006 final provider-removal completeness, or performance improvement/regression claimed; P2 marked Complete 2026-08-15 |
| P3 | Product removal | Complete (2026-08-17) — P3.1 Complete (2026-08-15); P3.2 Complete (2026-08-16); P3.3 Complete (2026-08-16); P3.4 Complete (2026-08-17) | test: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts`, `packages/kilo-vscode/tests/unit/chat-target.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts`, `packages/kilo-vscode/script/e2e-probe.ts` (focused real Extension Host `test:e2e:sidebar-removal` passed); P3.2: `packages/kilo-vscode/tests/unit/worktree-removal.test.ts`, `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts`, `packages/opencode/test/kilocode/agent-manager-absence.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe-worktree.ts` (focused real Extension Host `test:e2e:worktree-removal` passed; final campaign `p3-2-final-2026-08-16T03-09-32-000Z/`); P3.3: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts`, `packages/opencode/test/kilocode/cloud-claw-removal.test.ts`, `packages/opencode/test/kilocode/console-removed.test.ts`, `packages/script/tests/jetbrains-removed.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (focused real Extension Host `test:e2e:cloud-claw-removal` passed; final campaign `p3-3-final-2026-08-16T11-10-35-000Z/`); P3.4: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (20 pass / 253 assertions), `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (28 pass / 381 assertions), real Extension Host `test:e2e:p3-4-removal` pass (forbidden manifest/runtime/bundle/state hits empty, 0 model requests, Open-in-Tab and Agent Manager ready, cleanup complete); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-4-final-2026-08-17T00-52-00-000Z/`, `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl`, `.changeset/p34-docs-update.md`; P3.1-P3.3: `specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`, `.changeset/remove-sidebar-chat.md`, `.changeset/agent-manager-root-local-sessions.md`, `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` | P3.1 (sidebar, LOCK-001) Complete 2026-08-15 with per-category removal evidence (section 7), rollback = git revert of the P3.1 removal commit(s) (no feature flag/shim), and a descriptive scenario-1 cold-start campaign vs P0 baseline (section 8); P3.2 (worktree/Diff Viewer, LOCK-002) Complete 2026-08-16 with per-category removal evidence (section 7), rollback = git revert, and a descriptive scenario-1 campaign; P3.3 (cloud/JetBrains/Console/KiloClaw, LOCK-003) Complete 2026-08-16 with per-category removal evidence (section 7), rollback = git revert, and a descriptive scenario-1 campaign; P3.4 (indexing/memory/context-management/autocomplete, LOCK-004) Complete 2026-08-17 with per-category removal evidence (section 7), rollback = git revert, structural absence of removed-feature initialization proved statically and at runtime, H-13 retained parity re-proved (section 6), accepted native scenario-1 campaign recorded (section 8), excluded partial wrapper campaign explicitly excluded (section 8), no performance improvement/regression claim (R7), no new reconstruction-candidate rows (LOCK-015), CLI build corrected (stale indexing-worker entry removed) |
| P4 | Private runtime and configuration | Active — P4.1 Complete (2026-08-18); P4.2a storage cutover work units S0 Complete 2026-08-19, S1 Complete 2026-08-19, S2 Complete (2026-08-20 corrected), S3 Complete (2026-08-20), S4 Complete (2026-08-20), S5 Complete (2026-08-21) offline cutover; P4.2 H-10/H-11 post-cutover Extension Host five-boundary convergence Complete (2026-08-22) as the conjunction of real-restart transport reconnect + worker restart + window/reload and real-session session switch + panel close/reopen (validated=true/success=true, 26+33 source files, durable 2026-08-22T09-00-53Z-p4.2-h10h11/); P4.2b-1 R1 scaffold Complete (2026-08-22) scaffold-only (additive JSON-RPC 2.0 over stdio Content-Length, initialize handshake, bounded stderr, EOF/exit lifecycle; no production wiring at that date [Historical scaffold-only — superseded 2026-08-23 by production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease observer, managed bridge canonical KILO_DB via resolveCanonicalDbPath, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant)] — LOCK-009 bridge remains; R9 remains Open (R11/R12/R13/R14 Resolved 2026-08-23), required for P4.2 exit); P4.2b R9 wire foundation Complete (2026-08-22) — protocol foundation only: versioned `1.0` `observation/snapshot | read | ack | subscribe` + `observation/changed` payload-free cursor/gap→rehydrate; R9 Resolved 2026-08-23 pending storage integration/private-worker lifecycle convergence; R11/R12/R13/R14 Resolved 2026-08-23; no production wiring at that date — Historical wire foundation, superseded 2026-08-23 by production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease observer, managed bridge canonical KILO_DB via resolveCanonicalDbPath, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant); no P4.2 complete claimed (still true, R9 Resolved 2026-08-23); five-boundary production observation convergence proven (still Open); P4.2b standalone lease bootstrap Complete (2026-08-22; corrected 2026-08-22; packaging corrigendum 2026-08-22) — additive gated file-backed canonical DB via leased Database.layerFromPath (lease/marker/identity) + createChangefeedDeps [Historical leased — superseded 2026-08-23 by production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease observer, managed bridge canonical KILO_DB via resolveCanonicalDbPath, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant)], isolated from production HTTP/SSE, lifetime cleanup on worker dispose/EOF/SIGINT/SIGTERM (process 'exit' not awaited; stale-PID recovery via Database.layerFromPath on next acquire [Historical leased recovery — superseded 2026-08-23; current production no-lease has no stale-lease recovery, legacy kilo serve remains sole lease owner]), idempotent cleanup with signal/stdin/interval listener removal, real-child proof (initialize + observation/snapshot/read/ack/subscribe over real child, empty cursor, direct rehydrate:true on cursor-ahead, InvalidParams on truly invalid cursor, lease held while alive, released on close/EOF, reacquired sequentially; lease exclusivity proven via Database.layerFromPath: sequential reacquire proven 2026-08-22, concurrent-owner contention and stale-PID crash-recovery proven 2026-08-23 via real child (second concurrent worker fails observably with peer closed while winner lease/PID and observation remain healthy; abrupt SIGKILL of exact-PID worker leaves stale lease recoverable, next real child reacquires same DB and observes valid empty/current cursor with rehydrate:true on cursor-ahead)); packaging: default CJS lightweight (18 KiB, no `bun:sqlite`) and standalone ESM `dist/private-worker/standalone-worker.mjs` (`conditions: ["node","import"]`, 927 KiB, `node:sqlite`) with `bun run compile` proof; full R9 still Open pending five-boundary lifecycle convergence; R11/R12/R13/R14 Resolved 2026-08-23; P4 remains Active); P4.2b live canonical changefeed notification delivery Complete (2026-08-23) — additive test-only bridge `test/mutateChangefeed` reusing real Changefeed APIs (Changefeed.append/appendWithCaps) over file-backed canonical DB via private stdio for test proof [Historical leased test bridge — superseded 2026-08-23; current production is enabled:true no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed, legacy bridge sole lease owner] `observation/changed` (payload-free `seq/session_id/revision/kind/time`, v=1.0, monotonic cursor, read/ack, stale→rehydrate after truncation/eviction with maxRows 1, sequential reacquire on same file, no duplicate from prior host, run-owned temp XDG, exact PID/handle cleanup, bounded waits); tests `packages/opencode/test/private-worker/live-changefeed-notification.test.ts` (2 pass: live notification + payload-free/wire gate) + `packages/kilo-vscode/tests/unit/live-changefeed-notification.test.ts` (3 pass via PrivateWorkerHost incl. lightweight gate); standalone ESM gated by `KILO_PRIVATE_WORKER_TEST_BRIDGE=1` + `KILO_PRIVATE_WORKER_STANDALONE=1` + absolute `KILO_DB`, no production wiring, LOCK-009 bridge remains, payload-free, bounded, gap→rehydrate preserved, no new DB tables/migrations, no polling/timer/unbounded queue, no Failure/Outcome/Recovery; R9 Resolved 2026-08-23 pending five-boundary lifecycle convergence, R11/R12/R13/R14 Resolved 2026-08-23, does not claim P4.2 complete; P4.2b R12 Failure/Outcome normalization foundation Complete and R12 Resolved (2026-08-23) — additive pure private-runtime failure module (CLI ↔ VS Code parity): sole runtime-owned normalization boundary, minimal 9-field schema without taxonomy freeze, field tiers (durable/diagnostic/panel-visible), runtime-side redaction inside normalize before persistence/projection, five-source cancellation provenance, versioned 1.0 panel envelope via runtime projection; no production wiring, no new store, no HTTP/SSE/SDK/session change; tests packages/opencode/test/private-worker/failure.test.ts + packages/kilo-vscode/tests/unit/failure.test.ts (39+39 tests, 146+146 assertions); audited 2026-08-23 ACCEPT (0 blockers); R11/R12/R13/R14 Resolved 2026-08-23; R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); P4.2b private observation service Complete (2026-08-23) — additive `PrivateObservationService` (`enabled:true` production no-lease via `resolveCanonicalDbPath`/`Database.layerNoLease` (historical `enabled:false` gate-off superseded 2026-08-23), one `PrivateWorkerHost`, `observation/snapshot | read | ack | subscribe` + `observation/changed` forwarding, `isAbsolute` + canonical `KILO_DB`); test: `private-observation-service.test.ts` 5 pass/98 assertions; P4.2b R9-C2 cursor persistence Complete (2026-08-23) — bounded `ObservationCursorStore` abstraction, ack-persisted single integer, survives dispose/reinitialize, gap→rehydrate deterministic; `private-observation-cursor.test.ts` 5 pass; P4.2b R9-C3 lifecycle triggers Complete (2026-08-23) — debounced singleflight triggers (panel visibility, window focus, config change, session switch, peer-closed) coalesced trailing ~150ms → reconnect+read(persisted) gap→rehydrate, no polling, production enabled:true no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed initialization, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant), R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); `private-observation-lifecycle.test.ts` 5 pass; `extension.ts` now `enabled:true` no-lease production (historical `enabled:false` superseded 2026-08-23); additive only, R9 Resolved 2026-08-23; P4/P4.2 remain Active; R9 Resolved 2026-08-23; five-boundary convergence proven; P4.3/P4.4/P4.5 Not started | P4.1: doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (canonical config section); test: `packages/kilo-vscode/tests/unit/config-audit-blockers.test.ts`, `config-audit-closure.test.ts`, `config-registry.test.ts`, `config-write.test.ts`, `config-service.test.ts`, and related config unit tests (606 focused tests pass); validation: `bun run typecheck`, `bun run lint`, `bun run knip`, `bun run compile`, `bun run script/check-architecture-impact.ts --worktree` (1 medium signal), `bun run script/check-md-table-padding.ts`, `bun run script/extract-source-links.ts`; audit: independent source audit PASS (0 findings); known environmental caveat: Playwright Chromium absent/timed out; full unit suite known baseline: 4805 pass, 4 fail, 1 error (happy-dom/Bun baseline). P4.2b R9 wire: `packages/opencode/src/private-worker/observation.ts` ↔ `packages/kilo-vscode/src/private-worker/observation.ts`, `packages/opencode/src/private-worker/peer.ts` + `packages/kilo-vscode/src/private-worker/peer.ts` (InvalidParams preservation for observation validation), test: `packages/kilo-vscode/tests/unit/observation.test.ts` (18 pass), `packages/opencode/test/private-worker/observation.test.ts` (17 pass) proving versioned envelope, snapshot cursor, contiguous read, gap→rehydrate, ack validation/delegation, subscribe/notification shape, malformed params, no EOF/pending impact; additive, no DB/migration/timer/production wiring, no Failure/Outcome/Recovery, LOCK-009 bridge untouched. P4.2a evidence: S0: test: `packages/opencode/test/storage/s0-baseline-measurement.test.ts` (29 pass); S1: test: `packages/core/test/session-revision.test.ts`, `packages/opencode/test/session/session-revision.test.ts`, `packages/opencode/test/kilocode/session-import-service.test.ts`, `packages/opencode/test/kilocode/server/session-import-http.test.ts`, `packages/opencode/test/project/project.test.ts`, `packages/opencode/test/cli/import.test.ts`, `packages/opencode/test/kilocode/storage/json-migration.test.ts`, `packages/core/test/database-migration.test.ts` (138+29 pass); S2: test: `packages/opencode/test/storage/s2-production.test.ts` (13 pass), `packages/opencode/test/storage/s2-retention-engine.test.ts` (6 pass), `packages/core/test/retention-s2.test.ts` (8 pass); verified: `bun test ./test/storage/s2-production.test.ts` (13 pass), `bun test ./test/storage/s2-retention-engine.test.ts` (6 pass), `bun test ./test/retention-s2.test.ts` (8 pass), `bun test ./test/storage/s0-baseline-measurement.test.ts` (29 pass), `bun test ./test/session/session-revision.test.ts ./test/kilocode/session-import-service.test.ts ./test/kilocode/server/session-import-http.test.ts ./test/project/project.test.ts ./test/cli/import.test.ts ./test/kilocode/storage/json-migration.test.ts` (138 pass), `bun test ./test/session-revision.test.ts ./test/database-migration.test.ts` (29 pass), `bun run typecheck` from `packages/core` and `packages/opencode` pass, `bun run lint` pass, `script/check-opencode-promise-facades.ts` pass, `script/check-architecture-impact.ts --worktree` pass (3 signals: high effect-boundary reviewed, no doc update; medium session-contract reviewed), `script/check-md-table-padding.ts` pass, `script/extract-source-links.ts` pass, `script/check-workflows.ts` pass; single-instance proof via `AppLayer` + `memoMap` (Database, Storage, Ownership, Lease, Accounting, Maintenance) identity; S2 diagnostics verified; Core and OpenCode typechecks pass. S0/S1/S2 implementation names and bounded evidence are recorded in the P4.2a checklist below. P4.2a now claims retention and registry via S2 (changefeed tombstone, obligation, family deletion, accounting, maintenance, auto-vacuum, diagnostics, registry), snapshot/registry boundary via S3 (Storage.write/update closed boundary including empty prefixes, AST writer/migration audit, snapshot project live-ref aware pruning from Session.revert and snapshot-bearing Part rows, missing/failing DB skips prune, family retention separate), and storage-side bounded changefeed via S4 (global monotonic seq ordering, per-revision exactly-one changed entry, payload-free tombstone with final revision+1 and no FK cascade, 50,000 rows/64 MiB hard caps with oldest-first eviction including unacknowledged, ack truncation, gap→rehydration on any invalid/evicted/truncated/interior-gap/missing-tail, idempotency via (session_id, revision, kind) with no seq gap, reconstruction independence, actual 50,001-row production cap and rerun, canonical reads intact); S4 Complete (2026-08-20), S5 Complete (2026-08-21) offline cutover (archive integrity, fresh DB identity/zero gate, rollback rehearsal/recovery, lease/marker safety, focused production-path evidence — core 52 pass, `cutover-s5-production.test.ts` 8 pass/97 expects, session-export/respawn 22 pass, typecheck 17/17, lint 0, architecture-impact 0, guards pass, audit PASS); H-10/H-11 pending before P4.2b; P4.2b-P4.5 remain Not started. S2 evidence: `packages/core/test/retention-s2.test.ts` (8 pass), `packages/opencode/test/storage/s2-retention-engine.test.ts` (6 pass); verified commands: `bun test ./test/retention-s2.test.ts` from `packages/core` (8 pass), `bun test ./test/storage/s2-retention-engine.test.ts` from `packages/opencode` (6 pass), plus S0/S1 commands above, `bun run typecheck` from both packages. R15-R17 resolved 2026-08-20. | Subphases P4.1-P4.5 (file-authoritative read/write model, private runtime entrypoint, legacy-reader cutover, removal evidence + transport narrowing, old CLI/server deletion); owned by runtime spec section 7. P4.1 Complete (2026-08-18): extension-owned CanonicalConfigService, two canonical authored scopes (global `<globalRoot>/kilo.jsonc` per `paths.ts` + project `<workspaceRoot>/.kilo/kilo.jsonc`), closed 11-field JSONC registry, typed asset directories (agent/command/skill/tool/plugin/rules), typed composition/materialization, atomic WYSIWYG writes/watchers/stale/invalid handling, SecretStorage opaque refs, derived selector indexes, canonical mode/readiness contract. P4.2a storage cutover work units S0 Baseline and fixtures Complete (2026-08-19); S1 Canonical aggregate schema and revision commit model Complete (2026-08-19); S2 Complete (2026-08-20 corrected); S3 Complete (2026-08-20); S4 Complete (2026-08-20); S5 Complete (2026-08-21) offline cutover; H-10/H-11 post-cutover Extension Host five-boundary convergence remains pending before P4.2b; P4.2b-P4.5 Not started. P4.2 additionally requires Failure/Outcome/Recovery contract evidence and R11-R14 |
| P5 | Startup and selector readiness | Not started | - | Startup acceptance per runtime spec section 6 (six normative criteria: selector availability independent of worker lifecycle; no readiness-based selection disable; reconciliation preserves selector availability; structural absence of `extensionDataReady` and the port/health/SSE/generated-SDK fetch chain after P4.4/P4.5; real Extension Host worker-withheld/killed/failed scenario plus descriptive paint-before-ready timing per R7; cold/warm UI-selector vs runtime-action readiness distinction). P5 additionally requires every pending reconstruction-candidate entry to receive `rebuild` or `drop`; optional rebuild implementation is separately scoped and does not block P5 unless promoted to an H invariant (LOCK-015) |

Current-state amendment (2026-08-25) — Phase Overview: P4.3 legacy-reader cutover is **Complete** (evidence `specs/vscode-orchestrator/evidence/p4.3-cutover-evidence.md`; pre-cutover `p4.3-pre-cutover-reconciliation-checklist.md` preserved). P4 overall remains Active; next gated work is P4.4 per-row evidence/transport narrowing and P4.5 old-surface deletion — both remain open and unchecked in P4 exit checklist and section 7 `Effective-config source removal evidence` rows. P5 remains Not started. Earlier `P4.3/P4.4/P4.5 pending` phrasing in Current phase and P4 table row above is superseded for P4.3; P4.4/P4.5 pending remains.

Current-state amendment (2026-08-25, P4.4 first bounded unit) — Static 13-row legacy effective-config source-removal evidence matrix and reader/call-site/inactive-proof inventory is complete in `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md`. The matrix records effective-config inactivity after P4.3 while preserving truthful residual/open status for diagnostic/TUI/instruction, SDK-wrapper, i18n, build/package, provider-catalog, convergence, storage, and transport machinery. P4.4 remains **Active**; transport narrowing and residual machinery/storage rows remain open. P4.5 and P5 remain open. This amendment is an evidence-documentation update only: the one implemented change it covers is the current working-tree extension importer/Roo removal (matrix row 13), implemented and not yet committed as of 2026-08-25, which touched extension source/webview/i18n surfaces; no generated-SDK, runtime effective-config, CLI/TUI migration helper, or transport artifact was changed by this evidence-correcting unit, and the remaining matrix rows stay open.

Current-state amendment (2026-08-25, P4.4-T1a) — the extension-side periodic `/global/health` poll is removed from `KiloConnectionService` (source: `packages/kilo-vscode/src/services/cli-backend/connection-service.ts` — `HEALTH_POLL_INTERVAL_MS`, `startHealthPoll`/`stopHealthPoll`/`checkHealth`, the `healthPollTimer` field, and every call site in `dispose()`/`resetConnection()`/the confirmed-connection path are gone). This is a bounded transport-narrowing unit only: `SdkSSEAdapter` heartbeat/reconnect remains the sole liveness path on the migration bridge (LOCK-009), and request transport (`createKiloClient`, SDK client) plus direct server-info consumers (`getServerInfo`/`getServerConfig`) survive. Evidence: test: `packages/kilo-vscode/tests/unit/connection-service-health-poll-absence.test.ts` (6 tests / 22 expect calls — static poll-surface absence plus retained-transport presence guard); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (health-poll row removed from the connection liveness/disposal table; LOCK-013). Only the extension-side poll is removed; P4.4 remains **Active** — the server-side `/global/health` route, HTTP/SSE/generated-SDK bridge, provider/catalog (LOCK-006), convergence, storage/sync, CLI/TUI, and back-end-gated fetch chain all remain open together with P4.4 transport narrowing — and P4.5 and P5 remain open. No `Effective-config source removal evidence` row is marked complete and no numeric performance claim is made (LOCK-PERF-6). Implemented in the current working tree and not yet committed as of 2026-08-25.

Current-state amendment (2026-08-25, P4.4-T1b) — the legacy effective-config source-inventory reader and its reporting endpoint are **physically removed**: `packages/opencode/src/kilocode/config/sources.ts` (the 12-source `KilocodeConfigSources` diagnostic inventory) is deleted; the `KilocodeConfigSources` type/echo is gone from `packages/opencode/src/kilocode/config/overlay.ts` (`Result`/`Input`/`resolve` no longer carry `sources`); the `/config/sources` schema/path/endpoint/handler and `ConfigSourcesResponse` are removed from the config-console group and handlers, and the `sources` echo field is removed from `ConfigOverlayResponse`. Validated regeneration (`bun ./script/generate.ts` from repo root, run twice idempotently) removed `config.sources`/`ConfigSourcesResponse` from `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts`, and `packages/sdk/openapi.json`. Evidence: test: `packages/opencode/test/kilocode/p4-4-source-inventory-removal.test.ts` (static absence of the reader/endpoint and the generated contract plus retained config-console bridge presence), retained focused suites (`config-overlay`, `config-transaction`, `config-rules`, `config-model-state`, `tui-config`, `httpapi-public`, `p4-3-cutover`, `p4-3-overlay-mode`), SDK suite; docs: `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md` and skill `packages/opencode/src/kilocode/skills/kilo-config.md` now describe the removal rather than a surviving diagnostic surface; the removed behavior is recorded as dropped reconstruction candidate RC-P4.4-001 (LOCK-015). Retained config-console bridge endpoints and overlay/transaction resolution are unchanged (LOCK-009); the `sources.ts`-cited residual in section 7 rows 1-7, 9, 10, 12 is removed while each row keeps its other retained residues and stays open; no row is marked complete, no phase completion or performance claim is made (LOCK-PERF-6). Implemented in the current working tree and not yet committed as of 2026-08-25.

Current-state amendment (2026-08-25, P4.4-T2) — the orphaned primary-worktree mirror-read helper is **physically removed** after verification that no production consumer remains (managed worktree infrastructure permanently removed; canonical project assets live only under first workspace root `.kilo` boundary; no mirror-read fallback): `packages/opencode/src/kilocode/primary-worktree.ts` (64 lines, `primaryPaths`/`primaryWorktree`) deleted; direct test `packages/opencode/test/kilocode/primary-worktree.test.ts` deleted; glob entry `primary-worktree` removed from `packages/opencode/script/kilocode/test-profile.ts`. No production import remains — `config/config.ts`, `kilocode/config/config.ts`, `skill/index.ts`, and `config/paths.ts` contain no `primary-worktree`/`primaryPaths`/`primaryWorktree` reference; canonical-root handling (`canonicalRoot`) and `ConfigPaths` (`packages/opencode/src/config/paths.ts` unchanged per scope) plus all legacy-reader cutover proofs, retained config-console bridge endpoints, private worker bridge, and existing HTTP/SSE bridge remain untouched. Evidence: test: `packages/opencode/test/kilocode/p4-4-primary-worktree-removal.test.ts` (file-deletion, helper-import absence across CLI source, test-profile glob absence, canonical-root/`ConfigPaths` preservation); `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349` updated to assert helper file/test absence and zero helper surface in CLI source (P4.4 exception closed); retained `p4-3-cutover.test.ts` cutover anchors. Tracker §7 rows 6/8 and matrix rows 6/8 updated — row 6 helper source deleted, row 8 status moved from residual retained to removed (helper deleted) with file/import absence evidence; rows 1-5,7,9-13 unchanged. No new reconstruction-candidate entry is required: primary-worktree mirror reads are a permanent removal class (runtime §8.1) recorded in the §7 removal inventory, not a displaced behavior needing evidence preservation; inherent permanent removals are not candidate rows. No risk-area code was removed beyond the helper and its direct test; P4.4 remains Active; no performance claim is made (LOCK-PERF-6). Implemented in the current working tree and not yet committed as of 2026-08-25.

Current-state amendment (2026-08-25, P4.4-T3) — the unreachable legacy managed/MDM configuration helper is **physically removed** after verification that no production consumer remains (canonical authored config is only one global root and `<workspaceRoot>/.kilo/`; no generic managed/legacy overlay is a target source; permanent removal is direct with no shim; P4.3 cutover complete). `packages/opencode/src/config/managed.ts` (77 lines, `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences`, `MANAGED_PLIST_DOMAIN`) deleted; direct tests `ConfigManaged` import and 5 `parseManagedPlist` unit tests removed from `packages/opencode/test/config/config.test.ts` (helper-only tests; `p4-3-cutover.test.ts` preservation and `Config.Service` canonical loader behavior unchanged); supporting `managedConfigDir`/`writeManagedSettingsEffect` harness removed. No production import remains — `config/config.ts`, `kilocode/config/config.ts`, `kilocode/config/overlay.ts`, `config/paths.ts` contain no `ConfigManaged`/`managedConfigDir`/`readManagedPreferences`/`parseManagedPlist`/`MANAGED_PLIST`/`ai.opencode.managed` reference; `ConfigPaths` and canonical-root handling preserved per scope; daemon/transport, generated SDK/OpenAPI, provider catalog, TUI/instruction, and sandbox residues unchanged. Evidence: test: `packages/opencode/test/kilocode/p4-4-managed-removal.test.ts` (helper file-deletion, helper-import absence across CLI source including `KILO_TEST_MANAGED_CONFIG_DIR`, direct-test removal, canonical-loader managed-absence preservation, test-profile glob presence, `ConfigPaths`/`canonicalRoot` preservation); retained `p4-3-cutover.test.ts:35-38` cutover anchors continue to prove canonical-only loading. Tracker §7 row 9 and matrix row 9 updated — helper source deleted with file/import absence evidence; rows 1-8 and 10-13 keep their prior T2 delta. No new reconstruction-candidate entry is required: cloud/org/managed config sources are a permanent removal class (runtime §8.1) recorded in the §7 removal inventory; the unreachable helper has no target value and is not a displaced behavior needing evidence preservation; inherent permanent removals are not candidate rows. P4.4 remains Active; no phase completion or performance claim is made (LOCK-PERF-6). Implemented in the current working tree and not yet committed as of 2026-08-25.

Current-state amendment (2026-08-25, P4.4-T4) — the legacy CLI `.well-known/opencode` remote-auth discovery URL-login branch is **physically removed** from `kilo auth login` (LOCK-006 provider/org/cloud source removal). The `args.url` branch at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`fetch` `<url>/.well-known/opencode`, `Process.spawn` of `auth.command`, `auth.set(url, { type: "wellknown", key: wellknown.auth.env, token: token.trim() })` persistence) is deleted; the `login [url]` positional and its `Process`/`text` imports are removed and the command is now `login` without URL discovery. Stale well-known intercept helpers (`remoteConfigClient`, `wellKnownAuth`, `json` helper, `wellKnown` helper) were removed from `packages/opencode/test/config/config.test.ts` (helper-only fixtures; `Config.Service` canonical loader behavior unchanged). Evidence: test: `packages/opencode/test/kilocode/p4-4-wellknown-provider-auth-removal.test.ts` (CLI well-known discovery branch absence in `cli/cmd/providers.ts`, stale config.test helpers removed, retained generic provider-login guarded: `ProvidersLoginCommand`, `handlePluginAuth`, `resolvePluginProviders`, api-key/plugin/custom-provider paths, `Config.Service`/`Plugin`/`ModelsDev` preserved; P4.3 `p4-3-cutover.test.ts:35-38` well-known absence preserved); `packages/opencode/script/kilocode/test-profile.ts` sorted glob entry `p4-4-wellknown-provider-auth-removal` (managed < primary < wellknown). Tracker §7 row 9 and matrix row 9 updated — CLI well-known discovery source deleted with file/branch absence evidence; rows 1-8 and 10-13 keep their prior T3 delta. No new reconstruction-candidate entry is required: cloud/org/managed config sources are a permanent removal class (runtime §8.1) recorded in the §7 removal inventory; the CLI URL discovery is a direct LOCK-006 permanent removal with no displaced non-permanent behavior needing evidence preservation; `Auth.WellKnown` storage type remains in `packages/opencode/src/auth/index.ts` for persisted credential compatibility and is not a reconstruction candidate. Generic custom provider adapters, canonical effective config, and the legacy HTTP/SSE/generated-SDK bridge remain per LOCK-009; no SDK regeneration is required and none was performed. P4.4 remains Active; no row or phase is marked complete; no performance claim is made (LOCK-PERF-6). Implemented in the current working tree and not yet committed as of 2026-08-25.

Current-state amendment (2026-08-26, P4.4-T6) — the preset `@kilocode/kilo-gateway` bundled provider loader is **physically removed** (LOCK-006 preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change). `packages/opencode/src/kilocode/provider/provider.ts` `KILO_BUNDLED_PROVIDERS` (`"@kilocode/kilo-gateway": async () => createKilo`) and its `createKilo` import plus `BundledSDK` helper are deleted, and its import/spread (`...KILO_BUNDLED_PROVIDERS`) is removed from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS` (generic SDK loaders `@ai-sdk/*`, `@openrouter/ai-sdk-provider`, etc., retained). `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`/`patchConfigModel`/`kiloCustomLoaders`/`patchCustomLoaderResult`/`kiloSmallModelPriority`/`buildTimeoutSignal`, `models-api.json` 3.0 MB checked-in snapshot, `ModelsDev` catalog service (`provider/models.ts`) — `ModelCache` (`provider/model-cache.ts`) is historical at T6 and superseded 2026-08-27 by file deletion, custom-provider save/delete/validation/auth lifecycle (`kilocode/custom-provider.ts`, `custom-provider-save.ts`, `custom-provider-delete.ts`, `provider-auth-lifecycle.ts`), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) are retained per LOCK-006/009 — static source-presence preservation only; dynamic Kilo model resolution through fallback (models.dev/custom lifecycle) is intentionally not runtime-proven (historical `ModelCache` fallback at T6 superseded 2026-08-27 by file deletion) in this static unit (no network/package installation). No performance claim is made (LOCK-PERF-6). Added focused static absence/preservation regression `packages/opencode/test/kilocode/p4-4-bundled-provider-loader-removal.test.ts` (source presence only; dynamic fallback intentionally unproven) plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (bundled < managed < primary < provider < wellknown). Tracker §7 row 9 and matrix row 9/provider residue are updated for this preset-loader deletion; rows 1-8 and 10-13 keep their prior T5 delta. P4.4 remains Active; no row or phase is marked complete. Implemented in the current working tree and not yet committed as of 2026-08-26.

Current-state amendment (2026-08-25, P4.4-T5) — the preset provider display metadata enrichment is **physically removed** from the provider-list response (LOCK-006 preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved). `packages/opencode/src/kilocode/provider/metadata.ts` (preset-only `providerMetadata` helper — `noteKey`/`icon`/`priority` mapping) is deleted and its import plus `metadata: providerMetadata(item.id)` enrichment are removed from `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts`; the provider listing now returns `all: Object.values(validProviders).map((item) => Provider.toPublicInfo(item))` without preset enrichment while `all`/`connected`/`default`/`failed` aggregation and dependencies (`Config.Service`, `Provider.Service`, `ProviderAuth.Service`, `ModelCache.Service`, `ModelsDev.Service`, `filterPromptTrainingModels`, `overlayAnacondaDesktop`, `pickBy` guard) and endpoint wiring (`list`/`auth`/`authorize`/`callback` on `/provider`) are retained. The direct obsolete test `packages/opencode/test/kilocode/provider-metadata.test.ts` is deleted and focused absence/preservation regression `packages/opencode/test/kilocode/p4-4-provider-metadata-removal.test.ts` plus its sorted glob entry in `packages/opencode/script/kilocode/test-profile.ts` (managed < primary < provider < wellknown) is added. The optional `providerMetadata` field on `Provider.Info` (`Provider` OpenAPI schema, generated SDK `types.gen.ts` `metadata?:`) remains optional and contract-compatible when omitted; webview `provider-catalog.ts` fallback (`synthetic` icon, `FALLBACK_PROVIDER_IDS` priority, `noteKey` fallback) is preserved. Generic provider adapters (`KILO_BUNDLED_PROVIDERS`, `models-api.json` 3.0 MB checked-in snapshot, `provider/provider.ts`/`provider/models.ts`, custom-provider save/delete/validation, auth lifecycle, provider model/catalog loaders) and the HTTP/SSE/generated-SDK bridge remain untouched (historical `ModelCache` mention at T5 superseded 2026-08-27 by file deletion) per LOCK-006/009; preset catalogs, models.dev snapshot, and model caches remain open. Tracker §7 row 9 and matrix row 9/provider residue are updated for this preset-metadata deletion; rows 1-8 and 10-13 keep their prior T4 delta except for the provider-residue clarification. No new reconstruction-candidate entry is required: preset display metadata is an explicitly removed preset surface (LOCK-014/015 inherent permanent removal per LOCK-006) with no displaced behavior needing evidence preservation. P4.4 remains Active; no row or phase is marked complete; no performance claim is made (LOCK-PERF-6). Implemented in the current working tree and not yet committed as of 2026-08-25.

Current-state amendment (2026-08-26, P4.4-T7) — the `kilo` and `apertis` dynamic model-fetch/cache subsystem is **physically removed** (LOCK-006 preset/org source removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change). `packages/opencode/src/provider/model-cache.ts` is historical invalidation-only `clear` at T7 and superseded 2026-08-27 by file deletion (historical stubbed `get`/`fetch`/`refresh`/`getFailure`/`failedProviders` without network — `fetchKiloModels`/`KiloModelsService`/`kiloModelsLayer`/`APERTIS_BASE_URL`/`api.apertis.ai`/`fetchApertisModels`/`authOptions`/`KILO_API_KEY`/`APERTIS_API_KEY`/`kilocodeToken`/`HttpClient`/`FetchHttpClient`/`Auth.Service`/`Config.Service` deleted at that date); `packages/opencode/src/provider/models.ts` removed `ModelCache`/`Config`/`Auth` (later superseded by file deletion) and `kilo`/`apertis` injection (`cache.fetch`/`KILO_OPENROUTER_BASE`/`providers.kilo`/`apertis`/`baseURL`) — now static `Core` overlay only (`core.get` with defect catch); `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` removed `ModelCache.Service`/`failedProviders()` — now `failed: string[] = []` with `validProviders` guard `Object.keys(item.models).length > 0 || id in connected` (failedSet removed). Deleted obsolete kilo/apertis cache tests (`model-cache-effect.test.ts`, `model-cache-org.test.ts`, `provider-list-failed-state.test.ts`, `kilo-models-401-fallback.test.ts`) and simplified `kilo-loader-auth.test.ts` to `kiloCustomLoaders` helpers only; updated `p4-4-bundled-provider-loader-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts` expectations to reflect handler/models absence. Added focused absence/preservation/runtime regression `packages/opencode/test/kilocode/p4-4-model-cache-removal.test.ts` (historical at T7: kilo/apertis network-absence, handler/models absence, `models-api.json`/`models-dev.ts`/`BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge preservation; current 2026-08-27: file absent, no production `ModelCache`/`model-cache` reference, seams removed, catalog preserved, sorted test-profile `bundled < managed < model-cache < primary < provider < wellknown`). Generic adapters (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache at `${cache}/models.json`, fallback `GET ${source}/api.json` when cache+snapshot missing, 5-minute TTL, 60-minute scoped background refresh, explicit `kilo models --refresh`; `provider/models.ts` wrapper retained as static `Core` overlay), custom-provider save/delete/auth lifecycle (`ModelCache.clear` invalidation without network), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) are retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed (`bun run refresh:models` only refreshes the checked-in snapshot and is not the sole runtime refresh); dynamic `kilo`/`apertis` resolution intentionally not runtime-proven beyond invalidation (no `kilo`/`apertis` network). No performance claim is made (LOCK-PERF-6). Tracker §7 row 9 and matrix row 9/provider residue are updated for this dynamic-cache deletion; rows 1-8 and 10-13 keep their prior T6 delta. P4.4 remains Active; no row or phase is marked complete. Implemented in the current working tree and not yet committed as of 2026-08-26.

Current-state amendment (2026-08-26, P4.4-T8) — preset provider login selector identity is **physically removed** from CLI provider selectors (LOCK-006 preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI change; Core `ModelsDev` and `models-api.json` retained per scope; webview Kilo fallback identity out of scope). `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`buildProviderOptions` `providers.ts:236-257` / `matchProviderInput` `providers.ts:258-274`) priority map (`kilo: 0`, `anthropic: 2`, `github-copilot: 3`, `openai: 4`, `google: 5`, `openrouter: 6`, `vercel: 7`), `kilo: "recommended"` / `openai: "ChatGPT login or API key"` hints, and `kilocode_change` `codex` -> `openai` alias removed; `packages/opencode/src/cli/cmd/github.handler.ts` `githubInstall`/`promptProvider()` (`buildGithubProviderOptions` `github.handler.ts:156-166` / `promptProvider()` `github.handler.ts:240-250`) priority/recommended map (`kilo: 0`, `anthropic: 1`, `openai: 2`, `google: 3`, `hint: priority[x.id] === 0 ? "recommended"`) removed. Deterministic generic ordering retained as alphabetical `sortBy((x) => x.name ?? x.id)` via `buildProviderOptions`/`buildGithubProviderOptions` without preset identity (plugin hint `"plugin"` retained as generic). Generic catalog lookup (`ModelsDev.Service`), plugin/custom-provider auth (`handlePluginAuth`, `resolvePluginProviders`, `other` path), and HTTP/SSE/generated-SDK bridge (`/provider` `list`/`auth`/`authorize`/`callback` on `Provider.ListResult`) retained per LOCK-006/009. Core `ModelsDev` (`packages/core/src/models-dev.ts` with disk cache at `${cache}/models.json`, fallback `GET ${source}/api.json`, 5-minute TTL, 60-minute background refresh, explicit `kilo models --refresh` / `ModelsDev.refresh`; `bun run refresh:models` only refreshes checked-in snapshot) and `models-api.json` 3.0 MB build-time snapshot, generic SDK loaders (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel` and custom-provider save/delete/auth lifecycle retained per LOCK-006/009 (historical `ModelCache` `clear` at T8 superseded 2026-08-27 by file deletion) — only preset selector ordering/hint/alias was removed. No performance claim is made (LOCK-PERF-6). Added focused regression `packages/opencode/test/kilocode/p4-4-provider-login-preset-removal.test.ts` (static absence plus executable ordering/prompt/codex/plugin dispatch via `buildProviderOptions`/`buildGithubProviderOptions`/`matchProviderInput`/`resolvePluginProviders` — preset priority/hint/codex alias absence in `cli/cmd/providers.ts` (`ProvidersLoginCommand`/`buildProviderOptions`/`matchProviderInput`) and `cli/cmd/github.handler.ts` (`buildGithubProviderOptions`/`promptProvider()`), deterministic generic `sortBy((x) => x.name ?? x.id)` ordering with insertion-order tie preservation for same-name collisions and `matchProviderInput` exact id plus case-insensitive name dispatch with distinct-id assertions, `ModelsDev.Service`/plugin/custom-provider/`other` path and `/provider` bridge preservation, Core `ModelsDev`/`models-api.json` retained, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < wellknown`); `packages/opencode/script/kilocode/test-profile.ts` sorted glob entry added (bundled < managed < model-cache < primary < provider-login < provider < wellknown). Tracker §7 row 9 and matrix row 9/provider residue are updated for this selector-identity deletion; rows 1-8 and 10-13 keep their prior T7 delta. P4.4 remains Active; no row or phase is marked complete. Implemented in the current working tree and not yet committed as of 2026-08-26.


Current-state amendment (2026-08-26, P4.4-T9) — handwritten SDK `KILO_CONFIG_CONTENT` wrapper forwarding is **physically removed** (LOCK-010 canonical file authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014 bounded removal; no performance claim per LOCK-PERF-6). `packages/sdk/js/src/server.ts` and `packages/sdk/js/src/v2/server.ts` deleted `mergeConfig`, `parseExistingConfig`, `buildConfigEnv` helpers and `env: { ...process.env, KILO_CONFIG_CONTENT: buildConfigEnv(...) }` forwarding — wrappers now retain `env: { ...process.env }` and `logLevel` arg passthrough with `createKiloServer`/`createKiloTui` spawning and `Config` type for `ServerOptions`/`TuiOptions`; no production call site used the removed helpers. Generated SDK (`packages/sdk/js/src/gen/*`, `v2/gen/*`) and OpenAPI (`packages/sdk/openapi.json`) unchanged — verified no `KILO_CONFIG_CONTENT`/`buildConfigEnv` in generated surface; HTTP/SSE transport, custom-provider/plugin/auth, sandbox `KILO_CONFIG_CONTENT` deny list, canonical `Config.Service` loader/cutover proofs untouched. Tests: `packages/sdk/js/test/server.test.ts` obsolete `buildConfigEnv` suite removed and replaced with wrapper-absence regression (handwritten wrappers contain no merge helpers/`KILO_CONFIG_CONTENT`, `...process.env` preserved, `createKiloServer`/`createKiloTui` still exposed, generated SDK preserved); `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts` added (handwritten wrapper absence, generated SDK/OpenAPI unchanged, canonical `Config.Service` authority absence, sandbox deny preservation, custom-provider/plugin/auth preservation, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < sdk < wellknown`); retained `p4-3-cutover.test.ts`, `sandbox/policy.test.ts` cutover/deny anchors. Tracker §7 rows 3/10/11 and matrix rows 3/10/11 updated — handwritten wrapper forwarding deleted with generated-SDK/OpenAPI unchanged and sandbox deny preserved; rows 1-2,4-9,12-13 keep prior T8 delta. No new reconstruction-candidate entry required per LOCK-014/015: `KILO_CONFIG_CONTENT` env override is a permanent removal class (runtime §8.1) with no target reader after P4.3 cutover; handwritten forwarding is a displaced preset-env surface with no target value. P4.4 remains Active; no phase completion or performance claim (LOCK-PERF-6).

Current-state amendment (2026-08-26, P4.4-T10) — synthetic Kilo provider identity is **physically removed from VS Code settings surfaces** (LOCK-006 bounded settings-surface removal; not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; LOCK-PERF-6 no performance claim). `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` simplified `visibleConnectedIds` to identity `[...connected]` and `providersWithKiloFallback` to identity `return providers` (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` import); `provider-catalog.ts` removed Kilo-specific `providerIcon` `KILO_PROVIDER_ID` branch and `providerNoteKey` `settings.providers.note.kilo` branch and deleted `kiloFallbackProvider()` export (removed `KILO_PROVIDER_ID`/`createKiloFallbackProvider` imports; retained `isPopularProvider`/`popularProviderIndex`/`sortProviders` and generic `providerIcon`/`providerNoteKey`); `ProvidersTab.tsx` removed `providersWithKiloFallback` injection, `KILO_PROVIDER_ID` import, `canonicalMode` Kilo filter, `sourceTag` `settings.providers.tag.gateway`, `connectProvider` `server.goToLogin()` Kilo branch, add-list `common.signIn` Kilo label (now always `settings.providers.action.configure`), and `showAccountButton` Kilo routing (now always `false`); `ProviderSelectDialog.tsx` removed `kiloFallbackProvider()` injection (`withKilo` synthesis), `KILO_PROVIDER_ID` import, `server.goToLogin()` Kilo branch, and `dialog.provider.tag.recommended` Kilo tag. Retained out-of-scope surfaces untouched: `src/shared/provider-model.ts` `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` (with `settings.providers.note.kilo` metadata), speech-to-text `availability.ts` `KILO_PROVIDER_ID` guard, session/model `KILO_AUTO` fallback (`webview-ui/src/context/session.tsx`), persisted state/auth storage, backend `/provider` APIs, SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider sorting/icons/notes/configure/auth/delete flows. Tests: updated `packages/kilo-vscode/tests/unit/provider-visibility.test.ts` and `provider-catalog.test.ts` to prove no synthetic injection and generic behavior; added `packages/kilo-vscode/tests/unit/p4-4-settings-surface-no-synthetic-kilo.test.ts` (static absence of Kilo synthesis/login/icon/note/recommended/gateway branches in the 4 allowed files plus preservation of shared constants/speech/model fallback). No changeset per spec (internal migration). Tracker §7 and matrix rows keep prior T9 delta with this bounded settings-surface addition — no 13-row matrix row marked complete; this is not full preset catalog/org removal. P4.4 remains Active; no phase completion or performance claim.

Current-state amendment (2026-08-26, P4.4-T11 audit correction) — bounded settings-surface removal tightened to suppress `KILO_PROVIDER_ID` reconstruction via `provider-tab-helpers.ts` (`resolveConfiguredProvider` now returns `undefined` when backend omits Kilo and `buildConfiguredList` filters `undefined`, before config fallback; generic custom-provider reconstruction from auth/config preserved) and to delete dead `provider-visibility.ts` exports `visibleConnectedIds`/`providersWithKiloFallback` (no production callers; file retained as placeholder with no exports) with focused regression tests for the auth/config-present/backend-absent path (LOCK-006 bounded settings-surface removal, not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; LOCK-PERF-6 no performance claim). `provider-catalog.ts`/`ProvidersTab.tsx`/`ProviderSelectDialog.tsx` T10 removals retained; `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` and speech-to-text Kilo guard remain out of scope. Tracker §7 keeps prior T10 delta with this guard addition — still no 13-row matrix row marked complete.

Current-state amendment (2026-08-26, P4.4-T10 re-audit correction) — style-only cleanup within the existing T10/T11 settings-surface boundary, no new phase and not full LOCK-006 completion (LOCK-006 bounded settings-surface removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs to implemented reality; LOCK-PERF-6 no performance claim). `packages/kilo-vscode/webview-ui/src/components/settings/provider-visibility.ts` empty placeholder deleted (no production exports, no imports remain — module absence now asserted); `webview-ui/src/components/settings/provider-tab-helpers.ts` removed unreachable Kilo `account` path — deleted `isKiloProvider` predicate, removed `"account"` from `PrimarySlot` and `isKilo` from `resolvePrimarySlot` (priority now `edit → apiKey → chatgpt → anaconda → placeholder`); `webview-ui/src/components/settings/ProvidersTab.tsx` removed dead Kilo `account` branch — deleted `showAccountButton` (always `false`), `useServer` import/`server` const, and `primary() === "account"` `Match` with `IconButton person`/`server.goToProfile()` navigation. Generic custom-provider configuration, auth, API-key, edit, delete, sorting, add-list behavior and backend-present Kilo row rendering (as existing record only) remain preserved; shared `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`createKiloFallbackProvider` and speech-to-text Kilo guard remain out of scope. Tests: `provider-tab-helpers.test.ts` removed `isKiloProvider` suite and `account` case, `provider-connect-dialog-arch.test.ts` replaced account presence with absence asserts, `p4-4-settings-surface-no-synthetic-kilo.test.ts` now asserts file/module and import absence. Tracker §7 and matrix T10/T11 rows keep prior delta with placeholder now recorded as deleted and account slot as removed; still no matrix row marked complete.

Current-state amendment (2026-08-26, P4.4-T12) — dead synthetic Kilo provider factory and orphaned i18n residues are **physically removed** (LOCK-006 bounded dead-factory/i18n residue removal only; not full LOCK-006 completion; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-010 canonical config ownership unchanged; LOCK-013 docs only to implemented reality; LOCK-PERF-6 no performance claim). Source: `packages/kilo-vscode/src/shared/provider-model.ts` deleted `createKiloFallbackProvider()` and its synthetic Kilo metadata literals (`Kilo Gateway`, `settings.providers.note.kilo`, `KILO_API_KEY` env, `noteKey`/`icon`/`priority`/`custom` source) with no imports/types made unused beyond that deletion (file now exports `KILO_PROVIDER_ID`, `KILO_AUTO`, `PROVIDER_PRIORITY`, `providerOrderIndex` plus `CUSTOM_PROVIDER_PACKAGES`/`CUSTOM_PROVIDER_PACKAGE`, `PROVIDER_ID_PATTERN`, `isCustomProviderPackage`, `parseModelString` — `createKiloFallbackProvider` deleted); locale keys `settings.providers.tag.gateway` and `dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` dictionary (20 locales) and `settings.providers.note.kilo` removed from every `packages/kilo-i18n/src/*.ts` dictionary (20 locales) per repository dictionary conventions — no production consumer remains for those keys (settings-surface synthesis/login branches already removed in T10/T11). Retained protected surfaces untouched: `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`/`CUSTOM_PROVIDER_PACKAGES`, `KILO_AUTO`/session-model fallback (`webview-ui/src/context/session.tsx` `KILO_AUTO`/`KILO_PROVIDER_ID`), speech-to-text Kilo guard (`availability.ts` `KILO_PROVIDER_ID`), `PROVIDER_PRIORITY`/`ModelSelector` Kilo visibility, provider auth stitching/`goToLogin`/`KILO_AUTO` fallback, persisted auth/state, backend `/provider` API, generated SDK/OpenAPI, HTTP/SSE bridge, generic custom-provider UI/sorting/icons/notes/add/configure/auth/delete, locale schema/key typing, formatting conventions. Tests: `packages/kilo-vscode/tests/unit/p4-4-settings-surface-no-synthetic-kilo.test.ts` updated to prove factory/synthetic literal absence (`createKiloFallbackProvider`/`Kilo Gateway`/`settings.providers.note.kilo`/`noteKey`/`custom` source) and orphaned locale key absence across all dictionaries (20 + 20 locales) while proving retained `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`, speech guard, and session fallback present — reusing P4.4 settings-surface static test style without weakening meaningful coverage. Tracker §7 and matrix T12 rows updated for dead-factory/i18n residue only — still no 13-row matrix row marked complete; P4.4 remains Active and must not claim LOCK-006 completion.

Current-state amendment (2026-08-26, P4.4-T13) — legacy `KILO_CONFIG` TUI migration helper input is **physically removed** (LOCK-010 canonical file authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014 bounded removal; no performance claim per LOCK-PERF-6). `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` deleted the sole legacy `if (Flag.KILO_CONFIG) files.push(Flag.KILO_CONFIG)` effective-config input from `opencodeFiles` (lines 148-158 scope); `Flag` import retained for `Flag.KILO_DISABLE_PROJECT_CONFIG` (still used) so no unused import remains. No `ConfigPaths`, `session/instruction`, transport/server routes, generated SDK/OpenAPI, provider code, or storage/convergence artifact was changed per scope (only that one line removed). Retained TUI theme/keybind migration and `ConfigPaths` discovery remain: `migrateTuiConfig`, `normalizeTui`, `backupAndStripLegacy`, `TUI_SCHEMA_URL`, theme/keybinds/`tui` extraction, `tui.json` materialization, `Filesystem.findUp` `["kilo.json", "kilo.jsonc"]`, `ConfigPaths.fileInDirectory(Global.Path.config, "kilo")`, `unique` directories, `Filesystem.exists` filtering, `Flag.KILO_DISABLE_PROJECT_CONFIG` gate; `ConfigPaths` `KILO_CONFIG_DIR` profile and instruction `KILO_CONFIG_DIR` behavior (historical at T13 — instruction removed in P4.4-T17), server health, HTTP/SSE/generated SDK, provider/catalog, custom-provider lifecycle preserved. Tests: `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` added (tui-migrate absence, retained migration/canonical-loader boundaries, `ConfigPaths`/`instruction` preservation (historical at T13), generated SDK/OpenAPI unchanged, sandbox deny preservation, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < sdk < tui-migrate < wellknown`); retained `p4-3-cutover.test.ts`, `p4-3-readiness-inventory.test.ts` cutover anchors verify `Config.Service` has no `Flag.KILO_CONFIG` reader. Tracker §7 row 1 and matrix row 1 updated — TUI migration helper `Flag.KILO_CONFIG` injection deleted with `ConfigPaths`/instruction (historical at T13) and TUI migration preservation; rows 2-13 keep prior T12 delta; row 1 remains `residual retained` (open) for remaining `ConfigPaths`/TUI residues; no row/phase/P4/LOCK-006 complete claimed. P4.4 remains Active.

Current-state amendment (2026-08-26, P4.4-T14) — `ConfigPaths.directories()` `.kilocode` directory targets are **physically removed** (LOCK-010 exactly two canonical scopes — `.kilocode` is not a target canonical source; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014/015 bounded principle-first removal without shim; no performance claim per LOCK-PERF-6). `packages/opencode/src/config/paths.ts:29,35` `ConfigPaths.directories()` narrowed from `targets: [".kilocode", ".kilo"]` to `targets: [".kilo"]` in both the project-ancestor walk and the home walk; ancestor/home walk topology, `Global.Path.config`/`Global.Path.home`, `Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`, `ConfigPaths.files()`/`fileInDirectory()`, `tui.ts` filtering, plugin install, instruction profile, canonical `Config.Service`, provider/transport/storage/convergence, generated SDK/OpenAPI/source-links unchanged per scope; all other `.kilocode` occurrences outside `config/paths.ts` preserved (e.g. `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` retains `[".kilocode", ".kilo"]` per scope). Tests: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:100,165` and `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts:145`/`p4-4-managed-removal.test.ts:114`/`p4-4-primary-worktree-removal.test.ts:83` updated to assert `targets: [".kilo"]` while preserving `KILO_CONFIG_DIR`/`Global.Path.home`/`Global.Path.config` and non-canonical loader boundary checks. Tracker §7 rows 5 and 7 and matrix rows 5 and 7 updated — `.kilocode` target removal recorded with dated evidence 2026-08-26; rows remain `residual retained` (open) for remaining TUI/`ConfigPaths` residues; no row/phase/P4.4/LOCK-006 complete claimed. P4.4 remains Active.

Current-state amendment (2026-08-26, P4.4-T15) — `ConfigPaths.directories()` home `Global.Path.home` `.kilo` discovery block is **physically removed** (LOCK-010 exactly one canonical global root and workspace `.kilo`; arbitrary home `.kilo` discovery not target authority; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-014/015 bounded principle-first removal without shim; no performance claim per LOCK-PERF-6). `packages/opencode/src/config/paths.ts:34-38` home `...(yield* afs.up({ targets: [".kilo"], start: Global.Path.home, stop: Global.Path.home }))` spread deleted; `ConfigPaths.directories()` now contains exactly one `targets: [".kilo"]` via the project-ancestor walk (`Global.Path.home` absent, `Global.Path.config`/`Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`ConfigPaths.files()`/`fileInDirectory()` preserved per scope); `tui.ts` filtering, plugin install, `session/instruction.ts` `KILO_CONFIG_DIR` profile intentionally retained/deferred per scope — not removed in this unit — canonical `Config.Service`, provider/transport/storage/convergence, generated SDK/OpenAPI/source-links unchanged; all other `.kilocode` occurrences outside `config/paths.ts` preserved (e.g. `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` retains `[".kilocode", ".kilo"]` per scope). Tests: `packages/opencode/test/kilocode/p4-3-cutover.test.ts:97-104,154-171` and `p4-4-tui-migrate-kilo-config-removal.test.ts`/`p4-4-managed-removal.test.ts`/`p4-4-primary-worktree-removal.test.ts` now assert home walk absent (`not.toContain("Global.Path.home")`) and single `targets: [".kilo"]` while preserving `Global.Path.config`/`KILO_CONFIG_DIR`/`KILO_DISABLE_PROJECT_CONFIG`/`files`/`fileInDirectory` and instruction `KILO_CONFIG_DIR` profile. Tracker §7 row 7 and matrix row 7 updated — home `.kilo` discovery deletion recorded with dated evidence 2026-08-26; row remains `residual retained` (open) for remaining ancestor walk residue and other legacy consumers; instruction `KILO_CONFIG_DIR` profile intentionally retained/deferred; no row/phase/P4.4/LOCK-006 complete claimed. P4.4 remains Active.

Current-state amendment (2026-08-26, P4.4-T16) — `ConfigPaths.directories()` workspace-ancestor `.kilo` discovery walk and dead no-op guard/void references are **physically removed** and the required temporary TUI-local legacy discovery is preserved (LOCK-010 target authored scopes are one global root plus `<workspaceRoot>/.kilo/` — no ancestor walk is a target configuration source; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; LOCK-013 docs only to implemented reality; no performance claim per LOCK-PERF-6). `packages/opencode/src/config/paths.ts:23-27` sole remaining `...(!Flag.KILO_DISABLE_PROJECT_CONFIG ? yield* afs.up({ targets: [".kilo"], start: directory, stop: worktree }) : [])` ancestor-walk branch plus `void directory`/`void worktree` and `...(!Flag.KILO_DISABLE_PROJECT_CONFIG ? [] : [])` no-op deleted; `ConfigPaths.directories()` now `() => unique([Global.Path.config, ...(Flag.KILO_CONFIG_DIR ? [Flag.KILO_CONFIG_DIR] : [])])` (`Global.Path.config`/`Flag.KILO_CONFIG_DIR` preserved, `Flag.KILO_DISABLE_PROJECT_CONFIG`/`void`/`afs.up`/`targets: [".kilo"]`/`start: directory`/`stop: worktree` absent, `Global.Path.home` remains absent; signature narrowed to `directories(): Effect<string[]>`; `ConfigPaths.files()`/`fileInDirectory()` untouched). Temporary TUI-local legacy discovery preserved in `packages/opencode/src/cli/cmd/tui/config/tui.ts:211-225` — `yield* afs.up({ targets: [".kilocode", ".kilo"], start: ctx.directory })` gated by `Flag.KILO_DISABLE_PROJECT_CONFIG`, merged as `unique([global-filtered baseDirectories, ...legacyProjectDirs, ...KILO_CONFIG_DIR-filtered baseDirectories])` so merged order is exactly global → enabled legacy project discovery → KILO_CONFIG_DIR (last wins, preserving prior precedence); `KILO_CONFIG_DIR` instruction profile (`packages/opencode/src/session/instruction.ts` `Flag.KILO_CONFIG_DIR`/`prefer KILO_CONFIG_DIR profile`) preserved at this baseline and TUI `.kilocode`/`KILO_CONFIG_DIR` behavior remain deferred/intact at T16 (instruction deferred to T17 per LOCK-003; TUI retained per LOCK-001/002)

Current-state amendment (2026-08-26, P4.4-T17) — `session/instruction.ts` `KILO_CONFIG_DIR` profile fallback is **physically removed** (LOCK-001 historical ConfigPaths optional `KILO_CONFIG_DIR` at T17 baseline (superseded in P4.4-T18 — ConfigPaths is now global-only); LOCK-002 TUI precedence global → legacy project directories → `KILO_CONFIG_DIR` last wins retained; LOCK-003 instruction-only consumer; LOCK-005 canonical effective config already independent of `KILO_CONFIG_DIR`). `packages/opencode/src/session/instruction.ts` deleted both `Flag.KILO_CONFIG_DIR` reads — `...(Flag.KILO_CONFIG_DIR ? [path.join(Flag.KILO_CONFIG_DIR, "AGENTS.md")] : [])` from `globalFiles` and `Flag.KILO_CONFIG_DIR ?? global.config` from `relative()` — now the `KILO_CONFIG_DIR` `AGENTS.md` candidate is removed while `globalFiles` retains the canonical `path.join(global.config, "AGENTS.md")` plus the conditional `path.join(global.home, ".claude", "CLAUDE.md")` candidate, and `relative()` fallback is `global.config` only when `KILO_DISABLE_PROJECT_CONFIG` is set; `Flag` import retained for `KILO_DISABLE_PROJECT_CONFIG`; project instruction discovery when enabled, `KILO_DISABLE_PROJECT_CONFIG` gating, and trusted-origin semantics preserved; `Flag.KILO_CONFIG_DIR` getter, `ConfigPaths` (`Global.Path.config` plus optional `Flag.KILO_CONFIG_DIR` at T17 — superseded in P4.4-T18, now global-only), TUI (`tui.ts` global → legacy → `KILO_CONFIG_DIR`), sandbox deny, and broader P4.4/P4.5 surfaces untouched. Tests: `packages/opencode/test/kilocode/instruction.test.ts` rewritten to prove `KILO_CONFIG_DIR` does not override canonical global `AGENTS.md` and relative instruction with project disabled resolves from `Global.Path.config` not `KILO_CONFIG_DIR`; `p4-3-cutover.test.ts` and `p4-4-tui-migrate-kilo-config-removal.test.ts` now absence-guard `instruction.ts` for `Flag.KILO_CONFIG_DIR` while affirming `ConfigPaths`/`TUI`. Tracker §7 row 2 and matrix row 2 updated — instruction profile deleted, `ConfigPaths`/`TUI` residual retained at T17 (superseded in P4.4-T18 — ConfigPaths is now global-only, TUI is sole `KILO_CONFIG_DIR` reader); rows 1,3-13 keep prior T16 delta; no row/phase/P4.4/LOCK-006 complete claimed. P4.4 remains Active.



Current-state amendment (2026-08-26, P4.4-T18) — `ConfigPaths.directories()` `KILO_CONFIG_DIR` import/reader is **physically removed** and TUI-owned explicit compatibility is preserved at T18 (Historical at T18 — superseded in P4.4-T27; T27-correction removes all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig`/`.kilocode`/ancestor readers, no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30 clarification) (LOCK-001 effective-config ownership remains canonical; LOCK-002 TUI precedence global → legacy `.kilocode`/`.kilo` → `KILO_CONFIG_DIR` last wins preserved at T18; LOCK-003 `Global.Path.config` override and sandbox deny-list untouched; LOCK-004 transport/provider/catalog/daemon/HTTP/SDK untouched at T18). `packages/opencode/src/config/paths.ts:4,22-24` deleted `Flag` import and narrowed `directories()` to `unique([Global.Path.config])` only — `Flag.KILO_CONFIG_DIR` absent, `Global.Path.config` preserved, `ConfigPaths.files()`/`fileInDirectory()` untouched, no `targets: [".kilo"]`/`Global.Path.home`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`void`/`canonicalRoot` at T18. `packages/opencode/src/cli/cmd/tui/config/tui.ts:207-221` at T18 explicitly appended `Flag.KILO_CONFIG_DIR` after global + enabled legacy dirs (`unique([...baseDirectories.filter(dir !== Flag.KILO_CONFIG_DIR), ...legacyProjectDirs, ...(Flag.KILO_CONFIG_DIR ? [Flag.KILO_CONFIG_DIR] : [])])`) gated by `Flag.KILO_DISABLE_PROJECT_CONFIG` — preserved exact prior behavior global → legacy → `KILO_CONFIG_DIR` last wins at T18, including deduplication when `Global.Path.config` equals `KILO_CONFIG_DIR` and theme/plugin-origin precedence (historical, superseded T27). `Flag.KILO_CONFIG_DIR` getter, `Global.Path.config` override (`packages/core/src/global.ts:79`), sandbox deny-list (`KILO_CONFIG_DIR`), instruction absence, Config.Service absence, `ConfigPaths` APIs, legacy TUI discovery targets, transport/SDK/provider/Anaconda/Desktop/P4.5 surfaces unchanged per scope. Tests: `packages/opencode/test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` added (ConfigPaths absence, TUI explicit last-wins, sole retained reader, Global/sandbox preservation, precedence) plus updates to `p4-3-cutover.test.ts` and `p4-4-tui-migrate-kilo-config-removal.test.ts`/`p4-4-managed-removal.test.ts`/`p4-4-primary-worktree-removal.test.ts` to assert `ConfigPaths` no `Flag.KILO_CONFIG_DIR` (`return unique([Global.Path.config])`) while `tui.ts` retains explicit append; `p4-4-t16-tui-legacy-discovery.test.ts` still proves last-wins behavior. Tracker §7 row 2 and matrix rows 2/5/7 updated — ConfigPaths `KILO_CONFIG_DIR` deleted, TUI retained as sole direct reader; rows 1,3-4,6,8-13 keep prior T17 delta; no row/phase/P4.4/LOCK-006 complete claimed; P4.4 remains Active.

Current-state amendment (2026-08-26, P4.4-T19) — bounded runtime/source-removal evidence unit after T18 (no new production deletion; evidence only per LOCK-001/002/003/004/005; P4.4 remains Active; Historical at T18 — superseded in P4.4-T27; T27 removes all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/migrate/ancestor readers, see P4.4-T30). Cumulative T13-T18 removed readers (Flag.KILO_CONFIG in tui-migrate, `.kilocode` targets, Global.Path.home walk, workspace-ancestor walk + dead void, instruction KILO_CONFIG_DIR profile, ConfigPaths KILO_CONFIG_DIR) are proven not to initialize in the effective-config path: `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` runtime `ConfigPaths.directories()` global-only even with `KILO_CONFIG_DIR` set, `p4-3-cutover.test.ts` canonical-loader absence anchors and nested canonical-root convergence, `p4-4-t16-tui-legacy-discovery.test.ts` TUI gated legacy discovery + last-wins (historical at T16 — superseded T27), `p4-4-tui-migrate-kilo-config-removal.test.ts` tui-migrate absence; no new focused test added per the "only when needed" rule — all claims have executable proof — and `ConfigPaths.directories()` was confirmed global-only with TUI as sole explicit `KILO_CONFIG_DIR` reader at T18 (global → legacy `.kilocode`/`.kilo` → `KILO_CONFIG_DIR` last-wins, historical T18 — superseded T27, now no TUI reader) plus `Global.Path.config` override and sandbox deny-list untouched per LOCK-003/004. Descriptive backend benchmark (backend `Server.listen`/`AppLayer` HTTP/SSE observations only; does not execute TUI/ConfigPaths; no coverage/proof of reader removal; no causal attribution to T13-T18 or T18): existing backend harness `packages/opencode/test/benchmark/` scenarios 6 `first-prompt` and 12 `cold-during-generation` are backend-only observations and provide descriptive startup/resource timing context only; scenarios 7,8,9,11,13 and Extension Host/private-worker tiers deliberately not observed and not claimed — focused tests, not benchmark scenarios, are the inactive-reader/compatibility proof per LOCK-003/005. Commands: `bun test test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts test/kilocode/p4-3-cutover.test.ts test/kilocode/p4-4-t16-tui-legacy-discovery.test.ts test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts test/kilocode/p4-4-managed-removal.test.ts test/kilocode/p4-4-primary-worktree-removal.test.ts` from `packages/opencode/` (54 pass / 0 fail / 493 expects) and `bun run script/p0-benchmark.ts --scenarios 6,12 --samples 5 --warmup 1 --out /tmp/p4-4-t19-backend-6-12.jsonl` from `packages/opencode/` (raw versioned at `specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` — 36 lines, 59430 bytes, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`, byte-identical via `cmp`); provenance `darwin/arm64/node 24.3.0/bun 1.3.14/git 98a9d1d6e6 clean` with boot `Server.listen/AppLayer` HTTP/SSE (backend observations only; does not execute TUI/ConfigPaths); 10 measured +2 warmup samples status ok; medians/p95: scenario 6 `totalMs` med 604 p95 629, `firstConfigLoadMs` med 2 p95 3, `firstInstanceBootstrapMs` med 3 p95 4; scenario 12 `patchMs` med 19 p95 28, `commitToConvergedMs` med 284 p95 554; drift-noted vs P0 baseline `2026-08-12T06-08-27-792Z/backend.jsonl` (git 7a768502dd dirty; same harness class) — descriptive only, no threshold, no improvement/regression or causal claim per LOCK-005/LOCK-PERF-6/R7. Limitations: backend-harness tier only; Extension Host/private-worker/storage not observed. Evidence: `specs/vscode-orchestrator/evidence/p4.4-t19-runtime-evidence.md` with exact six-file command, environment/provenance, versioned raw (36 lines, 59430 bytes, sha256 `d575b85c8c34eee256c9df9efcff18c1958f5ab586dcaba503559b55f3adce3c`), samples, med/p95, limits, explicit inactive-reader conclusion (focused tests, not benchmark scenarios); `specs/vscode-orchestrator/evidence/p4.4-t19/backend-6-12.jsonl` byte-identical raw; `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` T19 note. P4.4 remains Active; no row/phase marked complete; no production code changed except harness execution.




Current-state amendment (2026-08-26, P4.4-T20) — extension webview preset-provider priority/catalog residue is **physically removed** (LOCK-006 bounded preset-only removal; LOCK-009 HTTP/SSE/generated-SDK bridge preserved; no SDK regeneration — no endpoint/OpenAPI/URL change; no P4.5/Config.Service/instruction/transport/daemon work). `packages/kilo-vscode/src/shared/provider-model.ts:9-17,30-33` deleted `PROVIDER_PRIORITY` (`kilo`/`anthropic`/`deepseek`/`openai`/`google`/`openrouter`/`vercel`) and `providerOrderIndex` (now exports only `KILO_PROVIDER_ID`, `KILO_AUTO`, `CUSTOM_PROVIDER_PACKAGES`/`CUSTOM_PROVIDER_PACKAGE`, `PROVIDER_ID_PATTERN`, `isCustomProviderPackage`, `parseModelString`); `webview-ui/src/components/settings/provider-catalog.ts` deleted `PROVIDER_PRIORITY as FALLBACK_PROVIDER_IDS` import, `fallback` Set, `isPopularProvider`, `popularProviderIndex`, `providerOrderIndex` dependence — `sortProviders` now `a.name.localeCompare(b.name) || a.id.localeCompare(b.id)` deterministic alphabetical; `webview-ui/src/components/shared/model-selector-utils.ts` deleted `PROVIDER_PRIORITY as PROVIDER_ORDER` import, `providerOrderIndex` import, `export { PROVIDER_ORDER }`, `providerSortKey` — `buildModelGroups` provider-group sort now `aLabel.localeCompare(bLabel) || aId.localeCompare(bId)` alphabetical by display name with id tie-break; `webview-ui/src/components/settings/provider-tab-helpers.ts` removed `isPopularProvider` import and `&& isPopularProvider(item)` filter — `buildAddList` now `sortProviders(Object.values(allProviders).filter(!configuredIds.has(id)))` generic alphabetical; `webview-ui/src/components/settings/ProviderSelectDialog.tsx` removed `isPopularProvider`/`popularProviderIndex` imports and priority `groupBy` (`recommended`/`other`) and `sortBy` rank logic and `sortGroupsBy` recommended-first — now `groupBy` single `dialog.provider.group.other`, `sortBy` alphabetical with `CUSTOM_PROVIDER_ID` first (`localeCompare` + id tie-break), `sortGroupsBy` alphabetical. Retained protected surfaces untouched: `KILO_PROVIDER_ID`/`KILO_AUTO`/`CUSTOM_PROVIDER_PACKAGES`, `PROVIDER_ID_PATTERN`/`isCustomProviderPackage`/`parseModelString`, `providerIcon`/`providerNoteKey` generic paths, `KILO_GATEWAY_ID`, `buildModelGroups` favorites/recommendedIndex/auto alphabetical, `provider-tab-helpers` Kilo reconstruction guard and generic custom-provider reconstruction, session `KILO_AUTO` fallback, speech-to-text Kilo guard, backend `/provider` APIs, generated SDK/OpenAPI, HTTP/SSE bridge. Tests: updated `provider-catalog.test.ts` (alphabetical sortProviders), `model-selector-utils.test.ts` (buildModelGroups alphabetical ordering), `provider-tab-helpers.test.ts` (buildAddList generic alphabetical), updated `p4-4-settings-surface-no-synthetic-kilo.test.ts` to assert T20 priority-symbol absence while preserving T12 checks, added `p4-4-provider-catalog-priority-removal.test.ts` (static absence across 5 scoped files, protected-symbol presence, deterministic alphabetical for sortProviders/buildAddList/buildModelGroups, custom-provider reconstruction preserved). No row/phase/P4.4/LOCK-006 complete claimed; P4.4 remains Active (tracker §7 rows 1-13 keep prior T19 delta except provider catalog residue now records T20 deletion with retained generic behavior; no 13-row matrix row marked complete).

Current-state amendment (2026-08-26, P4.4-T21) — dead legacy `KILO_CONFIG`, `KILO_CONFIG_CONTENT`, `KILO_PERMISSION` Flag entries **physically removed** (LOCK-001 P4.4 Active; LOCK-010 historical KILO_CONFIG_DIR last-wins at T21 — superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30; LOCK-011 convergence; no P4.5/transport/SDK). `packages/core/src/flag/flag.ts` deleted `KILO_CONFIG: process.env["KILO_CONFIG"]`, `KILO_CONFIG_CONTENT: process.env["KILO_CONFIG_CONTENT"]`, and `get KILO_PERMISSION() { return process.env["KILO_PERMISSION"] }` after post-T20 inspection proving no production consumers (last direct Flag definitions for three source classes whose readers/forwarders were already removed in P4.3/T9/T13; repository independently governs compatibility). Preserved exactly `get KILO_CONFIG_DIR()`, `Flag.KILO_CONFIG_DIR ?? Path.config` in `packages/core/src/global.ts:79` (historical at T21 — TUI explicit `unique([...baseDirectories.filter(dir !== Flag.KILO_CONFIG_DIR), ...legacyProjectDirs, ...(Flag.KILO_CONFIG_DIR ? [Flag.KILO_CONFIG_DIR] : [])])` last-wins in `packages/opencode/src/cli/cmd/tui/config/tui.ts:207-221` superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30), and sandbox literal deny entries `"KILO_CONFIG"`, `"KILO_CONFIG_CONTENT"`, `"KILO_CONFIG_DIR"` in `packages/opencode/src/kilocode/sandbox/policy.ts:168-175`; `ConfigPaths.directories()` remains `unique([Global.Path.config])` global-only, extension direct diagnostic `process.env` reads untouched per scope, no SDK regeneration or fixtures changed. Tests: `packages/opencode/test/kilocode/p4-4-flag-legacy-getter-removal.test.ts` added (Flag definitions/readers absent, protected KILO_CONFIG_DIR/Global/TUI/sandbox/ConfigPaths preservation, SDK/OpenAPI unchanged, sorted `bundled < flag < managed < model-cache < primary < provider-login < provider < sdk < t16 < t18 < tui-migrate < wellknown`); updated `p4-4-sdk-config-forwarding-removal.test.ts`/`p4-4-tui-migrate-kilo-config-removal.test.ts` sorted assertions and `script/kilocode/test-profile.ts` insertion at `bundled < flag < managed`. Changeset `p44-flag-legacy-getter-removal.md` patch for `@kilocode/cli`. Tracker §7 rows 1,3,4 and matrix rows 1,3,4 updated — Flag deletion recorded with residual/open retained; rows 2,5-13 keep prior T20 delta; no row/phase/P4.4/LOCK complete, no performance claim, no P4.5/transport/SDK expansion. P4.4 remains Active.

Current-state amendment (2026-08-26, P4.4-T22) — canonical-first selector readiness decoupling (LOCK-009 / R1, LOCK-012 / selector readiness, LOCK-013, LOCK-014; one bounded P4.4 unit; P4.4, P5, all rows, and LOCK-009 remain open; T22 replaces abandoned metadata-removal proposal; never modifies metadata paths). Ensure provider, agent, and configuration selectors publish from extension-owned canonical materialization/indexes without waiting for `KiloProvider.initializeConnection()` HTTP/SSE/generated-SDK connectivity or global `extensionDataReady`; HTTP/SSE/SDK remain background reconciliation; noncanonical/SDK-only categories unchanged. Source: `packages/kilo-vscode/src/KiloProvider.ts` decouples `webviewReady` canonical publish (now before `syncWebviewState`) and `doInitializeConnection` (canonical provider/agent/config fetches now background `void Promise.all` not blocking `extensionDataReady`; SDK-only `fetchAndSendSkills`/`fetchAndSendCommands`/`seedSessionStatusMap` still gate `extensionDataReady`); `webview-ui/src/context/provider.tsx` and `config.tsx` remove global `extensionDataReady` retry for provider/config (canonical-first immediate request + 3s fallback; SDK bridge remains for noncanonical/background); `webview-ui/src/context/session.tsx` removes `extensionDataReady` retry for agents (canonical-first) while retaining it for SDK-only `mcpStatus` (`requestMcpStatus` retry preserved). Preserved: default noncanonical mode, `KiloConnectionService` singleton/lifecycle, `ServerManager` ownership, `SdkSSEAdapter` heartbeat/reconnect, config revision ownership in `KiloConnectionService`/`SdkSSEAdapter` transaction deduplication, `extensionDataReady` for SDK-only domains (skills, commands, MCP, session status), full provider/agent/config fallback reconcile after connection, canonical `ready:false` before materialization and last-known valid state after. Not modified: `packages/opencode`, SDK generated files, private-worker protocol/worker methods, transport endpoint definitions, provider model metadata (`KILO_MODEL_SCHEMA_EXTENSIONS`, `patchModelsDevModel`, etc.), TUI/CLI surfaces, config-convergence code; `extensionDataReady` not deleted globally; no polling/retries/timers beyond existing lifecycle. Tests: `packages/kilo-vscode/tests/unit/p4-4-canonical-first-selector-readiness.test.ts` added (7 tests — canonical publish while connection unresolved/failed, webviewReady before syncWebviewState, doInitializeConnection decoupling, webview contexts no longer depend on global data-ready, noncanonical SDK bridge retained, preserved last-known valid). Retained-surface statement: SDK/SSE bridge remains for noncanonical or background reconciliation; `extensionDataReady` remains for SDK-only categories (skills, commands, MCP, session status). No P4.4/P5/row/LOCK completion claimed; no performance claim per LOCK-013 (LOCK-PERF-6). Changeset `p44-canonical-first-selector-readiness.md` patch for `kilo-code`.

Current-state amendment (2026-08-26, P4.4-T23) — obsolete preset-Kilo presence invalidation helper physically removed (LOCK-008 bounded removal; LOCK-001 P4.4 Active, P5 Not started, all rows/matrix/LOCK remain open/residual; LOCK-002/005/006/007 preserved; no P4.4/P5/LOCK completion claimed). Delete no-longer-owned preset `providerID === "kilo"` presence branch while preserving generic auth invalidation and KiloViewers ownership. Source: `packages/opencode/src/kilocode/server/provider-auth-lifecycle.ts` deleted `KiloViewers` import and `invalidatePresence` helper (`export const invalidatePresence = Effect.fn("KiloServer.invalidatePresence")` with `KiloViewers.Service`/`invalidateAuth()`); `packages/opencode/src/server/routes/instance/httpapi/handlers/control.ts` removed `invalidatePresence` import and the two guarded `if (ctx.params.providerID === "kilo") yield* invalidatePresence()` calls (authSet and authRemove) while retaining `invalidateAfterProviderAuthChange` with `cleanupDisabled:true` for authSet and without for authRemove; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` removed `invalidatePresence` import and the single guarded `if (ctx.params.providerID === "kilo") yield* invalidatePresence()` call (callback) while retaining `invalidateAfterProviderAuthChange` with `cleanupDisabled:true`. Preserved: `invalidateAfterProviderAuthChange` canonical coordinator (fence/snapshot/compensation), rollback/fence and disabled-provider semantics (historical `ModelCache.clear` under fence at T23 superseded 2026-08-27 by file deletion) (`cleanupDisabled`, `disabled_providers`, `ConfigRollbackFailed`, `withColdMutation`), `KiloViewers` service/layer and AppLayer provisioning (`packages/opencode/src/kilocode/presence/service.ts` with `Service`/`layer`/`defaultLayer`/`update`/`invalidateAuth`/`viewed`/`EventServiceClient`; `packages/opencode/src/effect/app-runtime.ts` with `KiloViewers.defaultLayer`), all other KILO identifier logic (`KILO_PROVIDER_ID`/`KILO_AUTO`, `auth.get("kilo")`, `KILO_API_KEY`, `KILO_MODEL_SCHEMA_EXTENSIONS`, custom-provider save/delete, `models-api.json`), and existing `providerID === "kilo"` uses outside the three removed branches (none removed per LOCK-008). Not modified: `presence/service.ts`, `effect/app-runtime.ts`, session handlers, SDK/OpenAPI/generated clients, KiloProvider/webview readiness work, models snapshot/catalog, global health, TUI files, or other `providerID === "kilo"` logic; no endpoint/contract/transport/daemon change (LOCK-005); no TUI migration/discovery change (LOCK-007). Tests: `packages/opencode/test/kilocode/p4-4-presence-invalidation-removal.test.ts` added (7 tests — helper/import absence in lifecycle + control/provider, no production `invalidatePresence` surface, generic lifecycle primitives preservation, KiloViewers service/layer and AppLayer preservation, other KILO identifier preservation, presence file untouched, sorted test-profile `bundled < flag < managed < model-cache < presence < primary < provider-login < provider < sdk < t16 < t18 < tui-migrate < wellknown`); retained focused suites (`provider-auth-lifecycle`, presence/service, custom-provider-save/delete) continue to pass. Tracker §7 row 9 and matrix row 9 provider residue updated — preset presence helper deleted with generic lifecycle preserved; rows 1-8,10-13 keep prior T22 delta; no row/phase/P4.4/LOCK complete claimed. Changeset `p44-presence-invalidation-removal.md` patch for `kilo-code`.

Current-state amendment (2026-08-27) — bounded TUI migration source rewrite/backup removal: `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` no longer creates `*.tui-migration.bak` files or strips `theme`, `keybinds`, and `tui` fields from legacy `kilo.json`/`kilo.jsonc` inputs. It retains materialization of a missing `tui.json` from those fields — schema, theme, keybinds, normalized `tui` payload, target-existence guard, discovery, and write of a missing `tui.json` — without replacing an existing `tui.json`. Removed helper `backupAndStripLegacy` and its call path, plus imports made unused by its removal (`access`/`constants` from `fs/promises`, `applyEdits`/`modify` from `jsonc-parser`). The sole caller `tui.ts` discovery/loading semantics, `ConfigPaths`/`Global.Path.config` override with `KILO_CONFIG_DIR` last-wins, sandbox deny-list, provider/auth/runtime-convergence/storage/SDK surfaces, and generated artifacts remain unchanged per scope. Tests: `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` now proves backup/rewrite helper absence while retained materialization and guard remain; `packages/opencode/test/config/tui.test.ts` updated to assert source retention and no backup creation while verifying `tui.json` payload and target-exists guard; retained `p4-3-cutover.test.ts`, `p4-4-t16-tui-legacy-discovery.test.ts`, and `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` cutover/discovery guards remain passing. No row or phase marked complete — rows 5/12/13 and overall P4.4/P5 remain residual/open.

Current-state amendment (2026-08-27, P4.4-T25) — CLI TUI provider dialog preset display residue physically removed (T25-SCOPE P4.4 CLI TUI display-only; T25-BEHAVIOR generic name+id ordering, unified Providers category, no preset Popular/recommendation/title/description — generic deterministic name+id order; LOCK-006 generic provider lifecycle/catalog/auth preserved; generic credential/error guidance retained without stale recommended copy; no P4.4/P5 completion — all remain open/residual; P5 product deletion out of scope). `packages/opencode/src/kilocode/cli/cmd/tui/component/dialog-provider.tsx:45-56` deleted `PROVIDER_PRIORITY` (`kilo: -1`, `anthropic: 0`, `github-copilot: 1`, `openai: 2`, `google: 3`, `anaconda-desktop: 4`), `PROVIDER_DESCRIPTIONS` (`kilo: "(Recommended)"`, `anthropic: "(Claude Max or API key)"`, `openai: "(ChatGPT login or API key)"`, `anaconda-desktop: "(Local models)"`), `PROVIDER_TITLES` (`openai: "OpenAI / Codex"`) and Kilo Gateway recommended `renderApiDescription` branch (`Kilo Gateway gives you access… https://kilo.ai/gateway`) — `atomic-chat` local guidance retained (`Connect to Atomic Chat on this machine… Leave API key empty`); `LOCAL_OPTIONAL_API_KEY`/`isLocalOptionalApiKey`/`LOCAL_API_KEY_PLACEHOLDER`, `renderGutter`/`failedDescription` (`connection error — click to reconnect`), `renderAutoMethod`/`apiKeyPlaceholder`/`selectProvider` retained per LOCK-006. `packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx:21,42-54,120-132` removed `PROVIDER_PRIORITY` import/usage and preset `sortBy((x) => PROVIDER_PRIORITY[x.id] ?? 99)` with `Popular` vs `Providers` category branching and `PROVIDER_DESCRIPTIONS`/`PROVIDER_TITLES` special cases — `providerOptions` now generic deterministic `sortBy((x) => x.name.toLowerCase() + "\0" + x.id.toLowerCase())` (display name case-insensitive with id tie-break per T8/T20) and constant `category: "Providers"`; `createDialogProviderOptions` now generic `title: provider.title` and `description: failedDesc ?? provider.description` (failedDesc preserved, baseDesc/title branch removed). Preserved: `failed`/`connected`/`consoleManaged`/`onboarded` gutter, `selectProvider`/`renderAutoMethod`, `isLocalOptionalApiKey`/`apiKeyPlaceholder`/`LOCAL_API_KEY_PLACEHOLDER`, `CUSTOM_PROVIDER_OPTION_VALUE`/`CUSTOM_PROVIDER_ID`/`normalizeCustomProviderID`, `DialogProvider`/`DialogModel`/`ApiMethod`/`AutoMethod`/`CodeMethod`/`PromptsMethod`, custom-provider `Other` flow, `ModelCache.clear`/fence/rollback/disabled cleanup, `KiloViewers`/`AppLayer`/session viewer, `KILO` identifiers, provider catalog/`models-api.json`/`ModelsDev`, transport/storage/SDK/ConfigPaths/Global/sandbox per T25-SCOPE (display-only, no catalog/transport/storage change). Not modified: `packages/kilo-vscode` and completed T20 artifacts, server/API/SDK/generated files, provider packages/auth support, P5 scope. Tests: `packages/opencode/test/kilocode/p4-4-tui-provider-dialog-preset-removal.test.ts` added (kilocode Absence + generic-presence, consumer absence + generic-presence, production-source preset-symbol absence, test-profile sorted insertion `tui-migrate < tui-provider-dialog < wellknown`, executable generic ordering with name+id tie-break and Popular/category absence) plus `packages/opencode/test/cli/cmd/tui/provider-options.test.ts` retained; `packages/opencode/script/kilocode/test-profile.ts` insertion at `tui-migrate < tui-provider-dialog < wellknown` sorted; retained `p4-4-provider-login-preset-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts` T8/T20 anchors and `p4-3-cutover.test.ts`. No row or phase marked complete — rows 5/12/13 and overall P4.4/P5/matrix/LOCK remain residual/open.

Current-state amendment (2026-08-27, P4.4-T26) — CLI TUI model dialog preset recommendation/popularity presentation physically removed (T26-SCOPE P4.4 CLI TUI display-only; T26-BEHAVIOR generic provider display-name category, no Kilo Recommended / Popular providers / kiloRank — generic deterministic provider-name category; T26-SCOPE generic provider lifecycle/auth/catalog/model selection, fence/rollback/disabled cleanup/KiloViewers (historical `ModelCache` at T26 superseded 2026-08-27 by file deletion), Kilo auto routing/auth, KILO identifiers, transport/storage/SDK, provider data schema including recommendedIndex outside dialog preserved; FreeModelDisclosure, BYOK/May-train/Free footer construction, ModelInfoPanel, footer-based free-first ordering, model actions, favorites/recents, provider-scoped newest-first ordering, generic DialogProvider flow preserved; no P4.4/P5 completion — all remain open/residual; P5 product deletion out of scope; dialog-provider, Kilo auto-auth, provider catalog/schema, SDK/generated surfaces, FreeModelDisclosure/ModelInfoPanel, completion row statuses not modified). `packages/opencode/src/cli/cmd/tui/component/dialog-model.tsx:5,8,28-33,133-138,145-171` removed `kiloRank` memo (`Map` of `recommendedIndex`), `Recommended` category/rank (`provider.id === "kilo" && info.recommendedIndex !== undefined ? "Recommended" : provider.name`), disconnected `Popular providers` synthesis (`pipe(providers(), map(category: "Popular providers"), take(6))`) and related unused imports/bindings (`take`, `createDialogProviderOptions` `providers`) — category collapsed to `connected() ? provider.name : undefined`; `sortModelOptions` call narrowed to `sortModelOptions(options, props.providerID !== undefined)` and `sortModelOptions` signature narrowed to `(options, newestFirst)` removing `rank: ReadonlyMap<string, number>` and `recommended` sort key while retaining footer `free-first` and `newestFirst` ordering. Preserved: `FreeModelDisclosure`/`ModelInfoPanel`, BYOK/May-train/Free footer, `Favorites`/`Recent`, `DialogProvider`/`DialogVariant`, `useConnected`/`useSync`/`useLocal`, `KILO` identifiers, provider catalog/`models-api.json`/`ModelsDev`, `KiloViewers`/`AppLayer`, transport/storage/SDK/ConfigPaths/Global/sandbox per T26-SCOPE (display-only; historical `ModelCache` at T26 superseded 2026-08-27 by file deletion). Not modified: `dialog-provider`, Kilo auto-auth, provider catalog/schema, SDK/generated surfaces, `FreeModelDisclosure`/`ModelInfoPanel`, completion row statuses. Tests: `packages/opencode/test/kilocode/p4-4-tui-model-dialog-preset-removal.test.ts` added (absence + generic-presence, category collapse, data-layer recommendedIndex preservation, test-profile sorted insertion `tui-migrate < tui-model-dialog < tui-provider-dialog < wellknown`, executable generic ordering with rank independence, `FreeModelDisclosure`/`ModelInfoPanel` preservation) plus `packages/opencode/test/cli/cmd/tui/model-options.test.ts` updated for rank independence (signature length 2, extra rank ignored); `packages/opencode/script/kilocode/test-profile.ts` insertion at `tui-migrate < tui-model-dialog < tui-provider-dialog < wellknown` sorted; retained `p4-4-tui-provider-dialog-preset-removal.test.ts`/`p4-4-provider-metadata-removal.test.ts`/`provider-options.test.ts` and `p4-3-cutover.test.ts`. No row or phase marked complete — rows 5/12/13 and overall P4.4/P5/matrix/LOCK remain residual/open.

P0 activation rationale (2026-08-10): the ADRs, both direction specs, this
tracker, and all product/runtime/config/performance decisions
(LOCK-001..013, LOCK-PERF-1..7) were already recorded. P0 work has now
started and durable artifacts exist in the working tree: the static
current-state inventory
([`p0-current-state-inventory.md`](p0-current-state-inventory.md)), the
runnable H-1..H-13 baseline fixture
(`packages/opencode/test/kilocode/p0-harness-baseline.test.ts`, executing
H-1/H-2/H-7/H-8/H-9/H-10/H-11/H-12/H-13 with H-3..H-6 recorded as explicit
gaps), opt-in `KILO_P0_PERF` instrumentation (extension
`packages/kilo-vscode/src/perf/perf-instrument.ts`, backend
`packages/opencode/src/kilocode/perf/instrument.ts`, webview
`packages/kilo-vscode/webview-ui/src/utils/perf.ts`), the backend benchmark
harness under `packages/opencode/test/benchmark/` (scenarios 6,7,8,9,11,12,13)
with runner `packages/opencode/script/p0-benchmark.ts`, and the VS Code
Extension Host benchmark harness under `packages/kilo-vscode/script/p0-bench/`
(scenarios 1,2,3,4,5,10) with entry points
`packages/kilo-vscode/script/e2e-p0-bench.ts` /
`e2e-p0-bench-launch.mjs` and runner
`packages/kilo-vscode/tests/e2e/p0-bench-runner.ts`. These are P0 progress
evidence only: the fixture is not a target-surface parity suite, no H
criterion is parity-proven, and no performance metric has changed from
`Not proven` (LOCK-PERF-6). Baseline smoke runs exist but are not durable
baseline measurements and are not cited as such.

P0 evidence update (2026-08-12): three formal repeated baseline campaigns are
accepted as objective P0 baseline evidence and recorded with exact paths under
`specs/vscode-orchestrator/evidence/p0-baseline/`: (1) Extension Host cold-start
(`2026-08-11T11-51-16-883Z/benchmark.jsonl`; fresh VS Code profile + fresh
scratch XDG, no seeded config; 1 warmup + 5 measured, status ok, scoped
memory-guard/backend-identity evidence); (2) Extension Host many-agent-MCP
(`many-agent-mcp-merged-2026-08-12T00-27-49-435Z/`; seeded scratch XDG kilo.json
with 20 real custom agents + one real local stdio MCP server fixture; exactly 1
warmup + 5 measured, status ok, `baselineComplete: true`, merge manifest with
per-segment sha256/hash record mapping, environment drift explicitly recorded
for `backendCli` temp paths only with one shared `cliSnapshotSha256`); and (3)
backend in-process `Server.listen`/`AppLayer` (`2026-08-11T04-46-19-926Z/backend.jsonl`;
scenarios 6,7,8,9,11,12,13, each 1 warmup + 5 measured, 42/42 ok, 79 n=5
summaries, status ok; CLI-side harness evidence only — `serve_cli_entry` is
CLI-process-tier and not emitted, and CLI process-tier files changed after this
run, so it must not be overclaimed as target-surface or private-worker
evidence). The H-3..H-6 fixture gaps are closed: the baseline fixture now
executes all H-1..H-13 as live tests through real production services and
run-owned fixtures (no gap records remain). These artifacts are P0 progress
evidence only: no H criterion is parity-proven (section 6), and section 8
records the measured values as descriptive n=5 sample statistics only — not SLA
or threshold claims (R7 Open).

P0 evidence update (2026-08-12, harness-fix campaign set): two more formal
repeated baseline campaigns are accepted as objective P0 baseline evidence,
bringing the accepted set to five: (4) Extension Host warm-view/no-provider/
custom-provider (`2026-08-12T05-52-07-672Z/benchmark.jsonl`, git 7a768502dd,
gitDirty true, run status ok, 19 samples — warm-view 2 warmup + 5 measured with
one lifecycle/shared live worker across panel close/reopen cycles and no seeded
config, no-provider 1 warmup + 5 measured on a seeded-empty persisted
providerless state (seeded scratch XDG kilo.json with explicit empty
provider/agent records) distinct from the fresh no-seed cold-start condition,
custom-provider 1 warmup + 5 measured with one real custom provider
(`@ai-sdk/openai-compatible`, loopback baseURL, 1 model); 27 n=5 summaries) and
(5) current-tier backend in-process `Server.listen`/`AppLayer`
(`2026-08-12T06-08-27-792Z/backend.jsonl`, git 7a768502dd, gitDirty true, run
status ok, 42/42 samples, 79 n=5 summaries, scenarios 6,7,8,9,11,12,13) — the
current CLI-side in-process tier for later-phase comparison; the older
`2026-08-11T04-46-19-926Z/backend.jsonl` (git 461781ffc0) is retained as
historical evidence for the 2026-08-11 tier only. Two pre-fix partial artifacts
are retained but never cited as baseline:
`2026-08-12T04-30-39-069Z/benchmark.jsonl` (no run-finish record, no
custom-provider samples; pre-harness-fix) and
`2026-08-12T06-05-12-356Z/backend.jsonl` (no run-finish record, scenarios
6/7/8 only; environmental first attempt). Harness fixes landed in the working
tree as P0 progress evidence (not product parity): scenario 4 fixture schema
correction, shared fixture schema test
(`test: packages/opencode/test/kilocode/p0-bench-seed-config-schema.test.ts`),
early `runTests` failure observation, and bounded blocked-sample fallback
(`test: packages/kilo-vscode/tests/unit/p0-bench-launch-failure.test.ts`).
These artifacts are P0 progress evidence only: no H criterion is parity-proven
(section 6), the backend campaigns remain CLI-side in-process harness evidence
(`serve_cli_entry` is CLI-process-tier and not emitted; neither target-surface
nor private-worker evidence), and section 8 records all values as descriptive
n=5 sample statistics only — not SLA or threshold claims (R7 Open).

P0 evidence update (2026-08-12, session-switch campaign): a sixth formal
repeated baseline campaign is accepted, closing the last unmeasured Extension
Host benchmark scenario (scenario 10): Extension Host session switch
(`2026-08-12T07-21-22-201Z/benchmark.jsonl`, git 7a768502dd, gitDirty true, run
status ok; one lifecycle, 5 seeded deterministic sessions, real Playwright
tab-strip clicks, settled when the active tab id and header title match; 1
warmup + 5 measured, all ok, one n=5 summary, memory-guard/backend-identity
evidence per sample). Section 8 records `switchSettleMs` as descriptive n=5
sample statistics only — not an SLA or threshold claim, and no P1 navigation
parity is claimed (R7 Open); session-switch is removed from the unmeasured P0
rows.

P0 decisions and closure (2026-08-12): Q3 and R1/R2/R5/R6/R8 are resolved and
recorded in section 9 (decisions) and in the owning artifacts (inventory
section 9 for the Q3 counts; runtime spec section 9 for R1/R2/R5/R6/R8). R7
remains Open by design and is **not** a P0 blocker: its required-by is clarified
to "before the first P3/P4/P5 performance gate that uses thresholds (and the
P1/P2 no-regression gate if applicable)", not the P0 baseline recording — P0
satisfies its component by recording descriptive runtime baselines
(LOCK-PERF-6 forbids inventing numeric thresholds from current
underpowered/noisy evidence; threshold policy must be recorded before each
affected phase starts/claims its gate, using comparable same-environment
control evidence). The five remaining `Not proven` metric rows are re-scoped to
later-phase gates, not P0 blockers: persisted-selector paint gates at P5 (no
extension-owned persisted indexes exist before P4.1); cost attribution and
per-event transport/webview flush gate at P2 (harness-parity/streaming);
removed-feature initialization count and startup-work net reduction gate at
P3/P4.4 (removal). P0 is marked **Complete** with the objective evidence above
(all 13 scenarios accepted, H-1..H-13 live baseline fixture with parity still
unproven, inventories, instrumentation, six accepted campaigns, exact paths
recorded in section 8); **P1 remains Not started** (this change does not start
P1). No GitHub issue is required; no target-surface parity, SLA, or removal
completion is claimed; canonical architecture docs are unchanged (LOCK-013).

P1 evidence update (2026-08-14): P1 (Orchestration-first navigation) is
Complete. Topic grouping/navigation landed over the migration bridge as a
derived read-model (`test: packages/kilo-vscode/webview-ui/agent-manager/topics.ts`):
root sessions define Topics, descendants belong via parentID, activity is max
member updatedAt, order is activity-descending with a deterministic ID
tie-break, orphan/missing-parent/cycle components degrade to independent
Topics, and Topic selection/expansion is presentation state only (Q1 resolved
2026-08-14, section 9). Durable evidence: topics unit tests (`test:
packages/kilo-vscode/tests/unit/topics.test.ts`); Topic-navigation Storybook
Playwright interaction spec — 7 passed, covering topic/child selection,
disclosure, active-child highlighting, root relabel, delete-degradation,
orphan/cycle degradation, and collapse persistence across inventory updates
(`test: packages/kilo-vscode/tests/topic-navigation.spec.ts`); agent-manager
arch and i18n tests (`test: packages/kilo-vscode/tests/unit/agent-manager-arch.test.ts`,
`test: packages/kilo-vscode/tests/unit/agent-manager-i18n.test.ts`,
`test: packages/kilo-vscode/tests/unit/agent-manager-i18n-split.test.ts`);
Extension Host lifecycle-convergence E2E over the migration bridge covering the
extension-owned view boundaries — session navigation (real Topic/child clicks
with active tab/header/topic convergence), panel close/reopen, and webview
reload — with the runner rehydrating fixture facts so each fresh webview
re-derives the same Topic hierarchy/active state (harness: `test:
packages/kilo-vscode/tests/e2e/runner.ts`, `test:
packages/kilo-vscode/script/e2e-probe.ts`; `test:e2e:topic-navigation` passed
and the full `test:e2e` passed). Affected-path performance comparison: a
formal scenario-10 (session-switch) campaign was recorded under
`doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-14T11-45-17-512Z/benchmark.jsonl`
(git 99d275a9d7, gitDirty true, VS Code 1.133.0, recorded CLI snapshot
sha256/size, 1 warmup + 5 measured, all ok, memory-guard/backend-identity
evidence per sample; summary n=5 min 154 / median 177 / p95=max 724 / mean
291.2 ms) compared against the P0 session-switch baseline
(`2026-08-12T07-21-22-201Z/benchmark.jsonl`, git 7a768502dd, VS Code 1.132.0,
n=5 min 115 / median 195 / p95=max 282 / mean 201.6 ms). Drift is recorded:
git 7a768502dd→99d275a9d7 both dirty, VS Code 1.132.0→1.133.0, CLI snapshot
changed, and one 724 ms outlier sample is present; no structural anomaly was
found and no performance conclusion is drawn — the comparison is descriptive,
affected-path, same-environment per R7 (no numeric threshold, no requirement
to improve; section 9). The P0 baseline rows in section 8 are unchanged.
H-1..H-13 target-surface parity was proven at P2 on 2026-08-15 on the
migration bridge (section 6); the removal
inventory is unchanged (section 7); canonical architecture docs are unchanged
(LOCK-013; architecture checker reported 0 signals for the P1 change) — Topic
navigation is a
presentation read-model over existing runtime/session facts with no new
persistence, API, protocol, boundary, state-ownership, lifecycle, or
architecture change.

P2 evidence update (2026-08-15): P2 (Harness surface parity) is Complete. The
final durable target-surface campaign is recorded under
`doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/`
with four sub-campaigns — real-completed, real-session, real-overflow,
real-restart — each a complete validated manifest (`validated: true`, status
`complete`, `success: true`, no missing/malformed files), 135 manifest entries
covering 127 unique files, all 127 unique files hash-verified, 28/28 generation requests run-owned `e2e-local/e2e-model`
(real-completed final 17, real-session final 4, real-overflow final 4,
real-restart final 3; all matrix phases 0 violations), and 12/12 UI-send pins
pass (real-completed 8, real-session 2, real-restart 1, real-overflow 1).
Capture provenance: focused Extension Host E2E harness
(`test: packages/kilo-vscode/script/e2e-probe.ts`, fixture-gated via
`KILO_E2E_FIXTURE`), VS Code 1.133.0
(`.vscode-test/vscode-darwin-arm64-1.133.0`), current working tree (git
f91d22ae1f, dirty), run-owned scratch/scripted-model/MCP fixtures with exact
PID cleanup (MCP child 39897 exited after disconnect;
`real-completed-mcp-disconnect-done` = `{"e2e-fixture":"disabled"}`), no owned
VS Code process or CDP port remaining. H-1..H-13 target-surface evidence is
recorded per row in section 6, grounded in the campaign artifacts: H-1 custom
agent (ModeSwitcher served/selected "E2E Agent"; `e2e-agent` on the pinned
generation requests), H-2 delegation (backend child `ses_ffb6f4a02ffehoL0gau0KztAZV`
via `mode: subagent` + completed `task` tool result flowing back to the parent,
child-open button rendered), H-3 user tool (`workspace/.kilo/tool/e2e_marker.ts`
through the real ToolRegistry; artifact `e2e-custom-called.txt` =
"echo:hello"), H-4 skill (`workspace/.kilo/skills/e2e-skill/SKILL.md` through
Skill.Service + SkillTool; `E2E_SKILL_CONTENT_MARKER`), H-5 MCP (real stdio
server `e2e-fixture` connected, `e2e_echo` executed inside the real loop,
server-side `calls.log` = "echo:hi", child cleaned by exact PID),
H-6 permission/question (backend pending Permission/Question observable, dock
visible→gone, reply drains and completes; `ask.txt` sentinel
`E2E_PERMISSION_SENTINEL`), H-7 parentID/children persist after panel reopen
(reopened panel renders parent topic "E2E Title" with child row), H-8 two
concurrent panel sessions busy with independent production Stop abort (abort A
leaves A idle and B busy; abort B leaves B idle; closed tab A persists in the
backend), H-9 per-session model/variant pins (A `e2e-agent`/low, B
`e2e-agent-b`/high; every pin verified against the run-owned scripted model),
H-10 persistence (same session/transcript resumed in a fresh Extension Host
after `workbench.action.reloadWindow`; artifact persists), H-11 all five
lifecycle boundaries (panel close/reopen, reload, session switch, SSE transport
reconnect, worker restart — plus true window/Extension Host restart),
H-12 checkpoint rollback (write→revert-to-here restores exact original bytes
`E2E_ROLLBACK_ORIGINAL`, RevertBanner lists the file, Redo All restores the
edited bytes `E2E_ROLLBACK_EDITED`, banner gone, reverted turns re-shown),
H-13 overflow safeguard (synthetic overflow `reportedUsage` input 45000 /
output 8000 triggers compaction part + summary + automatic continuation in one
turn; panel surface shows no context-management/compact controls — settings
rows 0, header context menu 0, header context actions 0; the shared TaskHeader
context popover trigger is recorded as evidence only and its Compact content is
not mounted). Supporting backend descriptive campaign:
`doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T06-18-28-167Z/backend.jsonl`
(git f91d22ae1f, dirty; scenarios 6 first-prompt, 7 large-stream, 9
permission-heavy; 1 warmup + 5 measured each, 18/18 ok, run finish status ok,
failures 0; CLI-side in-process `Server.listen`/`AppLayer` harness evidence —
not target-surface or private-worker evidence). Two pre-final P2-era evidence
directories are retained but excluded and never cited as closing evidence:
`2026-08-15T06-25-37Z/` (CONTAMINATION.md — the run completed an external
gateway `kilo/kilo-auto/small` title generation because the seed omitted
`small_model`, violating LOCK-006 zero-external-call at the request level) and
`2026-08-15T07-35-17-745Z/` (README.md — copy-race: three artifacts written in
the final ~1s window were not physically captured and were reconstructed
verbatim/maximum-fidelity, so the directory is not independent capture).
Affected-path performance comparison: the descriptive backend scenario 6/7/9
n=5 summaries and the target-surface SSE handling / webview lifecycle paint
records are recorded in section 8; per R7 there is no numeric threshold, no
requirement to improve, and no performance improvement/regression is claimed
(P0-baseline vs 2026-08-15 values are both recorded with drift — git
7a768502dd→f91d22ae1f, both dirty). Two non-independent P2 blockers are
recorded explicitly per the direction/runtime spec: per-event webview
render-flush was not emitted (only extension-side SSE handling spans exist) and
model HTTP duration was not separately measured; neither blocks P2 (runtime
spec section 10.11). VS Code validation on the P2 campaign: typecheck/lint/knip
pass; unit tests 4410 pass with 1 known-baseline failure in pull-back-contract,
reproduced identically at the isolated P1 SHA 99d275a9d7. No P3 removal,
LOCK-006 final provider-removal completeness, P4 private-runtime/config
parity, or performance claim is made; canonical
`packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` was
updated in this work unit to document the implemented env-gated fixture bridge:
the E2E harness table gained the `Real scenarios` row and the expanded
`Fixture bridge` row (read-only served-backend snapshot, MCP disconnect,
transport/process probes, fail-closed generation-request collector), and the
source-map rows now list `fixture-backend.ts`, `llm-request-collector.ts`, and
the restart probe (LOCK-013); source-link extraction
reported no diff.

P3.1 evidence update (2026-08-15): P3 is Active and P3.1 (Product removal —
ordinary single-chat sidebar, LOCK-001) is Complete. The ordinary single-chat
Activity Bar sidebar (`kilo-code.SidebarProvider` webview view under the
`kilo-code-ActivityBar` views container) is permanently removed — its manifest
contributions (container/view/menus/`sidebarTitle.*` commands/`sidebarVisible`
context), its webview-view registration/resolve path, its dependent i18n keys,
and the `TITLE_BUTTON_CLICKED` telemetry event name (emitted only by the
removed sidebar title-button wrappers; no remaining emitter). Chat commands
that previously used the sidebar fallback now route through
`resolveChatTarget`/`ensureChatTab` to the preserved editor surfaces (active
Open-in-Tab tab, else the Agent Manager panel, opened on demand), with
readiness-gated delivery. Durable evidence: `test:
packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` (static absence
contract for manifest/source/residue, preserved-surface survival, and command
re-routing), `test: packages/kilo-vscode/tests/unit/chat-target.test.ts`
(target preference, cold-open readiness success/failure, no-post-on-failure),
and the focused real Extension Host `test:e2e:sidebar-removal` scenario passed
via `test: packages/kilo-vscode/tests/e2e/runner.ts` (`assertSidebarRemoval`)
+ `test: packages/kilo-vscode/script/e2e-probe.ts` (loaded manifest contributes
no forbidden sidebar surface — identifier-based; production Open-in-Tab panel
reaches webview readiness through the env-gated fixture bridge; Agent Manager
still ready afterwards). Docs: `doc:
packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` was
updated in this work unit to document the implemented P3.1 removal — the new
"Chat surfaces (P3.1 sidebar removal)" section (removal statement, editor
surfaces, command re-routing, readiness-gated delivery) and its "Rollback
path" (git revert of the P3.1 removal commit(s); no feature flag/shim), plus
the consumer/build-table updates; release note `doc:
.changeset/remove-sidebar-chat.md` (minor; not modified here). Static
complexity deltas (section 8): webview message types 332 (delta 0), generated
v2 SDK methods 250 (delta 0), webview entries 6 (delta 0) — the removed
sidebar shared the editor-tab/Agent Manager webview bundle and no SDK contract
change exists (generated SDK `-` by nature for this removal). Removal evidence
is recorded per category in section 7 (source/tests/docs/config/i18n/build-
package complete). One reconstruction-candidate row is added (RC-P3.1-001,
sidebar-title telemetry, disposition `drop`, section 7); inherent sidebar
surface/view/title/context/assets and migrated command behavior are not
candidates (LOCK-015). Decisions Q2/Q4/R4 resolved 2026-08-15 (no numeric
adoption threshold; P1/P2 supplied migration affordances; section 9). P3
overall is Active, never Complete; P3.2-P3.4, P4, and P5 remain Not started.
Performance: a descriptive scenario-1 (cold-start) campaign was recorded under
`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`
(git 67436bdf70, gitDirty true, VS Code 1.133.0, recorded CLI snapshot
sha256/size, 1 warmup + 5 measured, run status ok; key medians/p95=max ms:
activate 24/32, `spawnToPortMs` 9407/13684, `connectToSseConnectedMs`
9535/13844, `activateToDataReadyMs` 16772/21748, `webviewLoadToPaintMs`
1175/1215, `backendEntryToListenerMs` 1330/2524) compared against the P0
cold-start baseline (`2026-08-11T11-51-16-883Z/benchmark.jsonl`, git
461781ffc0, VS Code 1.132.0, n=5 medians: activate 34, `spawnToPortMs` 5382,
`connectToSseConnectedMs` 5442, `activateToDataReadyMs` 9231,
`webviewLoadToPaintMs` 1291, `backendEntryToListenerMs` 644). Drift is
recorded: VS Code 1.132.0→1.133.0, git 461781ffc0 (dirty) → 67436bdf70
(dirty), CLI snapshot changed (sha256 `11636703...` → `6a8c6d5d...`).
Activation/render metrics moved negative and backend/transport metrics moved
positive, but no causal conclusion is drawn — the comparison is descriptive,
affected-path, same-environment per R7 (no numeric threshold, no requirement
to improve, no improvement/regression claim; section 8). Structural absence is
proved statically and at runtime separately from the performance records
(`sidebar-removal.test.ts` static contract + the `test:e2e:sidebar-removal`
Extension Host scenario). Full unit suite latest: 4449 pass with one known
pre-existing pull-back-contract failure; typecheck/lint/knip pass. An unrelated
auto-approve flake is not a P3.1 failure and is not recorded as one. No P3.2-
P3.4 removal, P4 private-runtime/config parity, or performance
improvement/regression claim is made.

P3.2 evidence update (2026-08-16): P3 is Active and P3.2 (Product removal —
managed worktree infrastructure + custom Diff Viewer surfaces, LOCK-002) is
Complete. The final implementation permanently removes the managed git-worktree
infrastructure (Agent Manager worktree manager/state/pollers/run/controller
subsystem/setup-run scripts/git-transfer/worktree-mode context/BranchSelect/
worktree message protocol/i18n/manifest contributions and the CLI-side
`Worktree` routes, `branch-name` route, `worktree-family`/`worktree-cleanup`
modules, and the `Worktree`/`BranchName` SDK groups), the custom Diff Viewer and
Diff Virtual surfaces (`DiffViewerProvider`/`DiffVirtualProvider`/`SourceController`
and the `diff-viewer`/`diff-virtual` webview bundles), and the orphaned
`agent_manager` list/prompt request-reply service (`agent-manager/service.ts`,
`protocol.ts`; `list`/`reply`/`reject` SDK methods); it retains root-local start
delegation (the `agent_manager` tool starts sessions only), the H paths, H-12
SessionRevert/Snapshot rollback, and generic worktree/project/snapshot
semantics, and P4.4's primary-worktree mirror-read removal explicitly remains
out of P3.2 scope. Docs: `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`
was updated in this work unit to document the implemented P3.2 removal (no
per-session worktree isolation / no setup-run scripts / no branch-PR import;
all Agent Manager sessions share the workspace root; no `.kilo/agent-manager.json`
and no `.kilo/worktrees/`; native VS Code diffs and `@git-changes` replace the
custom diff viewer; Rollback path = git revert of the P3.2 removal commit(s)),
user docs `packages/kilo-docs/pages/automate/agent-manager.md`,
`packages/kilo-docs/pages/automate/agent-manager-workflows.md` (and related
automate/getting-started pages) were updated to the root-local model, the
builtin `kilo-config` skill doc
(`packages/opencode/src/kilocode/skills/kilo-config.md`) was updated, and the
release note `doc: .changeset/agent-manager-root-local-sessions.md` (minor) was
added. Durable evidence: `test: packages/kilo-vscode/tests/unit/worktree-removal.test.ts`
(static absence contract for managed-worktree lifecycle/state/protocol/run/
transfer/import, Diff Viewer/Diff Virtual, branch-name route, worktree SDK
groups, and worktree/run i18n keys, plus survival of root-local orchestration,
H-12, permissions/questions, and reload-directory), `test: packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts`
(custom Diff Viewer/Diff Virtual static absence with preserved H-12 rollback,
RevertBanner, Agent Manager local git stats, and `@git-changes` prompt context),
and `test: packages/opencode/test/kilocode/agent-manager-absence.test.ts`
(orphaned `agent_manager` list/prompt request-reply contract absent across
source, HTTP groups, and the regenerated SDK — only `kilocode.agent_manager.start`
survives). Final runtime evidence: a focused real Extension Host
`test:e2e:worktree-removal` campaign was recorded under
`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-09-32-000Z/`
via `test: packages/kilo-vscode/tests/e2e/runner.ts` (`runWorktreeRemoval`) +
`test: packages/kilo-vscode/script/e2e-probe-worktree.ts` (extracted shared H-12
rollback phase) + `test: packages/kilo-vscode/script/e2e-probe.ts` — a complete
validated manifest (`status: complete`, `validated: true`, `missing: []`,
`malformed: []`, `success: true`), 29 manifest entries all 29 hash-verified, 36/36
probe PASS with 0 FAIL, two root-local session tabs (A/B) seeded through the
production session-open path and both controllable over the real webview
(tab-switch H-8 re-evidence on the root-local surface), H-12 byte-exact rollback
re-evidenced (`E2E_ROLLBACK_ORIGINAL\n` restored by Revert-to-here, `E2E_ROLLBACK_EDITED\n`
restored by Redo All via the shared `assertRealRollbackPhase`), 4/4 generation
requests run-owned `e2e-local/e2e-model` (0 matrix violations), runtime
manifest/command-table evidence clean (contributedViewIds `[]`, forbidden
view/container/command/binding/menu/setting/identifier hits all `[]`, `worktreesDir`
false, `agentManagerJson` false, setupScripts `[]`, panel surface
worktreeIds/runCards/sections/branchSelect all 0), no `.kilo/worktrees` or
`.kilo/agent-manager.json` created anywhere in the run-owned workspace/XDG
scratch, and clean resource lifecycle (no owned VS Code process remains, CDP
port released, scratch dir removed). The seeded deterministic plan session IDs
(`e2e-probe-47f91b0f-A` etc.) produce expected backend `Expected a string
starting with "ses"` validation messages on synthetic-id session queries — an
environment/seed classification, not a P3.2 failure (run `success: true`).
Final benchmark: a descriptive Extension Host scenario-1 (cold-start, fresh
no-seed) campaign was recorded under
`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`
(git 9d86428cc2, gitDirty true, VS Code 1.133.0, extension 7.4.11, recorded
final CLI snapshot sha256 `924cb638...`, 1 warmup + 5 measured, 13 n=5 summaries,
run status ok). Static complexity deltas (section 8): webview message types
332→274 (delta −58; WebviewMessage 149 + ExtensionMessage 125, disjoint), generated
v2 SDK public methods 250→239 (delta −11: Worktree 7 + BranchName 1 + AgentManager
list/reply/reject 3), webview esbuild entries 6→4 (delta −2: `diff-viewer` and
`diff-virtual` retired). Removal evidence is recorded per category in section 7
(worktree infrastructure and custom Diff Viewer/Diff Virtual rows complete across
source/tests/docs/config/i18n/build-package; generated SDK recorded as the −11
SDK surface reduction). Three reconstruction-candidate rows are added
(RC-P3.2-001 run-script subsystem, RC-P3.2-002 sibling-worktree recall family,
RC-P3.2-003 Agent Manager overview/targeted-prompt bridge + list-prompt
request-reply contract, all disposition `drop`, section 7); the inherent
managed-worktree creation/import/transfer/branch-naming/sections/multi-version/
custom-diff surfaces are permanent LOCK-002 removal evidence, not candidate rows
(LOCK-015). The earlier P3.2 evidence directories
`p3-2-worktree-removal-2026-08-15T18-00-47-000Z/` and
`p3-2-worktree-removal-2026-08-15T18-11-33-000Z/` remain historical (pre-final
state) and are never cited as closing evidence; final-tree completion evidence
cites only the two `p3-2-final-*` directories. Decisions: no P3.2 decision locks
changed; P3 overall remains Active, never Complete until P3.4 exits; P3.3-P3.4,
P4, and P5 remain Not started; LOCK-002 permanent removal is complete and P4.4's
primary-worktree mirror reads explicitly remain out of P3.2 scope. Typecheck
passes (opencode and kilo-vscode, including the E2E `tests/e2e/` tsconfig);
kilo-vscode lint and knip pass; kilo-vscode unit suite 4266 pass / 1 fail — the
known pre-existing pull-back-contract baseline
(`tests/unit/pull-back-contract.test.ts`, unchanged in this work unit),
reproduced identically at the P3.1 state; P3.2-focused unit tests pass
(worktree-removal + diff-viewer-removal 37, agent-manager-absence +
agent-manager-tool + session-list 29). The architecture-impact checker reports
PASS for the work unit (tracker-only spec does not substitute for docs; the
canonical docs were already updated in the tree). No P3.3/P3.4 removal, P4
private-runtime/config parity, P5 readiness, full H-matrix rerun, or performance
improvement/regression claim is made.

P3.3 evidence update (2026-08-16): P3 is Active and P3.3 (Product removal —
cloud sessions, JetBrains, Console, KiloClaw, LOCK-003) is Complete. The final
implementation permanently removes the four LOCK-003 products: cloud sessions
(preview/import/fork: the extension's cloud-session handler, `CloudSessionList`,
`CloudImportDialog`, cloud-session prune context, cloud deep link, and the cloud
session message protocol; the CLI-side `cloud-session.ts` module, `cloud-fork`
flags/helpers, and cloud import validation; the gateway's cloud-session
endpoints), JetBrains (the `kilo-jetbrains` package directory, JetBrains-only
workflows `test-jetbrains.yml`/`publish-jetbrains.yml`/`prepare-jetbrains-release.yml`/`codeql-kotlin.yml` and their allowlist entries, turbo tasks, release scripts/skills, container images, and CI build filters), the local Console (the `kilo-console`/`kilo-web-ui` packages, the CLI `console` command source/registration/assets, `/console` serve/workspace routing, the opencode Console build/copy steps, the Console CI workflow, and the Console user docs), and KiloClaw (the `KiloClawProvider`, the `kiloclaw` webview tree/bundle, the `kilo-code.new.kiloClawOpen` command, the KiloClaw TUI route/command/view wiring, and the KiloClaw message types). Retained shared surfaces are asserted intact: remote sessions, generic session import, the EventServiceClient/presence bridge, the migration-bridge `config-console`/`experimental.console.*`/generated-SDK Console group/core `consoleState`, the current `kilo serve` HTTP/SSE/SDK migration bridge, the retained `agentManagerOpen`/`openInTab`/`agentManager.newTab` commands, and the Open-in-Tab panel and Agent Manager readiness. Docs: `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`
was updated in this work unit to document the implemented P3.3 removal — the
"Product runtime absence (P3.3 removal)" section (cloud sessions, KiloClaw,
local Console, JetBrains each structurally absent with no manifest
contribution/runtime-registered command/bundled product entry/startup
registration; the shared editor-owned backend bridge unchanged per LOCK-009)
and its "Rollback path" (git revert of the P3.3 removal commit(s); no feature
flag or compatibility shim — the removed handlers/providers/webview
trees/message types/manifest contributions/bundle entries remain in git
history), plus the E2E harness table row for the `cloud-claw-removal` scenario;
`packages/kilo-docs/source-links.md` was updated; the release note `doc:
.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor) was added, and
the SDK/OpenAPI were regenerated. Durable evidence: `test:
packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (static absence
contract for the cloud-session and KiloClaw surfaces across extension/webview
source and message unions, manifest/esbuild/knip/tsconfig, and i18n keys, with
retained surfaces asserted intact, plus the E2E wiring assertions for the
`cloud-claw-removal` scenario registration/evidence-mapping/package script),
`test: packages/opencode/test/kilocode/cloud-claw-removal.test.ts` (CLI-side
cloud-session/KiloClaw structural absence across source, HTTP group/handlers,
and CLI/TUI), `test: packages/opencode/test/kilocode/console-removed.test.ts`
(Console product removal across packages, CLI command/serve routing/build/CI/
docs while the shared migration-bridge Console APIs survive), and `test:
packages/script/tests/jetbrains-removed.test.ts` (JetBrains package/workflows/
turbo/release/CI/build-filter absence). Final runtime evidence: a focused real
Extension Host `test:e2e:cloud-claw-removal` campaign was recorded under
`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-35-000Z/`
via `test: packages/kilo-vscode/tests/e2e/runner.ts` (`assertCloudClawRemoval`,
identifier-based forbidden product IDs and forbidden bundle prefixes) + `test:
packages/kilo-vscode/script/e2e-probe.ts` + `test:
packages/kilo-vscode/script/e2e-probe-launch.mjs` (`test:e2e:cloud-claw-removal`)
+ `test: packages/kilo-vscode/script/e2e-evidence.ts` (evidence inventory) — a
complete validated manifest (schema `kilo-e2e-evidence/1`, `status: complete`,
`validated: true`, run `success: true`, `missing: []`, `malformed: []`, 7 files
all hash-verified, no notes), the `cloud-claw-removal-runtime-evidence` records
contributed views `[]` and all forbidden manifest/runtime/bundle hits empty
(`viewHits`/`containerHits`/`commandHits`/`bindingHits`/`menuHits`/
`settingHits`/`identifierHits` all `[]`, runtime `kiloCommandTotal` 68 with
`forbiddenCommandHits` `[]`, `bundleCount` 76 with `forbiddenBundleHits` `[]`),
the retained `agentManagerOpen`/`openInTab`/`agentManager.newTab` commands
present, the Open-in-Tab panel (1, ready true) and Agent Manager (ready true)
both ready, no LLM/external requests, and exact PID/CDP-port/scratch cleanup
(no owned VS Code process remains). Command: `KILO_E2E_EVIDENCE_DIR=<abs>
bun run test:e2e:cloud-claw-removal` from `packages/kilo-vscode`. Final
benchmark: a descriptive Extension Host scenario-1 (cold-start, fresh no-seed)
campaign was recorded under
`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`
(git HEAD `2d61c228e71d6b67db15c2a7ff2be9e728e775e4`, gitDirty true, VS Code
1.133.0, extension 7.4.11, recorded final CLI snapshot sha256 prefix
`229cad7ae3…e5a3b`, snapshot/source size 156821474, 1 warmup + 5 measured, 6/6
samples ok, finish status ok, 13 n=5 summaries). Static complexity deltas
(section 8): webview message types 274→263 (delta −11: 144 WebviewMessage +
119 ExtensionMessage, disjoint), generated v2 SDK public methods 239→234 (delta
−5), webview esbuild entries 4→3 (delta −1: the `kiloclaw` bundle retired).
Removal evidence is recorded per category in section 7 for all four P3.3 rows
(cloud sessions, JetBrains, Console, KiloClaw — complete across the categories
that exist, with the shared migration-bridge Console APIs and generic retained
names explicitly asserted as surviving, not removed). No reconstruction-candidate
rows are added for P3.3: the four products are inherent permanent removals
(LOCK-003), not displaced behaviors needing evidence preservation, and are
recorded as not candidates (section 7; LOCK-015). The earlier P3.3 work is
already in the final tree (git HEAD `2d61c228e7` is the P3.2 commit and the
current worktree is dirty and uncommitted), so no pre-final P3.3 evidence
directories exist to exclude. Decisions: no P3.3 decision locks changed; P3
overall remains Active, never Complete until P3.4 exits; P3.4, P4, and P5 remain
Not started. Validation: SDK generator pass; `bun install` lockfile pass; SDK
typecheck/build pass; opencode/kilo-vscode/kilo-gateway/ui/script/sdk
typechecks pass; kilo-vscode lint pass; docs full suite 22/22; workflow guard 24
workflows pass; source-links pass/idempotent; architecture-impact pass (20
signals); promise facade pass; focused unit suites pass (combined
`cloud-claw-removal.test.ts` + `e2e-evidence.test.ts` 41, opencode
`cloud-claw-removal.test.ts` 5, `console-removed.test.ts` 11,
`jetbrains-removed.test.ts` 7, `check-architecture-impact.test.ts` 118,
regression campaign `snapshot-tool-race.test.ts` + `summary-file-diff.test.ts`
plus removal guards 18/18 with the snapshot race isolated 3/3). Known baselines: the pull-back-contract test file received a 2-line anchor adaptation (its `handleCloudSessionDataLoaded` end-of-block anchor was replaced with `// Actions` after the cloud removal), but the one-failure still reproduces at pristine P3.2 and is not a P3.3 failure; the cli-shutdown mock-leak likewise reproduces at pristine P3.2; the full VS Code unit suite carries the
same known pull-back baseline (no clean full-suite claim). No P3.4 removal, P4
private-runtime/config parity, P5 readiness, new H-1..H-13 parity campaign
(P2 remains the full parity proof; LOCK-008/H-1..H-13 parity stays intact), or
performance improvement/regression claim is made.

## 5. Phase Details And Exit Checklists

Exit criteria below are taken from the direction spec (section 10) and the runtime
spec (sections 5-7). Each checkbox is complete only when objective evidence
(issue/PR/test/doc) is recorded alongside it.

### P0 - Baseline inventory

- Status: Complete (2026-08-12; started Active 2026-08-10)
- Scope: Freeze the surface inventory (direction spec 1.3), message protocol
  inventory, removal inventory (direction spec section 9, tracker section 7), and
  runtime/config root-cause inventory (runtime spec section 2: config sources,
  provider sources/loaders, readiness chain stages, convergence machinery); build
  a runnable baseline fixture/inventory for the named H-1..H-13 flows; resolve the
  bounded implementation decisions required by P0/P1 (runtime spec section 9).
- Exit checklist (2026-08-10 state, evidence updated 2026-08-12; closed 2026-08-12):
  - [x] Runnable baseline fixture/inventory covering the named H-1..H-13 harness flows — MET (2026-08-12): fixture executes all H-1..H-13 as live tests through real production services/run-owned fixtures; H-3..H-6 gap records removed (H-3 real ToolRegistry, H-4 Skill.Service + SkillTool, H-5 production MCP transport with a run-owned stdio server cleaned by exact PID, H-6 Permission/Question ask -> reply with pending-state observation) (`test: packages/opencode/test/kilocode/p0-harness-baseline.test.ts`); fixture is baseline evidence only — no target-surface parity claimed for any H (section 6)
  - [x] Reproducible surface inventory (direction spec 1.3) — `doc: specs/vscode-orchestrator/p0-current-state-inventory.md` (section 3)
  - [x] Message protocol inventory (used/unused per the P0 protocol inventory) — `doc: specs/vscode-orchestrator/p0-current-state-inventory.md` (section 4; static classification only, runtime dispatch still Unknown, U-1)
  - [x] Removal inventory for all LOCK-002/003/004/006 removals with source paths (section 7) — `doc: specs/vscode-orchestrator/p0-current-state-inventory.md` (section 5; residual current-state evidence; no removal claimed complete)
  - [x] Runtime/config root-cause inventory: enumerated config sources, provider sources/loaders, readiness chain stages, convergence machinery paths (runtime spec section 2) — `doc: specs/vscode-orchestrator/p0-current-state-inventory.md` (section 6; 15 merge sources enumerated, readiness chain stages 1-10, convergence machinery file set)
  - [x] Baseline counts recorded in section 8 — MET (2026-08-12): Q3 approved; counts recorded in inventory (sections 4.4/9, reproducible commands in section 10) and reflected in tracker section 8 (webview message types 332, provider methods 250, webview entry points 6)
  - [x] Open question 3 resolved (exact baseline metric definitions) — MET (2026-08-12): Q3 resolved — 332 distinct webview message `type:` literals (WebviewMessage 189 + ExtensionMessage 143, disjoint sets); 250 generated v2 SDK `KiloClient` public methods (the extension imports `@kilocode/sdk/v2/client`); 6 HTML/webview esbuild entries, excluding the shiki worker asset (section 9)
  - [x] Bounded implementation decisions required by P0/P1 recorded (runtime spec section 9) — MET (2026-08-12): R1 (private transport = JSON-RPC 2.0 over child-process stdio, Content-Length framing, `vscode-jsonrpc` precedent; one extension-owned worker child; initialize handshake replaces port detection/health; requests carry commands, notifications carry normalized event envelopes; stderr bounded diagnostics; EOF/process exit owns lifecycle; HTTP/SSE/generated SDK remains bridge-only and is deleted; no retained-terminal protocol commitment), R2 (extension-owned product/UI config + persisted selector indexes in VS Code `globalState`; per-workspace runtime-tracking state in `workspaceState`; secrets in `SecretStorage`; runtime-owned session/event/artifact persistence remains runtime-owned; immutable worker snapshots are derived versioned values, not a second persisted store), R5 (one canonical project boundary = first VS Code workspace root; project-versioned harness assets only under `<workspaceRoot>/.kilo/`; P4.1 migrates root/legacy sources; P4.4 deletes ancestor walk, `.kilocode`/`.opencode`, global project-asset sources, primary-worktree mirror reads; no multi-source precedence remains), R6 (explicit deadline = the P4.3 phase boundary: dual-read opens only during P4.3, shrinks monotonically, gains no new bridge consumers, fully closed before P4.3 exits / P4.4 begins; P4.4/P4.5 delete source/transport/product code; no fallback read survives into P4.4), R8 (retain the existing two-harness tooling as the P0 and later comparison harness: Extension Host scenarios 1/2/3/4/5/10 under `packages/kilo-vscode/script/p0-bench/` + runner/merge/safety/provenance tools, backend scenarios 6/7/8/9/11/12/13 under `packages/opencode/test/benchmark/` + runner; limitations recorded: manual-only, platform/environment/provenance scoped, backend in-process `Server.listen`/`AppLayer` only, n=5 descriptive); R7 stays Open but is not a P0 blocker (required-by clarified, section 9). **Post-P0 decision revision (2026-08-13):** R2 and R6 were revised by a durable user clarification — file-authoritative hybrid configuration (canonical files/assets own effective config; `globalState`/`workspaceState` own only UI-local/derived state) and an atomic legacy-reader cutover at P4.3 (no dual-read window, no import tool). The original 2026-08-12 wording above remains the historical P0 record; the revised texts are current in section 9 and the runtime spec. P0 stays Complete; its recorded evidence is unchanged
  - [x] Performance instrumentation executed at the runtime spec (section 10.8) instrumentation points; per-stage cold/warm timings recorded in section 8 — MET (2026-08-12): opt-in `KILO_P0_PERF` instrumentation exists for backend/extension/webview (`test: packages/opencode/test/kilocode/p0-instrument.test.ts`, `test: packages/kilo-vscode/tests/unit/p0-perf-instrument.test.ts`); per-stage cold/warm timings are recorded in section 8 from the formal cold-start, many-agent-MCP, warm-view/no-provider/custom-provider, session-switch, and backend campaigns; target-only stages without an accepted campaign or an existing extension-owned index are re-scoped to later-phase gates (persisted-selector paint → P5; attribution + per-event transport/webview flush → P2; removed-feature init count + startup-work reduction → P3/P4.4)
  - [x] P0 performance baseline metrics recorded in section 8 with evidence links (all performance evidence Not proven until recorded; LOCK-PERF-6) — MET (2026-08-12): rows for worker cold start, server->SSE connect, UI fetch chain -> data ready, warm view, no-provider startup (fresh no-seed and seeded-empty persisted conditions), custom-provider startup, many-agent/MCP startup, backend prompt-submit, tool/permission round-trip, parallel sessions, hot/cold/burst config updates, and session switch now record measured n=5 values with evidence links and explicit scope (descriptive p95=max at n=5, not SLA/threshold); the five remaining `Not proven` rows are re-scoped later-phase gates, not P0 blockers (persisted-selector paint → P5; attribution + per-event transport/webview flush → P2; removed-feature init count + startup-work reduction → P3/P4.4)
  - [x] Benchmark scenarios (runtime spec section 10.9) runnable and reproducible — MET (2026-08-12): backend harness (scenarios 6,7,8,9,11,12,13) under `packages/opencode/test/benchmark/` with runner `packages/opencode/script/p0-benchmark.ts`; Extension Host harness (scenarios 1,2,3,4,5,10) under `packages/kilo-vscode/script/p0-bench/`; both have passing unit/integration tests, including harness-fix tests (`test: packages/kilo-vscode/tests/unit/p0-bench-launch-failure.test.ts`, `test: packages/opencode/test/kilocode/p0-bench-seed-config-schema.test.ts`). Full repeated baselines now recorded for all 13 defined scenarios: backend all seven scenarios (1 warmup + 5 measured each, 42/42 ok) in the historical `2026-08-11T04-46-19-926Z/backend.jsonl` and the current-tier rerun `2026-08-12T06-08-27-792Z/backend.jsonl`; Extension Host cold-start (`2026-08-11T11-51-16-883Z`); Extension Host many-agent-MCP (`many-agent-mcp-merged-2026-08-12T00-27-49-435Z`, baselineComplete); Extension Host warm-view/no-provider/custom-provider (`2026-08-12T05-52-07-672Z`, 19 samples, 27 n=5 summaries); Extension Host session-switch (`2026-08-12T07-21-22-201Z`, 1 warmup + 5 measured, one n=5 summary, status ok). Two pre-fix partial artifacts are retained but excluded and never cited as baseline: `2026-08-12T04-30-39-069Z/benchmark.jsonl` (no run finish, no custom-provider samples) and `2026-08-12T06-05-12-356Z/backend.jsonl` (no run finish, scenarios 6/7/8 only)
  - [x] Static redundancy candidate inventory recorded (module-scope AppRuntime handle + AppLayer graph construction at listener build, per-instance bootstrap, feature-layer startup, removed-feature startup contributions; runtime spec section 10.4) — `doc: specs/vscode-orchestrator/p0-current-state-inventory.md` (section 7, R-1..R-10)
  - Evidence: issue: - | PR: - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`, `packages/opencode/test/kilocode/p0-instrument.test.ts`, `packages/opencode/test/kilocode/p0-bench-seed-config-schema.test.ts`, `packages/opencode/test/benchmark/`, `packages/kilo-vscode/tests/unit/connection-service-model-first.test.ts`, `packages/kilo-vscode/tests/unit/p0-perf-instrument.test.ts`, `packages/kilo-vscode/tests/unit/p0-bench-capture.test.ts`, `packages/kilo-vscode/tests/unit/p0-bench-parse.test.ts`, `packages/kilo-vscode/tests/unit/p0-bench-stats.test.ts`, `packages/kilo-vscode/tests/unit/p0-bench-cleanup.test.ts`, `packages/kilo-vscode/tests/unit/p0-bench-launch-failure.test.ts`, `packages/kilo-vscode/tests/unit/stderr-tail.test.ts` | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md`, `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-11T11-51-16-883Z/benchmark.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl` + `merge-manifest.json`, `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-11T04-46-19-926Z/backend.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T05-52-07-672Z/benchmark.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T07-21-22-201Z/benchmark.jsonl`
- Next actions: none for P0 (complete). Keep the harness-fix regression tests green (scenario 4 fixture schema correction, shared fixture schema test, early `runTests` failure observation, bounded blocked-sample fallback). The five re-scoped metric rows are later-phase gates owned by their phases: persisted-selector paint → P5; cost attribution + per-event transport/webview flush → P2; removed-feature initialization count + startup-work net reduction → P3/P4.4. P1 is Not started by hub decision and must not start in this change. P0 closure uses tracker-local objective test/doc evidence and recorded decisions; no GitHub issue is required.

### P1 - Orchestration-first navigation

- Status: Complete (2026-08-14; P0 is Complete 2026-08-12; Q1 resolved 2026-08-14 — Topic is a derived navigation concept; P1 exit evidence recorded below)
- Scope: Topic/session navigation as the main view over the migration bridge
  backend; session picker; ordinary single-chat sidebar keeps working unchanged.
- Exit checklist:
  - [x] Topic/session navigation works across sessions without worktree dependency (LOCK-002) — MET (2026-08-14): derived Topic read-model (`packages/kilo-vscode/webview-ui/agent-manager/topics.ts`) groups sessions across the migration bridge from root session ID / parentID membership only — no worktree dependency; covered by topics unit tests (`test: packages/kilo-vscode/tests/unit/topics.test.ts`) and the Topic-navigation Storybook Playwright interaction spec (7 passed: topic/child selection, disclosure, active-child highlighting, root relabel, delete-degradation, orphan/cycle degradation, collapse persistence across inventory updates; `test: packages/kilo-vscode/tests/topic-navigation.spec.ts`)
  - [x] No ordinary single-chat sidebar capability change — MET (2026-08-14): P1 work touches only the Agent Manager orchestration surface (webview `topics.ts`, `SidebarSessionList.tsx`, agent-manager CSS/i18n, stories) — the ordinary single-chat sidebar surface is untouched; P3.1 removal scope and disposition are unchanged (section 7)
  - [x] Open question 1 (topic meaning) decided — MET (2026-08-14): Topic is a derived navigation concept (direction spec section 14); P1 implementation may begin
  - [x] Affected-path performance comparison recorded on the navigation/session-switch path against the P0 baseline — MET (2026-08-14): descriptive same-environment comparison recorded in section 4 — scenario-10 campaign `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-14T11-45-17-512Z/benchmark.jsonl` (git 99d275a9d7, gitDirty true, VS Code 1.133.0, 1 warmup + 5 measured, all ok; n=5 min 154 / median 177 / p95=max 724 / mean 291.2 ms) vs P0 session-switch baseline `2026-08-12T07-21-22-201Z/benchmark.jsonl` (git 7a768502dd, VS Code 1.132.0; n=5 min 115 / median 195 / p95=max 282 / mean 201.6 ms); drift recorded (git 7a768502dd→99d275a9d7 both dirty, VS Code 1.132.0→1.133.0, CLI snapshot changed) and one 724 ms outlier sample flagged; no structural anomaly, no performance conclusion — descriptive only, no threshold, no requirement to improve (R7 resolved 2026-08-14; runtime spec section 10.9)
  - [x] Lifecycle-boundary convergence evidence recorded for the extension-owned view boundaries — panel close/reopen, reload, session switch/navigation — on the migration bridge (direction spec section 11; runtime spec section 7.1) — MET (2026-08-14): Extension Host E2E topic-navigation scenario drives all three boundaries — session navigation (real Topic/child clicks with active tab/header/topic convergence), panel close/reopen (Agent Manager tab closed via the tab-groups API, reopened, fixture facts rehydrated), and webview reload (host webview reload action, fresh document re-derives the same Topic hierarchy/active state from re-seeded facts) — via the harness (`test: packages/kilo-vscode/tests/e2e/runner.ts`, `test: packages/kilo-vscode/script/e2e-probe.ts`); `test:e2e:topic-navigation` passed and the full `test:e2e` passed
  - Evidence: issue: - | PR: - | test: `packages/kilo-vscode/tests/unit/topics.test.ts`, `packages/kilo-vscode/tests/unit/agent-manager-arch.test.ts`, `packages/kilo-vscode/tests/unit/agent-manager-i18n.test.ts`, `packages/kilo-vscode/tests/unit/agent-manager-i18n-split.test.ts`, `packages/kilo-vscode/tests/topic-navigation.spec.ts`, `packages/kilo-vscode/tests/e2e/runner.ts`, `packages/kilo-vscode/script/e2e-probe.ts` | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-14T11-45-17-512Z/benchmark.jsonl`
- Next actions: none for P1 (complete). Keep the Topic-navigation Storybook spec, topics unit tests, agent-manager arch/i18n tests, and the Extension Host topic-navigation E2E scenario green. At P1 closure (2026-08-14) P2 was Not started by hub decision and did not start in that change; P2 has since exited Complete 2026-08-15 with its own recorded evidence (sections 4, 5, 6, 8), which claimed no removal, SLA, or production-behavior architecture change and documented the implemented env-gated fixture bridge in the canonical `vscode-extension.md` (sections 6-7, 10; LOCK-013/014); no GitHub issue is required.

### P2 - Harness surface parity

- Status: Complete (2026-08-15; P1 is Complete 2026-08-14; P2 exit evidence recorded below and in sections 4, 6, 8)
- Scope: Every H-1..H-13 capability reachable from orchestration panels
  (agent/model selectors, delegation, tools/skills/MCP, permission rendering,
  checkpoint review), proven on the current migration bridge for the in-scope
  current-behavior capability (target-contract split: typed-manifest, restrictive
  policy-stack, and WYSIWYG contracts gate at P4, never at P2).
- Exit checklist:
  - [x] Every H-1..H-13 target-surface acceptance criterion (direction spec section 6) passes from the target surface — MET (2026-08-15): final durable campaign `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/` — four sub-campaigns (real-completed, real-session, real-overflow, real-restart), each a complete validated manifest (135 manifest entries covering 127 unique files, all 127 hash-verified), 28/28 generation requests run-owned `e2e-local/e2e-model`, 0 matrix violations, 12/12 UI-send pins pass; per-H evidence in section 6
  - [x] Named evidence inventory for each H-1..H-13 criterion recorded in section 6 — MET (2026-08-15): every H-1..H-13 row records its falsifiable evidence path into the campaign artifacts (section 6)
  - [x] Affected-path performance comparison recorded on harness-parity flows (prompt submit, streaming, tool/permission) against the P0 baseline — descriptive, same-environment; record med/p95/sample/provenance and investigate obvious structural anomalies; no numeric threshold and no requirement to improve (R7 resolved 2026-08-14; runtime spec section 10.9) — MET (2026-08-15): descriptive backend scenario 6/7/9 n=5 summaries (`doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T06-18-28-167Z/backend.jsonl`) recorded vs the P0 baseline in section 8 with drift (git 7a768502dd→f91d22ae1f, both dirty), no threshold and no improvement/regression claim; cost attribution (LOCK-PERF-7) and per-event transport/webview render-flush recorded as descriptive evidence, not an independent P2 exit blocker (runtime spec section 10.11)
  - [x] Lifecycle-boundary convergence evidence recorded for all five boundaries — panel close/reopen, reload, session switch (view) plus transport reconnect and worker restart (runtime) — on harness-parity flows over the current bridge (direction spec section 11; runtime spec section 7.1) — MET (2026-08-15): real-restart sub-campaign records SSE transport reconnect (`rr-conn.json`: connecting→connected, same port/pid; "PASS backend session facts unchanged after SSE reconnect") and exact worker restart (`rr-kill.json` pid 53824 → `rr-reconnect.json` pid 54774/port 55436; transcript/artifact rehydrated), reload (`rr-reload-executed`/`rr-reloaded` markers) and true window/Extension Host restart (`workbench.action.reloadWindow`, extension-host pid 53694→55870, session resumed); panel close/reopen in real-session (`real-reopen-ready`; session B transcript rehydrates) and real-completed (reopened panel renders parent/child topic); session switch in real-session (tab A closed leaves tab B)
  - [x] Target-surface product/harness behavior established for what is in scope; temporary capability absence explicitly recorded and every recorded absence resolved before P2 exits; final H parity not waived by a tracker or reconstruction-candidate entry (LOCK-014) — MET (2026-08-15): in-scope H-1..H-13 current-behavior capability proven on the bridge (section 6); the P4-gated target contracts and final private-runtime parity are explicitly not proven at P2 (LOCK-014); non-independent blockers recorded: per-event webview render-flush not emitted and model HTTP duration not separately measured (runtime spec section 10.11)
  - Evidence: issue: - | PR: - | test: `packages/kilo-vscode/script/e2e-probe.ts` (real-completed/real-session/real-overflow/real-restart scenarios), `packages/kilo-vscode/script/e2e-evidence.ts`, `packages/kilo-vscode/script/e2e-pin.ts`, `packages/kilo-vscode/script/e2e-probe-dom.ts`, `packages/kilo-vscode/script/e2e-probe-restart.ts`, `packages/kilo-vscode/script/e2e-llm-matrix.ts`, `packages/kilo-vscode/script/e2e-completed-seed.ts`, `packages/kilo-vscode/script/e2e-restart-seed.ts`, `packages/kilo-vscode/script/e2e-scripted-model.ts`, `packages/kilo-vscode/src/agent-manager/fixture-backend.ts`, `packages/kilo-vscode/src/services/cli-backend/llm-request-collector.ts`, `packages/kilo-vscode/tests/unit/e2e-evidence.test.ts`, `packages/kilo-vscode/tests/unit/e2e-pin.test.ts`, `packages/kilo-vscode/tests/unit/e2e-probe-restart.test.ts`, `packages/kilo-vscode/tests/unit/e2e-scripted-model.test.ts`, `packages/kilo-vscode/tests/unit/fixture-backend.test.ts`, `packages/kilo-vscode/tests/unit/llm-request-collector.test.ts` | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/` (`real-completed/manifest.json` + `real-completed-dom-evidence` + `llm-matrix-real-completed-final.json` + `run.log`, `real-session/manifest.json` + `real-dom-evidence`, `real-overflow/manifest.json` + `real-overflow-dom-evidence`, `real-restart/manifest.json` + `rr-dom-evidence` + `rr-conn.json` + `rr-kill.json` + `rr-reconnect.json` + `rr-pin.json`), `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T06-18-28-167Z/backend.jsonl`
- Next actions: none for P2 (complete). Keep the fixture-gated E2E harness, request-level isolation assertions, and pin evidence green. P3 is the next phase (removal inventory unchanged, section 7); P2 proved no P3 removal and no P4 private-runtime/config parity (LOCK-014); the two non-independent P2 performance blockers (per-event webview render-flush not emitted; model HTTP duration not separately measured) remain descriptive evidence for later phases (runtime spec section 10.11).

### P3 - Product removal

- Status: Complete (2026-08-17) — P3.1 Complete (2026-08-15); P3.2 Complete (2026-08-16); P3.3 Complete (2026-08-16); P3.4 Complete (2026-08-17). P3 overall
  is Complete — every removal subphase has exited.
- Scope: Remove the decided product surfaces.
  - P3.1: ordinary single-chat sidebar removal (LOCK-001) — Complete (2026-08-15)
  - P3.2: worktree infrastructure + custom Diff Viewer surfaces (LOCK-002) — Complete (2026-08-16)
  - P3.3: cloud sessions, JetBrains, Console, KiloClaw (LOCK-003) — Complete (2026-08-16)
  - P3.4: indexing, project memory, user-visible context management/compaction,
    autocomplete (LOCK-004); retain only the internal overflow safeguard (LOCK-005) — Complete (2026-08-17)
- Exit checklist (P3-wide; each removal subphase completes its own subset):
  - [x] Each removal's evidence recorded in section 7 per category (source/tests/docs/generated SDK/config/i18n/build/package) — MET (2026-08-17): P3.1 sidebar row complete (2026-08-15); P3.2 worktree/Diff Viewer rows complete (2026-08-16); P3.3 cloud/JetBrains/Console/KiloClaw rows complete (2026-08-16); P3.4 indexing/memory/context-management/autocomplete rows complete (2026-08-17) — all per-category evidence in section 7
  - [x] H-1..H-13 parity intact (P2 evidence; direction spec section 11) — MET (2026-08-17): P2 remains the full H-1..H-13 parity proof (section 6); P3.4 re-proves H-13 retained parity on the post-removal surface (section 6); no H invariant weakened by any P3 removal
  - [x] Documented rollback/revert path shipped with each removal subphase — MET: P3.1 rollback documented (git revert; architecture docs 2026-08-15); P3.2 rollback documented (git revert; architecture docs 2026-08-16); P3.3 rollback documented (git revert; architecture docs 2026-08-16); P3.4 rollback documented (git revert; architecture docs 2026-08-17)
  - [x] No removed feature reclassified as deferred — MET: section 7 records complete per-category evidence with no deferred language for every P3 row
  - [x] Each removal subphase proves the removed feature's initialization/readers/listeners/resources are absent from worker startup and records affected startup/session-switch/memory/worker-lifecycle deltas against the P0 baseline; zero or positive noisy delta is allowed if no removed work remains and no structurally unbounded growth/resource leak appears (LOCK-PERF-3; runtime spec section 10.9-10.10) — MET: P3.1 structural absence proved statically + `test:e2e:sidebar-removal` (2026-08-15); P3.2 structural absence proved statically + runtime (`worktree-removal-runtime-evidence` — all forbidden hits empty; 2026-08-16); P3.3 structural absence proved statically + runtime (`cloud-claw-removal-runtime-evidence` — all forbidden hits empty; 2026-08-16); P3.4 structural absence proved statically + runtime (`p3-4-removal-runtime-evidence` — forbidden manifest/runtime/bundle/state hits empty, 0 model requests; 2026-08-17); descriptive scenario-1 cold-start campaigns recorded for each subphase vs P0 baseline (section 8); zero or positive noisy delta observed; no removed work remains and no structurally unbounded growth/resource leak appears
  - [x] Harness semantics unregressed on every removal (LOCK-PERF-5) — MET: H-13 P0 harness 8/8 and automatic overflow suites pass post-P3.4; H-8 and H-12 re-evidenced at P3.2 (section 6); all H-1..H-13 remain intact
  - Evidence: issue: - | PR: - | test: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts`, `packages/kilo-vscode/tests/unit/chat-target.test.ts`, `packages/kilo-vscode/tests/unit/worktree-removal.test.ts`, `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts`, `packages/opencode/test/kilocode/agent-manager-absence.test.ts`, `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts`, `packages/opencode/test/kilocode/cloud-claw-removal.test.ts`, `packages/opencode/test/kilocode/console-removed.test.ts`, `packages/script/tests/jetbrains-removed.test.ts`, `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts`, `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts`, `packages/kilo-vscode/script/e2e-probe.ts`, `packages/kilo-vscode/script/e2e-probe-worktree.ts`, `packages/kilo-vscode/script/e2e-probe-launch.mjs`, `packages/kilo-vscode/script/e2e-evidence.ts` | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-35-000Z/`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-4-final-2026-08-17T00-52-00-000Z/`, `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl`, `.changeset/remove-sidebar-chat.md`, `.changeset/agent-manager-root-local-sessions.md`, `.changeset/remove-jetbrains-console-cloud-kiloclaw.md`, `.changeset/p34-docs-update.md`

#### P3.1 - Ordinary single-chat sidebar removal (LOCK-001)

- Status: Complete (2026-08-15; P2 is Complete 2026-08-15; P3 is Active)
- Scope: Remove the ordinary single-chat Activity Bar sidebar
  (`kilo-code.SidebarProvider` webview view under the `kilo-code-ActivityBar`
  views container), its manifest contributions and dependent code, and route
  chat commands that previously used the sidebar fallback through the preserved
  editor surfaces (Agent Manager / Open in Tab). No numeric adoption threshold:
  P1/P2 supplied the migration affordances before removal (Q2/Q4/R4 resolved
  2026-08-15, section 9).
- Exit checklist:
  - [x] Sidebar surface and dependent code removed — MET (2026-08-15): static absence proven by `test: packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` — no `kilo-code-ActivityBar` container, no `kilo-code.SidebarProvider` view, no `registerWebviewViewProvider`, no `resolveWebviewView`/`setSidebarVisible`, no `kilo-code.new.sidebarVisible` context, no `sidebarTitle.*` commands/menus, and no stale sidebar focus references in any production source; preserved surfaces (Agent Manager, Open in Tab, standalone panels) and editor/title toolbar contributions asserted intact
  - [x] Chat command re-routing — MET (2026-08-15): `test: packages/kilo-vscode/tests/unit/chat-target.test.ts` exercises `resolveChatTarget`/`ensureChatTab`/`waitForChatReady` (active tab preferred, Agent Manager cold-open, readiness-gated delivery, no-post-on-failure); `test: packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` asserts routing through `resolveChatTarget`/`ensureChatTab` with no surrogate hidden sidebar provider
  - [x] i18n and telemetry residue removed — MET (2026-08-15): `test: packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` asserts the demonstrably sidebar-exclusive keys are absent from every app locale dictionary and all production source, the shared sidebar keys survive in every locale, and the `TITLE_BUTTON_CLICKED` telemetry event name — emitted only by the removed sidebar title wrappers, with no remaining emitter — is gone (reconstruction candidate RC-P3.1-001, disposition `drop`, section 7)
  - [x] Focused real Extension Host verification — MET (2026-08-15): `test:e2e:sidebar-removal` passed via `test: packages/kilo-vscode/tests/e2e/runner.ts` (`assertSidebarRemoval`) + `test: packages/kilo-vscode/script/e2e-probe.ts` — the loaded manifest contributes no Activity Bar sidebar surface under the forbidden ids/prefixes (identifier-based), the production Open-in-Tab editor panel reaches webview readiness through the env-gated fixture bridge, and the Agent Manager still reports readiness afterwards
  - [x] Rollback/revert path documented — MET (2026-08-15): documented in `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` ("Rollback path") — git revert of the P3.1 removal commit(s); no feature flag or runtime shim
  - [x] Removal evidence per category in section 7 — MET (2026-08-15): source/tests/docs/config/i18n/build-package complete; generated SDK `-` by nature (no SDK contract change; section 8 delta 0)
  - [x] Affected-path performance comparison recorded — MET (2026-08-15): descriptive scenario-1 cold-start campaign `doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl` vs the P0 baseline (section 8), drift recorded, no threshold and no improvement/regression claim (R7)
  - [x] No removed feature reclassified as deferred — MET (2026-08-15): section 7 sidebar row records complete per-category evidence with no deferred language
  - Evidence: issue: - | PR: - | test: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts`, `packages/kilo-vscode/tests/unit/chat-target.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts`, `packages/kilo-vscode/script/e2e-probe.ts` | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`, `.changeset/remove-sidebar-chat.md` (minor)
- Next actions: P3.2 (worktree infrastructure + custom Diff Viewer surfaces, LOCK-002) is the next subphase and remains Not started; P3.3/P3.4 follow. Keep the P3.1 static contract and chat-target unit tests and the `test:e2e:sidebar-removal` scenario green. At P3.1 closure (2026-08-15) P3.2 was Not started by hub decision and did not start in that change; P3.2 has since exited Complete 2026-08-16 with its own recorded evidence (sections 4, 5, 7, 8).

#### P3.2 - Worktree infrastructure + custom Diff Viewer removal (LOCK-002)

- Status: Complete (2026-08-16; P3.1 is Complete 2026-08-15; P3 is Active;
  P2 is Complete 2026-08-15)
- Scope: Permanently remove the managed git-worktree infrastructure and all
  custom Diff Viewer surfaces (LOCK-002), plus the orphaned `agent_manager`
  list/prompt request-reply service; retain root-local start delegation, the H
  paths, H-12 SessionRevert/Snapshot rollback, and generic
  worktree/project/snapshot semantics. P4.4's primary-worktree mirror-read
  removal is explicitly out of P3.2 scope.
- Exit checklist:
  - [x] Managed worktree infrastructure removed — MET (2026-08-16): static absence proven by `test: packages/kilo-vscode/tests/unit/worktree-removal.test.ts` — no `WorktreeManager`/`WorktreeStateManager`/`PRStatusPoller`/`SetupScriptService`, no `run/` controller subsystem, no `git-transfer`, no `worktree-mode` context, no `BranchSelect`, no worktree message types, no `agentManager.worktree.*` / `agentManager.run.*` i18n keys, no worktree commands/keybindings/settings in the manifest, no worktree build entries, and no `Worktree.appLayer`/worktree routes/branch-name route/SDK worktree groups on the CLI side; retained root-local orchestration (Agent Manager panel, session tabs, `openSession`, local-only `remoteSessions`, generic terminals, permissions/questions, `reload-directory`) asserted intact
  - [x] Custom Diff Viewer / Diff Virtual removed — MET (2026-08-16): `test: packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts` asserts no `DiffViewerProvider`/`DiffVirtualProvider`/`SourceController`, no `DiffViewerPanel`/`DiffVirtualPanel` view types, no `showChanges` command, no `diff.renderMarkdown` config, no `diff-viewer`/`diff-virtual` webview bundles or build entries, no `openChanges`/`openDiffVirtual`/`diffViewer.*`/`diffVirtual.*` message protocol, and no `diffViewer.*` i18n keys; preserved H-12 rollback, RevertBanner, Agent Manager local git stats, and `@git-changes` prompt context asserted
  - [x] Orphaned `agent_manager` list/prompt request-reply service removed — MET (2026-08-16): `test: packages/opencode/test/kilocode/agent-manager-absence.test.ts` asserts the `agent-manager` module holds only the `kilocode.agent_manager.start` event (no `protocol.ts`/`service.ts`), no `list`/`reply`/`reject` request-reply, and no orphaned HTTP/SDK surface across the regenerated SDK; the `agent_manager` tool now starts sessions only (start delegation retained)
  - [x] Focused real Extension Host verification — MET (2026-08-16): `test:e2e:worktree-removal` passed via `test: packages/kilo-vscode/tests/e2e/runner.ts` (`runWorktreeRemoval`) + `test: packages/kilo-vscode/script/e2e-probe-worktree.ts` + `test: packages/kilo-vscode/script/e2e-probe.ts` — final campaign `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-09-32-000Z/` (complete validated manifest, 29/29 hashes, 36/36 PASS 0 FAIL, two root-local sessions controllable, H-12 byte exact, 4/4 run-owned model requests, clean resources); runtime manifest/command-table absence of the managed-worktree and custom Diff Viewer surfaces and no `.kilo/worktrees`/`.kilo/agent-manager.json` state created
  - [x] Rollback/revert path documented — MET (2026-08-16): documented in `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` ("Rollback path") — git revert of the P3.2 removal commit(s); no feature flag or runtime shim
  - [x] Removal evidence per category in section 7 — MET (2026-08-16): worktree infrastructure and custom Diff Viewer/Diff Virtual rows complete across source/tests/docs/config/i18n/build-package; generated SDK recorded as the −11 surface reduction (Worktree 7 + BranchName 1 + AgentManager 3; section 8)
  - [x] Affected-path performance comparison recorded — MET (2026-08-16): descriptive scenario-1 cold-start campaign `doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl` vs the P0 baseline (section 8), drift recorded, no threshold and no improvement/regression claim (R7; LOCK-PERF-6/R7 — all metrics descriptive)
  - [x] No removed feature reclassified as deferred — MET (2026-08-16): section 7 worktree/diff rows record complete per-category evidence with no deferred language
  - [x] Removed-feature initialization absent and lifecycle/resources bounded — MET (2026-08-16): runtime evidence (`worktree-removal-runtime-evidence`) records forbidden view/container/command/binding/menu/setting/identifier hits all `[]`, `contributedViewIds` `[]`, `worktreesDir` false, `agentManagerJson` false, setupScripts `[]`, panel worktreeIds/runCards/sections/branchSelect all 0, and clean resources (no owned VS Code process, CDP port released, scratch removed); bounded lifecycle/runtime path evidence recorded in the final campaign (H-8 two root-local sessions controllable, H-12 byte-exact rollback re-evidenced)
  - Evidence: issue: - | PR: - | test: `packages/kilo-vscode/tests/unit/worktree-removal.test.ts`, `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts`, `packages/opencode/test/kilocode/agent-manager-absence.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts`, `packages/kilo-vscode/script/e2e-probe.ts`, `packages/kilo-vscode/script/e2e-probe-worktree.ts` | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-09-32-000Z/`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`, `.changeset/agent-manager-root-local-sessions.md` (minor)
- Next actions: P3.3 (cloud/JetBrains/Console/KiloClaw, LOCK-003) is the next subphase and has since exited Complete 2026-08-16 with its own recorded evidence (sections 4, 5, 7, 8); P3.4 follows. Keep the P3.2 static contract tests (`worktree-removal.test.ts`, `diff-viewer-removal.test.ts`, `agent-manager-absence.test.ts`) and the `test:e2e:worktree-removal` scenario green.

#### P3.3 - Cloud sessions, JetBrains, Console, KiloClaw removal (LOCK-003)

- Status: Complete (2026-08-16; P3.2 is Complete 2026-08-16; P3 is Active;
  P2 is Complete 2026-08-15)
- Scope: Permanently remove the four LOCK-003 products — cloud sessions
  (preview/import/fork), JetBrains, the local Console, and KiloClaw. They are
  inherent permanent removals, never deferred and never reconstruction
  candidates (LOCK-003/015). The shared migration-bridge surfaces are retained
  and asserted intact: remote sessions, generic session import, the
  EventServiceClient/presence bridge, the `config-console`/`experimental.console.*`/
  generated-SDK Console group/core `consoleState`, the current `kilo serve`
  HTTP/SSE/SDK migration bridge, and the `agentManagerOpen`/`openInTab`/
  `agentManager.newTab` commands.
- Exit checklist:
  - [x] Cloud sessions removed — MET (2026-08-16): static absence proven by `test: packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (extension `cloud-session` handler, `CloudSessionList`, `CloudImportDialog`, cloud prune context, cloud deep link, and cloud message protocol absent) and `test: packages/opencode/test/kilocode/cloud-claw-removal.test.ts` (CLI `cloud-session.ts` module, `cloud-fork` flags/helpers, cloud import validation absent across source/HTTP/CLI-TUI); retained remote-sessions/generic-import/EventServiceClient/presence asserted intact
  - [x] JetBrains removed — MET (2026-08-16): `test: packages/script/tests/jetbrains-removed.test.ts` asserts the `kilo-jetbrains` package directory, JetBrains-only workflows and their allowlist entries, turbo tasks, release scripts/skills, container images, and CI build filters are absent; shared non-product dependencies (Kotlin LSP URL/extractor, JetBrains Mono font/i18n name, generic protocol, shared icons) are asserted surviving
  - [x] Console removed — MET (2026-08-16): `test: packages/opencode/test/kilocode/console-removed.test.ts` asserts `kilo-console`/`kilo-web-ui` packages, CLI `console` command source/registration/assets, `/console` serve/workspace routing, opencode Console build/copy, the Console CI workflow, and the Console user docs are absent while the shared migration-bridge `config-console`/`experimental.console.*`/SDK Console group/core `consoleState` survive; generic `console.log` diagnostics remain
  - [x] KiloClaw removed — MET (2026-08-16): static absence proven by `test: packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (`KiloClawProvider`, `kiloclaw` webview tree/bundle, `kilo-code.new.kiloClawOpen` command, KiloClaw message types, esbuild/knip/tsconfig refs) and `test: packages/opencode/test/kilocode/cloud-claw-removal.test.ts` (KiloClaw TUI route/command/view wiring absent)
  - [x] i18n and build residue removed — MET (2026-08-16): `test: packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` asserts no cloud/KiloClaw i18n keys remain in any locale and the manifest/esbuild/knip/tsconfig drop KiloClaw references (build/package)
  - [x] Focused real Extension Host verification — MET (2026-08-16): `test:e2e:cloud-claw-removal` passed via `test: packages/kilo-vscode/tests/e2e/runner.ts` (`assertCloudClawRemoval`, identifier-based forbidden product IDs/bundle prefixes) + `test: packages/kilo-vscode/script/e2e-probe.ts` + `test: packages/kilo-vscode/script/e2e-probe-launch.mjs` + `test: packages/kilo-vscode/script/e2e-evidence.ts` — final campaign `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-35-000Z/` (complete validated manifest schema `kilo-e2e-evidence/1`, 7 files hash-verified, run `success: true`); `cloud-claw-removal-runtime-evidence` records all forbidden manifest/runtime/bundle hits empty and the retained Open-in-Tab panel + Agent Manager both ready
  - [x] Removed-feature initialization absent and lifecycle/resources bounded — MET (2026-08-16): runtime evidence records contributed views `[]`, runtime `kiloCommandTotal` 68 with `forbiddenCommandHits` `[]`, `bundleCount` 76 with `forbiddenBundleHits` `[]`, and all forbidden hits empty; no LLM/external requests; exact PID/CDP-port/scratch cleanup (no owned VS Code process remains) — structural absence of the four removed products' initialization per LOCK-PERF-3
  - [x] Rollback/revert path documented — MET (2026-08-16): documented in `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` ("Rollback path") — git revert of the P3.3 removal commit(s); no feature flag or compatibility shim
  - [x] Removal evidence per category in section 7 — MET (2026-08-16): all four P3.3 rows complete across the categories that exist (source/tests/docs/generated SDK/config/i18n/build-package; shared migration-bridge Console APIs and generic retained names asserted surviving, not removed; section 8 complexity deltas −11/−5/−1)
  - [x] Affected-path performance comparison recorded — MET (2026-08-16): descriptive scenario-1 cold-start campaign `doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl` vs the P0 baseline and alongside the P3.1/P3.2 campaigns (section 8), drift recorded, no threshold and no improvement/regression claim (R7; LOCK-PERF-6/R7)
  - [x] No removed feature reclassified as deferred — MET (2026-08-16): section 7 records all four LOCK-003 rows as complete permanent removals with no deferred language; no reconstruction-candidate rows added (inherent removals, LOCK-015)
  - Evidence: issue: - | PR: - | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts`, `packages/opencode/test/kilocode/cloud-claw-removal.test.ts`, `packages/opencode/test/kilocode/console-removed.test.ts`, `packages/script/tests/jetbrains-removed.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts`, `packages/kilo-vscode/script/e2e-probe.ts`, `packages/kilo-vscode/script/e2e-probe-launch.mjs`, `packages/kilo-vscode/script/e2e-evidence.ts` | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md`, `packages/kilo-docs/source-links.md`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-35-000Z/`, `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`, `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor)
- Next actions: P3.4 (indexing, project memory, user-visible context management/compaction, autocomplete, LOCK-004) has exited Complete 2026-08-17 with its own recorded evidence (sections 4, 5, 6, 7, 8); P3 is Complete. Keep the P3.3 static contract tests (`cloud-claw-removal.test.ts`, `console-removed.test.ts`, `jetbrains-removed.test.ts`) and the `test:e2e:cloud-claw-removal` scenario green; keep the P3.4 static contract tests (`p3-4-removal-absence.test.ts`, `p3-4-removal.test.ts`) and the `test:e2e:p3-4-removal` scenario green.

#### P3.4 - Indexing, project memory, context management/compaction, autocomplete removal (LOCK-004)

- Status: Complete (2026-08-17; P3.3 is Complete 2026-08-16; P3 is Complete (2026-08-17);
  P2 is Complete 2026-08-15)
- Scope: Permanently remove indexing/semantic search, project memory/injection/tools,
  user-visible context management/compaction (CLI/TUI/ACP/HTTP/SDK
  summarize+compact), and autocomplete/next-edit/chat autocomplete/FIM/edit
  (LOCK-004). Retain the invisible automatic internal context-overflow safeguard
  (H-13, LOCK-005) and generic search/recall. No new reconstruction-candidate
  rows — the removed surfaces are inherent observable features of the LOCK-004
  closed-set permanent removals; recording them as candidates would incorrectly
  reclassify permanent removal scope.
- Exit checklist:
  - [x] Indexing/semantic search removed — MET (2026-08-17): static absence proven by `test: packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (indexing module, semantic-search integration, indexing-worker build entry absent) and `test: packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (extension/webview indexing surfaces absent); stale indexing-worker esbuild entry removed from CLI build; CLI native build passes
  - [x] Project memory/injection/tools removed — MET (2026-08-17): static absence proven by `test: packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (memory injection, memory tools, project-memory module absent) and `test: packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (extension/webview memory surfaces absent)
  - [x] User-visible context management/compaction removed — MET (2026-08-17): static absence proven by `test: packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (CLI/TUI/ACP/HTTP/SDK summarize+compact commands/endpoints absent) and `test: packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (context management UI controls — settings rows 0, header context menu 0, header context actions 0); H-13 overflow safeguard retained and not surfaced
  - [x] Autocomplete/next-edit/chat autocomplete/FIM/edit removed — MET (2026-08-17): static absence proven by `test: packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (autocomplete module, next-edit, FIM endpoints absent) and `test: packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (extension autocomplete/FIM surfaces absent)
  - [x] H-13 retained parity re-proved — MET (2026-08-17): H-13 P0 harness 8/8 and automatic overflow suites pass post-P3.4 (`test: packages/opencode/test/kilocode/p3-4-removal-absence.test.ts`); the invisible automatic overflow safeguard continues to function with no user-facing context-management UI surfaced
  - [x] Focused real Extension Host verification — MET (2026-08-17): `test:e2e:p3-4-removal` passed — forbidden manifest/runtime/bundle/state hits all empty, 0 model requests, Open-in-Tab panel ready, Agent Manager ready, cleanup complete
  - [x] Rollback/revert path documented — MET (2026-08-17): documented in `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.4 section + Rollback path) — git revert of the P3.4 removal commit(s); no feature flag or compatibility shim
  - [x] Removal evidence per category in section 7 — MET (2026-08-17): indexing, project memory, user-visible context management/compaction, and autocomplete rows complete across source/tests/docs/generated SDK/config/i18n/build/package (section 7)
  - [x] No removed feature reclassified as deferred — MET (2026-08-17): section 7 LOCK-004 rows record complete per-category evidence with no deferred language; no new reconstruction-candidate rows added (LOCK-015)
  - [x] Removed-feature initialization absent and lifecycle/resources bounded — MET (2026-08-17): runtime evidence (`p3-4-removal-runtime-evidence` — forbidden manifest/runtime/bundle/state hits empty, 0 model requests, Open-in-Tab and Agent Manager ready) plus the 48 static assertions (20 pass/253 assertions opencode + 28 pass/381 assertions extension) and H-13 overflow suites
  - [x] Affected-path performance comparison recorded — MET (2026-08-17): accepted native scenario-1 (cold-start) campaign `doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl` (1 warmup + 5 measured, 6/6 ok, finish status ok, 13 n=5 summaries) recorded against the P0 baseline (section 8); no threshold and no improvement/regression claim (R7); excluded partial wrapper campaign `p3-4-final-2026-08-17T00-57-00-000Z/EXCLUDED` (316-byte wrapper, 4/5 measured blocked, non-comparable) explicitly excluded and never cited as accepted benchmark
  - [x] CLI build corrected — MET (2026-08-17): stale indexing-worker entry removed; native build passes; absence test pins the corrected state
  - [x] SDK/OpenAPI regenerated idempotently — MET (2026-08-17): regenerated twice; removed 18 generated methods total vs P3.3; final SDK method count 216; no removed endpoints/types remain
  - Evidence: issue: - | PR: - | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (20 pass / 253 assertions), `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (28 pass / 381 assertions), real Extension Host `test:e2e:p3-4-removal` (forbidden manifest/runtime/bundle/state hits empty, 0 model requests, Open-in-Tab and Agent Manager ready, cleanup complete), `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` (H-13 P0 harness 8/8 pass + automatic overflow suites) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.4 section + Rollback path), `specs/vscode-orchestrator/evidence/p0-baseline/p3-4-final-2026-08-17T00-52-00-000Z/` (validated manifest sha256 d3f6d863b5efadbe0baabc5db2c9f3ea42aae790f3d87e3d618362f9c9c6e002, runtime evidence sha256 746d7c87ff617a393a819c64e55308733f6340293584cf667c91f1c5d8f33bd0), `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl` (accepted native scenario-1, artifact sha256 `288cfcfd8b08514f9356a15a138de213bc604a35dcc12641cec87cf01a7567e7`, 63,134 bytes; native CLI snapshot sha256 `2945ac79748d9241afb10ef6caf897fa6b3237809ec824e8adbb1927ec231c2a`, 151,207,394 bytes), `.changeset/p34-docs-update.md` (minor)
- Next actions: P3 is Complete. P4 (Private runtime and configuration) is the next phase and remains Not started. Keep the P3.4 static contract tests (`p3-4-removal-absence.test.ts`, `p3-4-removal.test.ts`) and the `test:e2e:p3-4-removal` scenario green.

### P4.1 - File-authoritative GUI config implementation

- Status: Complete (2026-08-18; P4 is Active; P3 is Complete 2026-08-17)
- Scope: Implement the file-authoritative GUI read/write model per
  runtime spec sections 3.2, 5.1, 5.4: extension-owned
  `CanonicalConfigService`, two canonical authored scopes (one global config
  root and `<workspaceRoot>/.kilo/`), closed 11-field JSONC registry covering
  every configurable field class, typed asset directories (agent, command,
  skill, tool, plugin, rules), typed composition/materialization with
  structured provenance, atomic WYSIWYG writes with stale-detection and
  invalid-edit recovery, SecretStorage opaque credential references, derived
  selector indexes (provider, agent, model), and the canonical
  mode/readiness contract. R10 resolved with evidence. The CLI
  `kilo serve` HTTP/SSE/SDK path remains the active migration bridge
  (LOCK-009); no legacy-reader cutover occurs at P4.1.
- Exit checklist:
  - [x] Field registry covers every configurable field class — MET (2026-08-18): closed 11-field JSONC registry in `packages/kilo-vscode/src/config/registry.ts` (model, model_variant, model_variant_overrides, subagent_model, subagent_variant, subagent_variant_overrides, default_agent, provider, mcp, permission, instructions) with complete entries for owner, persistence, scope, operator, secret handling, snapshot inclusion, provenance, and removal disposition; validation is enforced by the separate closed `fieldSchemas` mapping and contextual validators (`packages/kilo-vscode/src/config/validate.ts`), not as per-entry registry metadata; typed asset classes (agent, command, skill, tool, plugin, rules) registered as asset directories in `packages/kilo-vscode/src/config/types.ts` — never as top-level JSONC records
  - [x] Canonical schema path and persistence assignments evidenced — MET (2026-08-18): `packages/kilo-vscode/src/config/paths.ts` resolves global root from `~/.config/kilo/` via `Roots` class (injectable for tests; not a hardcoded platform path) and project root from `<workspaceRoot>/.kilo/`; canonical files: `<globalRoot>/kilo.jsonc` and `<workspaceRoot>/.kilo/kilo.jsonc`; asset directories: `agent`, `command`, `skill`, `tool`, `plugin`, `rules` under each root
  - [x] WYSIWYG acceptance semantics evidenced — MET (2026-08-18): atomic JSONC writes with comment/trailing-comma preservation (`packages/kilo-vscode/src/config/write.ts`), stale-write detection via content hash comparison (`detectStaleWrite`), final CAS check immediately before rename, validation against closed registry before every write, invalid edits retain exact prior materialization (never fall back to legacy values), external edits observed via file watchers without manual reload
  - [x] Secret handling with opaque references — MET (2026-08-18): `packages/kilo-vscode/src/config/secret-adapter.ts` wraps VS Code SecretStorage; credential refs are exact `secret:kilo.credentials.<scope>.<kind>.<id>` format validated by `parseOwnedCredentialRef`; plaintext never leaves extension host; JSONC files contain only opaque refs
  - [x] Derived selector indexes — MET (2026-08-18): `packages/kilo-vscode/src/config/selectors.ts` builds provider, agent, and model indexes from materialized snapshots; persisted/rehydrated via state adapters; credential status derived per-provider from exact `credential` ref (no SecretStorage prefix scan)
  - [x] Canonical mode/readiness contract — MET (2026-08-18): `CanonicalConfigService.materializationReady` is true only after error-free materialization; snapshot existence alone is not readiness; late subscribers must use this gate
  - [x] Typed composition operators verified — MET (2026-08-18): single (model/variant fields — cross-scope conflict detected), keyed (provider/mcp/variant-overrides — duplicate ID conflict), ordered (instructions — global-then-project), restrictive (permission — layered for later evaluator)
  - [x] Cross-scope conflict detection — MET (2026-08-18): `validateCrossScope` in `packages/kilo-vscode/src/config/validate.ts` checks keyed-record cross-scope conflicts and single-field cross-scope conflicts before any commit
  - [x] Independent source audit PASS — MET (2026-08-18): 0 findings; validation passes typecheck, lint, knip, 606 focused tests, compile, fresh bundle regression, Storybook build; architecture-impact checker (1 medium signal — arch-adjacent path `packages/kilo-vscode/src/agent-manager/vscode-host.ts`, local semantic assessment PASS); source-links extraction idempotent; md-table-padding pass
  - [x] No claims beyond implemented reality — MET (2026-08-18; tracker updated 2026-08-19; S2 downgraded 2026-08-20; S3 completed 2026-08-20; S4 completed 2026-08-20; S5 completed 2026-08-21 offline cutover only): no claim beyond retention/registry via S2, snapshot/registry boundary via S3, storage-side bounded changefeed via S4, and offline cutover work unit via S5 (archive integrity, fresh DB identity/zero gate, rollback rehearsal/recovery, lease/marker safety, focused production-path evidence) is now made; still no claim of wire-level R9/private wire (P4.2b), legacy-reader cutover/deletion, HTTP/SSE/SDK removal, CLI deletion, worker-unavailable selector startup acceptance, final private-runtime H parity, or P4.2/P4.2a overall complete; CLI remains migration bridge; P4.2a storage cutover work units S0/S1/S2/S3/S4/S5 Complete (2026-08-21) offline cutover; H-10/H-11 post-cutover Extension Host five-boundary convergence and remaining private-runtime work remains pending before P4.2b; P4.2b-P4.5 remain Not started
  - Evidence: issue: - | PR: - | test: `packages/kilo-vscode/tests/unit/config-audit-blockers.test.ts` (closure, authority, registry, write, service), `packages/kilo-vscode/tests/unit/config-audit-closure.test.ts`, `packages/kilo-vscode/tests/unit/config-registry.test.ts`, `packages/kilo-vscode/tests/unit/config-write.test.ts`, `packages/kilo-vscode/tests/unit/config-service.test.ts`, and related config unit tests (606 focused tests pass), `bun run typecheck` (opencode + kilo-vscode), `bun run lint`, `bun run knip`, `bun run compile`, `bun run script/check-architecture-impact.ts --worktree` (1 medium signal), `bun run script/check-md-table-padding.ts`, `bun run script/extract-source-links.ts` | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (canonical config section updated), `packages/kilo-vscode/src/config/` (service, registry, types, paths, materialize, compose, validate, write, parse, selectors, snapshot, secret-adapter, state-adapter)
- Next actions: S5 Complete (2026-08-21) offline cutover work unit only — next gate is P4.2 H-10/H-11 post-cutover Extension Host five-boundary convergence against the canonical storage foundation plus remaining private-runtime work before P4.2b; P4.2b-P4.5 remain Not started. Final independent delivery-gate 2026-08-20 verified: Core 59 pass; OpenCode listed suites pass; S3 audit transient default timeout (environmental) → 8 pass with `--timeout 30000`; snapshot 60/1 under 180s; core/opencode/turbo typecheck 17/17; lint 0/6986; guards pass (source-links idempotent; architecture-impact 1 medium session-contract reviewed); final audit 0 blockers; no owned process/port — still no wire/P4.2b/P4.2 overall complete claim; S5 final validation 2026-08-21 is recorded in the P4 checklist below.

### P4 - Private runtime and configuration

- Status: Active — P4.1 Complete (2026-08-18); P4.2a storage cutover work units S0/S1/S2/S3/S4/S5 Complete (2026-08-21) offline cutover; P4.2 H-10/H-11 post-cutover Extension Host five-boundary convergence Complete (2026-08-22) as the conjunction of real-restart transport reconnect + worker restart + window/reload and real-session session switch + panel close/reopen (validated=true/success=true); P4.2b-1 R1 scaffold Complete (2026-08-22) scaffold-only — additive JSON-RPC 2.0 over stdio Content-Length with initialize handshake, bounded stderr (16 KiB/100 lines), EOF/exit lifecycle, no production wiring (LOCK-009 bridge remains; R1 resolved; R9 remains Open; R11/R12/R13/R14 Resolved 2026-08-23, required for P4.2 exit); P4.2b wire/R9, P4.2b standalone lease bootstrap Complete (2026-08-22; corrected 2026-08-22; packaging corrigendum 2026-08-22) — additive gated file-backed DB via leased Database.layerFromPath + createChangefeedDeps [Historical leased — superseded 2026-08-23 by production enabled:true via resolveCanonicalDbPath / private Database.layerNoLease observer, managed bridge canonical KILO_DB via resolveCanonicalDbPath, fire-and-forget fail-closed, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant)], isolated from production HTTP/SSE, lifetime cleanup on worker dispose/EOF/SIGINT/SIGTERM (process 'exit' not awaited; stale-PID recovery via Database.layerFromPath on next acquire [Historical leased recovery — superseded 2026-08-23; current production no-lease has no stale-lease recovery, legacy kilo serve remains sole lease owner]), idempotent cleanup with signal/stdin/interval listener removal, real-child lease/observation proof (direct rehydrate:true on cursor-ahead, sequential reacquire; lease exclusivity proven via Database.layerFromPath: sequential reacquire proven 2026-08-22, concurrent-owner contention and stale-PID crash-recovery proven 2026-08-23 via real child (second concurrent worker fails observably with peer closed while winner lease/PID and observation remain healthy; abrupt SIGKILL of exact-PID worker leaves stale lease recoverable, next real child reacquires same DB and observes valid empty/current cursor with rehydrate:true on cursor-ahead)); packaging: default CJS lightweight and standalone ESM bundled with `bun run compile` proof; P4.2b live canonical changefeed notification delivery Complete (2026-08-23) — additive test-only bridge `test/mutateChangefeed` reusing real Changefeed APIs over file-backed canonical DB via private stdio for test proof [Historical leased test bridge — superseded 2026-08-23; current production is enabled:true no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed, legacy bridge sole lease owner] `observation/changed` (payload-free, monotonic cursor, read/ack, stale→rehydrate after truncation/eviction, sequential reacquire, no duplicate from prior host; run-owned temp XDG, exact PID/handle cleanup; CJS lightweight preserved, ESM standalone gated by KILO_PRIVATE_WORKER_TEST_BRIDGE=1); P4.2b R12 Failure/Outcome normalization foundation Complete and R12 Resolved (2026-08-23) — additive pure private-runtime failure module (CLI ↔ VS Code parity): sole runtime-owned normalization boundary, minimal 9-field schema without taxonomy freeze, field tiers (durable/diagnostic/panel-visible), runtime-side redaction inside normalize before persistence/projection, five-source cancellation provenance, versioned 1.0 panel envelope via runtime projection; no production wiring, no new store, no HTTP/SSE/SDK/session change; tests packages/opencode/test/private-worker/failure.test.ts + packages/kilo-vscode/tests/unit/failure.test.ts (39+39 tests, 146+146 assertions); audited 2026-08-23 ACCEPT (0 blockers); R11/R12/R13/R14 Resolved 2026-08-23; R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3 Complete 2026-08-25; P4.4/P4.5 pending); full R9 still Open pending five-boundary lifecycle convergence; R11/R12/R13/R14 Resolved 2026-08-23; P4.2b private observation service Complete (2026-08-23) — additive `PrivateObservationService` (`enabled:true` production no-lease via `resolveCanonicalDbPath`/`Database.layerNoLease` (historical `enabled:false` gate-off superseded 2026-08-23), one `PrivateWorkerHost`, `observation/snapshot|read|ack|subscribe` + `observation/changed` forwarding, `isAbsolute` + canonical `KILO_DB`); test: `private-observation-service.test.ts` 5 pass/98 assertions; R9 Resolved 2026-08-23; P4/P4.2 remain Active; R9 Resolved 2026-08-23; five-boundary convergence proven, P4.3/P4.4/P4.5, legacy reader deletion/import, HTTP/SSE/SDK removal, real-user DB cutover remain pending; P4.2b-P4.5 Not started except P4.2b-1/R9 wire + standalone lease bootstrap + live notification + private observation service
- Scope (owned by runtime spec sections 3-7):
  - P4.1: file-authoritative GUI read/write model (bidirectional editor over
    canonical config files/assets — one global config root and
    `<workspaceRoot>/.kilo/`; SecretStorage for secrets; canonical two-level
    authored scopes; VS Code state is UI-local/derived only; no migration);
    canonical schema, field registry covering every configurable field class,
    and the deterministic materializer (runtime spec sections 3.2, 5.1);
    WYSIWYG acceptance semantics (runtime spec section 5.4); R10 resolved
  - P4.2: private runtime entrypoint and snapshot API (extension-owned headless
    worker; immutable versioned snapshots; generation pinning; observation
    surface per runtime spec section 7.1); Failure/Outcome/Recovery foundation
    per runtime spec section 7.2 with R11-R14 resolved (section 9). P4.2a is
    the storage foundation and cutover sub-boundary — canonical aggregate
    storage, invisible automatic retention, artifact field/owner/retention
    registry, and the offline archive cutover (ADR-0005; storage spec sections
    5-8, work units S0..S5) — landing before the P4.2b private-wire/schema
    freeze for R9 and R11-R14; requires R15-R17 resolved (runtime spec section 9)
  - P4.3: legacy-reader cutover — no dual-read compatibility window and no
    import tool; old sources may be used only by the current implementation
    before the cutover; at the P4.3 boundary all legacy readers are deleted
    together and cannot influence effective config; the sole user manually
    reconciles any desired current configuration into canonical files before
    the cutover, evidenced by a pre-cutover reconciliation checklist (manual
    only; no migration tooling), and an explicit mapping records each of the 15
    P0 enumerated sources onto a retained legal class or one of the 13 removal
    classes (runtime spec sections 5.1, 7, 8.1; R6 revised 2026-08-13)
  - P4.4: evidence and transport narrowing — records and verifies per-row
    inactive/removal evidence for all 13 legacy effective-config source classes
    per runtime spec section 8.1 (including legacy global config
    filenames/readers and legacy migration readers; the legacy readers were
    deleted together at the P4.3 cutover, each with no target reader), removes
    the residual legacy machinery (12-source merge, preset provider
    loaders/catalog, convergence machinery, backend-gated fetch chain), and
    narrows the public transport (private transport; SDK/public server surface
    ceases to be public); removal exits only on structural absence of
    removed-feature initialization/readers/listeners/resources and recorded
    affected startup/session-switch/memory/worker-lifecycle deltas against the
    P0 baseline — zero or positive noisy delta is allowed if no removed work
    remains and no structurally unbounded growth/resource leak appears
    (LOCK-PERF-3, runtime spec section 10.9-10.10)
  - P4.5: old CLI/server product deletion (CLI/TUI/Console products and public
    interfaces)
- Exit checklist:
  - [ ] Every datum has one owner and one persistence path (runtime spec section 3 table)
  - [ ] Field registry covers every configurable field class — canonical schema path, owner/storage, legal scope (global-only/project-only/both-with-typed-composition), composition operator, secret handling, generation-snapshot inclusion, provenance, removal disposition (runtime spec section 3.2); validation enforced by the separate closed `fieldSchemas` mapping and contextual validators; R10 resolved with the schema layout and exact persistence assignments recorded in section 9; effective config composes only through schema-declared operators (runtime spec section 5.1)
  - [ ] Custom-provider-only boundary enforced (runtime spec section 4)
  - [ ] Config update semantics verified: atomic version creation, generation snapshot pinning, no active-generation interruption, resource version ownership/disposal, validation before commit, rollback/error behavior (runtime spec section 5)
  - [ ] WYSIWYG acceptance semantics evidenced (runtime spec section 5.4): file-to-UI external edits observed without manual reload; atomic validated UI-to-file writes preserving JSONC/markdown formatting where possible; visible stale-draft conflicts (never silent overwrite); invalid external edits never partially apply and never fall back to legacy values; predictable deletion/unset; active generations pinned while new readers use the new valid snapshot (LOCK-011)
   - [x] Permission evaluator implements the restrictive policy stack (runtime spec section 5.3): monotonic deny/ask/allow composition, no widening, enclosing parent denies/session restrictions for children with no parent-allow inheritance, runtime-owned per-session approval records, question-flow vs `question`-tool distinction; target semantics evidenced by a permission-evaluator test surface (permission-evaluator gate)
  - [ ] P4.4 source removal proves removed-feature initialization/readers/listeners/resources are absent from worker startup and records affected startup/session-switch/memory/worker-lifecycle deltas against the P0 baseline; zero or positive noisy delta is allowed if no removed work remains and no structurally unbounded growth/resource leak appears (LOCK-PERF-1, LOCK-PERF-3; runtime spec section 10.9-10.10)
  - [ ] Each legacy effective-config source (runtime spec section 8.1, including legacy global config filenames/readers and legacy migration readers) records per-row removal evidence and is proven inactive; the active effective-config source set is the closed legal taxonomy only (tracker section 8 counting method; runtime spec sections 3.1, 5.1)
   - [x] Legacy-reader cutover completed at P4.3: no dual-read window and no import; after the cutover legacy sources cannot affect effective config because their readers are deleted together; old path deleted (runtime spec sections 5.1, 7; R6 revised 2026-08-13) — MET (2026-08-25): atomic deletion executed; no dual-read/import; canonical sources are global/project `kilo.jsonc` + canonical assets, SecretStorage, runtime defaults; instruction `KILO_CONFIG_DIR` profile was separate and not effective `Config.Service` authority at cutover (removed in P4.4-T17; `ConfigPaths` `KILO_CONFIG_DIR` reader removed in P4.4-T18 — `ConfigPaths` is now global-only; historical T18/T19 claim that TUI retained `KILO_CONFIG_DIR` with last-wins superseded in P4.4-T27/T27-correction — no TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate` reader remains; `ConfigPaths.files` API retained (`files`/`fileInDirectory` remain, only active TUI call-site removed in T27); `Global.Path.config` override and sandbox deny-list retained separately per compatibility, not an effective TUI source; historical TUI sole-reader last-wins wording preserved with this additive superseding clarification (P4.4-T30 2026-08-27, canonical file-authoritative through T30)); pre-cutover checklist `specs/vscode-orchestrator/evidence/p4.3-pre-cutover-reconciliation-checklist.md` (2026-08-24, `manual-reset-rebuild`, 15→4/13 mapping); cutover evidence `specs/vscode-orchestrator/evidence/p4.3-cutover-evidence.md`; verification 65 core config + 104 kilocode/config (2 skips) + 30 instruction/loader/r18 + 31 pass, 251 expects for P4.3/agent suites (seven-file scope: `p4-3-cutover.test.ts`, `p4-3-canonical-lock-regression.test.ts`, `p4-3-overlay-mode.test.ts`, `agent-remove-lock.test.ts`, `agent-remove-mode-untouched.test.ts`, `agent-unlink-visible.test.ts`, `agent-scan.test.ts`), typecheck and all repository guards pass with explicit 60s timeout; architecture-impact PASS (docs updated); `rules` gap deferred per `specs/vscode-orchestrator/evidence/p4.3-rules-contract-gap.md`; P4.4/P4.5 remain open
   - [x] Lifecycle-boundary convergence evidence recorded for all five boundaries against the private-worker observation surface (runtime spec section 7.1); R9 Resolved 2026-08-23 with the handshake/revision decision recorded in section 9 — MET (2026-08-23): five-boundary production observation convergence proven via secret-safe durable `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-23T13-59-07Z-r9-observation/` (manifest.redacted.json validated true success true, provenance source d2b3b8ec..., r9-observation-runtime-evidence 5 boundaries, r9-dom-evidence, canonical-gate/archive/cstate, audit PASS 0 blocker); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3 Complete 2026-08-25; P4.4/P4.5 pending)
  - [ ] Failure/Outcome/Recovery contract evidence recorded (runtime spec section 7.2): runtime-owned normalization boundary; operation/outcome identity for generation-path operations under the observation contract; minimal field tiers (durable vs diagnostic vs panel-visible); runtime-side redaction before persistence/projection; structured redacted panel projection through a versioned private envelope; cancellation provenance (user stop/steering/timeout/network disconnect/unknown); private runtime as sole semantic recovery authority with no client replay of accepted operations; recovery accounting/coordination (owner/scope, budget consumed/termination, next-at occurrence time, provenance, nested low-level attempt visibility); worker-crash in-flight disposition converging under section 7.1 with resource cleanup and no silent client replay; R11-R14 resolved in section 9. Post-reconstruction error maturity (runtime spec section 7.3) is not a P4.2 gate
  - [ ] Storage foundation and cutover landed (P4.2a, before the P4.2b wire/schema freeze): canonical aggregate schema/revision model with atomic revision commits; invisible automatic byte-budget retention with hysteresis and family protections; artifact field/owner/retention registry with no unregistered session-owned artifacts (project-scoped/shared revert snapshots assigned project-level ownership/GC separately from session-family deletion); bounded changefeed/outbox per R9 (storage-side ordering/truncation/idempotency in S4; wire-level R9 convergence at P4.2b); clean-DB cutover with offline archive, checksum/integrity evidence, empty fresh canonical DB boot (no pre-cutover sessions), and rollback authority retained; R15-R17 resolved (ADR-0005; storage spec sections 5-8; runtime spec sections 7.1-7.2, 9)
    - [x] S0 Baseline and fixtures — MET (2026-08-19): frozen section-10 measurement commands are asserted exactly by `packages/opencode/test/storage/s0-baseline-measurement.test.ts` and deterministic retained/active/maintenance-leased family fixtures are exercised through service APIs; `TestHoldToken` exposes held/released assertions and builders register finalizers immediately after session creation. Verification: `bun test ./test/storage/s0-baseline-measurement.test.ts` from `packages/opencode` (29 pass)
    - [x] S1 Canonical aggregate schema and revision commit model — MET (2026-08-19): `SessionTable.revision` is added by migration `0→1` with default `0`; `SessionRevision.advance` only (no `advanceBatch`) uses `UPDATE RETURNING` to prove one affected row; canonical mutations cover Prompted/todo/import/project writers, with per-family JSON savepoints and post-projector rollback evidence. Real HTTP route coverage is in `packages/opencode/test/kilocode/server/session-import-http.test.ts`; core lifecycle and migration coverage is in `packages/core/test/session-revision.test.ts`, `packages/opencode/test/session/session-revision.test.ts`, and `packages/core/test/database-migration.test.ts`; writer/import and rollback coverage is in `packages/opencode/test/kilocode/session-import-service.test.ts`, `packages/opencode/test/project/project.test.ts`, `packages/opencode/test/cli/import.test.ts`, and `packages/opencode/test/kilocode/storage/json-migration.test.ts`. Verification: the bounded opencode command (138 pass), core command (29 pass), and `bun run typecheck` from both `packages/core` and `packages/opencode` listed in the P4 evidence row above
    - [x] S2 Automatic retention engine — MET (2026-08-20 corrected): high 8 GiB/low 6 GiB, 7-day floor, activity asc then root ID, atomic family deletion with tombstone+obligation, WAL accounting, incremental vacuum/checkpoint, artifact registry, unregistered-write guard (including empty prefix), scoped SessionRunState cancellation via Effect (no detached Promise/AppRuntime, visible failure logging), deterministic Maintenance.runOnce diagnostics with real Accounting (below-high and HIGH→LOW), auto_vacuum skip not failure, obligation DB failures recorded with retry, typed Drizzle/JSON handling (no as never/as unknown casts). Verification: `bun test ./test/storage/s2-retention-engine.test.ts` (6 pass), `bun test ./test/storage/s2-production.test.ts` (13 pass), `packages/core/test/retention-s2.test.ts` (8 pass), `bun run typecheck` (core+opencode pass), affected Session remove/run-state tests pass.
    - [x] S3 Artifact field/owner/retention registry — MET (2026-08-20): closed `Storage.write`/`update` registry boundary including empty prefixes via `Artifact.assertFamilyWrite`/`assertFamilyWriteEffect` and `Storage.assertRegistered` (empty prefix throws `UnregisteredArtifactError`); AST writer/migration audit (`packages/opencode/test/storage/s3-audit.test.ts` 8 pass) proves every production `Storage.write`/`update` uses a registered prefix and storage migrations are explicit dispositioned destinations, with negative probes for unregistered/prefixed/destructured/indirect writes; snapshot pruning is project-owned live-ref aware — `live-collector` extracts live hashes from project-scoped `Session.revert.snapshot` and snapshot-bearing `Part` rows (`snapshot`/`patch`/`step-start`/`step-finish`), `KiloSnapshotMaterialize.prune` retains aged live refs and deletes aged unreferenced refs, `snapshot/cleanup-decision` skips prune when DB missing or `fetchLiveHashes` fails (`shouldPrune` false) and proceeds when live set present; family retention remains separate and `snapshot` stays project-owned not family-pruned. Verification: `packages/opencode/test/storage/s3-audit.test.ts` (8 pass), `packages/opencode/test/storage/s3-write-boundary.test.ts` (8 pass), `packages/opencode/test/snapshot/snapshot-prune-live.test.ts` (3 pass), `packages/opencode/test/snapshot/snapshot-live-db.test.ts` (2 pass), `packages/opencode/test/snapshot/snapshot-cleanup-decision.test.ts` (3 pass), `packages/opencode/test/kilocode/snapshot-seed.test.ts` (7 pass), `packages/opencode/test/snapshot/snapshot.test.ts` (60 pass / 1 skip) with DB-aware `cleanWithDb` fixture correction; `bun run typecheck` from `packages/core` and `packages/opencode` pass; `bun run script/check-opencode-promise-facades.ts` pass; affected storage/snapshot suites pass.
    - [x] S4 Bounded changefeed/outbox — MET (2026-08-20): storage-side bounded payload-free changefeed with global monotonic seq, session_id/revision/kind, runtime-owned time, no FK to session, UNIQUE(session_id, revision, kind); global ordering across sessions, per-revision exactly-one changed entry with payload-free shape, tombstone `deleted` at final revision current+1 before hard-delete in same transaction with no FK cascade; idempotent append reuses existing seq with no gap including concurrent duplicate, hard caps both 50,000 rows and 64 MiB (deterministic byteSize UTF-8 session_id+kind+24) evict oldest prefix even when unacknowledged, ack truncates seq<=cursor and keeps latest_seq with CursorAheadError on cursor>latest, gap detection forces rehydrate on any invalid/evicted/truncated/interior-gap/missing-tail cursor (contiguous only when cursor===latest or cursor+1===minSeq), singleton state survives full truncation with monotonic latest, transaction rollback leaves revision/feed/state unchanged, invalid kind rejected at typed/runtime/DB trigger boundary, production 50,001-row oldest-first cap enforcement and rerun idempotency with monotonic latest after ack-equivalent full truncation, canonical aggregate reads intact after ack and eviction, ordered cap migration <30s, storage-side ordering/caps/truncation/ack/gap→rehydration/idempotency/tombstone/reconstruction independence only — wire-level R9 convergence stays P4.2b. Verification: `packages/core/test/changefeed-s4.test.ts` (22 pass — ordering, per-revision, idempotent duplicate, row/byte caps with scaled eviction, ack truncation, gaps, singleton, tombstone, reconstruction independence, concurrent duplicate, rollback, interior-gap/missing-tail, invalid kind, actual 50,001-row cap and rerun, canonical reads), `packages/core/src/retention/changefeed.ts`, `packages/core/src/retention/sql.ts`, `packages/core/src/database/migration/20260821000000_add_changefeed_bounds.ts`, `packages/core/src/database/migration/20260822000000_add_changefeed_kind_check.ts`, `packages/core/src/session/revision.ts`, `packages/core/src/retention/retention.ts`; `bun test ./test/changefeed-s4.test.ts` from `packages/core` (22 pass), core regressions `bun test ./test/session-revision.test.ts ./test/database-migration.test.ts ./test/retention-s2.test.ts` from `packages/core` (37 pass), `bun test ./test/project/project.test.ts` from `packages/opencode` (39 pass), `bun test ./test/storage/s0-baseline-measurement.test.ts ./test/session/session-revision.test.ts ./test/kilocode/storage/json-migration.test.ts ./test/storage/storage.test.ts` from `packages/opencode` (38 pass aggregated), `bun run typecheck` from `packages/core` and `packages/opencode` pass; audit 2026-08-20 final pass-with-findings, 0 blockers; `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md` already documents storage-side bounded changefeed (Bounded changefeed/outbox S4 section; no edit required); Final independent delivery-gate 2026-08-20: Core matrix 59 pass; OpenCode listed suites pass (`changefeed-s4` 22, `session-revision`/`database-migration`/`retention-s2` 37, `project` 39, `storage`/`s0-baseline`/`session-revision`/`json-migration` 38 aggregated); S3 audit first default hook timeout (environmental) then 8 pass with `--timeout 30000`; snapshot combined 60 pass/1 skip under 180s (`snapshot.test.ts` 60/1 long-runtime baseline accurately recorded); core/opencode/turbo typecheck 17/17 pass; root lint 0 errors/6986 warnings; guards `check-opencode-promise-facades`/`check-workflows`/`check-md-table-padding`/`extract-source-links`/`check-architecture-impact` pass (source-links idempotent; architecture-impact 1 medium session-contract signal reviewed, no doc update); final audit 0 blockers; no validation-owned process/port
    - [x] S5 Offline cutover — MET (2026-08-21) offline cutover work unit only (archive integrity, fresh DB identity/zero gate, rollback rehearsal/recovery, lease/marker safety, focused production-path evidence): offline versioned archive under same-filesystem sibling `<data-basename>-archive/p4.2/<UTC>-<uuid>/` with temp dir under same `p4.2` parent and EXDEV fail-closed; manifest v1 with sorted path/bytes/SHA-256 and deterministic aggregate hash, preserved POSIX relative paths, `wal_checkpoint(TRUNCATE)` + `integrity_check` + `foreign_key_check`, fixed members `kilo.db`/`kilo.db-wal`/`kilo.db-shm` + `storage/session_diff`/`session_diff_base`/`session_share` + `session-export.db` WAL/SHM when present else absence recorded, `snapshot` excluded, fsync files/dirs + reverify + atomic rename + fsync parent, any failure leaves legacy active; fresh DB singleton identity (UUID, schema version, creation time, cutover archive ID) with boot gate verifying identity and zero sessions/artifacts before activation; rollback offline only (stop/close, verify archive, separately archive current store under distinct rollback ID, atomically restore legacy members, rerun integrity checks, boot legacy path) with deletion requiring separate explicit maintainer authorization, never automatic. Verification (focused production-path evidence only, no real user DB cutover): core focused 52 pass (hardened 16 and S5/lease 22 included), `packages/opencode/test/storage/cutover-s5-production.test.ts` 8 pass under `bun test --timeout 40000` (97 expects), `packages/opencode/test/storage/session-export.test.ts` + respawn/S2 22 pass, `bun turbo typecheck` 17/17 pass, lint 0 errors, `bun run script/check-architecture-impact.ts --worktree` 0 signals, `bun run script/check-md-table-padding.ts` pass, `bun run script/extract-source-links.ts` pass (unchanged), `bun run script/check-workflows.ts` pass, `bun run script/check-opencode-promise-facades.ts` pass, guards pass, final independent audit PASS (0 blocker, 0 acceptable-risk, 0 style/speculative). Known baselines truthfully recorded: core full suite 1143 pass/26 skip/16 fail — 1 known config v1 baseline, 14 known flaky EventV2, 1 environmental LocationServiceMap timeout; no new S5 failures. Source-links remained unchanged and no real user DB was deleted/rolled back; S5 rehearsal artifacts cleaned — 686 rehearsal archives/backups/markers, 10 debug roots, and validation logs cleaned; no test-owned processes/ports/leases/markers remain; real DB and active Kilo processes preserved. Real production DB was observed with the S5 migration table/empty identity row during investigation only, not claimed as S5 test evidence and not intentionally cut over.
   - [x] H-10/H-11 persistence and lifecycle evidence passes against the canonical storage foundation after the P4.2 cutover (direction spec section 6; runtime spec section 7.1; storage spec section 8) — MET (2026-08-22): post-cutover Extension Host five-boundary convergence Complete as the conjunction of exactly five boundaries: real-restart transport reconnect (rr-conn.json connecting→connected same port/pid validated), worker restart (rr-kill.json pid 74573 → rr-reconnect.json pid 75399/port 57734, transcript/artifact rehydrated, DOM rr-dom-evidence), window/reload (rr-reload-executed/rr-reloaded + true window/Extension Host restart, fresh runner 76195), real-session session switch (tab A closed leaves B, llm-matrix 4/4 runOwned e2e-agent/e2e-agent-b high/low) and panel close/reopen (real-reopen-ready, session B transcript rehydrates); canonical zero-state/archive stability: canonical-gate.json zeroState all 0, canonical-archive-before/after identical (30353bcc… / 9acb687…), lease true/cutover false, cstate materializationReady true v4, credential boolean hasCredential true, XDG_CONFIG_HOME alignment and $schema filtering evidenced via opaque kilo.jsonc, provider model variants parity via llm pin low/high; validated=true/success=true on both manifests; durable secret-safe evidence `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-22T09-00-53Z-p4.2-h10h11/` (real-restart/ 31 files + real-session/ 25 files, redacted provenance, no e2e-fixture-key) from sources `/tmp/kilo-e2e-real-restart-20260822T165842-fresh` (33 files, schema kilo-e2e-evidence/1) and `/tmp/kilo-e2e-real-session-20260822-165433-64916-fresh` (27 files, schema kilo-e2e-evidence/1); explicit exclusions remain: private wire/R1/R9, P4.2b, legacy reader deletion/import, HTTP/SSE/SDK removal, P4.3/P4.4/P4.5, real-user DB cutover; do not claim P4.2 overall complete
    - [x] P4.2b-1 R1 private transport scaffold — MET (2026-08-22) scaffold-only, additive: JSON-RPC 2.0 over child-process stdio Content-Length framing (FrameDecoder/encodeFrame — binary-safe buffering, pipelined frames, header/body split handling, malformed Content-Length deterministic ParseError -32700 → transitionClosed avoiding poisoning of next valid frame; splitBytes helper), initialize handshake replacing port detection/health (exactly-once per peer, -32600 Already initialized on repeat, 5s timeout → closed; InitializeResult protocolVersion 1.0 + serverInfo kilo-private-worker/7.4.11), bounded stderr via StderrTail (16 KiB / 100 lines; host relay via bounded tail, pendingBytes/reset), EOF/exit lifecycle (peer transitions closed on EOF/stream close/child exit, rejects pending with -32603, further requests → closed; dispose idempotent, removeListener/off fallback); platform-correct spawn via `src/util/process.ts` windowsHide true forced last, packaged artifact `dist/private-worker/worker.js` resolvable via HostOptions default candidates; notifications as event envelopes (no id); ErrorCode -32700/-32600/-32601/-32603, isInitializeParams, validateRequest; scaffold-only — not wired to production runtime, LOCK-009 HTTP/SSE/SDK bridge remains, no legacy reader deletion/import, no HTTP/SSE/SDK removal, no P4.3/P4.4/P4.5 work, no real-user DB cutover; R9 (observation/hydration) and R11-R14 (Failure/Outcome/Recovery) remain open, required for P4.2 exit per runtime spec section 9; does not claim P4.2 complete. Verification: test: `packages/kilo-vscode/tests/unit/private-worker.test.ts` (FrameDecoder chunk splits incl. 1-byte, header split, body split, UTF-8 multi-byte, pipelined; ParseError/InvalidRequest/MethodNotFound/InternalError; initialize exactly-once incl. default handler; stderr bounds lines/bytes/huge-line; EOF/child-exit lifecycle incl. real child via bun worker; notifications; malformed Content-Length → ParseError + closed deterministically; null id → InvalidRequest, string/number id ok; initialize timeout → closed; listener fallback divergences; spawn import + packaged artifact); source: `packages/kilo-vscode/src/private-worker/frame.ts`, `json-rpc.ts`, `peer.ts`, `host.ts`, `worker.ts` + `packages/opencode/src/private-worker/frame.ts`, `json-rpc.ts`, `peer.ts`, `worker.ts`; build: `packages/kilo-vscode/esbuild.js` workerCtx (`dist/private-worker/worker.js` 20 lines added, cjs node, external vscode); diff: esbuild.js + workerCtx/rebuild/dispose, process.ts windowsHide order fix, plus 10 new scaffold files (see `git status` untracked); verification (read-only post-edit): `bun test ./tests/unit/private-worker.test.ts` pending in current diff — not claimed as passed here; markdown table check pending
     - [x] P4.2b private observation service — MET (2026-08-23) [Superseded 2026-08-23 by P4.2b R9 production-enablement wiring below — historical gate-off/leased record retained; current production is no-lease `Database.layerNoLease` observer with `enabled:true` via `resolveCanonicalDbPath`]: additive extension-owned service `packages/kilo-vscode/src/private-worker/private-observation-service.ts` owning one `PrivateWorkerHost` lifecycle; delegates `observation/snapshot|read|ack|subscribe` and forwards `observation/changed` to injectable `onNotification` callback; gates on `enabled===true` and absolute `dbPath` via `isAbsolute`, originally resolved canonical `KILO_DB` via leased `Database.layerFromPath` (single env, no second store) — historical leased semantics, now corrected to `Database.layerNoLease` (canonical no-lease observer / legacy lease-owner invariant, sole lease owner remains `kilo serve`); originally wired in `packages/kilo-vscode/src/extension.ts` activation with `enabled:false` (gate-off historical) — now `enabled:true` production per below entry; no selector/readiness change, no schema migration, no polling, no Failure/Outcome/Recovery wiring. Verification (historical gate-off): `packages/kilo-vscode/tests/unit/private-observation-service.test.ts` 5 pass / 98 assertions (gate, forwarding, lifecycle, env); existing private-worker/standalone/live suites 38 pass / 228 assertions; `bun turbo typecheck` pass (kilo-vscode); `bun run script/check-architecture-impact.ts --worktree` 0 signals; `bun run script/extract-source-links.ts` clean; lint/compile blocked only by pre-existing HEAD `packages/kilo-vscode/src/private-worker/disposition.ts` complexity 24>20 unchanged by this increment. Additive only; LOCK-009 HTTP/SSE/SDK bridge remains; R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); no P4 overall complete claimed. Historical dates/entries preserved; current activation is `enabled:true` no-lease per P4.2b R9 production-enablement wiring.
      - [x] P4.2b R9-C1 private observation reconnect/reacquire bounded proof — MET (2026-08-23) [Historical leased wording superseded 2026-08-23 — current is no-lease `Database.layerNoLease` reacquire; historical dates preserved]: additive `PrivateObservationService.reconnect()` helper that disposes old host via bounded exact-PID `shutdown(2000)`, clears init state, and re-enters initialization with same canonical env (`KILO_PRIVATE_WORKER_STANDALONE=1` + absolute `KILO_DB`) while preserving fail-closed gate, canonical-env protection, idempotence (concurrent callers share same promise), and exact process cleanup (now no-lease, no lease reacquisition — single bounded `doInitialize` per canonical no-lease observer / legacy lease-owner invariant); guards `disposed` after awaited shutdown to prevent host spawn after dispose; extends `packages/kilo-vscode/src/private-worker/private-observation-service.ts` only (no transport framing/protocol/storage change, no selector/read-model cutover, no production wiring). Verification: `packages/kilo-vscode/tests/unit/private-observation-reconnect.test.ts` 6 pass (initial snapshot/notification/read continuity; reconnect reacquires with new PID (no-lease) and preserves latest cursor with no duplicate notification and idempotent concurrent reconnect; stale/evicted cursor returns rehydrate:true both before and after reconnect via real caps-evicted DB; abrupt SIGKILL of exact-PID child is recoverable and does not duplicate prior notifications; dispose-during-reconnect does not spawn new host after bounded shutdown (regression); gate-off reconnect returns undefined, disposed throws; initialize↔reconnect interop: initialize shares in-flight reconnect and reconnect supersedes in-flight initialize); run-owned temp XDG paths, exact PID cleanup verified via `waitForFileExists/Gone` and `readLeasePid` (historical) / exact-PID `getHostState` now; existing gate tests still pass, extension now `enabled:true` no-lease per below entry (historical `enabled:false` retained for date); `bun turbo typecheck` pass (kilo-vscode); `bun run script/check-architecture-impact.ts --worktree` 0 signals; no full-R9 or five-boundary convergence claimed; P4.2 remains Active; R9 Resolved 2026-08-23.
       - [x] P4.2b R9-C2 private observation cursor persistence bounded proof — MET (2026-08-23) [Historical `enabled:false`/lease wording superseded 2026-08-23 — current is `enabled:true` no-lease; dates preserved]: additive `ObservationCursorStore` abstraction (`packages/kilo-vscode/src/private-worker/observation-cursor-store.ts` — `OBSERVATION_CURSOR_KEY=kilo.privateObservation.cursor`, `ObservationCursorStore` + `InMemoryCursorStore` + `createMementoCursorStore` Memento adapter, single per-instance integer, no singleton/timers/Failure wiring) and `PrivateObservationService` extension accepting optional `cursorStore` (persist on successful `ack(cursor)` only, never on notification, fire-and-forget never throws, helpers `getPersistedCursor()/setPersistedCursor()/clearPersistedCursor`, gate-off no host/persist, exact-PID bounded shutdown preserved, now no-lease). Verification: `packages/kilo-vscode/tests/unit/private-observation-cursor.test.ts` 5 pass (ack persists and new instance with same store sees cursor and reads correctly incl. Memento adapter; persisted survives dispose→reinitialize same DB with new PID no duplicate; stale→rehydrate via caps maxRows:1 both via read(persisted) and fresh reinit reading stale; gate-off with store no host/persist; concurrent ack+reconnect idempotent); existing private-observation tests and private-worker tests green; `bun turbo typecheck` pass; `extension.ts` now `enabled:true` no-lease per below entry (historical `enabled:false` retained for date); additive only, no polling/timers/Failure wiring, no store-to-globalState production wiring beyond `createMementoCursorStore(context.globalState)` with `enabled:true`, no full-R9/five-boundary claim; P4.2 remains Active; R9 Resolved 2026-08-23.
       - [x] P4.2b R9-C3 private observation lifecycle triggers bounded proof — MET (2026-08-23) [Historical `enabled:false` gate-off wording superseded 2026-08-23 — current production is `enabled:true` no-lease via `resolveCanonicalDbPath`; trigger gate `if (!service.isEnabled()) return undefined` (no host spawn, now no-lease) preserved; dates preserved]: additive `PrivateObservationLifecycleTriggers` (`packages/kilo-vscode/src/private-worker/private-observation-lifecycle-triggers.ts` — vscode-free core + thin vscode adapter, class `PrivateObservationLifecycleTriggers implements vscode.Disposable`, single debounce timer trailing ~150ms per burst (not setInterval), singleflight promise for `trigger(reason)`, gate check `if (!service.isEnabled()) return undefined` (no host spawn, no-lease), else `await service.reconnect()` bounded exact-PID + `await service.read(getPersistedCursor() ?? undefined)` surfacing `rehydrate:true/false` without UI mutation, coalesces rapid 5x flap in 100ms into 1 call, explicit entrypoints `onPanelVisibilityChanged`/`onWindowStateChanged`/`onConfigChanged`/`onActiveSessionChanged`/`onPeerClosed` sharing same debounced promise via HostOptions.onClosed / JsonRpcPeer.onClosed event-driven ownership — peer-closed via explicit `onPeerClosed()` wired to HostOptions.onClosed, no polling, no host-state polling, to keep event-driven idempotent exact-PID cleanup. Now wired in `packages/kilo-vscode/src/extension.ts` with `cursorStore:createMementoCursorStore(context.globalState)` and `enabled:true` + canonical `dbPath` via `resolveCanonicalDbPath` (previous historical wiring was `enabled:false` and no `dbPath` so gate stayed off) — now production trigger is enabled no-lease, still pushed to `context.subscriptions`, lazy fire-and-forget, preserving `connectionService` pattern. Verification: `packages/kilo-vscode/tests/unit/private-observation-lifecycle.test.ts` 5 pass (gate-off no-ops no-lease; gate-on InMemoryCursorStore each entrypoint 5 rapid ->1 reconnect+read with debounce coalescence and concurrent trigger shares same promise; peer-closed after SIGKILL triggers reconnect+read; gap with caps maxRows:1 -> read(persisted) rehydrate:true after trigger; dispose clears timer/listeners no further fire), existing suites green (`private-observation-service` 5, `reconnect` 6 incl. dispose-during-reconnect, `cursor` 5, `standalone-lease`/`live-changefeed`/`private-worker` 38), `bun turbo typecheck` pass, now `extension.ts` gate `enabled:true` no-lease, no polling, no five-boundary claim; P4.2 remains Active; R9 Resolved 2026-08-23.
      - [ ] CLI/TUI/Console products and public interfaces deleted (LOCK-009)
      - [x] P4.2b R9 production-enablement wiring Complete (2026-08-23; corrected 2026-08-23 R9 audit) — canonical DB-path resolver `packages/kilo-vscode/src/private-worker/canonical-db-path.ts` (`resolveCanonicalDataDir`/`resolveCanonicalDbPath` mirroring `Global.Path.data/kilo.db` with `KILO_DISABLE_CHANNEL_DB=1` identity, XDG_DATA_HOME/kilo/kilo.db else homedir/.local/share/kilo/kilo.db, defensive CR/LF clean, absolute gate, pure/testable injectable, no side effects) + production wiring corrected to `Database.layerNoLease` derived observer (canonical no-lease observer / legacy lease-owner invariant: private worker is no-lease observer, legacy `kilo serve` remains sole data-root lease owner, no second-lease coexistence conflict; `PrivateObservationService` lease-contention retry removed/simplified to single bounded `shutdown(2000)` + `doInitialize`, preserving bounded reconnect/lifecycle/exact-PID cleanup) + managed bridge DB identity normalized via `packages/kilo-vscode/src/services/cli-backend/server-manager.ts` `resolveManagedServerEnv` reusing existing canonical resolver (exact absolute `KILO_DB`, ambient divergent override never leaked, `KILO_DISABLE_CHANNEL_DB=1` preserved, injectable `canonicalOverride` for test harness, no dependency cycle) + extension activation path resolution/service construction fail-closed with logging while preserving fire-and-forget `initialize().catch` (LOCK-003) + both standalone builders `packages/opencode/src/private-worker/standalone-worker.ts` ↔ `packages/kilo-vscode/src/private-worker/standalone-worker.ts` switched from `Database.layerFromPath` to `Database.layerNoLease`. Verification: `canonical-db-path.test.ts` 6 pass, `private-observation-production-wiring.test.ts` 4 pass (updated to noLease, exact-PID via host proc, no polling), `private-observation-coexistence.test.ts` 3 pass (canonical resolver + managed env exact KILO_DB + ambient not leaked, activation path failure fail-closed, leased legacy `Database.layerFromPath` + noLease `PrivateWorkerHost`/`PrivateObservationService` on one run-owned canonical XDG data root both healthy, same file/DB, same changefeed visible, `observation/snapshot`/`read`/`ack` coexist, ack does not break bridge, sequential reacquire after dispose), `server-manager-utils` updated to assert exact canonical KILO_DB and ambient block, `standalone-lease` suites updated to `layerNoLease`/`noLease` coexistence (81 pass kilo-vscode + 37 pass opencode private-worker), `bun turbo typecheck` pass, `bun run lint` pass; legacy HTTP/SSE/SDK bridge preserved (LOCK-001), one canonical DB identity (LOCK-002), R9 Resolved 2026-08-23 via durable five-boundary production observation convergence 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker), P4.3/P4.4/P4.5 pending
   - Evidence: issue: - | PR: - | test: core focused 52 pass (hardened 16 and S5/lease 22 included), `packages/opencode/test/storage/cutover-s5-production.test.ts` 8 pass (`bun test --timeout 40000`, 97 expects), `packages/opencode/test/storage/session-export.test.ts` + respawn/S2 22 pass; H-10/H-11: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-22T09-00-53Z-p4.2-h10h11/` — `real-restart/manifest.redacted.json` + `provenance.json` (source 4df6cc393… validated true success true, 33→31 files, rr-conn/rr-kill/rr-reconnect/rr-reload-executed/rr-reloaded/rr-pin/workspace/e2e-custom-called.txt + canonical-gate/archive/cstate/credential/llm-matrix final+phase0/A/B/llm-requests metadata/snapshots/DOM), `real-session/manifest.redacted.json` + `provenance.json` (source d3b3c3d70… validated true success true, 27→25 files, real-dom-evidence/real-reopen-ready/rs-credential/cstate/llm-matrix final+post-abort/llm-requests metadata/snapshots + kilo.jsonc opaque); P4.2b-1: test: `packages/kilo-vscode/tests/unit/private-worker.test.ts` (scaffold unit; see checklist for coverage — framing/handshake/bounded stderr/EOF-exit), source: `packages/kilo-vscode/src/private-worker/frame.ts`, `json-rpc.ts`, `peer.ts`, `host.ts`, `worker.ts` + `packages/opencode/src/private-worker/frame.ts`, `json-rpc.ts`, `peer.ts`, `worker.ts`, build `packages/kilo-vscode/esbuild.js` workerCtx; P4.2b private observation service: test: `packages/kilo-vscode/tests/unit/private-observation-service.test.ts` (5 pass / 98 assertions), source: `packages/kilo-vscode/src/private-worker/private-observation-service.ts` + `packages/kilo-vscode/src/extension.ts` (`enabled:true` production no-lease gate, historical `enabled:false` superseded 2026-08-23), private-worker/standalone/live suites 38 pass / 228 assertions, `bun turbo typecheck` pass, `bun run script/check-architecture-impact.ts --worktree` 0 signals, `bun run script/extract-source-links.ts` clean, lint/compile blocked only by pre-existing `packages/kilo-vscode/src/private-worker/disposition.ts` 24>20 | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-22T09-00-53Z-p4.2-h10h11/` (secret-safe, no plaintext e2e-fixture-key, no rr-model-requests.json/run.log), `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/` retained as prior P2 proof; P4.2 R11-R14 pure-module contract evidence-only: doc: `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md` (HEAD d112c37780c8d1d844ecd94e40d3fe6e83ec3e02, evidence-only, no production wiring, P4 remains Active; P4.2 remains Active; P4.3/P4.4/P4.5 remain pending, gate 976 remains unchecked) | test: `packages/core/test/operation-r11.test.ts` 14 pass, 126 expect, `packages/opencode/test/private-worker/failure.test.ts` 40 pass, 149 expect, `packages/opencode/test/private-worker/recovery.test.ts` 32 pass, 202 expect, `packages/opencode/test/private-worker/disposition.test.ts` 25 pass, 141 expect — aggregate 111 tests, 618 expect, 0 failures
- Next actions: P3 has exited Complete (2026-08-17). P4.1 (file-authoritative GUI read/write model per runtime spec sections 3.2, 5.1) is Complete (2026-08-18) with recorded evidence. S0 Complete (2026-08-19), S1 Complete (2026-08-19), S2 Complete (2026-08-20 corrected), S3 Complete (2026-08-20), S4 Complete (2026-08-20), S5 Complete (2026-08-21) offline cutover work unit only; P4.2 H-10/H-11 post-cutover Extension Host five-boundary convergence Complete (2026-08-22) as above (five-boundary conjunction, validated/success, durable 2026-08-22T09-00-53Z-p4.2-h10h11/); P4.2b-1 R1 scaffold Complete 2026-08-22; R9 wire foundation Complete 2026-08-22 (protocol foundation only); storage-backed observation adapter Complete 2026-08-22; standalone lease bootstrap Complete 2026-08-22 (corrected 2026-08-22); live canonical changefeed notification delivery Complete 2026-08-23; R12 Failure/Outcome normalization foundation Complete and R12 Resolved 2026-08-23; R11/R12/R13/R14 Resolved; P4.2b private observation service Complete (2026-08-23) — additive `PrivateObservationService` (`enabled:true` production no-lease via `resolveCanonicalDbPath`/`Database.layerNoLease` (historical `enabled:false` gate-off superseded 2026-08-23), one `PrivateWorkerHost`, `observation/snapshot|read|ack|subscribe` + `observation/changed` forwarding, `isAbsolute` + canonical `KILO_DB`); test: `private-observation-service.test.ts` 5 pass/98 assertions; P4.2b R9-C2 cursor persistence Complete (2026-08-23) — bounded `ObservationCursorStore` abstraction, ack-persisted single integer, survives dispose/reinitialize, gap→rehydrate deterministic; `private-observation-cursor.test.ts` 5 pass; P4.2b R9-C3 lifecycle triggers Complete (2026-08-23) — debounced singleflight triggers (panel visibility, window focus, config change, session switch, peer-closed) coalesced trailing ~150ms → reconnect+read(persisted) gap→rehydrate, no polling, production enabled:true no-lease via resolveCanonicalDbPath/Database.layerNoLease, managed bridge canonical KILO_DB, fire-and-forget fail-closed initialization, legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant), R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); `private-observation-lifecycle.test.ts` 5 pass; `extension.ts` now `enabled:true` no-lease production (historical `enabled:false` superseded 2026-08-23); additive only, R9 Resolved 2026-08-23; P4/P4.2 remain Active; R9 Resolved 2026-08-23; five-boundary convergence proven; P4.3 cutover checklist and P4.4/P4.5 removals remain pending. P4.3 readiness preparation evidence only (no cutover) — test: `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts` (13 pass, 229 assertions — closed taxonomy 15→4/13, no dual-read/import, no reader deletion) and doc: `specs/vscode-orchestrator/evidence/p4.3-pre-cutover-reconciliation-checklist.md` (pending template — all operator/reconciliation/signature fields PENDING, manual reconciliation and cutover remain pending, production readers unchanged, P4 remains Active). Final independent delivery-gate 2026-08-20 verified: Core 59 pass; OpenCode listed suites pass; S3 audit transient timeout → 8 pass with `--timeout 30000`; snapshot 60/1 under 180s; typecheck 17/17; lint 0/6986; guards pass (source-links idempotent; architecture 1 medium reviewed); final audit 0 blockers; no owned process/port — no changeset warranted (storage-only), no source-link change, still no wire/P4.2b/P4.2 overall complete claim. S5 final validation 2026-08-21: core focused 52 pass, `cutover-s5-production.test.ts` 8 pass/97 expects under `--timeout 40000`, session-export/respawn/S2 22 pass, `bun turbo typecheck` 17/17, lint 0 errors, architecture-impact 0 signals, guards pass, audit PASS (0 blockers), cleanup 686 archives/backups/markers + 10 debug roots + validation logs cleaned, no owned process/port/lease/marker remain, real DB and active Kilo processes preserved, source-links unchanged; core full 1143 pass/26 skip/16 fail only known baselines (1 config v1, 14 EventV2 flaky, 1 LocationServiceMap timeout) with no new S5 failures; real production DB observed with S5 migration table/empty identity row during investigation only, not claimed as S5 test evidence. P4.2 H-10/H-11 validation 2026-08-22: source manifests validated true/success true (session d3b3c3d70… / restart 4df6cc393…), durable secret-safe evidence created with redaction/provenance, tracker updated, changeset revised, no stash change, no commit. 2026-08-24 evidence-only R11-R14 contract artifact doc: `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md` records pure-module §7.2 proof (test: `packages/core/test/operation-r11.test.ts` 14/126 + `packages/opencode/test/private-worker/failure.test.ts` 40/149 + `packages/opencode/test/private-worker/recovery.test.ts` 32/202 + `packages/opencode/test/private-worker/disposition.test.ts` 25/141, aggregate 111/618, 0 failures) with no production wiring, no new store/queue/timer, unknowns remain for live integration; P4 remains Active; P4.2 remains Active; P4.3/P4.4/P4.5 remain pending (gate 976 unchecked, P4.3 checklist unchanged).

- Current-state amendment (2026-08-25) — P4.3 legacy-reader cutover: **Complete** (atomic deletion executed 2026-08-25; no dual-read/import). Evidence `specs/vscode-orchestrator/evidence/p4.3-cutover-evidence.md` with pre-cutover `p4.3-pre-cutover-reconciliation-checklist.md` (2026-08-24, 15→4/13 mapping) preserved as historical record; `rules` gap deferred via `specs/vscode-orchestrator/evidence/p4.3-rules-contract-gap.md`. Tracker gate `Legacy-reader cutover completed at P4.3` is now `[x] MET (2026-08-25)` in P4 exit checklist above. P4.4 per-row inactive/removal evidence and transport narrowing remain **open** (gates `[ ] P4.4 source removal...`, `[ ] Each legacy effective-config source...`, and section 7 `Effective-config source removal evidence` rows remain unchecked). P4.5 CLI/TUI/Console old-surface deletion remains **open** (`[ ] CLI/TUI/Console products ...` unchecked; no TUI/CLI source edits in P4.3). Next actions are P4.4 (not P5); P5 remains Not started. Historical `P4.3/P4.4/P4.5 pending` wording in earlier Next actions lines above is superseded by this amendment for P4.3.

### P5 - Startup and selector readiness

- Status: Not started
- Scope: Startup acceptance per runtime spec section 6: persisted custom
  providers/models/agents rendered and interactive from extension-owned
  persisted indexes independent of worker lifecycle; selectors not disabled by
  readiness; runtime-required actions gated individually with explicit
  state/reason; reconciliation preserves selector availability; structural
  absence of the old readiness chain proven; cold/warm instrumentation; no
  autocomplete prewarm dependency; no global `extensionDataReady` barrier.
  Worker startup may still take time; the target removes redundant connection
  stages, not just the spinner.
- Exit checklist (criteria per runtime spec section 6):
  - [ ] Custom provider/model/agent selectors render and remain interactive from persisted indexes while the worker is not started, deliberately unavailable, starting, reconnecting, or failed (criterion 1)
  - [ ] Selecting is never disabled solely by runtime/provider validation readiness; runtime-required actions are gated individually with explicit state and reason (criterion 2)
  - [ ] External canonical-file/index reconciliation preserves selector availability — last-known valid derived index or explicit invalid/stale presentation — and never reintroduces a worker-readiness dependency or blanks/disables selectors (criterion 3)
  - [ ] Structural absence of `extensionDataReady`, port-detection/health/SSE/generated-SDK selector fetch chain, and removed startup contributors proven after P4.4/P4.5 (criterion 4)
  - [ ] Objective evidence includes an Extension Host scenario with the worker withheld/killed/failed and selectors still rendered/interactive, plus descriptive paint-before-ready timing and provenance (R7; criterion 5)
  - [ ] Cold and warm activation evidence distinguishes UI selector readiness from runtime action readiness (criterion 6)
  - [ ] Invalid/stale entries visibly reconcile without erasing user choice (runtime spec section 6, criterion 3)
  - [ ] Cold/warm startup stages instrumented (runtime spec sections 6, 10.8)
  - [ ] No autocomplete prewarm dependency; autocomplete removed (LOCK-004)
  - [ ] Every pending reconstruction-candidate entry (section 7) receives `rebuild` or `drop`; optional rebuild implementation is separately scoped and does not block P5 unless promoted to an H invariant (LOCK-015)
  - Evidence: issue: - | PR: - | test: - | doc: -
- Next actions: P3 has exited Complete (2026-08-17); P4 is Active with P4.1 Complete (2026-08-18) and P4.2a storage cutover work units S0/S1/S2/S3/S4/S5 Complete (2026-08-21) offline cutover; H-10/H-11 post-cutover Extension Host five-boundary convergence Complete (2026-08-22) as above; P4.2b-1 R1 scaffold Complete 2026-08-22; R9 wire foundation Complete 2026-08-22 (protocol foundation only); storage-backed observation adapter Complete 2026-08-22; standalone lease bootstrap Complete 2026-08-22 (corrected 2026-08-22); live canonical changefeed notification delivery Complete 2026-08-23; R12 Failure/Outcome normalization foundation Complete and R12 Resolved 2026-08-23; R11 Operation/outcome identity and canonical persistence foundation Complete and R11 Resolved 2026-08-23 (14-test operation-r11 proof: migration/identity/redacted round trip/atomic row+revision+changed feed/idempotent+conflict no double revision/family cascade/retention); R11/R12/R13/R14 Resolved; P4.2b private observation service Complete (2026-08-23) — additive `PrivateObservationService` (`enabled:true` production no-lease via `resolveCanonicalDbPath`/`Database.layerNoLease` (historical `enabled:false` gate-off superseded 2026-08-23), one `PrivateWorkerHost`, `observation/snapshot|read|ack|subscribe` + `observation/changed` forwarding, `isAbsolute` + canonical `KILO_DB`); test: `private-observation-service.test.ts` 5 pass/98 assertions; R9 Resolved 2026-08-23; P4/P4.2 remain Active; R9 Resolved 2026-08-23; five-boundary convergence proven; R9 Resolved 2026-08-23; P4.2b five-boundary production observation convergence Complete via 2026-08-23T13-59-07Z-r9-observation (audit PASS 0 blocker, secret-safe durable); P4.2b lifecycle gate Complete, P4/P4.2 remain Active (P4.3 Complete 2026-08-25; P4.4/P4.5 pending); P4.3 Complete 2026-08-25; P4.4/P4.5 removals remain pending. Final independent delivery-gate 2026-08-20 verified (Core 59 pass; OpenCode listed suites pass; S3 audit transient → 8 pass with `--timeout 30000`; snapshot 60/1 under 180s; typecheck 17/17; lint 0/6986; guards pass; source-links idempotent; architecture 1 medium reviewed; audit 0 blockers; no owned process/port) — no wire/private-worker convergence claimed beyond P4.2b-1 scaffold. S5 final validation 2026-08-21: core focused 52 pass, `cutover-s5-production.test.ts` 8 pass under `--timeout 40000`, session-export/respawn/S2 22 pass, `bun turbo typecheck` 17/17, lint 0 errors, architecture-impact 0 signals, guards pass, audit PASS (0 blockers), 686 rehearsal archives/backups/markers + 10 debug roots + validation logs cleaned, no test-owned process/port/lease/marker remain, source-links unchanged, real DB preserved; no P4.2 overall complete claimed. 2026-08-24 evidence-only R11-R14 contract artifact doc: `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md` records contract evidence only; P4 remains Active; P4.2 remains Active; P4.3/P4.4/P4.5 remain pending (gate 976 unchecked).

## 6. Capability Parity Evidence (H-1..H-13)

Criterion text is the falsifiable target-surface acceptance criterion from the
direction spec (section 6). Status is `Not proven` until objective evidence is
recorded here. The P0 baseline fixture
(`packages/opencode/test/kilocode/p0-harness-baseline.test.ts`) executes all
H-1..H-13 against the real harness services (CLI-side) through production
services and run-owned fixtures (H-3 real ToolRegistry, H-4 Skill.Service +
SkillTool with a run-owned SKILL.md, H-5 production MCP transport with a
run-owned stdio server, H-6 Permission/Question ask -> reply with pending-state
observation); no gap records remain, and every entry carries
`parity: "unproven"`. The fixture is baseline evidence, not a target-surface
parity suite: no orchestration-panel acceptance criterion is claimed as
passing. Baseline smoke only; H parity was Not proven for all 13 at P0/P1.
P2 (2026-08-15) closed that gap for the in-scope current-behavior capability:
the final durable target-surface campaign
(`doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/`,
four complete validated manifests (135 manifest entries covering 127 unique files, all hash-verified), 28/28 requests
e2e-local/e2e-model, 0 violations, 12/12 UI pins pass) drives every H-1..H-13
criterion from the Agent Manager panel over the current migration bridge, and
every row below records its falsifiable evidence path into those artifacts.
The target contracts the harness does not implement today (typed-manifest
validation/canonical provenance, restrictive policy-stack semantics,
file-authoritative WYSIWYG editing) remain explicitly NOT proven at P2 and gate
at P4 (see the target-contract split below); final H parity under the private
runtime is proven at P4 and is never waived by this tracker or any
reconstruction-candidate entry (LOCK-014).
H-11 lifecycle evidence additionally spans the extension-owned view boundaries
(panel close/reopen, reload, session switch) and the runtime boundaries
(transport reconnect, worker restart) per the direction spec (section 6, H-11)
and the runtime observation and hydration contract (runtime spec section 7.1);
all five boundaries are recorded on harness-parity flows in the real-restart and
real-session sub-campaigns (2026-08-15).

P3.2 (2026-08-16) re-evidences only the affected H-8 and H-12 paths on the
root-local post-removal surface (P2 remains the full H-1..H-13 parity proof;
P3.2 records no full H-matrix rerun): H-8's two root-local concurrent sessions
remaining controllable without worktree isolation and H-12's byte-exact
SessionRevert/Snapshot rollback are re-evidenced by the final P3.2 campaign
(`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-09-32-000Z/`
— `worktree-removal-dom-evidence`, `worktree-removal-runtime-evidence`,
`p32-snap-*.json`, `run.log`; H-12 via the shared `assertRealRollbackPhase`).
The bounded lifecycle/runtime paths are recorded in the same campaign: two
root-local session tabs seeded through the production session-open path and both
controllable over the real webview, no `.kilo/worktrees` or `.kilo/agent-manager.json`
state created, clean process/resource lifecycle (no owned VS Code process, CDP
port released, scratch removed), and the removed features' initialization absent
from the loaded runtime manifest/command table. All other H-1..H-13 evidence
remains the P2 proof and is unchanged by P3.2.

Target-contract split (avoids a P2 deadlock): P2 proves the current
behavior/capability behind each criterion (a session runs under the selected
agent; a permission/question resolves inline and the outcome applies). The
target contracts the harness does not implement today — typed-manifest
validation and canonical provenance (runtime spec section 5.2), the restrictive
policy-stack semantics (runtime spec section 5.3), and file-authoritative WYSIWYG
editing (runtime spec section 5.4) — are proven at P4 through the
field-registry/schema gate, the permission-evaluator gate, and the WYSIWYG
acceptance gate (runtime spec section 10.9; direction spec section 11), never at
P2. P2 additionally proves no P3 removal, no LOCK-006 final
provider-removal completeness, and no performance improvement/regression.

| # | Capability | Target-surface acceptance criterion | Status | Issue/PR | Test/Doc |
|---|---|---|---|---|---|
| H-1 | Custom agents | From an orchestration panel, a user can spawn a session selecting a user-defined custom agent by name, and the session runs under that agent's manifest (P2 proves this current capability). The typed-manifest contract — one canonical manifest per ID, duplicate/conflict validation, canonical file/asset provenance — is proven at P4 via the field-registry/schema gate (runtime spec sections 3.2, 5.2), not at P2 | Proven at P2 (2026-08-15, migration bridge; current-behavior capability; typed-manifest contract gates at P4) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/` — `real-completed-dom-evidence` (ModeSwitcher served and selected "E2E Agent"; 8/8 UI pins e2e-agent/e2e-local/e2e-model/low), `llm-matrix-real-completed-final.json` (byAgent `e2e-agent` 15), `llm-requests-real-completed.json` (every generation `providerID` e2e-local / `modelID` e2e-model), `rc-snap-*.json` |
| H-2 | Sub-task delegation | From an orchestration panel, a session can delegate a defined sub-task to a child session and the result flows back to the parent, visible in navigation | Proven at P2 (2026-08-15, migration bridge) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/` — `llm-requests-real-completed.json` (child `ses_ffb6f4a02ffehoL0gau0KztAZV` mode `subagent`, agent `general`), `real-completed-dom-evidence` (`finalTopics` child under parent + child-open button; run.log "PASS H-2 delegation: backend child + completed task result in the parent"), `rc-snap-*.json` (parentID/children facts) |
| H-3 | Extensible tools | A session spawned from a panel exposes the full tool registry, and a user-defined tool is invocable in that session | Proven at P2 (2026-08-15, migration bridge) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` (live: user-defined tool loaded/executed through the real ToolRegistry; no core-service mocks); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/` — `workspace/.kilo/tool/e2e_marker.ts` (run-owned user tool), artifact `workspace/e2e-custom-called.txt` = "echo:hello", run.log "PASS H-3 user tool: completed tool part in the backend" |
| H-4 | Skills | A skill is loadable and runnable from a panel-hosted session, with selection remaining harness-owned | Proven at P2 (2026-08-15, migration bridge) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` (live: run-owned SKILL.md discovered and executed through Skill.Service + SkillTool); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/` — `workspace/.kilo/skills/e2e-skill/SKILL.md` (`E2E_SKILL_CONTENT_MARKER`), run.log "PASS H-4 skill: content returned through the real skill tool" |
| H-5 | MCP | A session spawned from a panel with MCP configured has its MCP tools available and usable | Proven at P2 (2026-08-15, migration bridge) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` (live: production MCP transport connects a run-owned stdio server, tool executes in the real session loop, child cleaned by exact PID); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/` — `workspace/mcp-fixture/server.js` (run-owned stdio server), `workspace/mcp-fixture/calls.log` = "echo:hi", run.log "PASS H-5 MCP: connected + tool executed inside the real loop" + child pid 39897 alive while connected and exited after disconnect, `real-completed-mcp-disconnect-done` = `{"e2e-fixture":"disabled"}` |
| H-6 | Permission/question flows | A tool permission or question raised by a panel-hosted session resolves inline through the permission flow, and the outcome is applied to that session (P2 proves this current behavior/capability). The restrictive-policy-stack semantics — monotonic deny/ask/allow composition, no widening, enclosing parent denies/session restrictions for children, bounded per-session approval records (runtime spec section 5.3) — are proven at P4 via the permission-evaluator gate, not at P2 | Proven at P2 (2026-08-15, migration bridge; current behavior/capability; restrictive-policy-stack semantics gate at P4) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` (live: real Permission and Question ask -> reply flows with pending-state observation); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/` — `workspace/ask.txt` (`E2E_PERMISSION_SENTINEL`), run.log "PASS H-6 permission: backend pending permission observable", dock visible→gone, reply drains and completes; same for question (dock visible→gone, reply drains); backend `permission_wait` 1821 ms / `question_wait` 560 ms spans in `run.log` |
| H-7 | Parent-child sessions | Topic/session navigation shows parent/child session hierarchy, and relations persist across panel restarts | Proven at P2 (2026-08-15, migration bridge) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/` — `real-completed-dom-evidence` (`finalTopics` parent "E2E Title" with child row; run.log "PASS H-7 backend parentID/children facts persist after panel reopen" + "PASS H-7 reopened panel renders parent topic with child row"), `rc-snap-*.json` (persisted parentID/children), `real-completed-reopen-ready` |
| H-8 | Background/parallel execution | Two or more panel-hosted sessions run concurrently in the background, and each remains controllable, without worktree isolation | Proven at P2 (2026-08-15, migration bridge); re-evidenced at P3.2 (2026-08-16, root-local post-removal surface) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-session/` — run.log "PASS two concurrent sessions pinned to their own agent/variant and busy", "PASS abort A leaves A idle and B busy", "PASS abort B leaves B idle", "PASS tab A closed leaves session B tab", "PASS closed session A persists in the backend"; `real-dom-evidence` (sessionA/sessionB ids); P3.2 re-evidence: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-09-32-000Z/` — `run.log` "PASS P3.2 two root-local session tabs render: e2e-probe-47f91b0f-A, e2e-probe-47f91b0f-B", "PASS P3.2 click A activates A", "PASS P3.2 click B activates B" |
| H-9 | User-selected custom-provider models | Each panel-hosted session selects its own model and reasoning variant from a user-defined provider independently, and the selection applies | Proven at P2 (2026-08-15, migration bridge) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/` — `real-session/real-dom-evidence` (pin A `e2e-agent`/`e2e-local/e2e-model`/low pass, pin B `e2e-agent-b`/`e2e-local/e2e-model`/high pass), `real-restart/rr-pin.json` (`e2e-agent`/`e2e-local/e2e-model`/low pass), `real-completed/real-completed-dom-evidence` (8 pins pass), `real-restart/rr-model-requests.json` (scripted-model HTTP bodies model `e2e-model`) |
| H-10 | Persistence | A panel-hosted session's transcript and registered artifacts persist across an extension restart and resume in place; the bounded changefeed is non-authoritative and is not H-10 history | Proven at P2 (2026-08-15, migration bridge); re-proved at P4.2 H-10/H-11 post-cutover (2026-08-22, five-boundary conjunction, validated=true/success=true, canonical zero-state/archive stability) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-restart/` — run.log "PASS same session/transcript resumed in the fresh Extension Host" (after `workbench.action.reloadWindow`, extension-host pid 53694→55870), "PASS artifact persists after true window restart", `rr-dom-evidence` (`finalTabs`/`finalHeader` after restart); `real-session/run.log` "PASS closed session A persists in the backend", "PASS session B backend transcript persists"; P4.2 re-proof: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-22T09-00-53Z-p4.2-h10h11/real-restart/` — `rr-reload-executed`/`rr-reloaded` + `rr-dom-evidence` (fresh Extension Host, session/artifact rehydrated, canonical-gate zeroState 0), `workspace/e2e-custom-called.txt`="echo:restart", `canonical-archive-before/after` identical, source `/tmp/kilo-e2e-real-restart-20260822T165842-fresh` manifest d3b3.../4df6… validated true |
| H-11 | Lifecycle correctness | Panel-driven create/pause/resume/close drives the harness lifecycle API and releases processes/resources correctly, with no bypass, and presentation state converges to runtime operational facts across panel close/reopen, reload, session switch, transport reconnect, and worker restart (runtime spec section 7.1) | Proven at P2 (2026-08-15, migration bridge; all five boundaries recorded); re-proved at P4.2 H-10/H-11 post-cutover (2026-08-22, five-boundary conjunction, validated=true/success=true, canonical zero-state/archive stability) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-restart/` — `rr-conn.json` (transport reconnect: connecting→connected, same port 55326/pid 53824; "PASS backend session facts unchanged after SSE reconnect"), `rr-kill.json` + `rr-reconnect.json` (exact worker restart: killed pid 53824/port 55326 → new pid 54774/port 55436; transcript + artifact rehydrated), `rr-reload-executed`/`rr-reloaded` + `rr-dom-evidence` (reload), run.log (true window/Extension Host restart, exact-PID cleanup — MCP child 39897 exited after disconnect, no owned VS Code process remains, CDP port released); panel close/reopen in `real-session/` (`real-reopen-ready`, session B transcript rehydrates) and `real-completed/` (reopened panel renders parent/child topic); session switch in `real-session/` (tab A closed leaves tab B); P4.2 re-proof: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-22T09-00-53Z-p4.2-h10h11/` — `real-restart/rr-conn.json` (connecting→connected same port 57677/pid 74573), `rr-kill.json` pid 74573 → `rr-reconnect.json` pid 75399/port 57734 (transcript/artifact rehydrated), `rr-reload-executed`/`rr-reloaded` (true reload), `real-session/real-reopen-ready` + `real-ready` (session switch + panel close/reopen), llm matrices 0 violations, credential boolean, XDG/`$schema`/variant parity via kilo.jsonc and rr-pin |
| H-12 | Checkpoint rollback | From a panel-hosted session, withdrawing/reverting a message restores the affected code state, the revert can be un-reverted or cleaned up, and lifecycle stays correct; distinct from the ADR-0005 canonical storage foundation and ADR-0001's historical checkpoint/resync | Proven at P2 (2026-08-15, migration bridge); re-evidenced at P3.2 (2026-08-16, root-local post-removal surface, shared `assertRealRollbackPhase`) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/` — `real-completed-dom-evidence` `rollback` (file `rollback.txt`, boundaryMessageID, editedBytes "E2E_ROLLBACK_EDITED\n"), `workspace/rollback.txt`, run.log "PASS H-12 exact initial bytes restored" (`E2E_ROLLBACK_ORIGINAL\n`), "PASS H-12 RevertBanner visible" + lists the file, "PASS H-12 edited bytes restored by Redo All", "PASS H-12 RevertBanner gone after Redo All", reverted turns re-shown; P3.2 re-evidence: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-09-32-000Z/` — `run.log` "PASS H-12 exact initial bytes restored" (`E2E_ROLLBACK_ORIGINAL\n`), "PASS H-12 edited bytes restored by Redo All", "PASS P3.2 H-12 final bytes restored by Redo All: \"E2E_ROLLBACK_EDITED\n\"", `workspace/rollback.txt`, `worktree-removal-dom-evidence` (`rollback` facts) |
| H-13 | Internal context-overflow safeguard | A long-running panel-hosted session remains functional at context overflow with no user-facing context-management UI, and the safeguard never surfaces as a context-management product | Proven at P2 (2026-08-15, migration bridge); re-proved at P3.4 (2026-08-17, post-removal surface — H-13 P0 harness 8/8 + automatic overflow suites pass; no user-facing context-management UI surfaced) | - | test: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts`; P3.4 re-proof: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (H-13 overflow suites); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-overflow/` — `real-overflow-dom-evidence` (`overflowConfig` reportedUsage input 45000/output 8000 synthetic threshold; `surface` settingsRows 0 / headerContextMenu 0 / headerContextActions 0 — no context-management/compact controls; shared TaskHeader context popover trigger recorded as evidence only, Compact content not mounted), `llm-matrix-real-overflow-final.json` (byAgent `compaction` 1 — threshold compaction turn), run.log "PASS H-13 backend: compaction part + summary + automatic continuation in one turn" + "PASS H-13 panel shows the continuation answer" + "PASS H-13 panel surface: no context-management/compact controls" |

## 7. Removal Inventory Evidence

Each removal row records evidence per category that exists for the item: `source`,
`tests`, `docs`, `generated SDK`, `config`, `i18n`, `build/package`. A `-` in a
category means no evidence exists (the category does not exist for the item, for
example generated SDK for an extension-side removal with no SDK contract change);
a removal is complete only when every existing category has recorded evidence. No
removal is reclassified as deferred. The static
current-state inventory records the residual evidence for every row below
([`p0-current-state-inventory.md`](p0-current-state-inventory.md), section 5);
`doc:` cells cite that artifact, which records current residue only — no removal
was claimed complete before P3.1; the ordinary single-chat sidebar row below is
**Complete (2026-08-15)**, the worktree-infrastructure and custom-Diff-Viewer
rows are **Complete (2026-08-16)**, and the cloud-sessions, JetBrains, Console,
and KiloClaw rows are **Complete (2026-08-16)** with per-category objective
evidence, and the
remaining rows stay residue-only. `Unknown` means the category may exist
but was not verifiable by the static search; those cells stay open.

| Removal (LOCK) | Phase | Source | Tests | Docs | Generated SDK | Config | i18n | Build/package |
|---|---|---|---|---|---|---|---|---|
| Ordinary single-chat sidebar (LOCK-001) | P3.1 (Complete 2026-08-15) | test: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` (static absence of `registerWebviewViewProvider`/`resolveWebviewView`/`setSidebarVisible` and sidebar focus path in all production source) | test: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts`, `packages/kilo-vscode/tests/unit/chat-target.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (focused real Extension Host `test:e2e:sidebar-removal` passed) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Chat surfaces (P3.1 sidebar removal); Rollback path) | - (by nature: no generated-SDK contract change; section 8 delta 0) | test: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` (manifest contributions — no `kilo-code-ActivityBar` container, `kilo-code.SidebarProvider` view, `sidebarTitle.*` commands/menus, or `kilo-code.new.sidebarVisible` context) | test: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` (sidebar-exclusive keys absent from every app locale dictionary and all production source; shared keys preserved; `TITLE_BUTTON_CLICKED` telemetry name gone) | test: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` (package.json manifest contributions; keybindings scoped to editor surfaces); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (build table — editor chat webview; entry points unchanged), `.changeset/remove-sidebar-chat.md` (minor release note) |
| Worktree infrastructure (LOCK-002) | P3.2 (Complete 2026-08-16) | test: `packages/kilo-vscode/tests/unit/worktree-removal.test.ts` (managed worktree lifecycle/state/protocol/run/transfer/import, branch-name route, worktree SDK groups, worktree/run i18n keys, and worktree manifest/build entries absent from extension/webview/CLI source) | test: `packages/kilo-vscode/tests/unit/worktree-removal.test.ts`, `packages/opencode/test/kilocode/agent-manager-absence.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe-worktree.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (focused real Extension Host `test:e2e:worktree-removal` passed; final campaign `p3-2-final-2026-08-16T03-09-32-000Z/`) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.2 removal; Rollback path), `packages/kilo-docs/pages/automate/agent-manager.md`, `packages/kilo-docs/pages/automate/agent-manager-workflows.md`, `packages/opencode/src/kilocode/skills/kilo-config.md` (root-local model); `.changeset/agent-manager-root-local-sessions.md` (minor) | test: `packages/sdk/js/src/v2/gen/sdk.gen.ts` (Worktree 7 + BranchName 1 methods removed; section 8 delta −8 from the −11 SDK reduction) | test: `packages/kilo-vscode/tests/unit/worktree-removal.test.ts` (no worktree commands/keybindings/settings in the manifest; no `agentManager.worktree.*`/`agentManager.run.*` i18n keys) | test: `packages/kilo-vscode/tests/unit/worktree-removal.test.ts` (worktree/run i18n keys absent from every app locale dictionary; shared keys preserved) | test: `packages/kilo-vscode/tests/unit/worktree-removal.test.ts` (package.json manifest — no worktree view/container/command/keybinding; no `diff-viewer`/`diff-virtual` build entries); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (build table — Agent Manager/editor chat webviews only) |
| Custom Diff Viewer surfaces (LOCK-002) | P3.2 (Complete 2026-08-16) | test: `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts` (no `DiffViewerProvider`/`DiffVirtualProvider`/`SourceController`, no `DiffViewerPanel`/`DiffVirtualPanel` view types, no `showChanges` command, no `diff.renderMarkdown` config, no `diff-viewer`/`diff-virtual` webview bundles) | test: `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe-worktree.ts` (runtime manifest/command-table absence of custom Diff Viewer surfaces) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (native VS Code diffs and `@git-changes` replace the custom diff viewer; Rollback path); `.changeset/agent-manager-root-local-sessions.md` (minor) | - (by nature: no dedicated SDK contract beyond the worktree/branch diff methods counted under the worktree row; section 8 delta covers the SDK reduction) | test: `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts` (no `diff.renderMarkdown` config) | test: `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts` (no `diffViewer.*`/`diffVirtual.*` i18n keys in every app locale dictionary) | test: `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts` (no `diff-viewer`/`diff-virtual` webview bundles or build entries) |
| Cloud sessions (LOCK-003) | P3.3 (Complete 2026-08-16) | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (extension `cloud-session` handler, `CloudSessionList`, `CloudImportDialog`, cloud-session prune context, cloud deep link, cloud message protocol absent from source), `packages/opencode/test/kilocode/cloud-claw-removal.test.ts` (CLI `cloud-session.ts` module, `cloud-fork` flags/helpers, cloud import validation absent across source/HTTP/CLI-TUI); gateway cloud-session endpoints removed (changeset) | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts`, `packages/opencode/test/kilocode/cloud-claw-removal.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` + `packages/kilo-vscode/script/e2e-probe-launch.mjs` + `packages/kilo-vscode/script/e2e-evidence.ts` (focused real Extension Host `test:e2e:cloud-claw-removal` passed; final campaign `p3-3-final-2026-08-16T11-10-35-000Z/`) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Product runtime absence (P3.3 removal); Rollback path); `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor) | test: `packages/sdk/js/src/v2/gen/sdk.gen.ts` (cloud-session/KiloClaw endpoints removed; part of the section 8 −5 SDK reduction) | - (by nature: no dedicated extension config category for the removed cloud-session product surface) | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (no cloud/KiloClaw i18n keys in any locale) | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (manifest/esbuild/knip/tsconfig drop the removed surfaces); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (build table) |
| JetBrains (LOCK-003) | P3.3 (Complete 2026-08-16) | test: `packages/script/tests/jetbrains-removed.test.ts` (kilo-jetbrains package directory, JetBrains-only workflows, turbo tasks, release scripts/skills, container images, CI build filters absent) | test: `packages/script/tests/jetbrains-removed.test.ts` (package/workflows/allowlist/turbo/release/CI/build-filter static absence) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Product runtime absence (P3.3 removal); Rollback path); `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor) | - (by nature: JetBrains is a separate package with no generated-SDK contract change; section 8 SDK delta covers the removed products that shipped SDK surfaces) | - (by nature: no extension config category for the separate JetBrains package) | - (by nature: shared JetBrains Mono font/i18n name asserted surviving, not removed) | test: `packages/script/tests/jetbrains-removed.test.ts` (turbo tasks, workflow allowlist, release scripts, container images, CI build filters absent); doc: `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor) |
| Console (LOCK-003) | P3.3 (Complete 2026-08-16) | test: `packages/opencode/test/kilocode/console-removed.test.ts` (kilo-console/kilo-web-ui packages, CLI `console` command source/registration/assets, `/console` serve/workspace routing, opencode Console build/copy, Console CI workflow, Console user docs absent) | test: `packages/opencode/test/kilocode/console-removed.test.ts` (static absence across packages/CLI/serve-routing/build/CI/docs; shared migration-bridge `config-console`/`experimental.console.*`/SDK Console group/core `consoleState` asserted surviving) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Product runtime absence (P3.3 removal); Rollback path); `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor) | - (by nature: the shared generated-SDK Console group is asserted surviving as a migration-bridge API, not removed — no dedicated removed Console SDK contract; section 8 SDK delta excludes it) | test: `packages/opencode/test/kilocode/console-removed.test.ts` (shared `experimental.console.*` server group and core `consoleState` survive as the migration bridge) | - (by nature: no product-specific Console i18n keys were removed; generic `console.log` diagnostics remain) | test: `packages/opencode/test/kilocode/console-removed.test.ts` (kilo-console/kilo-web-ui packages removed, opencode build/copy and CI workflow drop the Console product); doc: `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor) |
| KiloClaw (LOCK-003) | P3.3 (Complete 2026-08-16) | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (KiloClawProvider, kiloclaw webview tree/bundle, `kilo-code.new.kiloClawOpen` command, KiloClaw message types absent), `packages/opencode/test/kilocode/cloud-claw-removal.test.ts` (KiloClaw TUI route/command/view wiring absent) | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts`, `packages/opencode/test/kilocode/cloud-claw-removal.test.ts`, `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` + `packages/kilo-vscode/script/e2e-probe-launch.mjs` + `packages/kilo-vscode/script/e2e-evidence.ts` (focused real Extension Host `test:e2e:cloud-claw-removal` passed; final campaign `p3-3-final-2026-08-16T11-10-35-000Z/`) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Product runtime absence (P3.3 removal); Rollback path); `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor) | test: `packages/sdk/js/src/v2/gen/sdk.gen.ts` (cloud-session/KiloClaw endpoints removed; part of the section 8 −5 SDK reduction) | - (by nature: no extension config category for the removed KiloClaw surface) | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (no cloud/KiloClaw i18n keys in any locale) | test: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (manifest/esbuild/knip/tsconfig drop KiloClaw references — build/package); doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (build table) |
| Indexing (LOCK-004) | P3.4 (Complete 2026-08-17) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (indexing module, semantic-search integration absent across source) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (20 pass / 253 assertions — indexing/semantic-search static absence), `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (28 pass / 381 assertions — extension/webview indexing surfaces absent), `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (focused real Extension Host `test:e2e:p3-4-removal` passed; final campaign `p3-4-final-2026-08-17T00-52-00-000Z/`) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.4 section + Rollback path) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (indexing-worker esbuild entry removed; SDK/OpenAPI regenerated twice idempotently; removed 18 methods total vs P3.3, final count 216) | - (by nature: no dedicated extension config category for the indexing product surface) | - (by nature: no product-specific indexing i18n keys removed) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (stale indexing-worker build entry removed; native build passes); `.changeset/p34-docs-update.md` (minor) |
| Project memory (LOCK-004) | P3.4 (Complete 2026-08-17) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (memory injection, memory tools, project-memory module absent) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (20 pass / 253 assertions), `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (28 pass / 381 assertions — extension/webview memory surfaces absent), `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (focused real Extension Host `test:e2e:p3-4-removal` passed) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.4 section + Rollback path) | - (by nature: no generated-SDK contract change for the memory product surface) | - (by nature: no dedicated extension config category for the memory product surface) | - (by nature: no product-specific memory i18n keys removed) | - (by nature: no dedicated build/package entry for the memory product surface) |
| User-visible context management/compaction (LOCK-004) | P3.4 (Complete 2026-08-17) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (CLI/TUI/ACP/HTTP/SDK summarize+compact commands/endpoints absent) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (20 pass / 253 assertions), `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (28 pass / 381 assertions — context management UI controls absent: settings rows 0, header context menu 0, header context actions 0), `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (focused real Extension Host `test:e2e:p3-4-removal` passed) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.4 section + Rollback path) | - (by nature: summarize+compact SDK endpoints removed as part of the SDK reduction; section 8 records the delta) | - (by nature: no dedicated extension config category for the context management product surface) | - (by nature: no product-specific context-management i18n keys removed) | - |
| Autocomplete (LOCK-004) | P3.4 (Complete 2026-08-17) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (autocomplete module, next-edit, FIM endpoints absent) | test: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (20 pass / 253 assertions), `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (28 pass / 381 assertions — extension autocomplete/FIM surfaces absent), `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (focused real Extension Host `test:e2e:p3-4-removal` passed) | doc: `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.4 section + Rollback path) | - (by nature: autocomplete/FIM SDK endpoints removed as part of the SDK reduction; section 8 records the delta) | - (by nature: no dedicated extension config category for the autocomplete product surface) | - (by nature: no product-specific autocomplete i18n keys removed) | - (by nature: autocomplete webview bundle entry not present in the esbuild entries) |
| Preset providers/catalog/onboarding/org sources (LOCK-006) | P4.4 | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` | - | - |

### Effective-config source removal evidence

Per runtime spec section 8.1, each of the 13 legacy effective-config source
classes below records removal evidence per category that exists for the item
(same `source`/`tests`/`docs`/`generated SDK`/`config`/`i18n`/`build/package`
rules as the product removals above) plus an **inactive proof**: no read of that
source affects effective config after the P4.3 cutover, because its reader is
deleted together at the cutover. No migration/import tool is a target effective-
config authority; the extension legacy-migration/importer and Roo wiring are
removed, and the CLI/TUI migration helper `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` (`migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`) and `Flag.KILO_TUI_CONFIG` getter were physically deleted on 2026-08-27 as a bounded residual package (zero active callers since T27; canonical T27-T30 loader `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` global→direct→`.kilo`; `ConfigPaths.files` retained with no active TUI call). P4.4 remains Active/residual — broader migration/import tooling and P4.5-adjacent theme/TUI residues remain open — and there is no dual-read compatibility window. The sole user manually recreates any desired current
configuration in the canonical files before the cutover. A row is complete only
when every existing category records evidence and the inactive proof is
recorded. The P0 baseline enumerates 15 current merge sources (inventory §6.1);
each maps onto a retained legal source class (runtime spec section 3.1) or
exactly one of the 13 removal classes below, so no baseline source remains
unclassified after P4.4. The P4.4 evidence phase (runtime spec section 8.1;
tracker section 8) counts each row as one source class and verifies per-row
inactive/removal evidence; P4.4 exits with zero rows active.

| Source class (runtime spec section 8.1) | Evidence phase | Source | Tests | Docs | Generated SDK | Config | i18n | Build/package | Inactive proof |
|---|---|---|---|---|---|---|---|---|---|
| `KILO_CONFIG` env override | P4.4 (2026-08-26) P4.4-T21 | deleted: `sources.ts` inventory removed (P4.4-T1b); `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts:148-158` TUI migration helper `Flag.KILO_CONFIG` injection physically deleted (P4.4-T13) — `if (Flag.KILO_CONFIG) files.push(Flag.KILO_CONFIG)` removed; `Flag` import retained for `Flag.KILO_DISABLE_PROJECT_CONFIG`; `packages/core/src/flag/flag.ts` `KILO_CONFIG: process.env["KILO_CONFIG"]` Flag entry physically deleted (P4.4-T21) — no `Flag.KILO_CONFIG` definition or reader remains; `Flag.KILO_CONFIG_DIR` getter, `Global.Path.config` override (historical T18 TUI `KILO_CONFIG_DIR` last-wins claim superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains, see P4.4-T30/final-audit — canonical global→root direct→root `.kilo`), sandbox deny preserved per LOCK-003/010 | test: `p4-3-cutover.test.ts:13-78`; `packages/opencode/test/kilocode/p4-4-tui-migrate-kilo-config-removal.test.ts` (tui-migrate absence); `packages/opencode/test/kilocode/p4-4-flag-legacy-getter-removal.test.ts` (Flag `KILO_CONFIG` definition/reader absence, `KILO_CONFIG_DIR`/`Global`/`TUI`/`sandbox`/`ConfigPaths` preservation, SDK/OpenAPI unchanged, sorted `bundled < flag < managed < model-cache < primary < provider-login < provider < sdk < t16 < t18 < tui-migrate < wellknown`) | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 1 (P4.4-T21, historical T18 superseded in T27, see T30; current canonical T27-T30) | - | residual: TUI helper `Flag.KILO_CONFIG` injection deleted (P4.4-T13); Flag entry `KILO_CONFIG` deleted (P4.4-T21); `sources.ts` already removed (P4.4-T1b); canonical `config/config.ts` has no `Flag.KILO_CONFIG` reader; historical T18 TUI `KILO_CONFIG_DIR` sole-reader last-wins superseded (T27-correction removes all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/migrate readers) — now `Flag.KILO_CONFIG_DIR` has no effective TUI reader (T27-T30), `Global.Path.config` override and sandbox deny retained per LOCK-003 (not effective source) | - | residual retained: TUI migration helper `tui-migrate.ts` (`migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL`) and `Flag.KILO_TUI_CONFIG` physically deleted 2026-08-27 (zero active callers since T27; canonical `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` global→direct→`.kilo`; `ConfigPaths.files` retained with no active TUI call); `Flag.KILO_DISABLE_PROJECT_CONFIG` gate preserved | effective inactive: `Flag.KILO_CONFIG` absent from `Flag` and `Config.Service`; no effective-config reader (T13 + T21); sandbox `KILO_CONFIG` deny remains as safety (not effective source); row remains residual/open — not complete per LOCK-001 |
| `KILO_CONFIG_DIR` env override | P4.4 (2026-08-27) P4.4-T27-correction (supersedes T18) | deleted: `sources.ts:135-159,184-194` removed (P4.4-T1b); `packages/opencode/src/session/instruction.ts:60-88` `Flag.KILO_CONFIG_DIR` profile removed (P4.4-T17) — `...(Flag.KILO_CONFIG_DIR ? [path.join(Flag.KILO_CONFIG_DIR, "AGENTS.md")] : [])` and `Flag.KILO_CONFIG_DIR ?? global.config` deleted; `packages/opencode/src/config/paths.ts:4,22-24` `Flag.KILO_CONFIG_DIR` import/reader physically removed (P4.4-T18) — `Flag` import deleted, `directories()` now `unique([Global.Path.config])` only; **historical T18 TUI sole-reader superseded** — P4.4-T27 (T27-correction) physically removed all TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrateTuiConfig`/`.kilocode`/ancestor readers from `cli/cmd/tui/config/tui.ts` (deleted `migrateTuiConfig` import/call, `Flag.KILO_CONFIG_DIR`/`Flag.KILO_TUI_CONFIG` env reads, `yield* afs.up({ targets: [".kilocode", ".kilo"] })` walk); **current (T27-T30)**: TUI uses canonical Git worktree root (`resolveWorktree`/`canonicalRoot` via `Filesystem.resolve`, non-Git fallback to `directory`) with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only (`ConfigPaths.files` active call removed); `Global.Path.config` override in `packages/core/src/global.ts:79` and sandbox deny-list retained per LOCK-003 (not effective TUI source) | test: `p4-3-cutover.test.ts:152-168` (amended T27 — instruction/ConfigPaths have no `KILO_CONFIG_DIR`, TUI has no `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate`; historical T18 TUI explicit last-wins superseded in T27, see T30); `packages/opencode/test/kilocode/instruction.test.ts` (P4.4-T17); `packages/opencode/test/kilocode/p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` (ConfigPaths global-only; historical T18 TUI sole-reader superseded in T27, see T30; current canonical T27-T30); `p4-4-tui-migrate-kilo-config-removal` (amended T27) | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 2 (P4.4-T18, historical T18 superseded in T27, see T30; current canonical T27-T30) | - | deleted: instruction profile removed (P4.4-T17) and ConfigPaths reader removed (P4.4-T18) and TUI `KILO_CONFIG_DIR`/`KILO_TUI_CONFIG`/`migrate`/ancestor readers removed (P4.4-T27); historical T18 sole-reader superseded — no TUI `KILO_CONFIG_DIR` reader remains (T27-T30 canonical `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` only); `Global.Path.config` override and sandbox deny retained per LOCK-003, not `Config.Service` authority | - | residual: no TUI `KILO_CONFIG_DIR` reader remains (T27-T30); `ConfigPaths` now `unique([Global.Path.config])` only; instruction no longer uses `KILO_CONFIG_DIR`; `Global.Path.config` override and sandbox deny retained per LOCK-003 (not effective source) | effective inactive: canonical loader ignores flag; instruction now also ignores `KILO_CONFIG_DIR`; `ConfigPaths` now also ignores `KILO_CONFIG_DIR` — historical T18 TUI sole-reader last-wins superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR` reader remains (T27-T30 canonical global→root direct→root `.kilo`, see final-audit) (row remains open/residual; P4.4 not complete) |
| `KILO_CONFIG_CONTENT` env override | P4.4 (2026-08-26) P4.4-T21 | deleted: `sources.ts:171-183` removed (P4.4-T1b); handwritten SDK wrapper `KILO_CONFIG_CONTENT` env forwarding deleted at `packages/sdk/js/src/server.ts` and `packages/sdk/js/src/v2/server.ts` (`mergeConfig`/`parseExistingConfig`/`buildConfigEnv` helpers and `KILO_CONFIG_CONTENT: buildConfigEnv(...)` env forwarding) — wrappers now retain only `...process.env` and `logLevel` arg; `packages/sdk/js/test/server.test.ts` forwarding suite removed and replaced with wrapper-absence regression; `packages/core/src/flag/flag.ts` `KILO_CONFIG_CONTENT: process.env["KILO_CONFIG_CONTENT"]` Flag entry physically deleted (P4.4-T21) — no `Flag.KILO_CONFIG_CONTENT` definition or reader remains; `Flag.KILO_CONFIG_DIR`/`Global`/`TUI`/`sandbox` preserved per LOCK-003/010 | test: `p4-3-cutover.test.ts:39-47`; `packages/sdk/js/test/server.test.ts` (wrapper absence) and `packages/opencode/test/kilocode/p4-4-sdk-config-forwarding-removal.test.ts` (handwritten wrapper absence, generated SDK/OpenAPI unchanged); `packages/opencode/test/kilocode/p4-4-flag-legacy-getter-removal.test.ts` (Flag `KILO_CONFIG_CONTENT` definition/reader absence, protected paths preservation, SDK/OpenAPI unchanged, sorted profile) | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 3 (P4.4-T21) | SDK/server-wrapper evidence (handwritten): `packages/sdk/js/src/v2/server.ts`, `src/server.ts` — no `mergeConfig`/`parseExistingConfig`/`buildConfigEnv`/`KILO_CONFIG_CONTENT`; actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`; `packages/sdk/openapi.json` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009 HTTP/SSE bridge preserved) | residual: Flag entry `KILO_CONFIG_CONTENT` deleted (P4.4-T21); handwritten wrapper forwarding deleted (P4.4-T9); source inventory removed (P4.4-T1b); canonical `config/config.ts` has no `KILO_CONFIG_CONTENT` reader; `Flag.KILO_CONFIG_DIR`/`Global`/`TUI`/`sandbox` preserved | - | residual retained: handwritten SDK wrapper forwarding deleted; Flag entry deleted; generated SDK/OpenAPI and HTTP/SSE transport unchanged (LOCK-009) | effective inactive: `Flag.KILO_CONFIG_CONTENT` absent from `Flag` and `Config.Service`; handwritten forwarding cannot affect canonical config; sandbox `KILO_CONFIG_CONTENT` deny remains as safety (not effective source); row remains residual/open — not complete per LOCK-001 |
| `KILO_PERMISSION` env override | P4.4 (2026-08-26) P4.4-T21 | deleted: `sources.ts:275-291` removed (P4.4-T1b); `packages/core/src/flag/flag.ts` `get KILO_PERMISSION() { return process.env["KILO_PERMISSION"] }` Flag entry physically deleted (P4.4-T21) — no `Flag.KILO_PERMISSION` definition or reader remains; sandbox environment deny-list at `packages/opencode/src/kilocode/sandbox/policy.ts:168-175` retained with literals `"KILO_CONFIG"`, `"KILO_CONFIG_CONTENT"`, `"KILO_CONFIG_DIR"` exactly per LOCK-003; `Flag.KILO_CONFIG_DIR` getter and `Global.Path.config` override preserved | test: `p4-3-cutover.test.ts:39-51`; `packages/opencode/test/kilocode/p4-4-flag-legacy-getter-removal.test.ts` (Flag `KILO_PERMISSION` definition/reader absence, protected KILO_CONFIG_DIR/Global/TUI/sandbox preservation, SDK/OpenAPI unchanged, sorted profile); `packages/opencode/test/permission/r18-service.test.ts` (permission operational, no Flag reader) | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 4 (P4.4-T21) | - | residual: Flag entry `KILO_PERMISSION` deleted (P4.4-T21); sandbox safety deny preserved with exact literals; no `Flag.KILO_PERMISSION` effective overlay | - | - | effective inactive: `Flag.KILO_PERMISSION` absent from `Flag` and `Config.Service` (T21 + P4.3 `result.tools` absent); sandbox `KILO_PERMISSION` not an effective source — deny remains as safety; row remains residual/open — not complete per LOCK-001 |
| Legacy `opencode.*` keys, `.opencode` / `.kilocode` locations | P4.4 (2026-08-27) P4.4 residual package (supersedes T27-correction) | deleted: `sources.ts` inventory removed (P4.4-T1b); `packages/opencode/src/config/paths.ts:29,35` `.kilocode` targets physically removed (P4.4-T14), home `.kilo` removed (P4.4-T15), ancestor `.kilo` walk + dead guard removed (P4.4-T16), `ConfigPaths.directories()` now `unique([Global.Path.config])` only (`afs.up`/`targets: [".kilo"]`/`Global.Path.home`/`Flag.KILO_CONFIG_DIR`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`void` absent; `ConfigPaths.files()`/`fileInDirectory()` retained for other consumers); **historical T18 TUI legacy-directory last-wins superseded** — P4.4-T27 (T27-correction) physically removed all TUI legacy readers (`migrateTuiConfig`, `Flag.KILO_TUI_CONFIG`, `Flag.KILO_CONFIG_DIR`, `yield* afs.up({ targets: [".kilocode", ".kilo"] })` walk, `.kilocode` handling) and narrowed TUI to canonical Git worktree root (`resolveWorktree`/`canonicalRoot`, `ConfigPaths.fileInDirectory(root, "tui")` + `ConfigPaths.fileInDirectory(path.join(root, ".kilo"), "tui")` only; `ConfigPaths.files` active call removed); **P4.4 residual package (2026-08-27) physically deleted dead helper `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` (zero active callers since T27) and `Flag.KILO_TUI_CONFIG` getter (no production reader; canonical T27/T30 loader preserved);** `packages/opencode/src/cli/cmd/tui/context/theme.tsx:564` `[".kilocode", ".kilo"]` preserved per scope (P4.5-adjacent, not TUI effective source) | test: `p4-3-cutover.test.ts:97-108,161-184` now assert `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void`/`KILO_CONFIG_DIR` absent from `ConfigPaths` with `Global.Path.home` absent and `Global.Path.config` preserved (historical T18 claim of TUI `targets: [".kilocode", ".kilo"]` gated and `Flag.KILO_CONFIG_DIR` explicit last-wins — superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR`/`.kilocode`/ancestor reader remains, see P4.4-T30/final-audit); `p4-4-t16-tui-legacy-discovery.test.ts` behavior (historical at T16 — superseded in T27, see T30); `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` (ConfigPaths global-only; historical T18 TUI sole-reader superseded in T27, see T30); `p4-4-tui-migrate-kilo-config-removal.test.ts` (helper absent + Flag absent + canonical preserved) | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 5 (P4.4 residual package helper + getter deleted; row remains residual/open per LOCK-001) | - | deleted: TUI migration helper file and `Flag.KILO_TUI_CONFIG` getter deleted (2026-08-27); source inventory removed (P4.4-T1b); canonical stamp is `kilo.jsonc` only | - | residual: `theme.tsx:564` `[".kilocode", ".kilo"]` preserved per scope (P4.5-adjacent, not TUI effective source); no active TUI legacy-directory reader remains (T27-T30) | effective inactive: canonical filename/location anchors absent; `ConfigPaths.directories()` ancestor removal verified by absence of `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void` and `Global.Path.home` absence (P4.4-T16/T18); historical T18 TUI-local discovery with explicit `KILO_CONFIG_DIR` last-wins verified as historical (superseded in T27, see T30); current canonical TUI is T27-T30 global→root direct→root `.kilo` only; residual retained | kages/opencode/test/kilocode/config/config.test.ts` preserved | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 5 (P4.4-T18, historical T18 superseded in T27, see T30; current canonical T27-T30) | - | residual: source inventory removed (P4.4-T1b); `ConfigPaths.directories()` `.kilocode` removed (P4.4-T14), home walk removed (P4.4-T15), ancestor walk+dead guard removed (P4.4-T16) and `KILO_CONFIG_DIR` reader removed (P4.4-T18) — now `Global.Path.config` only; historical T18 TUI-local `.kilo`/`.kilocode` with explicit `KILO_CONFIG_DIR` last-wins superseded in P4.4-T27 — no TUI `KILO_CONFIG_DIR`/`.kilocode`/ancestor reader remains (T27-T30 canonical `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` only); canonical `.kilo/kilo.jsonc` only | - | residual: `theme.tsx:564` `[".kilocode", ".kilo"]` preserved per scope (P4.5-adjacent, not TUI effective source); no active TUI legacy-directory reader remains (T27-T30) | effective inactive: canonical filename/location anchors absent; `ConfigPaths.directories()` ancestor removal verified by absence of `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void` and `Global.Path.home` absence (P4.4-T16/T18); historical T18 TUI-local discovery with explicit `KILO_CONFIG_DIR` last-wins verified as historical (superseded in T27, see T30); current canonical TUI is T27-T30 global→root direct→root `.kilo` only; residual retained |
| Global project-asset sources | P4.4 (2026-08-25) P4.4-T2 | `sources.ts` inventory removed (P4.4-T1b); `packages/opencode/src/kilocode/primary-worktree.ts:7-37` helper deleted (P4.4-T2) | test: `p4-3-cutover.test.ts`; `p4-4-primary-worktree-removal.test.ts`; `packages/kilo-vscode/tests/unit/worktree-removal.test.ts` | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 6 | - | helper deleted; canonical project boundary only | - | - | effective inactive: no canonical-loader `primaryPaths`/legacy discovery; helper file absent; residual retained for other project-asset sources only |
| Ancestor directory walks | P4.4 (2026-08-27) P4.4-T27-correction (supersedes T18) | residual: `packages/opencode/src/config/paths.ts:23-27` workspace-ancestor `afs.up({ targets: [".kilo"], start: directory, stop: worktree })` walk plus dead guard/void removed (P4.4-T16) — `ConfigPaths.directories()` now `unique([Global.Path.config])` only (`afs.up`/`targets: [".kilo"]`/`start: directory`/`stop: worktree`/`Flag.KILO_DISABLE_PROJECT_CONFIG`/`void`/`Flag.KILO_CONFIG_DIR` absent, `Global.Path.home` remains absent, `Global.Path.config` preserved, signature `directories(): Effect<string[]>`; `ConfigPaths.files()`/`fileInDirectory()` retained for other consumers; `.kilocode` removed P4.4-T14, home `.kilo` removed P4.4-T15; `sources.ts:121-159` removed P4.4-T1b; `session/instruction.ts` `KILO_CONFIG_DIR` removed P4.4-T17); **historical T18 TUI-local legacy discovery superseded** — P4.4-T27 (T27-correction) removed `tui.ts:207-221` `yield* afs.up({ targets: [".kilocode", ".kilo"] })` gated walk and `KILO_CONFIG_DIR` last-wins merge, `migrateTuiConfig` and `KILO_TUI_CONFIG` readers, and active `ConfigPaths.files("tui", ctx.directory)` ancestor walk — now TUI uses canonical Git worktree root (`resolveWorktree`/`canonicalRoot`) with `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only; `theme.tsx:564` `[".kilocode", ".kilo"]` preserved per scope (P4.5-adjacent, not TUI effective source) | test: `p4-3-cutover.test.ts:97-108,161-184` now assert `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void`/`KILO_CONFIG_DIR` absent from `ConfigPaths` with `Global.Path.config`/`files`/`fileInDirectory` preserved (historical T18 claim of TUI `targets: [".kilocode", ".kilo"]` gated and `Flag.KILO_CONFIG_DIR` explicit last-wins — superseded in P4.4-T27; no TUI `KILO_CONFIG_DIR`/`.kilocode`/ancestor reader remains, see P4.4-T30/final-audit); `packages/opencode/test/kilocode/config/config.test.ts:563-590`; `p4-4-t16-tui-legacy-discovery.test.ts` behavior (historical at T16 — superseded in T27, see T30); `p4-4-t18-config-paths-kilo-config-dir-removal.test.ts` (ConfigPaths global-only; historical T18 TUI sole-reader superseded in T27, see T30); `p4-4-tui-migrate-kilo-config-removal`/`p4-4-managed-removal`/`p4-4-primary-worktree-removal` now assert `ConfigPaths` global-only (historical T18 TUI gated presence superseded in T27) | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 7 (P4.4-T18, historical T18 superseded in T27, see T30; current canonical T27-T30) | - | residual: `ConfigPaths.directories()` ancestor walk+dead guard removed (P4.4-T16) and `KILO_CONFIG_DIR` reader removed (P4.4-T18) — now `Global.Path.config` only; historical T18 temporary TUI-local `.kilo`/`.kilocode` discovery gated with explicit `KILO_CONFIG_DIR` last-wins superseded in P4.4-T27 — no TUI `KILO_CONFIG_DIR`/`.kilocode`/ancestor reader remains (T27-T30 canonical `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` only); `ConfigPaths.files()` ancestor walk for `tui` direct files removed (P4.4-T27, no active call); instruction `KILO_CONFIG_DIR` profile removed in P4.4-T17 (historical), `ConfigPaths` is global-only per P4.4-T18 | - | residual: no active TUI legacy-directory reader remains (T27-T30 canonical); `ConfigPaths.files` API retained with no active TUI call per P4.4-T30 clarification; `ConfigPaths.directories()` is global-only; instruction `KILO_CONFIG_DIR` removed in P4.4-T17 | effective inactive: canonical loader has no `ConfigPaths.files`/`directories`; `ConfigPaths.directories()` ancestor+dead guard deletion verified by absence of `afs.up`/`targets: [".kilo"]`/`KILO_DISABLE_PROJECT_CONFIG`/`void`/`Flag.KILO_CONFIG_DIR` and `Global.Path.home` absence (P4.4-T16/T18); historical T18 TUI-local legacy discovery verified as historical (superseded in T27, see T30); current canonical TUI is T27-T30 global→root direct→root `.kilo` only; instruction profile removal verified in P4.4-T17; residual retained (effective-config inactivity already established P4.3) |
| Primary-worktree mirror reads | P4.4 (2026-08-25) P4.4-T2 | deleted: `packages/opencode/src/kilocode/primary-worktree.ts` helper deleted (P4.4-T2); `test/kilocode/primary-worktree.test.ts` deleted; `script/kilocode/test-profile.ts` glob removed | test: `p4-4-primary-worktree-removal.test.ts`; `packages/kilo-vscode/tests/unit/worktree-removal.test.ts:334-349`; `p4-3-cutover.test.ts` | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 8 | - | helper deleted; canonical `canonicalRoot` is effective boundary | - | - | helper file absent and no production import remains; effective inactive: no canonical-loader `primaryPaths`; canonical tests prove mirror values do not load |
| Cloud/org/managed config sources | P4.4 (2026-08-26) P4.4-T23 | deleted: `sources.ts` inventory removed (P4.4-T1b); `packages/opencode/src/config/managed.ts` helper physically deleted (P4.4-T3) — `managedConfigDir`, `parseManagedPlist`, `readManagedPreferences` absent; CLI `.well-known/opencode` provider-auth discovery URL-login branch deleted at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`login [url]` positional and `args.url` `fetch`/`Process.spawn`/`wellknown` persistence) (P4.4-T4) — `args.url` fetch, `Process.spawn`, `wellknown` persistence removed; `login [url]` → `login`; preset provider display metadata helper `packages/opencode/src/kilocode/provider/metadata.ts` physically deleted (P4.4-T5) — `providerMetadata` preset `noteKey`/`icon`/`priority` mapping absent; `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts` import and `metadata: providerMetadata(item.id)` enrichment removed (P4.4-T5) — listing now `all: Object.values(validProviders).map(Provider.toPublicInfo)` without preset enrichment; preset `@kilocode/kilo-gateway` bundled provider loader `KILO_BUNDLED_PROVIDERS` (`"@kilocode/kilo-gateway": async () => createKilo`) deleted from `packages/opencode/src/kilocode/provider/provider.ts` and its `...KILO_BUNDLED_PROVIDERS` spread removed from `packages/opencode/src/provider/provider.ts` `BUNDLED_PROVIDERS` (P4.4-T6) — generic SDK loaders (`@ai-sdk/*`, `@openrouter/ai-sdk-provider`, etc.), `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache, fallback fetch, background refresh, explicit CLI refresh; `bun run refresh:models` only refreshes checked-in snapshot), custom-provider/auth lifecycle, and `/provider` bridge retained per LOCK-006/009 (`ModelCache` `clear` is historical at T6/T7 and superseded 2026-08-27 by file deletion; `ModelCache` service boundary deleted) — only `kilo`/`apertis` dynamic fetching was removed; dynamic `kilo`/`apertis` model resolution through removed injection intentionally not runtime-proven in this static unit; kilo/apertis dynamic fetch/cache physically removed (P4.4-T7) — `provider/model-cache.ts` simplified to invalidation-only `clear` (stubbed `get`/`fetch` without network; `fetchKiloModels`/`KiloModelsService`/`APERTIS_BASE_URL`/`fetchApertisModels`/`authOptions`/`HttpClient`/`KILO_API_KEY` deleted), `provider/models.ts` removed `ModelCache`/`Config`/`Auth`/`kilo`/`apertis` injection (`cache.fetch`/`KILO_OPENROUTER_BASE`/`providers.kilo`/`apertis`) — static `Core` overlay only, handler removed `ModelCache.Service`/`failedProviders()` — `failed: string[] = []` with `validProviders` guard without failedSet; committed `models-api.json` 3.0 MB build-time snapshot, generic `Core.ModelsDev` runtime (`packages/core/src/models-dev.ts` with disk cache, fallback fetch, 60-minute background refresh, explicit `kilo models --refresh`; `bun run refresh:models` only refreshes checked-in snapshot) plus static `Core` wrapper, `BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge retained per LOCK-006/009 — only `kilo`/`apertis` dynamic fetching was removed; CLI preset provider login selector identity physically removed (P4.4-T8) — `packages/opencode/src/cli/cmd/providers.ts` priority map (`kilo: 0`, `anthropic: 2`, `github-copilot: 3`, `openai: 4`, `google: 5`, `openrouter: 6`, `vercel: 7`), `kilo`/`openai` hints, `codex` -> `openai` alias removed; `packages/opencode/src/cli/cmd/github.handler.ts` priority/recommended map (`kilo: 0`, `anthropic: 1`, `openai: 2`, `google: 3`) removed; deterministic generic ordering retained as `sortBy((x) => x.name ?? x.id)` without preset identity; plugin/custom-provider auth, `ModelsDev.Service`, provider HTTP/SSE bridge, Core `ModelsDev`, `models-api.json` retained per LOCK-006/009; dead synthetic Kilo provider factory `createKiloFallbackProvider` and synthetic literals `Kilo Gateway`/`settings.providers.note.kilo`/`KILO_API_KEY`/`noteKey`/`icon`/`priority`/`custom` deleted from `packages/kilo-vscode/src/shared/provider-model.ts` — file now exports `KILO_PROVIDER_ID`, `KILO_AUTO`, `PROVIDER_PRIORITY`, `providerOrderIndex` plus `CUSTOM_PROVIDER_PACKAGES`/`PROVIDER_ID_PATTERN`/`parseModelString`/`isCustomProviderPackage` with no unused imports/types beyond that deletion (P4.4-T12); orphaned locale keys `settings.providers.tag.gateway`/`dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` (20 locales) and `settings.providers.note.kilo` from every `packages/kilo-i18n/src/*.ts` (20 locales) — retained `KILO_PROVIDER_ID`/`KILO_AUTO`/`PROVIDER_PRIORITY`/`providerOrderIndex`, session `KILO_AUTO` fallback, speech guard, generic custom-provider UI per LOCK-006/009/010; extension webview preset-provider priority/catalog residue physically removed (P4.4-T20) — `packages/kilo-vscode/src/shared/provider-model.ts:9-17,30-33` deleted `PROVIDER_PRIORITY` (`kilo`/`anthropic`/`deepseek`/`openai`/`google`/`openrouter`/`vercel`) and `providerOrderIndex` (now exports only `KILO_PROVIDER_ID`, `KILO_AUTO`, `CUSTOM_PROVIDER_PACKAGES` plus pattern/parse helpers); `webview-ui/src/components/settings/provider-catalog.ts:1-10,12-22,44-50` deleted `PROVIDER_PRIORITY as FALLBACK_PROVIDER_IDS` import, `fallback` Set, `isPopularProvider`, `popularProviderIndex`, `providerOrderIndex` dependence — `sortProviders` now `a.name.localeCompare(b.name) || a.id.localeCompare(b.id)` deterministic alphabetical; `webview-ui/src/components/shared/model-selector-utils.ts:1-9,77,145-147` deleted `PROVIDER_PRIORITY as PROVIDER_ORDER` import, `providerOrderIndex` import, `export { PROVIDER_ORDER }`, `providerSortKey` — `buildModelGroups` provider-group sort now `aLabel.localeCompare(bLabel) || aId.localeCompare(bId)` alphabetical by display name with id tie-break; `webview-ui/src/components/settings/provider-tab-helpers.ts:10,113-118` removed `isPopularProvider` import and `&& isPopularProvider(item)` filter — `buildAddList` now `sortProviders(Object.values(allProviders).filter(!configuredIds.has(id)))` generic alphabetical; `webview-ui/src/components/settings/ProviderSelectDialog.tsx:12-17,70-89` removed `isPopularProvider`/`popularProviderIndex` imports and priority `groupBy`/`sortBy`/`sortGroupsBy` — now single `dialog.provider.group.other` alphabetical with `CUSTOM_PROVIDER_ID` first; generic provider discovery, custom-provider save/delete/validation, provider model/catalog loaders, auth lifecycle, and HTTP/SSE/generated-SDK bridge retained per LOCK-006/009 (still open/residual; no row/phase complete) | test: `p4-3-cutover.test.ts:35-38` (canonical-loader absence); `p4-4-managed-removal.test.ts` (helper file/import-surface absence, direct-test removal, canonical-loader preservation); `p4-4-wellknown-provider-auth-removal.test.ts` (CLI well-known discovery branch absence, stale config.test helpers removed, retained generic provider-login guarded); `p4-4-provider-metadata-removal.test.ts` (preset helper file/import-surface absence, handler enrichment absence, `all`/`connected`/`default`/`failed` aggregation and auth/model/config dependency preservation, `Provider.Info` optional `metadata` contract unchanged, generated OpenAPI/SDK unchanged, generic adapters/custom-provider/bridge preservation, sorted test-profile `managed < primary < provider < wellknown`); `p4-4-bundled-provider-loader-removal.test.ts` (static source presence only — preset loader `KILO_BUNDLED_PROVIDERS`/`createKilo`/`"@kilocode/kilo-gateway"` loader absence, `BUNDLED_PROVIDERS` generic adapters/`models-api.json`/`ModelsDev`/`ModelCache`/custom-provider/`/provider` bridge static preservation, dynamic fallback intentionally unproven, sorted test-profile `bundled < managed < primary < provider < wellknown`); `p4-4-model-cache-removal.test.ts` (kilo/apertis network-absence, handler/models absence, `models-api.json`/`models-dev.ts`/`BUNDLED_PROVIDERS`/`KILO_MODEL_SCHEMA_EXTENSIONS`/custom-provider `clear`/bridge preservation, `clear`/`get`/`failedProviders` runtime invalidation, sorted test-profile `bundled < managed < model-cache < primary < provider < wellknown`); `p4-4-provider-login-preset-removal.test.ts` (preset priority/hint/codex alias absence in `cli/cmd/providers.ts` and `cli/cmd/github.handler.ts`, deterministic generic `sortBy((x) => x.name ?? x.id)` ordering with insertion-order tie preservation for same-name collisions and `matchProviderInput` exact id plus case-insensitive name dispatch with distinct-id assertions, `ModelsDev.Service`/plugin/custom-provider/`other` path and `/provider` bridge preservation, Core `ModelsDev`/`models-api.json` retained, sorted test-profile `bundled < managed < model-cache < primary < provider-login < provider < wellknown`); P4.4-T20: `packages/kilo-vscode/tests/unit/model-grouping.test.ts` alphabetical `Anthropic < Kilo Gateway < OpenAI` (`aLabel.localeCompare(bLabel) || aId.localeCompare(bId)`), `p4-4-provider-metadata-removal.test.ts` updated to assert `provider-catalog.ts` generic `icon`/`noteKey`/`synthetic`/`validIcon` preservation plus `FALLBACK_PROVIDER_IDS`/`PROVIDER_PRIORITY`/`provider.metadata?.priority`/`isPopularProvider`/`providerOrderIndex` absence and alphabetical `sortProviders` — generic/custom discovery retained, no preset priority | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 9; runtime §8.1 | - | helper deleted (P4.4-T3); source inventory removed (P4.4-T1b); CLI well-known discovery deleted (P4.4-T4); preset metadata enrichment deleted (P4.4-T5) — `Provider.Info` optional `metadata` remains contract-compatible when omitted; config-console source reporting removed; canonical loader has no managed/org read | orphaned locale keys `settings.providers.tag.gateway`/`dialog.provider.tag.recommended` removed from every `packages/kilo-vscode/webview-ui/src/i18n/*.ts` (20 locales) and `settings.providers.note.kilo` removed from every `packages/kilo-i18n/src/*.ts` (20 locales) (P4.4-T12) — no production consumer; other provider/org/catalog labels remain residual but not proof of an effective reader; `Auth.WellKnown` storage type remains in `packages/opencode/src/auth/index.ts` for persisted credential compatibility; `models-api.json` 3.0 MB, `models.dev` snapshot, `ModelCache`, generic SDK loaders, `KILO_MODEL_SCHEMA_EXTENSIONS`/`patchModelsDevModel`, custom-provider/auth lifecycle, and `Provider` optional `metadata` remain open P4.4 residues (LOCK-006; `KILO_BUNDLED_PROVIDERS`/`createKilo` deleted P4.4-T6) | deleted: CLI `.well-known/opencode` fetch at `packages/opencode/src/cli/cmd/providers.ts:327` removed (P4.4-T4); preset display metadata enrichment at `packages/opencode/src/server/routes/instance/httpapi/handlers/provider.ts:85` removed (P4.4-T5) — `Provider.toPublicInfo` without preset enrichment; preset `@kilocode/kilo-gateway` bundled provider loader `KILO_BUNDLED_PROVIDERS` at `packages/opencode/src/kilocode/provider/provider.ts:42` and its `...KILO_BUNDLED_PROVIDERS` spread at `packages/opencode/src/provider/provider.ts:205` removed (P4.4-T6) — generic `BUNDLED_PROVIDERS` retained; kilo/apertis cache at `provider/model-cache.ts`/`models.ts` and handler `ModelCache` removed (P4.4-T7) — `failed: string[] = []` with `validProviders` guard without failedSet; CLI preset selector priority/hint/alias at `packages/opencode/src/cli/cmd/providers.ts` `ProvidersLoginCommand` (`buildProviderOptions` `providers.ts:236-257` / `matchProviderInput` `providers.ts:258-274`) and `cli/cmd/github.handler.ts` `githubInstall`/`promptProvider()` (`buildGithubProviderOptions` `github.handler.ts:156-166` / `promptProvider()` `github.handler.ts:240-250`) removed (P4.4-T8) — deterministic generic `sortBy((x) => x.name ?? x.id)` ordering; no public server endpoint or generated SDK/OpenAPI regeneration required (LOCK-009 bridge preserved; optional `metadata` field unchanged; `models-api.json` retained) | helper file absent and no production import remains; effective inactive: managed/cloud anchors are absent from `Config.Service`; CLI well-known provider discovery is absent from `cli/cmd/providers.ts`; preset provider display metadata enrichment is absent from the provider handler (`providerMetadata` import and call absent; listing is `Provider.toPublicInfo` only); preset `@kilocode/kilo-gateway` bundled loader is absent from `kilocode/provider/provider.ts` (`KILO_BUNDLED_PROVIDERS`/`createKilo`/`"@kilocode/kilo-gateway"` absent) and its spread is absent from `provider/provider.ts` (`...KILO_BUNDLED_PROVIDERS` absent; generic `BUNDLED_PROVIDERS` retained); kilo/apertis dynamic fetch is absent from `provider/model-cache.ts`/`models.ts`/`server/routes/instance/httpapi/handlers/provider.ts` (`ModelCache`/`fetchKiloModels`/`APERTIS`/`failedProviders` absent; `failed: string[] = []` with `validProviders` guard without failedSet); CLI preset selector ordering/hint/alias is absent from `cli/cmd/providers.ts` (`priority` map, `kilo`/`openai` hints, `codex` alias absent; ordering is `sortBy((x) => x.name ?? x.id)`) and `cli/cmd/github.handler.ts` (`priority`/`recommended` absent; ordering is `sortBy((x) => x.name ?? x.id)`); generic `ModelsDev.Service`/plugin/custom paths and HTTP/SSE bridge remain; Core `ModelsDev`/`models-api.json` retained (preset selector removal only); canonical tests prove managed values do not load; extension webview preset priority/catalog removed (P4.4-T20) — `PROVIDER_PRIORITY`/`providerOrderIndex`/`FALLBACK_PROVIDER_IDS`/`isPopularProvider`/`popularProviderIndex`/`providerOrderIndex`/`providerSortKey`/`PROVIDER_ORDER` absent from `provider-model.ts`/`provider-catalog.ts`/`model-selector-utils.ts`/`provider-tab-helpers.ts`/`ProviderSelectDialog.tsx`; `sortProviders` is `a.name.localeCompare(b.name) || a.id.localeCompare(b.id)` and `buildModelGroups` is `aLabel.localeCompare(bLabel) || aId.localeCompare(bId)` deterministic alphabetical; generic provider discovery and custom-provider lifecycle retained; still open/residual, P4.4 Active, no row/phase complete |
| Top-level `mode`/`tools` conversions | P4.4 (2026-08-26) P4.4-T9 | deleted: `sources.ts` diagnostic taxonomy removed (P4.4-T1b); handwritten SDK wrapper `mergeConfig` that preserved `mode`/plugin/config for child env deleted (P4.4-T9) — wrappers now retain only `...process.env` and `logLevel` arg | test: exact P4.3 absence assertions in `packages/opencode/test/kilocode/p4-3-cutover.test.ts:48-51`; `p4-4-sdk-config-forwarding-removal.test.ts` verifies no `mergeConfig`/`mode` forwarding residue in wrappers and `logLevel` passthrough preserved | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 10 | SDK/server-wrapper evidence (handwritten): wrappers now contain no `mergeConfig`/`mode` preservation for child env (verified); actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009) | residual: no effective top-level conversion in canonical config; handwritten wrapper forwarding deleted; typed assets are canonical | - | removed: handwritten SDK wrapper no longer merges `mode`/plugin/config via `KILO_CONFIG_CONTENT`; package path remains with `...process.env` | effective inactive: `result.mode`, `result.tools`, `loadMode` anchors absent from canonical config/agent paths; wrapper no longer forwards `mode` via env |
| Arbitrary CLI/env override layers | P4.4 (2026-08-26) P4.4-T9 | residual: sandbox safety policy at `packages/opencode/src/kilocode/sandbox/policy.ts` (retained; `KILO_CONFIG_CONTENT` deny preserved); handwritten SDK wrapper `KILO_CONFIG_CONTENT` env construction deleted (P4.4-T9) — wrappers now retain only `env: { ...process.env }` and `logLevel` arg | test: `p4-3-cutover.test.ts`; `p4-4-sdk-config-forwarding-removal.test.ts` verifies wrapper forwarding deleted and sandbox deny preserved; `p4-3-readiness-inventory.test.ts` | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 11 | SDK/server-wrapper evidence (handwritten): wrapper now forwards only `...process.env` without `KILO_CONFIG_CONTENT` (verified); actual generated surface: `packages/sdk/js/src/gen/sdk.gen.ts`, `types.gen.ts`; `packages/sdk/js/src/v2/gen/sdk.gen.ts`, `types.gen.ts` — no `KILO_CONFIG_CONTENT`; row-specific generated-surface inventory verified unchanged (LOCK-009) | residual: safety policy remains (environment deny `KILO_CONFIG_CONTENT`/`KILO_CONFIG`/`KILO_CONFIG_DIR` preserved, not an effective-config overlay); handwritten wrapper env forwarding deleted | - | removed: handwritten SDK wrapper `KILO_CONFIG_CONTENT` forwarding deleted; SDK/server bridge narrowing open with `...process.env` | effective inactive: no generic `Config.Service` override layer; sandbox denial remains as safety but is not an effective source read; diagnostic SDK wrapper forwarding deleted |
| Legacy global config filenames/readers | P4.4 (2026-08-27) P4.4 residual package | deleted: `sources.ts` filename inventory removed (P4.4-T1b); **TUI migration helper `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` physically deleted (2026-08-27 residual package — zero active callers since T27); `Flag.KILO_TUI_CONFIG` getter physically deleted from `packages/core/src/flag/flag.ts` (no production reader);** canonical TUI loader is `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only (LOCK-SOURCE/LOCK-R6) | test: `p4-3-cutover.test.ts:20-27,64-78` (canonical-only); `packages/opencode/test/kilocode/config/config.test.ts:610-667`; `p4-4-tui-migrate-kilo-config-removal.test.ts` + `p4-4-flag-legacy-getter-removal.test.ts` + `p4-4-tui-canonical-source-removal.test.ts` (helper file absent + Flag absent + canonical preserved) | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 12 (helper + getter deleted; row remains residual/open per LOCK-001) | - | deleted: TUI migration helper file and `Flag.KILO_TUI_CONFIG` getter deleted (2026-08-27); source inventory removed (P4.4-T1b); canonical stamp is `kilo.jsonc` only | - | residual: TUI/diagnostic dead helper removed; `ConfigPaths.files`/`theme.tsx` preserved per LOCK-003 (not TUI source) | effective inactive: legacy filenames absent from loader; TUI migration helper no longer exists on disk and `Flag.KILO_TUI_CONFIG` has no production reader (row remains residual/open per LOCK-001) |
| Legacy migration readers/import tooling | P4.4 (2026-08-27) P4.4 residual package | deleted: extension legacy-migration and Roo import source removed (pre-2026-08-25); **TUI migration helper `packages/opencode/src/cli/cmd/tui/config/tui-migrate.ts` physically deleted (2026-08-27 residual package — zero active callers since T27; `migrateTuiConfig`/`normalizeTui`/`TUI_SCHEMA_URL` removed); `Flag.KILO_TUI_CONFIG` getter physically deleted from `packages/core/src/flag/flag.ts` (no production reader);** canonical TUI loader is `resolveWorktree`/`canonicalRoot` + `ConfigPaths.fileInDirectory(root, "tui")` + `path.join(root, ".kilo")` only (LOCK-SOURCE/LOCK-R6) | test: `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts:420-444` (extension importer absent); `p4-3-readiness-inventory.test.ts:107-114`; `p4-4-tui-migrate-kilo-config-removal.test.ts` + `p4-4-flag-legacy-getter-removal.test.ts` + `p4-4-tui-canonical-source-removal.test.ts` (helper file absent + Flag absent + canonical preserved) | doc: `specs/vscode-orchestrator/evidence/p4.4-source-removal-evidence-matrix.md` matrix row 13 (helper + getter deleted; row remains residual/open per LOCK-001); `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md:264-266` | - | deleted: TUI migration helper file and `Flag.KILO_TUI_CONFIG` getter deleted (2026-08-27); extension importer/Roo already removed | residual: TUI migration dead surface removed; `ConfigPaths.files`/`theme.tsx` preserved per LOCK-003 (not TUI source) | extension importer/Roo wiring is removed and cannot read legacy data or write canonical config; CLI/TUI migration helper file is physically removed (not just dead) and `Flag.KILO_TUI_CONFIG` has no production reader — no active TUI migration import remains. Row remains open/residual per LOCK-001 because broader migration/import tooling surface and P4.5-adjacent TUI/theme residues remain; no P4.4 phase completion claimed |

### Legacy storage reader/writer removal

Old event-log and sync surfaces are removed with the old runtime surfaces
(ADR-0005 I-4/I-11; runtime spec section 8.2; storage spec sections 3.4, 5.5).
Evidence follows the same per-category rules as the product removals above; no
removal is claimed complete, and none is reclassified as deferred. The P4.2a
cutover stops legacy writes at runtime; P4.4/P4.5 record and verify the
removal of the legacy storage code and surfaces.

| Removal | Phase | Source | Tests | Docs | Generated SDK | Config | i18n | Build/package |
|---|---|---|---|---|---|---|---|---|
| Multi-client sync/warp replay protocol (`/sync/history`, `/sync/replay`, `/sync/steal`, live SSE sync replay, session warp) | P4.4/P4.5 | - | - | - | - | - | - | - |
| Old-peer capability negotiation / released-client storage compatibility (`/sync/checkpoint` rejected, never built) | P4.4/P4.5 | - | - | - | - | - | - | - |
| Unbounded full-payload event log as authoritative history (permanent duplicate full-payload snapshots; generic full-object update history) | P4.4/P4.5 | - | - | - | - | - | - | - |
| Legacy storage writers/readers (event-log full-snapshot writers, event-log read paths, legacy `session/`/`message/`/`part/` file prefixes) | P4.4/P4.5 | - | - | - | - | - | - | - |

### Reconstruction candidate registry

A reconstruction-candidate entry is created only when an implemented behavior is
actually disabled/removed or intentionally not carried forward during the
migration (LOCK-015; ADR-0004). It is evidence preservation — never a
compatibility promise, implementation backlog, phase gate, or removal
classification — and never blocks P1-P4 exits. Before P5 completion every
pending entry receives `rebuild` or `drop`; optional rebuild implementation is
separately scoped and does not automatically block P5 unless promoted to an H
invariant. An entry that maps to H-1..H-13 cannot remain dropped at final
target acceptance; it may be reimplemented from scratch after the foundation
stabilizes. The registry starts empty and gains rows only when implementation
displaces a behavior; RC-P3.1-001 below is the first entry (2026-08-15, P3.1).

| ID | Behavior / user value | Provenance | Architectural obstruction | Date / phase | Maps to final invariant | Post-foundation decision point | Disposition (`pending`, `rebuild`, `drop`) |
|---|---|---|---|---|---|---|---|---|
| RC-P3.1-001 | Sidebar-title button telemetry (`TITLE_BUTTON_CLICKED`): click attribution for the removed sidebar title buttons | `packages/kilo-vscode/src/services/telemetry/types.ts` — the event name was removed with the sidebar surface in the P3.1 removal (asserted absent by `test: packages/kilo-vscode/tests/unit/sidebar-removal.test.ts`); the event was emitted only by the removed sidebar title-button wrappers and now has no remaining emitter, so this is a clearly-obsolete discard recorded as evidence preservation | The emitting surface (sidebar title buttons) is permanently removed (LOCK-001); no surface remains that could emit the event | 2026-08-15 / P3.1 | No (not an H-1..H-13 invariant) | Post-foundation logs/telemetry keep-list decision (tracker section 7 post-reconstruction error maturity backlog); disposition decided here | drop |
| RC-P3.2-001 | Root-local one-click run script (setup/run script subsystem under `packages/kilo-vscode/src/agent-manager/run/`): the one-click convenience that scripted setup/run steps for a managed task | The `run/` subsystem (controller/manager/message/service/task) was implemented during P3.2 work and then removed with the managed worktree infrastructure in the final P3.2 implementation (asserted absent by `test: packages/kilo-vscode/tests/unit/worktree-removal.test.ts` — no `run/` controller subsystem); the script surfaced the managed run/version worktree flow, which is a permanent LOCK-002 removal | The run/ one-click script depended on the managed worktree lifecycle that LOCK-002 permanently removes; Agent Manager root-local start delegation is retained but carries no run-script subsystem | 2026-08-16 / P3.2 | No (not an H-1..H-13 invariant; setup/run scripts are not a LOCK-008 invariant) | Post-foundation: decide whether a root-local run convenience is rebuilt (separately scoped, never a removal classification); disposition decided here | drop |
| RC-P3.2-002 | Sibling-worktree `kilo_local_recall` family search/read: recalling sibling sessions across the git-worktree family | `packages/opencode/src/kilocode/session/recall-search.ts` and `packages/opencode/src/tool/recall.ts` — the worktree-family expansion (via `WorktreeFamily`/`Git`) was removed; root-local recall is retained (recall-search now filters only the single project ID; `recall.ts` no longer imports `WorktreeFamily`/`Git`), asserted by the P3.2 change (tracker section 7 worktree row) | The family expansion traversed the managed worktree family that LOCK-002 permanently removes; root-local recall over the shared workspace project remains | 2026-08-16 / P3.2 | No (not an H-1..H-13 invariant) | Post-foundation: decide whether cross-worktree recall is rebuilt; root-local recall decision point | drop |
| RC-P3.2-003 | Agent Manager overview/targeted-prompt bridge + list-prompt request-reply contract: the `agent_manager` tool's existing-session overview/targeted-prompt and the list/reply/reject request-reply surface | `packages/opencode/src/kilocode/agent-manager/service.ts`, `protocol.ts`, and the `agent_manager` tool (`packages/opencode/src/kilocode/tool/agent-manager.ts`) — the request-reply service and its `list`/`reply`/`reject` SDK methods were removed; start delegation is retained (the tool starts sessions only via `kilocode.agent_manager.start`), asserted by `test: packages/opencode/test/kilocode/agent-manager-absence.test.ts` | The list-prompt request-reply contract serviced the managed Agent Manager session orchestration that is permanently removed; Agent Manager start delegation remains and the extension routes directly to root-local sessions | 2026-08-16 / P3.2 | No (not an H-1..H-13 invariant; sub-task delegation retained via the H-2 delegation path) | Post-foundation: decide whether an extension-side overview/targeted-prompt bridge is rebuilt; disposition decided here | drop |
| RC-P4.4-001 | Legacy effective-config source-inventory diagnostics and `/config/sources`: the 12-source diagnostic listing reader (`KilocodeConfigSources.list`) that inventoried env, legacy filename, project, cloud, managed, and runtime config sources, and the config-console source-inventory endpoint that returned it in load order | `packages/opencode/src/kilocode/config/sources.ts` — physically deleted on 2026-08-25 (P4.4-T1b); the `sources` echo field was removed from the overlay response and the `/config/sources` schema/path/endpoint/handler were removed from the config-console group/handlers; the checked-in generated SDK and OpenAPI were regenerated and no `config.sources`/`ConfigSourcesResponse` contract remains. Asserted absent by `test: packages/opencode/test/kilocode/p4-4-source-inventory-removal.test.ts`; the removed endpoint's behavior tests were deleted with the endpoint (`packages/opencode/test/kilocode/server/config-sources.test.ts`) | The inventory described permanently removed source classes from the legacy pre-P4.3 multi-source reader (LOCK-010); after P4.3 the canonical global/project files/assets and schema defaults/secret refs are the sole effective-config authority, so the inventory had diagnosis-only value and no target authority | 2026-08-25 / P4.4 | No (not an H-1..H-13 invariant; effective-config authority is the P4.3 canonical set) | No rebuild: the diagnostic listing described removed source classes with no retained authority; the retained config-console bridge surfaces (`/config/overlay`, `/config/effective`, `/config/transaction`, rules, model-state, TUI config/keybinds) already express the canonical-scope model | drop |

Not reconstruction candidates (LOCK-015 — not displaced behaviors needing
evidence preservation): the inherent sidebar surface/view/title/context/assets
are part of the decided permanent removal (LOCK-001) and record their evidence
in the section 7 removal inventory, and the migrated command behavior (chat
commands that used the sidebar fallback now route through
`resolveChatTarget`/`ensureChatTab` to the Agent Manager or an editor tab) is
carried forward, not displaced. Likewise the inherent managed-worktree
creation/import/transfer/branch-naming/sections/multi-version/custom-diff
surfaces are part of the decided permanent removal (LOCK-002), record their
evidence in the section 7 removal inventory, and are not candidate rows; the
retained root-local start delegation, H paths, H-12, and generic
worktree/project/snapshot semantics are carried forward, not displaced.
Likewise the inherent cloud-sessions/JetBrains/Console/KiloClaw products are
part of the decided permanent removal (LOCK-003) and record their evidence in
the section 7 removal inventory, and the retained shared migration-bridge
surfaces (remote sessions, generic session import, the EventServiceClient/
presence bridge, the `config-console`/`experimental.console.*`/generated-SDK
Console group/core `consoleState`, and the current `kilo serve` HTTP/SSE/SDK
migration bridge) are carried forward, not displaced; the four products are
permanent inherent removals, never deferred and never reconstruction
candidates (LOCK-003/015), so no P3.3 candidate rows exist. Likewise the
inherent indexing/semantic-search, project memory/injection/tools,
user-visible context management/compaction (CLI/TUI/ACP/HTTP/SDK
summarize+compact), and autocomplete/next-edit/chat autocomplete/FIM/edit
surfaces are part of the decided permanent removal (LOCK-004) and record
their evidence in the section 7 removal inventory, and are not candidate
rows; the retained invisible automatic overflow safeguard (H-13, LOCK-005)
and generic search/recall are carried forward, not displaced. P3.4 adds no
new reconstruction-candidate rows: indexing UX, memory UX/tools, manual
compact/context UX, autocomplete/ghost text/next-edit, and commit-message UX
are inherent observable surfaces of the LOCK-004 closed-set permanent
removals; recording them as candidates would incorrectly reclassify permanent
removal scope. The four LOCK-004 products are permanent inherent removals,
never deferred and never reconstruction candidates (LOCK-004/015).

### Observed candidates (not yet displaced)

Known examples that are already confirmed likely to be touched by the new
foundation. They are NOT marked disabled today and create no gates; they become
actual registry rows only when implementation displaces them (LOCK-015;
ADR-0004):

- Partial-output auto-continuation — same-session, same-turn continuation of
  truncated responses
  (`packages/opencode/src/session/prompt/auto-continue.ts`, introduced by commit
  `6f6e9cb177a4b79c84e67349db8d4a0bc9015606`). Likely touched by the private
  runtime outcome/recovery foundation (runtime spec section 7.2) and by the
  auto-continue non-preservation rule (ADR-0004).
- Elapsed session timer display —
  `packages/kilo-vscode/src/agent-manager/session-timing.ts` and the
  WorkingIndicator component
  (`packages/kilo-vscode/webview-ui/src/components/shared/WorkingIndicator.tsx`).
  Likely touched by the runtime-owned outcome/status facts (runtime spec
  sections 7.1-7.2) and the Agent Manager surface migration.

### Post-reconstruction error maturity backlog

Separately planned after core reconstruction (2026-08-14 decision; runtime spec
section 7.3). These are forward-looking maturity items, NOT reconstruction
candidates: they are not displaced legacy behaviors, they never create registry
rows above, and they do not block P4/P5 unless separately promoted:

- Taxonomy refinement — a complete error taxonomy; no freeze at P4.2.
- Diagnostic retention/sanitizer hardening — e.g. the paused error-system
  branch's byte/depth sanitizer contract is not a P4.2 requirement.
- Logs/telemetry keep-lists — which fields diagnostics/logs/telemetry retain.
- Rich error actions/notification routing — a user-facing action catalog and
  notification UX are not P4.2 requirements.
- Domain-specific error integrations — per provider/session/tool/permission/
  worker/UI domain, reusing the foundation's normalization boundary and schema.

Each item is promoted to scoped work by a recorded decision after core
reconstruction; promotion does not reopen the P4.2 foundation scope (runtime
spec section 7.2).

## 8. Baseline / Complexity / Runtime Metrics

P0 establishes the baselines; later phases record measured deltas. Counts are
counts, not aspirations (direction spec section 11). `TBD` values are not
evidence.

| Metric | P0 baseline | Delta at P3 | Delta at P4 | Delta at P5 | Measurement source |
|---|---|---|---|---|---|
| Webview message types | 332 distinct `type` literals (Q3 approved 2026-08-12; inventory §4.4/§9) | −82 cumulative P3.1→P3.4 (2026-08-17): −58 at P3.2 (recount 274 = WebviewMessage 149 + ExtensionMessage 125) then −11 at P3.3 (recount 263 = WebviewMessage 144 + ExtensionMessage 119) then −13 at P3.4 (recount 250 = WebviewMessage 136 + ExtensionMessage 114 — context-management/compaction, indexing, memory, autocomplete message types retired; P3.1 contributed delta 0, 2026-08-15) | TBD | TBD | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` (§4.4 reproducible count); recount via `rg -o 'type: "[a-zA-Z0-9_.-]+"'` over `webview-ui/src/types/messages/webview-messages.ts` (136) + `extension-messages.ts` (114) |
| Provider methods | 250 v2 SDK public methods (Q3 approved 2026-08-12; inventory §4.3/§9) | −34 cumulative P3.1→P3.4 (2026-08-17): −11 at P3.2 (recount 239 = Worktree 7 + BranchName 1 + AgentManager list/reply/reject 3; P3.1 contributed delta 0, 2026-08-15) then −5 at P3.3 (recount 234 — cloud-session/KiloClaw endpoints retired) then −18 at P3.4 (recount 216 — context-management/compaction, indexing, memory, autocomplete SDK methods retired; regenerated twice idempotently) | TBD | TBD | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` (§4.4 reproducible count); recount via the inventory §10 `awk` over `sdk.gen.ts` (216) |
| Webview entry points | 6 esbuild webview entry points (Q3 approved 2026-08-12; inventory §4.4/§9) | −3 cumulative P3.1→P3.4 (2026-08-17): −2 at P3.2 (recount 4 — `diff-viewer` and `diff-virtual` bundles retired; P3.1 contributed delta 0, 2026-08-15) then −1 at P3.3 (recount 3 — the `kiloclaw` bundle retired); delta 0 at P3.4 (recount 3 — no esbuild entry change for the removed features) | TBD | TBD | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` (§4.4 reproducible count); current entries in `packages/kilo-vscode/esbuild.js` (agent-manager, marketplace, webview) |
| Config sources merged | 15 enumerated sources (12+ floor; runtime-active count still Unknown, inventory §6.1, U-2) | TBD | 0 legacy classes active (each of the 13 tracker section 7 removal rows, mirroring runtime spec section 8.1, proven inactive; counting method = one row per source class, with each of the 15 P0 enumerated sources mapped to a retained legal class or exactly one removal row) | Closed legal taxonomy only (runtime spec section 3.1: the two canonical authored scopes — one global config root and `<workspaceRoot>/.kilo/` — plus SecretStorage credentials and schema defaults; every section 8.1 row removed) | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` (§6.1); tracker §7 (effective-config source removal evidence) |
| Provider source kinds (catalog/config/auth/org) | Enumerated in inventory §6.2 (runtime-active count Unknown, U-2) | TBD | TBD | 1 (custom records) | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` (§6.2) |
| Convergence/cold-rebuild passes | TBD (machinery enumerated in inventory §6.3; per-save durations measured in the performance rows below — `commitToConvergedMs`/`burstToConvergedMs` from backend scenarios 11/12/13; pass count per save not separately counted, U-5) | TBD | TBD | 0 | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` (§6.3) |
| Startup readiness stages before UI enable | 10 enumerated stages (inventory §6.4; per-stage durations for the measured stages recorded in the performance rows below) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` (§6.4) |
| Removal items with full evidence | 0 (no removal complete; residue only, inventory §5) | 11 (P3.1, 2026-08-15: ordinary single-chat sidebar row complete; P3.2, 2026-08-16: worktree-infrastructure and custom-Diff-Viewer rows complete; P3.3, 2026-08-16: cloud-sessions, JetBrains, Console, and KiloClaw rows complete; P3.4, 2026-08-17: indexing, project memory, user-visible context management/compaction, and autocomplete rows complete) | TBD | TBD | doc: `specs/vscode-orchestrator/p0-current-state-inventory.md` (§5); tracker §7 |

 The `Delta at P3` column above reflects **P3.1 + P3.2 + P3.3 + P3.4**
(2026-08-15/2026-08-16/2026-08-17):
the P3.1 sidebar removal shared the editor-tab/Agent Manager webview bundle and
made no generated-SDK contract change, so at P3.1 message types, provider
methods, and webview entry points recounted at their P0 baseline (332/250/6,
each delta 0) — a zero-delta boundary explanation, not incomplete removal.
P3.2 (2026-08-16) records the worktree/diff/agent-manager-run reductions: webview
message types 332→274 (−58; WebviewMessage 149 + ExtensionMessage 125), generated
v2 SDK public methods 250→239 (−11: Worktree 7 + BranchName 1 + AgentManager
list/reply/reject 3), and webview esbuild entries 6→4 (−2: `diff-viewer` and
`diff-virtual` retired). P3.3 (2026-08-16) records the cloud-session/KiloClaw/
Console reductions: webview message types 274→263 (−11: WebviewMessage 144 +
ExtensionMessage 119), generated v2 SDK public methods 239→234 (−5:
cloud-session/KiloClaw endpoints), and webview esbuild entries 4→3 (−1: the
`kiloclaw` bundle retired). P3.4 (2026-08-17) records the
indexing/memory/context-management/autocomplete reductions: webview message types
263→250 (−13: WebviewMessage 136 + ExtensionMessage 114), generated v2 SDK
public methods 234→216 (−18: context-management/compaction, indexing, memory,
autocomplete SDK methods), and webview esbuild entries 3→3 (delta 0). Count
provenance is
recorded above: the current webview message-type recount (136 + 114) and the
current SDK method recount (216) and the current webview entries (3:
agent-manager, marketplace, webview) are reproducible via the inventory §4.4/§10
commands and match the evidence in this tracker.

Counts above derive from the inventory and are reproducible (inventory
section 10 commands). Q3 is resolved (2026-08-12): the approved definitions are
the distinct webview message `type:` literals (332 = WebviewMessage 189 +
ExtensionMessage 143, disjoint sets), the 250 generated v2 SDK `KiloClient`
public methods imported by the extension (`@kilocode/sdk/v2/client`), and the 6
HTML/webview esbuild entry points excluding the shiki worker asset. The
decision is recorded in section 9 and in the inventory (section 9); current
counts stay in the inventory (sections 4.4/9) and are reused unchanged for all
later delta comparisons.

Repeated baseline campaigns (2026-08-12): six formal campaigns are accepted as
durable objective evidence under `specs/vscode-orchestrator/evidence/p0-baseline/`:
(1) Extension Host cold-start (`2026-08-11T11-51-16-883Z/benchmark.jsonl`;
fresh VS Code profile + fresh scratch XDG, no seeded config, agents 0 /
providers 0 / mcp null; 1 warmup + 5 measured, status ok, scoped
memory-guard/backend-identity evidence) — a fresh no-seed cold start; (2)
Extension Host many-agent-MCP
(`many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl` +
`merge-manifest.json`; seeded scratch XDG kilo.json with 20 real custom agents
+ one real local stdio MCP server fixture; exactly 1 warmup + 5 measured,
status ok, `baselineComplete: true`, per-segment sha256/hash record mapping,
environment drift limited to `backendCli` temp paths with one shared
`cliSnapshotSha256`); (3) historical backend in-process `Server.listen`/
`AppLayer` (`2026-08-11T04-46-19-926Z/backend.jsonl`, git 461781ffc0; scenarios
6 first-prompt, 7 large-stream, 8 parallel-sessions, 9 permission-heavy, 11
hot-config, 12 cold-during-generation, 13 burst-cold; each 1 warmup + 5
measured, 42/42 ok, 79 n=5 summaries, status ok) — retained as historical
evidence for the 2026-08-11 CLI process tier only, superseded for current-tier
comparison by campaign (5); (4) Extension Host warm-view/no-provider/
custom-provider (`2026-08-12T05-52-07-672Z/benchmark.jsonl`, git 7a768502dd,
gitDirty true, status ok, 19 samples, 27 n=5 summaries): warm-view (2 warmup +
5 measured, one lifecycle with a shared backend worker staying live across
panel close/reopen cycles, no seeded config), seeded-empty persisted
no-provider (1 warmup + 5 measured; seeded scratch XDG kilo.json with explicit
empty provider/agent records — a persisted providerless state, distinct from
the fresh no-seed condition of campaign (1)), and custom-provider (1 warmup + 5
measured; one real custom provider, `@ai-sdk/openai-compatible`, loopback
baseURL, 1 model); and (5) current-tier backend in-process `Server.listen`/
`AppLayer` (`2026-08-12T06-08-27-792Z/backend.jsonl`, git 7a768502dd, gitDirty
true, status ok; scenarios 6,7,8,9,11,12,13, each 1 warmup + 5 measured, 42/42
ok, 79 n=5 summaries) — the current CLI-side in-process tier for later-phase
comparison; and (6) Extension Host session-switch
(`2026-08-12T07-21-22-201Z/benchmark.jsonl`, git 7a768502dd, gitDirty true, run
status ok; one lifecycle, 5 seeded deterministic sessions, real Playwright
tab-strip clicks, settled when the active tab id and header title match; 1
warmup + 5 measured, all ok, one n=5 summary, memory-guard/backend-identity
evidence per sample). The backend campaigns are CLI-side harness evidence only — they run
the production `Server.listen`/`AppLayer` HTTP/SSE path in process
(`serve_cli_entry` is CLI-process-tier and not emitted) and are neither
target-surface nor private-worker evidence. Two pre-fix partial artifacts are
retained but never cited as baseline:
`2026-08-12T04-30-39-069Z/benchmark.jsonl` (no run-finish record, no
custom-provider samples; pre-harness-fix) and
`2026-08-12T06-05-12-356Z/backend.jsonl` (no run-finish record, scenarios
6/7/8 only; environmental first attempt). Earlier single-run smoke records,
incomplete partial runs, and stale `/tmp` JSONL runs are not cited as baseline.
All recorded values below are descriptive n=5 sample statistics (nearest-rank
p95 equals max at n=5) with explicit scope; they are NOT SLA or threshold
claims (R7 resolved 2026-08-14: no numeric performance thresholds, section 9).

Performance metrics (LOCK-PERF). P0 records the baseline with evidence links; no
performance metric is proven until then (LOCK-PERF-6). Rows below change from
`Not proven` to `Measured` only where a formal campaign above objectively
supports the metric with explicit scope. Rows that remain `Not proven` at P0 are
re-scoped to later-phase evidence, not P0 blockers: persisted-selector paint
gates at P5 (no extension-owned persisted indexes exist before P4.1; P5 evidence
per runtime spec section 6 criteria 1/5/6 — descriptive paint-before-ready
timing plus a worker-unavailable Extension Host scenario); cost
attribution and per-event transport/webview flush are descriptive evidence at
P2 (harness-parity/streaming), not an independent P2 exit blocker;
removed-feature initialization absence and affected-path deltas gate at
P3/P4.4 (removal). No numeric performance threshold exists (R7 resolved
2026-08-14, section 9).
P2 (2026-08-15) recorded the affected-path comparison on harness-parity flows:
descriptive backend scenario 6/7/9 n=5 summaries
(`doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T06-18-28-167Z/backend.jsonl`,
git f91d22ae1f, dirty) recorded against the P0-baseline values below (drift: git
7a768502dd→f91d22ae1f, both dirty) — prompt submit, add/update streaming, and
tool/permission rows below carry both values; target-surface SSE handling and
webview lifecycle paint were recorded from the final durable campaign's
`real-completed/run.log` (KILO_P0_PERF=1). Per R7 all values are descriptive
n=5 (or n=1/n=2 single-run) statistics — no threshold, no requirement to
improve, and no improvement/regression is claimed. Two measurement gaps are
recorded explicitly as non-independent P2 blockers (runtime spec section
10.11): per-event webview render-flush was not emitted (only extension-side SSE
handling spans exist) and model HTTP duration was not separately measured.

P3.1 (2026-08-15) recorded the affected-path comparison on the removal
subphase's measured path: a descriptive Extension Host scenario-1 (cold-start,
fresh no-seed) campaign
(`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`,
git 67436bdf70, gitDirty true, VS Code 1.133.0, recorded CLI snapshot
sha256/size, 1 warmup + 5 measured, run status ok) recorded against the P0
cold-start baseline (`2026-08-11T11-51-16-883Z/benchmark.jsonl`, git
461781ffc0, VS Code 1.132.0) — the affected rows below carry both values. Key
medians/p95=max ms (P3.1): activate 24/32, `spawnToPortMs` 9407/13684,
`connectToSseConnectedMs` 9535/13844, `activateToDataReadyMs` 16772/21748,
`webviewLoadToPaintMs` 1175/1215, `backendEntryToListenerMs` 1330/2524 (P0
cold-start medians: activate 34, `spawnToPortMs` 5382, `connectToSseConnectedMs`
5442, `activateToDataReadyMs` 9231, `webviewLoadToPaintMs` 1291,
`backendEntryToListenerMs` 644). Drift is recorded: VS Code 1.132.0→1.133.0,
git 461781ffc0→67436bdf70 both dirty, CLI snapshot changed (sha256
`11636703...`→`6a8c6d5d...`). Activation/render metrics moved negative and
backend/transport metrics moved positive, but no causal conclusion is drawn —
per R7 all values are descriptive n=5 statistics with explicit scope: no
threshold, no requirement to improve, and no improvement/regression claim.
Structural absence of the removed sidebar is proved statically and at runtime
separately from these measurements (`test: packages/kilo-vscode/tests/unit/sidebar-removal.test.ts`
+ the focused real Extension Host `test:e2e:sidebar-removal` scenario), per
LOCK-PERF-3. Only the affected scenario-1 rows were rerun per the per-phase
rule; all other P3 delta cells stay `TBD` (P3.3-P3.4 pending).

P3.2 (2026-08-16) recorded the affected-path comparison on the removal
subphase's measured path: a descriptive Extension Host scenario-1 (cold-start,
fresh no-seed) campaign
(`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`,
git 9d86428cc2, gitDirty true, VS Code 1.133.0, extension 7.4.11, recorded final
CLI snapshot sha256 `924cb638...`, 1 warmup + 5 measured, 13 n=5 summaries, run
status ok) recorded against the P0 cold-start baseline
(`2026-08-11T11-51-16-883Z/benchmark.jsonl`, git 461781ffc0, VS Code 1.132.0) and
alongside the P3.1 campaign (`p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`,
git 67436bdf70) — the affected rows below carry the P0/P3.1/P3.2 value sets. Key
P3.2 medians/p95=max ms: activate 26/28, `spawnToPortMs` 5332/6607,
`connectToSseConnectedMs` 5380/6682, `activateToDataReadyMs` 8805/12225,
`webviewLoadToPaintMs` 1025/1052, `backendEntryToListenerMs` 481/880,
`backendConfigLoadMs` 476/775, `backendProviderStateInitMs` 251/365,
`backendInstanceBootstrapMs` 541/853. Drift is recorded: VS Code 1.132.0→1.133.0
(P0→P3.1/P3.2, P3.1/P3.2 share VS Code 1.133.0), git 461781ffc0→67436bdf70→
9d86428cc2, all dirty, and the CLI snapshot changed across campaigns. No causal
conclusion is drawn and no direction-of-change claim is made — per R7 all values
are descriptive n=5 statistics with explicit scope (LOCK-PERF-6/R7): no
threshold, no requirement to improve, and no improvement/regression claim.
Structural absence of the removed managed-worktree and custom-Diff-Viewer
features is proved statically (`test: packages/kilo-vscode/tests/unit/worktree-removal.test.ts`,
`test: packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts`) and at
runtime (`test:e2e:worktree-removal`) separately from these measurements, per
LOCK-PERF-3. Only the affected scenario-1 rows were rerun per the per-phase
rule; all other P3 delta cells stay `TBD` (P3.3-P3.4 pending).

P3.3 (2026-08-16) recorded the affected-path comparison on the removal
subphase's measured path: a descriptive Extension Host scenario-1 (cold-start,
fresh no-seed) campaign
(`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`,
git HEAD `2d61c228e71d6b67db15c2a7ff2be9e728e775e4`, gitDirty true, VS Code
1.133.0, extension 7.4.11, recorded final CLI snapshot sha256 prefix
`229cad7ae3…e5a3b` (snapshot/source size 156821474), 1 warmup + 5 measured,
6/6 samples ok, finish status ok, 13 n=5 summaries) recorded against the P0
cold-start baseline (`2026-08-11T11-51-16-883Z/benchmark.jsonl`, git
461781ffc0, VS Code 1.132.0) and alongside the P3.1/P3.2 campaigns
(`p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`, git 67436bdf70;
`p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`, git 9d86428cc2) — the
affected rows below carry the P0/P3.1/P3.2/P3.3 value sets. Key P3.3
medians/p95=max ms (n=5, min/median/p95=max/mean): activate 24/26/27/25.8,
`spawnToPortMs` 4823/5217/5500/5130.8, `connectToSseConnectedMs`
4887/5305/5564/5198, `sseConnectToConnectedMs` 54/61/85/64.4,
`activateToDataReadyMs` 8357/9096/9439/9063.6, `dataReadySpanMs`
2690/2810/3787/3054, `webviewLoadToPaintMs` 1030/1040/1056/1042.2,
`loadToDataReadyMs` 6897/7539/7976/7557.6, `backendListenerMs` 424/496/525/485.4,
`backendEntryToListenerMs` 551/621/654/609.8, `backendConfigLoadMs`
525/569/639/574.6, `backendProviderStateInitMs` 262/334/388/334.2,
`backendInstanceBootstrapMs` 562/614/764/636.4. Drift is recorded: VS Code
1.132.0→1.133.0 (P0→P3.1/P3.2/P3.3, the P3 campaigns share VS Code 1.133.0),
git 461781ffc0→67436bdf70→9d86428cc2→2d61c228e7, all dirty, and the CLI
snapshot changed across campaigns. No causal conclusion is drawn and no
direction-of-change claim is made — per R7 all values are descriptive n=5
statistics with explicit scope (LOCK-PERF-6/R7): no threshold, no requirement
to improve, and no improvement/regression claim. Structural absence of the
removed cloud-session/JetBrains/Console/KiloClaw features is proved statically
(`test: packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts`, `test:
packages/opencode/test/kilocode/cloud-claw-removal.test.ts`, `test:
packages/opencode/test/kilocode/console-removed.test.ts`, `test:
packages/script/tests/jetbrains-removed.test.ts`) and at runtime
(`test:e2e:cloud-claw-removal`) separately from these measurements, per
LOCK-PERF-3. Only the affected scenario-1 rows were rerun per the per-phase
rule; all other P3 delta cells stay `TBD` (P3.4 pending).

P3.4 (2026-08-17) recorded the affected-path comparison on the removal
subphase's measured path: an accepted native Extension Host scenario-1
(cold-start, fresh no-seed) campaign
(`doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl`,
artifact sha256 `288cfcfd8b08514f9356a15a138de213bc604a35dcc12641cec87cf01a7567e7` (63,134 bytes); native CLI snapshot sha256 `2945ac79748d9241afb10ef6caf897fa6b3237809ec824e8adbb1927ec231c2a`, 151,207,394 bytes; exactly 1 warmup + 5 measured, 6/6 samples ok, finish status
ok, 13 n=5 summaries) recorded against the P0 cold-start baseline
(`2026-08-11T11-51-16-883Z/benchmark.jsonl`, git 461781ffc0, VS Code 1.132.0)
and alongside the P3.1/P3.2/P3.3 campaigns — the affected rows below carry the
P0/P3.1/P3.2/P3.3/P3.4 value sets. Key P3.4 medians ms (n=5):
`activateToDataReadyMs` 9400, `spawnToPortMs` 5658, `connectToSseConnectedMs`
5718, `webviewLoadToPaintMs` 1244, `backendEntryToListenerMs` 609,
`backendConfigLoadMs` 597, `backendProviderStateInitMs` 322,
`backendInstanceBootstrapMs` 652. Descriptive only; environment/git drift; no
performance improvement/regression claim (R7; LOCK-PERF-6). Structural absence
of the removed indexing/memory/context-management/autocomplete features is
proved statically (`test: packages/opencode/test/kilocode/p3-4-removal-absence.test.ts`,
`test: packages/kilo-vscode/tests/unit/p3-4-removal.test.ts`) and at runtime
(`test:e2e:p3-4-removal` — forbidden manifest/runtime/bundle/state hits empty,
0 model requests) separately from these measurements, per LOCK-PERF-3. CLI build
correction: stale indexing-worker entry removed; native build passes; absence
test pins it. The excluded partial wrapper campaign
`p3-4-final-2026-08-17T00-57-00-000Z/EXCLUDED` (316-byte wrapper, 4/5 measured
blocked, non-comparable) is explicitly excluded and never cited as an accepted
benchmark. P3 delta cells are now fully recorded for all subphases; no further
`TBD` P3 delta cells remain.

| Metric | P0 baseline | Delta at P3 | Delta at P4 | Delta at P5 | Measurement source |
|---|---|---|---|---|---|
| Worker cold start (extension spawn -> port detected) | Measured (n=5, descriptive p95=max; scope: Extension Host campaigns, git 461781ffc0 for cold-start/many-agent-MCP, git 7a768502dd for warm-view/no-provider/custom-provider): `spawnToPortMs` med 5382 / p95 6652 ms (cold-start, fresh no-seed); med 6331 / p95 7581 ms (many-agent-MCP, 20 agents + 1 MCP); med 11409 / p95 17305 ms (seeded-empty persisted no-provider); med 5898 / p95 6330 ms (custom-provider, 1 provider) — not an SLA or threshold (R7 resolved 2026-08-14) | P3.1 descriptive (2026-08-15, scenario 1 cold-start fresh no-seed, git 67436bdf70 dirty, VS Code 1.133.0, 1 warmup + 5 measured, status ok): `spawnToPortMs` med 9407 / p95=max 13684 ms. P3.2 descriptive (2026-08-16, scenario 1 cold-start fresh no-seed, git 9d86428cc2 dirty, VS Code 1.133.0, extension 7.4.11, 1 warmup + 5 measured, 13 n=5 summaries, status ok): `spawnToPortMs` med 5332 / p95=max 6607 ms. P3.3 descriptive (2026-08-16, scenario 1 cold-start fresh no-seed, git 2d61c228e7 dirty, VS Code 1.133.0, extension 7.4.11, CLI snapshot sha256 `229cad7ae3…e5a3b`, 1 warmup + 5 measured, 6/6 ok, 13 n=5 summaries, status ok): `spawnToPortMs` med 5217 / p95=max 5500 ms. P3.4 accepted native (2026-08-17, scenario 1 cold-start fresh no-seed, 1 warmup + 5 measured, 6/6 ok, 13 n=5 summaries, finish status ok): `spawnToPortMs` med 5658 ms — descriptive n=5 only, no threshold, no improvement/regression claim (R7; LOCK-PERF-6) | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-11T11-51-16-883Z/benchmark.jsonl`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T05-52-07-672Z/benchmark.jsonl`; P3.1: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`; P3.2: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`; P3.3: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`; P3.4: doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl` |
| Worker module graph + AppLayer graph construction | Measured (n=5, descriptive p95=max; scope: `backendEntryToListenerMs` = backend process entry -> HTTP listener, includes module graph + AppLayer graph construction, CLI-side tier of the Extension Host campaigns, git 461781ffc0 cold-start/many-agent-MCP, git 7a768502dd no-provider/custom-provider): med 644 / p95 700 ms (cold-start); med 500 / p95 641 ms (many-agent-MCP); med 1336 / p95 1444 ms (seeded-empty persisted no-provider); med 725 / p95 736 ms (custom-provider) — not an SLA or threshold (R7 resolved 2026-08-14) | P3.1 descriptive (2026-08-15, scenario 1 cold-start): `backendEntryToListenerMs` med 1330 / p95=max 2524 ms. P3.2 descriptive (2026-08-16, scenario 1 cold-start): `backendEntryToListenerMs` med 481 / p95=max 880 ms. P3.3 descriptive (2026-08-16, scenario 1 cold-start): `backendEntryToListenerMs` med 621 / p95=max 654 ms. P3.4 accepted native (2026-08-17, scenario 1 cold-start): `backendEntryToListenerMs` med 609 ms — descriptive n=5 only, no threshold, no improvement/regression claim (R7; LOCK-PERF-6) | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-11T11-51-16-883Z/benchmark.jsonl`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T05-52-07-672Z/benchmark.jsonl`; P3.1: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`; P3.2: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`; P3.3: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`; P3.4: doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl` |
| Server listening -> SSE connected | Measured (n=5, descriptive p95=max; scope: Extension Host campaigns, git 461781ffc0 cold-start/many-agent-MCP, git 7a768502dd no-provider/custom-provider): `connectToSseConnectedMs` med 5442 / p95 6706 ms (cold-start); med 6380 / p95 7642 ms (many-agent-MCP); med 11586 / p95 17479 ms (seeded-empty persisted no-provider); med 5956 / p95 6430 ms (custom-provider) — not an SLA or threshold (R7 resolved 2026-08-14) | P3.1 descriptive (2026-08-15, scenario 1 cold-start): `connectToSseConnectedMs` med 9535 / p95=max 13844 ms. P3.2 descriptive (2026-08-16, scenario 1 cold-start): `connectToSseConnectedMs` med 5380 / p95=max 6682 ms. P3.3 descriptive (2026-08-16, scenario 1 cold-start): `connectToSseConnectedMs` med 5305 / p95=max 5564 ms. P3.4 accepted native (2026-08-17, scenario 1 cold-start): `connectToSseConnectedMs` med 5718 ms — descriptive n=5 only, no threshold, no improvement/regression claim (R7; LOCK-PERF-6) | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-11T11-51-16-883Z/benchmark.jsonl`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T05-52-07-672Z/benchmark.jsonl`; P3.1: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`; P3.2: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`; P3.3: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`; P3.4: doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl` |
| UI fetch chain -> `extensionDataReady` (current global gate) | Measured (n=5, descriptive p95=max; scope: harness data-ready signal `activateToDataReadyMs` / `loadToDataReadyMs` in the Extension Host campaigns, git 461781ffc0 cold-start/many-agent-MCP, git 7a768502dd no-provider/custom-provider; exact `extensionDataReady` gate identity not separately isolated): med 9231 / p95 10189 ms and med 7539 / p95 8534 ms (cold-start); med 9810 / p95 15051 ms and med 8144 / p95 13098 ms (many-agent-MCP); med 23043 / p95 26167 ms and med 19702 / p95 21787 ms (seeded-empty persisted no-provider); med 10517 / p95 11924 ms and med 8563 / p95 10148 ms (custom-provider) — not an SLA or threshold (R7 resolved 2026-08-14) | P3.1 descriptive (2026-08-15, scenario 1 cold-start): `activateToDataReadyMs` med 16772 / p95=max 21748 ms. P3.2 descriptive (2026-08-16, scenario 1 cold-start): `activateToDataReadyMs` med 8805 / p95=max 12225 ms. P3.3 descriptive (2026-08-16, scenario 1 cold-start): `activateToDataReadyMs` med 9096 / p95=max 9439 ms. P3.4 accepted native (2026-08-17, scenario 1 cold-start): `activateToDataReadyMs` med 9400 ms — descriptive n=5 only, no threshold, no improvement/regression claim (R7; LOCK-PERF-6) | TBD | Removed (action-specific gates) | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-11T11-51-16-883Z/benchmark.jsonl`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl`; doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T05-52-07-672Z/benchmark.jsonl`; P3.1: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`; P3.2: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`; P3.3: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`; P3.4: doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl` |
| Persisted-selector paint -> worker ready | Not proven — P5 gate (no extension-owned persisted indexes exist before P4.1; cold-start campaign has no persisted selectors by design; `webviewLoadToPaintMs`/`dataReadySpanMs` recorded but not mapped to this span) | TBD | TBD | Gate — runtime spec section 6 criteria 1/5/6: descriptive paint-before-ready timing plus a worker-unavailable Extension Host scenario | P0 performance instrumentation (LOCK-PERF-4); P5 startup acceptance (runtime spec section 6) |
| Warm view (extension reopen with live worker; webview restore path) | Measured (n=5, descriptive p95=max; scope: Extension Host warm-view scenario, one lifecycle with a shared backend worker staying live across panel close/reopen cycles, no seeded config, git 7a768502dd): `webviewLoadToPaintMs` med 119 / p95 120 ms — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T05-52-07-672Z/benchmark.jsonl` |
| No-provider startup (fresh, no seeded config; startup without provider/auth records) | Measured (n=5, descriptive p95=max; scope: cold-start campaign condition = fresh VS Code profile + fresh scratch XDG, no seeded config, providers 0, git 461781ffc0): `spawnToPortMs` med 5382 / p95 6652 ms; `activateToDataReadyMs` med 9231 / p95 10189 ms — not an SLA or threshold (R7 resolved 2026-08-14) | P3.1 descriptive (2026-08-15, scenario 1 cold-start, same fresh no-seed condition, git 67436bdf70 dirty, VS Code 1.133.0): `spawnToPortMs` med 9407 / p95=max 13684 ms; `activateToDataReadyMs` med 16772 / p95=max 21748 ms. P3.2 descriptive (2026-08-16, scenario 1 cold-start, same fresh no-seed condition, git 9d86428cc2 dirty, VS Code 1.133.0): `spawnToPortMs` med 5332 / p95=max 6607 ms; `activateToDataReadyMs` med 8805 / p95=max 12225 ms. P3.3 descriptive (2026-08-16, scenario 1 cold-start, same fresh no-seed condition, git 2d61c228e7 dirty, VS Code 1.133.0): `spawnToPortMs` med 5217 / p95=max 5500 ms; `activateToDataReadyMs` med 9096 / p95=max 9439 ms. P3.4 accepted native (2026-08-17, scenario 1 cold-start, same fresh no-seed condition): `spawnToPortMs` med 5658 ms; `activateToDataReadyMs` med 9400 ms — descriptive n=5 only, no threshold, no improvement/regression claim (R7; LOCK-PERF-6) | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-11T11-51-16-883Z/benchmark.jsonl`; P3.1: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`; P3.2: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`; P3.3: doc: `specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`; P3.4: doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl` |
| No-provider startup (seeded-empty persisted providerless state) | Measured (n=5, descriptive p95=max; scope: warm-view/no-provider/custom-provider campaign no-provider scenario = seeded scratch XDG kilo.json with explicit empty provider/agent records, persisted providerless state, providers 0, git 7a768502dd): `spawnToPortMs` med 11409 / p95 17305 ms; `connectToSseConnectedMs` med 11586 / p95 17479 ms; `activateToDataReadyMs` med 23043 / p95 26167 ms; `loadToDataReadyMs` med 19702 / p95 21787 ms; `backendEntryToListenerMs` med 1336 / p95 1444 ms; `webviewLoadToPaintMs` med 1326 / p95 1368 ms; `backendConfigLoadMs` med 2034 / p95 2397 ms; `backendProviderStateInitMs` med 1349 / p95 2414 ms; `backendInstanceBootstrapMs` med 2238 / p95 2734 ms — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T05-52-07-672Z/benchmark.jsonl` |
| Custom-provider startup (one or more user-defined provider records; provider state build) | Measured (n=5, descriptive p95=max; scope: warm-view/no-provider/custom-provider campaign custom-provider scenario = seeded scratch XDG kilo.json with one real custom provider (`@ai-sdk/openai-compatible`, loopback baseURL, 1 model), git 7a768502dd): `spawnToPortMs` med 5898 / p95 6330 ms; `connectToSseConnectedMs` med 5956 / p95 6430 ms; `activateToDataReadyMs` med 10517 / p95 11924 ms; `loadToDataReadyMs` med 8563 / p95 10148 ms; `backendEntryToListenerMs` med 725 / p95 736 ms; `webviewLoadToPaintMs` med 1287 / p95 1313 ms; `backendConfigLoadMs` med 755 / p95 854 ms; `backendProviderStateInitMs` med 462 / p95 671 ms; `backendInstanceBootstrapMs` med 803 / p95 924 ms — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T05-52-07-672Z/benchmark.jsonl` |
| Many-agent/MCP startup (large agent set and several MCP servers; bootstrap and tool resolution) | Measured (n=5, descriptive p95=max; scope: many-agent-MCP campaign = 20 real custom agents + one real local stdio MCP server (not several), seeded scratch XDG, git 461781ffc0, baselineComplete): `mcpConnectMs` med 1585 / p95 2092 ms; `activateToDataReadyMs` med 9810 / p95 15051 ms; `spawnToPortMs` med 6331 / p95 7581 ms — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl` + `merge-manifest.json` |
| Prompt submit -> first model event/token | P0 baseline (n=5, descriptive p95=max; backend scenario 6 first-prompt, CLI-side in-process `Server.listen`/`AppLayer`, test-model/test-provider, git 7a768502dd; the 2026-08-11 campaign (git 461781ffc0) remains historical tier evidence only): `startToFirstDeltaMs` med 2067 / p95 2166 ms; `totalMs` med 2628 / p95 2691 ms. P2 descriptive rerun (2026-08-15, scenario 6, git f91d22ae1f, dirty; drift: git 7a768502dd→f91d22ae1f both dirty): `startToFirstDeltaMs` min 1117 / median 1211 / p95=max 1436 / mean 1253.8 ms; `firstDeltaToIdleMs` min 315 / median 357 / p95=max 437 / mean 362.2 ms; `totalMs` min 1432 / median 1610 / p95=max 1796 / mean 1616 ms. Target-surface record (2026-08-15, real-completed run, `[P0-Perf]` `prompt.submit`→`model.firstEvent` matched via parentID): n=8 min 84 / median 145 / max 1150 ms (nearest-rank p95 165; the 1150 ms outlier is the H-2 delegation turn including the delegated subagent round-trip) — descriptive n=5 and single-run only, no threshold, no improvement/regression claim (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` (P0 baseline); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T06-18-28-167Z/backend.jsonl` (P2 rerun); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/run.log` (target-surface) |
| Add/update streaming (large stream, backend scenario 7) | P0 baseline (n=5, descriptive p95=max; CLI-side in-process harness, git 7a768502dd): `totalMs` med 3076 / p95 4601 ms; `firstDeltaToLastDeltaMs` med 1 / p95 39 ms; `firstDeltaToIdleMs` med 603 / p95 687 ms; `streamBytesPerMs` med 65 / p95 86; `deltaEvents` 50/50. P2 descriptive rerun (2026-08-15, scenario 7, git f91d22ae1f, dirty; drift: git 7a768502dd→f91d22ae1f both dirty): `totalMs` min 1474 / median 1565 / p95=max 1732 / mean 1580.8 ms; `firstDeltaToLastDeltaMs` min 0 / median 1 / p95=max 1 / mean 0.6 ms; `firstDeltaToIdleMs` min 283 / median 361 / p95=max 385 / mean 349.6 ms; `streamBytesPerMs` min 115 / median 128 / p95=max 136 / mean 126.8; `deltaEvents` 50/50 — descriptive n=5 only, no threshold, no improvement/regression claim (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` (P0 baseline); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T06-18-28-167Z/backend.jsonl` (P2 rerun) |
| Model/network/tool/user-approval vs transport/event overhead | P2 descriptive evidence recorded (2026-08-15) — no independent P2 exit blocker (LOCK-PERF-7 attribution; runtime spec section 10.11): model HTTP duration was NOT separately measured (the scripted model records request url+body only, no per-request wire duration) — recorded explicitly as a non-independent P2 blocker; extension-side SSE handling span records (target-surface campaign `real-completed/run.log`, `[P0-Perf]` `stage=sse.event` end spans) n=655 min 0 / median 0 / max 1 ms (sum 140 ms) — localhost transport/event handling is small relative to the tool/approval spans below, but attribution remains descriptive; backend tool/permission spans in the same run.log: `tool_execute` n=7 min 6 / median 142 / max 2846 ms (max = H-2 task-delegation wait), `permission_wait` 1821 ms (n=1), `question_wait` 560 ms (n=1) — descriptive single-run, no threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/run.log` (target-surface campaign); P0 performance instrumentation (LOCK-PERF-7 attribution) |
| Per-event transport handling + webview render flush | P2 descriptive evidence recorded (2026-08-15) — no independent P2 exit blocker (runtime spec section 10.11): extension-side per-event SSE handling measured in the target-surface campaign (`real-completed/run.log`, `stage=sse.event` end spans) n=655 span records min 0 / median 0 / max 1 ms, incl. `message.part.updated` 121 + `message.part.updated.1` 121 (instrument `.1`-suffixed second span records with distinct timestamps — the count is of span records, not necessarily distinct SSE events), min 0 / med 0 / max 1 ms; per-event webview render-flush was NOT emitted by the current instrumentation (only extension-side SSE handling spans exist) — recorded explicitly as a non-independent P2 blocker; webview lifecycle paint (wd = ms from webview module load, n=2 initial + H-7 reopened document): `webview.load` 210/194, `webview.mount` 247/227, `webview.render` 1273/1156, `webview.paint` 1361/1245 — descriptive single-run records, no threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/real-completed/run.log` (target-surface campaign); P0 performance instrumentation |
| Tool/permission round-trip | P0 baseline (n=5, descriptive p95=max; backend scenario 9 permission-heavy, CLI-side in-process harness, test permission/tool flows, git 7a768502dd; the 2026-08-11 campaign (git 461781ffc0) remains historical tier evidence only): `startToFirstAskMs` med 407 / p95 501 ms; `reply1ToAsk2Ms` med 1332 / p95 1759 ms; `secondReplyToIdleMs` med 1614 / p95 2225 ms. P2 descriptive rerun (2026-08-15, scenario 9, git f91d22ae1f, dirty; drift: git 7a768502dd→f91d22ae1f both dirty): `startToFirstAskMs` min 212 / median 241 / p95 590 / max 590 / mean 307 ms; `reply1ToAsk2Ms` min 720 / median 824 / p95 1761 / max 1761 / mean 976.4 ms; `secondReplyToIdleMs` min 882 / median 1100 / p95 1333 / max 1333 / mean 1089.2 ms; counts per sample: asks 2, replies 2, bashExecutions 2, tool_execute 2, permission_wait 2 — descriptive n=5 only, no threshold, no improvement/regression claim (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` (P0 baseline); doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T06-18-28-167Z/backend.jsonl` (P2 rerun) |
| Session switch | Measured (n=5, descriptive p95=max; scope: Extension Host session-switch scenario, one lifecycle, 5 seeded deterministic sessions, real Playwright tab-strip clicks, settled when the active tab id and header title match, git 7a768502dd): `switchSettleMs` med 195 / p95=max 282 ms (min 115, max 282, mean 201.6) — descriptive n=5 only, not an SLA and no P1 navigation parity claim (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T07-21-22-201Z/benchmark.jsonl` |
| Parallel child sessions (concurrent generation with parent-child delegation) | Measured (n=5, descriptive p95=max; scope: backend scenario 8 parallel-sessions, 4 concurrent sessions, CLI-side in-process harness, current-tier rerun git 7a768502dd; parent-child hierarchy not separately attributed; the 2026-08-11 campaign (git 461781ffc0) remains historical tier evidence only): `totalMs` med 5578 / p95 5892 ms; `parallelToAcceptMs` med 452 / p95 581 ms; `acceptToAllIdleMs` med 5139 / p95 5440 ms — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` |
| Cold config commit -> convergence complete | Measured (n=5, descriptive p95=max; scope: backend scenario 12 cold-during-generation + 13 burst-cold, CLI-side in-process harness, current-tier rerun git 7a768502dd; the 2026-08-11 campaign (git 461781ffc0) remains historical tier evidence only): `commitToConvergedMs` med 1125 / p95 1833 ms (12); `burstToConvergedMs` med 1086 / p95 1259 ms (13) — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | 0 passes | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` |
| Hot config update (persist + cache invalidation + `config-updated` without rebuild) | Measured (n=5, descriptive p95=max; scope: backend scenario 11 hot-config, CLI-side in-process harness, current-tier rerun git 7a768502dd; the 2026-08-11 campaign (git 461781ffc0) remains historical tier evidence only): `hotPatchMs` med 126 / p95 154 ms; `heldToReleaseMs` med 2845 / p95 2991 ms; `releaseToIdleMs` med 775 / p95 2264 ms — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` |
| Cold config update during active generation (no interruption) | Measured (n=5, descriptive p95=max; scope: backend scenario 12 cold-during-generation, CLI-side in-process harness, current-tier rerun git 7a768502dd; the 2026-08-11 campaign (git 461781ffc0) remains historical tier evidence only): `patchMs` med 501 / p95 659 ms; `commitToConvergedMs` med 1125 / p95 1833 ms; `convergedToFollowUpMs` med 1379 / p95 1461 ms — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` |
| Burst config updates (coalescing of overlapping cold saves) | Measured (n=5, descriptive p95=max; scope: backend scenario 13 burst-cold, CLI-side in-process harness, current-tier rerun git 7a768502dd; the 2026-08-11 campaign (git 461781ffc0) remains historical tier evidence only): `burstMs` med 1408 / p95 1769 ms; `burstToConvergedMs` med 1086 / p95 1259 ms; `convergedToAllModelsMs` med 4533 / p95 5246 ms — not an SLA or threshold (R7 resolved 2026-08-14) | TBD | TBD | TBD | doc: `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl` |
| Startup-loaded services / removed-feature initialization count | Not proven — P3/P4.4 gate (backend boot-stage timing metrics `backendConfigLoadMs`/`backendProviderStateInitMs`/`backendInstanceBootstrapMs` are now recorded for the seeded-empty no-provider and custom-provider conditions — see those rows — and backend counts `config_load`/`instance_bootstrap`/`provider_state_init`/`processor_entry` exist per scenario; structural absence of removed-feature initialization remains to be proven per removal subphase) | TBD | Gate | TBD | P0 static inventory + runtime measurement (LOCK-PERF-3); P3/P4.4 removal gates |
| Startup-work delta vs baseline | Not proven — P3/P4.4 gate (descriptive, affected-path, same-environment deltas; zero or positive noisy delta is allowed if no removed work remains and no structurally unbounded growth/resource leak appears; no numeric threshold — R7 resolved 2026-08-14, section 9) | TBD | Gate | TBD | P0 baseline comparison (measured rows above); P3/P4.4 removal gates |

Estimates such as 20-40% or 30-50% startup reduction are hypotheses only and must
not be used as acceptance claims (LOCK-PERF-6). There are no numeric pass/fail
performance thresholds (R7 resolved 2026-08-14, section 9): P1/P2 compare
descriptive, affected-path, same-environment measurements; P3/P4 prove
structural absence of removed work and record affected-path deltas; only the
affected measured rows are rerun per phase. All measured values in the rows
above are descriptive n=5 sample statistics (nearest-rank p95 equals max at
n=5) with explicit scope; they are NOT SLA or threshold claims.

Storage metrics (ADR-0005; storage spec section 2). Current values are the
2026-08-14 read-only aggregate measurement of the live `kilo.db` (metadata/
length-sum queries only; no content was read). Post-cutover values are recorded
at P4.2a against the canonical storage foundation; reclaimed-byte figures are
descriptive maintenance diagnostics, not numeric gates (R15).

| Metric | Current baseline (2026-08-14) | Delta at P4.2a (post-cutover) | Measurement source |
|---|---|---|---|
| Legacy DB size (`kilo.db`) | ~24.56 GB (2.29M events; 2026-08-07 historical: ~16.9 GB, 1,597,938 events) | Fresh canonical DB; legacy DB archived offline with integrity evidence (R17) | doc: `specs/storage/session-storage-rewriting.md` (§2.2) |
| Event payload | ~17.96 GB (~99.2% full message/part update snapshots) | Canonical model: no permanent duplicate full-payload snapshots (LOCK-016) | doc: `specs/storage/session-storage-rewriting.md` (§2.2) |
| message/part payload | ~4 GB | Retained as canonical aggregates (R16) | doc: `specs/storage/session-storage-rewriting.md` (§2.2) |
| Growth rate | ~7.5 GB/7 days (derived: 24.56 GB on 2026-08-14 minus 16.9 GB on 2026-08-07 ≈ 7.7 GB over seven days); ~1 MB/min from a separate short active sample, not a sustained rate | Bounded by automatic retention (R15) | doc: `specs/storage/session-storage-rewriting.md` (§2.2) |
| Disk usage | ~91-92% | Monitored; automatic retention responds to the byte budget (R15) | doc: `specs/storage/session-storage-rewriting.md` (§2.2) |
| Rows/bytes reclaimed by automatic retention | n/a (not implemented) | Recorded descriptively as diagnostics (R15) | Tracker risk log; storage spec §5.3 |

## 9. Decisions / Open Questions

Product questions mirror the direction spec (section 14); runtime bounded
implementation decisions mirror the runtime spec (section 9). `TBD` means
undecided; decisions are returned to the hub, never invented here. Product
removals themselves are decided (ADR-0002 LOCK-001/002/003/004/006 plus the
non-preservation clause of LOCK-005 for the existing compaction implementation)
and are not open questions.
Resolved rows (Q1 resolved 2026-08-14; Q3, R1, R2, R5, R6, R8 resolved
2026-08-12; R7 resolved 2026-08-14; Q2/Q4/R4 resolved 2026-08-15) record the
durable
decision in this tracker and in the owning artifact (inventory section 9 for
Q3; runtime spec section 9 for R1/R2/R5/R6/R7/R8 and R4; direction spec
section 14 for Q1/Q2/Q4); they are closed and no longer
gate P0, and Q1's resolution closes the P1 exit-checklist dependency on open
question 1. Q2/Q4/R4 (sidebar deprecation/removal timing and adoption
thresholds) were resolved 2026-08-15: P1/P2 supplied the migration
affordances, P3.1 removes the sidebar now, and no numeric adoption threshold
applies; the release note is the minor `remove-sidebar-chat.md` changeset,
which is not modified here. On 2026-08-13 R2 and R6 were revised by a post-P0 user clarification —
file-authoritative hybrid configuration and an atomic legacy-reader cutover
instead of dual-read; the revised texts below are current, the original
2026-08-12 wording is preserved as historical evidence in this section and the
P0 checklist, and P0 stays Complete with its recorded evidence
unchanged. R9 (observation/hydration implementation details) is open as of
2026-08-13 (see row); required by P4.2 only, it is not a P1-P3 blocker and
does not gate P0 (Complete). R10 (canonical schema/field-registry layout and exact persistence
assignment) is open as of 2026-08-13 (see row), bounded within the
now-resolved file/SecretStorage topology; required by P4.1, it does not
gate P0 (Complete), and P4.1 cannot exit until the registry/schema/provenance
contract is evidenced. R11-R14 (Failure/Outcome/Recovery bounds: operation/
outcome identity, record location, and retention; minimum private-runtime
Failure/Outcome schema, normalization boundary, field tiers, runtime redaction,
cancellation provenance, and versioned panel envelope; recovery
accounting/coordination; worker-crash in-flight disposition) are open as of
2026-08-14 (see rows); all required by P4.2, they
do not gate P0 (Complete) or P1. R15-R17 (storage bounds: automatic retention
values; canonical aggregate schema/revision + changefeed disposition + artifact
registry; offline archive/cutover procedure) are resolved 2026-08-20
(see rows; ADR-0005); required by P4.2 (P4.2a storage sub-boundary), they do
not gate P0 (Complete) or P1-P3. Post-reconstruction error maturity (runtime
spec section 7.3) is tracked separately in section 7 and is not a gate.

| # | Question | Decision | Owner | Required by | Status |
|---|---|---|---|---|---|
| 1 | Final meaning of 'topic': derived navigation label, session-metadata facet, or new persisted grouping model? | Resolved (2026-08-14): in P1 Topic is a derived navigation concept — each root session defines one Topic; root session ID is stable Topic identity; root title is the label; descendants belong through parentID; activity is the max member updatedAt; order descending with deterministic ID tie-break; orphan/missing-parent/cycle components degrade to independent Topics; Topic selection/expansion is presentation state only; no independent Topic persistence/API/config/schema/metadata. Recorded in the direction spec (section 14) | Hub (decision) | P1 | Resolved |
| 2 | When does the sidebar deprecation notice ship relative to P1 navigation? | Resolved (2026-08-15): P1/P2 supplied the migration affordances; P3.1 removes the sidebar now; the deprecation step shipped as the P1/P2 migration surface plus the release note (`doc: .changeset/remove-sidebar-chat.md`, minor, not modified here); no separate deprecation notice remains | Hub | P3.1 | Resolved |
| 3 | What exact counts form the P0 complexity baseline (message types, provider methods, webview entry points)? | Resolved (2026-08-12): 332 distinct webview message `type:` literals — WebviewMessage 189 + ExtensionMessage 143, disjoint sets; 250 generated v2 SDK `KiloClient` public methods, because the extension imports `@kilocode/sdk/v2/client`; 6 HTML/webview esbuild entries, excluding the shiki worker asset. Reproducible commands in the inventory (section 10); current counts in the inventory (sections 4.4/9) and tracker section 8; reuse those commands for all later deltas | Hub (decision); inventory (evidence) | P0 | Resolved |
| 4 | Sidebar removal timing/order within P3.1 and adoption thresholds | Resolved (2026-08-15): P1/P2 supplied migration affordances and P3.1 removes the sidebar now; no numeric adoption threshold | Hub | P3.1 | Resolved |
| 5 | Which consolidated configuration surface replaces settings/profile/marketplace panels, and how does it present custom provider records? | TBD — presentation choices only; cannot gate selector availability (runtime spec section 6 criteria 1-2) | Hub | P3/P4.1 | Open |
| R1 | Private transport protocol | Resolved (2026-08-12): JSON-RPC 2.0 over child-process stdio with standard Content-Length framing (`vscode-jsonrpc` precedent). One extension-owned worker child; initialize handshake replaces port detection/health; requests carry commands, notifications carry normalized event envelopes; stderr remains bounded diagnostics; EOF/process exit owns lifecycle. HTTP/SSE/generated SDK remains bridge-only and is deleted. No retained-terminal protocol commitment: terminal/worktree surfaces are not LOCK-008 harness invariants and are handled by their removal/migration scope | Implementation | P4.2 | Resolved |
| R2 | Storage engine for extension-owned state | Resolved (2026-08-12; **revised 2026-08-13**): canonical files/assets own effective config — one global config root and `<workspaceRoot>/.kilo/` (runtime spec section 3.1); VS Code `globalState`/`workspaceState` own only UI-local/derived state (layout/churn, dismissed state, derived selector/read-model indexes); all secrets use `SecretStorage`; runtime-owned session/event/artifact persistence remains runtime-owned; immutable worker snapshots are derived versioned values — not a second persisted store — identified by canonical file content + schema version + opaque secret references, never by UI state. Original 2026-08-12 wording (product/UI config + selector indexes in `globalState`) preserved as historical evidence | Implementation | P4.1 | Resolved (revised 2026-08-13) |
| R3 | Numeric startup SLA | Bounded product decision. The behavioral and structural P5 gates apply regardless (runtime spec section 6 criteria 1-4, 6); this row decides only whether numeric thresholds are added on top | Hub | P5 | Open |
| R4 | Adoption thresholds for removal timing | Resolved (2026-08-15): no numeric adoption threshold applies to removal timing; removal proceeds per the decided phase order (P3.1..P3.4), each subphase preceded by the migration affordances already supplied (P1/P2) and recording its own exit evidence | Hub | P3 | Resolved |
| R5 | Exact project harness-assets path | Resolved (2026-08-12): one canonical project boundary = first VS Code workspace root; project-versioned harness assets live only under `<workspaceRoot>/.kilo/`, including `.kilo/kilo.json[c]`, agent/command/rules/skills/workflows/plans/config assets. P4.1 establishes the canonical project files and field registry; the ancestor walk, `.kilocode`/`.opencode`, global project-asset sources, and primary-worktree mirror reads are deleted together at the P4.3 cutover, with per-row removal evidence recorded at P4.4. No multi-source precedence remains; no migration/import tool exists | Implementation | P4.1 | Resolved |
| R6 | Legacy-reader cutover (formerly: dual-read window deadline) | Resolved (2026-08-12; **revised 2026-08-13**): there is no dual-read compatibility window and no import tool. P4.3 is the last legacy-reader phase boundary: the current implementation may use old sources only before the cutover; at the P4.3 boundary all legacy readers are deleted together and cannot influence effective config. The sole user manually reconciles any desired current configuration into canonical files before the cutover. Original 2026-08-12 decision (dual-read opens only during P4.3, shrinks monotonically, no new bridge consumers, fully closed before P4.3 exits / P4.4 begins) preserved as historical evidence | Implementation | P4.3 | Resolved (revised 2026-08-13) |
| R7 | Performance gate thresholds (startup stages, prompt-submit/first-token, stream-render, tool/permission, session-switch, config-update, removal reduction) | Resolved (2026-08-14): gates use no numeric pass/fail thresholds. P1/P2 compare descriptive, affected-path, same-environment measurements against the P0 baseline — record med/p95/sample/provenance and investigate obvious structural anomalies; measurement noise alone does not block and there is no requirement to improve. P3/P4 removal phases prove removed-feature initialization/readers/listeners/resources are absent and record affected startup/session-switch/memory/worker-lifecycle deltas; zero or positive noisy delta is allowed if no removed work remains and no structurally unbounded growth/resource leak appears; only affected measured rows are rerun per phase. Complexity budgets record deltas; zero reduction in a dimension is allowed with a stated phase-boundary reason; permanent-removal completeness remains required. `Same environment/comparable` means same benchmark scripts/scenario, machine/OS class, VS Code profile type, seeded/provider conditions, instrumentation mode, and recorded git SHA/dirty/environment drift; non-comparable runs are recorded but cannot support gate claims. Numeric product SLA is R3, resolved separately at P5. No invented numerics (LOCK-PERF-6) | Hub/Engineering | Resolved (2026-08-14); no threshold-using gate remains | Resolved |
| R8 | Benchmark tooling/harness choice | Resolved (2026-08-12): retain the existing two-harness tooling as the P0 and later comparison harness — Extension Host scenarios 1/2/3/4/5/10 under `packages/kilo-vscode/script/p0-bench/` + runner/merge/safety/provenance tools; backend scenarios 6/7/8/9/11/12/13 under `packages/opencode/test/benchmark/` + runner. Limitations recorded: manual-only, platform/environment/provenance scoped, backend in-process `Server.listen`/`AppLayer` only, n=5 descriptive | Implementation | P0 | Resolved |
| R9 | Observation/hydration implementation details (snapshot/event handshake; revision scope/ordering/idempotency; ephemeral-fact retention) | Resolved 2026-08-23 — bounded implementation decision under runtime spec 7.1 proven by real Extension Host five-boundary production observation convergence (panel, reload, switch, reconnect, restart) via private Database.layerNoLease derived observer (resolveCanonicalDbPath/resolveCanonicalDataDir, KILO_DISABLE_CHANNEL_DB=1, absolute gate, no second lease; legacy kilo serve sole lease owner (canonical no-lease observer / legacy lease-owner invariant)), PrivateWorkerHost/PrivateObservationService enabled:true, Memento/InMemory cursor persistence ack-only, singleflight debounced lifecycle triggers ~150ms (panel/window/config/session/peer-closed) via PrivateObservationLifecycleTriggers, reconnect bounded exact-PID shutdown(2000)+doInitialize, no polling. Evidence secret-safe durable `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-23T13-59-07Z-r9-observation/` (manifest.redacted.json + provenance.json, source manifest d2b3b8ecc9572ea4010585a64bacda1e06fdb21a6c7cd8ec7f647de4541e3f26, schema kilo-e2e-evidence/1, scenario r9-observation, success:true, validated:true, status:complete, 25 files, run.log omitted per minimal evidence policy, redacted provenance, secret scan no fixture-key) from audited source `/var/folders/9v/733tfwtj2jvgwygmtrbwj9gm0000gn/T/kilo/r9-evidence-1755961000-2082` (audit PASS 0 blocker) with r9-observation-runtime-evidence showing panel 1→1 continuity true rehydrate false, reload 1→1, switch 1→1 with switchConfirmation e2e-probe-d3cbd796-B, reconnect 1→2 with observation/changed seq2, restart 2→5 rehydrate:true seq3-5 evict cap, r9-dom-evidence persistedCursor 3 final pid 83485, canonical-gate zeroState 0 archive before/after identical 9f86…, cstate materializationReady true, plan consistent, protocol envelope v1.0 payload-free seq/session_id/revision/kind/time gap→rehydrate; no polling/unbounded queue/new store; HTTP/SSE/SDK bridge preserved per LOCK-009 until P4.4/P4.5 | Implementation | P4.2 | Resolved (2026-08-23) |
| R10 | Canonical schema/field-registry layout and exact persistence assignment | Resolved (2026-08-18): the closed 11-field JSONC registry (`packages/kilo-vscode/src/config/registry.ts`) covers the exact `CanonicalConfigPayload` field classes (model, model_variant, model_variant_overrides, subagent_model, subagent_variant, subagent_variant_overrides, default_agent, provider, mcp, permission, instructions). Typed asset classes (agent, command, skill, tool, plugin, rules) exist as typed markdown files in their canonical directories (`packages/kilo-vscode/src/config/types.ts`, `ASSET_DIRECTORIES`) — they are separately registered canonical asset classes, never top-level JSONC fields. Global root: `~/.config/kilo/kilo.jsonc` resolved from platform home via `Roots` class (`packages/kilo-vscode/src/config/paths.ts`, injectable for tests). Project root: `<workspaceRoot>/.kilo/kilo.jsonc`. Canonical project scope exists only when a first VS Code workspace folder is open; with no workspace, the service operates global-only and rejects project-scope writes and asset operations. Each registry entry carries complete scope, operator, owner, persistence, secret handling, snapshot inclusion, provenance, and removal disposition. Validation is enforced by the separate closed `fieldSchemas` mapping and contextual validators (`packages/kilo-vscode/src/config/validate.ts`), not as per-entry registry metadata. SecretStorage credentials use opaque refs only (`secret:kilo.credentials.<scope>.<kind>.<id>`); plaintext never in files. Evidence: `packages/kilo-vscode/src/config/registry.ts` (closed 11-field set `CLOSED_JSONC_FIELDS`), `packages/kilo-vscode/src/config/types.ts` (`ASSET_DIRECTORIES`, `CanonicalConfigPayload`, `CanonicalProviderPayload`, `CanonicalMcpPayload`, `RegistryEntry`), `packages/kilo-vscode/src/config/paths.ts` (`Roots`, `resolveCanonicalPaths`), `packages/kilo-vscode/src/config/validate.ts` (`fieldSchemas`), `packages/kilo-vscode/src/config/secret-adapter.ts` (owned-ref parser) | Implementation | P4.1 | Resolved (2026-08-18) |
| R11 | Operation/outcome identity and record location/retention | Bounded implementation decision under the normative Failure/Outcome/Recovery target (runtime spec section 7.2) and the observation contract (section 7.1): what an accepted semantic operation's identity is, where the canonical Failure/Outcome records live, and their minimal retention under existing storage — no new store mandate. Generation-path operations only (prompt/generation, provider attempt, tool call, permission/question wait, child/background task); config commit outcomes are owned by runtime spec sections 5/5.4 and are not R11 operations. It does not reopen runtime sole authority for operational facts or the client replay prohibition Resolved 2026-08-23 with additive canonical persistence foundation: new session-scoped aggregate table `session_operation` (`session_id REFERENCES session(id) ON DELETE CASCADE`, `CHECK` for `op_kind`/`outcome`, indexes on session/kind/time, `revision` inspectable, not a file artifact/new DB/retry ledger/changefeed history) via `packages/core/src/database/migration/20260824000000_add_operation_record.ts` + `packages/core/src/database/migration.gen.ts` and `packages/core/src/session/sql.ts` `SessionOperationTable`; small canonical API `packages/core/src/session/operation.ts` validates R12-compatible nine-field shape, provides stable constructors `prompt:msgId` / `provider:assistantId:attempt` (nonnegative int) / `tool:assistantId:callId` / `permission:requestId` / `task:childSessionId(:parentCallId)?` with colon-free validation and `parseOpId` cross-kind rejection, persists `select(record,"persist")` (full already-redacted record) with `op_kind`/`outcome`/`code`/`message`/`time`/`cancel`/`detail`/`stack`/`revision` inspectable columns, writes/upserts idempotently in one `BEGIN IMMEDIATE` transaction atomically advancing `SessionRevision` and emitting one payload-free `changed` feed row (existing `changed | deleted` kinds/caps unchanged), reads deterministically (`get`/`list` ordered by `op_id`), rejects cross-kind/cross-session/cross-identity, terminal→`in-flight` regression, terminal→terminal non-identical, and `in-flight`→`in-flight` non-identical conflicts without advancing revision/feed using narrowest R11 transition semantics (only `in-flight`→terminal forward allowed; R13 recovery accounting and R14 crash disposition deferred). Retention via existing 8/6 GiB family budget, seven-day/active/leased protections, and session cascade; no separate policy. Tests `packages/core/test/operation-r11.test.ts` (14 pass: migration/identity stability/validation, redacted full-record round trip, atomic row+revision+changed feed, idempotent replay and rejected conflict no double revision/feed, terminal cannot regress, payload-free `changed` notification, family cascade/retention integration) + existing `changefeed-s4`/`retention-s2`/`session-revision` still pass; `bun run typecheck` pass (core/opencode/kilo-vscode); no new ledger/store/retry, no production session/permission wiring, no PrivateWorkerHost wiring, no R13/R14/full R9 wiring; architecture docs `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md` updated (Persistence + Operation/Outcome record R11 + source map). R13 recovery accounting, R14 crash disposition, and full R9 five-boundary production observation convergence remain Open | Implementation | P4.2 | Resolved (2026-08-23) |
| R12 | Minimum private-runtime Failure/Outcome schema + panel projection/redaction | Open — bounded implementation decision under runtime spec section 7.2: the minimum Failure/Outcome schema, the runtime-owned normalization boundary, minimal field tiers (durable vs diagnostic vs panel-visible), runtime-side redaction before persistence/projection, cancellation provenance (at least user stop/steering/timeout/network disconnect/unknown), and a versioned private panel envelope/projection. No complete taxonomy and no byte/depth sanitizer contract freeze. The private error envelope's version/compatibility is owned by R1/R9/R12 as appropriate Resolved 2026-08-23 with the additive private-runtime failure module and dual-side tests (runtime spec section 9; changelog 2026-08-23; tests packages/opencode/test/private-worker/failure.test.ts + packages/kilo-vscode/tests/unit/failure.test.ts) | Implementation | P4.2 | Resolved (2026-08-23) |
| R13 | Recovery accounting/coordination and low-level retry visibility | Bounded implementation decision under runtime spec section 7.2 — P4.2 is accounting/coordination only: owner/scope, budget consumed/termination, next-at occurrence time, provenance, and visibility of nested low-level attempts within the owning operation. It may reuse current bounded behavior. Retryability algorithms, delay shapes, and future recovery features are post-foundation (runtime spec section 7.3). It does not preserve old retry shapes, delay ladders, the environment retry flag, or the auto-continue implementation Resolved 2026-08-23 as implementation decision — additive pure private-runtime recovery accounting module `packages/opencode/src/private-worker/recovery.ts` ↔ `packages/kilo-vscode/src/private-worker/recovery.ts` (semantic parity): versioned 1.0; closed sets owner `runtime`, scope `operation`, provenance `runtime,user,offline-restore`, layer `semantic,sdk,provider,transport,task`, termination `budget-exhausted,non-retryable,replay-unsafe,cancelled,completed`; immutable `RecoveryRecord`/`RecoveryAttempt` with R11 five-form opId/opKind validation locally; `create`/`charge`/`terminate`/`snapshot` (derived projection only, no second store); `charge` idempotent on exact replay, rejects conflicting same id, enforces budget before accept (consumed exactly once, nested visible, shared), optional `nextAt` >= time and cleared on termination, explicit replay safety (`nextAt` requires `replaySafe === true`, unsafe → `replay-unsafe` termination clears `nextAt`); narrowest `limit=0` (no charge, consumed 0, explicit `budget-exhausted` termination) and limit-reached (last charge succeeds, next fails `budget exhausted`, no overrun); runtime occurrence times, no clock/timer, no algorithm/delay constants, no DB/migration/Map, no notification, no R11/R12 change, no wiring; tests `packages/opencode/test/private-worker/recovery.test.ts` + `packages/kilo-vscode/tests/unit/recovery.test.ts` (32+32 tests, 202+202 assertions proving closed schema/parity, five kinds, immutable/idempotent, nested/shared, provenance, occurrence-vs-nextAt, unsafe replay, budget/no overrun, explicit termination, no timer/constants, derived projection); implementation evidence only (32+32 tests, 202+202 assertions, typecheck, lint, knip, semantic parity), independent re-audit 2026-08-23 ACCEPT, 0 blockers, one evidence-count style correction applied; R14/full R9 remain Open | Implementation | P4.2 | Resolved (2026-08-23) |
| R14 | Worker-crash in-flight disposition | Resolved 2026-08-23 — additive pure private-runtime module `packages/opencode/src/private-worker/disposition.ts` ↔ `packages/kilo-vscode/src/private-worker/disposition.ts` (semantic parity): versioned 1.0 worker-crash disposition; closed outcome `succeeded,failed,ambiguous,in-flight,superseded,abandoned` and provenance `worker-crash`; explicit cleanup `released` and occurrence/receipt times (receipt >= occurrence, distinct caller-supplied); `replayed:false` no silent replay; R11 five-form identity `prompt:msgId`/`provider:assistantId:attempt`/`tool:assistantId:callId`/`permission:requestId`/`task:childSessionId(:parentCallId)?` with local `parseOpId` cross-kind rejection; `create` (in-flight) + `apply` (crash disposition) with `validateRecord`/`isTerminal`; same crash event replay idempotent (exact facts return same), conflicting same-op facts reject, only `in-flight`→terminal allowed, terminal exact replay allowed, cross-kind/cross-identity/regressive reject; immutable frozen records; pure no DB/migration/Map/service/timer/queue/notification/retry/production import or wiring, no store, no clock; tests `packages/opencode/test/private-worker/disposition.test.ts` + `packages/kilo-vscode/tests/unit/disposition.test.ts` (25+25 tests) prove five-forms/closed sets/crash provenance/cleanup/occurrence vs receipt/no silent replay/idempotency/conflicting/terminal forward/terminal replay/cross-kind/cross-identity/regressive/static purity/immutability; does not claim P4.2 complete, R9 Resolved (2026-08-23), R11/R12/R13 remain Resolved, P4.2 remains Active | Implementation | P4.2 | Resolved (2026-08-23) |
| R15 | Automatic retention bounds (byte-budget high/low watermarks, recent-retention floor, family eligibility/ordering, diagnostics) | Resolved (2026-08-20): Budget scope is physical bytes of active canonical DB main file + WAL and registered session-family artifacts; excludes offline archives, logs/cache, project-owned snapshot storage. High 8 GiB, low 6 GiB — resource bound not performance SLA, 25% hysteresis; values change only via later recorded architecture decision, never user config. Protect every root family whose max runtime-owned activity across root+descendants is within 7 days; additionally requires all members terminal/idle and no active/in-flight or maintenance/read lease; protections revalidated in deletion transaction. Order eligible roots by activity asc then root ID. Maintenance coalesced after boot/commits, runs only off generation hot path when idle and high exceeded, deletes complete families until at/below low or none eligible. Canonical DB deletion is one immediate transaction; filesystem cleanup uses durable idempotent cleanup obligation; never partial transcript truncation. Fresh DB uses incremental auto-vacuum; after pruning checkpoint/reclaim incrementally off hot path; WAL-inclusive accounting; if floor/protections prevent low, stop safely and emit pressure diagnostics. Per-run diagnostics: trigger, before/after bytes, selected/deleted/skipped counts with reasons, rows/artifact bytes reclaimed, checkpoint/vacuum result, failures; no setting/pin/dashboard/manual cleanup/UI. Rationale: 8/6 separates normal operation from ~24.56 GiB legacy emergency; 7 days preserves recent continuity while bounding growth | Implementation | P4.2 (P4.2a storage sub-boundary). Not a P1-P3 blocker | Resolved (2026-08-20) |
| R16 | Canonical aggregate schema/revision model + bounded outbox/changefeed disposition + artifact ownership/retention registry | Resolved (2026-08-20): Canonical durable truth is normalized session/message/part/todo/share and registered operation/outcome/failure aggregates as R11 fields land, plus registered artifacts. `SessionTable.revision` is per-session monotonic revision; each semantic mutation and its revision commit in one immediate transaction. Legacy `event`/`event_sequence` not required for reconstruction, remains removal scope. Deletion obtains final revision=current+1 and atomically writes payload-free delete tombstone to changefeed before hard-deleting aggregate in same transaction; feed rows have no FK cascade to session. Bounded payload-free changefeed with global monotonic sequence, session ID, session revision, kind, runtime-owned occurrence time; uniqueness/idempotency is `(session_id, revision, kind)`; derived, never reconstruction authority; snapshot hydration establishes sequence cursor; acknowledged rows truncatable; hard cap both 50,000 rows and 64 MiB — exceeding either may evict oldest even if unacknowledged; cursor gap forces full rehydration; wire handshake remains R9/P4.2b. Closed registry: canonical DB aggregate rows (SessionShare/todo/message/part/context and future outcome/failure rows) are schema-owned cascade/transaction governed, not file artifacts; `session_diff`, `session_diff_base`, `session_share` are session-family-owned file artifacts retained/deleted with family; `snapshot` is project-owned collected only by project reachability/refcount, never family pruning; `session-export.db` is legacy cutover material under R17, not canonical artifact. New artifact writes require registry entry before landing. Sequencing: R16 registry available to S2; S2 materializes/consumes for deletion; S3 closes/audits verifying every writer/class registered, snapshot ownership correct, zero unregistered; thus S2→S3 preserved. Rationale: payload-free bounded deltas avoid recreating full-object growth while allowing reconnect efficiency; hard-cap gaps safe because snapshot hydration is authoritative | Implementation | P4.2 (P4.2a storage sub-boundary). Not a P1-P3 blocker | Resolved (2026-08-20) |
| R17 | Offline archive format/location/integrity + fresh-DB cutover identity + rollback/archive-deletion authority | Resolved (2026-08-20): Cutover archive is offline versioned dir under same-filesystem sibling derived from `Global.Path.data`: `<data-basename>-archive/p4.2/<UTC>-<uuid>/`; temp archive under same `p4.2` parent; EXDEV fallback forbidden/fails closed; manifest v1 and preserved POSIX relative paths; no runtime reader. Stop sole runtime and close handles; run `wal_checkpoint(TRUNCATE)`, `integrity_check`, `foreign_key_check`. Fixed members are `kilo.db` plus `kilo.db-wal`/`kilo.db-shm` if present after checkpoint, `storage/session_diff`, `storage/session_diff_base`, `storage/session_share`; explicitly include `session-export.db` and its WAL/SHM when present else record absence; explicitly exclude project-owned `snapshot` (not rollback material); nothing else archived. Manifest v1 records archive ID, UTC creation, source data root, each sorted path with bytes and SHA-256, present/absent fixed members, deterministic aggregate SHA-256 over sorted path/bytes/hash records; preserve empty fixed dirs; fsync files/dirs, reverify, atomically rename temp to final, fsync parent; any failure leaves legacy active and aborts cutover. Fresh DB has singleton identity: UUID, schema version, creation time, cutover archive ID; boot gate verifies identity and zero sessions/artifacts before activation. Rollback is offline only: stop/close, verify archive, separately archive current store under distinct rollback ID, atomically restore legacy members, rerun integrity checks, then boot legacy path; never dual-read. Deletion requires separate explicit maintainer authorization after cutover, never a side effect; no runtime/UI/API automatic deletion path. Rationale: directory+manifest permits transparent offline integrity/restore without runtime compatibility reader; same-parent atomic rename and explicit optional members fail closed | Implementation | P4.2 (P4.2a storage sub-boundary). Not a P1-P3 blocker | Resolved (2026-08-20) |

| R18 | Permission evaluator bounded semantics (hard-safety ceilings, ownership, identifiers, approval lifetime, provenance, child propagation, single-document ordering, layered composition) | Resolved (2026-08-24) as specification-only bounded decision under runtime spec section 5.3; no runtime implementation claimed and permission-evaluator implementation/test gate remains open. Mirrors runtime spec R18: (1) complete v1 ceiling catalog — (a) runtime hard deny absolute deny — cannot be overridden by policy, approval, or allow-everything; (b) protected canonical control/config paths (`~/.config/kilo/kilo.jsonc`, `<workspaceRoot>/.kilo/kilo.jsonc`, all typed/config assets under canonical `<workspaceRoot>/.kilo/` (legacy `.kilocode/` is a pre-cutover safety boundary only until the P4.3 reader cutover — not a canonical target and does not survive cutover) except `plans/` (agent, command, rules, skills, workflows, config assets), `kilo.json`, `kilo.jsonc`, `opencode.json`, `opencode.jsonc`, `AGENTS.md`) cap mutating at ask unless exact agent/path safety approval exists; broad/wildcard patterns (`*`, `**`, prefix `*`), permission `*`, and allow-everything/session allow do not bypass; (c) broad/wildcard read grants for `*.env` and `*.env.*` cap at ask, `*.env.example` exempt, only exact approval for the exact `*.env` pattern/file resolves the ask; literal `*.env.example` is exempt (allow permitted); (a) is absolute deny, (b) and (c) are ask ceilings — a single exact bounded approval for the same operation (same session, same agent, same permission, exact canonical pattern set) resolves every matching ordinary `ask` and every (b)/(c) `ask-ceiling` for that exact request (no separate class discriminator); no approval resolves (a), any `deny`, any wildcard/broad ceiling attempt, permission `*`, or `allow-everything` beyond ordinary `ask` via `allow-everything` only; catalog complete v1, additions require new bounded decision; (2) runtime-owned enforcement in `packages/opencode`, extension authors/renders only, shared core may hold IDs/types, no extension authoritative enforcement; (3) canonical IDs `question` for the free-form question flow (`question` permission) and `question_tool` for the question tool invocation (`question_tool` permission) separate registry/provenance identities, `mcp` not an authorization alias; built-ins include `read`, `edit`, `bash`, `glob`, `grep`, `list`, `task`, `external_directory`, `skill`, `agent_manager`, `webfetch`, `websearch`, `lsp`, `doom_loop`, `todowrite`, `todoread`, `notebook_read`, `notebook_edit`, `notebook_execute` plus `question`/`question_tool`, no invented plugin IDs; (4) per-session ephemeral approvals — `once` one operation identity `operationId = permission:<permissionRequestId>` (for example `permission:per_abc123`) correlated via `request.operationId == approval.operationId`, distinct from `request.permission` (`edit`, `question_tool`); `session` only same session/agent/permission ID/exact canonicalized pattern set until session end/revoke, no wildcard for ceilings, children never inherit parent approvals, crash/disposal drops, durable changes only via canonical file edit; a single exact `once`/`session` approval for the same operation (same session, same agent, same permission, exact canonical pattern set) resolves every matching ordinary `ask` and every class (b)/(c) `ask-ceiling` for that exact request (no separate class discriminator; does not cover class (a), any deny, wildcard/broad, permission `*`, or allow-everything); exact bounded approvals may resolve only (b)/(c) ask ceilings; (5) v1 in-memory provenance shape with schema version `1`, `request.permissionRequestId`/`request.operationId` (`operationId = permission:<permissionRequestId>`, for example `permission:per_abc123`) and `request.permission` (`edit`, `question`, `question_tool`) plus canonical patterns, all contributing layers (provenance layer-decision representation is `deny | ask | ask-ceiling | allow | no-ceiling` for `layers[].decision`; `no-ceiling` valid only for the always-evaluated `runtime-ceiling` when no (a)/(b)/(c) matches — explicit neutral, not `ask`, never a final `decisive.result` which is only `deny | ask | ask-ceiling | allow`; only applicable policy layers contribute otherwise; non-applicable policy layers with no authored document/selected manifest contribute no decision), source kind/path/asset ID plus per-layer decision, rule identity/pattern/action/order, decisive result/reason (`deny`/`ask`/`ask-ceiling`/`allow` only — never `no-ceiling` — with `ceilingId` (a)/(b)/(c) when `ask-ceiling`), approval metadata (`operationId` for `once`; a single exact approval for the same operation (same session, same agent, same permission, exact canonical pattern set) is recorded as covering both matching ordinary and ceiling asks when applicable), an applicable authored/selected document with no matching rule contributes `layers[].decision = "ask"` (no-rule `ask`), runtime neutral `no-ceiling` is included as `layers[].decision = "no-ceiling"` but does not create `ask`, diagnostic/read-model not second store; (6) child propagation inherits denies/restrictions, never allows/approvals, each child re-evaluates global/project/own manifest/own restrictions; (7) single-document ordering exact literal first, then greater literal specificity (more literals/fewer wildcards), then later declaration order, never crosses layers; an applicable authored/selected document with no matching rule contributes `ask` (no-rule ask), non-applicable layers contribute no decision; runtime neutral `no-ceiling` never ordered; (8) composition — runtime-ceiling layer is always evaluated and yields `deny` (a), `ask-ceiling` (b/c), or explicit neutral `no-ceiling` (`layers[].decision = "no-ceiling"`, not `ask`); an authored/selected policy layer is applicable, a layer with no authored document/selected manifest/session restriction is non-applicable and contributes no decision; an applicable authored/selected document with no matching rule contributes `ask` (no-rule ask); neutral `no-ceiling` does not block `allow`; provenance layer-decision representation is `deny | ask | ask-ceiling | allow | no-ceiling` for layers, final `decisive.result` is only `deny | ask | ask-ceiling | allow` (never `no-ceiling`); deny dominates ask dominates allow (LOCK-006); if any layer yields `deny` => `deny` (class (a) never resolved); otherwise if any layer yields `ask`/`ask-ceiling` (including applicable no-rule `ask`) => `ask`/`ask-ceiling` unless every ask is resolved — a single exact `once`/`session` approval for the same operation (same session, same agent, same permission, exact canonical pattern set — `once` via `permission:<permissionRequestId>`) resolves every matching ordinary `ask` and every class (b)/(c) `ask-ceiling` for that exact request (no separate class discriminator; no wildcard, permission `*`, or allow-everything for ceilings); ordinary `ask` may also be resolved by `allow-everything`; every ceiling ask and every ordinary ask must be covered before `allow`; a mixed ordinary+ceiling request with one matching exact approval therefore yields `allow`; if any ordinary `ask` remains unresolved => `ask`; if any ceiling `ask-ceiling` remains unresolved => `ask-ceiling` (deny still dominant); `allow` only when every applicable policy layer has decisive `allow`, runtime is neutral `no-ceiling`, and at least one `allow` exists (e.g., global `allow` + existing project document with no matching rule => project applicable `ask` => overall `ask`; standalone `question_tool` `allow` where other policy layers non-applicable and runtime `no-ceiling` neutral => `allow` if all applicable allow); toggles only change availability or add restrictions. Evidence artifact `specs/vscode-orchestrator/evidence/p4-permission-evaluator-semantics.md` is specification-only; permission-evaluator implementation/test gate (P4 checklist) remains open and unchecked; P4.3 manual checklist byte-identical and still pending; current flat last-match behavior remains non-normative | Hub | P4 (permission-evaluator gate; not a P1-P3 blocker) | Resolved (2026-08-24) — specification-only; implementation/test gate remains open (no runtime code claimed) |

## 10. Risk / Blocker Log

| Date | Phase | Risk / Blocker | Impact | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|
| 2026-08-10 | All | Residual worktree/Diff Viewer and other removed features remain implemented in the current branch | Removal phases must treat them as cleanup scope, never as retained capabilities | Removal inventory (section 7) and P3 gates; truthful current-state claims (LOCK-013) | Manifestor | Monitored |
| 2026-08-10 | P4 | Dual authority could outlive the migration bridge | Old path retained indefinitely; contract drift | Atomic legacy-reader cutover at P4.3 under revised R6; old CLI/server deletion (P4.5) | Manifestor | Monitored |
| 2026-08-10 | P4 | Config authority change could interrupt active generations | Generation snapshot instability | Atomic version creation + snapshot pinning (LOCK-011, runtime spec section 5) | Manifestor | Monitored |
| 2026-08-10 | P5 | Selector availability gated on backend state could regress during migration | Startup degraded until backend ready | Persisted indexes before worker readiness; action-specific gates (LOCK-012) | Manifestor | Monitored |
| 2026-08-10 | All | Performance claims based on estimates (e.g. 20-40%/30-50%) could become acceptance claims | Unmeasured magnitudes; false performance claims | Evidence-only gates (LOCK-PERF-6); hypotheses flagged; P0 descriptive n=5 baselines recorded with evidence links (section 8, six accepted campaigns); no numeric thresholds exist (R7 resolved 2026-08-14, section 9) — descriptive same-environment comparisons only | Manifestor | Resolved (2026-08-12) |
| 2026-08-10 | P3-P5 | Removed features might still initialize at worker startup (residual code) | Startup work does not decline despite removal | Structural absence proof per removal subphase — initialization/readers/listeners/resources absent — plus recorded affected-path deltas; zero or positive noisy delta is allowed if no removed work remains (LOCK-PERF-3, runtime spec section 10) | Manifestor | Monitored |
| 2026-08-10 | P0-P2 | Transport/event overhead misattributed as the dominant latency | Wrong optimization target | LOCK-PERF-7: measure model/network/tool/approval separately from transport/event overhead | Manifestor | Monitored |
| 2026-08-10 | P0 | Unbounded benchmark capture could grow process memory without bound during a long/hung sample run | Harness OOM; invalid benchmark run | Bounded capture implemented: raw output retained only as a `capBytes` byte-capped tail with `MAX_SEGMENTS` object bound (`packages/kilo-vscode/script/p0-bench/parse.ts`); covered by `tests/unit/p0-bench-capture.test.ts`, `p0-bench-parse.test.ts`, `p0-bench-cleanup.test.ts`, `p0-bench-stats.test.ts` | Manifestor | Resolved |
| 2026-08-10 | P0 | Unbounded worker stderr accumulation could grow extension-host memory over the worker lifetime | Extension-host memory growth; lost diagnostics | Bounded diagnostic stderr tail: `MAX_STDERR_TAIL_LINES` (100 lines) and `MAX_STDERR_TAIL_BYTES` (16 KiB) caps with reassembly (`packages/kilo-vscode/src/services/cli-backend/stderr-tail.ts`); covered by `tests/unit/stderr-tail.test.ts` | Manifestor | Resolved |
| 2026-08-10 | P0 | Environmental esbuild-watcher process attribution (a non-owned process observed during harness runs) could be misread as a harness/benchmark defect | Misattributed performance or resource claims | Left unresolved as an attribution question; it is not a benchmark claim and no metric depends on it (LOCK-PERF-6) | Manifestor | Monitored |
| 2026-08-12 | P0 | Merged many-agent-MCP campaign segments ran with different `backendCli` temp paths (`environmentDrift` in `merge-manifest.json`) | Cross-segment timing comparability | Drift recorded and limited to temp paths; all segments share one `cliSnapshotSha256`; values stay descriptive n=5, not SLA (R7 Open) | Manifestor | Monitored |
| 2026-08-12 | P0 | CLI process-tier files changed after the 2026-08-11 backend campaign ran | Backend campaign numbers describe an older CLI process tier if re-used for later-phase comparison | Current-tier rerun completed 2026-08-12 (`specs/vscode-orchestrator/evidence/p0-baseline/2026-08-12T06-08-27-792Z/backend.jsonl`, git 7a768502dd, 42/42 ok, 79 n=5 summaries); the environmental first-attempt partial (`2026-08-12T06-05-12-356Z/backend.jsonl`, no run finish, scenarios 6/7/8 only) is retained as excluded evidence; the 2026-08-11 campaign remains historical tier evidence only (section 8) | Manifestor | Resolved |
| 2026-08-13 | P1-P5 | Presentation state can diverge from runtime operational facts across lifecycle boundaries — extension-owned view boundaries (panel close/reopen, reload, session switch) and runtime boundaries (transport reconnect, worker restart). Current-state motivation: the extension derives and persists per-session status/timing read-model state from `session.status` events (`packages/kilo-vscode/src/agent-manager/session-timing.ts`, `packages/kilo-vscode/src/agent-manager/AgentManagerProvider.ts`), while the runtime owns session status (`packages/opencode/src/session/status.ts`); derived state is not revalidated against the runtime at every boundary | Stale or duplicated presentation state; UI-held facts surviving a lifecycle boundary; orphaned processes/resources; lost updates | Sole runtime authority for operational facts with derived presentation state (runtime spec section 7.1); lifecycle-boundary convergence evidence recorded per phase (direction spec section 11); R9 records the bounded implementation decision (section 9) | Manifestor | Monitored |
| 2026-08-13 | P4 | The config refactor relocates storage but keeps source competition: generic compatibility readers/overlays still contribute to effective config after migration, or dual-read becomes permanent | Effective config keeps merging from competing sources; the removal gates become unverifiable | Closed legal source taxonomy and field registry (runtime spec sections 3.1-3.2); deterministic materialization with typed composition and provenance (runtime spec section 5.1); per-source removal evidence with inactive proof (runtime spec section 8.1; tracker section 7); P4.1/P4.3/P4.4 exit gates (section 5); R10 resolved 2026-08-18 (section 9) — required by P4.1, now closed with evidence (closed 11-field registry/schema/provenance) | Manifestor | Monitored |
| 2026-08-13 | P4 | File and UI effective configuration diverge — external edits, stale drafts, invalid external edits, or deletion/unset produce silent overwrites, partial applies, or legacy fallback | Users lose edits; effective config no longer matches the canonical files; WYSIWYG guarantee broken | Bidirectional file-editing contract with watched external edits, content/version-stamp conflict detection, validation-before-write, and visible reconciliation (runtime spec section 5.4); WYSIWYG acceptance gate at P4.1 (runtime spec section 10.9; direction spec section 11); exact presentation under direction spec Q5 | Manifestor | Monitored |
| 2026-08-13 | P0-P4 | The 2026-08-13 post-P0 decision revision (file-authoritative hybrid; atomic cutover instead of dual-read) is misread as retroactively changing P0 evidence or as a new ADR | Historical P0 evidence distorted; decision authority confusion | Revision recorded as a durable clarification/revision dated 2026-08-13: ADR-0003 amended in place (not a new ADR), ADR-0002 unchanged, `p0-current-state-inventory.md` unchanged, P0 stays Complete with original R2/R6 wording preserved as historical evidence (runtime spec section 9; tracker section 9) | Manifestor | Monitored |
| 2026-08-13 | P4 | P4.3 cutover proceeds without a pre-cutover manual reconciliation checklist/evidence for the sole user, or without an explicit mapping of the 15 P0 enumerated sources onto the 13 removal classes / retained legal classes | Desired current configuration lost at the cutover; unclassified baseline sources survive | Pre-cutover reconciliation checklist/evidence recorded for the sole user (manual only; no migration tooling); explicit P0 15-source to 13-removal-class mapping recorded before P4.3 (runtime spec section 8.1; tracker section 7); no-migration decision unchanged | Manifestor | Monitored |
| 2026-08-14 | P1-P5 | Inventory-first analysis paralysis — an exhaustive pre-migration classification of legacy features blocks P1/P2/P4 from starting | Migration stalls before any implementation begins | Principle-first policy (LOCK-014): no exhaustive pre-migration inventory or suspension registry is required; first-touch disposition is decided just in time (ADR-0004) | Manifestor | Monitored |
| 2026-08-14 | P1-P5 | Unrecorded feature loss — a displaced behavior disappears with no evidence trail | Users lose observable behavior with no record of value or disposition | Just-in-time candidate registration (LOCK-015): a registry entry is created when an implemented behavior is actually disabled/removed or intentionally not carried forward (tracker section 7) | Manifestor | Monitored |
| 2026-08-14 | P1-P5 | Reconstruction candidate used as a compatibility entitlement — an entry is treated as a promise to restore or keep a behavior | Scope creep; migration stalls on compatibility detours | Registry is evidence preservation only (LOCK-015): no restore path, parity waiver, phase gate, or implementation backlog derives from an entry (ADR-0004) | Manifestor | Monitored |
| 2026-08-14 | P2-P5 | Candidate loophole weakens final H parity — a tracker or registry entry is used to waive a final H-1..H-13 gate | Final target acceptance weakened | H-1..H-13 are final target/parity gates (LOCK-014): never waived by a tracker or candidate entry; P2 proves in-scope target-surface behavior and P4 proves the H invariants under the private runtime | Manifestor | Monitored |
| 2026-08-14 | P1-P2 | Noisy descriptive measurements stall a phase without a structural finding | Phase blocked on measurement noise | Measurement noise alone does not block; comparisons are descriptive, affected-path, same-environment, and obvious structural anomalies are investigated (R7 resolved 2026-08-14; runtime spec section 10) | Manifestor | Monitored |
| 2026-08-14 | P3-P4 | A zero or positive noisy delta masks incomplete removal | Removed work silently retained | Structural absence of removed-feature initialization/readers/listeners/resources remains the mandatory gate; deltas are recorded evidence, not the exit criterion (R7 resolved 2026-08-14; runtime spec section 10.9-10.10) | Manifestor | Monitored |
| 2026-08-14 | P4 | DB exhaustion before the cutover: live `kilo.db` ~24.56 GB (2.29M events; event payload ~17.96 GB, ~99.2% full message/part update snapshots), growth ~7.5 GB/7 days (derived from the two dated observations) and ~1 MB/min during a separate short active sample, disk ~91-92% | Disk exhaustion; runtime failure before the P4.2 storage cutover | Operational containment (bounded manual operation: full archive + keep last-week-active sessions in the current legacy DB) records its policy and evidence slots without claiming completion (storage spec section 4); the kept last-week set exists only in the legacy store before the cutover and in the offline legacy archive afterward — the P4.2 cutover boots an empty canonical DB with no pre-cutover sessions (storage spec section 6.2); storage is not a P1-P3 code prerequisite; the P4.2a cutover is the target bound (ADR-0005) | Manifestor | Monitored |
| 2026-08-14 | P4 | Operational containment overclaimed as target behavior or as a phase gate | False completion claims; P1-P3 blocked on the wrong thing | Containment is a bounded manual operation, not target behavior and not a phase gate; its evidence slots stay pending until execution evidence exists (LOCK-013; storage spec section 4.3) | Manifestor | Monitored |
| 2026-08-14 | P4 | Storage cutover proceeds with migration/dual-read/archive-reader behavior, or old-history storage compatibility is retained as a target | Compatibility detour contradicts the canonical storage target | Clean cutover enforced (ADR-0005 I-8): no migration/import, no dual-reader, no runtime archive reader; legacy event-log sync/warp surfaces removed at P4.4/P4.5 with no default compatibility entitlement (runtime spec section 8.2; direction spec section 12) | Manifestor | Monitored |
| 2026-08-14 | P1 | The P1 scenario-10 comparison runs with environment drift (git 7a768502dd→99d275a9d7 both dirty, VS Code 1.132.0→1.133.0, changed CLI snapshot) and one 724 ms outlier sample | The comparison could be misread as a regression or improvement claim | Drift and the outlier are recorded with the comparison (section 4); values stay descriptive affected-path n=5 statistics only — no numeric threshold and no performance conclusion (R7 resolved 2026-08-14, section 9); re-measurement on a comparable environment is a later-phase option, not a P1 gate | Manifestor | Monitored |
| 2026-08-14 | P1 | The Topic presentation read-model is mistaken for a persisted domain model or an implemented presentation filter | Scope creep toward a Topic persistence model; false feature claims | Q1 wording updated to remove `/filter` — derived selection/expansion only (direction spec sections 4/5/7/14; tracker section 9); derivation is pure over runtime/session facts with no persistence/API/config/schema/metadata; parity and removal gates unaffected (LOCK-014/015, sections 6-7) | Manifestor | Monitored |
| 2026-08-15 | P5 | Reconciliation or post-removal cleanup reintroduces a worker-readiness dependency into the selector path, or P5 exits without a worker-unavailable Extension Host scenario and structural-absence proof | Selector UX regresses to waiting/disabled; acceptance not falsifiable | Normative P5 criteria (runtime spec section 6 criteria 1-6): selector availability independent of worker lifecycle; no readiness-based selection disable; reconciliation preserves the last-known valid index or an explicit stale/invalid presentation and never blanks/disables selectors; structural absence of `extensionDataReady` and the port/health/SSE/generated-SDK fetch chain proven at P4.4/P4.5; Extension Host worker-withheld/killed/failed scenario is required evidence; cold/warm UI-selector vs runtime-action readiness recorded separately | Manifestor | Monitored |
| 2026-08-15 | P2 | Pre-final P2-era evidence directories could be mistaken for closing evidence: `2026-08-15T06-25-37Z/` (contaminated — external gateway `kilo/kilo-auto/small` title generation because the seed omitted `small_model`, violating LOCK-006 at the request level) and `2026-08-15T07-35-17-745Z/` (copy-race — three final-window artifacts not physically captured; two reconstructed verbatim + one maximum-fidelity) | LOCK-006 violation or non-independent capture cited as proof | Both directories retained unmodified with their contamination/copy-race provenance notes (CONTAMINATION.md / README.md) and never cited as closing evidence; the final durable campaign `2026-08-15T08-35-00Z-p2/` uses physical captures, fail-closed request-level isolation (every `service=llm` generation pinned to `e2e-local/e2e-model`, any `kilo/kilo-auto/*` line fails the scenario), and per-file sha256 manifests (135 manifest entries covering 127 unique files, all 127 hash-verified) | Manifestor | Monitored |
| 2026-08-15 | P2 | P2 descriptive measurements could be misread as a performance improvement/regression claim | False performance conclusion | R7: both P0-baseline and 2026-08-15 values are recorded in section 8 with drift (git 7a768502dd→f91d22ae1f, both dirty); descriptive n=5 only, no threshold, no requirement to improve, and explicitly no improvement/regression claim | Manifestor | Monitored |
| 2026-08-15 | P2 | P2 target-surface parity could be misread as P4 private-runtime parity, typed-manifest/permission-stack/WYSIWYG contract proof, P3 removal proof, or LOCK-006 final provider-removal completeness | Overclaimed phase boundaries | Target-contract split recorded (section 6): P2 proves only the in-scope current-behavior capability over the current migration bridge; the P4-gated contracts and final private-runtime parity are explicitly NOT proven at P2 and never waived (LOCK-014); P2 claims no P3 removal and no performance improvement/regression | Manifestor | Monitored |
| 2026-08-15 | P2 | Non-independent P2 performance blockers could be misreported as satisfied or as P2 exit blockers: per-event webview render-flush was not emitted (only extension-side SSE handling spans exist) and model HTTP duration was not separately measured | Fabricated or mis-scoped performance evidence | Recorded explicitly as non-independent P2 blockers (runtime spec section 10.11) in the P2 narrative and section 8 rows; no per-event flush or model HTTP duration value is fabricated; both remain descriptive evidence for later phases | Manifestor | Monitored |
| 2026-08-15 | P3.1 | The P3.1 scenario-1 campaign's activation/render metrics moved negative and backend/transport metrics moved positive under environment drift (VS Code 1.132.0→1.133.0, git 461781ffc0→67436bdf70 both dirty, CLI snapshot changed) — the comparison could be misread as a performance improvement or regression claim | False performance conclusion | Drift and both value sets are recorded in section 8; the comparison is descriptive affected-path n=5 only, no threshold, no requirement to improve, and explicitly no improvement/regression claim (R7 resolved 2026-08-14, section 9); re-measurement on a comparable environment is a later-phase option, not a P3.1 gate | Manifestor | Monitored |
| 2026-08-15 | P3.1 | The zero static complexity deltas (webview message types 332, v2 SDK methods 250, webview entries 6, each delta 0) or the `-` generated-SDK cell could be misread as incomplete sidebar removal | Removal completeness misjudged | Zero-delta boundary explanations recorded (section 8): the removed sidebar shared the editor-tab/Agent Manager webview bundle and no SDK contract change exists (generated SDK `-` by nature); structural absence is proven statically (`test: packages/kilo-vscode/tests/unit/sidebar-removal.test.ts`) and at runtime (`test:e2e:sidebar-removal`) separately from the complexity counts | Manifestor | Monitored |
| 2026-08-15 | P3.1 | The RC-P3.1-001 drop entry could be misread as a compatibility promise or as weakening the permanent sidebar removal | Scope creep or removal misjudgment | The registry is evidence preservation only (LOCK-015): no restore path, parity waiver, phase gate, or implementation backlog derives from the entry; the inherent sidebar surface/view/title/context/assets and the migrated command behavior are recorded as not candidates (section 7) | Manifestor | Monitored |
| 2026-08-16 | P3.2 | The pre-final P3.2 evidence directories (`p3-2-worktree-removal-2026-08-15T18-00-47-000Z/`, `p3-2-worktree-removal-2026-08-15T18-11-33-000Z/`) could be mistaken for closing evidence | Pre-final state cited as completion proof | Both directories remain historical (pre-final state, before the final managed-worktree/Diff removal landed) and are never cited as closing evidence; final-tree completion evidence cites only the two `p3-2-final-*` directories (section 4) | Manifestor | Monitored |
| 2026-08-16 | P3.2 | The P3.2 descriptive scenario-1 campaign could be misread as a performance improvement/regression claim | False performance conclusion | R7/LOCK-PERF-6: P0, P3.1, and P3.2 value sets are all recorded in section 8 with drift (git 461781ffc0→67436bdf70→9d86428cc2, all dirty; VS Code 1.132.0→1.133.0; changed CLI snapshot); descriptive n=5 only, no threshold, no requirement to improve, and explicitly no improvement/regression claim | Manifestor | Monitored |
| 2026-08-16 | P3.2 | The RC-P3.2-00x drop entries could be misread as compatibility promises or as weakening the permanent LOCK-002 removal | Scope creep or removal misjudgment | The registry is evidence preservation only (LOCK-015): no restore path, parity waiver, phase gate, or implementation backlog derives from an entry; the inherent managed-worktree creation/import/transfer/branch-naming/sections/multi-version/custom-diff surfaces and the retained root-local start delegation/H paths/H-12/generic semantics are recorded as not candidates (section 7) | Manifestor | Monitored |
| 2026-08-16 | P3.2 | The seeded deterministic plan session IDs (`e2e-probe-47f91b0f-A` etc.) produce expected backend `Expected a string starting with "ses"` validation messages when synthetic-id sessions are queried | Seed noise misread as a P3.2 failure | Environment/seed classification, not a failure: the plan session IDs are synthetic fixture facts seeded through the production session-open path; the final campaign run is `success: true` with 36/36 probe PASS 0 FAIL and no P3.2 defect recorded (section 4) | Manifestor | Monitored |
| 2026-08-16 | P3.2 | The single known pre-existing pull-back-contract unit failure could be misattributed to P3.2 | Removal work wrongly blamed for a baseline failure | The failing test `tests/unit/pull-back-contract.test.ts` is unchanged in this work unit and reproduces identically at the P3.1 state; recorded as a known baseline, not a P3.2 failure (section 4) | Manifestor | Monitored |
| 2026-08-16 | P3.3 | The P3.3 descriptive scenario-1 campaign could be misread as a performance improvement/regression claim | False performance conclusion | R7/LOCK-PERF-6: P0, P3.1, P3.2, and P3.3 value sets are all recorded in section 8 with drift (git 461781ffc0→67436bdf70→9d86428cc2→2d61c228e7, all dirty; VS Code 1.132.0→1.133.0; changed CLI snapshot); descriptive n=5 only, no threshold, no requirement to improve, and explicitly no improvement/regression claim | Manifestor | Monitored |
| 2026-08-16 | P3.3 | The four LOCK-003 products (cloud sessions, JetBrains, Console, KiloClaw) could be misread as deferred, suspended, or reconstruction-candidate features, or the retained shared migration-bridge APIs (remote sessions, generic session import, EventServiceClient/presence, `config-console`/`experimental.console.*`/SDK Console group/core `consoleState`, `kilo serve` HTTP/SSE/SDK bridge) could be mistaken for a removed surface | Scope creep or removal misclassification | The four products are inherent permanent removals (LOCK-003/015), never deferred and never candidates (recorded as not candidates in section 7); the retained shared bridge APIs are asserted surviving by `test: packages/opencode/test/kilocode/console-removed.test.ts` and `test: packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (section 7) | Manifestor | Monitored |
| 2026-08-16 | P3.3 | The P3.3 removal could be misread as a new H-1..H-13 parity campaign or as proving any H criterion at P3.3 | Overclaimed phase boundaries | P2 remains the full H-1..H-13 parity proof (section 6); P3.3 records no H-matrix rerun and claims no new H parity; LOCK-008/H-1..H-13 parity remains intact (section 4) | Manifestor | Monitored |
| 2026-08-16 | P3.3 | Known unit-suite baselines (pull-back-contract one failure; cli-shutdown mock leak) could be misattributed to P3.3 | Removal work wrongly blamed for baseline failures | Both reproduce at pristine P3.2 and are recorded as known baselines, not P3.3 failures; no clean full-suite claim is made (section 4) | Manifestor | Monitored |
| 2026-08-16 | P3.3 | The P3.3 complexity deltas (−11 message types, −5 SDK methods, −1 webview entry) could be misread as proof of the LOCK-PERF-3 startup-work reduction rather than as structural absence | Performance gate misjudged | Structural absence of the removed products' initialization is proven statically and at runtime (`test:e2e:cloud-claw-removal`) separately from the complexity counts and the descriptive benchmark (LOCK-PERF-3/R7; sections 4, 7, 8) | Manifestor | Monitored |
| 2026-08-17 | P3.4 | The P3.4 descriptive scenario-1 campaign could be misread as a performance improvement/regression claim | False performance conclusion | R7/LOCK-PERF-6: P0, P3.1, P3.2, P3.3, and P3.4 value sets are all recorded in section 8 with drift; descriptive n=5 only, no threshold, no requirement to improve, and explicitly no improvement/regression claim; the excluded partial wrapper campaign `p3-4-final-2026-08-17T00-57-00-000Z/EXCLUDED` is explicitly excluded and never cited as an accepted benchmark | Manifestor | Monitored |
| 2026-08-17 | P3.4 | The four LOCK-004 removals (indexing, memory, context-management/compaction, autocomplete) could be misread as deferred, suspended, or reconstruction-candidate features | Scope creep or removal misclassification | The four features are inherent permanent removals (LOCK-004/015), never deferred and never candidates; indexing UX, memory UX/tools, manual compact/context UX, autocomplete/ghost text/next-edit, and commit-message UX are inherent observable surfaces of the LOCK-004 closed-set permanent removals; recording them as candidates would incorrectly reclassify permanent removal scope (section 7; LOCK-015) | Manifestor | Monitored |
| 2026-08-17 | P3.4 | The P3.4 removal could be misread as a new H-1..H-13 parity campaign or as proving any H criterion at P3.4 | Overclaimed phase boundaries | P2 remains the full H-1..H-13 parity proof (section 6); P3.4 re-proves only H-13 retained parity on the post-removal surface (section 6); no H-1..H-12 parity rerun at P3.4; LOCK-008/H-1..H-13 parity remains intact (section 4) | Manifestor | Monitored |
| 2026-08-17 | P3.4 | Known unit-suite baselines (pull-back-contract one failure; cli-shutdown mock leak) could be misattributed to P3.4 | Removal work wrongly blamed for baseline failures | Both reproduce at pristine P3.2 and are recorded as known baselines, not P3.4 failures; no clean full-suite claim is made (section 4) | Manifestor | Monitored |
| 2026-08-17 | P3.4 | The CLI build correction (stale indexing-worker entry removed) could be misread as an incomplete or broken build | Build stability concern | The stale entry was removed and the native build passes; the absence test pins the corrected state; this is a build cleanup, not a new defect | Manifestor | Monitored |
| 2026-08-18 | P4.1 | P4.1 evidence could be misread as claiming private worker, legacy-reader cutover, HTTP/SSE/SDK removal, CLI deletion, or final H parity | Overclaimed phase boundaries | P4.1 implements only the file-authoritative GUI config layer; CLI remains migration bridge; P4.2-P4.5 Not started; no private runtime, cutover, or H parity claimed (section 5, P4.1 evidence) | Manifestor | Monitored |
| 2026-08-18 | P4.1 | Environmental Playwright Chromium absence could block E2E verification | UI verification unverifiable for config writes | Known environmental caveat recorded; unit tests (606 focused) cover config implementation; Playwright is not required for P4.1 acceptance (section 5) | Manifestor | Monitored |

## 11. Change Log

| Date | Change | By |
|---|---|---|
| 2026-08-10 | Initial creation: tracker for ADR-0002 migration; documentation foundation complete; all phases Not started | Manifestor execution of audit-correction task |
| 2026-08-10 | Final product boundaries (LOCK-001..008) and runtime/config decisions (LOCK-009..012, ADR-0003 + runtime spec) recorded; tracker revised: new decision locks, phase set P0-P5 with subphases, new H-1..H-13 parity matrix, removal inventory evidence, runtime metrics, updated decisions/risks; all phases still Not started; no implementation evidence | Manifestor execution of final-boundaries documentation task |
| 2026-08-10 | Performance made first-class: LOCK-PERF-1..7 recorded; performance model, cost attribution, instrumentation plan, benchmark scenarios, and regression gates added to the runtime spec (section 10); performance objective and removed-features-not-startup-dependencies rules added to the direction spec; tracker gained performance metrics (all Not proven), P0 profiling tasks, and P1/P2/P3 performance gates; no measurements exist on the current branch | Manifestor execution of performance-model documentation task |
| 2026-08-10 | Performance-doc audit corrections applied: TUI lazy rendering vs static CLI/TUI module-graph imports distinguished (magnitude unmeasured); AppLayer described as layer definition + lazy runtime handle with graph construction at server listener build, not at first import; benchmark scenarios mapped to explicit tracker metric rows (warm view, no-provider/custom-provider/many-agent-MCP startup, parallel child sessions, hot config update, cold-update no-interruption, burst coalescing); P4.4 exit evidence requires measured net startup-work reduction and absent removed-feature initialization; ambiguous provider citations qualified to `packages/opencode/src/provider/provider.ts`; worker CLI entry/module-graph load added to instrumentation points; all performance values remain Not proven | Manifestor execution of performance-doc audit-correction task |
| 2026-08-10 | P0 activated: static current-state inventory (`specs/vscode-orchestrator/p0-current-state-inventory.md`) checked in with surfaces, message/SSE/SDK protocol counts, removal residue, runtime/config root-cause graph, static redundancy candidates R-1..R-10, H-1..H-13 baseline map, proposed baseline-count definitions (Q3), and dynamic unknowns U-1..U-10; tracker P0 phase set Active with inventory/harness evidence; no removal claimed complete; no performance metric changed from Not proven | Manifestor execution of P0-evidence update task |
| 2026-08-10 | H-1..H-13 baseline fixture added: `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` executes H-1, H-2, H-7, H-8, H-9, H-10, H-11, H-12, H-13 against real harness services and records H-3..H-6 as explicit gaps with references; every entry carries `parity: "unproven"`; tracker section 6 statuses updated to baseline-smoke/gap-recorded with parity Not proven; no target-surface criterion claimed passing | Manifestor execution of P0-evidence update task |
| 2026-08-10 | P0 instrumentation added (opt-in `KILO_P0_PERF`): extension host `packages/kilo-vscode/src/perf/perf-instrument.ts`, backend `packages/opencode/src/kilocode/perf/instrument.ts`, webview `packages/kilo-vscode/webview-ui/src/utils/perf.ts`; covered by `packages/opencode/test/kilocode/p0-instrument.test.ts` and `packages/kilo-vscode/tests/unit/p0-perf-instrument.test.ts`; instrumentation points exist but no durable per-stage timings recorded (section 8 remains Not proven) | Manifestor execution of P0-evidence update task |
| 2026-08-10 | Benchmark harnesses added: backend under `packages/opencode/test/benchmark/` (scenarios 6,7,8,9,11,12,13; runner `packages/opencode/script/p0-benchmark.ts` via `bun run bench:p0`) and VS Code Extension Host under `packages/kilo-vscode/script/p0-bench/` (scenarios 1,2,3,4,5,10; `packages/kilo-vscode/script/e2e-p0-bench.ts` + `e2e-p0-bench-launch.mjs` via `bun run test:p0-bench`, runner `packages/kilo-vscode/tests/e2e/p0-bench-runner.ts`); both have passing unit/integration tests; only smoke runs so far — no full repeated baseline, so benchmark-runnable criterion is PARTIAL | Manifestor execution of P0-evidence update task |
| 2026-08-10 | OOM correction: bounded benchmark capture (`capBytes` + `MAX_SEGMENTS` in `packages/kilo-vscode/script/p0-bench/parse.ts`) and bounded worker stderr diagnostic tail (`MAX_STDERR_TAIL_LINES`/`MAX_STDERR_TAIL_BYTES` in `packages/kilo-vscode/src/services/cli-backend/stderr-tail.ts`) added with unit tests; risk rows recorded; environmental non-owned esbuild watcher attribution left unresolved and not a benchmark claim | Manifestor execution of P0-evidence update task |
| 2026-08-12 | P0 objective evidence update: H-3..H-6 fixture gaps closed (fixture now executes all H-1..H-13 as live tests through real production services/run-owned fixtures; no gap records remain; section 6 H-3..H-6 rows moved from gap-recorded to baseline-smoke with parity still Not proven); three formal repeated baseline campaigns recorded under `specs/vscode-orchestrator/evidence/p0-baseline/` (Extension Host cold-start `2026-08-11T11-51-16-883Z`, merged many-agent-MCP `many-agent-mcp-merged-2026-08-12T00-27-49-435Z` with `baselineComplete` + merge manifest + `environmentDrift`, backend `2026-08-11T04-46-19-926Z/backend.jsonl` 42/42 ok, 79 n=5 summaries); section 8 metric rows updated to Measured only where a campaign objectively supports the metric with explicit scope, all values descriptive n=5 p95=max and NOT SLA/threshold (R7 Open); remaining rows stay Not proven; P0 stays Active; no removal claimed complete; risk log entries added for environment drift and post-campaign CLI process-tier changes | Manifestor execution of P0-evidence update task |
| 2026-08-12 | P0 harness-fix and current-tier evidence update: accepted Extension Host warm-view/no-provider/custom-provider campaign (`2026-08-12T05-52-07-672Z/benchmark.jsonl`, git 7a768502dd, gitDirty true, run status ok, 19 samples — warm-view 2 warmup + 5 measured, no-provider 1 + 5, custom-provider 1 + 5 — 27 n=5 summaries) and current-tier backend rerun (`2026-08-12T06-08-27-792Z/backend.jsonl`, git 7a768502dd, gitDirty true, 42/42 ok, 79 n=5 summaries, CLI-side in-process `Server.listen`/`AppLayer`, `serve_cli_entry` not emitted), bringing the accepted campaign set to five (cold-start, many-agent-MCP, historical backend, warm-view/no-provider/custom-provider, current-tier backend); two pre-fix partial artifacts retained but excluded and never cited as baseline (`2026-08-12T04-30-39-069Z/benchmark.jsonl` no finish/no custom-provider, `2026-08-12T06-05-12-356Z/backend.jsonl` no finish/scenarios 6/7/8 only); harness-fix tests added (`test: packages/kilo-vscode/tests/unit/p0-bench-launch-failure.test.ts`, `test: packages/opencode/test/kilocode/p0-bench-seed-config-schema.test.ts`) covering scenario 4 fixture schema correction, shared fixture schema test, early `runTests` failure observation, and bounded blocked-sample fallback (P0 progress evidence, not product parity); section 8 warm-view row measured, no-provider row split into fresh no-seed vs seeded-empty persisted conditions, custom-provider row measured, and backend scenario 6/8/9/11/12/13 rows updated to current-tier values with the 2026-08-11 campaign retained as historical tier evidence; risk row for post-2026-08-11 CLI process-tier drift resolved; all values descriptive n=5 p95=max and NOT SLA/threshold (R7 Open); P0 stays Active; no removal claimed complete | Manifestor execution of P0-evidence update task |
| 2026-08-12 | P0 baseline issue requirement removed: P0 closure no longer references or requires a planned baseline GitHub issue; closure uses tracker-local objective test/doc evidence and recorded decisions. Tracker schema (generic `issue: -` / PR/test/doc evidence fields), metrics, campaigns, locks, statuses, and other decisions unchanged. | Manifestor execution of baseline-issue-removal task |
| 2026-08-12 | P0 session-switch evidence update: accepted the sixth formal repeated baseline campaign — Extension Host session switch (`2026-08-12T07-21-22-201Z/benchmark.jsonl`, git 7a768502dd, gitDirty true, run status ok; one lifecycle, 5 seeded deterministic sessions, real Playwright tab-strip clicks, settled when the active tab id and header title match; 1 warmup + 5 measured, all ok, one n=5 summary); section 8 Session switch row changed from Not proven to Measured (`switchSettleMs` med 195 / p95=max 282 ms, min 115, max 282, mean 201.6; descriptive n=5 only, not SLA/threshold, no P1 navigation parity claim, R7 Open); benchmark-scenarios exit checklist marked MET — full repeated baselines now recorded for all 13 defined scenarios (runtime spec 10.9); session-switch removed from the unmeasured P0 rows and next actions; P0 stays Active; no removal claimed complete | Manifestor execution of P0-evidence update task |
| 2026-08-12 | P0 decisions finalized and P0 marked Complete: Q3 and R1/R2/R5/R6/R8 resolved and recorded (tracker section 9; inventory section 9 for Q3; runtime spec section 9 for R1/R2/R5/R6/R8) under the locked decision text; R7 stays Open with required-by clarified to "before the first P3/P4/P5 performance gate that uses thresholds (and the P1/P2 no-regression gate if applicable), not the P0 baseline recording"; five `Not proven` metric rows re-scoped to later-phase gates (persisted-selector paint → P5; attribution + per-event transport/webview flush → P2; removed-feature init count + startup-work net reduction → P3/P4.4) and removed from P0 blockers/next actions; P0 exit checklist marked MET for baseline counts, Q3, bounded decisions, instrumentation/baselines, and benchmark reproducibility; P0 status set Complete (2026-08-12) with the recorded objective evidence; P1 left Not started explicitly; risk row for "no performance measurements exist" resolved; no GitHub issue, no target-surface parity, no SLA, no removal completion claimed; canonical architecture docs unchanged (LOCK-013); no commit or push | Manifestor execution of P0 decision-finalization task |
| 2026-08-13 | Documentation reinforcement: lifecycle-boundary observation generalized into architecture obligations — runtime sole authority for operational facts, presentation derivation, view/session lifecycle isolation, hydration/reconnect convergence, and lifecycle-boundary acceptance evidence. Direction spec: terminology rows (section 4), H-11 (section 6), ownership (section 7), bounded architecture (section 8), acceptance gates (section 11), risks (section 13). Runtime spec: scope cross-reference (section 1), observation/hydration contract (section 7.1), constraints pointer (section 7), R9 added open (section 9). Tracker: Session Bootstrap section, lifecycle-boundary evidence checkboxes in P1/P2/P4, H-11 criterion synced with the direction spec, R9 row, risk and change-log entries. No ADR, inventory, canonical-doc, code, or evidence-file changes; P0 stays Complete; all resolved decision texts unchanged; no commit or push | Manifestor execution of documentation-reinforcement task |
| 2026-08-13 | Audit corrections accepted: lifecycle-boundary phase mapping aligned — P1 records only the extension-owned view boundaries (panel close/reopen, reload, session switch) on the migration bridge; P2 records all five boundaries (view boundaries plus transport reconnect and worker restart) on harness-parity flows over the current bridge; P4 records all five against the private-worker observation surface. R9 required by P4.2 only (tracker and runtime spec section 9); P1/P2 test current behavior without deciding future private handshake mechanics. Tracker exit checkboxes link to spec sections instead of restating runtime spec 7.1 outcome language; tracker section 6 H-11 note and risk row distinguish view boundaries from runtime boundaries; risk-row provenance replaced with repository evidence (`packages/kilo-vscode/src/agent-manager/session-timing.ts`, `packages/kilo-vscode/src/agent-manager/AgentManagerProvider.ts`, `packages/opencode/src/session/status.ts`); direction spec section 13 risk-table rows corrected to two columns; direction spec section 11 gate and H-11 invariant distinguish boundary classes. P0 stays Complete; all resolved decision texts unchanged; no commit or push | Manifestor execution of audit-correction task |
| 2026-08-13 | Config-refactor contract strengthened: bounded target configuration model defined in the runtime spec — legal source taxonomy and field registry (sections 3.1-3.2), config authority and deterministic materialization with typed composition, provenance, and env/CLI disposition (section 5.1), typed agent manifests (section 5.2), restrictive permission composition (section 5.3), and effective-config source removal inventory (section 8.1); evidence-vs-target rows added (section 2.6); R10 added open, required by P4.1 (runtime spec and tracker section 9); P4 exit checklist gained field-registry/schema/provenance, dual-read cutoff, and per-source inactive-evidence gates; effective-config source removal evidence table added (tracker section 7) with a counting obligation in section 8 (9 source classes; P4.4 exits with zero rows active); risk and change-log entries added. Direction spec: H-1/H-6 updated to typed agent manifests and restrictive policy composition, preserving capabilities. No ADR, inventory, canonical-doc, code, or evidence-file changes; P0 stays Complete; all resolved decision texts unchanged; no commit or push | Manifestor execution of config-refactor documentation task |
| 2026-08-13 | File-authoritative hybrid decision revision: durable user decision — all user-authored effective configuration is file-authoritative and WYSIWYG through the UI under exactly two canonical authored scopes (one global config root; `<workspaceRoot>/.kilo/`); no migration/import tool and no dual-read compatibility window (atomic legacy-reader cutover at P4.3; sole user manually reconciles desired configuration before cutover). ADR-0003 amended in place (Decision, LOCK-010/I-2, alternatives, consequences, follow-up; revision dated 2026-08-13; not a new ADR); ADR-0002 unchanged. Runtime spec: ownership domains and legal source taxonomy rewritten (sections 3-3.2), snapshot identity includes canonical file content + schema version + opaque secret references (section 5.1), agent markdown retained as a typed canonical project/global asset (section 5.2), permission contract expanded — child inheritance, question-flow vs `question`-tool distinction, per-session approval records, no-rule=ask, toggle semantics, generated availability defaults (section 5.3) — new bidirectional file-editing/WYSIWYG contract (section 5.4), migration strategy revised to legacy-reader cutover with no dual-read/import (section 7), removal disposition changed to delete with no target reader for 13 source classes including legacy global config filenames/readers and legacy migration readers (section 8.1), R2/R6 revised with original wording preserved as historical evidence and R10 bounded within the resolved topology (section 9), WYSIWYG and permission-evaluator gates added (section 10.9), agent/permission code evidence added (section 2.2). Direction spec: LOCK-010, terminology, surfaces, H-1/H-6 target-contract split (P2 proves current capability; §5.2/§5.3/§5.4 contracts gate at P4), ownership, bounded architecture, gates, compatibility, risks, Q5 (WYSIWYG presentation open; semantics decided). Tracker: Session Bootstrap, phase overview, P0 checklist revision note, P4 scope/exit checklist (cutover, WYSIWYG, permission-evaluator), H-1/H-6 synced, section 7 removal table now 13 rows with delete disposition, section 8 source-count wording fixed, R2/R6/R10 rows, risks and this change-log entry. `p0-current-state-inventory.md` unchanged; P0 stays Complete; canonical architecture docs unchanged (LOCK-013); no commit or push | Manifestor execution of file-authoritative hybrid revision task |
| 2026-08-13 | Final audit corrections accepted: internal lettered orchestration labels removed from all artifacts and replaced with direct product-language statements or durable IDs (LOCK-010, LOCK-011, R2/R6); runtime spec section 8.1 and tracker section 7 effective-config removal tables completed to 13 source classes with `Evidence phase` columns — adding global project-asset sources and primary-worktree mirror reads — plus the P0 15-source to 13-removal-class mapping obligation and pre-cutover manual reconciliation evidence; phase semantics clarified everywhere — P4.3 performs the atomic legacy-reader cutover/deletion, P4.4 records and verifies per-row inactive/removal evidence and narrows the public transport, P4.5 deletes old CLI/server/product/public interfaces (ADR-0003 consequence corrected; obsolete deadline phrasing removed); runtime spec section 3 table header renamed to `Canonical scopes and legal inputs`. ADR-0002, inventory, and code untouched; P0 stays Complete; no commit or push | Manifestor execution of final-audit-correction task |
| 2026-08-14 | Architecture-first direct reconstruction + bounded Failure/Outcome/Recovery: ADR-0004 created (principle-first policy, two closed sets, no default compatibility entitlement, just-in-time disposition, LOCK-014/015, error-branch input-evidence-only); direction spec corrected to final-gate language for H-1..H-13 and scoped compatibility policy (LOCK-014); runtime spec gained error/retry/recovery ownership evidence (2.7) and the bounded Failure/Outcome/Recovery target (7.2) with R11-R14 open and required by P4.2 (section 9); tracker gained just-in-time maintenance rules, Reconstruction candidate status, reconstruction candidate registry (initially empty) + observed-candidates note (auto-continue, timer display), P2/P4/P5 phase and gate corrections per LOCK-014/015, risk and change-log entries, and the ADR-0004 link. P0 stays Complete; P1-P5 Not started; ADR-0002/0003 and the P0 inventory unchanged; no commit or push | Manifestor execution of direct-reconstruction documentation task |
| 2026-08-14 | Audit corrections accepted: discard always records a drop reconstruction-candidate entry — ADR-0004 Decision/I-3, the LOCK-014 rows (direction spec and tracker), direction spec section 12, and the tracker maintenance rules now state that every intentional non-carry-forward, including a clearly-obsolete discard immediately assigned `drop`, records an entry preserving user value/provenance/decision evidence. Permanent-removal set stated precisely, replacing the coarse removal-range citation — ADR-0002 LOCK-001/002/003/004/006 plus the non-preservation clause of LOCK-005 for the existing compaction implementation, with LOCK-005's minimal internal overflow safeguard explicitly retained in the final-required set through H-13 (ADR-0004 Context/I-2, the LOCK-014 rows, the tracker maintenance rules, the direction spec section 9 removal-inventory caption, and the tracker section 9 note). ADR-0002/0003 decisions unchanged; P0 stays Complete; no commit or push | Manifestor execution of accepted audit-correction task |
| 2026-08-14 | Error architecture scope split + performance guardrail decision: error foundation is independently valuable, not merely retry support, and is split between the P4.2 private-runtime foundation (runtime spec section 7.2: runtime-owned normalization boundary, minimal Failure/Outcome schema without taxonomy freeze, operation identity/outcome facts for generation-path operations, minimal durable/diagnostic/panel-visible field tiers, runtime-side redaction before persistence/projection, structured redacted panel projection, cancellation provenance, schema/envelope ownership, semantic recovery authority, worker-crash in-flight disposition) and separately planned post-reconstruction error maturity (runtime spec section 7.3; tracker section 7 backlog — taxonomy refinement, diagnostic retention/sanitizer hardening, logs/telemetry keep-lists, rich error actions/notification routing, domain-specific integrations), which is not a reconstruction candidate and not a P4/P5 blocker. R11-R14 refined: R11 record location/retention under existing storage (no new store), R12 schema + normalization boundary + field tiers + redaction + cancellation provenance + versioned envelope (no taxonomy or byte/depth sanitizer contract freeze), R13 accounting/coordination only (may reuse current bounded behavior; retryability algorithms/delay shapes are post-foundation), R14 crash disposition without resumability or a new op ledger. R7 resolved 2026-08-14: no numeric pass/fail performance thresholds — P1/P2 descriptive same-environment affected-path comparisons (no requirement to improve; noise alone does not block); P3/P4 structural absence proof plus recorded deltas (zero/positive noisy delta allowed if no removed work remains); only affected rows rerun per phase; complexity budgets record deltas; numeric SLA stays R3 at P5. All mirrored gates updated (direction spec §8/§9/§11/§13, runtime spec §9/§10.1/§10.7/§10.9-10.11, tracker P1/P2/P3/P4 checklists, metrics statuses, risks). ADR-0004 updated (error foundation/maturity split, refined I-9/I-10, new I-12). P0 stays Complete; ADR-0002/0003, inventory, and canonical docs unchanged; no commit or push | Manifestor execution of error-scope-and-performance-guardrail documentation task |
| 2026-08-14 | Bounded private-runtime storage foundation integrated: ADR-0005 created (canonical aggregate storage, invisible automatic byte-budget retention, artifact field/owner/retention registry, offline archive cutover; LOCK-016/017) and ADR-0001 marked Superseded (metadata/supersession links only; historical decision body preserved). ADR-0004 updated (direct reconstruction includes the clean P4.2 storage cutover with no compatibility entitlement). Storage spec rewritten as the implementation source of truth for the new target (current state with the 2026-08-14 read-only aggregate measurement ~24.56 GB DB / ~17.96 GB event payload / ~99.2% full update snapshots / disk ~91-92% / growth derived from the two dated observations ≈ 7.5 GB per 7 days, with ~1 MB/min labeled as a separate short active sample, no content read; operational containment as a pending bounded manual operation, not a phase gate; target model; cutover; storage work units S0..S5 under P4.2a; gates/tests; terminology). Runtime spec updated (storage foundation in scope, ownership row, R9 changefeed and R11 storage interaction, P4.2a-before-P4.2b ordering and P4.2 gates, R15-R17 open required by P4.2 and not P1-P3 blockers, maintenance performance guardrails, legacy storage reader/writer removal section 8.2). Direction spec updated (H-10/H-11/H-12, bounded-architecture storage bullet, terminology note, final-H-parity-only compatibility, ADR-0005 links). Tracker updated (bootstrap links, P4 overview/checklist with P4.2a/b, R15-R17 rows, storage metrics table, legacy storage reader/writer removal evidence, DB-exhaustion/containment/cutover risk rows, links, change log). P0 stays Complete; P1-P5 Not started; ADR-0002/0003, inventory, evidence, and canonical docs unchanged; no commit or push | Manifestor execution of storage-architecture-integration documentation task |
| 2026-08-14 | Storage-document audit corrections accepted: fresh canonical DB semantics made explicit and unambiguous — the P4.2 cutover boots an empty canonical DB with no pre-cutover sessions; the operational-containment last-week set remains only in the offline legacy archive and is not migrated/read by the new runtime; "retained sessions" / lossless-continuation wording in ADR-0005 and the storage spec scoped to canonical-era runtime-retained sessions (ADR-0005 I-8/I-9/I-10; storage spec sections 1.3, 4.1, 4.3, 5.1, 6.2). Revert snapshots reconciled as project-scoped/shared: not deleted as part of one session family; artifact registry assigns ownership/GC (project-level reachability/refcount) separately, no silent orphaning (ADR-0005 I-6; storage spec sections 3.3, 5.3, 5.4). H-10 target wording corrected to sessions + registered artifacts persist/resume with the bounded changefeed non-authoritative and not H-10 history (direction spec section 6; tracker section 6). S4/P4.2 ordering corrected: S4 verifies storage-side bounded changefeed ordering/truncation/idempotency; wire-level R9 convergence is the P4.2b gate (storage spec sections 7, 8.2). R9 blocker wording aligned to "Required by P4.2 only; not a P1-P3 blocker" (runtime spec and tracker section 9). Growth provenance corrected: both dated observations preserved, derived interval/rate method stated (~7.5 GB/7 days from 16.9 GB on 2026-08-07 to 24.56 GB on 2026-08-14), ~1 MB/min labeled as a separate short active sample, and the experimental-flags probe recorded as current-state probe evidence with date/method, not a repo-cited proof (ADR-0005; storage spec sections 2.2, 4.1; tracker sections 8, 10). Cutover archive set made explicit and R17 distinguishes the operational containment archive from the P4.2 cutover archive/rollback authority, including `event_sequence`, session-owned sidecars, and inert `session-export.db` disposition (storage spec sections 3.3, 6.1, 6.4; runtime spec and tracker section 9 R17). No stale durable question references remain in the doc set. P0 stays Complete; all resolved decision texts unchanged; no commit or push | Manifestor execution of storage-document audit-correction task |
| 2026-08-14 | Q1 (topic meaning) resolved as a durable decision: in P1 Topic is a derived navigation concept — each root session defines one Topic; root session ID is stable Topic identity; root title is the label; descendants belong through parentID; activity is max member updatedAt; order descending with deterministic ID tie-break; orphan/missing-parent/cycle components degrade to independent Topics; Topic selection/expansion is presentation state only; no independent Topic persistence/API/config/schema/metadata. Direction spec: implementation status row (section 1.2), terminology row (section 4), target surface (section 5), ownership (section 7), bounded architecture (section 8), risk row (section 13), and section 14 question 1 marked RESOLVED (decision text recorded). Tracker: Session Bootstrap, Q1 decision row (section 9) resolved with owner/date/decision, section 9 preamble updated, and P1 phase overview/status/checklist/next actions updated to reflect that Q1 is decided and P1 implementation may begin. P1 stays Not started with no implementation evidence; P2-P5 untouched; no navigation implementation, runtime evidence, parity, or completion claim (LOCK-013/014); ADR-0002/0003/0004/0005, canonical architecture docs, runtime spec, inventory, evidence, and source/tests unchanged; no commit or push | Manifestor execution of Q1-decision record task |
| 2026-08-14 | P1 (Orchestration-first navigation) marked Complete: Topic grouping/navigation implemented over the migration bridge as a derived read-model (`packages/kilo-vscode/webview-ui/agent-manager/topics.ts` — root sessions define Topics, descendants belong via parentID, activity is max member updatedAt, activity-descending order with deterministic ID tie-break, orphan/missing-parent/cycle degradation, selection/expansion presentation-only per Q1). Evidence recorded: topics unit tests (`test: packages/kilo-vscode/tests/unit/topics.test.ts`); Topic-navigation Storybook Playwright interaction spec, 7 passed (`test: packages/kilo-vscode/tests/topic-navigation.spec.ts`); agent-manager arch/i18n tests (`test: packages/kilo-vscode/tests/unit/agent-manager-arch.test.ts`, `test: packages/kilo-vscode/tests/unit/agent-manager-i18n.test.ts`, `test: packages/kilo-vscode/tests/unit/agent-manager-i18n-split.test.ts`); Extension Host lifecycle-convergence E2E covering the extension-owned view boundaries — session navigation, panel close/reopen, webview reload — on the migration bridge (`test: packages/kilo-vscode/tests/e2e/runner.ts`, `test: packages/kilo-vscode/script/e2e-probe.ts`; `test:e2e:topic-navigation` passed, full `test:e2e` passed); descriptive scenario-10 session-switch comparison recorded (`doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-14T11-45-17-512Z/benchmark.jsonl`, n=5 min 154 / median 177 / p95=max 724 / mean 291.2 ms vs P0 baseline `2026-08-12T07-21-22-201Z` n=5 min 115 / median 195 / p95=max 282 / mean 201.6 ms; drift recorded — git 7a768502dd→99d275a9d7 both dirty, VS Code 1.132.0→1.133.0, changed CLI snapshot — one 724 ms outlier; no structural anomaly, no performance conclusion, R7). Q1 presentation-state wording corrected to remove `/filter` where it falsely implied an implemented presentation filter — selection/expansion remain presentation-only (direction spec sections 4/5/7/14; tracker section 9). Tracker: Session Bootstrap, phase overview, P1 table row, P1 checklist/evidence/next actions, P1 campaign narrative (section 4), risk entries (section 10), change log. Changeset `kilo-code` minor added for Topic grouping/navigation. P2-P5 stay Not started; H-1..H-13 target-surface parity stays Not proven (section 6); removal inventory unchanged (section 7); canonical architecture docs unchanged (LOCK-013; architecture checker 0 signals; Topic navigation is a presentation read-model over existing runtime/session facts — no boundary/state/lifecycle/persistence/protocol change); no commit or push | Manifestor execution of P1-completion evidence task |
| 2026-08-15 | P5 startup acceptance tightened to falsifiable gates: runtime spec section 6 rewritten to six normative criteria — (1) selector availability independent of worker lifecycle (not started, deliberately unavailable/withheld/killed/failed, starting, reconnecting, failed); (2) selection never disabled by readiness; runtime-required actions gated individually with explicit state and reason; (3) external canonical-file/index reconciliation preserves the last-known valid derived index or an explicit invalid/stale presentation and never reintroduces a worker-readiness dependency or blanks/disables selectors; (4) structural absence of `extensionDataReady`, port-detection/health/SSE/generated-SDK selector fetch chain, and removed startup contributors proven after P4.4/P4.5; (5) P5 objective evidence includes a real Extension Host scenario with the worker withheld/killed/failed and selectors still rendered/interactive plus descriptive paint-before-ready timing and provenance (R7); (6) cold and warm activation distinguish UI selector readiness from runtime action readiness. Clarifications added: worker startup may still take time but must not block selector/navigation UI, and the target removes redundant connection stages rather than shortening a spinner; Q5 presentation cannot gate selector availability; R3 numeric SLA stays open with behavioral/structural gates mandatory regardless. Section 2.6 target row, R3/R10 bounded-by cells, section 10.7/10.9/10.10/10.11 performance references updated for consistency. Tracker: P5 scope/exit checklist and phase overview rewritten, Session Bootstrap startup-acceptance link, persisted-selector paint metric row and section 8 performance note, R3/Q5/R10 decision rows, risk log row, change log entry. P2-P5 remain Not started; no implementation, completion, or parity claim; no numeric SLA introduced; ADR locks and canonical architecture docs unchanged; no commit or push | Manifestor execution of P5-acceptance-tightening task |
| 2026-08-15 | P2 (Harness surface parity) marked Complete: final durable target-surface campaign recorded under `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-15T08-35-00Z-p2/` — four sub-campaigns (real-completed, real-session, real-overflow, real-restart), each a complete validated manifest (`validated: true`, `status: complete`, `success: true`, no missing/malformed), 135 manifest entries covering 127 unique files, all 127 unique files hash-verified, 28/28 generation requests run-owned `e2e-local/e2e-model` (real-completed final 17, real-session final 4, real-overflow final 4, real-restart final 3; 0 matrix violations), 12/12 UI-send pins pass (real-completed 8, real-session 2, real-restart 1, real-overflow 1). H-1..H-13 target-surface parity proven on the migration bridge for the in-scope current-behavior capability (section 6): H-1 custom agent, H-2 delegation with subagent flow-back, H-3 user tool + artifact, H-4 skill, H-5 stdio MCP with exact-PID disconnect cleanup, H-6 permission/question docks with pending-state observation, H-7 persisted parentID/navigation after panel reopen, H-8 two concurrent sessions with independent production abort, H-9 per-session model/variant pins, H-10 transcript+artifact persistence across true window/Extension Host restart, H-11 all five lifecycle boundaries (panel close/reopen, reload, session switch, SSE transport reconnect, exact worker restart), H-12 byte-exact rollback + Redo All, H-13 threshold compaction + summary + synthetic continuation with no user-facing context-management UI. Lifecycle evidence files: `real-restart/rr-conn.json` (transport reconnect), `rr-kill.json` + `rr-reconnect.json` (exact worker restart pid 53824→54774), `rr-reload-executed`/`rr-reloaded` (reload), `rr-dom-evidence`; panel reopen in `real-session/real-reopen-ready` + `real-completed/real-completed-reopen-ready`. Supporting descriptive backend campaign `2026-08-15T06-18-28-167Z/backend.jsonl` (scenarios 6/7/9, 1 warmup + 5 measured each, 18/18 ok, git f91d22ae1f, dirty) recorded in section 8 (prompt submit, add/update streaming, tool/permission rows carry P0-baseline and 2026-08-15 values with drift and no improvement/regression claim; target-surface SSE handling n=655 span records min 0/med 0/max 1 ms and webview lifecycle paint wd records from `real-completed/run.log`; per-event webview render-flush not emitted and model HTTP duration not separately measured — recorded as non-independent P2 blockers per runtime spec section 10.11). Two pre-final P2-era evidence dirs retained but excluded with provenance notes: `2026-08-15T06-25-37Z/` (contaminated — external gateway `kilo/kilo-auto/small` title generation) and `2026-08-15T07-35-17-745Z/` (copy-race — final-window artifacts reconstructed). VS Code validation: typecheck/lint/knip pass; unit 4410 pass/1 known-baseline failure in pull-back-contract reproduced identically at isolated P1 SHA 99d275a9d7. Tracker: Session Bootstrap, phase overview, P2 table row, P2 checklist/evidence/next actions, section 6 H-1..H-13 rows + intro, section 8 performance rows + narrative, risk log, change log, links. P2 claims no P3 removal, no LOCK-006 final provider-removal completeness, no P4 private-runtime/config parity, and no performance improvement/regression; canonical architecture doc `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` updated in this P2 work unit to document the implemented env-gated fixture bridge; the 2 high + 3 medium architecture signals are covered by that update; source-link extraction produced no diff; no commit or push | Manifestor execution of P2-completion evidence task |
| 2026-08-15 | P3 (Product removal) activated and P3.1 (ordinary single-chat sidebar, LOCK-001) marked Complete: the `kilo-code.SidebarProvider` Activity Bar webview view under `kilo-code-ActivityBar`, its manifest contributions (container/view/menus/`sidebarTitle.*` commands/`sidebarVisible` context), its webview-view registration/resolve path, its sidebar-exclusive i18n keys, and the `TITLE_BUTTON_CLICKED` telemetry event name (emitted only by the removed sidebar title-button wrappers; no remaining emitter) are permanently removed; chat commands that used the sidebar fallback route through `resolveChatTarget`/`ensureChatTab` to the preserved editor surfaces (active tab or Agent Manager) with readiness-gated delivery. Evidence: `test: packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` (static manifest/source/residue absence contract, preserved-surface survival, command re-routing), `test: packages/kilo-vscode/tests/unit/chat-target.test.ts` (target preference, cold-open readiness, no-post-on-failure), focused real Extension Host `test:e2e:sidebar-removal` passed (`test: packages/kilo-vscode/tests/e2e/runner.ts` `assertSidebarRemoval` + `test: packages/kilo-vscode/script/e2e-probe.ts`); docs `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Chat surfaces (P3.1 sidebar removal); Rollback path = git revert of the removal commit(s), no feature flag/shim); release note `doc: .changeset/remove-sidebar-chat.md` (minor, not modified here). Removal inventory sidebar row completed per category (source/tests/docs/config/i18n/build-package; generated SDK `-` by nature — no SDK contract change). Reconstruction registry gained its first entry RC-P3.1-001 (sidebar-title telemetry, disposition `drop`; inherent sidebar surface/view/title/context/assets and migrated command behavior recorded as not candidates). Decisions Q2/Q4/R4 resolved 2026-08-15 (no numeric adoption threshold; P1/P2 supplied migration affordances). Section 8: static complexity deltas recorded (332/250/6, each delta 0, shared editor-tab/Agent Manager bundle + no SDK contract change; P3.2-P3.4 deltas pending) and a descriptive scenario-1 cold-start campaign recorded (`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl`, git 67436bdf70 dirty, VS Code 1.133.0, 1 warmup + 5 measured, status ok; activate 24/32, `spawnToPortMs` 9407/13684, `connectToSseConnectedMs` 9535/13844, `activateToDataReadyMs` 16772/21748, `webviewLoadToPaintMs` 1175/1215, `backendEntryToListenerMs` 1330/2524) vs the P0 cold-start baseline (`2026-08-11T11-51-16-883Z`, git 461781ffc0, VS Code 1.132.0) with drift recorded (VS Code 1.132.0→1.133.0, git 461781ffc0→67436bdf70 both dirty, CLI snapshot changed) — activation/render moved negative, backend/transport moved positive, descriptive only, no improvement/regression claim (R7). Full unit suite latest: 4449 pass with one known pre-existing pull-back-contract failure; typecheck/lint/knip pass; an unrelated auto-approve flake is not a P3.1 failure. P3 stays Active, never Complete; P3.2-P3.4, P4, P5 remain Not started; no commit or push | Manifestor execution of P3.1-completion evidence task |
| 2026-08-16 | P3.2 (Product removal — managed worktree infrastructure + custom Diff Viewer/Diff Virtual, LOCK-002) marked Complete: the managed git-worktree infrastructure (Agent Manager worktree manager/state/pollers/run/controller subsystem/setup-run scripts/git-transfer/worktree-mode context/BranchSelect/worktree message protocol/i18n/manifest contributions; CLI-side `Worktree` routes, `branch-name` route, `worktree-family`/`worktree-cleanup`, `Worktree`/`BranchName` SDK groups), the custom Diff Viewer/Diff Virtual surfaces (`DiffViewerProvider`/`DiffVirtualProvider`/`SourceController`, `diff-viewer`/`diff-virtual` webview bundles), and the orphaned `agent_manager` list/prompt request-reply service (`agent-manager/service.ts`, `protocol.ts`; `list`/`reply`/`reject` SDK methods) are permanently removed; root-local start delegation (the `agent_manager` tool starts sessions only), the H paths, H-12 SessionRevert/Snapshot rollback, and generic worktree/project/snapshot semantics are retained; P4.4's primary-worktree mirror reads explicitly remain out of P3.2 scope. Evidence: `test: packages/kilo-vscode/tests/unit/worktree-removal.test.ts` (managed-worktree/Diff/branch-name/SDK-group/i18n static absence + retained root-local orchestration), `test: packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts` (custom Diff Viewer/Diff Virtual absence + preserved H-12 rollback/RevertBanner/`@git-changes`), `test: packages/opencode/test/kilocode/agent-manager-absence.test.ts` (orphaned list/prompt request-reply contract absent across source/HTTP/SDK — `kilocode.agent_manager.start` only); focused real Extension Host `test:e2e:worktree-removal` passed via `test: packages/kilo-vscode/tests/e2e/runner.ts` (`runWorktreeRemoval`) + `test: packages/kilo-vscode/script/e2e-probe-worktree.ts` + `test: packages/kilo-vscode/script/e2e-probe.ts`, with the final campaign `doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-09-32-000Z/` (complete validated manifest, 29/29 hashes, 36/36 PASS 0 FAIL, two root-local sessions controllable, H-12 byte exact, 4/4 run-owned model requests, clean resources; no `.kilo/worktrees`/`.kilo/agent-manager.json`). Docs: `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.2 removal; Rollback path = git revert of the removal commit(s)), user docs `packages/kilo-docs/pages/automate/agent-manager.md`, `agent-manager-workflows.md`, and related pages, the builtin `kilo-config` skill doc, and release note `doc: .changeset/agent-manager-root-local-sessions.md` (minor). Removal inventory worktree and custom-Diff-Viewer rows completed per category (generated SDK recorded as the −11 surface reduction). Reconstruction registry gained RC-P3.2-001 (run-script subsystem), RC-P3.2-002 (sibling-worktree recall family), RC-P3.2-003 (Agent Manager overview/targeted-prompt bridge + list-prompt request-reply), all disposition `drop`; inherent managed-worktree creation/import/transfer/branch-naming/sections/multi-version/custom-diff surfaces recorded as not candidates. Section 8: static complexity deltas 332→274 (−58; WebviewMessage 149 + ExtensionMessage 125), 250→239 (−11: Worktree 7 + BranchName 1 + AgentManager 3), webview entries 6→4 (−2); descriptive scenario-1 cold-start campaign recorded (`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl`, git 9d86428cc2 dirty, VS Code 1.133.0, extension 7.4.11, final CLI sha256 `924cb638...`, 1 warmup + 5 measured, 13 n=5 summaries, status ok; activate 26/28, `spawnToPortMs` 5332/6607, `connectToSseConnectedMs` 5380/6682, `activateToDataReadyMs` 8805/12225, `webviewLoadToPaintMs` 1025/1052, `backendEntryToListenerMs` 481/880) vs the P0 cold-start baseline with drift recorded — descriptive only, no improvement/regression claim (R7/LOCK-PERF-6). Earlier P3.2 evidence dirs (`p3-2-worktree-removal-2026-08-15T18-00-47-000Z/`, `p3-2-worktree-removal-2026-08-15T18-11-33-000Z/`) retained as historical, never cited as closing evidence. Typecheck (opencode + kilo-vscode incl. E2E tsconfig), kilo-vscode lint and knip pass; kilo-vscode unit suite 4266 pass / 1 known pre-existing pull-back-contract baseline (`tests/unit/pull-back-contract.test.ts`, unchanged in this work unit); P3.2-focused tests pass (worktree-removal + diff-viewer-removal 37, agent-manager-absence + agent-manager-tool + session-list 29); architecture-impact checker PASS (canonical docs already updated in tree). P3 stays Active, never Complete; P3.3-P3.4, P4, P5 remain Not started; no P3.3/P3.4 removal, P4 private-runtime/config parity, P5 readiness, full H-matrix rerun, or performance conclusion claimed; no commit or push | Manifestor execution of P3.2-completion evidence task |
| 2026-08-16 | P3.3 (Product removal — cloud sessions, JetBrains, Console, KiloClaw, LOCK-003) marked Complete: the four LOCK-003 products are permanently removed. Cloud sessions (extension `cloud-session` handler, `CloudSessionList`, `CloudImportDialog`, cloud prune context, cloud deep link, cloud message protocol; CLI `cloud-session.ts`, `cloud-fork` flags/helpers, cloud import validation; gateway cloud-session endpoints); JetBrains (`kilo-jetbrains` package, JetBrains-only workflows `test-jetbrains.yml`/`publish-jetbrains.yml`/`prepare-jetbrains-release.yml`/`codeql-kotlin.yml` and allowlist entries, turbo tasks, release scripts/skills, container images, CI build filters); local Console (`kilo-console`/`kilo-web-ui` packages, CLI `console` command source/registration/assets, `/console` serve/workspace routing, opencode Console build/copy, Console CI workflow, Console user docs); KiloClaw (`KiloClawProvider`, `kiloclaw` webview tree/bundle, `kilo-code.new.kiloClawOpen` command, KiloClaw TUI route/command/view wiring, KiloClaw message types). Retained shared surfaces asserted intact: remote sessions, generic session import, EventServiceClient/presence, `config-console`/`experimental.console.*`/SDK Console group/core `consoleState`, the current `kilo serve` HTTP/SSE/SDK bridge, and `agentManagerOpen`/`openInTab`/`agentManager.newTab`. Evidence: `test: packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (cloud-session/KiloClaw static absence + E2E wiring), `test: packages/opencode/test/kilocode/cloud-claw-removal.test.ts` (CLI-side absence), `test: packages/opencode/test/kilocode/console-removed.test.ts` (Console removal + shared bridge survival), `test: packages/script/tests/jetbrains-removed.test.ts` (JetBrains package/workflows/turbo/release/CI absence); focused real Extension Host `test:e2e:cloud-claw-removal` passed via `test: packages/kilo-vscode/tests/e2e/runner.ts` (`assertCloudClawRemoval`) + `test: packages/kilo-vscode/script/e2e-probe.ts` + `test: packages/kilo-vscode/script/e2e-probe-launch.mjs` + `test: packages/kilo-vscode/script/e2e-evidence.ts`, with the final campaign `doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-35-000Z/` (complete validated manifest schema `kilo-e2e-evidence/1`, 7 files hash-verified, run `success: true`; `cloud-claw-removal-runtime-evidence` — contributed views `[]`, all forbidden manifest/runtime/bundle hits empty, `kiloCommandTotal` 68 with `forbiddenCommandHits` `[]`, `bundleCount` 76 with `forbiddenBundleHits` `[]`, retained `agentManagerOpen`/`openInTab`/`agentManager.newTab`, Open-in-Tab panel 1 ready + Agent Manager ready, no LLM/external requests, exact PID/CDP/scratch cleanup). Docs: `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Product runtime absence (P3.3 removal); Rollback path = git revert of the removal commit(s), no feature flag/shim), `packages/kilo-docs/source-links.md`, and release note `doc: .changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor); SDK/OpenAPI regenerated. Removal inventory cloud-sessions/JetBrains/Console/KiloClaw rows completed per category; no reconstruction-candidate rows added (inherent LOCK-003 removals, LOCK-015; recorded as not candidates). Section 8: static complexity deltas 274→263 (−11; WebviewMessage 144 + ExtensionMessage 119), 239→234 (−5 SDK methods), webview entries 4→3 (−1, `kiloclaw` bundle retired); removal items with full evidence 3→7; descriptive scenario-1 cold-start campaign recorded (`doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl`, git HEAD 2d61c228e71d6b67db15c2a7ff2be9e728e775e4 dirty, VS Code 1.133.0, extension 7.4.11, CLI snapshot sha256 `229cad7ae3…e5a3b`/size 156821474, 1 warmup + 5 measured, 6/6 ok, 13 n=5 summaries, status ok; activate 24/26/27/25.8, `spawnToPortMs` 4823/5217/5500/5130.8, `connectToSseConnectedMs` 4887/5305/5564/5198, `activateToDataReadyMs` 8357/9096/9439/9063.6, `webviewLoadToPaintMs` 1030/1040/1056/1042.2, `backendEntryToListenerMs` 551/621/654/609.8) vs the P0 cold-start baseline with drift recorded — descriptive only, no improvement/regression claim (R7/LOCK-PERF-6). Validation: SDK generator/lockfile/typecheck/build pass; opencode/kilo-vscode/kilo-gateway/ui/script/sdk typechecks pass; kilo-vscode lint pass; docs full suite 22/22; workflow guard 24 workflows pass; source-links pass/idempotent; architecture-impact pass (20 signals); promise facade pass; focused unit suites pass (combined `cloud-claw-removal`+`e2e-evidence` 41, opencode `cloud-claw-removal` 5, `console-removed` 11, `jetbrains-removed` 7, `check-architecture-impact` 118, regression campaign 18/18 with snapshot race isolated 3/3). Known baselines: pull-back-contract one failure and cli-shutdown mock leak reproduce at pristine P3.2 and are not P3.3 failures; no clean full-suite claim. P3 stays Active, never Complete; P3.4, P4, P5 remain Not started; no P3.4 removal, P4 private-runtime/config parity, P5 readiness, new H-1..H-13 parity campaign (P2 remains the full parity proof; LOCK-008/H-1..H-13 parity intact), or performance conclusion claimed; no commit or push | Manifestor execution of P3.3-completion evidence task |
| 2026-08-17 | P3.4 (Product removal — indexing/semantic search, project memory/injection/tools, user-visible context management/compaction, autocomplete/next-edit/chat autocomplete/FIM/edit, LOCK-004) marked Complete; P3 marked Complete: the four LOCK-004 feature groups are permanently removed — indexing/semantic search (indexing module, semantic-search integration, indexing-worker build entry), project memory (memory injection, memory tools, project-memory module), user-visible context management/compaction (CLI/TUI/ACP/HTTP/SDK summarize+compact commands/endpoints; context management UI controls absent: settings rows 0, header context menu 0, header context actions 0), and autocomplete/next-edit/chat autocomplete/FIM/edit (autocomplete module, next-edit, FIM endpoints, extension autocomplete/FIM surfaces). The invisible automatic internal context-overflow safeguard (H-13, LOCK-005) is retained — H-13 P0 harness 8/8 and automatic overflow suites pass post-removal; generic search/recall retained. CLI build correction: stale indexing-worker entry removed; native build passes. SDK/OpenAPI regenerated twice idempotently; removed 18 generated methods total vs P3.3; final SDK method count 216; no removed endpoints/types remain. Evidence: `test: packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (20 pass / 253 assertions — indexing/memory/context-management/autocomplete/SDK static absence), `test: packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (28 pass / 381 assertions — extension/webview surface absence + retained H-13 evidence), real Extension Host `test:e2e:p3-4-removal` passed (forbidden manifest/runtime/bundle/state hits empty, 0 model requests, Open-in-Tab and Agent Manager ready, cleanup complete); `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` (H-13 P0 harness 8/8 pass + automatic overflow suites). Docs: `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.4 section + Rollback path = git revert of the P3.4 removal commit(s), no feature flag/compatibility shim); `.changeset/p34-docs-update.md` (minor release note); canonical architecture P3.4 section + rollback documented; related canonical/public docs/nav/redirects updated. Accepted native scenario-1 campaign: `doc: specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl` (1 warmup + 5 measured, 6/6 ok, finish status ok, 13 n=5 summaries; key medians: activate 11, spawnToPort 5658, connectToSseConnected 5718, activateToDataReady 9400, webviewLoadToPaint 1244, backendEntryToListener 609, configLoad 597, providerStateInit 322, instanceBootstrap 652 ms); descriptive only, environment/git drift, no performance improvement/regression claim (R7). Excluded partial wrapper campaign `p3-4-final-2026-08-17T00-57-00-000Z/EXCLUDED` (316-byte wrapper, 4/5 measured blocked, non-comparable) explicitly excluded and never cited as accepted benchmark. Accepted removal E2E evidence: `doc: specs/vscode-orchestrator/evidence/p0-baseline/p3-4-final-2026-08-17T00-52-00-000Z/` (validated manifest sha256 d3f6d863b5efadbe0baabc5db2c9f3ea42aae790f3d87e3d618362f9c9c6e002, runtime evidence sha256 746d7c87ff617a393a819c64e55308733f6340293584cf667c91f1c5d8f33bd0). Static complexity deltas (section 8): webview message types 263→250 (−13; WebviewMessage 136 + ExtensionMessage 114), generated v2 SDK public methods 234→216 (−18), webview esbuild entries 3→3 (delta 0), SSE GlobalEvent union 140 (supplemental). Removal inventory rows for indexing, project memory, user-visible context management/compaction, and autocomplete completed per category in section 7 (source/tests/docs/generated SDK/config/i18n/build-package). No new reconstruction-candidate rows (LOCK-015): the removed surfaces are inherent observable features of the LOCK-004 closed-set permanent removals; recording them as candidates would incorrectly reclassify permanent removal scope. Reconstruction-candidate registry rationale updated (section 7). P3 is Complete (2026-08-17); P4 and P5 remain Not started; next action is P4.1 (file-authoritative GUI read/write model per runtime spec dependencies). Full unit suites pass with known pre-existing baselines (pull-back-contract, cli-shutdown); typecheck/lint/knip/docs/SDK/gateway/core guards pass; docs build/typecheck/tests 25/25 pass; architecture-impact checker pass. No commit or push | Manifestor execution of P3.4-completion evidence task |
| 2026-08-19 | S0 (Baseline and fixtures, P4.2a storage foundation) re-audit corrections applied: (1) `du` file-store command separated from sqlite3 SQL rendering — `section10FileStoresCommand()` returns the exact `du -sh` shell string without sqlite3 wrapping, and `section10Sqlite3Command()` wraps only SQL fragments; (2) `TestHoldToken` class introduced with observable `isHeld`/`isReleased` state, idempotent `release()`, and `assertHeld()`/`assertReleased()` assertion methods — hold lifecycle asserted in 6 dedicated tests; (3) builders register `Effect.addFinalizer` immediately after session creation so partial construction failures clean session/sidecar/status state; (4) unused `expectedMessages`/`expectedParts`/`expectedPartTexts` cardinality metadata removed from builder returns — assertions use direct service API reads. Frozen measurement commands asserted exactly (5 SQL commands + 1 du command = 6 exact string assertions + 2 dispatch tests). Verification: `bun test ./test/storage/s0-baseline-measurement.test.ts` from `packages/opencode` (29 pass); `bun run typecheck` from `packages/opencode` pass; md-table-padding pass; architecture-impact review found a high HTTP signal, reviewed as an internal migration-bridge classification with no target/public boundary change, so no canonical architecture docs update. P4 remains Active; P4.2a Active; S1 complete; S2-S5 not started; R15-R17 open. No changeset (test/internal tracker-only foundation) | Manifestor execution of S0-re-audit-corrections task |
| 2026-08-19 | S1 (Canonical aggregate schema and revision commit model, P4.2a storage foundation) marked Complete: `SessionTable.revision` migration `0→1` defaults existing and new rows to `0`; `SessionRevision.advance` is the sole revision helper and uses `UPDATE RETURNING` to prove one affected row. Prompted/todo/import/project writers advance revisions atomically; JSON migration uses per-family savepoints with rollback/release error handling; post-projector rollback and real HTTP route negative evidence are covered. Evidence: `packages/core/test/session-revision.test.ts`, `packages/opencode/test/session/session-revision.test.ts`, `packages/core/test/database-migration.test.ts`, `packages/opencode/test/kilocode/session-import-service.test.ts`, `packages/opencode/test/kilocode/server/session-import-http.test.ts`, `packages/opencode/test/project/project.test.ts`, `packages/opencode/test/cli/import.test.ts`, `packages/opencode/test/kilocode/storage/json-migration.test.ts`; verification: bounded opencode tests (138 pass), core tests (29 pass), and `bun run typecheck` from `packages/core` and `packages/opencode` pass. S2-S5, P4.2b-P4.5, retention, artifact registry, outbox/changefeed, cutover, and private wire remain unclaimed; R15-R17 remain open. Architecture impact: high HTTP signal reviewed; internal migration-bridge error classification and the already-specified storage revision model do not change a target/public boundary, so no canonical docs update. No changeset; no canonical doc edits | Manifestor execution of S1-completion tracker update |
| 2026-08-18 | P4.1 (File-authoritative GUI config implementation) marked Complete; P4 marked Active; R10 resolved. Canonical config service (`CanonicalConfigService`) lifecycle-owned singleton with file watchers, materialization, SecretStorage adapter, derived selector indexes. Two canonical authored scopes: global `<globalRoot>/kilo.jsonc` (resolved via `Roots` class from platform home, `packages/kilo-vscode/src/config/paths.ts`) and project `<workspaceRoot>/.kilo/kilo.jsonc`. Closed 11-field JSONC registry (`CLOSED_JSONC_FIELDS` in `packages/kilo-vscode/src/config/registry.ts`) with complete entry metadata. Typed asset directories (agent, command, skill, tool, plugin, rules) as separately registered canonical asset classes — not top-level JSONC fields. SecretStorage opaque refs (`secret:kilo.credentials.<scope>.<kind>.<id>`). Atomic WYSIWYG writes with comment preservation, stale-write detection, final CAS, cross-scope validation, invalid-edit recovery. Derived provider/agent/model indexes. Canonical mode/readiness contract. Validation: typecheck/lint/knip/606 focused tests/compile/fresh bundle/Storybook/architecture-impact (1 medium signal)/source-links/md-table-padding. Independent source audit PASS (0 findings). Environmental caveat: Playwright Chromium absent/timed out. Full unit suite: 4805 pass / 4 fail / 1 error (happy-dom/Bun baseline). Docs: `doc: packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (canonical config section). No private worker/storage, legacy-reader cutover, HTTP/SSE/SDK removal, or CLI deletion claimed; CLI remains migration bridge; P4.2-P4.5 Not started. Tracker: Session Bootstrap, Phase Overview, P4.1 evidence subsection (section 5), R10 resolved (section 9), change log, risk log | Manifestor execution of P4.1-completion task |
| 2026-08-20 | S3 (Artifact field/owner/retention registry, P4.2a storage foundation) marked Complete: closed `Storage.write`/`update` registry boundary including empty prefixes (`packages/core/src/retention/artifact.ts`, `packages/opencode/src/storage/storage.ts` via `assertFamilyWrite`/`assertFamilyWriteEffect` and `Storage.assertRegistered` — empty prefix throws `UnregisteredArtifactError`); AST writer/migration audit (`packages/opencode/test/storage/s3-audit.test.ts` 8 pass) proves every production `Storage.write`/`update` uses a registered prefix and storage migrations are explicit dispositioned destinations; snapshot pruning is project live-ref aware — `packages/opencode/src/snapshot/live-collector.ts` extracts live hashes from project-scoped `Session.revert.snapshot` and snapshot-bearing `Part` rows, `packages/opencode/src/kilocode/snapshot/materialize.ts` `prune` retains aged live refs and deletes aged unreferenced refs, `packages/opencode/src/snapshot/cleanup-decision.ts` skips prune when DB missing or `fetchLiveHashes` fails and proceeds when live set present; family retention remains separate and `snapshot` stays project-owned not family-pruned. Evidence: `packages/opencode/test/storage/s3-audit.test.ts` (8 pass), `packages/opencode/test/storage/s3-write-boundary.test.ts` (8 pass), `packages/opencode/test/snapshot/snapshot-prune-live.test.ts` (3 pass), `packages/opencode/test/snapshot/snapshot-live-db.test.ts` (2 pass), `packages/opencode/test/snapshot/snapshot-cleanup-decision.test.ts` (3 pass), `packages/opencode/test/kilocode/snapshot-seed.test.ts` (7 pass), `packages/opencode/test/snapshot/snapshot.test.ts` (60 pass / 1 skip) with DB-aware `cleanWithDb` fixture correction; `bun run typecheck` from `packages/core` and `packages/opencode` pass; `bun run script/check-opencode-promise-facades.ts` pass; root lint 0 errors, remaining warnings descriptive; full final validation to rerun after docs (audited pass; focused suites now pass after seed fixture correction; inspector stale occurrences corrected). Tracker: Session Bootstrap, Current phase, P4 overview/table, P4.1 no-claims/next-actions, P4 status, P4.2a checklist (S3 MET), P4/P5 next actions, change log, and canonical docs corrected. S4/S5 remain Not started; next boundary S4 (bounded changefeed/outbox) | Manifestor execution of S3-completion tracker/docs/changeset task |
| 2026-08-20 | S4 (Bounded changefeed/outbox, P4.2a storage foundation) marked Complete (2026-08-20): storage-side bounded payload-free changefeed with global monotonic seq ordering across sessions, per-revision exactly-one changed entry, payload-free tombstone with final revision+1 and no FK cascade, 50,000 rows/64 MiB hard caps with oldest-first eviction including unacknowledged rows, ack truncation with CursorAheadError, gap→rehydration on any invalid/evicted/truncated/interior-gap/missing-tail cursor, idempotency via (session_id, revision, kind) with no seq gap (including concurrent duplicate), transaction rollback leaves revision/feed/state unchanged, invalid kind rejected at typed/runtime/DB trigger, reconstruction independence, actual 50,001-row production cap and rerun, canonical reads intact after truncation/eviction, ordered migration <30s, storage-side only — wire-level R9 stays P4.2b. Evidence: `packages/core/test/changefeed-s4.test.ts` (22 pass), core regressions `packages/core/test/session-revision.test.ts` + `database-migration.test.ts` + `retention-s2.test.ts` (37 pass), production project `packages/opencode/test/project/project.test.ts` (39 pass), opencode storage/revision `packages/opencode/test/storage/storage.test.ts` + `session/session-revision.test.ts` + `storage/s0-baseline-measurement.test.ts` + `kilocode/storage/json-migration.test.ts` (38 pass aggregated), `packages/core/src/retention/changefeed.ts`, `sql.ts`, `database/migration/20260821000000_add_changefeed_bounds.ts`, `database/migration/20260822000000_add_changefeed_kind_check.ts`, `session/revision.ts`, `retention/retention.ts`; verification: `bun test ./test/changefeed-s4.test.ts` from `packages/core` (22 pass), `bun test ./test/session-revision.test.ts ./test/database-migration.test.ts ./test/retention-s2.test.ts` from `packages/core` (37 pass), `bun test ./test/project/project.test.ts` from `packages/opencode` (39 pass), opencode aggregates (38 pass), `bun run typecheck` from `packages/core` and `packages/opencode` pass; audit 2026-08-20 final pass-with-findings, 0 blockers; `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md` already documents storage-side bounded changefeed (no doc edit required); no changeset warranted (storage-only), no source-link change. Final independent delivery-gate 2026-08-20 verified after initial tracker update: Core matrix 59 pass; OpenCode listed suites pass; S3 audit first default hook timeout (environmental) then 8 pass with `--timeout 30000`; snapshot combined 60 pass/1 skip under 180s (long-runtime baseline accurately recorded); core/opencode/turbo typecheck 17/17 pass; root lint 0 errors/6986 warnings; guards `check-opencode-promise-facades`/`check-workflows`/`check-md-table-padding`/`extract-source-links`/`check-architecture-impact` pass (source-links idempotent; architecture-impact 1 medium session-contract signal reviewed, no doc update); final audit 0 blockers; no validation-owned process/port. Tracker: Session Bootstrap, Current phase, P4 overview/evidence row, P4.1 no-claim/next-actions, P4/P4.2a/S4 checklist (S4 evidence now includes final gate), P4/P5 next actions, risk/change log (this row appended final gate). P4 remains Active, P4.2a remains Active because S5 Not started; S4 Complete, S5 Not started; P4.2b-P4.5 and P5 remain Not started — still no wire/P4.2b/S5 claim, no performance claim | Manifestor execution of S4-completion tracker task |
| 2026-08-21 | S5 (Offline cutover, P4.2a storage foundation) marked Complete (2026-08-21) for the offline cutover work unit only — archive integrity, fresh DB identity/zero gate, rollback rehearsal/recovery, lease/marker safety, and focused production-path evidence. Archive: offline versioned dir under same-filesystem sibling `<data-basename>-archive/p4.2/<UTC>-<uuid>/` with temp under same `p4.2` parent and EXDEV fail-closed; manifest v1 with sorted path/bytes/SHA-256 and deterministic aggregate hash, preserved POSIX relative paths, `wal_checkpoint(TRUNCATE)` + `integrity_check` + `foreign_key_check`, fixed members `kilo.db`/`kilo.db-wal`/`kilo.db-shm` + `storage/session_diff`/`session_diff_base`/`session_share` + `session-export.db` WAL/SHM when present else absence recorded, `snapshot` excluded, fsync files/dirs + reverify + atomic rename + fsync parent, any failure leaves legacy active; fresh DB singleton identity (UUID, schema version, creation time, cutover archive ID) with boot gate verifying identity and zero sessions/artifacts before activation; rollback offline only (stop/close, verify archive, separately archive current store under distinct rollback ID, atomically restore legacy members, rerun integrity checks, boot legacy path) with deletion requiring separate explicit maintainer authorization, never automatic. Focused production-path evidence: core focused 52 pass (hardened 16 and S5/lease 22 included), `packages/opencode/test/storage/cutover-s5-production.test.ts` 8 pass under `bun test --timeout 40000` (97 expects), `packages/opencode/test/storage/session-export.test.ts` + respawn/S2 22 pass, `bun turbo typecheck` 17/17 pass, lint 0 errors, `bun run script/check-architecture-impact.ts --worktree` 0 signals, `bun run script/check-md-table-padding.ts` pass, `bun run script/extract-source-links.ts` pass (unchanged), `bun run script/check-workflows.ts` pass, `bun run script/check-opencode-promise-facades.ts` pass, guards pass, final independent audit PASS (0 blocker, 0 acceptable-risk, 0 style/speculative); cleanup 686 rehearsal archives/backups/markers + 10 debug roots + validation logs cleaned, no test-owned processes/ports/leases/markers remain, real DB and active Kilo processes preserved, source-links unchanged, no real user DB deleted/rolled back and rehearsal artifacts cleaned; real production DB observed with S5 migration table/empty identity row during investigation only, not claimed as S5 test evidence and not intentionally cut over; known baselines: core full 1143 pass/26 skip/16 fail (1 known config v1 baseline, 14 known flaky EventV2, 1 environmental LocationServiceMap timeout) with no new S5 failures. Earlier S5 implementation commit 4a763a6 was S4 only and remains unrelated; current S5 work is uncommitted; no fabricated issue/PR links, no changeset, no commit in this task. P4.2a storage cutover work units S0..S5 Complete; P4.2 H-10/H-11 post-cutover Extension Host five-boundary convergence and remaining private-runtime work remains pending before P4.2b; P4.2b-P4.5 and P5 remain Not started; tracker updated: Session Bootstrap, Current phase, P4 overview/evidence row, P4.1 no-claim/next-actions, P4/P4.2a/S5 checklist (S5 MET), P4/P5 next actions, risk/change log (this row) | Manifestor execution of S5-completion tracker task |

| 2026-08-22 | P4.2 H-10/H-11 post-cutover five-boundary convergence marked Complete (2026-08-22): conjunction of real-restart transport reconnect + worker restart + window/reload and real-session session switch + panel close/reopen validated=true/success=true against canonical storage foundation (zero-state/archive stability, materializationReady v4, XDG/`$schema`/variant parity); durable secret-safe evidence `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-22T09-00-53Z-p4.2-h10h11/` (real-restart 31 files + real-session 25 files, manifest.redacted.json/provenance.json, no e2e-fixture-key, no rr-model-requests/run.log) from sources `/tmp/kilo-e2e-real-restart-20260822T165842-fresh` (33 files d4… validated true) and `/tmp/kilo-e2e-real-session-20260822-165433-64916-fresh` (27 files d3… validated true); explicit exclusions remain: private wire/R1/R9, P4.2b, legacy reader deletion/import, HTTP/SSE/SDK removal, P4.3/P4.4/P4.5, real-user DB cutover; do not claim P4.2 overall complete; tracker P4 overview/status/checklist/evidence/next-action and H-10/H-11 rows updated; changeset revised to 12 fields including `$schema` and four parity changes | Manifestor execution of P4.2 H-10/H-11 evidence and tracker/changeset update task |
| 2026-08-22 | P4.2b-1 R1 private transport scaffold marked Complete (2026-08-22) scaffold-only — additive JSON-RPC 2.0 over stdio Content-Length (FrameDecoder/encodeFrame, binary-safe, pipelined, malformed → ParseError -32700 + closed), initialize handshake exactly-once (5s timeout → closed), bounded stderr via StderrTail (16 KiB/100 lines), EOF/exit lifecycle (closed + rejects pending -32603); platform-correct spawn via `src/util/process.ts`, packaged artifact `dist/private-worker/worker.js` via workerCtx in `esbuild.js`; source: `packages/kilo-vscode/src/private-worker/frame.ts`, `json-rpc.ts`, `peer.ts`, `host.ts`, `worker.ts` + `packages/opencode/src/private-worker/frame.ts`, `json-rpc.ts`, `peer.ts`, `worker.ts`; test: `packages/kilo-vscode/tests/unit/private-worker.test.ts`; scaffold-only, not wired to production runtime, LOCK-009 HTTP/SSE/SDK bridge remains, no legacy reader deletion/import, no HTTP/SSE/SDK removal, no P4.3/P4.4/P4.5 work, no real-user DB cutover; R1 is resolved as above, R9, R11-R14 remain open, required for P4.2 exit (runtime spec section 9); does not claim P4.2 complete; canonical docs unchanged (LOCK-013); no commit or push | Manifestor execution of P4.2b-1 R1 scaffold tracker update |
| 2026-08-22 | P4.2b R9 observation wire foundation marked Complete (2026-08-22) — protocol foundation only: additive protocol-level R9 observation contract/controller over private JSON-RPC carrier (no production wiring, no storage integration/private-worker lifecycle convergence; R9 Resolved 2026-08-23 pending that convergence; R11-R14 remain Open; P4.2 remains Active, not complete). Versioned envelope `1.0` with `observation/snapshot`, `observation/read`, `observation/ack`, `observation/subscribe` requests and `observation/changed` notification; payload-free `seq/session_id/revision/kind/time` with closed kind `changed | deleted` (ADR-0005), cursor/gap→rehydrate, InvalidParams validation including explicit undefined cursor and invalid kind before response/notification, duplicate idempotent via cursor/seq, explicit rehydrate on stale/gapped cursor never fabricated deltas; injected controller boundary `ObservationController` with callbacks `getSnapshot/readAfter/ack`; subscribe is a foundation cursor-establishment hook, not production push registration; peer InvalidParams preservation fixed for observation validation; source: `packages/opencode/src/private-worker/observation.ts` ↔ `packages/kilo-vscode/src/private-worker/observation.ts` (closed kind, invalid-kind rejection, explicit undefined cursor validation) + `packages/opencode/src/private-worker/peer.ts` + `packages/kilo-vscode/src/private-worker/peer.ts`; test: `packages/kilo-vscode/tests/unit/observation.test.ts` (now incl. closed-kind and explicit-undefined-cursor validation) + `packages/opencode/test/private-worker/observation.test.ts` (parity) with actual JsonRpcPeer implementation, plus `private-worker.test.ts` unchanged; typecheck pass both packages, knip pass; additive, no DB/migration/timer/polling/unbounded buffer/payload delta, no selector/readiness wiring, no Failure/Outcome/Recovery, no HTTP/SSE/SDK removal; five-boundary convergence remains unproven; tracker and runtime spec R9 row reworded to protocol foundation only; no commit or push | Manifestor execution of P4.2b R9 wire foundation task (audited 2026-08-22 corrections applied) |
| 2026-08-22 | P4.2 observation storage adapter (protocol + canonical changefeed bridge + private-worker routing) — storage-backed private observation increment toward R9, R9 Resolved 2026-08-23 pending lifecycle convergence. Bridges canonical `Changefeed` Effects (`currentCursor`, `readAfter`, `ack`, `getState`, payload-free `seq/session_id/revision/kind/time`, bounded 50k/64MiB eviction, `CursorAheadError`→`InvalidParams`) to Promise `ObservationDeps` via `packages/opencode/src/private-worker/changefeed-adapter.ts` ↔ `packages/kilo-vscode/src/private-worker/changefeed-adapter.ts` (no second store; canonical aggregate remains authoritative, reconstruction independent of changefeed per ADR-0005). Routes `observation/snapshot | read | ack | subscribe` through private-worker `startWorker({ observationDeps })` in both package copies (parity) preserving `initialize`/`ping`/`echo`/`error`, JSON-RPC error codes `-32700/-32600/-32601/-32602/-32603`, lifecycle cleanup, HTTP/SSE/SDK bridge, selector/readiness unchanged, no new persistent ledger, no Failure/Outcome, no global rebuild. Host forwards `observation/changed` notifications via `PrivateWorkerHost` `onNotification`. Tests: `packages/opencode/test/private-worker/observation-changefeed.test.ts` (6 pass, real `:memory:` DB, snapshot/read/ack-gap/ack-ahead→InvalidParams/payload-free deleted/kind/reconstruction independence/notification via real `JsonRpcPeer`, plus EOF cleanup) + `packages/kilo-vscode/tests/unit/observation-changefeed.test.ts` (6 pass parity + host notification) = storage-backed routing/adapter proven with real DB; existing `observation.test.ts` parity (21 pass each) and `private-worker.test.ts` unchanged still pass; typecheck pass both packages. Additive only; does not claim five-boundary production observation convergence; does not claim R9 complete; P4.2 remains Active; R11-R14 remain Open; lifecycle convergence (transport reconnect/worker restart/panel lifecycle with snapshot/revision convergence) remains unproven and required before R9 can close; blocker for full production wiring is the private worker owning the canonical DB lease — current adapter is injectable (`createChangefeedDeps(db)`) and does not itself acquire the lease, so production spawn still requires a recorded runtime owner decision (reported, not invented) | Manifestor execution of P4.2 observation storage adapter task |
| 2026-08-22 | P4.2b standalone lease bootstrap + real-child observation proof — additive gated file-backed canonical DB lease ownership. Both private-worker copies (`packages/opencode/src/private-worker/worker.ts` ↔ `packages/kilo-vscode/src/private-worker/worker.ts`) add opt-in bootstrap gated by `KILO_PRIVATE_WORKER_STANDALONE=1` + absolute `KILO_DB`, using existing leased `Database.layerFromPath` (lease acquisition, marker/identity checks, `isAbsolute` validation) and `createChangefeedDeps` to wire `observation/*` (`snapshot`/`read`/`ack`/`subscribe` v=1.0, `observation/changed`, InvalidParams on bad cursor) through the same `startWorker({ observationDeps })` path preserving `initialize`/`ping`/`echo`, JSON-RPC codes, bounded stderr, EOF semantics, and leaving production HTTP/SSE/SDK/selector untouched. Lifetime cleanup on worker dispose/EOF/SIGINT/SIGTERM via `ManagedRuntime.dispose` releases the lease. No second store, no new DB tables, no polling/timer, no Failure/Outcome/Recovery, no production wiring. Tests reuse real child + run-owned temp XDG + absolute KILO_DB: `packages/opencode/test/private-worker/standalone-lease.test.ts` (2 pass: fresh file-backed DB opens with lease held while worker lives, initialize + observation/snapshot/read/ack/subscribe route, direct rehydrate:true on cursor-ahead, InvalidParams on truly invalid cursor, lease released on close/EOF, same DB reacquired sequentially; lease exclusivity is provided by Database.layerFromPath but concurrent-owner behavior was not exercised in this increment; plus injected-deps precedence and default startup unchanged; corrected 2026-08-22 to remove async process 'exit' handler, add idempotent signal/stdin/interval listener removal, add real-child negative gate without KILO_PRIVATE_WORKER_STANDALONE, and replace swallowed ahead-read with direct rehydrate assertion) + `packages/kilo-vscode/tests/unit/standalone-lease.test.ts` (2 pass parity via `PrivateWorkerHost` + `JsonRpcPeer` over real child) = 4 new real-child tests. Existing `observation.test.ts` (21 pass each), `observation-changefeed.test.ts` (8 pass opencode / 6 pass vscode), `private-worker.test.ts` (61 pass vscode) still pass; `bun run typecheck` passes both packages. Parity preserved via public `core` `Database` API (no vscode→opencode private import). Additive only; does not claim five-boundary lifecycle convergence; R9 Resolved 2026-08-23 pending transport reconnect/worker restart/panel close/reopen/reload/session-switch snapshot/revision convergence; R11-R14 remain Open; does not claim P4.2 complete | Manifestor execution of P4.2b standalone lease bootstrap task |
| 2026-08-22 | P4.2b packaging isolation corrigendum — default CJS worker remains lightweight without static `Effect`/`Database`/`createChangefeedDeps`/`bun:sqlite` graph (verified `dist/private-worker/worker.js` 18 KiB, zero `Database`/`effect` strings, no `bun:sqlite`); standalone ESM artifact `dist/private-worker/standalone-worker.mjs` (927 KiB) with `conditions: ["node","import"]`, `platform: node`, `format: esm` bundles leased `Database.layerFromPath` + `createChangefeedDeps` graph via `node:sqlite` (2 imports) and no `bun:sqlite`/TLA; helper `packages/core/src/cutover/archive-path.ts` extracts pure `deriveArchive`/`makeArchiveID` (no `bun:sqlite` import) and `database.ts` now imports from it, `archive.ts` re-exports and retains `checkpointAndVerify` Bun behavior; host `packages/kilo-vscode/src/private-worker/host.ts` selects standalone ESM only when `KILO_PRIVATE_WORKER_STANDALONE=1` + absolute `KILO_DB` (effective env merged), otherwise CJS, preserving command/args overrides and `onNotification` forwarding; tests updated to exercise `standalone-worker.ts` explicitly (`spawnStandalone`/`standaloneTs` overrides) plus lightweight import assertions and `isStandaloneEnabled` checks; `bun run typecheck`/`lint`/`compile` pass (compile emits both artifacts, compile exit 0, `bun:sqlite` absent from CJS, `node:sqlite` present in ESM); still additive, no production wiring, no store/semantics change, R9 Resolved 2026-08-23 pending five-boundary convergence, R11-R14 remain Open | Manifestor execution of P4.2b packaging corrigendum |
| 2026-08-23 | P4.2b concurrent lease contention and crash-recovery — real-child standalone private-worker lease evidence (P4.2b bounded increment). Adds focused tests proving (a) two real child workers cannot own same DB lease concurrently and loser fails observably without corrupting winner, and (b) exact-PID abrupt SIGKILL leaves recoverable stale lease so subsequent real child reacquires same DB and observes valid empty/current cursor (rehydrate:true on cursor-ahead). Tests reuse run-owned temp directories and exact child-handle/PID cleanup on all success/failure paths (no global pkill, no shared-path deletion, bounded timeouts): `packages/opencode/test/private-worker/standalone-lease.test.ts` (6 pass: 2 existing sequential/negative-gate + 2 new contention/crash-recovery + 2 existing injected-deps/lightweight — new tests: concurrent second real child fails to acquire while first holds (peer closed/initialize rejected, winner lease/PID and ping/snapshot healthy), abrupt SIGKILL of exact-PID worker leaves stale lease (isPidAlive false) and next real child reacquires with snapshot cursor 0 and valid read/rehydrate) + `packages/kilo-vscode/tests/unit/standalone-lease.test.ts` (6 pass parity via PrivateWorkerHost: concurrent second PrivateWorkerHost fails observably (closed, timed out) while winner remains healthy with observation intact; SIGKILL of exact host proc leaves stale lease, next host reacquires and observes valid cursor). Sequential reacquire behavior, standalone gate, CJS lightweight worker, ESM standalone packaging, observation protocol shapes, and HTTP/SSE production bridge preserved; no PrivateWorkerHost→KiloConnectionService wiring, no five-boundary production observation convergence claimed (focused real-child lease coexistence/crash-recovery capability proven only), no Failure/Outcome/Recovery, no canonical docs/ADRs, no migration/import. Does not mark R9, R11-R14, or P4.2 complete — R9 Resolved 2026-08-23 pending lifecycle convergence, R11-R14 remain Open, remaining unknowns narrowed to five-boundary lifecycle convergence and recovery accounting. Verification: `bun test ./test/private-worker/standalone-lease.test.ts` from `packages/opencode` (6 pass) and `bun test ./tests/unit/standalone-lease.test.ts` from `packages/kilo-vscode` (6 pass) on 2026-08-23; bounded waits, no leaked child processes or owned temp resources. | Manifestor execution of P4.2b concurrent lease contention and crash-recovery increment |
| 2026-08-23 | P4.2b live canonical changefeed notification delivery and reconnect cursor/rehydrate proof (P4.2b bounded increment) — proves real file-backed canonical DB mutation produces payload-free `observation/changed` notification over private stdio via narrowly scoped test-only bridge `test/mutateChangefeed` reusing real Changefeed APIs (`Changefeed.append` / `appendWithCaps`) on leased Database.layerFromPath, and sequential reacquire/reconnect preserves snapshot/read/ack/gap→rehydrate semantics. Tests prove with run-owned temp XDG dirs, absolute KILO_DB, exact child handles/PIDs, bounded waits/timeouts, and finally/teardown cleanup (no global pkill, no shared-path deletion, no user DB/process leaks): real child initialize, initial snapshot/read empty (cursor 0), one live `observation/changed` with monotonic cursor and payload-free `seq/session_id/revision/kind/time` over private stdio, read at prior cursor returns deltas with matching entry and cursor, read at current empty, ack truncates, stale cursor 0 → rehydrate true after truncation, duplicate read idempotent, ack ahead → InvalidParams (-32602), second live mutation monotonic with second notification, sequential host reacquire on same file (first host disposed, lease gone, no duplicate from prior host, second host snapshot cursor equals prior latest and preserves monotonicity), stale 0 still rehydrates on second host, read before latest returns deltas, new mutation on second host yields exactly one new notification with monotonic cursor and payload-free shape, prior host notifications unchanged, eviction via `caps: { maxRows: 1 }` → `appendWithCaps` forces bounded oldest-first eviction and stale 0 still rehydrates. Bridge gated by `KILO_PRIVATE_WORKER_TEST_BRIDGE=1` + `KILO_PRIVATE_WORKER_STANDALONE=1` + absolute `KILO_DB`, additive, test-only, not production wiring (LOCK-009 HTTP/SSE/SDK bridge remains), no polling/timer/unbounded queue, no Failure/Outcome/Recovery (R11-R14 remain Open), no new DB tables/migrations, no selector/readiness or extensionDataReady change, CJS lightweight worker preserved (verified no Database strings), ESM standalone artifact remains `dist/private-worker/standalone-worker.mjs` with `node:sqlite`. Verification: `bun test test/private-worker/live-changefeed-notification.test.ts` from `packages/opencode` (2 pass, 85 expects) and `bun test tests/unit/live-changefeed-notification.test.ts` from `packages/kilo-vscode` (3 pass, 73 expects) on 2026-08-23; `bun test test/private-worker/standalone-lease.test.ts` (6 pass) + `tests/unit/standalone-lease.test.ts` (6 pass) still green, `bun run typecheck` pass both packages, `bun run script/check-architecture-impact.ts --worktree` 0 signals; no commit or push. | Manifestor execution of P4.2b live canonical changefeed notification delivery unit |
| 2026-08-23 | P4.2b R12 Failure/Outcome normalization foundation Complete and R12 Resolved. Additive pure private-runtime failure module (packages/opencode/src/private-worker/failure.ts ↔ packages/kilo-vscode/src/private-worker/failure.ts, semantic parity, zero imports): sole runtime-owned normalization boundary (normalize), classification separate from recovery, minimal 9-field schema without taxonomy freeze with stable unknown fallback codes, field tiers as exposure ceilings driven by FIELD_TIERS (persist/diagnose/project), runtime-side redaction inside normalize before persistence/projection (secret-key scrub, string value scrub, cycle guard, depth cap, 500/1000/2000 caps), five-source cancellation provenance (user_stop/steering/timeout/network_disconnect/unknown → abandoned), versioned 1.0 panel envelope with runtime-side projection. No production wiring, no new store/DB table, no HTTP/SSE/SDK/session/retry change; LOCK-009 bridge remains. Tests: packages/opencode/test/private-worker/failure.test.ts + packages/kilo-vscode/tests/unit/failure.test.ts (39+39 tests, 146+146 assertions incl. stack-secret redaction). Verification: typecheck pass both packages, focused test files pass both sides, knip clean (no new warnings), architecture impact 0 signals. Audit 2026-08-23 ACCEPT, 0 blockers. R11/R13/R14 and full R9 (five-boundary production observation convergence) remain Open. | Manifestor execution of R12 Failure/Outcome foundation task |
| 2026-08-23 | P4.2b-2 R11 Operation/outcome identity and canonical persistence foundation Complete and R11 Resolved — bounded additive foundation, no production wiring, no R13/R14/full R9. New canonical aggregate `session_operation` (`session_id REFERENCES session(id) ON DELETE CASCADE`, `CHECK` for `op_kind`/`outcome`, indexes on session/kind/time, inspectable `op_id`/`session_id`/`op_kind`/`outcome`/`time`/`revision` plus `code`/`message`/`cancel`/`detail`/`stack`) via `packages/core/src/database/migration/20260824000000_add_operation_record.ts` + `packages/core/src/database/migration.gen.ts` + `packages/core/src/session/sql.ts` `SessionOperationTable` and `packages/core/src/session/operation.ts` (R12-compatible nine-field `FailureRecord`, `FIELD_TIERS` consumed unchanged, `select(record,"persist")` inclusive tier-ceiling, stable identity constructors `prompt:msgId` / `provider:assistantId:attempt` / `tool:assistantId:callId` / `permission:requestId` / `task:childSessionId(:parentCallId)?` with `parseOpId` validation, narrowest state transition only `in-flight`→terminal idempotent or forward, rejects cross-kind/cross-identity/terminal regression without revision/feed, one `BEGIN IMMEDIATE` transaction atomically advances `SessionRevision` with one payload-free `changed` feed row). No separate retention: 8/6 GiB family cascade owns rows. Tests `packages/core/test/operation-r11.test.ts` (13 pass covering migration/FK/cascade/checks/indexes/rerunnable, five-kind identity stability/colon/attempt/cross-kind validation, redacted full-record round trip with inspectable columns, atomic row+revision+changed feed, idempotent replay no revision/feed, cross-kind/cross-identity/terminal regression rejected without revision/feed, narrowest `in-flight`→terminal forward, deterministic `list` ordering and payload-free feed shape, family cascade deletes operations but retains `deleted` tombstone, transaction rollback unchanged, retention eligibility with operations) and existing `changefeed-s4` (11 pass), `retention-s2` (8 pass), `session-revision` (16 pass), `database-migration` (13 pass), `failure` dual-side (40+40 pass) still green; `bun run typecheck` pass (core/opencode/kilo-vscode 17/17), `bun run script/check-architecture-impact.ts --worktree` 0→1 high signal documented (new canonical aggregate/state mutation boundary, docs updated), guards pass; docs `packages/kilo-docs/pages/contributing/architecture/cli-runtime.md` updated (Persistence + Operation/Outcome record R11 + source map). R13 recovery accounting, R14 crash disposition, and full R9 five-boundary production observation convergence remain Open; P4.2 remains Active. | Manifestor execution of R11 Operation/outcome identity and canonical persistence foundation |

| 2026-08-23 | P4.2b private observation service — additive extension-owned `PrivateObservationService` (`packages/kilo-vscode/src/private-worker/private-observation-service.ts` owning one `PrivateWorkerHost`; delegates `observation/snapshot | read | ack | subscribe` and forwards `observation/changed` to injectable `onNotification`; gates on `enabled===true` and absolute `dbPath` via `isAbsolute`, originally resolves canonical `KILO_DB` via leased `Database.layerFromPath` — historical leased semantics, now corrected to `Database.layerNoLease` (canonical no-lease observer / legacy lease-owner invariant); originally wired in `packages/kilo-vscode/src/extension.ts` activation with `enabled:false` gate-off — superseded 2026-08-23 by P4.2b R9 production-enablement wiring (current `enabled:true` no-lease via `resolveCanonicalDbPath`)); no selector/readiness change, no second store/schema migration/polling/Failure/Outcome/Recovery wiring, no legacy-reader cutover/deletion, no five-boundary production observation convergence, no HTTP/SSE removal, no P4.3/P4.4/P4.5 claim; LOCK-009 bridge remains, R9 Resolved 2026-08-23, P4.2 remains Active. Verification: `packages/kilo-vscode/tests/unit/private-observation-service.test.ts` 5 pass / 98 assertions, private-worker/standalone/live suites 38 pass / 228 assertions; `bun turbo typecheck` pass, `bun run script/check-architecture-impact.ts --worktree` 0 signals, `bun run script/extract-source-links.ts` clean, lint/compile blocked only by pre-existing HEAD `packages/kilo-vscode/src/private-worker/disposition.ts` complexity 24>20 unchanged by this increment. Tracker: Session Bootstrap, Phase Overview, P4 Status/checklist/evidence/next-actions, P5 next-actions, R9 Open-row, change log | Manifestor execution of P4.2b private observation service tracker update |
| 2026-08-23 | P4.2b R9 production wiring + pending ownership + singleflight — reconnect private observation activation and close pending-worker ownership races without weakening legacy bridge. `packages/kilo-vscode/src/extension.ts` now wires normal production path via `resolveCanonicalDbPath()` with `enabled:true`, Memento cursor store, `context.subscriptions` ownership, and fire-and-forget `initialize().catch` (fail-closed fallback to `enabled:false` on path resolution/construction failure); legacy bridge and selector startup unchanged. `packages/kilo-vscode/src/private-worker/private-observation-service.ts` and `host.ts` now track pending shutdown ownership via actual child exit (`pendingShutdownProc`/`lastProc`/`hasExited`/`getProc`/`waitForExit` with `exitCode`/`signalCode`), not host state set before exit; pending not cleared until exact child has exited, timeout retains explicit owner and blocks both `reconnect()` and `initialize()` from spawning; `dispose()` retains/cleans owner; bounded exact-PID, no global kills, no polling, no new subsystem. `reconnect()` establishes singleflight before awaiting `initPromise`, preventing concurrent callers from entering `doReconnect` twice; tests prove concurrent reconnect during init shares single promise and initialize while pending alive fails closed without spawning. Stale comments updated, synthetic PID fallbacks removed, `server-manager-utils.test.ts` aligned; tracker wording aligned, R9 remains Open pending five-boundary Extension Host evidence. | Manifestor execution of R9 production wiring + pending ownership + singleflight task |
| 2026-08-23 | P4.2b R9 observation five-boundary convergence — secret-safe durable evidence `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-23T13-59-07Z-r9-observation/` persisted from audited source `/var/folders/9v/733tfwtj2jvgwygmtrbwj9gm0000gn/T/kilo/r9-evidence-1755961000-2082` (schema kilo-e2e-evidence/1, scenario r9-observation, success:true, status:complete, validated:true, audit PASS 0 blocker/acceptable-risk, 25 files, source manifest d2b3b8ecc9572ea4010585a64bacda1e06fdb21a6c7cd8ec7f647de4541e3f26, 8 required, run.log omitted per minimal evidence policy, secret scan no fixture-key). Evidence: r9-observation-runtime-evidence 5 boundaries — panel 1→1 continuity true rehydrate false, reload 1→1, switch 1→1 with switchConfirmation e2e-probe-d3cbd796-B, reconnect 1→2 with observation/changed seq2, restart 2→5 rehydrate:true seq3-5 evict caps; r9-dom-evidence persistedCursor 3 final pid 83485 selected true, canonical-gate zeroState 0 archive before/after identical 9f86…, cstate materializationReady true, plan consistent, protocol v1.0 payload-free gap→rehydrate. Tracker: Phase Overview/P4 table/P4.2 checklist lifecycle-boundary [x] MET, Decision R9 Resolved (2026-08-23), Next actions; runtime spec R9 to be marked Resolved in same work unit; P4.2b lifecycle gate Complete while P4/P4.2 remain Active (P4.3/P4.4/P4.5 pending); no commit/push per instruction. | Manifestor execution of R9 evidence persistence and tracker/spec update task |

| 2026-08-24 | P4.3 readiness documentation reconciliation — preparation evidence only: recorded test: `packages/opencode/test/kilocode/p4-3-readiness-inventory.test.ts` (13 pass, 229 assertions) and pending template doc: `specs/vscode-orchestrator/evidence/p4.3-pre-cutover-reconciliation-checklist.md` (all manual fields PENDING, no cutover) in P4 tracking; corrected stale risk-log R10 wording to Resolved 2026-08-18 (section 9) using existing R10 evidence; no production reader/code change, no phase completion, P4 remains Active | Manifestor execution of P4.3 readiness documentation-reconciliation task |
| 2026-08-24 | P4.2 R11-R14 contract evidence-only artifact — `specs/vscode-orchestrator/evidence/p4.2-r11-r14-contract-evidence.md` (HEAD d112c37780c8d1d844ecd94e40d3fe6e83ec3e02, evidence-only, no production wiring). Records §7.2 matrix (normalization, 9-field schema, five-form identity, tiers, redaction 500/1000/2000, envelope 1.0, cancellation five, sole recovery authority, accounting/coordination, nested visibility, worker-crash disposition with cleanup, version ownership 1.0, canonical storage session_operation FK cascade/CHECKs/indexes/revision/payload-free changed), exact source/test paths, dynamic counts test: `packages/core/test/operation-r11.test.ts` 14/126 + `packages/opencode/test/private-worker/failure.test.ts` 40/149 + `packages/opencode/test/private-worker/recovery.test.ts` 32/202 + `packages/opencode/test/private-worker/disposition.test.ts` 25/141, aggregate 111/618, 0 failures, explicit no-new-store/no-queue/timer and unknowns for live integration; tracker P4 Evidence/Next actions updated with test: and doc: refs, stale R11 count corrected 13→14 where directly established (historical rows otherwise preserved), gate 976 remains unchecked, P4.3 checklist unchanged, P4 remains Active; P4.2 remains Active; P4.3/P4.4/P4.5 remain pending. | Manifestor execution of p4.2-r11-r14 contract evidence task |
| 2026-08-24 | Bounded permission-evaluator semantics (R18) — specification-only: adds R18 to `specs/vscode-orchestrator/runtime-and-configuration-direction.md` §9 (after R17, preamble updated) and mirrored R18 row to `specs/vscode-orchestrator/migration-tracker.md` §9 (Owner Hub, Required by P4, implementation/test gate remains open); creates `specs/vscode-orchestrator/evidence/p4-permission-evaluator-semantics.md` (Status SPECIFICATION-ONLY, cites R18 and §5.3/gate, decision matrix, future acceptance-test matrix including negative cases, explicitly states P4.3 checklist untouched and still pending); resolves eight areas precisely — (a)(b)(c) ceilings with protected paths (`~/.config/kilo/kilo.jsonc`, `<workspaceRoot>/.kilo/kilo.jsonc`, all typed/config assets under canonical `<workspaceRoot>/.kilo/` (legacy `.kilocode/` is a pre-cutover safety boundary only until the P4.3 reader cutover — not a canonical target and does not survive cutover) except `plans/`, `kilo.json[c]`, `opencode.json[c]`, `AGENTS.md`) and `*.env`/`*.env.*` vs `*.env.example`, exact-approval exception, complete v1 catalog; runtime-owned evaluator in `packages/opencode`, extension authoring only; canonical IDs `question`/`question_tool` separate, `mcp` not alias; per-session `once` (`permission:per_abc123` via `operationId`) / `session` ephemeral approvals with exact pattern set, no wildcard for ceilings, no parent-allow inheritance, crash drops; v1 provenance shape (schema version, `permissionRequestId`/`operationId`=`permission:<permissionRequestId>` + `request.permission`, all applicable layers, source kind/path/asset ID, rule identity/order, decisive result/reason, approval metadata with `operationId` for `once`, runtime safety/default sources for applicable no-rule, diagnostic not second store); child propagation inherits denies/restrictions only; single-document wildcard ordering exact→specificity→declaration order, never cross-layer with applicable no-rule `ask`; composition with coherent missing-layer rule (applicable vs non-applicable, applicable no-rule => `ask`; standalone allow with other layers non-applicable => `allow` if all applicable allow; ceiling `ask` only exact approval, ordinary `ask` exact or allow-everything, but allow-everything never resolves ceiling). No runtime code, no import/dual-read, P4/P4.2/P4.3/P4.4/P4.5 gates remain unchecked, P4.3 byte-identical. | Manifestor execution of p4-permission-evaluator semantics amendment |

## 12. Links

- ADR-0001: [`../adr/0001-lossless-session-storage-rewriting.md`](../adr/0001-lossless-session-storage-rewriting.md) (Superseded by ADR-0005; historical decision body preserved)
- ADR-0002: [`../adr/0002-focus-vscode-on-agent-orchestration.md`](../adr/0002-focus-vscode-on-agent-orchestration.md)
- ADR-0003: [`../adr/0003-replace-cli-configuration-with-private-gui-runtime.md`](../adr/0003-replace-cli-configuration-with-private-gui-runtime.md)
- ADR-0004: [`../adr/0004-architecture-first-direct-reconstruction.md`](../adr/0004-architecture-first-direct-reconstruction.md)
- ADR-0005: [`../adr/0005-bounded-private-runtime-storage.md`](../adr/0005-bounded-private-runtime-storage.md)
- Storage spec: [`../storage/session-storage-rewriting.md`](../storage/session-storage-rewriting.md)
- Direction spec: [`agent-orchestration-direction.md`](agent-orchestration-direction.md)
- Runtime/config spec: [`runtime-and-configuration-direction.md`](runtime-and-configuration-direction.md)
- P0 current-state inventory: [`p0-current-state-inventory.md`](p0-current-state-inventory.md)
- P0 baseline evidence: [`evidence/p0-baseline/`](evidence/p0-baseline/) (accepted: cold-start `2026-08-11T11-51-16-883Z/benchmark.jsonl`, many-agent-MCP merged `many-agent-mcp-merged-2026-08-12T00-27-49-435Z/benchmark.jsonl` + `merge-manifest.json`, historical backend `2026-08-11T04-46-19-926Z/backend.jsonl`, Extension Host warm-view/no-provider/custom-provider `2026-08-12T05-52-07-672Z/benchmark.jsonl`, current-tier backend `2026-08-12T06-08-27-792Z/backend.jsonl`, Extension Host session-switch `2026-08-12T07-21-22-201Z/benchmark.jsonl`; excluded partials, retained but never cited as baseline: `2026-08-12T04-30-39-069Z/benchmark.jsonl`, `2026-08-12T06-05-12-356Z/backend.jsonl`); P2 target-surface campaign `2026-08-15T08-35-00Z-p2/` (four validated sub-campaign manifests: `real-completed/manifest.json`, `real-session/manifest.json`, `real-overflow/manifest.json`, `real-restart/manifest.json`; closing evidence for H-1..H-13 at P2), P2 descriptive backend campaign `2026-08-15T06-18-28-167Z/backend.jsonl` (scenarios 6/7/9, 18/18 ok); retained but excluded as closing evidence: `2026-08-15T06-25-37Z/` (contaminated — external gateway title generation, CONTAMINATION.md), `2026-08-15T07-35-17-745Z/` (copy-race — final-window artifacts reconstructed, README.md); P3.1 descriptive scenario-1 cold-start campaign `p3-1-sidebar-2026-08-15T11-19-02-000Z/benchmark.jsonl` (git 67436bdf70 dirty, VS Code 1.133.0, 1 warmup + 5 measured, status ok); P3.2 final runtime campaign `p3-2-final-2026-08-16T03-09-32-000Z/` (complete validated manifest, 29/29 hashes, 36/36 PASS, two root-local sessions, H-12 byte exact, 4/4 run-owned model requests, clean resources; closing evidence for P3.2) and P3.2 final descriptive scenario-1 benchmark `p3-2-final-2026-08-16T03-18-36-000Z/benchmark.jsonl` (git 9d86428cc2 dirty, VS Code 1.133.0, extension 7.4.11, final CLI sha256 `924cb638...`, 1 warmup + 5 measured, 13 n=5 summaries, status ok); P3.3 final runtime campaign `p3-3-final-2026-08-16T11-10-35-000Z/` (complete validated manifest schema `kilo-e2e-evidence/1`, 7 files hash-verified, run `success: true`, all forbidden manifest/runtime/bundle hits empty, retained `agentManagerOpen`/`openInTab`/`agentManager.newTab`, Open-in-Tab panel + Agent Manager ready, no LLM/external requests, exact PID/CDP/scratch cleanup; closing evidence for P3.3) and P3.3 final descriptive scenario-1 benchmark `p3-3-final-2026-08-16T11-10-36-000Z/benchmark.jsonl` (git HEAD 2d61c228e71d6b67db15c2a7ff2be9e728e775e4 dirty, VS Code 1.133.0, extension 7.4.11, CLI snapshot sha256 `229cad7ae3…e5a3b`/size 156821474, 1 warmup + 5 measured, 6/6 ok, 13 n=5 summaries, status ok); earlier P3.2 evidence dirs retained as historical, never cited as closing evidence: `p3-2-worktree-removal-2026-08-15T18-00-47-000Z/`, `p3-2-worktree-removal-2026-08-15T18-11-33-000Z/`
- P3.1 removal evidence: `packages/kilo-vscode/tests/unit/sidebar-removal.test.ts` (static absence contract), `packages/kilo-vscode/tests/unit/chat-target.test.ts` (chat-target routing), focused real Extension Host `test:e2e:sidebar-removal` via `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts`; `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Chat surfaces (P3.1 sidebar removal); Rollback path); `.changeset/remove-sidebar-chat.md` (minor release note)
- P3.2 removal evidence: `packages/kilo-vscode/tests/unit/worktree-removal.test.ts` (managed-worktree/Diff/SDK-group/i18n static absence + retained root-local orchestration), `packages/kilo-vscode/tests/unit/diff-viewer-removal.test.ts` (custom Diff Viewer/Diff Virtual absence + preserved H-12 rollback), `packages/opencode/test/kilocode/agent-manager-absence.test.ts` (orphaned list/prompt request-reply contract absence — `kilocode.agent_manager.start` only), focused real Extension Host `test:e2e:worktree-removal` via `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe-worktree.ts` + `packages/kilo-vscode/script/e2e-probe.ts` (final campaign `p3-2-final-2026-08-16T03-09-32-000Z/`); `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.2 removal; Rollback path), `packages/kilo-docs/pages/automate/agent-manager.md`, `packages/kilo-docs/pages/automate/agent-manager-workflows.md`, `packages/opencode/src/kilocode/skills/kilo-config.md` (root-local model); `.changeset/agent-manager-root-local-sessions.md` (minor release note)
- P3.3 removal evidence: `packages/kilo-vscode/tests/unit/cloud-claw-removal.test.ts` (cloud-session/KiloClaw static absence + E2E wiring), `packages/opencode/test/kilocode/cloud-claw-removal.test.ts` (CLI-side cloud-session/KiloClaw absence), `packages/opencode/test/kilocode/console-removed.test.ts` (Console removal + shared migration-bridge survival), `packages/script/tests/jetbrains-removed.test.ts` (JetBrains package/workflows/turbo/release/CI absence), focused real Extension Host `test:e2e:cloud-claw-removal` via `packages/kilo-vscode/tests/e2e/runner.ts` + `packages/kilo-vscode/script/e2e-probe.ts` + `packages/kilo-vscode/script/e2e-probe-launch.mjs` + `packages/kilo-vscode/script/e2e-evidence.ts` (final campaign `p3-3-final-2026-08-16T11-10-35-000Z/`); `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (Product runtime absence (P3.3 removal); Rollback path), `packages/kilo-docs/source-links.md`; `.changeset/remove-jetbrains-console-cloud-kiloclaw.md` (minor release note)
- P3.4 removal evidence: `packages/opencode/test/kilocode/p3-4-removal-absence.test.ts` (20 pass / 253 assertions — indexing/memory/context-management/autocomplete/SDK static absence), `packages/kilo-vscode/tests/unit/p3-4-removal.test.ts` (28 pass / 381 assertions — extension/webview surface absence + retained H-13 evidence), real Extension Host `test:e2e:p3-4-removal` (forbidden manifest/runtime/bundle/state hits empty, 0 model requests, Open-in-Tab and Agent Manager ready, cleanup complete), `packages/opencode/test/kilocode/p0-harness-baseline.test.ts` (H-13 P0 harness 8/8 + automatic overflow suites); `packages/kilo-docs/pages/contributing/architecture/vscode-extension.md` (P3.4 section + Rollback path); `.changeset/p34-docs-update.md` (minor release note); accepted native scenario-1 campaign `specs/vscode-orchestrator/evidence/p0-baseline/2026-08-17T01-45-01-088Z/benchmark.jsonl`; accepted removal E2E evidence `specs/vscode-orchestrator/evidence/p0-baseline/p3-4-final-2026-08-17T00-52-00-000Z/` (validated manifest sha256 d3f6d86…e002, runtime evidence sha256 746d7c8…3bd0); excluded partial wrapper campaign `p3-4-final-2026-08-17T00-57-00-000Z/EXCLUDED`
- Canonical architecture docs: `packages/kilo-docs/pages/contributing/architecture/` (LOCK-013; `vscode-extension.md` updated in the P2 work unit to document the implemented env-gated fixture bridge, in the P3.1 work unit to document the implemented sidebar removal, in the P3.2 work unit to document the managed-worktree/Diff removal and root-local Agent Manager model, in the P3.3 work unit to document the Product runtime absence (P3.3 removal) and its Rollback path, and in the P3.4 work unit to document the P3.4 removal and its Rollback path)
